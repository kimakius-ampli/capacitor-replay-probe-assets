(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __esm = (fn, res) => function __init() {
    return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
  };
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

  // node_modules/tslib/tslib.es6.mjs
  function __extends(d2, b2) {
    if (typeof b2 !== "function" && b2 !== null)
      throw new TypeError("Class extends value " + String(b2) + " is not a constructor or null");
    extendStatics(d2, b2);
    function __() {
      this.constructor = d2;
    }
    d2.prototype = b2 === null ? Object.create(b2) : (__.prototype = b2.prototype, new __());
  }
  function __rest(s2, e2) {
    var t2 = {};
    for (var p2 in s2) if (Object.prototype.hasOwnProperty.call(s2, p2) && e2.indexOf(p2) < 0)
      t2[p2] = s2[p2];
    if (s2 != null && typeof Object.getOwnPropertySymbols === "function")
      for (var i2 = 0, p2 = Object.getOwnPropertySymbols(s2); i2 < p2.length; i2++) {
        if (e2.indexOf(p2[i2]) < 0 && Object.prototype.propertyIsEnumerable.call(s2, p2[i2]))
          t2[p2[i2]] = s2[p2[i2]];
      }
    return t2;
  }
  function __awaiter(thisArg, _arguments, P2, generator) {
    function adopt(value) {
      return value instanceof P2 ? value : new P2(function(resolve) {
        resolve(value);
      });
    }
    return new (P2 || (P2 = Promise))(function(resolve, reject) {
      function fulfilled(value) {
        try {
          step(generator.next(value));
        } catch (e2) {
          reject(e2);
        }
      }
      function rejected(value) {
        try {
          step(generator["throw"](value));
        } catch (e2) {
          reject(e2);
        }
      }
      function step(result3) {
        result3.done ? resolve(result3.value) : adopt(result3.value).then(fulfilled, rejected);
      }
      step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
  }
  function __generator(thisArg, body) {
    var _2 = { label: 0, sent: function() {
      if (t2[0] & 1) throw t2[1];
      return t2[1];
    }, trys: [], ops: [] }, f2, y2, t2, g2 = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g2.next = verb(0), g2["throw"] = verb(1), g2["return"] = verb(2), typeof Symbol === "function" && (g2[Symbol.iterator] = function() {
      return this;
    }), g2;
    function verb(n4) {
      return function(v2) {
        return step([n4, v2]);
      };
    }
    function step(op) {
      if (f2) throw new TypeError("Generator is already executing.");
      while (g2 && (g2 = 0, op[0] && (_2 = 0)), _2) try {
        if (f2 = 1, y2 && (t2 = op[0] & 2 ? y2["return"] : op[0] ? y2["throw"] || ((t2 = y2["return"]) && t2.call(y2), 0) : y2.next) && !(t2 = t2.call(y2, op[1])).done) return t2;
        if (y2 = 0, t2) op = [op[0] & 2, t2.value];
        switch (op[0]) {
          case 0:
          case 1:
            t2 = op;
            break;
          case 4:
            _2.label++;
            return { value: op[1], done: false };
          case 5:
            _2.label++;
            y2 = op[1];
            op = [0];
            continue;
          case 7:
            op = _2.ops.pop();
            _2.trys.pop();
            continue;
          default:
            if (!(t2 = _2.trys, t2 = t2.length > 0 && t2[t2.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _2 = 0;
              continue;
            }
            if (op[0] === 3 && (!t2 || op[1] > t2[0] && op[1] < t2[3])) {
              _2.label = op[1];
              break;
            }
            if (op[0] === 6 && _2.label < t2[1]) {
              _2.label = t2[1];
              t2 = op;
              break;
            }
            if (t2 && _2.label < t2[2]) {
              _2.label = t2[2];
              _2.ops.push(op);
              break;
            }
            if (t2[2]) _2.ops.pop();
            _2.trys.pop();
            continue;
        }
        op = body.call(thisArg, _2);
      } catch (e2) {
        op = [6, e2];
        y2 = 0;
      } finally {
        f2 = t2 = 0;
      }
      if (op[0] & 5) throw op[1];
      return { value: op[0] ? op[1] : void 0, done: true };
    }
  }
  function __values(o2) {
    var s2 = typeof Symbol === "function" && Symbol.iterator, m2 = s2 && o2[s2], i2 = 0;
    if (m2) return m2.call(o2);
    if (o2 && typeof o2.length === "number") return {
      next: function() {
        if (o2 && i2 >= o2.length) o2 = void 0;
        return { value: o2 && o2[i2++], done: !o2 };
      }
    };
    throw new TypeError(s2 ? "Object is not iterable." : "Symbol.iterator is not defined.");
  }
  function __read(o2, n4) {
    var m2 = typeof Symbol === "function" && o2[Symbol.iterator];
    if (!m2) return o2;
    var i2 = m2.call(o2), r2, ar = [], e2;
    try {
      while ((n4 === void 0 || n4-- > 0) && !(r2 = i2.next()).done) ar.push(r2.value);
    } catch (error) {
      e2 = { error };
    } finally {
      try {
        if (r2 && !r2.done && (m2 = i2["return"])) m2.call(i2);
      } finally {
        if (e2) throw e2.error;
      }
    }
    return ar;
  }
  function __spreadArray(to, from, pack) {
    if (pack || arguments.length === 2) for (var i2 = 0, l2 = from.length, ar; i2 < l2; i2++) {
      if (ar || !(i2 in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i2);
        ar[i2] = from[i2];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  }
  var extendStatics, __assign;
  var init_tslib_es6 = __esm({
    "node_modules/tslib/tslib.es6.mjs"() {
      extendStatics = function(d2, b2) {
        extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d3, b3) {
          d3.__proto__ = b3;
        } || function(d3, b3) {
          for (var p2 in b3) if (Object.prototype.hasOwnProperty.call(b3, p2)) d3[p2] = b3[p2];
        };
        return extendStatics(d2, b2);
      };
      __assign = function() {
        __assign = Object.assign || function __assign3(t2) {
          for (var s2, i2 = 1, n4 = arguments.length; i2 < n4; i2++) {
            s2 = arguments[i2];
            for (var p2 in s2) if (Object.prototype.hasOwnProperty.call(s2, p2)) t2[p2] = s2[p2];
          }
          return t2;
        };
        return __assign.apply(this, arguments);
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/event/event.js
  var IdentifyOperation, SpecialEventType;
  var init_event = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/event/event.js"() {
      (function(IdentifyOperation4) {
        IdentifyOperation4["SET"] = "$set";
        IdentifyOperation4["SET_ONCE"] = "$setOnce";
        IdentifyOperation4["ADD"] = "$add";
        IdentifyOperation4["APPEND"] = "$append";
        IdentifyOperation4["PREPEND"] = "$prepend";
        IdentifyOperation4["REMOVE"] = "$remove";
        IdentifyOperation4["PREINSERT"] = "$preInsert";
        IdentifyOperation4["POSTINSERT"] = "$postInsert";
        IdentifyOperation4["UNSET"] = "$unset";
        IdentifyOperation4["CLEAR_ALL"] = "$clearAll";
      })(IdentifyOperation || (IdentifyOperation = {}));
      (function(SpecialEventType3) {
        SpecialEventType3["IDENTIFY"] = "$identify";
        SpecialEventType3["GROUP_IDENTIFY"] = "$groupidentify";
        SpecialEventType3["REVENUE"] = "revenue_amount";
      })(SpecialEventType || (SpecialEventType = {}));
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/constants.js
  var UNSET_VALUE, AMPLITUDE_PREFIX, STORAGE_PREFIX, DEFAULT_INSTANCE_NAME, AMPLITUDE_SERVER_URL, EU_AMPLITUDE_SERVER_URL, AMPLITUDE_BATCH_SERVER_URL, EU_AMPLITUDE_BATCH_SERVER_URL, UTM_CAMPAIGN, UTM_CONTENT, UTM_ID, UTM_MEDIUM, UTM_SOURCE, UTM_TERM, DCLID, FBCLID, GBRAID, GCLID, KO_CLICK_ID, LI_FAT_ID, MSCLKID, RDT_CID, TTCLID, TWCLID, WBRAID, BASE_CAMPAIGN, SAFE_HEADERS, FORBIDDEN_HEADERS;
  var init_constants = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/constants.js"() {
      UNSET_VALUE = "-";
      AMPLITUDE_PREFIX = "AMP";
      STORAGE_PREFIX = "".concat(AMPLITUDE_PREFIX, "_unsent");
      DEFAULT_INSTANCE_NAME = "$default_instance";
      AMPLITUDE_SERVER_URL = "https://api2.amplitude.com/2/httpapi";
      EU_AMPLITUDE_SERVER_URL = "https://api.eu.amplitude.com/2/httpapi";
      AMPLITUDE_BATCH_SERVER_URL = "https://api2.amplitude.com/batch";
      EU_AMPLITUDE_BATCH_SERVER_URL = "https://api.eu.amplitude.com/batch";
      UTM_CAMPAIGN = "utm_campaign";
      UTM_CONTENT = "utm_content";
      UTM_ID = "utm_id";
      UTM_MEDIUM = "utm_medium";
      UTM_SOURCE = "utm_source";
      UTM_TERM = "utm_term";
      DCLID = "dclid";
      FBCLID = "fbclid";
      GBRAID = "gbraid";
      GCLID = "gclid";
      KO_CLICK_ID = "ko_click_id";
      LI_FAT_ID = "li_fat_id";
      MSCLKID = "msclkid";
      RDT_CID = "rdt_cid";
      TTCLID = "ttclid";
      TWCLID = "twclid";
      WBRAID = "wbraid";
      BASE_CAMPAIGN = {
        utm_campaign: void 0,
        utm_content: void 0,
        utm_id: void 0,
        utm_medium: void 0,
        utm_source: void 0,
        utm_term: void 0,
        referrer: void 0,
        referring_domain: void 0,
        dclid: void 0,
        gbraid: void 0,
        gclid: void 0,
        fbclid: void 0,
        ko_click_id: void 0,
        li_fat_id: void 0,
        msclkid: void 0,
        rdt_cid: void 0,
        ttclid: void 0,
        twclid: void 0,
        wbraid: void 0
      };
      SAFE_HEADERS = [
        "access-control-allow-origin",
        "access-control-allow-credentials",
        "access-control-expose-headers",
        "access-control-max-age",
        "access-control-allow-methods",
        "access-control-allow-headers",
        "accept-patch",
        "accept-ranges",
        "age",
        "allow",
        "alt-svc",
        "cache-control",
        "connection",
        "content-disposition",
        "content-encoding",
        "content-language",
        "content-length",
        "content-location",
        "content-md5",
        "content-range",
        "content-type",
        "date",
        "delta-base",
        "etag",
        "expires",
        "im",
        "last-modified",
        "link",
        "location",
        "permanent",
        "p3p",
        "pragma",
        "proxy-authenticate",
        "public-key-pins",
        "retry-after",
        "server",
        "status",
        "strict-transport-security",
        "trailer",
        "transfer-encoding",
        "tk",
        "upgrade",
        "vary",
        "via",
        "warning",
        "www-authenticate",
        "x-b3-traceid",
        "x-frame-options"
      ];
      FORBIDDEN_HEADERS = ["authorization", "cookie", "set-cookie"];
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/valid-properties.js
  var MAX_PROPERTY_KEYS, isValidObject, isValidProperties;
  var init_valid_properties = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/valid-properties.js"() {
      init_tslib_es6();
      MAX_PROPERTY_KEYS = 1e3;
      isValidObject = function(properties) {
        if (Object.keys(properties).length > MAX_PROPERTY_KEYS) {
          return false;
        }
        for (var key in properties) {
          var value = properties[key];
          if (!isValidProperties(key, value))
            return false;
        }
        return true;
      };
      isValidProperties = function(property, value) {
        var e_1, _a4;
        if (typeof property !== "string")
          return false;
        if (Array.isArray(value)) {
          var isValid2 = true;
          try {
            for (var value_1 = __values(value), value_1_1 = value_1.next(); !value_1_1.done; value_1_1 = value_1.next()) {
              var valueElement = value_1_1.value;
              if (Array.isArray(valueElement)) {
                return false;
              } else if (typeof valueElement === "object") {
                isValid2 = isValid2 && isValidObject(valueElement);
              } else if (!["number", "string"].includes(typeof valueElement)) {
                return false;
              }
              if (!isValid2) {
                return false;
              }
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (value_1_1 && !value_1_1.done && (_a4 = value_1.return)) _a4.call(value_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
        } else if (value === null || value === void 0) {
          return false;
        } else if (typeof value === "object") {
          return isValidObject(value);
        } else if (!["number", "string", "boolean"].includes(typeof value)) {
          return false;
        }
        return true;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/identify.js
  var Identify, IdentifyOperation2, OrderedIdentifyOperations;
  var init_identify = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/identify.js"() {
      init_tslib_es6();
      init_constants();
      init_valid_properties();
      Identify = /** @class */
      (function() {
        function Identify2() {
          this._propertySet = /* @__PURE__ */ new Set();
          this._properties = {};
        }
        Identify2.prototype.getUserProperties = function() {
          return __assign({}, this._properties);
        };
        Identify2.prototype.set = function(property, value) {
          this._safeSet(IdentifyOperation2.SET, property, value);
          return this;
        };
        Identify2.prototype.setOnce = function(property, value) {
          this._safeSet(IdentifyOperation2.SET_ONCE, property, value);
          return this;
        };
        Identify2.prototype.append = function(property, value) {
          this._safeSet(IdentifyOperation2.APPEND, property, value);
          return this;
        };
        Identify2.prototype.prepend = function(property, value) {
          this._safeSet(IdentifyOperation2.PREPEND, property, value);
          return this;
        };
        Identify2.prototype.postInsert = function(property, value) {
          this._safeSet(IdentifyOperation2.POSTINSERT, property, value);
          return this;
        };
        Identify2.prototype.preInsert = function(property, value) {
          this._safeSet(IdentifyOperation2.PREINSERT, property, value);
          return this;
        };
        Identify2.prototype.remove = function(property, value) {
          this._safeSet(IdentifyOperation2.REMOVE, property, value);
          return this;
        };
        Identify2.prototype.add = function(property, value) {
          this._safeSet(IdentifyOperation2.ADD, property, value);
          return this;
        };
        Identify2.prototype.unset = function(property) {
          this._safeSet(IdentifyOperation2.UNSET, property, UNSET_VALUE);
          return this;
        };
        Identify2.prototype.clearAll = function() {
          this._properties = {};
          this._properties[IdentifyOperation2.CLEAR_ALL] = UNSET_VALUE;
          return this;
        };
        Identify2.prototype._safeSet = function(operation, property, value) {
          if (this._validate(operation, property, value)) {
            var userPropertyMap = this._properties[operation];
            if (userPropertyMap === void 0) {
              userPropertyMap = {};
              this._properties[operation] = userPropertyMap;
            }
            userPropertyMap[property] = value;
            this._propertySet.add(property);
            return true;
          }
          return false;
        };
        Identify2.prototype._validate = function(operation, property, value) {
          if (this._properties[IdentifyOperation2.CLEAR_ALL] !== void 0) {
            return false;
          }
          if (this._propertySet.has(property)) {
            return false;
          }
          if (operation === IdentifyOperation2.ADD) {
            return typeof value === "number";
          }
          if (operation !== IdentifyOperation2.UNSET && operation !== IdentifyOperation2.REMOVE) {
            return isValidProperties(property, value);
          }
          return true;
        };
        return Identify2;
      })();
      (function(IdentifyOperation4) {
        IdentifyOperation4["SET"] = "$set";
        IdentifyOperation4["SET_ONCE"] = "$setOnce";
        IdentifyOperation4["ADD"] = "$add";
        IdentifyOperation4["APPEND"] = "$append";
        IdentifyOperation4["PREPEND"] = "$prepend";
        IdentifyOperation4["REMOVE"] = "$remove";
        IdentifyOperation4["PREINSERT"] = "$preInsert";
        IdentifyOperation4["POSTINSERT"] = "$postInsert";
        IdentifyOperation4["UNSET"] = "$unset";
        IdentifyOperation4["CLEAR_ALL"] = "$clearAll";
      })(IdentifyOperation2 || (IdentifyOperation2 = {}));
      OrderedIdentifyOperations = [
        IdentifyOperation2.CLEAR_ALL,
        IdentifyOperation2.UNSET,
        IdentifyOperation2.SET,
        IdentifyOperation2.SET_ONCE,
        IdentifyOperation2.ADD,
        IdentifyOperation2.APPEND,
        IdentifyOperation2.PREPEND,
        IdentifyOperation2.PREINSERT,
        IdentifyOperation2.POSTINSERT,
        IdentifyOperation2.REMOVE
      ];
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/messages.js
  var SUCCESS_MESSAGE, UNEXPECTED_ERROR_MESSAGE, MAX_RETRIES_EXCEEDED_MESSAGE, OPT_OUT_MESSAGE, EMPTY_EVENT_TYPE_MESSAGE, MISSING_API_KEY_MESSAGE, INVALID_API_KEY, CLIENT_NOT_INITIALIZED;
  var init_messages = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/messages.js"() {
      SUCCESS_MESSAGE = "Event tracked successfully";
      UNEXPECTED_ERROR_MESSAGE = "Unexpected error occurred";
      MAX_RETRIES_EXCEEDED_MESSAGE = "Event rejected due to exceeded retry count";
      OPT_OUT_MESSAGE = "Event skipped due to optOut config";
      EMPTY_EVENT_TYPE_MESSAGE = "Event skipped due to empty event type";
      MISSING_API_KEY_MESSAGE = "Event rejected due to missing API key";
      INVALID_API_KEY = "Invalid API key";
      CLIENT_NOT_INITIALIZED = "Client not initialized";
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/status.js
  var Status;
  var init_status = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/status.js"() {
      (function(Status2) {
        Status2["Unknown"] = "unknown";
        Status2["Skipped"] = "skipped";
        Status2["Success"] = "success";
        Status2["RateLimit"] = "rate_limit";
        Status2["PayloadTooLarge"] = "payload_too_large";
        Status2["Invalid"] = "invalid";
        Status2["Failed"] = "failed";
        Status2["Timeout"] = "Timeout";
        Status2["SystemError"] = "SystemError";
      })(Status || (Status = {}));
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/result-builder.js
  var buildResult;
  var init_result_builder = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/result-builder.js"() {
      init_status();
      buildResult = function(event, code, message) {
        if (code === void 0) {
          code = 0;
        }
        if (message === void 0) {
          message = Status.Unknown;
        }
        return { event, code, message };
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/global-scope.js
  var getGlobalScope;
  var init_global_scope = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/global-scope.js"() {
      getGlobalScope = function() {
        var ampIntegrationContextName = "ampIntegrationContext";
        if (typeof globalThis !== "undefined" && typeof globalThis[ampIntegrationContextName] !== "undefined") {
          return globalThis[ampIntegrationContextName];
        }
        if (typeof globalThis !== "undefined") {
          return globalThis;
        }
        if (typeof window !== "undefined") {
          return window;
        }
        if (typeof self !== "undefined") {
          return self;
        }
        if (typeof global !== "undefined") {
          return global;
        }
        return void 0;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/uuid.js
  var legacyUUID, hex, UUID;
  var init_uuid = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/uuid.js"() {
      init_tslib_es6();
      init_global_scope();
      legacyUUID = function(a2) {
        return a2 ? (
          // a random number from 0 to 15
          (a2 ^ // unless b is 8,
          Math.random() * // in which case
          16 >> // a random number from
          a2 / 4).toString(16)
        ) : (
          // or otherwise a concatenated string:
          (String(1e7) + // 10000000 +
          String(-1e3) + // -1000 +
          String(-4e3) + // -4000 +
          String(-8e3) + // -80000000 +
          String(-1e11)).replace(
            // replacing
            /[018]/g,
            // zeroes, ones, and eights with
            UUID
          )
        );
      };
      hex = __spreadArray([], __read(Array(256).keys()), false).map(function(index4) {
        return index4.toString(16).padStart(2, "0");
      });
      UUID = function(a2) {
        var _a4;
        var globalScope3 = getGlobalScope();
        if (!((_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.crypto) === null || _a4 === void 0 ? void 0 : _a4.getRandomValues)) {
          return legacyUUID(a2);
        }
        var r2 = globalScope3.crypto.getRandomValues(new Uint8Array(16));
        r2[6] = r2[6] & 15 | 64;
        r2[8] = r2[8] & 63 | 128;
        return __spreadArray([], __read(r2.entries()), false).map(function(_a5) {
          var _b = __read(_a5, 2), index4 = _b[0], int = _b[1];
          return [4, 6, 8, 10].includes(index4) ? "-".concat(hex[int]) : hex[int];
        }).join("");
      };
    }
  });

  // node_modules/safe-json-stringify/index.js
  var require_safe_json_stringify = __commonJS({
    "node_modules/safe-json-stringify/index.js"(exports, module) {
      var hasProp = Object.prototype.hasOwnProperty;
      function throwsMessage(err) {
        return "[Throws: " + (err ? err.message : "?") + "]";
      }
      function safeGetValueFromPropertyOnObject(obj, property) {
        if (hasProp.call(obj, property)) {
          try {
            return obj[property];
          } catch (err) {
            return throwsMessage(err);
          }
        }
        return obj[property];
      }
      function ensureProperties(obj) {
        var seen = [];
        function visit(obj2) {
          if (obj2 === null || typeof obj2 !== "object") {
            return obj2;
          }
          if (seen.indexOf(obj2) !== -1) {
            return "[Circular]";
          }
          seen.push(obj2);
          if (typeof obj2.toJSON === "function") {
            try {
              var fResult = visit(obj2.toJSON());
              seen.pop();
              return fResult;
            } catch (err) {
              return throwsMessage(err);
            }
          }
          if (Array.isArray(obj2)) {
            var aResult = obj2.map(visit);
            seen.pop();
            return aResult;
          }
          var result3 = Object.keys(obj2).reduce(function(result4, prop) {
            result4[prop] = visit(safeGetValueFromPropertyOnObject(obj2, prop));
            return result4;
          }, {});
          seen.pop();
          return result3;
        }
        ;
        return visit(obj);
      }
      module.exports = function(data, replacer, space) {
        return JSON.stringify(ensureProperties(data), replacer, space);
      };
      module.exports.ensureProperties = ensureProperties;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/safe-stringify.js
  var safeJsonStringifyModule, safeJsonStringify;
  var init_safe_stringify = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/safe-stringify.js"() {
      safeJsonStringifyModule = __toESM(require_safe_json_stringify());
      safeJsonStringify = safeJsonStringifyModule.default || safeJsonStringifyModule;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/timeline.js
  var Timeline;
  var init_timeline = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/timeline.js"() {
      init_tslib_es6();
      init_result_builder();
      init_uuid();
      init_safe_stringify();
      Timeline = /** @class */
      (function() {
        function Timeline2(client) {
          this.client = client;
          this.queue = [];
          this.applying = false;
          this.plugins = [];
          this.pluginStatus = /* @__PURE__ */ new Map();
          this._optOutListeners = [];
        }
        Timeline2.prototype.register = function(plugin, config2) {
          var _a4, _b;
          return __awaiter(this, void 0, void 0, function() {
            var name;
            return __generator(this, function(_c) {
              switch (_c.label) {
                case 0:
                  if (plugin.name === void 0) {
                    plugin.name = UUID();
                    this.loggerProvider.warn("Plugin name is undefined.\n      Generating a random UUID for plugin name: ".concat(plugin.name, ".\n      Set a name for the plugin to prevent it from being added multiple times."));
                  }
                  name = plugin.name;
                  if (this.pluginStatus.has(name)) {
                    this.loggerProvider.warn("Plugin with name ".concat(name, " already exists, skipping registration"));
                    return [
                      2
                      /*return*/
                    ];
                  }
                  plugin.type = (_a4 = plugin.type) !== null && _a4 !== void 0 ? _a4 : "enrichment";
                  this.pluginStatus.set(name, "locked");
                  return [4, (_b = plugin.setup) === null || _b === void 0 ? void 0 : _b.call(plugin, config2, this.client)];
                case 1:
                  _c.sent();
                  if (this.pluginStatus.get(name) !== "locked") {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  this.plugins.push(plugin);
                  this.pluginStatus.set(name, "installed");
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Timeline2.prototype.deregister = function(pluginName, config2) {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            var index4, plugin;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  this.pluginStatus.delete(pluginName);
                  index4 = this.plugins.findIndex(function(plugin2) {
                    return plugin2.name === pluginName;
                  });
                  if (index4 === -1) {
                    config2.loggerProvider.warn("Plugin with name ".concat(pluginName, " does not exist, skipping deregistration"));
                    return [
                      2
                      /*return*/
                    ];
                  }
                  plugin = this.plugins[index4];
                  this.plugins.splice(index4, 1);
                  return [4, (_a4 = plugin.teardown) === null || _a4 === void 0 ? void 0 : _a4.call(plugin)];
                case 1:
                  _b.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Timeline2.prototype.reset = function(client) {
          this._clearOptOutListeners();
          this.applying = false;
          var plugins = this.plugins;
          plugins.map(function(plugin) {
            var _a4;
            return (_a4 = plugin.teardown) === null || _a4 === void 0 ? void 0 : _a4.call(plugin);
          });
          this.plugins = [];
          this.pluginStatus.clear();
          this.client = client;
        };
        Timeline2.prototype.push = function(event) {
          var _this = this;
          return new Promise(function(resolve) {
            _this.queue.push([event, resolve]);
            _this.scheduleApply(0);
          });
        };
        Timeline2.prototype.scheduleApply = function(timeout) {
          var _this = this;
          if (this.applying)
            return;
          this.applying = true;
          setTimeout(function() {
            void _this.apply(_this.queue.shift()).then(function() {
              _this.applying = false;
              if (_this.queue.length > 0) {
                _this.scheduleApply(0);
              }
            });
          }, timeout);
        };
        Timeline2.prototype.apply = function(item) {
          return __awaiter(this, void 0, void 0, function() {
            var _a4, event, _b, resolve, before, before_1, before_1_1, plugin, e2, e_1_1, enrichment, enrichment_1, enrichment_1_1, plugin, e2, e_2_1, destination, executeDestinations;
            var e_1, _c, e_2, _d;
            return __generator(this, function(_e) {
              switch (_e.label) {
                case 0:
                  if (!item) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  _a4 = __read(item, 1), event = _a4[0];
                  _b = __read(item, 2), resolve = _b[1];
                  this.loggerProvider.log("Timeline.apply: Initial event", event);
                  before = this.plugins.filter(function(plugin2) {
                    return plugin2.type === "before";
                  });
                  _e.label = 1;
                case 1:
                  _e.trys.push([1, 6, 7, 8]);
                  before_1 = __values(before), before_1_1 = before_1.next();
                  _e.label = 2;
                case 2:
                  if (!!before_1_1.done) return [3, 5];
                  plugin = before_1_1.value;
                  if (!plugin.execute) {
                    return [3, 4];
                  }
                  return [4, plugin.execute(__assign({}, event))];
                case 3:
                  e2 = _e.sent();
                  if (e2 === null) {
                    this.loggerProvider.log("Timeline.apply: Event filtered out by before plugin '".concat(String(plugin.name), "', event: ").concat(safeJsonStringify(event)));
                    resolve({ event, code: 0, message: "" });
                    return [
                      2
                      /*return*/
                    ];
                  } else {
                    event = e2;
                    this.loggerProvider.log("Timeline.apply: Event after before plugin '".concat(String(plugin.name), "', event: ").concat(safeJsonStringify(event)));
                  }
                  _e.label = 4;
                case 4:
                  before_1_1 = before_1.next();
                  return [3, 2];
                case 5:
                  return [3, 8];
                case 6:
                  e_1_1 = _e.sent();
                  e_1 = { error: e_1_1 };
                  return [3, 8];
                case 7:
                  try {
                    if (before_1_1 && !before_1_1.done && (_c = before_1.return)) _c.call(before_1);
                  } finally {
                    if (e_1) throw e_1.error;
                  }
                  return [
                    7
                    /*endfinally*/
                  ];
                case 8:
                  enrichment = this.plugins.filter(function(plugin2) {
                    return plugin2.type === "enrichment" || plugin2.type === void 0;
                  });
                  _e.label = 9;
                case 9:
                  _e.trys.push([9, 14, 15, 16]);
                  enrichment_1 = __values(enrichment), enrichment_1_1 = enrichment_1.next();
                  _e.label = 10;
                case 10:
                  if (!!enrichment_1_1.done) return [3, 13];
                  plugin = enrichment_1_1.value;
                  if (!plugin.execute) {
                    return [3, 12];
                  }
                  return [4, plugin.execute(__assign({}, event))];
                case 11:
                  e2 = _e.sent();
                  if (e2 === null) {
                    this.loggerProvider.log("Timeline.apply: Event filtered out by enrichment plugin '".concat(String(plugin.name), "', event: ").concat(safeJsonStringify(event)));
                    resolve({ event, code: 0, message: "" });
                    return [
                      2
                      /*return*/
                    ];
                  } else {
                    event = e2;
                    this.loggerProvider.log("Timeline.apply: Event after enrichment plugin '".concat(String(plugin.name), "', event: ").concat(safeJsonStringify(event)));
                  }
                  _e.label = 12;
                case 12:
                  enrichment_1_1 = enrichment_1.next();
                  return [3, 10];
                case 13:
                  return [3, 16];
                case 14:
                  e_2_1 = _e.sent();
                  e_2 = { error: e_2_1 };
                  return [3, 16];
                case 15:
                  try {
                    if (enrichment_1_1 && !enrichment_1_1.done && (_d = enrichment_1.return)) _d.call(enrichment_1);
                  } finally {
                    if (e_2) throw e_2.error;
                  }
                  return [
                    7
                    /*endfinally*/
                  ];
                case 16:
                  destination = this.plugins.filter(function(plugin2) {
                    return plugin2.type === "destination";
                  });
                  this.loggerProvider.log("Timeline.apply: Final event before destinations, event: ".concat(safeJsonStringify(event)));
                  executeDestinations = destination.map(function(plugin2) {
                    var eventClone = __assign({}, event);
                    return plugin2.execute(eventClone).catch(function(e3) {
                      return buildResult(eventClone, 0, String(e3));
                    });
                  });
                  void Promise.all(executeDestinations).then(function(_a5) {
                    var _b2 = __read(_a5, 1), result3 = _b2[0];
                    var resolveResult = result3 || buildResult(event, 100, "Event not tracked, no destination plugins on the instance");
                    resolve(resolveResult);
                  });
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Timeline2.prototype.flush = function() {
          return __awaiter(this, void 0, void 0, function() {
            var queue, destination, executeDestinations;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  queue = this.queue;
                  this.queue = [];
                  return [4, Promise.all(queue.map(function(item) {
                    return _this.apply(item);
                  }))];
                case 1:
                  _a4.sent();
                  destination = this.plugins.filter(function(plugin) {
                    return plugin.type === "destination";
                  });
                  executeDestinations = destination.map(function(plugin) {
                    return plugin.flush && plugin.flush();
                  });
                  return [4, Promise.all(executeDestinations)];
                case 2:
                  _a4.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Timeline2.prototype.addOptOutListener = function(cb) {
          this._optOutListeners.push(cb);
        };
        Timeline2.prototype._clearOptOutListeners = function() {
          this._optOutListeners = [];
        };
        Timeline2.prototype.onIdentityChanged = function(identity) {
          this.plugins.forEach(function(plugin) {
            var _a4;
            void ((_a4 = plugin.onIdentityChanged) === null || _a4 === void 0 ? void 0 : _a4.call(plugin, identity));
          });
        };
        Timeline2.prototype.onSessionIdChanged = function(sessionId) {
          this.plugins.forEach(function(plugin) {
            var _a4;
            void ((_a4 = plugin.onSessionIdChanged) === null || _a4 === void 0 ? void 0 : _a4.call(plugin, sessionId));
          });
        };
        Timeline2.prototype.onOptOutChanged = function(optOut) {
          this.plugins.forEach(function(plugin) {
            var _a4;
            void ((_a4 = plugin.onOptOutChanged) === null || _a4 === void 0 ? void 0 : _a4.call(plugin, optOut));
          });
          void this._callOptOutListeners(optOut);
        };
        Timeline2.prototype._callOptOutListeners = function(optOut) {
          return __awaiter(this, void 0, void 0, function() {
            var _a4, _b, listener, e_3, e_4_1;
            var e_4, _c;
            return __generator(this, function(_d) {
              switch (_d.label) {
                case 0:
                  _d.trys.push([0, 7, 8, 9]);
                  _a4 = __values(this._optOutListeners), _b = _a4.next();
                  _d.label = 1;
                case 1:
                  if (!!_b.done) return [3, 6];
                  listener = _b.value;
                  _d.label = 2;
                case 2:
                  _d.trys.push([2, 4, , 5]);
                  return [4, listener(optOut)];
                case 3:
                  _d.sent();
                  return [3, 5];
                case 4:
                  e_3 = _d.sent();
                  this.loggerProvider.error("Error calling optOut listener", e_3);
                  return [3, 5];
                case 5:
                  _b = _a4.next();
                  return [3, 1];
                case 6:
                  return [3, 9];
                case 7:
                  e_4_1 = _d.sent();
                  e_4 = { error: e_4_1 };
                  return [3, 9];
                case 8:
                  try {
                    if (_b && !_b.done && (_c = _a4.return)) _c.call(_a4);
                  } finally {
                    if (e_4) throw e_4.error;
                  }
                  return [
                    7
                    /*endfinally*/
                  ];
                case 9:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Timeline2.prototype.onReset = function() {
          this.plugins.forEach(function(plugin) {
            var _a4;
            void ((_a4 = plugin.onReset) === null || _a4 === void 0 ? void 0 : _a4.call(plugin));
          });
        };
        return Timeline2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/event-builder.js
  var createTrackEvent, createIdentifyEvent, createGroupIdentifyEvent, createGroupEvent, createRevenueEvent;
  var init_event_builder = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/event-builder.js"() {
      init_tslib_es6();
      init_identify();
      init_event();
      createTrackEvent = function(eventInput, eventProperties, eventOptions) {
        var baseEvent = typeof eventInput === "string" ? { event_type: eventInput } : eventInput;
        return __assign(__assign(__assign({}, baseEvent), eventOptions), eventProperties && { event_properties: eventProperties });
      };
      createIdentifyEvent = function(identify2, eventOptions) {
        var identifyEvent = __assign(__assign({}, eventOptions), { event_type: SpecialEventType.IDENTIFY, user_properties: identify2.getUserProperties() });
        return identifyEvent;
      };
      createGroupIdentifyEvent = function(groupType, groupName, identify2, eventOptions) {
        var _a4;
        var groupIdentify2 = __assign(__assign({}, eventOptions), { event_type: SpecialEventType.GROUP_IDENTIFY, group_properties: identify2.getUserProperties(), groups: (_a4 = {}, _a4[groupType] = groupName, _a4) });
        return groupIdentify2;
      };
      createGroupEvent = function(groupType, groupName, eventOptions) {
        var _a4;
        var identify2 = new Identify();
        identify2.set(groupType, groupName);
        var groupEvent = __assign(__assign({}, eventOptions), { event_type: SpecialEventType.IDENTIFY, user_properties: identify2.getUserProperties(), groups: (_a4 = {}, _a4[groupType] = groupName, _a4) });
        return groupEvent;
      };
      createRevenueEvent = function(revenue2, eventOptions) {
        return __assign(__assign({}, eventOptions), { event_type: SpecialEventType.REVENUE, event_properties: revenue2.getEventProperties() });
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/return-wrapper.js
  var returnWrapper;
  var init_return_wrapper = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/return-wrapper.js"() {
      returnWrapper = function(awaitable) {
        return {
          promise: awaitable || Promise.resolve()
        };
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/core-client.js
  var AmplitudeCore;
  var init_core_client = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/core-client.js"() {
      init_tslib_es6();
      init_event();
      init_identify();
      init_messages();
      init_timeline();
      init_event_builder();
      init_result_builder();
      init_return_wrapper();
      AmplitudeCore = /** @class */
      (function() {
        function AmplitudeCore2(name) {
          if (name === void 0) {
            name = "$default";
          }
          this.initializing = false;
          this.isReady = false;
          this.q = [];
          this.dispatchQ = [];
          this.logEvent = this.track.bind(this);
          this.timeline = new Timeline(this);
          this.name = name;
        }
        AmplitudeCore2.prototype._init = function(config2) {
          return __awaiter(this, void 0, void 0, function() {
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  this.config = config2;
                  this.timeline.reset(this);
                  this.timeline.loggerProvider = this.config.loggerProvider;
                  return [4, this.runQueuedFunctions("q", function() {
                    return _this.isReady = true;
                  })];
                case 1:
                  _a4.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        AmplitudeCore2.prototype.runQueuedFunctions = function(queueName, onQueueEmpty) {
          return __awaiter(this, void 0, void 0, function() {
            var queuedFunctions, queuedFunctions_1, queuedFunctions_1_1, queuedFunction, val, e_1_1;
            var e_1, _a4;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  queuedFunctions = this[queueName];
                  this[queueName] = [];
                  _b.label = 1;
                case 1:
                  _b.trys.push([1, 8, 9, 10]);
                  queuedFunctions_1 = __values(queuedFunctions), queuedFunctions_1_1 = queuedFunctions_1.next();
                  _b.label = 2;
                case 2:
                  if (!!queuedFunctions_1_1.done) return [3, 7];
                  queuedFunction = queuedFunctions_1_1.value;
                  val = queuedFunction();
                  if (!(val && "promise" in val)) return [3, 4];
                  return [4, val.promise];
                case 3:
                  _b.sent();
                  return [3, 6];
                case 4:
                  return [4, val];
                case 5:
                  _b.sent();
                  _b.label = 6;
                case 6:
                  queuedFunctions_1_1 = queuedFunctions_1.next();
                  return [3, 2];
                case 7:
                  return [3, 10];
                case 8:
                  e_1_1 = _b.sent();
                  e_1 = { error: e_1_1 };
                  return [3, 10];
                case 9:
                  try {
                    if (queuedFunctions_1_1 && !queuedFunctions_1_1.done && (_a4 = queuedFunctions_1.return)) _a4.call(queuedFunctions_1);
                  } finally {
                    if (e_1) throw e_1.error;
                  }
                  return [
                    7
                    /*endfinally*/
                  ];
                case 10:
                  if (!this[queueName].length) return [3, 12];
                  return [4, this.runQueuedFunctions(queueName, onQueueEmpty)];
                case 11:
                  _b.sent();
                  return [3, 13];
                case 12:
                  onQueueEmpty === null || onQueueEmpty === void 0 ? void 0 : onQueueEmpty();
                  _b.label = 13;
                case 13:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        AmplitudeCore2.prototype.track = function(eventInput, eventProperties, eventOptions) {
          var event = createTrackEvent(eventInput, eventProperties, eventOptions);
          this.userProperties = this.getOperationAppliedUserProperties(event.user_properties);
          return returnWrapper(this.dispatch(event));
        };
        AmplitudeCore2.prototype.identify = function(identify2, eventOptions) {
          var event = createIdentifyEvent(identify2, eventOptions);
          this.userProperties = this.getOperationAppliedUserProperties(event.user_properties);
          return returnWrapper(this.dispatch(event));
        };
        AmplitudeCore2.prototype.groupIdentify = function(groupType, groupName, identify2, eventOptions) {
          var event = createGroupIdentifyEvent(groupType, groupName, identify2, eventOptions);
          return returnWrapper(this.dispatch(event));
        };
        AmplitudeCore2.prototype.setGroup = function(groupType, groupName, eventOptions) {
          var event = createGroupEvent(groupType, groupName, eventOptions);
          this.userProperties = this.getOperationAppliedUserProperties(event.user_properties);
          return returnWrapper(this.dispatch(event));
        };
        AmplitudeCore2.prototype.revenue = function(revenue2, eventOptions) {
          var event = createRevenueEvent(revenue2, eventOptions);
          return returnWrapper(this.dispatch(event));
        };
        AmplitudeCore2.prototype.add = function(plugin) {
          if (!this.isReady) {
            this.q.push(this._addPlugin.bind(this, plugin));
            return returnWrapper();
          }
          return this._addPlugin(plugin);
        };
        AmplitudeCore2.prototype._addPlugin = function(plugin) {
          return returnWrapper(this.timeline.register(plugin, this.config));
        };
        AmplitudeCore2.prototype.remove = function(pluginName) {
          if (!this.isReady) {
            this.q.push(this._removePlugin.bind(this, pluginName));
            return returnWrapper();
          }
          return this._removePlugin(pluginName);
        };
        AmplitudeCore2.prototype._removePlugin = function(pluginName) {
          return returnWrapper(this.timeline.deregister(pluginName, this.config));
        };
        AmplitudeCore2.prototype.dispatchWithCallback = function(event, callback) {
          if (!this.isReady) {
            return callback(buildResult(event, 0, CLIENT_NOT_INITIALIZED));
          }
          void this.process(event).then(callback);
        };
        AmplitudeCore2.prototype.dispatch = function(event) {
          return __awaiter(this, void 0, void 0, function() {
            var _this = this;
            return __generator(this, function(_a4) {
              if (!this.isReady) {
                return [2, new Promise(function(resolve) {
                  _this.dispatchQ.push(_this.dispatchWithCallback.bind(_this, event, resolve));
                })];
              }
              return [2, this.process(event)];
            });
          });
        };
        AmplitudeCore2.prototype.getOperationAppliedUserProperties = function(userProperties) {
          var _a4;
          var base = (_a4 = this.userProperties) !== null && _a4 !== void 0 ? _a4 : {};
          var updatedProperties = __assign({}, base);
          if (userProperties === void 0) {
            return updatedProperties;
          }
          var nonOpProperties = {};
          Object.keys(userProperties).forEach(function(key) {
            if (!Object.values(IdentifyOperation).includes(key)) {
              nonOpProperties[key] = userProperties[key];
            }
          });
          OrderedIdentifyOperations.forEach(function(operation) {
            if (!Object.keys(userProperties).includes(operation))
              return;
            var opProperties = userProperties[operation];
            switch (operation) {
              case IdentifyOperation.CLEAR_ALL:
                Object.keys(updatedProperties).forEach(function(prop) {
                  delete updatedProperties[prop];
                });
                break;
              case IdentifyOperation.UNSET:
                Object.keys(opProperties).forEach(function(prop) {
                  delete updatedProperties[prop];
                });
                break;
              case IdentifyOperation.SET:
                Object.assign(updatedProperties, opProperties);
                break;
            }
          });
          Object.assign(updatedProperties, nonOpProperties);
          return updatedProperties;
        };
        AmplitudeCore2.prototype.process = function(event) {
          return __awaiter(this, void 0, void 0, function() {
            var eventType, result3, e_2, message, result3;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (this.config.optOut) {
                    return [2, buildResult(event, 0, OPT_OUT_MESSAGE)];
                  }
                  eventType = event.event_type;
                  if (typeof eventType !== "string" || eventType.trim().length === 0) {
                    this.config.loggerProvider.error(EMPTY_EVENT_TYPE_MESSAGE);
                    return [2, buildResult(event, 0, EMPTY_EVENT_TYPE_MESSAGE)];
                  }
                  if (event.event_type === SpecialEventType.IDENTIFY) {
                    this.timeline.onIdentityChanged({ userProperties: this.userProperties });
                  }
                  return [4, this.timeline.push(event)];
                case 1:
                  result3 = _a4.sent();
                  result3.code === 200 ? this.config.loggerProvider.log(result3.message) : result3.code === 100 ? this.config.loggerProvider.warn(result3.message) : this.config.loggerProvider.error(result3.message);
                  return [2, result3];
                case 2:
                  e_2 = _a4.sent();
                  message = String(e_2);
                  this.config.loggerProvider.error(message);
                  result3 = buildResult(event, 0, message);
                  return [2, result3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        AmplitudeCore2.prototype.setOptOut = function(optOut) {
          if (!this.isReady) {
            this.q.push(this._setOptOut.bind(this, Boolean(optOut)));
            return;
          }
          this._setOptOut(optOut);
        };
        AmplitudeCore2.prototype._setOptOut = function(optOut) {
          if (this.config.optOut !== optOut) {
            this.config.optOut = Boolean(optOut);
            this.timeline.onOptOutChanged(optOut);
          }
        };
        AmplitudeCore2.prototype.flush = function() {
          return returnWrapper(this.timeline.flush());
        };
        AmplitudeCore2.prototype.plugin = function(name) {
          var plugin = this.timeline.plugins.find(function(plugin2) {
            return plugin2.name === name;
          });
          if (plugin === void 0) {
            this.config.loggerProvider.debug("Cannot find plugin with name ".concat(name));
            return void 0;
          }
          return plugin;
        };
        AmplitudeCore2.prototype.plugins = function(pluginClass) {
          return this.timeline.plugins.filter(function(plugin) {
            return plugin instanceof pluginClass;
          });
        };
        return AmplitudeCore2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/revenue.js
  var Revenue, RevenueProperty;
  var init_revenue = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/revenue.js"() {
      init_tslib_es6();
      init_valid_properties();
      Revenue = /** @class */
      (function() {
        function Revenue2() {
          this.productId = "";
          this.quantity = 1;
          this.price = 0;
        }
        Revenue2.prototype.setProductId = function(productId) {
          this.productId = productId;
          return this;
        };
        Revenue2.prototype.setQuantity = function(quantity) {
          if (quantity > 0) {
            this.quantity = quantity;
          }
          return this;
        };
        Revenue2.prototype.setPrice = function(price) {
          this.price = price;
          return this;
        };
        Revenue2.prototype.setRevenueType = function(revenueType) {
          this.revenueType = revenueType;
          return this;
        };
        Revenue2.prototype.setCurrency = function(currency) {
          this.currency = currency;
          return this;
        };
        Revenue2.prototype.setRevenue = function(revenue2) {
          this.revenue = revenue2;
          return this;
        };
        Revenue2.prototype.setReceipt = function(receipt) {
          this.receipt = receipt;
          return this;
        };
        Revenue2.prototype.setReceiptSig = function(receiptSig) {
          this.receiptSig = receiptSig;
          return this;
        };
        Revenue2.prototype.setEventProperties = function(properties) {
          try {
            var filtered = JSON.parse(JSON.stringify(properties));
            if (isValidObject(filtered)) {
              this.properties = filtered;
            }
          } catch (_a4) {
          }
          return this;
        };
        Revenue2.prototype.getEventProperties = function() {
          var eventProperties = this.properties ? __assign({}, this.properties) : {};
          eventProperties[RevenueProperty.REVENUE_PRODUCT_ID] = this.productId;
          eventProperties[RevenueProperty.REVENUE_QUANTITY] = this.quantity;
          eventProperties[RevenueProperty.REVENUE_PRICE] = this.price;
          eventProperties[RevenueProperty.REVENUE_TYPE] = this.revenueType;
          eventProperties[RevenueProperty.REVENUE_CURRENCY] = this.currency;
          eventProperties[RevenueProperty.REVENUE] = this.revenue;
          eventProperties[RevenueProperty.RECEIPT] = this.receipt;
          eventProperties[RevenueProperty.RECEIPT_SIG] = this.receiptSig;
          return eventProperties;
        };
        return Revenue2;
      })();
      (function(RevenueProperty3) {
        RevenueProperty3["REVENUE_PRODUCT_ID"] = "$productId";
        RevenueProperty3["REVENUE_QUANTITY"] = "$quantity";
        RevenueProperty3["REVENUE_PRICE"] = "$price";
        RevenueProperty3["REVENUE_TYPE"] = "$revenueType";
        RevenueProperty3["REVENUE_CURRENCY"] = "$currency";
        RevenueProperty3["REVENUE"] = "$revenue";
        RevenueProperty3["RECEIPT"] = "$receipt";
        RevenueProperty3["RECEIPT_SIG"] = "$receiptSig";
      })(RevenueProperty || (RevenueProperty = {}));
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/chunk.js
  var chunk;
  var init_chunk = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/chunk.js"() {
      chunk = function(arr, size) {
        var chunkSize = Math.max(size, 1);
        return arr.reduce(function(chunks, element, index4) {
          var chunkIndex = Math.floor(index4 / chunkSize);
          if (!chunks[chunkIndex]) {
            chunks[chunkIndex] = [];
          }
          chunks[chunkIndex].push(element);
          return chunks;
        }, []);
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/loglevel.js
  var LogLevel;
  var init_loglevel = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/loglevel.js"() {
      (function(LogLevel2) {
        LogLevel2[LogLevel2["None"] = 0] = "None";
        LogLevel2[LogLevel2["Error"] = 1] = "Error";
        LogLevel2[LogLevel2["Warn"] = 2] = "Warn";
        LogLevel2[LogLevel2["Verbose"] = 3] = "Verbose";
        LogLevel2[LogLevel2["Debug"] = 4] = "Debug";
      })(LogLevel || (LogLevel = {}));
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/logger.js
  var PREFIX, Logger;
  var init_logger = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/logger.js"() {
      init_loglevel();
      PREFIX = "Amplitude Logger ";
      Logger = /** @class */
      (function() {
        function Logger2() {
          this.logLevel = LogLevel.None;
        }
        Logger2.prototype.disable = function() {
          this.logLevel = LogLevel.None;
        };
        Logger2.prototype.enable = function(logLevel) {
          if (logLevel === void 0) {
            logLevel = LogLevel.Warn;
          }
          this.logLevel = logLevel;
        };
        Logger2.prototype.log = function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          if (this.logLevel < LogLevel.Verbose) {
            return;
          }
          console.log("".concat(PREFIX, "[Log]: ").concat(args.join(" ")));
        };
        Logger2.prototype.warn = function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          if (this.logLevel < LogLevel.Warn) {
            return;
          }
          console.warn("".concat(PREFIX, "[Warn]: ").concat(args.join(" ")));
        };
        Logger2.prototype.error = function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          if (this.logLevel < LogLevel.Error) {
            return;
          }
          console.error("".concat(PREFIX, "[Error]: ").concat(args.join(" ")));
        };
        Logger2.prototype.debug = function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          if (this.logLevel < LogLevel.Debug) {
            return;
          }
          console.log("".concat(PREFIX, "[Debug]: ").concat(args.join(" ")));
        };
        return Logger2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/instance-name.js
  var normalizeInstanceName;
  var init_instance_name = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/instance-name.js"() {
      init_constants();
      normalizeInstanceName = function(instanceName) {
        return instanceName === void 0 || instanceName === "" ? DEFAULT_INSTANCE_NAME : instanceName;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/config.js
  var getDefaultConfig, Config, getServerUrl, createServerConfig, RequestMetadata;
  var init_config = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/config.js"() {
      init_constants();
      init_logger();
      init_loglevel();
      init_instance_name();
      getDefaultConfig = function() {
        return {
          flushMaxRetries: 12,
          flushQueueSize: 200,
          flushIntervalMillis: 1e4,
          instanceName: DEFAULT_INSTANCE_NAME,
          logLevel: LogLevel.Warn,
          loggerProvider: new Logger(),
          offline: false,
          optOut: false,
          serverUrl: AMPLITUDE_SERVER_URL,
          serverZone: "US",
          useBatch: false
        };
      };
      Config = /** @class */
      (function() {
        function Config2(options) {
          var _a4, _b, _c, _d;
          this._optOut = false;
          var defaultConfig = getDefaultConfig();
          this.apiKey = options.apiKey;
          this.flushIntervalMillis = (_a4 = options.flushIntervalMillis) !== null && _a4 !== void 0 ? _a4 : defaultConfig.flushIntervalMillis;
          this.flushMaxRetries = options.flushMaxRetries || defaultConfig.flushMaxRetries;
          this.flushQueueSize = options.flushQueueSize || defaultConfig.flushQueueSize;
          this.instanceName = normalizeInstanceName(options.instanceName);
          this.loggerProvider = options.loggerProvider || defaultConfig.loggerProvider;
          this.logLevel = (_b = options.logLevel) !== null && _b !== void 0 ? _b : defaultConfig.logLevel;
          this.minIdLength = options.minIdLength;
          this.plan = options.plan;
          this.ingestionMetadata = options.ingestionMetadata;
          this.offline = options.offline !== void 0 ? options.offline : defaultConfig.offline;
          this.optOut = (_c = options.optOut) !== null && _c !== void 0 ? _c : defaultConfig.optOut;
          this.serverUrl = options.serverUrl;
          this.serverZone = options.serverZone || defaultConfig.serverZone;
          this.storageProvider = options.storageProvider;
          this.transportProvider = options.transportProvider;
          this.useBatch = (_d = options.useBatch) !== null && _d !== void 0 ? _d : defaultConfig.useBatch;
          this.loggerProvider.enable(this.logLevel);
          var serverConfig = createServerConfig(options.serverUrl, options.serverZone, options.useBatch);
          this.serverZone = serverConfig.serverZone;
          this.serverUrl = serverConfig.serverUrl;
        }
        Object.defineProperty(Config2.prototype, "optOut", {
          get: function() {
            return this._optOut;
          },
          set: function(optOut) {
            this._optOut = optOut;
          },
          enumerable: false,
          configurable: true
        });
        return Config2;
      })();
      getServerUrl = function(serverZone, useBatch) {
        if (serverZone === "EU") {
          return useBatch ? EU_AMPLITUDE_BATCH_SERVER_URL : EU_AMPLITUDE_SERVER_URL;
        }
        return useBatch ? AMPLITUDE_BATCH_SERVER_URL : AMPLITUDE_SERVER_URL;
      };
      createServerConfig = function(serverUrl, serverZone, useBatch) {
        if (serverUrl === void 0) {
          serverUrl = "";
        }
        if (serverZone === void 0) {
          serverZone = getDefaultConfig().serverZone;
        }
        if (useBatch === void 0) {
          useBatch = getDefaultConfig().useBatch;
        }
        if (serverUrl) {
          return { serverUrl, serverZone: void 0 };
        }
        var _serverZone = ["US", "EU"].includes(serverZone) ? serverZone : getDefaultConfig().serverZone;
        return {
          serverZone: _serverZone,
          serverUrl: getServerUrl(_serverZone, useBatch)
        };
      };
      RequestMetadata = /** @class */
      (function() {
        function RequestMetadata2() {
          this.sdk = {
            metrics: {
              histogram: {}
            }
          };
        }
        RequestMetadata2.prototype.recordHistogram = function(key, value) {
          this.sdk.metrics.histogram[key] = value;
        };
        return RequestMetadata2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/status-code.js
  function isSuccessStatusCode(code) {
    return code >= 200 && code < 300;
  }
  var init_status_code = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/status-code.js"() {
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/debug.js
  var getStacktrace, getClientLogConfig, getValueByStringPath, getClientStates, debugWrapper;
  var init_debug = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/debug.js"() {
      init_tslib_es6();
      init_loglevel();
      getStacktrace = function(ignoreDepth) {
        if (ignoreDepth === void 0) {
          ignoreDepth = 0;
        }
        var trace = new Error().stack || "";
        return trace.split("\n").slice(2 + ignoreDepth).map(function(text) {
          return text.trim();
        });
      };
      getClientLogConfig = function(client) {
        return function() {
          var _a4 = __assign({}, client.config), logger = _a4.loggerProvider, logLevel = _a4.logLevel;
          return {
            logger,
            logLevel
          };
        };
      };
      getValueByStringPath = function(obj, path) {
        var e_1, _a4;
        path = path.replace(/\[(\w+)\]/g, ".$1");
        path = path.replace(/^\./, "");
        try {
          for (var _b = __values(path.split(".")), _c = _b.next(); !_c.done; _c = _b.next()) {
            var attr2 = _c.value;
            if (attr2 in obj) {
              obj = obj[attr2];
            } else {
              return;
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        return obj;
      };
      getClientStates = function(client, paths) {
        return function() {
          var e_2, _a4;
          var res = {};
          try {
            for (var paths_1 = __values(paths), paths_1_1 = paths_1.next(); !paths_1_1.done; paths_1_1 = paths_1.next()) {
              var path = paths_1_1.value;
              res[path] = getValueByStringPath(client, path);
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (paths_1_1 && !paths_1_1.done && (_a4 = paths_1.return)) _a4.call(paths_1);
            } finally {
              if (e_2) throw e_2.error;
            }
          }
          return res;
        };
      };
      debugWrapper = function(fn, fnName, getLogConfig2, getStates, fnContext) {
        if (fnContext === void 0) {
          fnContext = null;
        }
        return function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          var _a4 = getLogConfig2(), logger = _a4.logger, logLevel = _a4.logLevel;
          if (logLevel && logLevel < LogLevel.Debug || !logLevel || !logger) {
            return fn.apply(fnContext, args);
          }
          var debugContext = {
            type: "invoke public method",
            name: fnName,
            args,
            stacktrace: getStacktrace(1),
            time: {
              start: (/* @__PURE__ */ new Date()).toISOString()
            },
            states: {}
          };
          if (getStates && debugContext.states) {
            debugContext.states.before = getStates();
          }
          var result3 = fn.apply(fnContext, args);
          if (result3 && result3.promise) {
            result3.promise.then(function() {
              if (getStates && debugContext.states) {
                debugContext.states.after = getStates();
              }
              if (debugContext.time) {
                debugContext.time.end = (/* @__PURE__ */ new Date()).toISOString();
              }
              logger.debug(JSON.stringify(debugContext, null, 2));
            });
          } else {
            if (getStates && debugContext.states) {
              debugContext.states.after = getStates();
            }
            if (debugContext.time) {
              debugContext.time.end = (/* @__PURE__ */ new Date()).toISOString();
            }
            logger.debug(JSON.stringify(debugContext, null, 2));
          }
          return result3;
        };
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/plugins/destination.js
  function getErrorMessage(error) {
    if (error instanceof Error)
      return error.message;
    return String(error);
  }
  function getResponseBodyString(res) {
    var responseBodyString = "";
    try {
      if ("body" in res) {
        responseBodyString = JSON.stringify(res.body, null, 2);
      }
    } catch (_a4) {
    }
    return responseBodyString;
  }
  var DEFAULT_AMPLITUDE_SERVER_URLS, shouldCompressUploadBodyForRequest, Destination;
  var init_destination = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/plugins/destination.js"() {
      init_tslib_es6();
      init_status();
      init_messages();
      init_constants();
      init_chunk();
      init_result_builder();
      init_config();
      init_uuid();
      init_status_code();
      init_debug();
      DEFAULT_AMPLITUDE_SERVER_URLS = /* @__PURE__ */ new Set([
        AMPLITUDE_SERVER_URL,
        EU_AMPLITUDE_SERVER_URL,
        AMPLITUDE_BATCH_SERVER_URL,
        EU_AMPLITUDE_BATCH_SERVER_URL
      ]);
      shouldCompressUploadBodyForRequest = function(serverUrl, enableRequestBodyCompression) {
        if (enableRequestBodyCompression === void 0) {
          enableRequestBodyCompression = false;
        }
        if (DEFAULT_AMPLITUDE_SERVER_URLS.has(serverUrl)) {
          return true;
        }
        return enableRequestBodyCompression;
      };
      Destination = /** @class */
      (function() {
        function Destination2(context) {
          this.name = "amplitude";
          this.type = "destination";
          this.retryTimeout = 1e3;
          this.throttleTimeout = 3e4;
          this.storageKey = "";
          this.scheduleId = null;
          this.scheduledTimeout = 0;
          this.flushId = null;
          this.queue = [];
          this.diagnosticsClient = context === null || context === void 0 ? void 0 : context.diagnosticsClient;
        }
        Destination2.prototype.setup = function(config2) {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            var unsent;
            var _this = this;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  this.config = config2;
                  this.storageKey = "".concat(STORAGE_PREFIX, "_").concat(this.config.apiKey.substring(0, 10));
                  return [4, (_a4 = this.config.storageProvider) === null || _a4 === void 0 ? void 0 : _a4.get(this.storageKey)];
                case 1:
                  unsent = _b.sent();
                  if (unsent && unsent.length > 0) {
                    void Promise.all(unsent.map(function(event) {
                      return _this.execute(event);
                    })).catch();
                  }
                  return [2, Promise.resolve(void 0)];
              }
            });
          });
        };
        Destination2.prototype.execute = function(event) {
          var _this = this;
          if (!event.insert_id) {
            event.insert_id = UUID();
          }
          return new Promise(function(resolve) {
            var context = {
              event,
              attempts: 0,
              callback: function(result3) {
                return resolve(result3);
              },
              timeout: 0
            };
            _this.queue.push(context);
            _this.schedule(_this.config.flushIntervalMillis);
            _this.saveEvents();
          });
        };
        Destination2.prototype.removeEventsExceedFlushMaxRetries = function(list) {
          var _this = this;
          return list.filter(function(context) {
            context.attempts += 1;
            if (context.attempts < _this.config.flushMaxRetries) {
              return true;
            }
            void _this.fulfillRequest([context], 500, MAX_RETRIES_EXCEEDED_MESSAGE);
            return false;
          });
        };
        Destination2.prototype.scheduleEvents = function(list) {
          var _this = this;
          list.forEach(function(context) {
            _this.schedule(context.timeout === 0 ? _this.config.flushIntervalMillis : context.timeout);
          });
        };
        Destination2.prototype.schedule = function(timeout) {
          var _this = this;
          if (this.config.offline) {
            return;
          }
          if (this.scheduleId === null || this.scheduleId && timeout > this.scheduledTimeout) {
            if (this.scheduleId) {
              clearTimeout(this.scheduleId);
            }
            this.scheduledTimeout = timeout;
            this.scheduleId = setTimeout(function() {
              _this.queue = _this.queue.map(function(context) {
                context.timeout = 0;
                return context;
              });
              void _this.flush(true);
            }, timeout);
            return;
          }
        };
        Destination2.prototype.resetSchedule = function() {
          this.scheduleId = null;
          this.scheduledTimeout = 0;
        };
        Destination2.prototype.flush = function(useRetry) {
          if (useRetry === void 0) {
            useRetry = false;
          }
          return __awaiter(this, void 0, void 0, function() {
            var list, later, batches;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (this.config.offline) {
                    this.resetSchedule();
                    this.config.loggerProvider.debug("Skipping flush while offline.");
                    return [
                      2
                      /*return*/
                    ];
                  }
                  if (this.flushId) {
                    this.resetSchedule();
                    this.config.loggerProvider.debug("Skipping flush because previous flush has not resolved.");
                    return [
                      2
                      /*return*/
                    ];
                  }
                  this.flushId = this.scheduleId;
                  this.resetSchedule();
                  list = [];
                  later = [];
                  this.queue.forEach(function(context) {
                    return context.timeout === 0 ? list.push(context) : later.push(context);
                  });
                  batches = chunk(list, this.config.flushQueueSize);
                  return [4, batches.reduce(function(promise, batch) {
                    return __awaiter(_this, void 0, void 0, function() {
                      return __generator(this, function(_a5) {
                        switch (_a5.label) {
                          case 0:
                            return [4, promise];
                          case 1:
                            _a5.sent();
                            return [4, this.send(batch, useRetry)];
                          case 2:
                            return [2, _a5.sent()];
                        }
                      });
                    });
                  }, Promise.resolve())];
                case 1:
                  _a4.sent();
                  this.flushId = null;
                  this.scheduleEvents(this.queue);
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Destination2.prototype.send = function(list, useRetry) {
          var _a4;
          if (useRetry === void 0) {
            useRetry = true;
          }
          return __awaiter(this, void 0, void 0, function() {
            var payload, serverUrl, shouldCompressUploadBody, res, e_1, errorMessage;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  if (!this.config.apiKey) {
                    return [2, this.fulfillRequest(list, 400, MISSING_API_KEY_MESSAGE)];
                  }
                  payload = {
                    api_key: this.config.apiKey,
                    events: list.map(function(context) {
                      var _a5 = context.event, extra = _a5.extra, eventWithoutExtra = __rest(_a5, ["extra"]);
                      return eventWithoutExtra;
                    }),
                    options: {
                      min_id_length: this.config.minIdLength
                    },
                    client_upload_time: (/* @__PURE__ */ new Date()).toISOString(),
                    request_metadata: this.config.requestMetadata
                  };
                  this.config.requestMetadata = new RequestMetadata();
                  _b.label = 1;
                case 1:
                  _b.trys.push([1, 3, , 4]);
                  serverUrl = createServerConfig(this.config.serverUrl, this.config.serverZone, this.config.useBatch).serverUrl;
                  shouldCompressUploadBody = shouldCompressUploadBodyForRequest(serverUrl, this.config.enableRequestBodyCompression);
                  return [4, this.config.transportProvider.send(serverUrl, payload, shouldCompressUploadBody)];
                case 2:
                  res = _b.sent();
                  if (res === null) {
                    this.fulfillRequest(list, 0, UNEXPECTED_ERROR_MESSAGE);
                    return [
                      2
                      /*return*/
                    ];
                  }
                  if (!useRetry) {
                    if ("body" in res) {
                      this.fulfillRequest(list, res.statusCode, "".concat(res.status, ": ").concat(getResponseBodyString(res)));
                    } else {
                      this.fulfillRequest(list, res.statusCode, res.status);
                    }
                    return [
                      2
                      /*return*/
                    ];
                  }
                  this.handleResponse(res, list);
                  return [3, 4];
                case 3:
                  e_1 = _b.sent();
                  errorMessage = getErrorMessage(e_1);
                  this.config.loggerProvider.error(errorMessage);
                  (_a4 = this.diagnosticsClient) === null || _a4 === void 0 ? void 0 : _a4.recordEvent("analytics.events.unsuccessful.from.catch.error", {
                    events: list.map(function(context) {
                      return context.event.event_type;
                    }),
                    message: errorMessage,
                    stack_trace: getStacktrace()
                  });
                  this.handleResponse({ status: Status.Failed, statusCode: 0 }, list);
                  return [3, 4];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        Destination2.prototype.handleResponse = function(res, list) {
          var _a4;
          if (!isSuccessStatusCode(res.statusCode)) {
            (_a4 = this.diagnosticsClient) === null || _a4 === void 0 ? void 0 : _a4.recordEvent("analytics.events.unsuccessful", {
              events: list.map(function(context) {
                return context.event.event_type;
              }),
              code: res.statusCode,
              status: res.status,
              body: getResponseBodyString(res),
              stack_trace: getStacktrace()
            });
          }
          var status = res.status;
          switch (status) {
            case Status.Success: {
              this.handleSuccessResponse(res, list);
              break;
            }
            case Status.Invalid: {
              this.handleInvalidResponse(res, list);
              break;
            }
            case Status.PayloadTooLarge: {
              this.handlePayloadTooLargeResponse(res, list);
              break;
            }
            case Status.RateLimit: {
              this.handleRateLimitResponse(res, list);
              break;
            }
            default: {
              this.config.loggerProvider.warn(`{code: 0, error: "Status '`.concat(status, "' provided for ").concat(list.length, ' events"}'));
              this.handleOtherResponse(list);
              break;
            }
          }
        };
        Destination2.prototype.handleSuccessResponse = function(res, list) {
          this.fulfillRequest(list, res.statusCode, SUCCESS_MESSAGE);
        };
        Destination2.prototype.handleInvalidResponse = function(res, list) {
          var _this = this;
          if (res.body.missingField || res.body.error.startsWith(INVALID_API_KEY)) {
            this.fulfillRequest(list, res.statusCode, res.body.error);
            return;
          }
          var dropIndex = __spreadArray(__spreadArray(__spreadArray(__spreadArray([], __read(Object.values(res.body.eventsWithInvalidFields)), false), __read(Object.values(res.body.eventsWithMissingFields)), false), __read(Object.values(res.body.eventsWithInvalidIdLengths)), false), __read(res.body.silencedEvents), false).flat();
          var dropIndexSet = new Set(dropIndex);
          var retry = list.filter(function(context, index4) {
            if (dropIndexSet.has(index4)) {
              _this.fulfillRequest([context], res.statusCode, res.body.error);
              return;
            }
            return true;
          });
          if (retry.length > 0) {
            this.config.loggerProvider.warn(getResponseBodyString(res));
          }
          var tryable = this.removeEventsExceedFlushMaxRetries(retry);
          this.scheduleEvents(tryable);
        };
        Destination2.prototype.handlePayloadTooLargeResponse = function(res, list) {
          if (list.length === 1) {
            this.fulfillRequest(list, res.statusCode, res.body.error);
            return;
          }
          this.config.loggerProvider.warn(getResponseBodyString(res));
          this.config.flushQueueSize /= 2;
          var tryable = this.removeEventsExceedFlushMaxRetries(list);
          this.scheduleEvents(tryable);
        };
        Destination2.prototype.handleRateLimitResponse = function(res, list) {
          var _this = this;
          var dropUserIds = Object.keys(res.body.exceededDailyQuotaUsers);
          var dropDeviceIds = Object.keys(res.body.exceededDailyQuotaDevices);
          var throttledIndex = res.body.throttledEvents;
          var dropUserIdsSet = new Set(dropUserIds);
          var dropDeviceIdsSet = new Set(dropDeviceIds);
          var throttledIndexSet = new Set(throttledIndex);
          var retry = list.filter(function(context, index4) {
            if (context.event.user_id && dropUserIdsSet.has(context.event.user_id) || context.event.device_id && dropDeviceIdsSet.has(context.event.device_id)) {
              _this.fulfillRequest([context], res.statusCode, res.body.error);
              return;
            }
            if (throttledIndexSet.has(index4)) {
              context.timeout = _this.throttleTimeout;
            }
            return true;
          });
          if (retry.length > 0) {
            this.config.loggerProvider.warn(getResponseBodyString(res));
          }
          var tryable = this.removeEventsExceedFlushMaxRetries(retry);
          this.scheduleEvents(tryable);
        };
        Destination2.prototype.handleOtherResponse = function(list) {
          var _this = this;
          var later = list.map(function(context) {
            context.timeout = context.attempts * _this.retryTimeout;
            return context;
          });
          var tryable = this.removeEventsExceedFlushMaxRetries(later);
          this.scheduleEvents(tryable);
        };
        Destination2.prototype.fulfillRequest = function(list, code, message) {
          var _a4, _b, _c;
          if (!isSuccessStatusCode(code)) {
            (_a4 = this.diagnosticsClient) === null || _a4 === void 0 ? void 0 : _a4.increment("analytics.events.dropped", list.length);
            (_b = this.diagnosticsClient) === null || _b === void 0 ? void 0 : _b.recordEvent("analytics.events.dropped", {
              events: list.map(function(context) {
                return context.event.event_type;
              }),
              code,
              message,
              stack_trace: getStacktrace()
            });
          } else {
            (_c = this.diagnosticsClient) === null || _c === void 0 ? void 0 : _c.increment("analytics.events.sent", list.length);
          }
          this.removeEvents(list);
          list.forEach(function(context) {
            return context.callback(buildResult(context.event, code, message));
          });
        };
        Destination2.prototype.saveEvents = function() {
          if (!this.config.storageProvider) {
            return;
          }
          var updatedEvents = this.queue.map(function(context) {
            return context.event;
          });
          void this.config.storageProvider.set(this.storageKey, updatedEvents);
        };
        Destination2.prototype.removeEvents = function(eventsToRemove) {
          this.queue = this.queue.filter(function(queuedContext) {
            return !eventsToRemove.some(function(context) {
              return context.event.insert_id === queuedContext.event.insert_id;
            });
          });
          this.saveEvents();
        };
        return Destination2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-connector/dist/analytics-connector.esm.js
  var ApplicationContextProviderImpl, getLanguage, EventBridgeImpl, isEqual, ID_OP_SET, ID_OP_UNSET, ID_OP_CLEAR_ALL, IdentityStoreImpl, safeGlobal, AnalyticsConnector;
  var init_analytics_connector_esm = __esm({
    "node_modules/@amplitude/analytics-connector/dist/analytics-connector.esm.js"() {
      ApplicationContextProviderImpl = class {
        getApplicationContext() {
          return {
            versionName: this.versionName,
            language: getLanguage(),
            platform: "Web",
            os: void 0,
            deviceModel: void 0
          };
        }
      };
      getLanguage = () => {
        return typeof navigator !== "undefined" && (navigator.languages && navigator.languages[0] || navigator.language) || "";
      };
      EventBridgeImpl = class {
        constructor() {
          this.queue = [];
        }
        logEvent(event) {
          if (!this.receiver) {
            if (this.queue.length < 512) {
              this.queue.push(event);
            }
          } else {
            this.receiver(event);
          }
        }
        setEventReceiver(receiver) {
          this.receiver = receiver;
          if (this.queue.length > 0) {
            this.queue.forEach((event) => {
              receiver(event);
            });
            this.queue = [];
          }
        }
      };
      isEqual = (obj1, obj2) => {
        const primitive = ["string", "number", "boolean", "undefined"];
        const typeA = typeof obj1;
        const typeB = typeof obj2;
        if (typeA !== typeB) {
          return false;
        }
        for (const p2 of primitive) {
          if (p2 === typeA) {
            return obj1 === obj2;
          }
        }
        if (obj1 == null && obj2 == null) {
          return true;
        } else if (obj1 == null || obj2 == null) {
          return false;
        }
        if (obj1.length !== obj2.length) {
          return false;
        }
        const isArrayA = Array.isArray(obj1);
        const isArrayB = Array.isArray(obj2);
        if (isArrayA !== isArrayB) {
          return false;
        }
        if (isArrayA && isArrayB) {
          for (let i2 = 0; i2 < obj1.length; i2++) {
            if (!isEqual(obj1[i2], obj2[i2])) {
              return false;
            }
          }
        } else {
          const sorted1 = Object.keys(obj1).sort();
          const sorted2 = Object.keys(obj2).sort();
          if (!isEqual(sorted1, sorted2)) {
            return false;
          }
          let result3 = true;
          Object.keys(obj1).forEach((key) => {
            if (!isEqual(obj1[key], obj2[key])) {
              result3 = false;
            }
          });
          return result3;
        }
        return true;
      };
      ID_OP_SET = "$set";
      ID_OP_UNSET = "$unset";
      ID_OP_CLEAR_ALL = "$clearAll";
      if (!Object.entries) {
        Object.entries = function(obj) {
          const ownProps = Object.keys(obj);
          let i2 = ownProps.length;
          const resArray = new Array(i2);
          while (i2--) {
            resArray[i2] = [ownProps[i2], obj[ownProps[i2]]];
          }
          return resArray;
        };
      }
      IdentityStoreImpl = class {
        constructor() {
          this.identity = {
            userProperties: {}
          };
          this.listeners = /* @__PURE__ */ new Set();
        }
        editIdentity() {
          const self2 = this;
          const actingUserProperties = Object.assign({}, this.identity.userProperties);
          const actingIdentity = Object.assign(Object.assign({}, this.identity), {
            userProperties: actingUserProperties
          });
          return {
            setUserId: function(userId) {
              actingIdentity.userId = userId;
              return this;
            },
            setDeviceId: function(deviceId) {
              actingIdentity.deviceId = deviceId;
              return this;
            },
            setUserProperties: function(userProperties) {
              actingIdentity.userProperties = userProperties;
              return this;
            },
            setOptOut(optOut) {
              actingIdentity.optOut = optOut;
              return this;
            },
            updateUserProperties: function(actions) {
              let actingProperties = actingIdentity.userProperties || {};
              for (const [action, properties] of Object.entries(actions)) {
                switch (action) {
                  case ID_OP_SET:
                    for (const [key, value] of Object.entries(properties)) {
                      actingProperties[key] = value;
                    }
                    break;
                  case ID_OP_UNSET:
                    for (const key of Object.keys(properties)) {
                      delete actingProperties[key];
                    }
                    break;
                  case ID_OP_CLEAR_ALL:
                    actingProperties = {};
                    break;
                }
              }
              actingIdentity.userProperties = actingProperties;
              return this;
            },
            commit: function() {
              self2.setIdentity(actingIdentity);
              return this;
            }
          };
        }
        getIdentity() {
          return Object.assign({}, this.identity);
        }
        setIdentity(identity) {
          const originalIdentity = Object.assign({}, this.identity);
          this.identity = Object.assign({}, identity);
          if (!isEqual(originalIdentity, this.identity)) {
            this.listeners.forEach((listener) => {
              listener(identity);
            });
          }
        }
        addIdentityListener(listener) {
          this.listeners.add(listener);
        }
        removeIdentityListener(listener) {
          this.listeners.delete(listener);
        }
      };
      safeGlobal = typeof globalThis !== "undefined" ? globalThis : typeof global !== "undefined" ? global : self;
      AnalyticsConnector = class _AnalyticsConnector {
        constructor() {
          this.identityStore = new IdentityStoreImpl();
          this.eventBridge = new EventBridgeImpl();
          this.applicationContextProvider = new ApplicationContextProviderImpl();
        }
        static getInstance(instanceName) {
          if (!safeGlobal["analyticsConnectorInstances"]) {
            safeGlobal["analyticsConnectorInstances"] = {};
          }
          if (!safeGlobal["analyticsConnectorInstances"][instanceName]) {
            safeGlobal["analyticsConnectorInstances"][instanceName] = new _AnalyticsConnector();
          }
          return safeGlobal["analyticsConnectorInstances"][instanceName];
        }
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/analytics-connector.js
  var getAnalyticsConnector, setConnectorUserId, setConnectorDeviceId;
  var init_analytics_connector = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/analytics-connector.js"() {
      init_analytics_connector_esm();
      init_instance_name();
      getAnalyticsConnector = function(instanceName) {
        return AnalyticsConnector.getInstance(normalizeInstanceName(instanceName));
      };
      setConnectorUserId = function(userId, instanceName) {
        getAnalyticsConnector(instanceName).identityStore.editIdentity().setUserId(userId).commit();
      };
      setConnectorDeviceId = function(deviceId, instanceName) {
        getAnalyticsConnector(instanceName).identityStore.editIdentity().setDeviceId(deviceId).commit();
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/plugins/identity.js
  var IdentityEventSender;
  var init_identity = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/plugins/identity.js"() {
      init_tslib_es6();
      init_analytics_connector();
      IdentityEventSender = /** @class */
      (function() {
        function IdentityEventSender2() {
          this.name = "identity";
          this.type = "before";
          this.identityStore = getAnalyticsConnector().identityStore;
        }
        IdentityEventSender2.prototype.execute = function(context) {
          return __awaiter(this, void 0, void 0, function() {
            var userProperties;
            return __generator(this, function(_a4) {
              userProperties = context.user_properties;
              if (userProperties) {
                this.identityStore.editIdentity().updateUserProperties(userProperties).commit();
              }
              return [2, context];
            });
          });
        };
        IdentityEventSender2.prototype.setup = function(config2) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              if (config2.instanceName) {
                this.identityStore = getAnalyticsConnector(config2.instanceName).identityStore;
              }
              return [
                2
                /*return*/
              ];
            });
          });
        };
        return IdentityEventSender2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/session.js
  var isNewSession;
  var init_session = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/session.js"() {
      isNewSession = function(sessionTimeout, lastEventTime) {
        if (lastEventTime === void 0) {
          lastEventTime = Date.now();
        }
        var currentTime = Date.now();
        var timeSinceLastEvent = currentTime - lastEventTime;
        return timeSinceLastEvent > sessionTimeout;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/cookie-name.js
  var getCookieName, getOldCookieName;
  var init_cookie_name = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/cookie-name.js"() {
      init_constants();
      getCookieName = function(apiKey2, postKey, limit) {
        if (postKey === void 0) {
          postKey = "";
        }
        if (limit === void 0) {
          limit = 10;
        }
        return [AMPLITUDE_PREFIX, postKey, apiKey2.substring(0, limit)].filter(Boolean).join("_");
      };
      getOldCookieName = function(apiKey2) {
        return "".concat(AMPLITUDE_PREFIX.toLowerCase(), "_").concat(apiKey2.substring(0, 6));
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/language.js
  var getLanguage2;
  var init_language = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/language.js"() {
      getLanguage2 = function() {
        var _a4, _b, _c, _d;
        if (typeof navigator === "undefined")
          return "";
        var userLanguage = navigator.userLanguage;
        return (_d = (_c = (_b = (_a4 = navigator.languages) === null || _a4 === void 0 ? void 0 : _a4[0]) !== null && _b !== void 0 ? _b : navigator.language) !== null && _c !== void 0 ? _c : userLanguage) !== null && _d !== void 0 ? _d : "";
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/query-params.js
  var getQueryParams, tryDecodeURIComponent;
  var init_query_params = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/query-params.js"() {
      init_global_scope();
      getQueryParams = function() {
        var _a4;
        var globalScope3 = getGlobalScope();
        if (!((_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location) === null || _a4 === void 0 ? void 0 : _a4.search)) {
          return {};
        }
        var pairs = globalScope3.location.search.substring(1).split("&").filter(Boolean);
        var params = pairs.reduce(function(acc, curr) {
          var query = curr.split("=", 2);
          var key = tryDecodeURIComponent(query[0]);
          var value = tryDecodeURIComponent(query[1]);
          if (!value) {
            return acc;
          }
          acc[key] = value;
          return acc;
        }, {});
        return params;
      };
      tryDecodeURIComponent = function(value) {
        if (value === void 0) {
          value = "";
        }
        try {
          return decodeURIComponent(value);
        } catch (_a4) {
          return "";
        }
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/url-utils.js
  var isUrlMatchAllowlist, getDecodeURI;
  var init_url_utils = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/url-utils.js"() {
      isUrlMatchAllowlist = function(url, allowlist) {
        if (!allowlist || !allowlist.length) {
          return true;
        }
        return allowlist.some(function(allowedUrl) {
          if (typeof allowedUrl === "string") {
            return url === allowedUrl;
          }
          return url.match(allowedUrl);
        });
      };
      getDecodeURI = function(locationStr, loggerProvider) {
        var decodedLocationStr = locationStr;
        try {
          decodedLocationStr = decodeURI(locationStr);
        } catch (e2) {
          loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Malformed URI sequence: ", e2);
        }
        return decodedLocationStr;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/sampling.js
  var generateHashCode, isTimestampInSampleTemp;
  var init_sampling = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/sampling.js"() {
      generateHashCode = function(str) {
        var hash = 0;
        if (str.length === 0)
          return hash;
        for (var i2 = 0; i2 < str.length; i2++) {
          var chr = str.charCodeAt(i2);
          hash = (hash << 5) - hash + chr;
          hash |= 0;
        }
        return hash;
      };
      isTimestampInSampleTemp = function(timestamp, sampleRate) {
        var hashNumber = generateHashCode(timestamp.toString());
        var absHash = Math.abs(hashNumber);
        var absHashMultiply = absHash * 31;
        var mod = absHashMultiply % 1e5;
        return mod / 1e5 < sampleRate;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/omit-undefined.js
  var omitUndefined;
  var init_omit_undefined = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/omit-undefined.js"() {
      omitUndefined = function(input3) {
        var obj = {};
        for (var key in input3) {
          var val = input3[key];
          if (val) {
            obj[key] = val;
          }
        }
        return obj;
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/storage/memory.js
  var MemoryStorage;
  var init_memory = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/storage/memory.js"() {
      init_tslib_es6();
      MemoryStorage = /** @class */
      (function() {
        function MemoryStorage2() {
          this.memoryStorage = /* @__PURE__ */ new Map();
        }
        MemoryStorage2.prototype.isEnabled = function() {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              return [2, true];
            });
          });
        };
        MemoryStorage2.prototype.get = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              return [2, this.memoryStorage.get(key)];
            });
          });
        };
        MemoryStorage2.prototype.getRaw = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            var value;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  return [4, this.get(key)];
                case 1:
                  value = _a4.sent();
                  return [2, value ? JSON.stringify(value) : void 0];
              }
            });
          });
        };
        MemoryStorage2.prototype.set = function(key, value) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              this.memoryStorage.set(key, value);
              return [
                2
                /*return*/
              ];
            });
          });
        };
        MemoryStorage2.prototype.remove = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              this.memoryStorage.delete(key);
              return [
                2
                /*return*/
              ];
            });
          });
        };
        MemoryStorage2.prototype.reset = function() {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              this.memoryStorage.clear();
              return [
                2
                /*return*/
              ];
            });
          });
        };
        return MemoryStorage2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/storage/cookie.js
  var getLocks, CookieStorage, decodeCookiesAsDefault, decodeCookiesWithDoubleUrlEncoding, decodeCookieValue, isDomainEqual;
  var init_cookie = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/storage/cookie.js"() {
      init_tslib_es6();
      init_global_scope();
      getLocks = function() {
        var _a4;
        var globalScope3 = getGlobalScope();
        return (_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.navigator) === null || _a4 === void 0 ? void 0 : _a4.locks;
      };
      CookieStorage = /** @class */
      (function() {
        function CookieStorage2(options, config2) {
          if (config2 === void 0) {
            config2 = {};
          }
          this.options = __assign({}, options);
          this.config = config2;
        }
        CookieStorage2.prototype.isEnabled = function() {
          return __awaiter(this, void 0, void 0, function() {
            var testKey, testCookieOptions, testStorage, testValue;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  testKey = "AMP_TEST";
                  testCookieOptions = __assign({}, this.options);
                  testStorage = new CookieStorage2(testCookieOptions);
                  testValue = String(Date.now());
                  return [4, testStorage.transaction(testKey, function(storage) {
                    var _a5, _b;
                    try {
                      storage.set(testValue);
                      var value = storage.get();
                      var result3 = value === testValue;
                      if (!result3 && _this.config.diagnosticsClient) {
                        (_a5 = _this.config.diagnosticsClient) === null || _a5 === void 0 ? void 0 : _a5.recordEvent("cookies.isEnabled.failure", {
                          reason: "Test Value mismatch",
                          testKey,
                          testValue,
                          sync: true
                        });
                      }
                      return result3;
                    } catch (e2) {
                      if (_this.config.diagnosticsClient) {
                        var errMessage = e2 instanceof Error ? e2.message : String(e2);
                        (_b = _this.config.diagnosticsClient) === null || _b === void 0 ? void 0 : _b.recordEvent("cookies.isEnabled.failure", {
                          reason: "Cookie getter/setter failed",
                          testKey,
                          testValue,
                          error: errMessage,
                          sync: true
                        });
                      }
                      return false;
                    } finally {
                      storage.set(null);
                    }
                  })];
                case 1:
                  return [2, _a4.sent()];
              }
            });
          });
        };
        CookieStorage2.prototype.get = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            var value;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  return [4, this.getRaw(key)];
                case 1:
                  value = _a4.sent();
                  return [2, this.decodeCookieValue(key, value)];
              }
            });
          });
        };
        CookieStorage2.prototype.decodeCookieValue = function(key, value) {
          if (!value) {
            return void 0;
          }
          try {
            var decodedValue = decodeCookieValue(value);
            if (decodedValue === void 0) {
              console.error("Amplitude Logger [Error]: Failed to decode cookie value for key: ".concat(key, ", value: ").concat(value));
              return void 0;
            }
            return JSON.parse(decodedValue);
          } catch (_a4) {
            console.error("Amplitude Logger [Error]: Failed to parse cookie value for key: ".concat(key, ", value: ").concat(value));
            return void 0;
          }
        };
        CookieStorage2.prototype.getSync = function(key) {
          var value = this.getRawSync(key);
          return this.decodeCookieValue(key, value);
        };
        CookieStorage2.prototype.getRaw = function(key) {
          var _a4, _b;
          return __awaiter(this, void 0, void 0, function() {
            var globalScope3, globalScopeWithCookiesStore, cookieStore, cookies, cookies_1, cookies_1_1, cookie, ignoreError_1;
            var e_1, _c;
            return __generator(this, function(_d) {
              switch (_d.label) {
                case 0:
                  globalScope3 = getGlobalScope();
                  globalScopeWithCookiesStore = globalScope3;
                  _d.label = 1;
                case 1:
                  _d.trys.push([1, 4, , 5]);
                  cookieStore = globalScopeWithCookiesStore === null || globalScopeWithCookiesStore === void 0 ? void 0 : globalScopeWithCookiesStore.cookieStore;
                  if (!cookieStore) return [3, 3];
                  return [4, cookieStore.getAll(key)];
                case 2:
                  cookies = _d.sent();
                  if (cookies) {
                    if (cookies.length > 1) {
                      (_a4 = this.config.diagnosticsClient) === null || _a4 === void 0 ? void 0 : _a4.recordEvent("cookies.duplicate", {
                        cookies: cookies.map(function(cookie2) {
                          return cookie2.domain;
                        })
                      });
                      (_b = this.config.diagnosticsClient) === null || _b === void 0 ? void 0 : _b.increment("cookies.duplicate.occurrence.cookieStore");
                    }
                    try {
                      for (cookies_1 = __values(cookies), cookies_1_1 = cookies_1.next(); !cookies_1_1.done; cookies_1_1 = cookies_1.next()) {
                        cookie = cookies_1_1.value;
                        if (isDomainEqual(cookie.domain, this.options.domain)) {
                          return [2, cookie.value];
                        }
                      }
                    } catch (e_1_1) {
                      e_1 = { error: e_1_1 };
                    } finally {
                      try {
                        if (cookies_1_1 && !cookies_1_1.done && (_c = cookies_1.return)) _c.call(cookies_1);
                      } finally {
                        if (e_1) throw e_1.error;
                      }
                    }
                  }
                  _d.label = 3;
                case 3:
                  return [3, 5];
                case 4:
                  ignoreError_1 = _d.sent();
                  return [3, 5];
                case 5:
                  return [2, this.getRawSync(key)];
              }
            });
          });
        };
        CookieStorage2.prototype.getRawSync = function(key) {
          var _this = this;
          var _a4, _b;
          var globalScope3 = getGlobalScope();
          var cookies = ((_b = (_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.document) === null || _a4 === void 0 ? void 0 : _a4.cookie.split("; ")) !== null && _b !== void 0 ? _b : []).filter(function(c2) {
            return c2.indexOf(key + "=") === 0;
          });
          var match = void 0;
          var duplicateResolverFn = this.config.duplicateResolverFn;
          if (typeof duplicateResolverFn === "function" && cookies.length > 1) {
            match = cookies.find(function(c2) {
              var _a5;
              try {
                var res = duplicateResolverFn(c2.substring(key.length + 1));
                if (!res) {
                  (_a5 = _this.config.diagnosticsClient) === null || _a5 === void 0 ? void 0 : _a5.increment("cookies.duplicate.occurrence.document.cookie");
                }
                return res;
              } catch (ignoreError) {
                return false;
              }
            });
          }
          if (!match) {
            match = cookies[0];
          }
          if (!match) {
            return void 0;
          }
          return match.substring(key.length + 1);
        };
        CookieStorage2.prototype.set = function(key, value) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              this.setSync(key, value);
              return [
                2
                /*return*/
              ];
            });
          });
        };
        CookieStorage2.prototype.setSync = function(key, value) {
          var _a4;
          try {
            var globalScope3 = getGlobalScope();
            if (!globalScope3 || !globalScope3.document || typeof globalScope3.btoa !== "function" || typeof globalScope3.encodeURIComponent !== "function") {
              return;
            }
            var expirationDays = (_a4 = this.options.expirationDays) !== null && _a4 !== void 0 ? _a4 : 0;
            var expires = value !== null ? expirationDays : -1;
            var expireDate = void 0;
            if (expires) {
              var date = /* @__PURE__ */ new Date();
              date.setTime(date.getTime() + expires * 24 * 60 * 60 * 1e3);
              expireDate = date;
            }
            var str = "".concat(key, "=").concat(globalScope3.btoa(globalScope3.encodeURIComponent(JSON.stringify(value))));
            if (expireDate) {
              str += "; expires=".concat(expireDate.toUTCString());
            }
            str += "; path=/";
            if (this.options.domain) {
              str += "; domain=".concat(this.options.domain);
            }
            if (this.options.secure) {
              str += "; Secure";
            }
            if (this.options.sameSite) {
              str += "; SameSite=".concat(this.options.sameSite);
            }
            globalScope3.document.cookie = str;
          } catch (error) {
            var errorMessage = error instanceof Error ? error.message : String(error);
            console.error("Amplitude Logger [Error]: Failed to set cookie for key: ".concat(key, ". Error: ").concat(errorMessage));
          }
        };
        CookieStorage2.prototype.remove = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  return [4, this.set(key, null)];
                case 1:
                  _a4.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        CookieStorage2.prototype.reset = function() {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              return [
                2
                /*return*/
              ];
            });
          });
        };
        CookieStorage2.isDomainWritable = function(domain) {
          return __awaiter(this, void 0, void 0, function() {
            var options, storageKey, storage, res, error_1;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (CookieStorage2.cachedTlds[domain]) {
                    return [2, true];
                  }
                  options = {
                    domain: "." + domain
                  };
                  storageKey = "AMP_TLDTEST";
                  storage = new CookieStorage2(options);
                  _a4.label = 1;
                case 1:
                  _a4.trys.push([1, 3, , 4]);
                  return [4, storage.transaction(storageKey, function(storageSync) {
                    if (CookieStorage2.cachedTlds[domain]) {
                      return true;
                    }
                    try {
                      storageSync.set(1);
                      var result3 = !!storageSync.get();
                      if (result3) {
                        CookieStorage2.cachedTlds[domain] = true;
                      }
                      return result3;
                    } finally {
                      storageSync.set(null);
                    }
                  })];
                case 2:
                  res = _a4.sent();
                  return [2, !!res];
                case 3:
                  error_1 = _a4.sent();
                  return [2, false];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        CookieStorage2.prototype.transaction = function(key, callback) {
          return __awaiter(this, void 0, void 0, function() {
            var locks, callbackWrapper2, error_2;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  locks = getLocks();
                  callbackWrapper2 = function() {
                    var storageSync = {
                      get: function() {
                        return _this.getSync(key);
                      },
                      set: function(value) {
                        return _this.setSync(key, value);
                      }
                    };
                    return callback(storageSync);
                  };
                  if (!locks) {
                    return [2, callbackWrapper2()];
                  }
                  _a4.label = 1;
                case 1:
                  _a4.trys.push([1, 3, , 4]);
                  return [4, locks.request("com.amplitude:cookie-lock:".concat(key), callbackWrapper2)];
                case 2:
                  return [2, _a4.sent()];
                case 3:
                  error_2 = _a4.sent();
                  return [2, callbackWrapper2()];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        CookieStorage2.cachedTlds = {};
        return CookieStorage2;
      })();
      decodeCookiesAsDefault = function(value) {
        try {
          return decodeURIComponent(atob(value));
        } catch (_a4) {
          return void 0;
        }
      };
      decodeCookiesWithDoubleUrlEncoding = function(value) {
        try {
          return decodeURIComponent(atob(decodeURIComponent(value)));
        } catch (_a4) {
          return void 0;
        }
      };
      decodeCookieValue = function(value) {
        var _a4;
        return (_a4 = decodeCookiesAsDefault(value)) !== null && _a4 !== void 0 ? _a4 : decodeCookiesWithDoubleUrlEncoding(value);
      };
      isDomainEqual = function(domain1, domain2) {
        if (domain1 === "" && domain2 === "") {
          return true;
        }
        if (!domain1 || !domain2) {
          return false;
        }
        var normalized1 = domain1.startsWith(".") ? domain1.substring(1) : domain1;
        var normalized2 = domain2.startsWith(".") ? domain2.substring(1) : domain2;
        return normalized1.toLowerCase() === normalized2.toLowerCase();
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/storage/helpers.js
  var getStorageKey;
  var init_helpers = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/storage/helpers.js"() {
      init_constants();
      getStorageKey = function(apiKey2, postKey, limit) {
        if (postKey === void 0) {
          postKey = "";
        }
        if (limit === void 0) {
          limit = 10;
        }
        return [AMPLITUDE_PREFIX, postKey, apiKey2.substring(0, limit)].filter(Boolean).join("_");
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/storage/browser-storage.js
  var BrowserStorage;
  var init_browser_storage = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/storage/browser-storage.js"() {
      init_tslib_es6();
      BrowserStorage = /** @class */
      (function() {
        function BrowserStorage2(storage) {
          this.storage = storage;
        }
        BrowserStorage2.prototype.isEnabled = function() {
          return __awaiter(this, void 0, void 0, function() {
            var random, testStorage, testKey, value, _a4;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  if (!this.storage) {
                    return [2, false];
                  }
                  random = String(Date.now());
                  testStorage = new BrowserStorage2(this.storage);
                  testKey = "AMP_TEST";
                  _b.label = 1;
                case 1:
                  _b.trys.push([1, 4, 5, 7]);
                  return [4, testStorage.set(testKey, random)];
                case 2:
                  _b.sent();
                  return [4, testStorage.get(testKey)];
                case 3:
                  value = _b.sent();
                  return [2, value === random];
                case 4:
                  _a4 = _b.sent();
                  return [2, false];
                case 5:
                  return [4, testStorage.remove(testKey)];
                case 6:
                  _b.sent();
                  return [
                    7
                    /*endfinally*/
                  ];
                case 7:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        BrowserStorage2.prototype.get = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            var value, _a4;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  _b.trys.push([0, 2, , 3]);
                  return [4, this.getRaw(key)];
                case 1:
                  value = _b.sent();
                  if (!value) {
                    return [2, void 0];
                  }
                  return [2, JSON.parse(value)];
                case 2:
                  _a4 = _b.sent();
                  console.error("[Amplitude] Error: Could not get value from storage");
                  return [2, void 0];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        BrowserStorage2.prototype.getRaw = function(key) {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_b) {
              return [2, ((_a4 = this.storage) === null || _a4 === void 0 ? void 0 : _a4.getItem(key)) || void 0];
            });
          });
        };
        BrowserStorage2.prototype.set = function(key, value) {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_b) {
              try {
                (_a4 = this.storage) === null || _a4 === void 0 ? void 0 : _a4.setItem(key, JSON.stringify(value));
              } catch (_c) {
              }
              return [
                2
                /*return*/
              ];
            });
          });
        };
        BrowserStorage2.prototype.remove = function(key) {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_b) {
              try {
                (_a4 = this.storage) === null || _a4 === void 0 ? void 0 : _a4.removeItem(key);
              } catch (_c) {
              }
              return [
                2
                /*return*/
              ];
            });
          });
        };
        BrowserStorage2.prototype.reset = function() {
          var _a4;
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_b) {
              try {
                (_a4 = this.storage) === null || _a4 === void 0 ? void 0 : _a4.clear();
              } catch (_c) {
              }
              return [
                2
                /*return*/
              ];
            });
          });
        };
        return BrowserStorage2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/diagnostics/diagnostics-storage.js
  var MAX_PERSISTENT_STORAGE_EVENTS_COUNT, DB_VERSION, TABLE_NAMES, INTERNAL_KEYS, DiagnosticsStorage;
  var init_diagnostics_storage = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/diagnostics/diagnostics-storage.js"() {
      init_tslib_es6();
      init_global_scope();
      MAX_PERSISTENT_STORAGE_EVENTS_COUNT = 10;
      DB_VERSION = 1;
      TABLE_NAMES = {
        TAGS: "tags",
        COUNTERS: "counters",
        HISTOGRAMS: "histograms",
        EVENTS: "events",
        INTERNAL: "internal"
        // New table for internal storage like flush timestamps
      };
      INTERNAL_KEYS = {
        LAST_FLUSH_TIMESTAMP: "last_flush_timestamp"
      };
      DiagnosticsStorage = /** @class */
      (function() {
        function DiagnosticsStorage2(apiKey2, logger) {
          this.dbPromise = null;
          this.logger = logger;
          this.dbName = "AMP_diagnostics_".concat(apiKey2.substring(0, 10));
        }
        DiagnosticsStorage2.isSupported = function() {
          var _a4;
          return ((_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.indexedDB) !== void 0;
        };
        DiagnosticsStorage2.prototype.getDB = function() {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              if (!this.dbPromise) {
                this.dbPromise = this.openDB();
              }
              return [2, this.dbPromise];
            });
          });
        };
        DiagnosticsStorage2.prototype.openDB = function() {
          var _this = this;
          return new Promise(function(resolve, reject) {
            var request = indexedDB.open(_this.dbName, DB_VERSION);
            request.onerror = function() {
              _this.dbPromise = null;
              reject(new Error("Failed to open IndexedDB"));
            };
            request.onsuccess = function() {
              var db = request.result;
              db.onclose = function() {
                _this.dbPromise = null;
                _this.logger.debug("DiagnosticsStorage: DB connection closed.");
              };
              db.onerror = function(event) {
                _this.logger.debug("DiagnosticsStorage: A global database error occurred.", event);
                db.close();
              };
              resolve(db);
            };
            request.onupgradeneeded = function() {
              var db = request.result;
              if (!db) {
                _this.logger.debug("DiagnosticsStorage: Missing DB during upgrade.");
                return;
              }
              _this.createTables(db);
            };
          });
        };
        DiagnosticsStorage2.prototype.createTables = function(db) {
          if (!db.objectStoreNames.contains(TABLE_NAMES.TAGS)) {
            db.createObjectStore(TABLE_NAMES.TAGS, { keyPath: "key" });
          }
          if (!db.objectStoreNames.contains(TABLE_NAMES.COUNTERS)) {
            db.createObjectStore(TABLE_NAMES.COUNTERS, { keyPath: "key" });
          }
          if (!db.objectStoreNames.contains(TABLE_NAMES.HISTOGRAMS)) {
            db.createObjectStore(TABLE_NAMES.HISTOGRAMS, {
              keyPath: "key"
            });
          }
          if (!db.objectStoreNames.contains(TABLE_NAMES.EVENTS)) {
            var eventsStore = db.createObjectStore(TABLE_NAMES.EVENTS, {
              keyPath: "id",
              autoIncrement: true
            });
            eventsStore.createIndex("time_idx", "time", { unique: false });
          }
          if (!db.objectStoreNames.contains(TABLE_NAMES.INTERNAL)) {
            db.createObjectStore(TABLE_NAMES.INTERNAL, { keyPath: "key" });
          }
        };
        DiagnosticsStorage2.prototype.setTags = function(tags) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_1, store_1, error_1;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (Object.entries(tags).length === 0) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_1 = db.transaction([TABLE_NAMES.TAGS], "readwrite");
                  store_1 = transaction_1.objectStore(TABLE_NAMES.TAGS);
                  return [2, new Promise(function(resolve) {
                    var entries = Object.entries(tags);
                    transaction_1.oncomplete = function() {
                      resolve();
                    };
                    transaction_1.onabort = function(event) {
                      _this.logger.debug("DiagnosticsStorage: Failed to set tags", event);
                      resolve();
                    };
                    entries.forEach(function(_a5) {
                      var _b = __read(_a5, 2), key = _b[0], value = _b[1];
                      var putRequest = store_1.put({ key, value });
                      putRequest.onerror = function(event) {
                        _this.logger.debug("DiagnosticsStorage: Failed to set tag", key, value, event);
                      };
                    });
                  })];
                case 2:
                  error_1 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to set tags", error_1);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.incrementCounters = function(counters) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_2, store_2, error_2;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (Object.entries(counters).length === 0) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_2 = db.transaction([TABLE_NAMES.COUNTERS], "readwrite");
                  store_2 = transaction_2.objectStore(TABLE_NAMES.COUNTERS);
                  return [2, new Promise(function(resolve) {
                    var entries = Object.entries(counters);
                    transaction_2.oncomplete = function() {
                      resolve();
                    };
                    transaction_2.onabort = function(event) {
                      _this.logger.debug("DiagnosticsStorage: Failed to increment counters", event);
                      resolve();
                    };
                    entries.forEach(function(_a5) {
                      var _b = __read(_a5, 2), key = _b[0], incrementValue = _b[1];
                      var getRequest = store_2.get(key);
                      getRequest.onsuccess = function() {
                        var existingRecord = getRequest.result;
                        var existingValue = existingRecord ? existingRecord.value : 0;
                        var putRequest = store_2.put({ key, value: existingValue + incrementValue });
                        putRequest.onerror = function(event) {
                          _this.logger.debug("DiagnosticsStorage: Failed to update counter", key, event);
                        };
                      };
                      getRequest.onerror = function(event) {
                        _this.logger.debug("DiagnosticsStorage: Failed to read existing counter", key, event);
                      };
                    });
                  })];
                case 2:
                  error_2 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to increment counters", error_2);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.setHistogramStats = function(histogramStats) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_3, store_3, error_3;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (Object.entries(histogramStats).length === 0) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_3 = db.transaction([TABLE_NAMES.HISTOGRAMS], "readwrite");
                  store_3 = transaction_3.objectStore(TABLE_NAMES.HISTOGRAMS);
                  return [2, new Promise(function(resolve) {
                    var entries = Object.entries(histogramStats);
                    transaction_3.oncomplete = function() {
                      resolve();
                    };
                    transaction_3.onabort = function(event) {
                      _this.logger.debug("DiagnosticsStorage: Failed to set histogram stats", event);
                      resolve();
                    };
                    entries.forEach(function(_a5) {
                      var _b = __read(_a5, 2), key = _b[0], newStats = _b[1];
                      var getRequest = store_3.get(key);
                      getRequest.onsuccess = function() {
                        var existingRecord = getRequest.result;
                        var updatedStats;
                        if (existingRecord) {
                          updatedStats = {
                            key,
                            count: existingRecord.count + newStats.count,
                            min: Math.min(existingRecord.min, newStats.min),
                            max: Math.max(existingRecord.max, newStats.max),
                            sum: existingRecord.sum + newStats.sum
                          };
                        } else {
                          updatedStats = {
                            key,
                            count: newStats.count,
                            min: newStats.min,
                            max: newStats.max,
                            sum: newStats.sum
                          };
                        }
                        var putRequest = store_3.put(updatedStats);
                        putRequest.onerror = function(event) {
                          _this.logger.debug("DiagnosticsStorage: Failed to set histogram stats", key, event);
                        };
                      };
                      getRequest.onerror = function(event) {
                        _this.logger.debug("DiagnosticsStorage: Failed to read existing histogram stats", key, event);
                      };
                    });
                  })];
                case 2:
                  error_3 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to set histogram stats", error_3);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.addEventRecords = function(events) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_4, store_4, error_4;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (events.length === 0) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_4 = db.transaction([TABLE_NAMES.EVENTS], "readwrite");
                  store_4 = transaction_4.objectStore(TABLE_NAMES.EVENTS);
                  return [2, new Promise(function(resolve) {
                    transaction_4.oncomplete = function() {
                      resolve();
                    };
                    transaction_4.onabort = function(event) {
                      _this.logger.debug("DiagnosticsStorage: Failed to add event records", event);
                      resolve();
                    };
                    var countRequest = store_4.count();
                    countRequest.onsuccess = function() {
                      var currentCount = countRequest.result;
                      var availableSlots = Math.max(0, MAX_PERSISTENT_STORAGE_EVENTS_COUNT - currentCount);
                      if (availableSlots < events.length) {
                        _this.logger.debug("DiagnosticsStorage: Only added ".concat(availableSlots, " of ").concat(events.length, " events due to storage limit"));
                      }
                      events.slice(0, availableSlots).forEach(function(event) {
                        var request = store_4.add(event);
                        request.onerror = function(event2) {
                          _this.logger.debug("DiagnosticsStorage: Failed to add event record", event2);
                        };
                      });
                    };
                    countRequest.onerror = function(event) {
                      _this.logger.debug("DiagnosticsStorage: Failed to count existing events", event);
                    };
                  })];
                case 2:
                  error_4 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to add event records", error_4);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.setInternal = function(key, value) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_5, store_5, error_5;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_5 = db.transaction([TABLE_NAMES.INTERNAL], "readwrite");
                  store_5 = transaction_5.objectStore(TABLE_NAMES.INTERNAL);
                  return [2, new Promise(function(resolve, reject) {
                    transaction_5.onabort = function() {
                      return reject(new Error("Failed to set internal value"));
                    };
                    var request = store_5.put({ key, value });
                    request.onsuccess = function() {
                      return resolve();
                    };
                    request.onerror = function() {
                      return reject(new Error("Failed to set internal value"));
                    };
                  })];
                case 2:
                  error_5 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to set internal value", error_5);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.getInternal = function(key) {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction_6, store_6, error_6;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  return [4, this.getDB()];
                case 1:
                  db = _a4.sent();
                  transaction_6 = db.transaction([TABLE_NAMES.INTERNAL], "readonly");
                  store_6 = transaction_6.objectStore(TABLE_NAMES.INTERNAL);
                  return [2, new Promise(function(resolve, reject) {
                    transaction_6.onabort = function() {
                      return reject(new Error("Failed to get internal value"));
                    };
                    var request = store_6.get(key);
                    request.onsuccess = function() {
                      return resolve(request.result);
                    };
                    request.onerror = function() {
                      return reject(new Error("Failed to get internal value"));
                    };
                  })];
                case 2:
                  error_6 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to get internal value", error_6);
                  return [2, void 0];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.getLastFlushTimestamp = function() {
          return __awaiter(this, void 0, void 0, function() {
            var record2, error_7;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  return [4, this.getInternal(INTERNAL_KEYS.LAST_FLUSH_TIMESTAMP)];
                case 1:
                  record2 = _a4.sent();
                  return [2, record2 ? parseInt(record2.value, 10) : void 0];
                case 2:
                  error_7 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to get last flush timestamp", error_7);
                  return [2, void 0];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.setLastFlushTimestamp = function(timestamp) {
          return __awaiter(this, void 0, void 0, function() {
            var error_8;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  return [4, this.setInternal(INTERNAL_KEYS.LAST_FLUSH_TIMESTAMP, timestamp.toString())];
                case 1:
                  _a4.sent();
                  return [3, 3];
                case 2:
                  error_8 = _a4.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to set last flush timestamp", error_8);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.clearTable = function(transaction, tableName) {
          return new Promise(function(resolve, reject) {
            var store = transaction.objectStore(tableName);
            var request = store.clear();
            request.onsuccess = function() {
              return resolve();
            };
            request.onerror = function() {
              return reject(new Error("Failed to clear table ".concat(tableName)));
            };
          });
        };
        DiagnosticsStorage2.prototype.getAllAndClear = function() {
          return __awaiter(this, void 0, void 0, function() {
            var db, transaction, _a4, tags, counters, histogramStats, events, error_9;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  _b.trys.push([0, 4, , 5]);
                  return [4, this.getDB()];
                case 1:
                  db = _b.sent();
                  transaction = db.transaction([TABLE_NAMES.TAGS, TABLE_NAMES.COUNTERS, TABLE_NAMES.HISTOGRAMS, TABLE_NAMES.EVENTS], "readwrite");
                  return [4, Promise.all([
                    this.getAllFromStore(transaction, TABLE_NAMES.TAGS),
                    this.getAllFromStore(transaction, TABLE_NAMES.COUNTERS),
                    this.getAllFromStore(transaction, TABLE_NAMES.HISTOGRAMS),
                    this.getAllFromStore(transaction, TABLE_NAMES.EVENTS)
                  ])];
                case 2:
                  _a4 = __read.apply(void 0, [_b.sent(), 4]), tags = _a4[0], counters = _a4[1], histogramStats = _a4[2], events = _a4[3];
                  return [4, Promise.all([
                    this.clearTable(transaction, TABLE_NAMES.COUNTERS),
                    this.clearTable(transaction, TABLE_NAMES.HISTOGRAMS),
                    this.clearTable(transaction, TABLE_NAMES.EVENTS)
                  ])];
                case 3:
                  _b.sent();
                  return [2, { tags, counters, histogramStats, events }];
                case 4:
                  error_9 = _b.sent();
                  this.logger.debug("DiagnosticsStorage: Failed to get all and clear data", error_9);
                  return [2, { tags: [], counters: [], histogramStats: [], events: [] }];
                case 5:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsStorage2.prototype.getAllFromStore = function(transaction, tableName) {
          return new Promise(function(resolve, reject) {
            var store = transaction.objectStore(tableName);
            var request = store.getAll();
            request.onsuccess = function() {
              return resolve(request.result);
            };
            request.onerror = function() {
              return reject(new Error("Failed to get all from ".concat(tableName)));
            };
          });
        };
        return DiagnosticsStorage2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/diagnostics/uncaught-sdk-errors.js
  var GLOBAL_KEY, EVENT_NAME_ERROR_UNCAUGHT, getNormalizedScriptUrls, enableSdkErrorListeners, detectSdkOrigin, extractFilenameFromStack, stringifyReason;
  var init_uncaught_sdk_errors = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/diagnostics/uncaught-sdk-errors.js"() {
      init_tslib_es6();
      init_global_scope();
      GLOBAL_KEY = "__AMPLITUDE_SCRIPT_URL__";
      EVENT_NAME_ERROR_UNCAUGHT = "sdk.error.uncaught";
      getNormalizedScriptUrls = function() {
        var scope = getGlobalScope();
        if (!scope) {
          return [];
        }
        var value = scope[GLOBAL_KEY];
        if (Array.isArray(value)) {
          return value;
        }
        if (typeof value === "string") {
          return [value];
        }
        return [];
      };
      enableSdkErrorListeners = function(client) {
        var scope = getGlobalScope();
        if (!scope || typeof scope.addEventListener !== "function") {
          return;
        }
        var handleError = function(event) {
          var error = event.error instanceof Error ? event.error : void 0;
          var stack = error === null || error === void 0 ? void 0 : error.stack;
          var match = detectSdkOrigin({ filename: event.filename, stack });
          if (!match) {
            return;
          }
          capture({
            type: "error",
            message: event.message,
            stack,
            filename: event.filename,
            errorName: error === null || error === void 0 ? void 0 : error.name,
            metadata: {
              colno: event.colno,
              lineno: event.lineno,
              isTrusted: event.isTrusted,
              matchReason: match
            }
          });
        };
        var handleRejection = function(event) {
          var _a4;
          var error = event.reason instanceof Error ? event.reason : void 0;
          var stack = error === null || error === void 0 ? void 0 : error.stack;
          var filename = extractFilenameFromStack(stack);
          var match = detectSdkOrigin({ filename, stack });
          if (!match) {
            return;
          }
          capture({
            type: "unhandledrejection",
            message: (_a4 = error === null || error === void 0 ? void 0 : error.message) !== null && _a4 !== void 0 ? _a4 : stringifyReason(event.reason),
            stack,
            filename,
            errorName: error === null || error === void 0 ? void 0 : error.name,
            metadata: {
              isTrusted: event.isTrusted,
              matchReason: match
            }
          });
        };
        var capture = function(context) {
          client.recordEvent(EVENT_NAME_ERROR_UNCAUGHT, __assign({ type: context.type, message: context.message, filename: context.filename, error_name: context.errorName, stack: context.stack }, context.metadata));
        };
        scope.addEventListener("error", handleError, true);
        scope.addEventListener("unhandledrejection", handleRejection, true);
      };
      detectSdkOrigin = function(payload) {
        var e_1, _a4;
        var normalizedScriptUrls = getNormalizedScriptUrls();
        if (normalizedScriptUrls.length === 0) {
          return void 0;
        }
        try {
          for (var normalizedScriptUrls_1 = __values(normalizedScriptUrls), normalizedScriptUrls_1_1 = normalizedScriptUrls_1.next(); !normalizedScriptUrls_1_1.done; normalizedScriptUrls_1_1 = normalizedScriptUrls_1.next()) {
            var normalizedScriptUrl = normalizedScriptUrls_1_1.value;
            if (payload.filename && payload.filename.includes(normalizedScriptUrl)) {
              return "filename";
            }
            if (payload.stack && payload.stack.includes(normalizedScriptUrl)) {
              return "stack";
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (normalizedScriptUrls_1_1 && !normalizedScriptUrls_1_1.done && (_a4 = normalizedScriptUrls_1.return)) _a4.call(normalizedScriptUrls_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        return void 0;
      };
      extractFilenameFromStack = function(stack) {
        if (!stack) {
          return void 0;
        }
        var match = stack.match(/(https?:\/\/\S+?)(?=[)\s]|$)/);
        return match ? match[1] : void 0;
      };
      stringifyReason = function(reason) {
        if (typeof reason === "string") {
          return reason;
        }
        try {
          return JSON.stringify(reason);
        } catch (_a4) {
          return "[object Object]";
        }
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/diagnostics/diagnostics-client.js
  var SAVE_INTERVAL_MS, FLUSH_INTERVAL_MS, DIAGNOSTICS_US_SERVER_URL, DIAGNOSTICS_EU_SERVER_URL, MAX_MEMORY_STORAGE_COUNT, MAX_MEMORY_STORAGE_EVENTS_COUNT, DiagnosticsClient;
  var init_diagnostics_client = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/diagnostics/diagnostics-client.js"() {
      init_tslib_es6();
      init_diagnostics_storage();
      init_global_scope();
      init_sampling();
      init_uncaught_sdk_errors();
      SAVE_INTERVAL_MS = 1e3;
      FLUSH_INTERVAL_MS = 5 * 60 * 1e3;
      DIAGNOSTICS_US_SERVER_URL = "https://diagnostics.prod.us-west-2.amplitude.com/v1/capture";
      DIAGNOSTICS_EU_SERVER_URL = "https://diagnostics.prod.eu-central-1.amplitude.com/v1/capture";
      MAX_MEMORY_STORAGE_COUNT = 1e4;
      MAX_MEMORY_STORAGE_EVENTS_COUNT = 10;
      DiagnosticsClient = /** @class */
      (function() {
        function DiagnosticsClient2(apiKey2, logger, serverZone, options, storage) {
          if (serverZone === void 0) {
            serverZone = "US";
          }
          this.inMemoryTags = {};
          this.inMemoryCounters = {};
          this.inMemoryHistograms = {};
          this.inMemoryEvents = [];
          this.saveTimer = null;
          this.flushTimer = null;
          this.apiKey = apiKey2;
          this.logger = logger;
          this.serverUrl = serverZone === "US" ? DIAGNOSTICS_US_SERVER_URL : DIAGNOSTICS_EU_SERVER_URL;
          this.logger.debug("DiagnosticsClient: Initializing with options", JSON.stringify(options, null, 2));
          this.config = __assign({ enabled: true, sampleRate: 0 }, options);
          this.startTimestamp = Date.now();
          this.shouldTrack = isTimestampInSampleTemp(this.startTimestamp, this.config.sampleRate) && this.config.enabled;
          if (storage) {
            this.storage = storage;
          } else if (DiagnosticsStorage.isSupported()) {
            this.storage = new DiagnosticsStorage(apiKey2, logger);
          } else {
            this.logger.debug("DiagnosticsClient: IndexedDB is not supported");
          }
          void this.initializeFlushInterval();
          if (this.shouldTrack) {
            this.increment("sdk.diagnostics.sampled.in.and.enabled");
            enableSdkErrorListeners(this);
          }
        }
        DiagnosticsClient2.prototype.isStorageAndTrackEnabled = function() {
          return Boolean(this.storage) && Boolean(this.shouldTrack);
        };
        DiagnosticsClient2.prototype.setTag = function(name, value) {
          if (!this.isStorageAndTrackEnabled()) {
            return;
          }
          if (Object.keys(this.inMemoryTags).length >= MAX_MEMORY_STORAGE_COUNT) {
            this.logger.debug("DiagnosticsClient: Early return setTags as reaching memory limit");
            return;
          }
          this.inMemoryTags[name] = value;
          this.startTimersIfNeeded();
        };
        DiagnosticsClient2.prototype.increment = function(name, size) {
          if (size === void 0) {
            size = 1;
          }
          if (!this.isStorageAndTrackEnabled()) {
            return;
          }
          if (Object.keys(this.inMemoryCounters).length >= MAX_MEMORY_STORAGE_COUNT) {
            this.logger.debug("DiagnosticsClient: Early return increment as reaching memory limit");
            return;
          }
          this.inMemoryCounters[name] = (this.inMemoryCounters[name] || 0) + size;
          this.startTimersIfNeeded();
        };
        DiagnosticsClient2.prototype.recordHistogram = function(name, value) {
          if (!this.isStorageAndTrackEnabled()) {
            return;
          }
          if (Object.keys(this.inMemoryHistograms).length >= MAX_MEMORY_STORAGE_COUNT) {
            this.logger.debug("DiagnosticsClient: Early return recordHistogram as reaching memory limit");
            return;
          }
          var existing = this.inMemoryHistograms[name];
          if (existing) {
            existing.count += 1;
            existing.min = Math.min(existing.min, value);
            existing.max = Math.max(existing.max, value);
            existing.sum += value;
          } else {
            this.inMemoryHistograms[name] = {
              count: 1,
              min: value,
              max: value,
              sum: value
            };
          }
          this.startTimersIfNeeded();
        };
        DiagnosticsClient2.prototype.recordEvent = function(name, properties) {
          if (!this.isStorageAndTrackEnabled()) {
            return;
          }
          if (this.inMemoryEvents.length >= MAX_MEMORY_STORAGE_EVENTS_COUNT) {
            this.logger.debug("DiagnosticsClient: Early return recordEvent as reaching memory limit");
            return;
          }
          this.inMemoryEvents.push({
            event_name: name,
            time: Date.now(),
            event_properties: properties
          });
          this.startTimersIfNeeded();
        };
        DiagnosticsClient2.prototype.startTimersIfNeeded = function() {
          var _this = this;
          if (!this.saveTimer) {
            this.saveTimer = setTimeout(function() {
              _this.saveAllDataToStorage().catch(function(error) {
                _this.logger.debug("DiagnosticsClient: Failed to save all data to storage", error);
              }).finally(function() {
                _this.saveTimer = null;
              });
            }, SAVE_INTERVAL_MS);
          }
          if (!this.flushTimer) {
            this.flushTimer = setTimeout(function() {
              _this._flush().catch(function(error) {
                _this.logger.debug("DiagnosticsClient: Failed to flush", error);
              }).finally(function() {
                _this.flushTimer = null;
              });
            }, FLUSH_INTERVAL_MS);
          }
        };
        DiagnosticsClient2.prototype.saveAllDataToStorage = function() {
          return __awaiter(this, void 0, void 0, function() {
            var tagsToSave, countersToSave, histogramsToSave, eventsToSave;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (!this.storage) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  tagsToSave = __assign({}, this.inMemoryTags);
                  countersToSave = __assign({}, this.inMemoryCounters);
                  histogramsToSave = __assign({}, this.inMemoryHistograms);
                  eventsToSave = __spreadArray([], __read(this.inMemoryEvents), false);
                  this.inMemoryEvents = [];
                  this.inMemoryTags = {};
                  this.inMemoryCounters = {};
                  this.inMemoryHistograms = {};
                  return [4, Promise.all([
                    this.storage.setTags(tagsToSave),
                    this.storage.incrementCounters(countersToSave),
                    this.storage.setHistogramStats(histogramsToSave),
                    this.storage.addEventRecords(eventsToSave)
                  ])];
                case 1:
                  _a4.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsClient2.prototype._flush = function() {
          return __awaiter(this, void 0, void 0, function() {
            var _a4, tagRecords, counterRecords, histogramStatsRecords, eventRecords, tags, counters, histogram, events, payload;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  if (!this.storage) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  return [4, this.saveAllDataToStorage()];
                case 1:
                  _b.sent();
                  this.saveTimer = null;
                  this.flushTimer = null;
                  return [4, this.storage.getAllAndClear()];
                case 2:
                  _a4 = _b.sent(), tagRecords = _a4.tags, counterRecords = _a4.counters, histogramStatsRecords = _a4.histogramStats, eventRecords = _a4.events;
                  void this.storage.setLastFlushTimestamp(Date.now());
                  tags = {};
                  tagRecords.forEach(function(record2) {
                    tags[record2.key] = record2.value;
                  });
                  counters = {};
                  counterRecords.forEach(function(record2) {
                    counters[record2.key] = record2.value;
                  });
                  histogram = {};
                  histogramStatsRecords.forEach(function(stats) {
                    histogram[stats.key] = {
                      count: stats.count,
                      min: stats.min,
                      max: stats.max,
                      avg: Math.round(stats.sum / stats.count * 100) / 100
                      // round the average to 2 decimal places.
                    };
                  });
                  events = eventRecords.map(function(record2) {
                    return {
                      event_name: record2.event_name,
                      time: record2.time,
                      event_properties: record2.event_properties
                    };
                  });
                  if (Object.keys(counters).length === 0 && Object.keys(histogram).length === 0 && events.length === 0) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  payload = {
                    tags,
                    histogram,
                    counters,
                    events
                  };
                  void this.fetch(payload);
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsClient2.prototype.fetch = function(payload) {
          return __awaiter(this, void 0, void 0, function() {
            var response, error_1;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 2, , 3]);
                  if (!getGlobalScope()) {
                    throw new Error("DiagnosticsClient: Fetch is not supported");
                  }
                  return [4, fetch(this.serverUrl, {
                    method: "POST",
                    headers: {
                      "X-ApiKey": this.apiKey,
                      "Content-Type": "application/json"
                    },
                    body: JSON.stringify(payload)
                  })];
                case 1:
                  response = _a4.sent();
                  if (!response.ok) {
                    this.logger.debug("DiagnosticsClient: Failed to send diagnostics data.");
                    return [
                      2
                      /*return*/
                    ];
                  }
                  this.logger.debug("DiagnosticsClient: Successfully sent diagnostics data");
                  return [3, 3];
                case 2:
                  error_1 = _a4.sent();
                  this.logger.debug("DiagnosticsClient: Failed to send diagnostics data. ", error_1);
                  return [3, 3];
                case 3:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsClient2.prototype.initializeFlushInterval = function() {
          return __awaiter(this, void 0, void 0, function() {
            var now, lastFlushTimestamp, timeSinceLastFlush;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (!this.storage) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  now = Date.now();
                  return [4, this.storage.getLastFlushTimestamp()];
                case 1:
                  lastFlushTimestamp = _a4.sent() || -1;
                  if (lastFlushTimestamp === -1) {
                    void this.storage.setLastFlushTimestamp(now);
                    this._setFlushTimer(FLUSH_INTERVAL_MS);
                    return [
                      2
                      /*return*/
                    ];
                  }
                  timeSinceLastFlush = now - lastFlushTimestamp;
                  if (timeSinceLastFlush >= FLUSH_INTERVAL_MS) {
                    void this._flush();
                    return [
                      2
                      /*return*/
                    ];
                  } else {
                    this._setFlushTimer(FLUSH_INTERVAL_MS - timeSinceLastFlush);
                  }
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        DiagnosticsClient2.prototype._setFlushTimer = function(delay) {
          var _this = this;
          this.flushTimer = setTimeout(function() {
            _this._flush().catch(function(error) {
              _this.logger.debug("DiagnosticsClient: Failed to flush", error);
            }).finally(function() {
              _this.flushTimer = null;
            });
          }, delay);
        };
        DiagnosticsClient2.prototype._setSampleRate = function(sampleRate) {
          this.logger.debug("DiagnosticsClient: Setting sample rate to", sampleRate);
          this.config.sampleRate = sampleRate;
          this.shouldTrack = isTimestampInSampleTemp(this.startTimestamp, this.config.sampleRate) && this.config.enabled;
          this.logger.debug("DiagnosticsClient: Should track is", this.shouldTrack);
        };
        return DiagnosticsClient2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/transports/base.js
  var BaseTransport;
  var init_base = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/transports/base.js"() {
      init_status();
      init_status_code();
      BaseTransport = /** @class */
      (function() {
        function BaseTransport2() {
        }
        BaseTransport2.prototype.send = function(_serverUrl, _payload, _enableRequestBodyCompression) {
          return Promise.resolve(null);
        };
        BaseTransport2.prototype.buildResponse = function(responseJSON) {
          var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x;
          if (typeof responseJSON !== "object") {
            return null;
          }
          var statusCode = responseJSON.code || 0;
          var status = this.buildStatus(statusCode);
          switch (status) {
            case Status.Success:
              return {
                status,
                statusCode,
                body: {
                  eventsIngested: (_a4 = responseJSON.events_ingested) !== null && _a4 !== void 0 ? _a4 : 0,
                  payloadSizeBytes: (_b = responseJSON.payload_size_bytes) !== null && _b !== void 0 ? _b : 0,
                  serverUploadTime: (_c = responseJSON.server_upload_time) !== null && _c !== void 0 ? _c : 0
                }
              };
            case Status.Invalid:
              return {
                status,
                statusCode,
                body: {
                  error: (_d = responseJSON.error) !== null && _d !== void 0 ? _d : "",
                  missingField: (_e = responseJSON.missing_field) !== null && _e !== void 0 ? _e : "",
                  eventsWithInvalidFields: (_f = responseJSON.events_with_invalid_fields) !== null && _f !== void 0 ? _f : {},
                  eventsWithMissingFields: (_g = responseJSON.events_with_missing_fields) !== null && _g !== void 0 ? _g : {},
                  eventsWithInvalidIdLengths: (_h = responseJSON.events_with_invalid_id_lengths) !== null && _h !== void 0 ? _h : {},
                  epsThreshold: (_j = responseJSON.eps_threshold) !== null && _j !== void 0 ? _j : 0,
                  exceededDailyQuotaDevices: (_k = responseJSON.exceeded_daily_quota_devices) !== null && _k !== void 0 ? _k : {},
                  silencedDevices: (_l = responseJSON.silenced_devices) !== null && _l !== void 0 ? _l : [],
                  silencedEvents: (_m = responseJSON.silenced_events) !== null && _m !== void 0 ? _m : [],
                  throttledDevices: (_o = responseJSON.throttled_devices) !== null && _o !== void 0 ? _o : {},
                  throttledEvents: (_p = responseJSON.throttled_events) !== null && _p !== void 0 ? _p : []
                }
              };
            case Status.PayloadTooLarge:
              return {
                status,
                statusCode,
                body: {
                  error: (_q = responseJSON.error) !== null && _q !== void 0 ? _q : ""
                }
              };
            case Status.RateLimit:
              return {
                status,
                statusCode,
                body: {
                  error: (_r = responseJSON.error) !== null && _r !== void 0 ? _r : "",
                  epsThreshold: (_s = responseJSON.eps_threshold) !== null && _s !== void 0 ? _s : 0,
                  throttledDevices: (_t = responseJSON.throttled_devices) !== null && _t !== void 0 ? _t : {},
                  throttledUsers: (_u = responseJSON.throttled_users) !== null && _u !== void 0 ? _u : {},
                  exceededDailyQuotaDevices: (_v = responseJSON.exceeded_daily_quota_devices) !== null && _v !== void 0 ? _v : {},
                  exceededDailyQuotaUsers: (_w = responseJSON.exceeded_daily_quota_users) !== null && _w !== void 0 ? _w : {},
                  throttledEvents: (_x = responseJSON.throttled_events) !== null && _x !== void 0 ? _x : []
                }
              };
            case Status.Timeout:
            default:
              return {
                status,
                statusCode
              };
          }
        };
        BaseTransport2.prototype.buildStatus = function(code) {
          if (isSuccessStatusCode(code)) {
            return Status.Success;
          }
          if (code === 429) {
            return Status.RateLimit;
          }
          if (code === 413) {
            return Status.PayloadTooLarge;
          }
          if (code === 408) {
            return Status.Timeout;
          }
          if (code >= 400 && code < 500) {
            return Status.Invalid;
          }
          if (code >= 500) {
            return Status.Failed;
          }
          return Status.Unknown;
        };
        return BaseTransport2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/transports/fetch.js
  var FetchTransport;
  var init_fetch = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/transports/fetch.js"() {
      init_tslib_es6();
      init_base();
      FetchTransport = /** @class */
      (function(_super) {
        __extends(FetchTransport3, _super);
        function FetchTransport3(customHeaders) {
          if (customHeaders === void 0) {
            customHeaders = {};
          }
          var _this = _super.call(this) || this;
          _this.customHeaders = customHeaders;
          return _this;
        }
        FetchTransport3.prototype.send = function(serverUrl, payload) {
          return __awaiter(this, void 0, void 0, function() {
            var options, response, responseText;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (typeof fetch === "undefined") {
                    throw new Error("FetchTransport is not supported");
                  }
                  options = {
                    headers: __assign({ "Content-Type": "application/json", Accept: "*/*" }, this.customHeaders),
                    body: JSON.stringify(payload),
                    method: "POST"
                  };
                  return [4, fetch(serverUrl, options)];
                case 1:
                  response = _a4.sent();
                  return [4, response.text()];
                case 2:
                  responseText = _a4.sent();
                  try {
                    return [2, this.buildResponse(JSON.parse(responseText))];
                  } catch (_b) {
                    return [2, this.buildResponse({ code: response.status })];
                  }
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        return FetchTransport3;
      })(BaseTransport);
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/transports/gzip.js
  function isCompressionStreamAvailable() {
    return typeof CompressionStream !== "undefined";
  }
  function compressToGzipArrayBuffer(data) {
    return __awaiter(this, void 0, void 0, function() {
      var CompressionStreamImpl, stream, _a4;
      return __generator(this, function(_b) {
        switch (_b.label) {
          case 0:
            CompressionStreamImpl = CompressionStream;
            if (typeof CompressionStreamImpl === "undefined") {
              return [2, void 0];
            }
            _b.label = 1;
          case 1:
            _b.trys.push([1, 3, , 4]);
            stream = new Blob([data]).stream().pipeThrough(new CompressionStreamImpl("gzip"));
            return [4, new Response(stream).arrayBuffer()];
          case 2:
            return [2, _b.sent()];
          case 3:
            _a4 = _b.sent();
            return [2, void 0];
          case 4:
            return [
              2
              /*return*/
            ];
        }
      });
    });
  }
  var MIN_GZIP_UPLOAD_BODY_SIZE_BYTES;
  var init_gzip = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/transports/gzip.js"() {
      init_tslib_es6();
      MIN_GZIP_UPLOAD_BODY_SIZE_BYTES = 2 * 1024;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/remote-config/remote-config-localstorage.js
  var RemoteConfigLocalStorage;
  var init_remote_config_localstorage = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/remote-config/remote-config-localstorage.js"() {
      RemoteConfigLocalStorage = /** @class */
      (function() {
        function RemoteConfigLocalStorage2(apiKey2, logger) {
          this.key = "AMP_remote_config_".concat(apiKey2.substring(0, 10));
          this.logger = logger;
        }
        RemoteConfigLocalStorage2.prototype.fetchConfig = function() {
          var result3 = null;
          var failedRemoteConfigInfo = {
            remoteConfig: null,
            lastFetch: /* @__PURE__ */ new Date()
          };
          try {
            result3 = localStorage.getItem(this.key);
          } catch (error) {
            this.logger.debug("Remote config localstorage failed to access: ", error);
            return Promise.resolve(failedRemoteConfigInfo);
          }
          if (result3 === null) {
            this.logger.debug("Remote config localstorage gets null because the key does not exist");
            return Promise.resolve(failedRemoteConfigInfo);
          }
          try {
            var remoteConfigInfo = JSON.parse(result3);
            this.logger.debug("Remote config localstorage parsed successfully: ".concat(JSON.stringify(remoteConfigInfo)));
            return Promise.resolve({
              remoteConfig: remoteConfigInfo.remoteConfig,
              lastFetch: new Date(remoteConfigInfo.lastFetch)
            });
          } catch (error) {
            this.logger.debug("Remote config localstorage failed to parse: ", error);
            localStorage.removeItem(this.key);
            return Promise.resolve(failedRemoteConfigInfo);
          }
        };
        RemoteConfigLocalStorage2.prototype.setConfig = function(config2) {
          try {
            localStorage.setItem(this.key, JSON.stringify(config2));
            this.logger.debug("Remote config localstorage set successfully.");
            return Promise.resolve(true);
          } catch (error) {
            this.logger.debug("Remote config localstorage failed to set: ", error);
          }
          return Promise.resolve(false);
        };
        return RemoteConfigLocalStorage2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/remote-config/remote-config.js
  function abortableFetch(promise, signal) {
    return new Promise(function(resolve, reject) {
      var onAbort = function() {
        var err = new Error("Remote config custom fetch aborted by timeout");
        err.name = "AbortError";
        reject(err);
      };
      signal.addEventListener("abort", onAbort, { once: true });
      promise.then(function(value) {
        signal.removeEventListener("abort", onAbort);
        resolve(value);
      }, function(err) {
        signal.removeEventListener("abort", onAbort);
        reject(err);
      });
    });
  }
  var US_SERVER_URL, EU_SERVER_URL, DEFAULT_MAX_RETRIES, CODE_STATUS, DEFAULT_TIMEOUT, DEFAULT_MIN_TIME_BETWEEN_FETCHES, RemoteConfigClient;
  var init_remote_config = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/remote-config/remote-config.js"() {
      init_tslib_es6();
      init_remote_config_localstorage();
      init_uuid();
      US_SERVER_URL = "https://sr-client-cfg.amplitude.com/config";
      EU_SERVER_URL = "https://sr-client-cfg.eu.amplitude.com/config";
      DEFAULT_MAX_RETRIES = 3;
      CODE_STATUS = {
        INVALID_API_KEY: 401,
        FORBIDDEN: 403,
        RATE_LIMIT: 429
      };
      DEFAULT_TIMEOUT = 1e3;
      DEFAULT_MIN_TIME_BETWEEN_FETCHES = 5 * 60 * 1e3;
      RemoteConfigClient = /** @class */
      (function() {
        function RemoteConfigClient2(apiKey2, logger, serverZone, serverUrl, customFetch, configGroup) {
          if (serverZone === void 0) {
            serverZone = "US";
          }
          if (configGroup === void 0) {
            configGroup = RemoteConfigClient2.CONFIG_GROUP;
          }
          this.callbackInfos = [];
          this.lastSuccessfulFetch = null;
          this.fetchPromise = null;
          this.isLastFetchInvalidApiKey = false;
          this.apiKey = apiKey2;
          this.serverUrl = serverUrl || (serverZone === "US" ? US_SERVER_URL : EU_SERVER_URL);
          this.logger = logger;
          this.storage = new RemoteConfigLocalStorage(apiKey2, logger);
          this.customFetch = customFetch;
          this.configGroup = configGroup;
        }
        RemoteConfigClient2.prototype.subscribe = function(key, deliveryMode, callback) {
          var id2 = UUID();
          var callbackInfo = {
            id: id2,
            key,
            deliveryMode,
            callback
          };
          this.callbackInfos.push(callbackInfo);
          if (deliveryMode === "all") {
            void this.subscribeAll(callbackInfo);
          } else {
            void this.subscribeWaitForRemote(callbackInfo, deliveryMode.timeout);
          }
          return id2;
        };
        RemoteConfigClient2.prototype.unsubscribe = function(id2) {
          var index4 = this.callbackInfos.findIndex(function(callbackInfo) {
            return callbackInfo.id === id2;
          });
          if (index4 === -1) {
            this.logger.debug("Remote config client unsubscribe failed because callback with id ".concat(id2, " doesn't exist."));
            return false;
          }
          this.callbackInfos.splice(index4, 1);
          this.logger.debug("Remote config client unsubscribe succeeded removing callback with id ".concat(id2, "."));
          return true;
        };
        RemoteConfigClient2.prototype.updateConfigs = function() {
          return __awaiter(this, void 0, void 0, function() {
            var timeSinceLastFetch, result3;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (this.lastSuccessfulFetch) {
                    timeSinceLastFetch = Date.now() - this.lastSuccessfulFetch;
                    if (timeSinceLastFetch < DEFAULT_MIN_TIME_BETWEEN_FETCHES) {
                      this.logger.debug("Remote config client skipping updateConfigs: Too recent");
                      return [
                        2
                        /*return*/
                      ];
                    }
                  }
                  return [4, this.getOrCreateFetchPromise()];
                case 1:
                  result3 = _a4.sent();
                  void this.storage.setConfig(result3);
                  this.callbackInfos.forEach(function(callbackInfo) {
                    _this.sendCallback(callbackInfo, result3, "remote");
                  });
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        RemoteConfigClient2.prototype.getOrCreateFetchPromise = function() {
          var _this = this;
          if (this.fetchPromise) {
            return this.fetchPromise;
          }
          if (this.isLastFetchInvalidApiKey) {
            this.logger.debug("Remote config client skipping fetch: Invalid API key");
            this.fetchPromise = Promise.resolve({
              remoteConfig: null,
              lastFetch: /* @__PURE__ */ new Date()
            }).finally(function() {
              _this.fetchPromise = null;
            });
            return this.fetchPromise;
          }
          this.fetchPromise = this.fetch().then(function(result3) {
            if (result3.remoteConfig !== null) {
              _this.lastSuccessfulFetch = Date.now();
            }
            return result3;
          }).finally(function() {
            _this.fetchPromise = null;
          });
          return this.fetchPromise;
        };
        RemoteConfigClient2.prototype.subscribeAll = function(callbackInfo) {
          return __awaiter(this, void 0, void 0, function() {
            var remotePromise, cachePromise, result3;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  remotePromise = this.getOrCreateFetchPromise().then(function(result4) {
                    _this.logger.debug("Remote config client subscription all mode fetched from remote: ".concat(JSON.stringify(result4)));
                    _this.sendCallback(callbackInfo, result4, "remote");
                    void _this.storage.setConfig(result4);
                  });
                  cachePromise = this.storage.fetchConfig().then(function(result4) {
                    return result4;
                  });
                  return [4, Promise.race([remotePromise, cachePromise])];
                case 1:
                  result3 = _a4.sent();
                  if (result3 !== void 0) {
                    this.logger.debug("Remote config client subscription all mode fetched from cache: ".concat(JSON.stringify(result3)));
                    if (result3.remoteConfig !== null) {
                      this.sendCallback(callbackInfo, result3, "cache");
                    } else {
                      this.logger.debug("Remote config client skips sending callback because cache is empty (first time user).");
                    }
                  }
                  return [4, remotePromise];
                case 2:
                  _a4.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        RemoteConfigClient2.prototype.subscribeWaitForRemote = function(callbackInfo, timeout) {
          return __awaiter(this, void 0, void 0, function() {
            var timeoutPromise, result3, error_1, result3;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  timeoutPromise = new Promise(function(_2, reject) {
                    setTimeout(function() {
                      reject("Timeout exceeded");
                    }, timeout);
                  });
                  _a4.label = 1;
                case 1:
                  _a4.trys.push([1, 3, , 5]);
                  return [4, Promise.race([
                    this.getOrCreateFetchPromise(),
                    timeoutPromise
                  ])];
                case 2:
                  result3 = _a4.sent();
                  this.logger.debug("Remote config client subscription wait for remote mode returns from remote.");
                  this.sendCallback(callbackInfo, result3, "remote");
                  void this.storage.setConfig(result3);
                  return [3, 5];
                case 3:
                  error_1 = _a4.sent();
                  this.logger.debug("Remote config client subscription wait for remote mode exceeded timeout. Try to fetch from cache.");
                  return [4, this.storage.fetchConfig()];
                case 4:
                  result3 = _a4.sent();
                  if (result3.remoteConfig !== null) {
                    this.logger.debug("Remote config client subscription wait for remote mode returns a cached copy.");
                    this.sendCallback(callbackInfo, result3, "cache");
                  } else {
                    this.logger.debug("Remote config client subscription wait for remote mode failed to fetch cache.");
                    this.sendCallback(callbackInfo, result3, "remote");
                  }
                  return [3, 5];
                case 5:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        RemoteConfigClient2.prototype.sendCallback = function(callbackInfo, remoteConfigInfo, source) {
          callbackInfo.lastCallback = /* @__PURE__ */ new Date();
          var filteredConfig;
          if (callbackInfo.key) {
            filteredConfig = callbackInfo.key.split(".").reduce(function(config2, key) {
              if (config2 === null) {
                return config2;
              }
              return key in config2 ? config2[key] : null;
            }, remoteConfigInfo.remoteConfig);
          } else {
            filteredConfig = remoteConfigInfo.remoteConfig;
          }
          callbackInfo.callback(filteredConfig, source, remoteConfigInfo.lastFetch);
        };
        RemoteConfigClient2.prototype.fetch = function(retries, timeout) {
          if (retries === void 0) {
            retries = DEFAULT_MAX_RETRIES;
          }
          if (timeout === void 0) {
            timeout = DEFAULT_TIMEOUT;
          }
          return __awaiter(this, void 0, void 0, function() {
            var interval, failedRemoteConfigInfo, _loop_1, this_1, attempt, state_1;
            var _this = this;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  interval = timeout / retries;
                  failedRemoteConfigInfo = {
                    remoteConfig: null,
                    lastFetch: /* @__PURE__ */ new Date()
                  };
                  _loop_1 = function(attempt2) {
                    var shouldRetry, abortController, timeoutId, url, headers, res, _b, isSuccess, body, remoteConfig, error_2;
                    return __generator(this, function(_c) {
                      switch (_c.label) {
                        case 0:
                          shouldRetry = true;
                          abortController = new AbortController();
                          timeoutId = setTimeout(function() {
                            return abortController.abort();
                          }, timeout);
                          _c.label = 1;
                        case 1:
                          _c.trys.push([1, 10, 11, 12]);
                          url = this_1.getUrlParams();
                          headers = { Accept: "*/*" };
                          if (!this_1.customFetch) return [3, 3];
                          return [4, abortableFetch(this_1.customFetch({ url, method: "GET", headers, signal: abortController.signal }), abortController.signal)];
                        case 2:
                          _b = _c.sent();
                          return [3, 5];
                        case 3:
                          return [4, fetch(url, {
                            method: "GET",
                            headers,
                            signal: abortController.signal
                          })];
                        case 4:
                          _b = _c.sent();
                          _c.label = 5;
                        case 5:
                          res = _b;
                          isSuccess = res.ok || res.status >= 200 && res.status < 300;
                          if (!!isSuccess) return [3, 7];
                          return [4, res.text()];
                        case 6:
                          body = _c.sent();
                          this_1.logger.debug("Remote config client fetch with retry time ".concat(retries, " failed with ").concat(res.status, ": ").concat(body));
                          if (res.status === CODE_STATUS.INVALID_API_KEY || res.status === CODE_STATUS.FORBIDDEN) {
                            this_1.logger.error("Remote config client fetch failed with ".concat(res.status, ". Invalid API key; future fetches will be skipped."));
                            this_1.isLastFetchInvalidApiKey = true;
                            shouldRetry = false;
                          } else if (res.status >= 400 && res.status < 500 && res.status !== CODE_STATUS.RATE_LIMIT) {
                            shouldRetry = false;
                          }
                          return [3, 9];
                        case 7:
                          return [4, res.json()];
                        case 8:
                          remoteConfig = _c.sent();
                          return [2, { value: {
                            remoteConfig,
                            lastFetch: /* @__PURE__ */ new Date()
                          } }];
                        case 9:
                          return [3, 12];
                        case 10:
                          error_2 = _c.sent();
                          if (error_2 instanceof Error && error_2.name === "AbortError") {
                            this_1.logger.debug("Remote config client fetch with retry time ".concat(retries, " timed out after ").concat(timeout, "ms"));
                          } else {
                            this_1.logger.debug("Remote config client fetch with retry time ".concat(retries, " is rejected because: "), error_2);
                          }
                          return [3, 12];
                        case 11:
                          clearTimeout(timeoutId);
                          return [
                            7
                            /*endfinally*/
                          ];
                        case 12:
                          if (!shouldRetry) {
                            return [2, "break"];
                          }
                          if (!(attempt2 < retries - 1)) return [3, 14];
                          return [4, new Promise(function(resolve) {
                            return setTimeout(resolve, _this.getJitterDelay(interval));
                          })];
                        case 13:
                          _c.sent();
                          _c.label = 14;
                        case 14:
                          return [
                            2
                            /*return*/
                          ];
                      }
                    });
                  };
                  this_1 = this;
                  attempt = 0;
                  _a4.label = 1;
                case 1:
                  if (!(attempt < retries)) return [3, 4];
                  return [5, _loop_1(attempt)];
                case 2:
                  state_1 = _a4.sent();
                  if (typeof state_1 === "object")
                    return [2, state_1.value];
                  if (state_1 === "break")
                    return [3, 4];
                  _a4.label = 3;
                case 3:
                  attempt++;
                  return [3, 1];
                case 4:
                  return [2, failedRemoteConfigInfo];
              }
            });
          });
        };
        RemoteConfigClient2.prototype.getJitterDelay = function(baseDelay) {
          return Math.floor(Math.random() * baseDelay);
        };
        RemoteConfigClient2.prototype.getUrlParams = function() {
          var encodedApiKey = encodeURIComponent(this.apiKey);
          var urlParams = new URLSearchParams();
          urlParams.append("config_group", this.configGroup);
          return "".concat(this.serverUrl, "/").concat(encodedApiKey, "?").concat(urlParams.toString());
        };
        RemoteConfigClient2.CONFIG_GROUP = "browser";
        return RemoteConfigClient2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/observers/console.js
  function overrideConsole(logLevel) {
    if (!originalConsole) {
      return false;
    }
    if (typeof originalConsole[logLevel] !== "function") {
      return false;
    }
    if (originalFn[logLevel]) {
      return true;
    }
    var handler = function() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      try {
        if (handlers[logLevel] && !inConsoleOverride) {
          inConsoleOverride = true;
          var callbacks = handlers[logLevel];
          if (callbacks) {
            callbacks.forEach(function(callback) {
              try {
                callback(logLevel, args);
              } catch (_a4) {
              }
            });
          }
        }
      } catch (_a4) {
      }
      inConsoleOverride = false;
      return originalFn[logLevel].apply(originalConsole, args);
    };
    originalFn[logLevel] = originalConsole[logLevel];
    originalConsole[logLevel] = handler;
    return true;
  }
  function addListener(level, callback) {
    var res = overrideConsole(level);
    if (!res) {
      return new Error("Console override failed");
    }
    if (handlers[level]) {
      handlers[level].push(callback);
    } else {
      handlers[level] = [callback];
    }
  }
  function removeListener(callback) {
    var e_1, _a4;
    try {
      for (var _b = __values(Object.values(handlers)), _c = _b.next(); !_c.done; _c = _b.next()) {
        var callbacks = _c.value;
        for (var i2 = callbacks.length - 1; i2 >= 0; i2--) {
          if (callbacks[i2] === callback) {
            callbacks.splice(i2, 1);
            break;
          }
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
  }
  function _restoreConsole() {
    var e_2, _a4;
    try {
      for (var _b = __values(Object.entries(originalFn)), _c = _b.next(); !_c.done; _c = _b.next()) {
        var _d = __read(_c.value, 2), key = _d[0], originalHandler = _d[1];
        if (originalHandler) {
          originalConsole[key] = originalHandler;
        }
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    originalFn = {};
    handlers = {};
  }
  var globalScope, originalConsole, handlers, originalFn, inConsoleOverride, consoleObserver;
  var init_console = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/observers/console.js"() {
      init_tslib_es6();
      init_global_scope();
      globalScope = getGlobalScope();
      originalConsole = globalScope === null || globalScope === void 0 ? void 0 : globalScope.console;
      handlers = {};
      originalFn = {};
      inConsoleOverride = false;
      consoleObserver = {
        addListener,
        removeListener,
        _restoreConsole
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/server-zone.js
  var ServerZone;
  var init_server_zone = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/server-zone.js"() {
      (function(ServerZone2) {
        ServerZone2["US"] = "US";
        ServerZone2["EU"] = "EU";
        ServerZone2["STAGING"] = "STAGING";
      })(ServerZone || (ServerZone = {}));
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/offline.js
  var OfflineDisabled;
  var init_offline = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/offline.js"() {
      OfflineDisabled = null;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/element-interactions.js
  var DEFAULT_CSS_SELECTOR_ALLOWLIST, DEFAULT_DATA_ATTRIBUTE_PREFIX, DEFAULT_ACTION_CLICK_ALLOWLIST, DEFAULT_EXPOSURE_DURATION;
  var init_element_interactions = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/element-interactions.js"() {
      DEFAULT_CSS_SELECTOR_ALLOWLIST = [
        "a",
        "button",
        "input",
        "select",
        "textarea",
        "label",
        "video",
        "audio",
        '[contenteditable="true" i]',
        "[data-amp-default-track]",
        ".amp-default-track"
      ];
      DEFAULT_DATA_ATTRIBUTE_PREFIX = "data-amp-track-";
      DEFAULT_ACTION_CLICK_ALLOWLIST = ["div", "span", "h1", "h2", "h3", "h4", "h5", "h6"];
      DEFAULT_EXPOSURE_DURATION = 150;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/frustration-interactions.js
  var CLICKABLE_ELEMENT_SELECTORS, DEFAULT_ERROR_AND_DEAD_CLICK_ALLOWLIST, DEFAULT_DEAD_CLICK_ALLOWLIST, DEFAULT_ERROR_CLICK_ALLOWLIST, DEFAULT_RAGE_CLICK_ALLOWLIST, DEFAULT_RAGE_CLICK_WINDOW_MS, DEFAULT_RAGE_CLICK_THRESHOLD, DEFAULT_RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD;
  var init_frustration_interactions = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/frustration-interactions.js"() {
      init_tslib_es6();
      CLICKABLE_ELEMENT_SELECTORS = [
        "a",
        "button",
        '[role="button"]',
        '[role="link"]',
        '[role="menuitem"]',
        '[role="menuitemcheckbox"]',
        '[role="menuitemradio"]',
        '[role="option"]',
        '[role="tab"]',
        '[role="treeitem"]',
        '[contenteditable="true" i]'
      ];
      DEFAULT_ERROR_AND_DEAD_CLICK_ALLOWLIST = __spreadArray([
        'input[type="button"]',
        'input[type="submit"]',
        'input[type="reset"]',
        'input[type="image"]',
        'input[type="file"]'
      ], __read(CLICKABLE_ELEMENT_SELECTORS), false);
      DEFAULT_DEAD_CLICK_ALLOWLIST = DEFAULT_ERROR_AND_DEAD_CLICK_ALLOWLIST;
      DEFAULT_ERROR_CLICK_ALLOWLIST = DEFAULT_ERROR_AND_DEAD_CLICK_ALLOWLIST;
      DEFAULT_RAGE_CLICK_ALLOWLIST = ["*"];
      DEFAULT_RAGE_CLICK_WINDOW_MS = 1e3;
      DEFAULT_RAGE_CLICK_THRESHOLD = 4;
      DEFAULT_RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD = 50;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/json-query.js
  function isJsonPrimitive(json) {
    return typeof json === "string" || typeof json === "number" || typeof json === "boolean" || json === null || json === void 0;
  }
  function pruneJson(json, allowlist, excludelist) {
    if (!json)
      return;
    var allowlistTokens = allowlist.map(tokenizeJsonPath);
    var excludelistTokens = excludelist.map(tokenizeJsonPath);
    _pruneJson({
      json,
      allowlist: allowlistTokens,
      excludelist: excludelistTokens,
      ancestors: []
    });
  }
  function _pruneJson(_a4) {
    var e_1, _b;
    var json = _a4.json, targetObject = _a4.targetObject, allowlist = _a4.allowlist, excludelist = _a4.excludelist, ancestors = _a4.ancestors, parentObject = _a4.parentObject, targetKey = _a4.targetKey;
    if (!targetObject) {
      targetObject = json;
    }
    var keys = Object.keys(targetObject);
    try {
      for (var keys_1 = __values(keys), keys_1_1 = keys_1.next(); !keys_1_1.done; keys_1_1 = keys_1.next()) {
        var key = keys_1_1.value;
        var path = __spreadArray(__spreadArray([], __read(ancestors), false), [key], false);
        if (isJsonPrimitive(targetObject[key])) {
          if (!hasPathMatchInList(path, allowlist) || hasPathMatchInList(path, excludelist)) {
            delete targetObject[key];
          }
        } else {
          _pruneJson({
            json,
            targetObject: targetObject[key],
            allowlist,
            excludelist,
            ancestors: path,
            parentObject: targetObject,
            targetKey: key
          });
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (keys_1_1 && !keys_1_1.done && (_b = keys_1.return)) _b.call(keys_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    if (Object.keys(targetObject).length === 0 && parentObject && targetKey) {
      delete parentObject[targetKey];
    }
  }
  function tokenizeJsonPath(path) {
    if (path.startsWith("/")) {
      path = path.slice(1);
    }
    return path.split("/").map(function(token) {
      return token.replace(/~0/g, "~").replace(/~1/g, "/");
    });
  }
  function isPathMatch(path, pathMatcher, i2, j) {
    if (i2 === void 0) {
      i2 = 0;
    }
    if (j === void 0) {
      j = 0;
    }
    if (j === pathMatcher.length) {
      return i2 === path.length;
    }
    if (i2 === path.length) {
      while (j < pathMatcher.length && pathMatcher[j] === "**") {
        j++;
      }
      return j === pathMatcher.length;
    }
    var currentMatcher = pathMatcher[j];
    if (currentMatcher === "**") {
      if (j + 1 === pathMatcher.length) {
        return true;
      }
      for (var k2 = i2; k2 <= path.length; k2++) {
        if (isPathMatch(path, pathMatcher, k2, j + 1)) {
          return true;
        }
      }
      return false;
    } else if (currentMatcher === "*" || currentMatcher === path[i2]) {
      return isPathMatch(path, pathMatcher, i2 + 1, j + 1);
    } else {
      return false;
    }
  }
  function hasPathMatchInList(path, allowOrExcludeList) {
    return allowOrExcludeList.some(function(l2) {
      return isPathMatch(path, l2);
    });
  }
  var init_json_query = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/json-query.js"() {
      init_tslib_es6();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/network-request-event.js
  function getBodySize(bodyUnsafe, maxEntries) {
    var e_2, _a4;
    var bodySize;
    var global2 = getGlobalScope();
    var TextEncoder2 = global2 === null || global2 === void 0 ? void 0 : global2.TextEncoder;
    if (!TextEncoder2) {
      return;
    }
    if (bodyUnsafe == null) {
      return;
    }
    var bodySafe;
    if (typeof bodyUnsafe === "string") {
      bodySafe = bodyUnsafe;
      bodySize = new TextEncoder2().encode(bodySafe).length;
    } else if (typeof Blob !== "undefined" && bodyUnsafe instanceof Blob) {
      bodySafe = bodyUnsafe;
      bodySize = bodySafe.size;
    } else if (typeof URLSearchParams !== "undefined" && bodyUnsafe instanceof URLSearchParams) {
      bodySafe = bodyUnsafe;
      bodySize = new TextEncoder2().encode(bodySafe.toString()).length;
    } else if (ArrayBuffer.isView(bodyUnsafe)) {
      bodySafe = bodyUnsafe;
      bodySize = bodySafe.byteLength;
    } else if (bodyUnsafe instanceof ArrayBuffer) {
      bodySafe = bodyUnsafe;
      bodySize = bodySafe.byteLength;
    } else if (typeof FormData !== "undefined" && bodyUnsafe instanceof FormData) {
      var formData = bodyUnsafe;
      var total = 0;
      var count = 0;
      try {
        for (var _b = __values(formData.entries()), _c = _b.next(); !_c.done; _c = _b.next()) {
          var _d = __read(_c.value, 2), key = _d[0], value = _d[1];
          total += key.length;
          if (typeof value === "string") {
            total += new TextEncoder2().encode(value).length;
          } else if (typeof Blob !== "undefined" && value instanceof Blob) {
            total += value.size;
          } else {
            return;
          }
          if (++count >= maxEntries) {
            return;
          }
        }
      } catch (e_2_1) {
        e_2 = { error: e_2_1 };
      } finally {
        try {
          if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
        } finally {
          if (e_2) throw e_2.error;
        }
      }
      bodySize = total;
    } else if (typeof ReadableStream !== "undefined" && bodyUnsafe instanceof ReadableStream) {
      bodySafe = bodyUnsafe;
      return;
    }
    return bodySize;
  }
  function safeParseAndPruneBody(text, allow, exclude) {
    if (!text)
      return null;
    try {
      var json = JSON.parse(text);
      pruneJson(json, allow, exclude);
      return json;
    } catch (error) {
      return null;
    }
  }
  var TEXT_READ_TIMEOUT, MAXIMUM_ENTRIES, RequestWrapperFetch, RequestWrapperXhr, ResponseWrapperFetch, ResponseWrapperXhr, PRUNE_STRATEGY, REDACTED_VALUE, pruneHeaders, NetworkRequestEvent;
  var init_network_request_event = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/network-request-event.js"() {
      init_tslib_es6();
      init_global_scope();
      init_json_query();
      init_constants();
      TEXT_READ_TIMEOUT = 500;
      MAXIMUM_ENTRIES = 100;
      RequestWrapperFetch = /** @class */
      (function() {
        function RequestWrapperFetch2(request) {
          this.request = request;
        }
        RequestWrapperFetch2.prototype.headers = function(allow) {
          var e_1, _a4;
          if (allow === void 0) {
            allow = [];
          }
          var headersUnsafe = this.request.headers;
          var headersSafeCopy = {};
          if (Array.isArray(headersUnsafe)) {
            headersUnsafe.forEach(function(_a5) {
              var _b2 = __read(_a5, 2), headerName = _b2[0], headerValue = _b2[1];
              headersSafeCopy[headerName] = headerValue;
            });
          } else if (headersUnsafe instanceof Headers) {
            headersUnsafe.forEach(function(value2, key2) {
              headersSafeCopy[key2] = value2;
            });
          } else if (typeof headersUnsafe === "object" && headersUnsafe !== null) {
            try {
              for (var _b = __values(Object.entries(headersUnsafe)), _c = _b.next(); !_c.done; _c = _b.next()) {
                var _d = __read(_c.value, 2), key = _d[0], value = _d[1];
                headersSafeCopy[key] = value;
              }
            } catch (e_1_1) {
              e_1 = { error: e_1_1 };
            } finally {
              try {
                if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
              } finally {
                if (e_1) throw e_1.error;
              }
            }
          }
          return pruneHeaders(headersSafeCopy, { allow });
        };
        Object.defineProperty(RequestWrapperFetch2.prototype, "bodySize", {
          get: function() {
            if (typeof this._bodySize === "number")
              return this._bodySize;
            var global2 = getGlobalScope();
            if (!(global2 === null || global2 === void 0 ? void 0 : global2.TextEncoder)) {
              return;
            }
            var body = this.request.body;
            this._bodySize = getBodySize(body, MAXIMUM_ENTRIES);
            return this._bodySize;
          },
          enumerable: false,
          configurable: true
        });
        Object.defineProperty(RequestWrapperFetch2.prototype, "method", {
          get: function() {
            return this.request.method;
          },
          enumerable: false,
          configurable: true
        });
        Object.defineProperty(RequestWrapperFetch2.prototype, "body", {
          get: function() {
            if (typeof this.request.body === "string") {
              return this.request.body;
            }
            return null;
          },
          enumerable: false,
          configurable: true
        });
        RequestWrapperFetch2.prototype.json = function(allow, exclude) {
          if (allow === void 0) {
            allow = [];
          }
          if (exclude === void 0) {
            exclude = [];
          }
          return __awaiter(this, void 0, void 0, function() {
            var text;
            return __generator(this, function(_a4) {
              if (allow.length === 0) {
                return [2, null];
              }
              text = this.body;
              return [2, safeParseAndPruneBody(text, allow, exclude)];
            });
          });
        };
        return RequestWrapperFetch2;
      })();
      RequestWrapperXhr = /** @class */
      (function() {
        function RequestWrapperXhr2(bodyRaw, requestHeaders) {
          this.bodyRaw = bodyRaw;
          this.requestHeaders = requestHeaders;
        }
        RequestWrapperXhr2.prototype.headers = function(allow) {
          if (allow === void 0) {
            allow = [];
          }
          return pruneHeaders(this.requestHeaders, { allow });
        };
        Object.defineProperty(RequestWrapperXhr2.prototype, "bodySize", {
          get: function() {
            return getBodySize(this.bodyRaw, MAXIMUM_ENTRIES);
          },
          enumerable: false,
          configurable: true
        });
        Object.defineProperty(RequestWrapperXhr2.prototype, "body", {
          get: function() {
            if (typeof this.bodyRaw === "string") {
              return this.bodyRaw;
            }
            return null;
          },
          enumerable: false,
          configurable: true
        });
        RequestWrapperXhr2.prototype.json = function(allow, exclude) {
          if (allow === void 0) {
            allow = [];
          }
          if (exclude === void 0) {
            exclude = [];
          }
          return __awaiter(this, void 0, void 0, function() {
            var text;
            return __generator(this, function(_a4) {
              if (allow.length === 0) {
                return [2, null];
              }
              text = this.body;
              return [2, safeParseAndPruneBody(text, allow, exclude)];
            });
          });
        };
        return RequestWrapperXhr2;
      })();
      ResponseWrapperFetch = /** @class */
      (function() {
        function ResponseWrapperFetch2(response) {
          this.response = response;
        }
        ResponseWrapperFetch2.prototype.headers = function(allow) {
          var _a4;
          if (allow === void 0) {
            allow = [];
          }
          if (this.response.headers instanceof Headers) {
            var headersSafe = this.response.headers;
            var headersOut_1 = {};
            (_a4 = headersSafe === null || headersSafe === void 0 ? void 0 : headersSafe.forEach) === null || _a4 === void 0 ? void 0 : _a4.call(headersSafe, function(value, key) {
              headersOut_1[key] = value;
            });
            return pruneHeaders(headersOut_1, { allow });
          }
          return;
        };
        Object.defineProperty(ResponseWrapperFetch2.prototype, "bodySize", {
          get: function() {
            var _a4, _b;
            if (this._bodySize !== void 0)
              return this._bodySize;
            var contentLength = (_b = (_a4 = this.response.headers) === null || _a4 === void 0 ? void 0 : _a4.get) === null || _b === void 0 ? void 0 : _b.call(_a4, "content-length");
            var bodySize = contentLength ? parseInt(contentLength, 10) : void 0;
            this._bodySize = bodySize;
            return bodySize;
          },
          enumerable: false,
          configurable: true
        });
        Object.defineProperty(ResponseWrapperFetch2.prototype, "status", {
          get: function() {
            return this.response.status;
          },
          enumerable: false,
          configurable: true
        });
        ResponseWrapperFetch2.prototype.text = function() {
          return __awaiter(this, void 0, void 0, function() {
            var textPromise, timer, text, error_1;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (!this.clonedResponse) {
                    this.clonedResponse = this.response.clone();
                  }
                  _a4.label = 1;
                case 1:
                  _a4.trys.push([1, 3, , 4]);
                  textPromise = this.clonedResponse.text();
                  timer = new Promise(function(resolve) {
                    return setTimeout(
                      /* istanbul ignore next */
                      function() {
                        return resolve(null);
                      },
                      TEXT_READ_TIMEOUT
                    );
                  });
                  return [4, Promise.race([textPromise, timer])];
                case 2:
                  text = _a4.sent();
                  return [2, text];
                case 3:
                  error_1 = _a4.sent();
                  return [2, null];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        ResponseWrapperFetch2.prototype.json = function(allow, exclude) {
          if (allow === void 0) {
            allow = [];
          }
          if (exclude === void 0) {
            exclude = [];
          }
          return __awaiter(this, void 0, void 0, function() {
            var text;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (allow.length === 0) {
                    return [2, null];
                  }
                  return [4, this.text()];
                case 1:
                  text = _a4.sent();
                  return [2, safeParseAndPruneBody(text, allow, exclude)];
              }
            });
          });
        };
        return ResponseWrapperFetch2;
      })();
      ResponseWrapperXhr = /** @class */
      (function() {
        function ResponseWrapperXhr2(statusCode, headersString, size, getJson) {
          this.statusCode = statusCode;
          this.headersString = headersString;
          this.size = size;
          this.getJson = getJson;
        }
        Object.defineProperty(ResponseWrapperXhr2.prototype, "bodySize", {
          get: function() {
            return this.size;
          },
          enumerable: false,
          configurable: true
        });
        Object.defineProperty(ResponseWrapperXhr2.prototype, "status", {
          get: function() {
            return this.statusCode;
          },
          enumerable: false,
          configurable: true
        });
        ResponseWrapperXhr2.prototype.headers = function(allow) {
          var e_3, _a4;
          if (allow === void 0) {
            allow = [];
          }
          if (!this.headersString) {
            return {};
          }
          var headers = {};
          var headerLines = this.headersString.split("\r\n");
          try {
            for (var headerLines_1 = __values(headerLines), headerLines_1_1 = headerLines_1.next(); !headerLines_1_1.done; headerLines_1_1 = headerLines_1.next()) {
              var line2 = headerLines_1_1.value;
              var _b = __read(line2.split(": "), 2), key = _b[0], value = _b[1];
              if (key && value) {
                headers[key] = value;
              }
            }
          } catch (e_3_1) {
            e_3 = { error: e_3_1 };
          } finally {
            try {
              if (headerLines_1_1 && !headerLines_1_1.done && (_a4 = headerLines_1.return)) _a4.call(headerLines_1);
            } finally {
              if (e_3) throw e_3.error;
            }
          }
          return pruneHeaders(headers, { allow });
        };
        ResponseWrapperXhr2.prototype.json = function(allow, exclude) {
          if (allow === void 0) {
            allow = [];
          }
          if (exclude === void 0) {
            exclude = [];
          }
          return __awaiter(this, void 0, void 0, function() {
            var jsonBody;
            return __generator(this, function(_a4) {
              if (allow.length === 0) {
                return [2, null];
              }
              jsonBody = this.getJson();
              if (jsonBody) {
                pruneJson(jsonBody, allow, exclude);
                return [2, jsonBody];
              }
              return [2, null];
            });
          });
        };
        return ResponseWrapperXhr2;
      })();
      (function(PRUNE_STRATEGY2) {
        PRUNE_STRATEGY2["REDACT"] = "redact";
        PRUNE_STRATEGY2["REMOVE"] = "remove";
      })(PRUNE_STRATEGY || (PRUNE_STRATEGY = {}));
      REDACTED_VALUE = "[REDACTED]";
      pruneHeaders = function(headers, options) {
        var e_4, _a4;
        var _b = options.allow, allow = _b === void 0 ? [] : _b, _c = options.strategy, strategy = _c === void 0 ? PRUNE_STRATEGY.REMOVE : _c;
        var exclude = __spreadArray([], __read(FORBIDDEN_HEADERS), false);
        var headersPruned = {};
        var _loop_1 = function(key2) {
          var lowerKey = key2.toLowerCase();
          if (exclude.find(function(e2) {
            return e2.toLowerCase() === lowerKey;
          })) {
            if (strategy === PRUNE_STRATEGY.REDACT) {
              headersPruned[key2] = REDACTED_VALUE;
            }
          } else if (!allow.find(function(i2) {
            return i2.toLowerCase() === lowerKey;
          })) {
            if (strategy === PRUNE_STRATEGY.REDACT) {
              headersPruned[key2] = REDACTED_VALUE;
            }
          } else {
            headersPruned[key2] = headers[key2];
          }
        };
        try {
          for (var _d = __values(Object.keys(headers)), _e = _d.next(); !_e.done; _e = _d.next()) {
            var key = _e.value;
            _loop_1(key);
          }
        } catch (e_4_1) {
          e_4 = { error: e_4_1 };
        } finally {
          try {
            if (_e && !_e.done && (_a4 = _d.return)) _a4.call(_d);
          } finally {
            if (e_4) throw e_4.error;
          }
        }
        return headersPruned;
      };
      NetworkRequestEvent = /** @class */
      (function() {
        function NetworkRequestEvent2(type, method, timestamp, startTime, url, requestWrapper, status, duration, responseWrapper, error, endTime) {
          if (status === void 0) {
            status = 0;
          }
          this.type = type;
          this.method = method;
          this.timestamp = timestamp;
          this.startTime = startTime;
          this.url = url;
          this.requestWrapper = requestWrapper;
          this.status = status;
          this.duration = duration;
          this.responseWrapper = responseWrapper;
          this.error = error;
          this.endTime = endTime;
        }
        NetworkRequestEvent2.prototype.toSerializable = function() {
          var _a4, _b, _c, _d;
          var serialized = {
            type: this.type,
            method: this.method,
            url: this.url,
            timestamp: this.timestamp,
            status: this.status,
            duration: this.duration,
            error: this.error,
            startTime: this.startTime,
            endTime: this.endTime,
            requestHeaders: (_a4 = this.requestWrapper) === null || _a4 === void 0 ? void 0 : _a4.headers(__spreadArray([], __read(SAFE_HEADERS), false)),
            requestBodySize: (_b = this.requestWrapper) === null || _b === void 0 ? void 0 : _b.bodySize,
            responseHeaders: (_c = this.responseWrapper) === null || _c === void 0 ? void 0 : _c.headers(__spreadArray([], __read(SAFE_HEADERS), false)),
            responseBodySize: (_d = this.responseWrapper) === null || _d === void 0 ? void 0 : _d.bodySize
          };
          return Object.fromEntries(Object.entries(serialized).filter(function(_a5) {
            var _b2 = __read(_a5, 2), _2 = _b2[0], v2 = _b2[1];
            return v2 !== void 0;
          }));
        };
        return NetworkRequestEvent2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/observers/network.js
  function isRequest(requestInfo) {
    return typeof requestInfo === "object" && requestInfo !== null && "url" in requestInfo && "method" in requestInfo;
  }
  function safeInvoke(fn) {
    try {
      fn();
    } catch (err) {
    }
  }
  var NetworkEventCallback, NetworkObserver, networkObserver;
  var init_network = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/observers/network.js"() {
      init_tslib_es6();
      init_global_scope();
      init_uuid();
      init_network_request_event();
      NetworkEventCallback = /** @class */
      /* @__PURE__ */ (function() {
        function NetworkEventCallback2(callback, id2) {
          if (id2 === void 0) {
            id2 = UUID();
          }
          this.callback = callback;
          this.id = id2;
        }
        return NetworkEventCallback2;
      })();
      NetworkObserver = /** @class */
      (function() {
        function NetworkObserver2(logger) {
          this.eventCallbacks = /* @__PURE__ */ new Map();
          this.isObserving = false;
          this.logger = logger;
          var globalScope3 = getGlobalScope();
          if (!NetworkObserver2.isSupported()) {
            return;
          }
          this.globalScope = globalScope3;
        }
        NetworkObserver2.isSupported = function() {
          var globalScope3 = getGlobalScope();
          return !!globalScope3 && !!globalScope3.fetch;
        };
        NetworkObserver2.prototype.subscribe = function(eventCallback, logger) {
          var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k;
          if (!this.logger) {
            this.logger = logger;
          }
          this.eventCallbacks.set(eventCallback.id, eventCallback);
          if (!this.isObserving) {
            var originalXhrOpen = (_c = (_b = (_a4 = this.globalScope) === null || _a4 === void 0 ? void 0 : _a4.XMLHttpRequest) === null || _b === void 0 ? void 0 : _b.prototype) === null || _c === void 0 ? void 0 : _c.open;
            var originalXhrSend = (_f = (_e = (_d = this.globalScope) === null || _d === void 0 ? void 0 : _d.XMLHttpRequest) === null || _e === void 0 ? void 0 : _e.prototype) === null || _f === void 0 ? void 0 : _f.send;
            var originalXhrSetRequestHeader = (_j = (_h = (_g = this.globalScope) === null || _g === void 0 ? void 0 : _g.XMLHttpRequest) === null || _h === void 0 ? void 0 : _h.prototype) === null || _j === void 0 ? void 0 : _j.setRequestHeader;
            if (originalXhrOpen && originalXhrSend && originalXhrSetRequestHeader) {
              this.observeXhr(originalXhrOpen, originalXhrSend, originalXhrSetRequestHeader);
            }
            var originalFetch = (_k = this.globalScope) === null || _k === void 0 ? void 0 : _k.fetch;
            if (originalFetch) {
              this.observeFetch(originalFetch);
            }
            this.isObserving = true;
          }
        };
        NetworkObserver2.prototype.unsubscribe = function(eventCallback) {
          this.eventCallbacks.delete(eventCallback.id);
        };
        NetworkObserver2.prototype.triggerEventCallbacks = function(event) {
          var _this = this;
          this.eventCallbacks.forEach(function(callback) {
            try {
              callback.callback(event);
            } catch (err) {
              safeInvoke(function() {
                var _a4;
                (_a4 = _this.logger) === null || _a4 === void 0 ? void 0 : _a4.debug("an unexpected error occurred while triggering event callbacks", err);
              });
            }
          });
        };
        NetworkObserver2.prototype.handleNetworkRequestEvent = function(requestType, requestInfo, requestWrapper, responseWrapper, typedError, startTime, durationStart) {
          var _a4;
          if (startTime === void 0 || durationStart === void 0) {
            return;
          }
          var url;
          var method = "GET";
          if (isRequest(requestInfo)) {
            url = requestInfo["url"];
            method = requestInfo["method"];
          } else {
            url = (_a4 = requestInfo === null || requestInfo === void 0 ? void 0 : requestInfo.toString) === null || _a4 === void 0 ? void 0 : _a4.call(requestInfo);
          }
          if (url) {
            try {
              var parsedUrl = new URL(url);
              url = "".concat(parsedUrl.protocol, "//").concat(parsedUrl.host).concat(parsedUrl.pathname).concat(parsedUrl.search).concat(parsedUrl.hash);
            } catch (err) {
            }
          }
          method = (requestWrapper === null || requestWrapper === void 0 ? void 0 : requestWrapper.method) || method;
          var status, error;
          if (responseWrapper) {
            status = responseWrapper.status;
          }
          if (typedError) {
            error = {
              name: typedError.name || "UnknownError",
              message: typedError.message || "An unknown error occurred"
            };
            status = 0;
          }
          var duration = Math.floor(performance.now() - durationStart);
          var endTime = Math.floor(startTime + duration);
          var requestEvent = new NetworkRequestEvent(
            requestType,
            method,
            startTime,
            // timestamp and startTime are aliases
            startTime,
            url,
            requestWrapper,
            status,
            duration,
            responseWrapper,
            error,
            endTime
          );
          this.triggerEventCallbacks(requestEvent);
        };
        NetworkObserver2.prototype.getTimestamps = function() {
          var _a4, _b;
          return {
            startTime: (_a4 = Date.now) === null || _a4 === void 0 ? void 0 : _a4.call(Date),
            durationStart: (_b = performance === null || performance === void 0 ? void 0 : performance.now) === null || _b === void 0 ? void 0 : _b.call(performance)
          };
        };
        NetworkObserver2.prototype.observeFetch = function(originalFetch) {
          var _this = this;
          if (!this.globalScope || !originalFetch) {
            return;
          }
          this.globalScope.fetch = function(requestInfo, requestInit) {
            return __awaiter(_this, void 0, void 0, function() {
              var timestamps, originalResponse, originalError, err_1;
              var _this2 = this;
              return __generator(this, function(_a4) {
                switch (_a4.label) {
                  case 0:
                    try {
                      timestamps = this.getTimestamps();
                    } catch (error) {
                      safeInvoke(function() {
                        var _a5;
                        return (_a5 = _this2.logger) === null || _a5 === void 0 ? void 0 : _a5.debug("an unexpected error occurred while retrieving timestamps", error);
                      });
                    }
                    _a4.label = 1;
                  case 1:
                    _a4.trys.push([1, 3, , 4]);
                    return [4, originalFetch(requestInfo, requestInit)];
                  case 2:
                    originalResponse = _a4.sent();
                    return [3, 4];
                  case 3:
                    err_1 = _a4.sent();
                    originalError = err_1;
                    return [3, 4];
                  case 4:
                    try {
                      this.handleNetworkRequestEvent(
                        "fetch",
                        requestInfo,
                        requestInit ? new RequestWrapperFetch(requestInit) : void 0,
                        originalResponse ? new ResponseWrapperFetch(originalResponse) : void 0,
                        originalError,
                        /* istanbul ignore next */
                        timestamps === null || timestamps === void 0 ? void 0 : timestamps.startTime,
                        /* istanbul ignore next */
                        timestamps === null || timestamps === void 0 ? void 0 : timestamps.durationStart
                      );
                    } catch (err) {
                      safeInvoke(function() {
                        var _a5;
                        return (_a5 = _this2.logger) === null || _a5 === void 0 ? void 0 : _a5.debug("an unexpected error occurred while handling fetch", err);
                      });
                    }
                    if (originalResponse) {
                      return [2, originalResponse];
                    } else {
                      throw originalError;
                    }
                    return [
                      2
                      /*return*/
                    ];
                }
              });
            });
          };
        };
        NetworkObserver2.createXhrJsonParser = function(xhrUnsafe, context) {
          return function() {
            var _a4;
            try {
              if (xhrUnsafe.responseType === "json") {
                if ((_a4 = context.globalScope) === null || _a4 === void 0 ? void 0 : _a4.structuredClone) {
                  return context.globalScope.structuredClone(xhrUnsafe.response);
                }
              } else if (["text", ""].includes(xhrUnsafe.responseType)) {
                return JSON.parse(xhrUnsafe.responseText);
              }
            } catch (err) {
              if (err instanceof Error && err.name === "InvalidStateError") {
                safeInvoke(function() {
                  var _a5;
                  return (_a5 = context.logger) === null || _a5 === void 0 ? void 0 : _a5.debug("unexpected error when retrieving responseText. responseType='".concat(xhrUnsafe.responseType, "'"));
                });
              }
              return null;
            }
            return null;
          };
        };
        NetworkObserver2.prototype.observeXhr = function(originalXhrOpen, originalXhrSend, originalXhrSetRequestHeader) {
          if (!this.globalScope || !originalXhrOpen || !originalXhrSend) {
            return;
          }
          var xhrProto = this.globalScope.XMLHttpRequest.prototype;
          var networkObserverContext = this;
          xhrProto.open = function() {
            var _a4;
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
              args[_i] = arguments[_i];
            }
            var xhrSafe = this;
            var _b = __read(args, 2), method = _b[0], url = _b[1];
            try {
              xhrSafe.$$AmplitudeAnalyticsEvent = __assign({ method, url: (_a4 = url === null || url === void 0 ? void 0 : url.toString) === null || _a4 === void 0 ? void 0 : _a4.call(url), headers: {} }, networkObserverContext.getTimestamps());
            } catch (err) {
              safeInvoke(function() {
                var _a5;
                return (_a5 = networkObserverContext.logger) === null || _a5 === void 0 ? void 0 : _a5.debug("an unexpected error occurred while calling xhr open", err);
              });
            }
            return originalXhrOpen.apply(xhrSafe, args);
          };
          xhrProto.send = function() {
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
              args[_i] = arguments[_i];
            }
            var xhrUnsafe = this;
            var xhrSafe = xhrUnsafe;
            var getJson = NetworkObserver2.createXhrJsonParser(xhrUnsafe, networkObserverContext);
            var body = args[0];
            var requestEvent = xhrSafe.$$AmplitudeAnalyticsEvent;
            if (xhrSafe.$$AmplitudeAnalyticsEvent) {
              xhrSafe.addEventListener("loadend", function() {
                try {
                  var responseHeaders = xhrSafe.getAllResponseHeaders();
                  var responseBodySize = xhrSafe.getResponseHeader("content-length");
                  var responseWrapper = new ResponseWrapperXhr(
                    xhrSafe.status,
                    responseHeaders,
                    /* istanbul ignore next */
                    responseBodySize ? parseInt(responseBodySize, 10) : void 0,
                    getJson
                  );
                  var requestHeaders = xhrSafe.$$AmplitudeAnalyticsEvent.headers;
                  var requestWrapper = new RequestWrapperXhr(body, requestHeaders);
                  requestEvent.status = xhrSafe.status;
                  networkObserverContext.handleNetworkRequestEvent("xhr", { url: requestEvent.url, method: requestEvent.method }, requestWrapper, responseWrapper, void 0, requestEvent.startTime, requestEvent.durationStart);
                } catch (err) {
                  safeInvoke(function() {
                    var _a4;
                    return (_a4 = networkObserverContext.logger) === null || _a4 === void 0 ? void 0 : _a4.debug("an unexpected error occurred while handling xhr send", err);
                  });
                }
              });
            }
            return originalXhrSend.apply(xhrSafe, args);
          };
          xhrProto.setRequestHeader = function(headerName, headerValue) {
            var xhrSafe = this;
            try {
              var analyticsEvent = xhrSafe.$$AmplitudeAnalyticsEvent;
              if (analyticsEvent) {
                analyticsEvent.headers[headerName] = headerValue;
              }
            } catch (err) {
              safeInvoke(function() {
                var _a4;
                return (_a4 = networkObserverContext.logger) === null || _a4 === void 0 ? void 0 : _a4.debug("an unexpected error occurred while calling xhr setRequestHeader", err);
              });
            }
            originalXhrSetRequestHeader.apply(xhrSafe, [headerName, headerValue]);
          };
        };
        return NetworkObserver2;
      })();
      networkObserver = new NetworkObserver();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/network-tracking-config.js
  var normalizeNetworkCaptureRules;
  var init_network_tracking_config = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/network-tracking-config.js"() {
      init_tslib_es6();
      normalizeNetworkCaptureRules = function(captureRules, logger) {
        return captureRules === null || captureRules === void 0 ? void 0 : captureRules.map(function(rule3) {
          var _a4, _b;
          if (((_a4 = rule3.urls) === null || _a4 === void 0 ? void 0 : _a4.length) && ((_b = rule3.hosts) === null || _b === void 0 ? void 0 : _b.length)) {
            var hostsString = JSON.stringify(rule3.hosts);
            var urlsString = JSON.stringify(rule3.urls);
            logger === null || logger === void 0 ? void 0 : logger.warn("Found network capture rule with both urls='".concat(urlsString, "' and hosts='").concat(hostsString, "' set. ") + "Definition of urls takes precedence over hosts, so ignoring hosts.");
            return __assign(__assign({}, rule3), { hosts: void 0 });
          }
          return rule3;
        });
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/campaign/campaign-parser.js
  var CampaignParser;
  var init_campaign_parser = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/campaign/campaign-parser.js"() {
      init_tslib_es6();
      init_query_params();
      init_constants();
      CampaignParser = /** @class */
      (function() {
        function CampaignParser2() {
        }
        CampaignParser2.prototype.parse = function() {
          return __awaiter(this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              return [2, __assign(__assign(__assign(__assign({}, BASE_CAMPAIGN), this.getUtmParam()), this.getReferrer()), this.getClickIds())];
            });
          });
        };
        CampaignParser2.prototype.getUtmParam = function() {
          var params = getQueryParams();
          var utmCampaign = params[UTM_CAMPAIGN];
          var utmContent = params[UTM_CONTENT];
          var utmId = params[UTM_ID];
          var utmMedium = params[UTM_MEDIUM];
          var utmSource = params[UTM_SOURCE];
          var utmTerm = params[UTM_TERM];
          return {
            utm_campaign: utmCampaign,
            utm_content: utmContent,
            utm_id: utmId,
            utm_medium: utmMedium,
            utm_source: utmSource,
            utm_term: utmTerm
          };
        };
        CampaignParser2.prototype.getReferrer = function() {
          var _a4, _b;
          var data = {
            referrer: void 0,
            referring_domain: void 0
          };
          try {
            data.referrer = document.referrer || void 0;
            data.referring_domain = (_b = (_a4 = data.referrer) === null || _a4 === void 0 ? void 0 : _a4.split("/")[2]) !== null && _b !== void 0 ? _b : void 0;
          } catch (_c) {
          }
          return data;
        };
        CampaignParser2.prototype.getClickIds = function() {
          var _a4;
          var params = getQueryParams();
          return _a4 = {}, _a4[DCLID] = params[DCLID], _a4[FBCLID] = params[FBCLID], _a4[GBRAID] = params[GBRAID], _a4[GCLID] = params[GCLID], _a4[KO_CLICK_ID] = params[KO_CLICK_ID], _a4[LI_FAT_ID] = params[LI_FAT_ID], _a4[MSCLKID] = params[MSCLKID], _a4[RDT_CID] = params[RDT_CID], _a4[TTCLID] = params[TTCLID], _a4[TWCLID] = params[TWCLID], _a4[WBRAID] = params[WBRAID], _a4;
        };
        return CampaignParser2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/plugins/helpers.js
  var TEXT_MASK_ATTRIBUTE, MASKED_TEXT_VALUE, CC_REGEX, SSN_REGEX, EMAIL_REGEX, replaceSensitiveString, getPageTitle;
  var init_helpers2 = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/plugins/helpers.js"() {
      init_tslib_es6();
      TEXT_MASK_ATTRIBUTE = "data-amp-mask";
      MASKED_TEXT_VALUE = "*****";
      CC_REGEX = /\b(?:\d[ -]*?){13,16}\b/;
      SSN_REGEX = /(\d{3}-?\d{2}-?\d{4})/g;
      EMAIL_REGEX = /[^\s@]+@[^\s@.]+\.[^\s@]+/g;
      replaceSensitiveString = function(text, additionalMaskTextPatterns) {
        var e_1, _a4;
        if (additionalMaskTextPatterns === void 0) {
          additionalMaskTextPatterns = [];
        }
        if (typeof text !== "string") {
          return "";
        }
        var result3 = text;
        result3 = result3.replace(CC_REGEX, MASKED_TEXT_VALUE);
        result3 = result3.replace(SSN_REGEX, MASKED_TEXT_VALUE);
        result3 = result3.replace(EMAIL_REGEX, MASKED_TEXT_VALUE);
        try {
          for (var additionalMaskTextPatterns_1 = __values(additionalMaskTextPatterns), additionalMaskTextPatterns_1_1 = additionalMaskTextPatterns_1.next(); !additionalMaskTextPatterns_1_1.done; additionalMaskTextPatterns_1_1 = additionalMaskTextPatterns_1.next()) {
            var pattern = additionalMaskTextPatterns_1_1.value;
            try {
              result3 = result3.replace(pattern, MASKED_TEXT_VALUE);
            } catch (_b) {
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (additionalMaskTextPatterns_1_1 && !additionalMaskTextPatterns_1_1.done && (_a4 = additionalMaskTextPatterns_1.return)) _a4.call(additionalMaskTextPatterns_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        return result3;
      };
      getPageTitle = function(parseTitleFunction) {
        if (typeof document === "undefined" || !document.title) {
          return "";
        }
        var titleElement = document.querySelector("title");
        if (titleElement && titleElement.hasAttribute(TEXT_MASK_ATTRIBUTE)) {
          return MASKED_TEXT_VALUE;
        }
        return parseTitleFunction ? parseTitleFunction(document.title) : document.title;
      };
    }
  });

  // node_modules/zen-observable/lib/Observable.js
  var require_Observable = __commonJS({
    "node_modules/zen-observable/lib/Observable.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", {
        value: true
      });
      exports.Observable = void 0;
      var hasSymbol = (name) => Boolean(Symbol[name]);
      var getSymbol = (name) => hasSymbol(name) ? Symbol[name] : "@@" + name;
      var SymbolIterator = getSymbol("iterator");
      var SymbolObservable = getSymbol("observable");
      var SymbolSpecies = getSymbol("species");
      function getMethod2(obj, key) {
        let value = obj[key];
        if (value == null) return void 0;
        if (typeof value !== "function") throw new TypeError(value + " is not a function");
        return value;
      }
      function getSpecies(obj) {
        let ctor = obj.constructor;
        if (ctor !== void 0) {
          ctor = ctor[SymbolSpecies];
          if (ctor === null) {
            ctor = void 0;
          }
        }
        return ctor !== void 0 ? ctor : Observable;
      }
      function isObservable(x2) {
        return x2 instanceof Observable;
      }
      function hostReportError(e2) {
        if (hostReportError.log) {
          hostReportError.log(e2);
        } else {
          setTimeout(() => {
            throw e2;
          });
        }
      }
      function enqueue(fn) {
        Promise.resolve().then(() => {
          try {
            fn();
          } catch (e2) {
            hostReportError(e2);
          }
        });
      }
      function cleanupSubscription(subscription2) {
        let cleanup = subscription2._cleanup;
        if (cleanup === void 0) return;
        subscription2._cleanup = void 0;
        if (!cleanup) {
          return;
        }
        try {
          if (typeof cleanup === "function") {
            cleanup();
          } else {
            let unsubscribe = getMethod2(cleanup, "unsubscribe");
            if (unsubscribe) {
              unsubscribe.call(cleanup);
            }
          }
        } catch (e2) {
          hostReportError(e2);
        }
      }
      function closeSubscription(subscription2) {
        subscription2._observer = void 0;
        subscription2._queue = void 0;
        subscription2._state = "closed";
      }
      function flushSubscription(subscription2) {
        let queue = subscription2._queue;
        if (!queue) {
          return;
        }
        subscription2._queue = void 0;
        subscription2._state = "ready";
        for (let i2 = 0; i2 < queue.length; ++i2) {
          notifySubscription(subscription2, queue[i2].type, queue[i2].value);
          if (subscription2._state === "closed") break;
        }
      }
      function notifySubscription(subscription2, type, value) {
        subscription2._state = "running";
        let observer = subscription2._observer;
        try {
          let m2 = getMethod2(observer, type);
          switch (type) {
            case "next":
              if (m2) m2.call(observer, value);
              break;
            case "error":
              closeSubscription(subscription2);
              if (m2) m2.call(observer, value);
              else throw value;
              break;
            case "complete":
              closeSubscription(subscription2);
              if (m2) m2.call(observer);
              break;
          }
        } catch (e2) {
          hostReportError(e2);
        }
        if (subscription2._state === "closed") cleanupSubscription(subscription2);
        else if (subscription2._state === "running") subscription2._state = "ready";
      }
      function onNotify(subscription2, type, value) {
        if (subscription2._state === "closed") return;
        if (subscription2._state === "buffering") {
          subscription2._queue.push({
            type,
            value
          });
          return;
        }
        if (subscription2._state !== "ready") {
          subscription2._state = "buffering";
          subscription2._queue = [{
            type,
            value
          }];
          enqueue(() => flushSubscription(subscription2));
          return;
        }
        notifySubscription(subscription2, type, value);
      }
      var Subscription = class {
        constructor(observer, subscriber) {
          this._cleanup = void 0;
          this._observer = observer;
          this._queue = void 0;
          this._state = "initializing";
          let self2 = this;
          let subscriptionObserver = {
            get closed() {
              return self2._state === "closed";
            },
            next(value) {
              onNotify(self2, "next", value);
            },
            error(value) {
              onNotify(self2, "error", value);
            },
            complete() {
              onNotify(self2, "complete");
            }
          };
          try {
            this._cleanup = subscriber.call(void 0, subscriptionObserver);
          } catch (e2) {
            subscriptionObserver.error(e2);
          }
          if (this._state === "initializing") this._state = "ready";
        }
        get closed() {
          return this._state === "closed";
        }
        unsubscribe() {
          if (this._state !== "closed") {
            closeSubscription(this);
            cleanupSubscription(this);
          }
        }
      };
      var Observable = class _Observable {
        constructor(subscriber) {
          if (!(this instanceof _Observable)) throw new TypeError("Observable cannot be called as a function");
          if (typeof subscriber !== "function") throw new TypeError("Observable initializer must be a function");
          this._subscriber = subscriber;
        }
        subscribe(observer) {
          if (typeof observer !== "object" || observer === null) {
            observer = {
              next: observer,
              error: arguments[1],
              complete: arguments[2]
            };
          }
          return new Subscription(observer, this._subscriber);
        }
        forEach(fn) {
          return new Promise((resolve, reject) => {
            if (typeof fn !== "function") {
              reject(new TypeError(fn + " is not a function"));
              return;
            }
            function done() {
              subscription2.unsubscribe();
              resolve();
            }
            let subscription2 = this.subscribe({
              next(value) {
                try {
                  fn(value, done);
                } catch (e2) {
                  reject(e2);
                  subscription2.unsubscribe();
                }
              },
              error: reject,
              complete: resolve
            });
          });
        }
        map(fn) {
          if (typeof fn !== "function") throw new TypeError(fn + " is not a function");
          let C2 = getSpecies(this);
          return new C2((observer) => this.subscribe({
            next(value) {
              try {
                value = fn(value);
              } catch (e2) {
                return observer.error(e2);
              }
              observer.next(value);
            },
            error(e2) {
              observer.error(e2);
            },
            complete() {
              observer.complete();
            }
          }));
        }
        filter(fn) {
          if (typeof fn !== "function") throw new TypeError(fn + " is not a function");
          let C2 = getSpecies(this);
          return new C2((observer) => this.subscribe({
            next(value) {
              try {
                if (!fn(value)) return;
              } catch (e2) {
                return observer.error(e2);
              }
              observer.next(value);
            },
            error(e2) {
              observer.error(e2);
            },
            complete() {
              observer.complete();
            }
          }));
        }
        reduce(fn) {
          if (typeof fn !== "function") throw new TypeError(fn + " is not a function");
          let C2 = getSpecies(this);
          let hasSeed = arguments.length > 1;
          let hasValue = false;
          let seed = arguments[1];
          let acc = seed;
          return new C2((observer) => this.subscribe({
            next(value) {
              let first = !hasValue;
              hasValue = true;
              if (!first || hasSeed) {
                try {
                  acc = fn(acc, value);
                } catch (e2) {
                  return observer.error(e2);
                }
              } else {
                acc = value;
              }
            },
            error(e2) {
              observer.error(e2);
            },
            complete() {
              if (!hasValue && !hasSeed) return observer.error(new TypeError("Cannot reduce an empty sequence"));
              observer.next(acc);
              observer.complete();
            }
          }));
        }
        async all() {
          let values = [];
          await this.forEach((value) => values.push(value));
          return values;
        }
        concat(...sources) {
          let C2 = getSpecies(this);
          return new C2((observer) => {
            let subscription2;
            let index4 = 0;
            function startNext(next) {
              subscription2 = next.subscribe({
                next(v2) {
                  observer.next(v2);
                },
                error(e2) {
                  observer.error(e2);
                },
                complete() {
                  if (index4 === sources.length) {
                    subscription2 = void 0;
                    observer.complete();
                  } else {
                    startNext(C2.from(sources[index4++]));
                  }
                }
              });
            }
            startNext(this);
            return () => {
              if (subscription2) {
                subscription2.unsubscribe();
                subscription2 = void 0;
              }
            };
          });
        }
        flatMap(fn) {
          if (typeof fn !== "function") throw new TypeError(fn + " is not a function");
          let C2 = getSpecies(this);
          return new C2((observer) => {
            let subscriptions = [];
            let outer = this.subscribe({
              next(value) {
                if (fn) {
                  try {
                    value = fn(value);
                  } catch (e2) {
                    return observer.error(e2);
                  }
                }
                let inner = C2.from(value).subscribe({
                  next(value2) {
                    observer.next(value2);
                  },
                  error(e2) {
                    observer.error(e2);
                  },
                  complete() {
                    let i2 = subscriptions.indexOf(inner);
                    if (i2 >= 0) subscriptions.splice(i2, 1);
                    completeIfDone();
                  }
                });
                subscriptions.push(inner);
              },
              error(e2) {
                observer.error(e2);
              },
              complete() {
                completeIfDone();
              }
            });
            function completeIfDone() {
              if (outer.closed && subscriptions.length === 0) observer.complete();
            }
            return () => {
              subscriptions.forEach((s2) => s2.unsubscribe());
              outer.unsubscribe();
            };
          });
        }
        [SymbolObservable]() {
          return this;
        }
        static from(x2) {
          let C2 = typeof this === "function" ? this : _Observable;
          if (x2 == null) throw new TypeError(x2 + " is not an object");
          let method = getMethod2(x2, SymbolObservable);
          if (method) {
            let observable = method.call(x2);
            if (Object(observable) !== observable) throw new TypeError(observable + " is not an object");
            if (isObservable(observable) && observable.constructor === C2) return observable;
            return new C2((observer) => observable.subscribe(observer));
          }
          if (hasSymbol("iterator")) {
            method = getMethod2(x2, SymbolIterator);
            if (method) {
              return new C2((observer) => {
                enqueue(() => {
                  if (observer.closed) return;
                  for (let item of method.call(x2)) {
                    observer.next(item);
                    if (observer.closed) return;
                  }
                  observer.complete();
                });
              });
            }
          }
          if (Array.isArray(x2)) {
            return new C2((observer) => {
              enqueue(() => {
                if (observer.closed) return;
                for (let i2 = 0; i2 < x2.length; ++i2) {
                  observer.next(x2[i2]);
                  if (observer.closed) return;
                }
                observer.complete();
              });
            });
          }
          throw new TypeError(x2 + " is not observable");
        }
        static of(...items) {
          let C2 = typeof this === "function" ? this : _Observable;
          return new C2((observer) => {
            enqueue(() => {
              if (observer.closed) return;
              for (let i2 = 0; i2 < items.length; ++i2) {
                observer.next(items[i2]);
                if (observer.closed) return;
              }
              observer.complete();
            });
          });
        }
        static get [SymbolSpecies]() {
          return this;
        }
      };
      exports.Observable = Observable;
      Object.defineProperty(Observable, Symbol("extensions"), {
        value: {
          symbol: SymbolObservable,
          hostReportError
        },
        configurable: true
      });
    }
  });

  // node_modules/zen-observable/index.js
  var require_zen_observable = __commonJS({
    "node_modules/zen-observable/index.js"(exports, module) {
      module.exports = require_Observable().Observable;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/observable.js
  function asyncMap(observable, fn) {
    return new import_zen_observable.default(function(observer) {
      observable.subscribe({
        next: function(value) {
          fn(value).then(function(result3) {
            return observer.next(result3);
          }).catch(function(error) {
            return observer.error(error);
          });
        },
        error: function(error) {
          observer.error(error);
        },
        complete: function() {
          observer.complete();
        }
      });
    });
  }
  function merge(sourceA, sourceB) {
    return new import_zen_observable.default(function(observer) {
      var closed = false;
      var subscriptions = /* @__PURE__ */ new Set();
      var cleanup = function() {
        var e_1, _a4;
        closed = true;
        try {
          for (var subscriptions_1 = __values(subscriptions), subscriptions_1_1 = subscriptions_1.next(); !subscriptions_1_1.done; subscriptions_1_1 = subscriptions_1.next()) {
            var sub = subscriptions_1_1.value;
            try {
              sub.unsubscribe();
            } catch (_b) {
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (subscriptions_1_1 && !subscriptions_1_1.done && (_a4 = subscriptions_1.return)) _a4.call(subscriptions_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        subscriptions.clear();
      };
      var subscribeTo = function(source) {
        var sub = source.subscribe({
          next: function(value) {
            if (!closed)
              observer.next(value);
          },
          error: function(err) {
            if (!closed) {
              closed = true;
              observer.error(err);
              cleanup();
            }
          },
          complete: function() {
            subscriptions.delete(sub);
            if (!closed && subscriptions.size === 0) {
              observer.complete();
              cleanup();
              closed = true;
            }
          }
        });
        subscriptions.add(sub);
      };
      subscribeTo(sourceA);
      subscribeTo(sourceB);
      return cleanup;
    });
  }
  function multicast(source) {
    var observers = /* @__PURE__ */ new Set();
    var subscription2 = null;
    function cleanup() {
      subscription2 === null || subscription2 === void 0 ? void 0 : subscription2.unsubscribe();
      subscription2 = null;
      observers.clear();
    }
    return new import_zen_observable.default(function(observer) {
      observers.add(observer);
      if (subscription2 === null) {
        subscription2 = source.subscribe({
          next: function(value) {
            var e_2, _a4;
            var _b;
            try {
              for (var observers_1 = __values(observers), observers_1_1 = observers_1.next(); !observers_1_1.done; observers_1_1 = observers_1.next()) {
                var obs = observers_1_1.value;
                (_b = obs.next) === null || _b === void 0 ? void 0 : _b.call(obs, value);
              }
            } catch (e_2_1) {
              e_2 = { error: e_2_1 };
            } finally {
              try {
                if (observers_1_1 && !observers_1_1.done && (_a4 = observers_1.return)) _a4.call(observers_1);
              } finally {
                if (e_2) throw e_2.error;
              }
            }
          },
          error: function(err) {
            var e_3, _a4;
            var _b;
            try {
              for (var observers_2 = __values(observers), observers_2_1 = observers_2.next(); !observers_2_1.done; observers_2_1 = observers_2.next()) {
                var obs = observers_2_1.value;
                (_b = obs.error) === null || _b === void 0 ? void 0 : _b.call(obs, err);
              }
            } catch (e_3_1) {
              e_3 = { error: e_3_1 };
            } finally {
              try {
                if (observers_2_1 && !observers_2_1.done && (_a4 = observers_2.return)) _a4.call(observers_2);
              } finally {
                if (e_3) throw e_3.error;
              }
            }
            cleanup();
          },
          complete: function() {
            var e_4, _a4;
            var _b;
            try {
              for (var observers_3 = __values(observers), observers_3_1 = observers_3.next(); !observers_3_1.done; observers_3_1 = observers_3.next()) {
                var obs = observers_3_1.value;
                (_b = obs.complete) === null || _b === void 0 ? void 0 : _b.call(obs);
              }
            } catch (e_4_1) {
              e_4 = { error: e_4_1 };
            } finally {
              try {
                if (observers_3_1 && !observers_3_1.done && (_a4 = observers_3.return)) _a4.call(observers_3);
              } finally {
                if (e_4) throw e_4.error;
              }
            }
            cleanup();
          }
        });
      }
      return function() {
        observers.delete(observer);
        if (observers.size === 0 && subscription2) {
          subscription2.unsubscribe();
          subscription2 = null;
        }
      };
    });
  }
  var import_zen_observable;
  var init_observable = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/observable.js"() {
      init_tslib_es6();
      import_zen_observable = __toESM(require_zen_observable());
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/messenger/constants.js
  var AMPLITUDE_ORIGIN, AMPLITUDE_ORIGIN_EU, AMPLITUDE_ORIGIN_STAGING, AMPLITUDE_ORIGINS_MAP, AMPLITUDE_BACKGROUND_CAPTURE_SCRIPT_URL;
  var init_constants2 = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/messenger/constants.js"() {
      AMPLITUDE_ORIGIN = "https://app.amplitude.com";
      AMPLITUDE_ORIGIN_EU = "https://app.eu.amplitude.com";
      AMPLITUDE_ORIGIN_STAGING = "https://apps.stag2.amplitude.com";
      AMPLITUDE_ORIGINS_MAP = {
        US: AMPLITUDE_ORIGIN,
        EU: AMPLITUDE_ORIGIN_EU,
        STAGING: AMPLITUDE_ORIGIN_STAGING
      };
      AMPLITUDE_BACKGROUND_CAPTURE_SCRIPT_URL = "https://cdn.amplitude.com/libs/background-capture-1.0.3.js.gz";
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/messenger/utils.js
  function generateUniqueId() {
    return "".concat(Date.now(), "-").concat(Math.random().toString(36).substr(2, 9));
  }
  var asyncLoadScript;
  var init_utils = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/messenger/utils.js"() {
      asyncLoadScript = function(url) {
        var existing = document.querySelector('script[src="'.concat(CSS.escape(url), '"]'));
        if (existing) {
          return Promise.resolve({ status: true });
        }
        return new Promise(function(resolve, reject) {
          var _a4;
          try {
            var scriptElement = document.createElement("script");
            scriptElement.type = "text/javascript";
            scriptElement.async = true;
            scriptElement.src = url;
            scriptElement.addEventListener("load", function() {
              resolve({ status: true });
            }, { once: true });
            scriptElement.addEventListener("error", function() {
              reject({
                status: false,
                message: "Failed to load the script ".concat(url)
              });
            });
            (_a4 = document.head) === null || _a4 === void 0 ? void 0 : _a4.appendChild(scriptElement);
          } catch (error) {
            reject(error);
          }
        });
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/messenger/base-window-messenger.js
  function isWindowMessenger(value) {
    return typeof value === "object" && value !== null && MESSENGER_BRAND in value && value[MESSENGER_BRAND] === true;
  }
  function getOrCreateWindowMessenger(options) {
    var globalScope3 = getGlobalScope();
    var existing = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3[MESSENGER_GLOBAL_KEY];
    if (isWindowMessenger(existing)) {
      return existing;
    }
    var messenger = new BaseWindowMessenger(options);
    if (globalScope3) {
      globalScope3[MESSENGER_GLOBAL_KEY] = messenger;
    }
    return messenger;
  }
  var _a, MESSENGER_BRAND, MESSENGER_GLOBAL_KEY, BaseWindowMessenger;
  var init_base_window_messenger = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/messenger/base-window-messenger.js"() {
      init_tslib_es6();
      init_global_scope();
      init_constants2();
      init_utils();
      MESSENGER_BRAND = "__AMPLITUDE_MESSENGER_INSTANCE__";
      MESSENGER_GLOBAL_KEY = "__AMPLITUDE_MESSENGER__";
      BaseWindowMessenger = /** @class */
      (function() {
        function BaseWindowMessenger2(_b) {
          var _c = _b === void 0 ? {} : _b, _d = _c.origin, origin = _d === void 0 ? AMPLITUDE_ORIGIN : _d;
          this[_a] = true;
          this.isSetup = false;
          this.messageHandler = null;
          this.requestCallbacks = {};
          this.actionHandlers = /* @__PURE__ */ new Map();
          this.pendingMessages = /* @__PURE__ */ new Map();
          this.scriptLoadPromises = /* @__PURE__ */ new Map();
          this.endpoint = origin;
        }
        BaseWindowMessenger2.prototype.notify = function(message) {
          var _b, _c, _d, _e;
          (_c = (_b = this.logger) === null || _b === void 0 ? void 0 : _b.debug) === null || _c === void 0 ? void 0 : _c.call(_b, "Message sent: ", JSON.stringify(message));
          (_e = (_d = window.opener) === null || _d === void 0 ? void 0 : _d.postMessage) === null || _e === void 0 ? void 0 : _e.call(_d, message, this.endpoint);
        };
        BaseWindowMessenger2.prototype.sendRequest = function(action, args, options) {
          var _this = this;
          if (options === void 0) {
            options = { timeout: 15e3 };
          }
          var id2 = generateUniqueId();
          var request = { id: id2, action, args };
          var promise = new Promise(function(resolve, reject) {
            _this.requestCallbacks[id2] = { resolve, reject };
            _this.notify(request);
            if (options.timeout > 0) {
              setTimeout(function() {
                reject(new Error("".concat(action, " timed out (id: ").concat(id2, ")")));
                delete _this.requestCallbacks[id2];
              }, options.timeout);
            }
          });
          return promise;
        };
        BaseWindowMessenger2.prototype.handleResponse = function(response) {
          var _b;
          if (!this.requestCallbacks[response.id]) {
            (_b = this.logger) === null || _b === void 0 ? void 0 : _b.warn("No callback found for request id: ".concat(response.id));
            return;
          }
          this.requestCallbacks[response.id].resolve(response.responseData);
          delete this.requestCallbacks[response.id];
        };
        BaseWindowMessenger2.prototype.registerActionHandler = function(action, handler) {
          var e_1, _b;
          var _c, _d;
          if (this.actionHandlers.has(action)) {
            (_d = (_c = this.logger) === null || _c === void 0 ? void 0 : _c.warn) === null || _d === void 0 ? void 0 : _d.call(_c, "Overwriting existing action handler for: ".concat(action));
          }
          this.actionHandlers.set(action, handler);
          var queued = this.pendingMessages.get(action);
          if (queued) {
            this.pendingMessages.delete(action);
            try {
              for (var queued_1 = __values(queued), queued_1_1 = queued_1.next(); !queued_1_1.done; queued_1_1 = queued_1.next()) {
                var data = queued_1_1.value;
                handler(data);
              }
            } catch (e_1_1) {
              e_1 = { error: e_1_1 };
            } finally {
              try {
                if (queued_1_1 && !queued_1_1.done && (_b = queued_1.return)) _b.call(queued_1);
              } finally {
                if (e_1) throw e_1.error;
              }
            }
          }
        };
        BaseWindowMessenger2.prototype.loadScriptOnce = function(url) {
          return __awaiter(this, void 0, void 0, function() {
            var existing, loadPromise, error_1;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  existing = this.scriptLoadPromises.get(url);
                  if (existing) {
                    return [2, existing];
                  }
                  loadPromise = asyncLoadScript(url).then(function() {
                  });
                  this.scriptLoadPromises.set(url, loadPromise);
                  _b.label = 1;
                case 1:
                  _b.trys.push([1, 3, , 4]);
                  return [4, loadPromise];
                case 2:
                  _b.sent();
                  return [3, 4];
                case 3:
                  error_1 = _b.sent();
                  this.scriptLoadPromises.delete(url);
                  throw error_1;
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        BaseWindowMessenger2.prototype.setup = function(_b) {
          var _this = this;
          var _c, _d;
          var _e = _b === void 0 ? {} : _b, logger = _e.logger, endpoint = _e.endpoint;
          if (logger) {
            this.logger = logger;
          }
          if (endpoint && this.endpoint === AMPLITUDE_ORIGIN) {
            this.endpoint = endpoint;
          }
          if (this.isSetup) {
            return;
          }
          this.isSetup = true;
          (_d = (_c = this.logger) === null || _c === void 0 ? void 0 : _c.debug) === null || _d === void 0 ? void 0 : _d.call(_c, "Setting up messenger");
          this.messageHandler = function(event) {
            var _b2, _c2, _d2, _e2, _f;
            (_c2 = (_b2 = _this.logger) === null || _b2 === void 0 ? void 0 : _b2.debug) === null || _c2 === void 0 ? void 0 : _c2.call(_b2, "Message received: ", JSON.stringify(event));
            if (_this.endpoint !== event.origin) {
              return;
            }
            var eventData = event.data;
            var action = eventData === null || eventData === void 0 ? void 0 : eventData.action;
            if (!action) {
              return;
            }
            if ("id" in eventData && eventData.id) {
              (_e2 = (_d2 = _this.logger) === null || _d2 === void 0 ? void 0 : _d2.debug) === null || _e2 === void 0 ? void 0 : _e2.call(_d2, "Received Response to previous request: ", JSON.stringify(event));
              _this.handleResponse(eventData);
            } else {
              if (action === "ping") {
                _this.notify({ action: "pong" });
                return;
              }
              var handler = _this.actionHandlers.get(action);
              if (handler) {
                handler(eventData.data);
              } else {
                var queue = (_f = _this.pendingMessages.get(action)) !== null && _f !== void 0 ? _f : [];
                queue.push(eventData.data);
                _this.pendingMessages.set(action, queue);
              }
            }
          };
          window.addEventListener("message", this.messageHandler);
          this.notify({ action: "page-loaded" });
        };
        BaseWindowMessenger2.prototype.destroy = function() {
          if (this.messageHandler) {
            window.removeEventListener("message", this.messageHandler);
            this.messageHandler = null;
          }
          this.isSetup = false;
          this.actionHandlers.clear();
          this.pendingMessages.clear();
          this.requestCallbacks = {};
          this.scriptLoadPromises.clear();
          var globalScope3 = getGlobalScope();
          if ((globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3[MESSENGER_GLOBAL_KEY]) === this) {
            delete globalScope3[MESSENGER_GLOBAL_KEY];
          }
        };
        return BaseWindowMessenger2;
      })();
      _a = MESSENGER_BRAND;
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/messenger/background-capture.js
  function enableBackgroundCapture(messenger, options) {
    var _a4;
    var branded = messenger;
    if (branded[BG_CAPTURE_BRAND] === true) {
      return;
    }
    branded[BG_CAPTURE_BRAND] = true;
    var scriptUrl = (_a4 = options === null || options === void 0 ? void 0 : options.scriptUrl) !== null && _a4 !== void 0 ? _a4 : AMPLITUDE_BACKGROUND_CAPTURE_SCRIPT_URL;
    var backgroundCaptureInstance = null;
    var onBackgroundCapture = function(type, backgroundCaptureData) {
      var _a5, _b;
      if (type === "background-capture-complete") {
        (_b = (_a5 = messenger.logger) === null || _a5 === void 0 ? void 0 : _a5.debug) === null || _b === void 0 ? void 0 : _b.call(_a5, "Background capture complete");
        messenger.notify({ action: "background-capture-complete", data: backgroundCaptureData });
      }
    };
    messenger.registerActionHandler("initialize-background-capture", function() {
      var _a5, _b;
      (_b = (_a5 = messenger.logger) === null || _a5 === void 0 ? void 0 : _a5.debug) === null || _b === void 0 ? void 0 : _b.call(_a5, "Initializing background capture (external script)");
      var resolvedUrl = new URL(scriptUrl, messenger.endpoint).toString();
      messenger.loadScriptOnce(resolvedUrl).then(function() {
        var _a6, _b2, _c;
        (_b2 = (_a6 = messenger.logger) === null || _a6 === void 0 ? void 0 : _a6.debug) === null || _b2 === void 0 ? void 0 : _b2.call(_a6, "Background capture script loaded (external)");
        backgroundCaptureInstance = /* istanbul ignore next -- window is always defined in browser */
        (_c = window === null || window === void 0 ? void 0 : window.amplitudeBackgroundCapture) === null || _c === void 0 ? void 0 : _c.call(window, {
          messenger,
          onBackgroundCapture
        });
        messenger.notify({ action: "background-capture-loaded" });
      }).catch(function() {
        var _a6;
        (_a6 = messenger.logger) === null || _a6 === void 0 ? void 0 : _a6.warn("Failed to initialize background capture");
      });
    });
    messenger.registerActionHandler("close-background-capture", function() {
      var _a5;
      (_a5 = backgroundCaptureInstance === null || backgroundCaptureInstance === void 0 ? void 0 : backgroundCaptureInstance.close) === null || _a5 === void 0 ? void 0 : _a5.call(backgroundCaptureInstance);
      backgroundCaptureInstance = null;
    });
  }
  var BG_CAPTURE_BRAND;
  var init_background_capture = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/messenger/background-capture.js"() {
      init_constants2();
      BG_CAPTURE_BRAND = "__AMPLITUDE_BACKGROUND_CAPTURE__";
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/types/config/browser-config.js
  var EXCLUDE_INTERNAL_REFERRERS_CONDITIONS;
  var init_browser_config = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/types/config/browser-config.js"() {
      EXCLUDE_INTERNAL_REFERRERS_CONDITIONS = {
        always: "always",
        ifEmptyCampaign: "ifEmptyCampaign"
      };
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/utils/environment.js
  function isChromeExtension() {
    var _a4, _b;
    var globalScope3 = getGlobalScope();
    return typeof ((_b = (_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.chrome) === null || _a4 === void 0 ? void 0 : _a4.runtime) === null || _b === void 0 ? void 0 : _b.id) === "string";
  }
  function isReactNative() {
    var _a4;
    var globalScope3 = getGlobalScope();
    return ((_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.navigator) === null || _a4 === void 0 ? void 0 : _a4.product) === "ReactNative";
  }
  var init_environment = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/utils/environment.js"() {
      init_global_scope();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/config/joined-config.js
  function mergeUrls(urlsExact, urlsRegex, logger) {
    var e_1, _a4;
    var regexList = [];
    try {
      for (var _b = __values(urlsRegex !== null && urlsRegex !== void 0 ? urlsRegex : []), _c = _b.next(); !_c.done; _c = _b.next()) {
        var pattern = _c.value;
        try {
          regexList.push(new RegExp(pattern));
        } catch (regexError) {
          logger.warn("Invalid regex pattern: ".concat(pattern), regexError);
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return urlsExact.concat(regexList);
  }
  function transformNetworkTrackingRemoteConfig(networkTracking, logger) {
    var e_2, _a4;
    var _b, _c, _d;
    if (typeof networkTracking !== "object" || networkTracking === null || !((_b = networkTracking.captureRules) === null || _b === void 0 ? void 0 : _b.length)) {
      return void 0;
    }
    var transformed = __assign({}, networkTracking);
    var captureRules = (_c = transformed.captureRules) !== null && _c !== void 0 ? _c : [];
    try {
      for (var captureRules_1 = __values(captureRules), captureRules_1_1 = captureRules_1.next(); !captureRules_1_1.done; captureRules_1_1 = captureRules_1.next()) {
        var rule3 = captureRules_1_1.value;
        rule3.urls = mergeUrls((_d = rule3.urls) !== null && _d !== void 0 ? _d : [], rule3.urlsRegex, logger);
        delete rule3.urlsRegex;
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (captureRules_1_1 && !captureRules_1_1.done && (_a4 = captureRules_1.return)) _a4.call(captureRules_1);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    return transformed;
  }
  function translateRemoteConfigToLocal(config2) {
    var e_3, _a4, e_4, _b, e_5, _c;
    var _d, _e, _f, _g, _h, _j;
    if (typeof config2 !== "object" || config2 === null) {
      return;
    }
    if (Array.isArray(config2)) {
      return;
    }
    var propertyNames = Object.keys(config2);
    try {
      for (var propertyNames_1 = __values(propertyNames), propertyNames_1_1 = propertyNames_1.next(); !propertyNames_1_1.done; propertyNames_1_1 = propertyNames_1.next()) {
        var propertyName = propertyNames_1_1.value;
        try {
          var value = config2[propertyName];
          if (typeof (value === null || value === void 0 ? void 0 : value.enabled) === "boolean") {
            if (value.enabled) {
              delete value.enabled;
              if (Object.keys(value).length === 0) {
                config2[propertyName] = true;
              }
            } else {
              config2[propertyName] = false;
            }
          }
          translateRemoteConfigToLocal(value);
        } catch (e2) {
        }
      }
    } catch (e_3_1) {
      e_3 = { error: e_3_1 };
    } finally {
      try {
        if (propertyNames_1_1 && !propertyNames_1_1.done && (_a4 = propertyNames_1.return)) _a4.call(propertyNames_1);
      } finally {
        if (e_3) throw e_3.error;
      }
    }
    try {
      if ((_f = (_e = (_d = config2.autocapture) === null || _d === void 0 ? void 0 : _d.networkTracking) === null || _e === void 0 ? void 0 : _e.captureRules) === null || _f === void 0 ? void 0 : _f.length) {
        try {
          for (var _k = __values(config2.autocapture.networkTracking.captureRules), _l = _k.next(); !_l.done; _l = _k.next()) {
            var rule3 = _l.value;
            try {
              for (var _m = (e_5 = void 0, __values(["responseHeaders", "requestHeaders"])), _o = _m.next(); !_o.done; _o = _m.next()) {
                var header = _o.value;
                var _p = (_g = rule3[header]) !== null && _g !== void 0 ? _g : {}, captureSafeHeaders = _p.captureSafeHeaders, allowlist = _p.allowlist;
                if (!captureSafeHeaders && !allowlist) {
                  continue;
                }
                if (allowlist !== void 0 && !Array.isArray(allowlist)) {
                  delete rule3[header];
                  continue;
                }
                rule3[header] = __spreadArray(__spreadArray([], __read(captureSafeHeaders ? SAFE_HEADERS : []), false), __read(allowlist !== null && allowlist !== void 0 ? allowlist : []), false);
              }
            } catch (e_5_1) {
              e_5 = { error: e_5_1 };
            } finally {
              try {
                if (_o && !_o.done && (_c = _m.return)) _c.call(_m);
              } finally {
                if (e_5) throw e_5.error;
              }
            }
          }
        } catch (e_4_1) {
          e_4 = { error: e_4_1 };
        } finally {
          try {
            if (_l && !_l.done && (_b = _k.return)) _b.call(_k);
          } finally {
            if (e_4) throw e_4.error;
          }
        }
      }
    } catch (e2) {
    }
    var frustrationInteractions = (_h = config2.autocapture) === null || _h === void 0 ? void 0 : _h.frustrationInteractions;
    if (frustrationInteractions) {
      if (frustrationInteractions.rageClick) {
        frustrationInteractions.rageClicks = frustrationInteractions.rageClick;
        delete frustrationInteractions.rageClick;
      }
      if (frustrationInteractions.deadClick) {
        frustrationInteractions.deadClicks = frustrationInteractions.deadClick;
        delete frustrationInteractions.deadClick;
      }
    }
    try {
      var elementInteractions = (_j = config2.autocapture) === null || _j === void 0 ? void 0 : _j.elementInteractions;
      if (elementInteractions && typeof elementInteractions === "object") {
        if (elementInteractions.viewportContentUpdated === true) {
          elementInteractions.viewportContentUpdated = {};
        }
        if (elementInteractions.viewportContentUpdated === false) {
          elementInteractions.viewportContentUpdated = { enabled: false };
        }
        if (elementInteractions.exposureDuration !== void 0) {
          var viewportContentUpdated = elementInteractions.viewportContentUpdated;
          if (viewportContentUpdated === void 0) {
            elementInteractions.viewportContentUpdated = { exposureDuration: elementInteractions.exposureDuration };
          } else if (typeof viewportContentUpdated === "object" && viewportContentUpdated.exposureDuration === void 0 && viewportContentUpdated.enabled !== false) {
            viewportContentUpdated.exposureDuration = elementInteractions.exposureDuration;
          }
          delete elementInteractions.exposureDuration;
        }
      }
    } catch (e2) {
    }
  }
  var init_joined_config = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/config/joined-config.js"() {
      init_tslib_es6();
      init_constants();
    }
  });

  // node_modules/@amplitude/analytics-core/lib/esm/index.js
  var init_esm = __esm({
    "node_modules/@amplitude/analytics-core/lib/esm/index.js"() {
      init_core_client();
      init_identify();
      init_revenue();
      init_destination();
      init_identity();
      init_config();
      init_logger();
      init_global_scope();
      init_analytics_connector();
      init_session();
      init_cookie_name();
      init_language();
      init_query_params();
      init_return_wrapper();
      init_debug();
      init_uuid();
      init_event_builder();
      init_url_utils();
      init_sampling();
      init_omit_undefined();
      init_memory();
      init_cookie();
      init_helpers();
      init_browser_storage();
      init_diagnostics_client();
      init_base();
      init_fetch();
      init_gzip();
      init_remote_config();
      init_loglevel();
      init_constants();
      init_console();
      init_event();
      init_server_zone();
      init_offline();
      init_element_interactions();
      init_frustration_interactions();
      init_status();
      init_network();
      init_network_tracking_config();
      init_constants();
      init_constants();
      init_campaign_parser();
      init_helpers2();
      init_observable();
      init_safe_stringify();
      init_json_query();
      init_base_window_messenger();
      init_background_capture();
      init_constants2();
      init_browser_config();
      init_environment();
      init_joined_config();
    }
  });

  // node_modules/idb/build/index.js
  function getIdbProxyableTypes() {
    return idbProxyableTypes || (idbProxyableTypes = [
      IDBDatabase,
      IDBObjectStore,
      IDBIndex,
      IDBCursor,
      IDBTransaction
    ]);
  }
  function getCursorAdvanceMethods() {
    return cursorAdvanceMethods || (cursorAdvanceMethods = [
      IDBCursor.prototype.advance,
      IDBCursor.prototype.continue,
      IDBCursor.prototype.continuePrimaryKey
    ]);
  }
  function promisifyRequest(request) {
    const promise = new Promise((resolve, reject) => {
      const unlisten = () => {
        request.removeEventListener("success", success);
        request.removeEventListener("error", error);
      };
      const success = () => {
        resolve(wrap(request.result));
        unlisten();
      };
      const error = () => {
        reject(request.error);
        unlisten();
      };
      request.addEventListener("success", success);
      request.addEventListener("error", error);
    });
    reverseTransformCache.set(promise, request);
    return promise;
  }
  function cacheDonePromiseForTransaction(tx) {
    if (transactionDoneMap.has(tx))
      return;
    const done = new Promise((resolve, reject) => {
      const unlisten = () => {
        tx.removeEventListener("complete", complete);
        tx.removeEventListener("error", error);
        tx.removeEventListener("abort", error);
      };
      const complete = () => {
        resolve();
        unlisten();
      };
      const error = () => {
        reject(tx.error || new DOMException("AbortError", "AbortError"));
        unlisten();
      };
      tx.addEventListener("complete", complete);
      tx.addEventListener("error", error);
      tx.addEventListener("abort", error);
    });
    transactionDoneMap.set(tx, done);
  }
  function replaceTraps(callback) {
    idbProxyTraps = callback(idbProxyTraps);
  }
  function wrapFunction(func) {
    if (getCursorAdvanceMethods().includes(func)) {
      return function(...args) {
        func.apply(unwrap(this), args);
        return wrap(this.request);
      };
    }
    return function(...args) {
      return wrap(func.apply(unwrap(this), args));
    };
  }
  function transformCachableValue(value) {
    if (typeof value === "function")
      return wrapFunction(value);
    if (value instanceof IDBTransaction)
      cacheDonePromiseForTransaction(value);
    if (instanceOfAny(value, getIdbProxyableTypes()))
      return new Proxy(value, idbProxyTraps);
    return value;
  }
  function wrap(value) {
    if (value instanceof IDBRequest)
      return promisifyRequest(value);
    if (transformCache.has(value))
      return transformCache.get(value);
    const newValue = transformCachableValue(value);
    if (newValue !== value) {
      transformCache.set(value, newValue);
      reverseTransformCache.set(newValue, value);
    }
    return newValue;
  }
  function openDB(name, version2, { blocked, upgrade, blocking, terminated } = {}) {
    const request = indexedDB.open(name, version2);
    const openPromise = wrap(request);
    if (upgrade) {
      request.addEventListener("upgradeneeded", (event) => {
        upgrade(wrap(request.result), event.oldVersion, event.newVersion, wrap(request.transaction), event);
      });
    }
    if (blocked) {
      request.addEventListener("blocked", (event) => blocked(
        // Casting due to https://github.com/microsoft/TypeScript-DOM-lib-generator/pull/1405
        event.oldVersion,
        event.newVersion,
        event
      ));
    }
    openPromise.then((db) => {
      if (terminated)
        db.addEventListener("close", () => terminated());
      if (blocking) {
        db.addEventListener("versionchange", (event) => blocking(event.oldVersion, event.newVersion, event));
      }
    }).catch(() => {
    });
    return openPromise;
  }
  function getMethod(target, prop) {
    if (!(target instanceof IDBDatabase && !(prop in target) && typeof prop === "string")) {
      return;
    }
    if (cachedMethods.get(prop))
      return cachedMethods.get(prop);
    const targetFuncName = prop.replace(/FromIndex$/, "");
    const useIndex = prop !== targetFuncName;
    const isWrite = writeMethods.includes(targetFuncName);
    if (
      // Bail if the target doesn't exist on the target. Eg, getAll isn't in Edge.
      !(targetFuncName in (useIndex ? IDBIndex : IDBObjectStore).prototype) || !(isWrite || readMethods.includes(targetFuncName))
    ) {
      return;
    }
    const method = async function(storeName, ...args) {
      const tx = this.transaction(storeName, isWrite ? "readwrite" : "readonly");
      let target2 = tx.store;
      if (useIndex)
        target2 = target2.index(args.shift());
      return (await Promise.all([
        target2[targetFuncName](...args),
        isWrite && tx.done
      ]))[0];
    };
    cachedMethods.set(prop, method);
    return method;
  }
  async function* iterate(...args) {
    let cursor = this;
    if (!(cursor instanceof IDBCursor)) {
      cursor = await cursor.openCursor(...args);
    }
    if (!cursor)
      return;
    cursor = cursor;
    const proxiedCursor = new Proxy(cursor, cursorIteratorTraps);
    ittrProxiedCursorToOriginalProxy.set(proxiedCursor, cursor);
    reverseTransformCache.set(proxiedCursor, unwrap(cursor));
    while (cursor) {
      yield proxiedCursor;
      cursor = await (advanceResults.get(proxiedCursor) || cursor.continue());
      advanceResults.delete(proxiedCursor);
    }
  }
  function isIteratorProp(target, prop) {
    return prop === Symbol.asyncIterator && instanceOfAny(target, [IDBIndex, IDBObjectStore, IDBCursor]) || prop === "iterate" && instanceOfAny(target, [IDBIndex, IDBObjectStore]);
  }
  var instanceOfAny, idbProxyableTypes, cursorAdvanceMethods, transactionDoneMap, transformCache, reverseTransformCache, idbProxyTraps, unwrap, readMethods, writeMethods, cachedMethods, advanceMethodProps, methodMap, advanceResults, ittrProxiedCursorToOriginalProxy, cursorIteratorTraps;
  var init_build = __esm({
    "node_modules/idb/build/index.js"() {
      instanceOfAny = (object, constructors) => constructors.some((c2) => object instanceof c2);
      transactionDoneMap = /* @__PURE__ */ new WeakMap();
      transformCache = /* @__PURE__ */ new WeakMap();
      reverseTransformCache = /* @__PURE__ */ new WeakMap();
      idbProxyTraps = {
        get(target, prop, receiver) {
          if (target instanceof IDBTransaction) {
            if (prop === "done")
              return transactionDoneMap.get(target);
            if (prop === "store") {
              return receiver.objectStoreNames[1] ? void 0 : receiver.objectStore(receiver.objectStoreNames[0]);
            }
          }
          return wrap(target[prop]);
        },
        set(target, prop, value) {
          target[prop] = value;
          return true;
        },
        has(target, prop) {
          if (target instanceof IDBTransaction && (prop === "done" || prop === "store")) {
            return true;
          }
          return prop in target;
        }
      };
      unwrap = (value) => reverseTransformCache.get(value);
      readMethods = ["get", "getKey", "getAll", "getAllKeys", "count"];
      writeMethods = ["put", "add", "delete", "clear"];
      cachedMethods = /* @__PURE__ */ new Map();
      replaceTraps((oldTraps) => ({
        ...oldTraps,
        get: (target, prop, receiver) => getMethod(target, prop) || oldTraps.get(target, prop, receiver),
        has: (target, prop) => !!getMethod(target, prop) || oldTraps.has(target, prop)
      }));
      advanceMethodProps = ["continue", "continuePrimaryKey", "advance"];
      methodMap = {};
      advanceResults = /* @__PURE__ */ new WeakMap();
      ittrProxiedCursorToOriginalProxy = /* @__PURE__ */ new WeakMap();
      cursorIteratorTraps = {
        get(target, prop) {
          if (!advanceMethodProps.includes(prop))
            return target[prop];
          let cachedFunc = methodMap[prop];
          if (!cachedFunc) {
            cachedFunc = methodMap[prop] = function(...args) {
              advanceResults.set(this, ittrProxiedCursorToOriginalProxy.get(this)[prop](...args));
            };
          }
          return cachedFunc;
        }
      };
      replaceTraps((oldTraps) => ({
        ...oldTraps,
        get(target, prop, receiver) {
          if (isIteratorProp(target, prop))
            return iterate;
          return oldTraps.get(target, prop, receiver);
        },
        has(target, prop) {
          return isIteratorProp(target, prop) || oldTraps.has(target, prop);
        }
      }));
    }
  });

  // node_modules/@amplitude/experiment-core/dist/experiment-core.esm.js
  function __extends2(d2, b2) {
    if (typeof b2 !== "function" && b2 !== null) throw new TypeError("Class extends value " + String(b2) + " is not a constructor or null");
    extendStatics2(d2, b2);
    function __() {
      this.constructor = d2;
    }
    d2.prototype = b2 === null ? Object.create(b2) : (__.prototype = b2.prototype, new __());
  }
  function __awaiter2(thisArg, _arguments, P2, generator) {
    function adopt(value) {
      return value instanceof P2 ? value : new P2(function(resolve) {
        resolve(value);
      });
    }
    return new (P2 || (P2 = Promise))(function(resolve, reject) {
      function fulfilled(value) {
        try {
          step(generator.next(value));
        } catch (e2) {
          reject(e2);
        }
      }
      function rejected(value) {
        try {
          step(generator["throw"](value));
        } catch (e2) {
          reject(e2);
        }
      }
      function step(result3) {
        result3.done ? resolve(result3.value) : adopt(result3.value).then(fulfilled, rejected);
      }
      step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
  }
  function __generator2(thisArg, body) {
    var _2 = {
      label: 0,
      sent: function() {
        if (t2[0] & 1) throw t2[1];
        return t2[1];
      },
      trys: [],
      ops: []
    }, f2, y2, t2, g2;
    return g2 = {
      next: verb(0),
      "throw": verb(1),
      "return": verb(2)
    }, typeof Symbol === "function" && (g2[Symbol.iterator] = function() {
      return this;
    }), g2;
    function verb(n4) {
      return function(v2) {
        return step([n4, v2]);
      };
    }
    function step(op) {
      if (f2) throw new TypeError("Generator is already executing.");
      while (g2 && (g2 = 0, op[0] && (_2 = 0)), _2) try {
        if (f2 = 1, y2 && (t2 = op[0] & 2 ? y2["return"] : op[0] ? y2["throw"] || ((t2 = y2["return"]) && t2.call(y2), 0) : y2.next) && !(t2 = t2.call(y2, op[1])).done) return t2;
        if (y2 = 0, t2) op = [op[0] & 2, t2.value];
        switch (op[0]) {
          case 0:
          case 1:
            t2 = op;
            break;
          case 4:
            _2.label++;
            return {
              value: op[1],
              done: false
            };
          case 5:
            _2.label++;
            y2 = op[1];
            op = [0];
            continue;
          case 7:
            op = _2.ops.pop();
            _2.trys.pop();
            continue;
          default:
            if (!(t2 = _2.trys, t2 = t2.length > 0 && t2[t2.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _2 = 0;
              continue;
            }
            if (op[0] === 3 && (!t2 || op[1] > t2[0] && op[1] < t2[3])) {
              _2.label = op[1];
              break;
            }
            if (op[0] === 6 && _2.label < t2[1]) {
              _2.label = t2[1];
              t2 = op;
              break;
            }
            if (t2 && _2.label < t2[2]) {
              _2.label = t2[2];
              _2.ops.push(op);
              break;
            }
            if (t2[2]) _2.ops.pop();
            _2.trys.pop();
            continue;
        }
        op = body.call(thisArg, _2);
      } catch (e2) {
        op = [6, e2];
        y2 = 0;
      } finally {
        f2 = t2 = 0;
      }
      if (op[0] & 5) throw op[1];
      return {
        value: op[0] ? op[1] : void 0,
        done: true
      };
    }
  }
  function __values2(o2) {
    var s2 = typeof Symbol === "function" && Symbol.iterator, m2 = s2 && o2[s2], i2 = 0;
    if (m2) return m2.call(o2);
    if (o2 && typeof o2.length === "number") return {
      next: function() {
        if (o2 && i2 >= o2.length) o2 = void 0;
        return {
          value: o2 && o2[i2++],
          done: !o2
        };
      }
    };
    throw new TypeError(s2 ? "Object is not iterable." : "Symbol.iterator is not defined.");
  }
  function __read2(o2, n4) {
    var m2 = typeof Symbol === "function" && o2[Symbol.iterator];
    if (!m2) return o2;
    var i2 = m2.call(o2), r2, ar = [], e2;
    try {
      while ((n4 === void 0 || n4-- > 0) && !(r2 = i2.next()).done) ar.push(r2.value);
    } catch (error) {
      e2 = {
        error
      };
    } finally {
      try {
        if (r2 && !r2.done && (m2 = i2["return"])) m2.call(i2);
      } finally {
        if (e2) throw e2.error;
      }
    }
    return ar;
  }
  function __spreadArray2(to, from, pack) {
    if (pack || arguments.length === 2) for (var i2 = 0, l2 = from.length, ar; i2 < l2; i2++) {
      if (ar || !(i2 in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i2);
        ar[i2] = from[i2];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  }
  var extendStatics2, __assign2, EvaluationOperator, stringToUtf8ByteArray, C1_32, C2_32, R1_32, R2_32, M_32, N_32, hash32x86, mix32, fmix32, rotateLeft, readIntLe, reverseBytes, select, MAJOR_MINOR_REGEX, PATCH_REGEX, PRERELEASE_REGEX, VERSION_PATTERN, SemanticVersion, EvaluationEngine, version, VERSION4, _hasatob, _hasbtoa, _hasBuffer, _TD, _TE, b64ch, b64chs, b64tab, b64re, _fromCC, _U8Afrom, _mkUriSafe, _tidyB64, btoaPolyfill, _btoa, _fromUint8Array, fromUint8Array, cb_utob, re_utob, utob, _encode, encode, encodeURI2, re_btou, cb_btou, btou, atobPolyfill, _atob, _toUint8Array, toUint8Array, _decode, _unURI, decode2, isValid, _noEnum, extendString, extendUint8Array, extendBuiltins, gBase64, FetchError, SdkEvaluationApi, SdkFlagApi, safeGlobal2, Poller;
  var init_experiment_core_esm = __esm({
    "node_modules/@amplitude/experiment-core/dist/experiment-core.esm.js"() {
      extendStatics2 = function(d2, b2) {
        extendStatics2 = Object.setPrototypeOf || {
          __proto__: []
        } instanceof Array && function(d3, b3) {
          d3.__proto__ = b3;
        } || function(d3, b3) {
          for (var p2 in b3) if (Object.prototype.hasOwnProperty.call(b3, p2)) d3[p2] = b3[p2];
        };
        return extendStatics2(d2, b2);
      };
      __assign2 = function() {
        __assign2 = Object.assign || function __assign3(t2) {
          for (var s2, i2 = 1, n4 = arguments.length; i2 < n4; i2++) {
            s2 = arguments[i2];
            for (var p2 in s2) if (Object.prototype.hasOwnProperty.call(s2, p2)) t2[p2] = s2[p2];
          }
          return t2;
        };
        return __assign2.apply(this, arguments);
      };
      EvaluationOperator = {
        IS: "is",
        IS_NOT: "is not",
        CONTAINS: "contains",
        DOES_NOT_CONTAIN: "does not contain",
        LESS_THAN: "less",
        LESS_THAN_EQUALS: "less or equal",
        GREATER_THAN: "greater",
        GREATER_THAN_EQUALS: "greater or equal",
        VERSION_LESS_THAN: "version less",
        VERSION_LESS_THAN_EQUALS: "version less or equal",
        VERSION_GREATER_THAN: "version greater",
        VERSION_GREATER_THAN_EQUALS: "version greater or equal",
        SET_IS: "set is",
        SET_IS_NOT: "set is not",
        SET_CONTAINS: "set contains",
        SET_DOES_NOT_CONTAIN: "set does not contain",
        SET_CONTAINS_ANY: "set contains any",
        SET_DOES_NOT_CONTAIN_ANY: "set does not contain any",
        REGEX_MATCH: "regex match",
        REGEX_DOES_NOT_MATCH: "regex does not match"
      };
      stringToUtf8ByteArray = function(str) {
        var out = [];
        var p2 = 0;
        for (var i2 = 0; i2 < str.length; i2++) {
          var c2 = str.charCodeAt(i2);
          if (c2 < 128) {
            out[p2++] = c2;
          } else if (c2 < 2048) {
            out[p2++] = c2 >> 6 | 192;
            out[p2++] = c2 & 63 | 128;
          } else if ((c2 & 64512) == 55296 && i2 + 1 < str.length && (str.charCodeAt(i2 + 1) & 64512) == 56320) {
            c2 = 65536 + ((c2 & 1023) << 10) + (str.charCodeAt(++i2) & 1023);
            out[p2++] = c2 >> 18 | 240;
            out[p2++] = c2 >> 12 & 63 | 128;
            out[p2++] = c2 >> 6 & 63 | 128;
            out[p2++] = c2 & 63 | 128;
          } else {
            out[p2++] = c2 >> 12 | 224;
            out[p2++] = c2 >> 6 & 63 | 128;
            out[p2++] = c2 & 63 | 128;
          }
        }
        return Uint8Array.from(out);
      };
      C1_32 = -862048943;
      C2_32 = 461845907;
      R1_32 = 15;
      R2_32 = 13;
      M_32 = 5;
      N_32 = -430675100;
      hash32x86 = function(input3, seed) {
        if (seed === void 0) {
          seed = 0;
        }
        var data = stringToUtf8ByteArray(input3);
        var length = data.length;
        var nBlocks = length >> 2;
        var hash = seed;
        for (var i2 = 0; i2 < nBlocks; i2++) {
          var index_1 = i2 << 2;
          var k2 = readIntLe(data, index_1);
          hash = mix32(k2, hash);
        }
        var index4 = nBlocks << 2;
        var k1 = 0;
        switch (length - index4) {
          case 3:
            k1 ^= data[index4 + 2] << 16;
            k1 ^= data[index4 + 1] << 8;
            k1 ^= data[index4];
            k1 = Math.imul(k1, C1_32);
            k1 = rotateLeft(k1, R1_32);
            k1 = Math.imul(k1, C2_32);
            hash ^= k1;
            break;
          case 2:
            k1 ^= data[index4 + 1] << 8;
            k1 ^= data[index4];
            k1 = Math.imul(k1, C1_32);
            k1 = rotateLeft(k1, R1_32);
            k1 = Math.imul(k1, C2_32);
            hash ^= k1;
            break;
          case 1:
            k1 ^= data[index4];
            k1 = Math.imul(k1, C1_32);
            k1 = rotateLeft(k1, R1_32);
            k1 = Math.imul(k1, C2_32);
            hash ^= k1;
            break;
        }
        hash ^= length;
        return fmix32(hash) >>> 0;
      };
      mix32 = function(k2, hash) {
        var kResult = k2;
        var hashResult = hash;
        kResult = Math.imul(kResult, C1_32);
        kResult = rotateLeft(kResult, R1_32);
        kResult = Math.imul(kResult, C2_32);
        hashResult ^= kResult;
        hashResult = rotateLeft(hashResult, R2_32);
        hashResult = Math.imul(hashResult, M_32);
        return hashResult + N_32 | 0;
      };
      fmix32 = function(hash) {
        var hashResult = hash;
        hashResult ^= hashResult >>> 16;
        hashResult = Math.imul(hashResult, -2048144789);
        hashResult ^= hashResult >>> 13;
        hashResult = Math.imul(hashResult, -1028477387);
        hashResult ^= hashResult >>> 16;
        return hashResult;
      };
      rotateLeft = function(x2, n4, width) {
        if (width === void 0) {
          width = 32;
        }
        if (n4 > width)
          n4 = n4 % width;
        var mask = 4294967295 << width - n4 >>> 0;
        var r2 = (x2 & mask) >>> 0 >>> width - n4 >>> 0;
        return (x2 << n4 | r2) >>> 0;
      };
      readIntLe = function(data, index4) {
        if (index4 === void 0) {
          index4 = 0;
        }
        var n4 = data[index4] << 24 | data[index4 + 1] << 16 | data[index4 + 2] << 8 | data[index4 + 3];
        return reverseBytes(n4);
      };
      reverseBytes = function(n4) {
        return (n4 & -16777216) >>> 24 | (n4 & 16711680) >>> 8 | (n4 & 65280) << 8 | (n4 & 255) << 24;
      };
      select = function(selectable, selector2) {
        var e_1, _a4;
        if (!selector2 || selector2.length === 0) {
          return void 0;
        }
        try {
          for (var selector_1 = __values2(selector2), selector_1_1 = selector_1.next(); !selector_1_1.done; selector_1_1 = selector_1.next()) {
            var selectorElement = selector_1_1.value;
            if (!selectorElement || !selectable || typeof selectable !== "object") {
              return void 0;
            }
            selectable = selectable[selectorElement];
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (selector_1_1 && !selector_1_1.done && (_a4 = selector_1.return)) _a4.call(selector_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        if (!selectable) {
          return void 0;
        } else {
          return selectable;
        }
      };
      MAJOR_MINOR_REGEX = "(\\d+)\\.(\\d+)";
      PATCH_REGEX = "(\\d+)";
      PRERELEASE_REGEX = "(-(([-\\w]+\\.?)*))?";
      VERSION_PATTERN = "^".concat(MAJOR_MINOR_REGEX, "(\\.").concat(PATCH_REGEX).concat(PRERELEASE_REGEX, ")?$");
      SemanticVersion = /** @class */
      (function() {
        function SemanticVersion2(major, minor, patch3, preRelease) {
          if (preRelease === void 0) {
            preRelease = void 0;
          }
          this.major = major;
          this.minor = minor;
          this.patch = patch3;
          this.preRelease = preRelease;
        }
        SemanticVersion2.parse = function(version2) {
          if (!version2) {
            return void 0;
          }
          var matchGroup = new RegExp(VERSION_PATTERN).exec(version2);
          if (!matchGroup) {
            return void 0;
          }
          var major = Number(matchGroup[1]);
          var minor = Number(matchGroup[2]);
          if (isNaN(major) || isNaN(minor)) {
            return void 0;
          }
          var patch3 = Number(matchGroup[4]) || 0;
          var preRelease = matchGroup[5] || void 0;
          return new SemanticVersion2(major, minor, patch3, preRelease);
        };
        SemanticVersion2.prototype.compareTo = function(other) {
          if (this.major > other.major)
            return 1;
          if (this.major < other.major)
            return -1;
          if (this.minor > other.minor)
            return 1;
          if (this.minor < other.minor)
            return -1;
          if (this.patch > other.patch)
            return 1;
          if (this.patch < other.patch)
            return -1;
          if (this.preRelease && !other.preRelease)
            return -1;
          if (!this.preRelease && other.preRelease)
            return 1;
          if (this.preRelease && other.preRelease) {
            if (this.preRelease > other.preRelease)
              return 1;
            if (this.preRelease < other.preRelease)
              return -1;
            return 0;
          }
          return 0;
        };
        return SemanticVersion2;
      })();
      EvaluationEngine = /** @class */
      (function() {
        function EvaluationEngine2() {
        }
        EvaluationEngine2.prototype.evaluate = function(context, flags) {
          var e_1, _a4;
          var results = {};
          var target = {
            context,
            result: results
          };
          try {
            for (var flags_1 = __values2(flags), flags_1_1 = flags_1.next(); !flags_1_1.done; flags_1_1 = flags_1.next()) {
              var flag = flags_1_1.value;
              var variant = this.evaluateFlag(target, flag);
              if (variant) {
                results[flag.key] = variant;
              }
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (flags_1_1 && !flags_1_1.done && (_a4 = flags_1.return)) _a4.call(flags_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          return results;
        };
        EvaluationEngine2.prototype.evaluateFlag = function(target, flag) {
          var e_2, _a4;
          var result3;
          try {
            for (var _b = __values2(flag.segments), _c = _b.next(); !_c.done; _c = _b.next()) {
              var segment = _c.value;
              result3 = this.evaluateSegment(target, flag, segment);
              if (result3) {
                var metadata = __assign2(__assign2(__assign2({}, flag.metadata), segment.metadata), result3.metadata);
                result3 = __assign2(__assign2({}, result3), { metadata });
                break;
              }
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
            } finally {
              if (e_2) throw e_2.error;
            }
          }
          return result3;
        };
        EvaluationEngine2.prototype.evaluateSegment = function(target, flag, segment) {
          var e_3, _a4, e_4, _b;
          if (!segment.conditions) {
            var variantKey = this.bucket(target, segment);
            if (variantKey !== void 0) {
              return flag.variants[variantKey];
            } else {
              return void 0;
            }
          }
          try {
            for (var _c = __values2(segment.conditions), _d = _c.next(); !_d.done; _d = _c.next()) {
              var conditions = _d.value;
              var match = true;
              try {
                for (var conditions_1 = (e_4 = void 0, __values2(conditions)), conditions_1_1 = conditions_1.next(); !conditions_1_1.done; conditions_1_1 = conditions_1.next()) {
                  var condition = conditions_1_1.value;
                  match = this.matchCondition(target, condition);
                  if (!match) {
                    break;
                  }
                }
              } catch (e_4_1) {
                e_4 = { error: e_4_1 };
              } finally {
                try {
                  if (conditions_1_1 && !conditions_1_1.done && (_b = conditions_1.return)) _b.call(conditions_1);
                } finally {
                  if (e_4) throw e_4.error;
                }
              }
              if (match) {
                var variantKey = this.bucket(target, segment);
                if (variantKey !== void 0) {
                  return flag.variants[variantKey];
                } else {
                  return void 0;
                }
              }
            }
          } catch (e_3_1) {
            e_3 = { error: e_3_1 };
          } finally {
            try {
              if (_d && !_d.done && (_a4 = _c.return)) _a4.call(_c);
            } finally {
              if (e_3) throw e_3.error;
            }
          }
          return void 0;
        };
        EvaluationEngine2.prototype.matchCondition = function(target, condition) {
          var propValue = select(target, condition.selector);
          if (!propValue) {
            return this.matchNull(condition.op, condition.values);
          } else if (this.isSetOperator(condition.op)) {
            var propValueStringList = this.coerceStringArray(propValue);
            if (!propValueStringList) {
              return false;
            }
            return this.matchSet(propValueStringList, condition.op, condition.values);
          } else {
            var propValueString = this.coerceString(propValue);
            if (propValueString !== void 0) {
              return this.matchString(propValueString, condition.op, condition.values);
            } else {
              return false;
            }
          }
        };
        EvaluationEngine2.prototype.getHash = function(key) {
          return hash32x86(key);
        };
        EvaluationEngine2.prototype.bucket = function(target, segment) {
          var e_5, _a4, e_6, _b;
          if (!segment.bucket) {
            return segment.variant;
          }
          var bucketingValue = this.coerceString(select(target, segment.bucket.selector));
          if (!bucketingValue || bucketingValue.length === 0) {
            return segment.variant;
          }
          var keyToHash = "".concat(segment.bucket.salt, "/").concat(bucketingValue);
          var hash = this.getHash(keyToHash);
          var allocationValue = hash % 100;
          var distributionValue = Math.floor(hash / 100);
          try {
            for (var _c = __values2(segment.bucket.allocations), _d = _c.next(); !_d.done; _d = _c.next()) {
              var allocation = _d.value;
              var allocationStart = allocation.range[0];
              var allocationEnd = allocation.range[1];
              if (allocationValue >= allocationStart && allocationValue < allocationEnd) {
                try {
                  for (var _e = (e_6 = void 0, __values2(allocation.distributions)), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var distribution = _f.value;
                    var distributionStart = distribution.range[0];
                    var distributionEnd = distribution.range[1];
                    if (distributionValue >= distributionStart && distributionValue < distributionEnd) {
                      return distribution.variant;
                    }
                  }
                } catch (e_6_1) {
                  e_6 = { error: e_6_1 };
                } finally {
                  try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                  } finally {
                    if (e_6) throw e_6.error;
                  }
                }
              }
            }
          } catch (e_5_1) {
            e_5 = { error: e_5_1 };
          } finally {
            try {
              if (_d && !_d.done && (_a4 = _c.return)) _a4.call(_c);
            } finally {
              if (e_5) throw e_5.error;
            }
          }
          return segment.variant;
        };
        EvaluationEngine2.prototype.matchNull = function(op, filterValues) {
          var containsNone = this.containsNone(filterValues);
          switch (op) {
            case EvaluationOperator.IS:
            case EvaluationOperator.CONTAINS:
            case EvaluationOperator.LESS_THAN:
            case EvaluationOperator.LESS_THAN_EQUALS:
            case EvaluationOperator.GREATER_THAN:
            case EvaluationOperator.GREATER_THAN_EQUALS:
            case EvaluationOperator.VERSION_LESS_THAN:
            case EvaluationOperator.VERSION_LESS_THAN_EQUALS:
            case EvaluationOperator.VERSION_GREATER_THAN:
            case EvaluationOperator.VERSION_GREATER_THAN_EQUALS:
            case EvaluationOperator.SET_IS:
            case EvaluationOperator.SET_CONTAINS:
            case EvaluationOperator.SET_CONTAINS_ANY:
              return containsNone;
            case EvaluationOperator.IS_NOT:
            case EvaluationOperator.DOES_NOT_CONTAIN:
            case EvaluationOperator.SET_DOES_NOT_CONTAIN:
            case EvaluationOperator.SET_DOES_NOT_CONTAIN_ANY:
              return !containsNone;
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.matchSet = function(propValues, op, filterValues) {
          switch (op) {
            case EvaluationOperator.SET_IS:
              return this.setEquals(propValues, filterValues);
            case EvaluationOperator.SET_IS_NOT:
              return !this.setEquals(propValues, filterValues);
            case EvaluationOperator.SET_CONTAINS:
              return this.matchesSetContainsAll(propValues, filterValues);
            case EvaluationOperator.SET_DOES_NOT_CONTAIN:
              return !this.matchesSetContainsAll(propValues, filterValues);
            case EvaluationOperator.SET_CONTAINS_ANY:
              return this.matchesSetContainsAny(propValues, filterValues);
            case EvaluationOperator.SET_DOES_NOT_CONTAIN_ANY:
              return !this.matchesSetContainsAny(propValues, filterValues);
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.matchString = function(propValue, op, filterValues) {
          var _this = this;
          switch (op) {
            case EvaluationOperator.IS:
              return this.matchesIs(propValue, filterValues);
            case EvaluationOperator.IS_NOT:
              return !this.matchesIs(propValue, filterValues);
            case EvaluationOperator.CONTAINS:
              return this.matchesContains(propValue, filterValues);
            case EvaluationOperator.DOES_NOT_CONTAIN:
              return !this.matchesContains(propValue, filterValues);
            case EvaluationOperator.LESS_THAN:
            case EvaluationOperator.LESS_THAN_EQUALS:
            case EvaluationOperator.GREATER_THAN:
            case EvaluationOperator.GREATER_THAN_EQUALS:
              return this.matchesComparable(propValue, op, filterValues, function(value) {
                return _this.parseNumber(value);
              }, this.comparator);
            case EvaluationOperator.VERSION_LESS_THAN:
            case EvaluationOperator.VERSION_LESS_THAN_EQUALS:
            case EvaluationOperator.VERSION_GREATER_THAN:
            case EvaluationOperator.VERSION_GREATER_THAN_EQUALS:
              return this.matchesComparable(propValue, op, filterValues, function(value) {
                return SemanticVersion.parse(value);
              }, this.versionComparator);
            case EvaluationOperator.REGEX_MATCH:
              return this.matchesRegex(propValue, filterValues);
            case EvaluationOperator.REGEX_DOES_NOT_MATCH:
              return !this.matchesRegex(propValue, filterValues);
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.matchesIs = function(propValue, filterValues) {
          if (this.containsBooleans(filterValues)) {
            var lower_1 = propValue.toLowerCase();
            if (lower_1 === "true" || lower_1 === "false") {
              return filterValues.some(function(value) {
                return value.toLowerCase() === lower_1;
              });
            }
          }
          return filterValues.some(function(value) {
            return propValue === value;
          });
        };
        EvaluationEngine2.prototype.matchesContains = function(propValue, filterValues) {
          var e_7, _a4;
          try {
            for (var filterValues_1 = __values2(filterValues), filterValues_1_1 = filterValues_1.next(); !filterValues_1_1.done; filterValues_1_1 = filterValues_1.next()) {
              var filterValue = filterValues_1_1.value;
              if (propValue.toLowerCase().includes(filterValue.toLowerCase())) {
                return true;
              }
            }
          } catch (e_7_1) {
            e_7 = { error: e_7_1 };
          } finally {
            try {
              if (filterValues_1_1 && !filterValues_1_1.done && (_a4 = filterValues_1.return)) _a4.call(filterValues_1);
            } finally {
              if (e_7) throw e_7.error;
            }
          }
          return false;
        };
        EvaluationEngine2.prototype.matchesComparable = function(propValue, op, filterValues, typeTransformer, typeComparator) {
          var _this = this;
          var propValueTransformed = typeTransformer(propValue);
          var filterValuesTransformed = filterValues.map(function(filterValue) {
            return typeTransformer(filterValue);
          }).filter(function(filterValue) {
            return filterValue !== void 0;
          });
          if (propValueTransformed === void 0 || filterValuesTransformed.length === 0) {
            return filterValues.some(function(filterValue) {
              return _this.comparator(propValue, op, filterValue);
            });
          } else {
            return filterValuesTransformed.some(function(filterValueTransformed) {
              return typeComparator(propValueTransformed, op, filterValueTransformed);
            });
          }
        };
        EvaluationEngine2.prototype.comparator = function(propValue, op, filterValue) {
          switch (op) {
            case EvaluationOperator.LESS_THAN:
            case EvaluationOperator.VERSION_LESS_THAN:
              return propValue < filterValue;
            case EvaluationOperator.LESS_THAN_EQUALS:
            case EvaluationOperator.VERSION_LESS_THAN_EQUALS:
              return propValue <= filterValue;
            case EvaluationOperator.GREATER_THAN:
            case EvaluationOperator.VERSION_GREATER_THAN:
              return propValue > filterValue;
            case EvaluationOperator.GREATER_THAN_EQUALS:
            case EvaluationOperator.VERSION_GREATER_THAN_EQUALS:
              return propValue >= filterValue;
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.versionComparator = function(propValue, op, filterValue) {
          var compareTo = propValue.compareTo(filterValue);
          switch (op) {
            case EvaluationOperator.LESS_THAN:
            case EvaluationOperator.VERSION_LESS_THAN:
              return compareTo < 0;
            case EvaluationOperator.LESS_THAN_EQUALS:
            case EvaluationOperator.VERSION_LESS_THAN_EQUALS:
              return compareTo <= 0;
            case EvaluationOperator.GREATER_THAN:
            case EvaluationOperator.VERSION_GREATER_THAN:
              return compareTo > 0;
            case EvaluationOperator.GREATER_THAN_EQUALS:
            case EvaluationOperator.VERSION_GREATER_THAN_EQUALS:
              return compareTo >= 0;
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.matchesRegex = function(propValue, filterValues) {
          return filterValues.some(function(filterValue) {
            return Boolean(new RegExp(filterValue).exec(propValue));
          });
        };
        EvaluationEngine2.prototype.containsNone = function(filterValues) {
          return filterValues.some(function(filterValue) {
            return filterValue === "(none)";
          });
        };
        EvaluationEngine2.prototype.containsBooleans = function(filterValues) {
          return filterValues.some(function(filterValue) {
            switch (filterValue.toLowerCase()) {
              case "true":
              case "false":
                return true;
              default:
                return false;
            }
          });
        };
        EvaluationEngine2.prototype.parseNumber = function(value) {
          var _a4;
          return (_a4 = Number(value)) !== null && _a4 !== void 0 ? _a4 : void 0;
        };
        EvaluationEngine2.prototype.coerceString = function(value) {
          if (!value) {
            return void 0;
          }
          if (typeof value === "object") {
            return JSON.stringify(value);
          }
          return String(value);
        };
        EvaluationEngine2.prototype.coerceStringArray = function(value) {
          var _this = this;
          if (Array.isArray(value)) {
            var anyArray = value;
            return anyArray.map(function(e2) {
              return _this.coerceString(e2);
            }).filter(Boolean);
          }
          var stringValue = String(value);
          try {
            var parsedValue = JSON.parse(stringValue);
            if (Array.isArray(parsedValue)) {
              var anyArray = value;
              return anyArray.map(function(e2) {
                return _this.coerceString(e2);
              }).filter(Boolean);
            } else {
              return void 0;
            }
          } catch (_a4) {
            return void 0;
          }
        };
        EvaluationEngine2.prototype.isSetOperator = function(op) {
          switch (op) {
            case EvaluationOperator.SET_IS:
            case EvaluationOperator.SET_IS_NOT:
            case EvaluationOperator.SET_CONTAINS:
            case EvaluationOperator.SET_DOES_NOT_CONTAIN:
            case EvaluationOperator.SET_CONTAINS_ANY:
            case EvaluationOperator.SET_DOES_NOT_CONTAIN_ANY:
              return true;
            default:
              return false;
          }
        };
        EvaluationEngine2.prototype.setEquals = function(xa, ya) {
          var xs = new Set(xa);
          var ys = new Set(ya);
          return xs.size === ys.size && __spreadArray2([], __read2(ys), false).every(function(y2) {
            return xs.has(y2);
          });
        };
        EvaluationEngine2.prototype.matchesSetContainsAll = function(propValues, filterValues) {
          var e_8, _a4;
          if (propValues.length < filterValues.length) {
            return false;
          }
          try {
            for (var filterValues_2 = __values2(filterValues), filterValues_2_1 = filterValues_2.next(); !filterValues_2_1.done; filterValues_2_1 = filterValues_2.next()) {
              var filterValue = filterValues_2_1.value;
              if (!this.matchesIs(filterValue, propValues)) {
                return false;
              }
            }
          } catch (e_8_1) {
            e_8 = { error: e_8_1 };
          } finally {
            try {
              if (filterValues_2_1 && !filterValues_2_1.done && (_a4 = filterValues_2.return)) _a4.call(filterValues_2);
            } finally {
              if (e_8) throw e_8.error;
            }
          }
          return true;
        };
        EvaluationEngine2.prototype.matchesSetContainsAny = function(propValues, filterValues) {
          var e_9, _a4;
          try {
            for (var filterValues_3 = __values2(filterValues), filterValues_3_1 = filterValues_3.next(); !filterValues_3_1.done; filterValues_3_1 = filterValues_3.next()) {
              var filterValue = filterValues_3_1.value;
              if (this.matchesIs(filterValue, propValues)) {
                return true;
              }
            }
          } catch (e_9_1) {
            e_9 = { error: e_9_1 };
          } finally {
            try {
              if (filterValues_3_1 && !filterValues_3_1.done && (_a4 = filterValues_3.return)) _a4.call(filterValues_3);
            } finally {
              if (e_9) throw e_9.error;
            }
          }
          return false;
        };
        return EvaluationEngine2;
      })();
      version = "3.7.5";
      VERSION4 = version;
      _hasatob = typeof atob === "function";
      _hasbtoa = typeof btoa === "function";
      _hasBuffer = typeof Buffer === "function";
      _TD = typeof TextDecoder === "function" ? new TextDecoder() : void 0;
      _TE = typeof TextEncoder === "function" ? new TextEncoder() : void 0;
      b64ch = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
      b64chs = Array.prototype.slice.call(b64ch);
      b64tab = ((a2) => {
        let tab = {};
        a2.forEach((c2, i2) => tab[c2] = i2);
        return tab;
      })(b64chs);
      b64re = /^(?:[A-Za-z\d+\/]{4})*?(?:[A-Za-z\d+\/]{2}(?:==)?|[A-Za-z\d+\/]{3}=?)?$/;
      _fromCC = String.fromCharCode.bind(String);
      _U8Afrom = typeof Uint8Array.from === "function" ? Uint8Array.from.bind(Uint8Array) : (it) => new Uint8Array(Array.prototype.slice.call(it, 0));
      _mkUriSafe = (src) => src.replace(/=/g, "").replace(/[+\/]/g, (m0) => m0 == "+" ? "-" : "_");
      _tidyB64 = (s2) => s2.replace(/[^A-Za-z0-9\+\/]/g, "");
      btoaPolyfill = (bin) => {
        let u32, c0, c1, c2, asc = "";
        const pad = bin.length % 3;
        for (let i2 = 0; i2 < bin.length; ) {
          if ((c0 = bin.charCodeAt(i2++)) > 255 || (c1 = bin.charCodeAt(i2++)) > 255 || (c2 = bin.charCodeAt(i2++)) > 255) throw new TypeError("invalid character found");
          u32 = c0 << 16 | c1 << 8 | c2;
          asc += b64chs[u32 >> 18 & 63] + b64chs[u32 >> 12 & 63] + b64chs[u32 >> 6 & 63] + b64chs[u32 & 63];
        }
        return pad ? asc.slice(0, pad - 3) + "===".substring(pad) : asc;
      };
      _btoa = _hasbtoa ? (bin) => btoa(bin) : _hasBuffer ? (bin) => Buffer.from(bin, "binary").toString("base64") : btoaPolyfill;
      _fromUint8Array = _hasBuffer ? (u8a) => Buffer.from(u8a).toString("base64") : (u8a) => {
        const maxargs = 4096;
        let strs = [];
        for (let i2 = 0, l2 = u8a.length; i2 < l2; i2 += maxargs) {
          strs.push(_fromCC.apply(null, u8a.subarray(i2, i2 + maxargs)));
        }
        return _btoa(strs.join(""));
      };
      fromUint8Array = (u8a, urlsafe = false) => urlsafe ? _mkUriSafe(_fromUint8Array(u8a)) : _fromUint8Array(u8a);
      cb_utob = (c2) => {
        if (c2.length < 2) {
          var cc = c2.charCodeAt(0);
          return cc < 128 ? c2 : cc < 2048 ? _fromCC(192 | cc >>> 6) + _fromCC(128 | cc & 63) : _fromCC(224 | cc >>> 12 & 15) + _fromCC(128 | cc >>> 6 & 63) + _fromCC(128 | cc & 63);
        } else {
          var cc = 65536 + (c2.charCodeAt(0) - 55296) * 1024 + (c2.charCodeAt(1) - 56320);
          return _fromCC(240 | cc >>> 18 & 7) + _fromCC(128 | cc >>> 12 & 63) + _fromCC(128 | cc >>> 6 & 63) + _fromCC(128 | cc & 63);
        }
      };
      re_utob = /[\uD800-\uDBFF][\uDC00-\uDFFFF]|[^\x00-\x7F]/g;
      utob = (u2) => u2.replace(re_utob, cb_utob);
      _encode = _hasBuffer ? (s2) => Buffer.from(s2, "utf8").toString("base64") : _TE ? (s2) => _fromUint8Array(_TE.encode(s2)) : (s2) => _btoa(utob(s2));
      encode = (src, urlsafe = false) => urlsafe ? _mkUriSafe(_encode(src)) : _encode(src);
      encodeURI2 = (src) => encode(src, true);
      re_btou = /[\xC0-\xDF][\x80-\xBF]|[\xE0-\xEF][\x80-\xBF]{2}|[\xF0-\xF7][\x80-\xBF]{3}/g;
      cb_btou = (cccc) => {
        switch (cccc.length) {
          case 4:
            var cp = (7 & cccc.charCodeAt(0)) << 18 | (63 & cccc.charCodeAt(1)) << 12 | (63 & cccc.charCodeAt(2)) << 6 | 63 & cccc.charCodeAt(3), offset = cp - 65536;
            return _fromCC((offset >>> 10) + 55296) + _fromCC((offset & 1023) + 56320);
          case 3:
            return _fromCC((15 & cccc.charCodeAt(0)) << 12 | (63 & cccc.charCodeAt(1)) << 6 | 63 & cccc.charCodeAt(2));
          default:
            return _fromCC((31 & cccc.charCodeAt(0)) << 6 | 63 & cccc.charCodeAt(1));
        }
      };
      btou = (b2) => b2.replace(re_btou, cb_btou);
      atobPolyfill = (asc) => {
        asc = asc.replace(/\s+/g, "");
        if (!b64re.test(asc)) throw new TypeError("malformed base64.");
        asc += "==".slice(2 - (asc.length & 3));
        let u24, bin = "", r1, r2;
        for (let i2 = 0; i2 < asc.length; ) {
          u24 = b64tab[asc.charAt(i2++)] << 18 | b64tab[asc.charAt(i2++)] << 12 | (r1 = b64tab[asc.charAt(i2++)]) << 6 | (r2 = b64tab[asc.charAt(i2++)]);
          bin += r1 === 64 ? _fromCC(u24 >> 16 & 255) : r2 === 64 ? _fromCC(u24 >> 16 & 255, u24 >> 8 & 255) : _fromCC(u24 >> 16 & 255, u24 >> 8 & 255, u24 & 255);
        }
        return bin;
      };
      _atob = _hasatob ? (asc) => atob(_tidyB64(asc)) : _hasBuffer ? (asc) => Buffer.from(asc, "base64").toString("binary") : atobPolyfill;
      _toUint8Array = _hasBuffer ? (a2) => _U8Afrom(Buffer.from(a2, "base64")) : (a2) => _U8Afrom(_atob(a2).split("").map((c2) => c2.charCodeAt(0)));
      toUint8Array = (a2) => _toUint8Array(_unURI(a2));
      _decode = _hasBuffer ? (a2) => Buffer.from(a2, "base64").toString("utf8") : _TD ? (a2) => _TD.decode(_toUint8Array(a2)) : (a2) => btou(_atob(a2));
      _unURI = (a2) => _tidyB64(a2.replace(/[-_]/g, (m0) => m0 == "-" ? "+" : "/"));
      decode2 = (src) => _decode(_unURI(src));
      isValid = (src) => {
        if (typeof src !== "string") return false;
        const s2 = src.replace(/\s+/g, "").replace(/={0,2}$/, "");
        return !/[^\s0-9a-zA-Z\+/]/.test(s2) || !/[^\s0-9a-zA-Z\-_]/.test(s2);
      };
      _noEnum = (v2) => {
        return {
          value: v2,
          enumerable: false,
          writable: true,
          configurable: true
        };
      };
      extendString = function() {
        const _add = (name, body) => Object.defineProperty(String.prototype, name, _noEnum(body));
        _add("fromBase64", function() {
          return decode2(this);
        });
        _add("toBase64", function(urlsafe) {
          return encode(this, urlsafe);
        });
        _add("toBase64URI", function() {
          return encode(this, true);
        });
        _add("toBase64URL", function() {
          return encode(this, true);
        });
        _add("toUint8Array", function() {
          return toUint8Array(this);
        });
      };
      extendUint8Array = function() {
        const _add = (name, body) => Object.defineProperty(Uint8Array.prototype, name, _noEnum(body));
        _add("toBase64", function(urlsafe) {
          return fromUint8Array(this, urlsafe);
        });
        _add("toBase64URI", function() {
          return fromUint8Array(this, true);
        });
        _add("toBase64URL", function() {
          return fromUint8Array(this, true);
        });
      };
      extendBuiltins = () => {
        extendString();
        extendUint8Array();
      };
      gBase64 = {
        version,
        VERSION: VERSION4,
        atob: _atob,
        atobPolyfill,
        btoa: _btoa,
        btoaPolyfill,
        fromBase64: decode2,
        toBase64: encode,
        encode,
        encodeURI: encodeURI2,
        encodeURL: encodeURI2,
        utob,
        btou,
        decode: decode2,
        isValid,
        fromUint8Array,
        toUint8Array,
        extendString,
        extendUint8Array,
        extendBuiltins
      };
      FetchError = /** @class */
      (function(_super) {
        __extends2(FetchError2, _super);
        function FetchError2(statusCode, message) {
          var _this = _super.call(this, message) || this;
          _this.statusCode = statusCode;
          Object.setPrototypeOf(_this, FetchError2.prototype);
          return _this;
        }
        return FetchError2;
      })(Error);
      SdkEvaluationApi = /** @class */
      (function() {
        function SdkEvaluationApi2(deploymentKey, serverUrl, httpClient) {
          this.deploymentKey = deploymentKey;
          this.serverUrl = serverUrl;
          this.httpClient = httpClient;
        }
        SdkEvaluationApi2.prototype.getVariants = function(user, options) {
          return __awaiter2(this, void 0, void 0, function() {
            var userJsonBase64, headers, response;
            return __generator2(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  userJsonBase64 = gBase64.encodeURL(JSON.stringify(user));
                  headers = {
                    Authorization: "Api-Key ".concat(this.deploymentKey),
                    "X-Amp-Exp-User": userJsonBase64
                  };
                  if (options === null || options === void 0 ? void 0 : options.flagKeys) {
                    headers["X-Amp-Exp-Flag-Keys"] = gBase64.encodeURL(JSON.stringify(options.flagKeys));
                  }
                  if (options === null || options === void 0 ? void 0 : options.trackingOption) {
                    headers["X-Amp-Exp-Track"] = options.trackingOption;
                  }
                  return [4, this.httpClient.request({
                    requestUrl: "".concat(this.serverUrl, "/sdk/v2/vardata?v=0"),
                    method: "GET",
                    headers,
                    timeoutMillis: options === null || options === void 0 ? void 0 : options.timeoutMillis
                  })];
                case 1:
                  response = _a4.sent();
                  if (response.status != 200) {
                    throw new FetchError(response.status, "Fetch error response: status=".concat(response.status));
                  }
                  return [2, JSON.parse(response.body)];
              }
            });
          });
        };
        return SdkEvaluationApi2;
      })();
      SdkFlagApi = /** @class */
      (function() {
        function SdkFlagApi2(deploymentKey, serverUrl, httpClient) {
          this.deploymentKey = deploymentKey;
          this.serverUrl = serverUrl;
          this.httpClient = httpClient;
        }
        SdkFlagApi2.prototype.getFlags = function(options) {
          return __awaiter2(this, void 0, void 0, function() {
            var headers, response, flagsArray;
            return __generator2(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  headers = {
                    Authorization: "Api-Key ".concat(this.deploymentKey)
                  };
                  if ((options === null || options === void 0 ? void 0 : options.libraryName) && (options === null || options === void 0 ? void 0 : options.libraryVersion)) {
                    headers["X-Amp-Exp-Library"] = "".concat(options.libraryName, "/").concat(options.libraryVersion);
                  }
                  return [4, this.httpClient.request({
                    requestUrl: "".concat(this.serverUrl, "/sdk/v2/flags"),
                    method: "GET",
                    headers,
                    timeoutMillis: options === null || options === void 0 ? void 0 : options.timeoutMillis
                  })];
                case 1:
                  response = _a4.sent();
                  if (response.status != 200) {
                    throw Error("Flags error response: status=".concat(response.status));
                  }
                  flagsArray = JSON.parse(response.body);
                  return [2, flagsArray.reduce(function(map, flag) {
                    map[flag.key] = flag;
                    return map;
                  }, {})];
              }
            });
          });
        };
        return SdkFlagApi2;
      })();
      safeGlobal2 = typeof globalThis !== "undefined" ? globalThis : global || self;
      Poller = /** @class */
      (function() {
        function Poller2(action, ms) {
          this.poller = void 0;
          this.action = action;
          this.ms = ms;
        }
        Poller2.prototype.start = function() {
          if (this.poller) {
            return;
          }
          this.poller = safeGlobal2.setInterval(this.action, this.ms);
          void this.action();
        };
        Poller2.prototype.stop = function() {
          if (!this.poller) {
            return;
          }
          safeGlobal2.clearInterval(this.poller);
          this.poller = void 0;
        };
        return Poller2;
      })();
    }
  });

  // node_modules/@amplitude/targeting/lib/esm/targeting-idb-store.js
  var MAX_IDB_STORAGE_LENGTH3, TargetingIDBStore2, targetingIDBStore2;
  var init_targeting_idb_store = __esm({
    "node_modules/@amplitude/targeting/lib/esm/targeting-idb-store.js"() {
      init_tslib_es6();
      init_build();
      MAX_IDB_STORAGE_LENGTH3 = 1e3 * 60 * 60 * 24 * 2;
      TargetingIDBStore2 = /** @class */
      /* @__PURE__ */ (function() {
        function TargetingIDBStore3() {
          var _this = this;
          this.dbs = {};
          this.createStore = function(dbName) {
            return __awaiter(_this, void 0, void 0, function() {
              return __generator(this, function(_a4) {
                switch (_a4.label) {
                  case 0:
                    return [4, openDB(dbName, 1, {
                      upgrade: function(db) {
                        if (!db.objectStoreNames.contains("eventTypesForSession")) {
                          db.createObjectStore("eventTypesForSession", {
                            keyPath: "sessionId"
                          });
                        }
                      }
                    })];
                  case 1:
                    return [2, _a4.sent()];
                }
              });
            });
          };
          this.openOrCreateDB = function(apiKey2) {
            return __awaiter(_this, void 0, void 0, function() {
              var dbName, db;
              return __generator(this, function(_a4) {
                switch (_a4.label) {
                  case 0:
                    if (this.dbs && this.dbs[apiKey2]) {
                      return [2, this.dbs[apiKey2]];
                    }
                    dbName = "".concat(apiKey2.substring(0, 10), "_amp_targeting");
                    return [4, this.createStore(dbName)];
                  case 1:
                    db = _a4.sent();
                    this.dbs[apiKey2] = db;
                    return [2, db];
                }
              });
            });
          };
          this.updateEventListForSession = function(_a4) {
            var sessionId = _a4.sessionId, eventType = _a4.eventType, eventTime = _a4.eventTime, loggerProvider = _a4.loggerProvider, tx = _a4.tx;
            return __awaiter(_this, void 0, void 0, function() {
              var sessionIdStr, sessionData, eventTypes, eventTypeStore, updatedEventTypes, updatedSessionData, e_1;
              var _b, _c;
              return __generator(this, function(_d) {
                switch (_d.label) {
                  case 0:
                    _d.trys.push([0, 3, , 4]);
                    sessionIdStr = String(sessionId);
                    return [4, tx.store.get(sessionIdStr)];
                  case 1:
                    sessionData = _d.sent();
                    eventTypes = sessionData ? sessionData.eventTypes : {};
                    eventTypeStore = eventTypes[eventType] || {};
                    updatedEventTypes = __assign(__assign({}, eventTypes), (_b = {}, _b[eventType] = __assign(__assign({}, eventTypeStore), (_c = {}, _c[eventTime] = { event_type: eventType }, _c)), _b));
                    updatedSessionData = {
                      sessionId: sessionIdStr,
                      eventTypes: updatedEventTypes,
                      lastUpdated: Date.now()
                    };
                    return [4, tx.store.put(updatedSessionData)];
                  case 2:
                    _d.sent();
                    return [2, updatedEventTypes];
                  case 3:
                    e_1 = _d.sent();
                    loggerProvider.warn("Failed to store events for targeting ".concat(sessionId, ": ").concat(e_1));
                    return [3, 4];
                  case 4:
                    return [2, void 0];
                }
              });
            });
          };
          this.deleteOldSessionEventTypes = function(_a4) {
            var currentSessionId = _a4.currentSessionId, loggerProvider = _a4.loggerProvider, tx = _a4.tx;
            return __awaiter(_this, void 0, void 0, function() {
              var currentSessionIdStr, allSessions, i2, session, amountOfTimeSinceSession, e_2;
              return __generator(this, function(_b) {
                switch (_b.label) {
                  case 0:
                    _b.trys.push([0, 6, , 7]);
                    currentSessionIdStr = String(currentSessionId);
                    return [4, tx.store.getAll()];
                  case 1:
                    allSessions = _b.sent();
                    i2 = 0;
                    _b.label = 2;
                  case 2:
                    if (!(i2 < allSessions.length)) return [3, 5];
                    session = allSessions[i2];
                    amountOfTimeSinceSession = Date.now() - session.lastUpdated;
                    if (!(session.sessionId !== currentSessionIdStr && amountOfTimeSinceSession > MAX_IDB_STORAGE_LENGTH3)) return [3, 4];
                    return [4, tx.store.delete(session.sessionId)];
                  case 3:
                    _b.sent();
                    _b.label = 4;
                  case 4:
                    i2++;
                    return [3, 2];
                  case 5:
                    return [3, 7];
                  case 6:
                    e_2 = _b.sent();
                    loggerProvider.warn("Failed to clear old session event types for targeting: ".concat(e_2));
                    return [3, 7];
                  case 7:
                    return [
                      2
                      /*return*/
                    ];
                }
              });
            });
          };
          this.storeEventTypeForSession = function(_a4) {
            var loggerProvider = _a4.loggerProvider, sessionId = _a4.sessionId, eventType = _a4.eventType, eventTime = _a4.eventTime, apiKey2 = _a4.apiKey;
            return __awaiter(_this, void 0, void 0, function() {
              var db, tx, updatedEventTypes, e_3;
              return __generator(this, function(_b) {
                switch (_b.label) {
                  case 0:
                    _b.trys.push([0, 5, , 6]);
                    return [4, this.openOrCreateDB(apiKey2)];
                  case 1:
                    db = _b.sent();
                    tx = db.transaction("eventTypesForSession", "readwrite");
                    if (!tx) {
                      return [
                        2
                        /*return*/
                      ];
                    }
                    return [4, this.updateEventListForSession({
                      sessionId,
                      tx,
                      loggerProvider,
                      eventType,
                      eventTime
                    })];
                  case 2:
                    updatedEventTypes = _b.sent();
                    return [4, this.deleteOldSessionEventTypes({ currentSessionId: sessionId, tx, loggerProvider })];
                  case 3:
                    _b.sent();
                    return [4, tx.done];
                  case 4:
                    _b.sent();
                    return [2, updatedEventTypes];
                  case 5:
                    e_3 = _b.sent();
                    loggerProvider.warn("Failed to store events for targeting ".concat(sessionId, ": ").concat(e_3));
                    return [3, 6];
                  case 6:
                    return [2, void 0];
                }
              });
            });
          };
        }
        return TargetingIDBStore3;
      })();
      targetingIDBStore2 = new TargetingIDBStore2();
    }
  });

  // node_modules/@amplitude/targeting/lib/esm/targeting.js
  var Targeting;
  var init_targeting = __esm({
    "node_modules/@amplitude/targeting/lib/esm/targeting.js"() {
      init_tslib_es6();
      init_experiment_core_esm();
      init_targeting_idb_store();
      Targeting = /** @class */
      /* @__PURE__ */ (function() {
        function Targeting2() {
          var _this = this;
          this.evaluateTargeting = function(_a4) {
            var apiKey2 = _a4.apiKey, loggerProvider = _a4.loggerProvider, event = _a4.event, sessionId = _a4.sessionId, userProperties = _a4.userProperties, deviceId = _a4.deviceId, page = _a4.page, flag = _a4.flag;
            return __awaiter(_this, void 0, void 0, function() {
              var eventTypes, _b, eventStrings, context, targetingBucket;
              return __generator(this, function(_c) {
                switch (_c.label) {
                  case 0:
                    if (!(event && event.time)) return [3, 2];
                    return [4, targetingIDBStore2.storeEventTypeForSession({
                      loggerProvider,
                      apiKey: apiKey2,
                      sessionId,
                      eventType: event.event_type,
                      eventTime: event.time
                    })];
                  case 1:
                    _b = _c.sent();
                    return [3, 3];
                  case 2:
                    _b = void 0;
                    _c.label = 3;
                  case 3:
                    eventTypes = _b;
                    eventStrings = eventTypes && new Set(Object.keys(eventTypes));
                    context = {
                      session_id: sessionId,
                      event,
                      event_types: eventStrings && Array.from(eventStrings),
                      user: __assign(__assign({}, userProperties), { device_id: deviceId, user_properties: userProperties }),
                      page
                    };
                    targetingBucket = this.evaluationEngine.evaluate(context, [flag]);
                    return [2, targetingBucket];
                }
              });
            });
          };
          this.evaluationEngine = new EvaluationEngine();
        }
        return Targeting2;
      })();
    }
  });

  // node_modules/@amplitude/targeting/lib/esm/targeting-factory.js
  var createInstance2, targeting_factory_default;
  var init_targeting_factory = __esm({
    "node_modules/@amplitude/targeting/lib/esm/targeting-factory.js"() {
      init_targeting();
      createInstance2 = function() {
        var targeting = new Targeting();
        return {
          evaluateTargeting: targeting.evaluateTargeting.bind(targeting)
        };
      };
      targeting_factory_default = createInstance2();
    }
  });

  // node_modules/@amplitude/targeting/lib/esm/index.js
  var esm_exports = {};
  __export(esm_exports, {
    evaluateTargeting: () => evaluateTargeting
  });
  var evaluateTargeting;
  var init_esm2 = __esm({
    "node_modules/@amplitude/targeting/lib/esm/index.js"() {
      init_targeting_factory();
      evaluateTargeting = targeting_factory_default.evaluateTargeting;
    }
  });

  // node_modules/@amplitude/session-replay-browser/lib/esm/worker/index.js
  var worker_exports = {};
  __export(worker_exports, {
    compressionScript: () => compressionScript,
    trackDestinationScript: () => trackDestinationScript
  });
  var compressionScript, trackDestinationScript;
  var init_worker = __esm({
    "node_modules/@amplitude/session-replay-browser/lib/esm/worker/index.js"() {
      compressionScript = 'var WebWorker=function(e){"use strict";onmessage=function(e){var s="string"==typeof e.data?JSON.parse(e.data):e.data,t=s.event,a=s.sessionId,n=t.type,r=t.timestamp,i=t.delay,o=t.data,d=null!=i?JSON.stringify({type:n,timestamp:r,delay:i,data:o}):JSON.stringify({type:n,timestamp:r,data:o});postMessage({compressedEvent:d,sessionId:a})};var s=onmessage;return e.compressionOnMessage=s,Object.defineProperty(e,"__esModule",{value:!0}),e}({});\n';
      trackDestinationScript = 'var WebWorker=function(e){"use strict";var t,r=function(){return r=Object.assign||function(e){for(var t,r=1,n=arguments.length;r<n;r++)for(var s in t=arguments[r])Object.prototype.hasOwnProperty.call(t,s)&&(e[s]=t[s]);return e},r.apply(this,arguments)};function n(e,t,r,n){return new(r||(r=Promise))(function(s,o){function a(e){try{u(n.next(e))}catch(e){o(e)}}function i(e){try{u(n.throw(e))}catch(e){o(e)}}function u(e){var t;e.done?s(e.value):(t=e.value,t instanceof r?t:new r(function(e){e(t)})).then(a,i)}u((n=n.apply(e,t||[])).next())})}function s(e,t){var r,n,s,o={label:0,sent:function(){if(1&s[0])throw s[1];return s[1]},trys:[],ops:[]},a=Object.create(("function"==typeof Iterator?Iterator:Object).prototype);return a.next=i(0),a.throw=i(1),a.return=i(2),"function"==typeof Symbol&&(a[Symbol.iterator]=function(){return this}),a;function i(i){return function(u){return function(i){if(r)throw new TypeError("Generator is already executing.");for(;a&&(a=0,i[0]&&(o=0)),o;)try{if(r=1,n&&(s=2&i[0]?n.return:i[0]?n.throw||((s=n.return)&&s.call(n),0):n.next)&&!(s=s.call(n,i[1])).done)return s;switch(n=0,s&&(i=[2&i[0],s.value]),i[0]){case 0:case 1:s=i;break;case 4:return o.label++,{value:i[1],done:!1};case 5:o.label++,n=i[1],i=[0];continue;case 7:i=o.ops.pop(),o.trys.pop();continue;default:if(!(s=o.trys,(s=s.length>0&&s[s.length-1])||6!==i[0]&&2!==i[0])){o=0;continue}if(3===i[0]&&(!s||i[1]>s[0]&&i[1]<s[3])){o.label=i[1];break}if(6===i[0]&&o.label<s[1]){o.label=s[1],s=i;break}if(s&&o.label<s[2]){o.label=s[2],o.ops.push(i);break}s[2]&&o.ops.pop(),o.trys.pop();continue}i=t.call(e,o)}catch(e){i=[6,e],n=0}finally{r=s=0}if(5&i[0])throw i[1];return{value:i[0]?i[1]:void 0,done:!0}}([i,u])}}}"function"==typeof SuppressedError&&SuppressedError,function(e){e.US="US",e.EU="EU",e.STAGING="STAGING"}(t||(t={})),t.US;var o,a=/payload.*exceed/i,i="X-Session-Replay-Event-Skipped";!function(e){e.GET_SR_PROPS="get-sr-props",e.DEBUG_INFO="debug-info",e.FETCH_REQUEST="fetch-request",e.METADATA="metadata",e.TARGETING_DECISION="targeting-decision",e.REPLAY_GATE_DECISION="replay-gate-decision"}(o||(o={}));var u="Network error occurred, event batch rejected";function c(e,t){return n(this,void 0,void 0,function(){var r,o,a,i,u,c,l,d,p,f,v,y,h,b,g=this;return s(this,function(m){switch(m.label){case 0:return m.trys.push([0,4,,5]),r=t.CompressionStream,o=new r("gzip"),a=o.writable.getWriter(),i=o.readable.getReader(),u=[],c=n(g,void 0,void 0,function(){var e,t,r;return s(this,function(n){switch(n.label){case 0:return[4,i.read()];case 1:if(e=n.sent(),t=e.done,r=e.value,t)return[3,3];u.push(r),n.label=2;case 2:return[3,0];case 3:return[2]}})}),[4,a.write((new TextEncoder).encode(e))];case 1:return m.sent(),[4,a.close()];case 2:return m.sent(),[4,c];case 3:m.sent(),l=u.reduce(function(e,t){return e+t.length},0),d=new Uint8Array(l),p=0;try{for(f=function(e){var t="function"==typeof Symbol&&Symbol.iterator,r=t&&e[t],n=0;if(r)return r.call(e);if(e&&"number"==typeof e.length)return{next:function(){return e&&n>=e.length&&(e=void 0),{value:e&&e[n++],done:!e}}};throw new TypeError(t?"Object is not iterable.":"Symbol.iterator is not defined.")}(u),v=f.next();!v.done;v=f.next())y=v.value,d.set(y,p),p+=y.length}catch(e){h={error:e}}finally{try{v&&!v.done&&(b=f.return)&&b.call(f)}finally{if(h)throw h.error}}return[2,d];case 4:return m.sent(),[2,null];case 5:return[2]}})})}var l=0,d=new Map,p=function(e,t){return fetch(e,t)},f=function(e,t){var r="".concat(++l);return new Promise(function(n,s){var o=t.signal,a=function(){d.delete(r);var e=new Error("Delegated session replay fetch aborted by send timeout");e.name="AbortError",s(e)};o.addEventListener("abort",a,{once:!0}),d.set(r,function(e){o.removeEventListener("abort",a),e.error?s(new Error(e.body)):n({status:e.status,headers:{get:function(t){return t===i?e.skipHeader:null}},text:function(){return Promise.resolve(e.body)}})}),postMessage({type:"fetch-request",requestId:r,url:e,method:"POST",headers:t.headers,body:t.body,keepalive:t.keepalive})})};function v(e,t,o){var l,d,p,f,v,y,h,b;return n(this,void 0,void 0,function(){var n,g,m,w,S,T,E,k,R,x,C,I,A,M,O;return s(this,function(s){switch(s.label){case 0:n=null!==(l=t.sendTimeoutMs)&&void 0!==l?l:1e4,s.label=1;case 1:return s.trys.push([1,14,,15]),!1!==t.enableTransportCompression&&"CompressionStream"in self?[4,c(e,self)]:[3,3];case 2:return m=s.sent(),[3,4];case 3:m=null,s.label=4;case 4:g=m,w="".concat(null!==(p=null===(d=t.version)||void 0===d?void 0:d.type)&&void 0!==p?p:"standalone","/").concat(null!==(v=null===(f=t.version)||void 0===f?void 0:f.version)&&void 0!==v?v:t.sdkVersion),S=new URLSearchParams({device_id:t.deviceId,session_id:"".concat(t.sessionId),type:t.eventType}),T="".concat((U=t.serverZone,P=t.trackServerUrl,P||("STAGING"===U?"https://api-sr.stag2.amplitude.com/sessions/v2/track":"EU"===U?"https://api-sr.eu.amplitude.com/sessions/v2/track":"https://api-sr.amplitude.com/sessions/v2/track")),"?").concat(S.toString()),E=r({"Content-Type":"application/json",Accept:"*/*",Authorization:"Bearer ".concat(t.apiKey),"X-Client-Version":t.sdkVersion,"X-Client-Library":w,"X-Client-Url":t.currentUrl.substring(0,1e3),"X-Client-Sample-Rate":"".concat(t.sampleRate),"X-Sampling-Hash-Alg":"xxhash32"},g?{"Content-Encoding":"gzip"}:{}),k=g?g.byteLength:new Blob([e]).size,R=new AbortController,x=n>0?setTimeout(function(){R.abort()},n):void 0,C=void 0,s.label=5;case 5:return s.trys.push([5,,7,8]),[4,o(T,{method:"POST",headers:E,body:null!=g?g:e,keepalive:k<=65536,signal:R.signal})];case 6:return C=s.sent(),[3,8];case 7:return x&&clearTimeout(x),[7];case 8:if(null===C)return[2,{shouldRetry:!1,success:!1,message:"Unexpected error occurred"}];if(C.status>=200&&C.status<300)return I=Math.round(new Blob(t.events).size/1024),A=null!==(b=null===(h=null===(y=C.headers)||void 0===y?void 0:y.get)||void 0===h?void 0:h.call(y,i))&&void 0!==b?b:null,[2,{shouldRetry:!1,success:!0,message:"Session replay event batch tracked successfully for session id ".concat(t.sessionId,", size of events: ").concat(I," KB"),skipCode:A}];if(413!==C.status)return[3,13];M="",s.label=9;case 9:return s.trys.push([9,11,,12]),[4,C.text()];case 10:return M=s.sent(),[3,12];case 11:return s.sent(),[3,12];case 12:return[2,{shouldRetry:!1,success:!1,message:u,payloadTooLarge:!0,isWaf:a.test(M)}];case 13:return C.status>=500||408===C.status||429===C.status||499===C.status?[2,{shouldRetry:!0,success:!1,message:"HTTP ".concat(C.status)}]:[2,{shouldRetry:!1,success:!1,message:u}];case 14:return(O=s.sent())&&"object"==typeof O&&"AbortError"===O.name?[2,{shouldRetry:!0,success:!1,message:"Session replay worker request timed out after ".concat(n,"ms")}]:[2,{shouldRetry:!1,success:!1,message:String(O)}];case 15:return[2]}var U,P})})}function y(e,t,r,o,a){var i;return n(this,void 0,void 0,function(){var n,u,c;return s(this,function(s){switch(s.label){case 0:n=1,s.label=1;case 1:return[4,v(t,r,a)];case 2:return(u=s.sent()).success?(postMessage({type:"log",id:e,message:u.message}),postMessage({type:"complete",id:e,skipCode:null!==(i=u.skipCode)&&void 0!==i?i:null}),[2]):u.payloadTooLarge&&o?(postMessage({type:"payload_too_large",id:e,isWaf:!0===u.isWaf}),[2]):o&&u.shouldRetry&&n<r.flushMaxRetries?[4,new Promise(function(e){return setTimeout(e,Math.random()*n*1e3)})]:[3,4];case 3:return s.sent(),n++,[3,5];case 4:return c=n>=r.flushMaxRetries?"Session replay event batch rejected due to exceeded retry count":u.message,postMessage({type:"warn",id:e,message:c}),postMessage({type:"complete",id:e}),[2];case 5:return[3,1];case 6:return[2]}})})}onmessage=function(e){return n(void 0,void 0,void 0,function(){var t,r,n,o,a,i,u,c;return s(this,function(s){switch(s.label){case 0:return"fetch-response"===(t=e.data).type?((r=d.get(t.requestId))&&(d.delete(t.requestId),r(t)),[2]):"send"!==t.type?[3,2]:(n=t.id,o=t.payload,a=t.context,i=t.useRetry,u=t.useCustomTransport,c=JSON.stringify(o),[4,y(n,c,a,i,u?f:p)]);case 1:s.sent(),s.label=2;case 2:return[2]}})})};var h=onmessage;return e.trackDestinationOnMessage=h,Object.defineProperty(e,"__esModule",{value:!0}),e}({});\n';
    }
  });

  // node_modules/@amplitude/rrweb-plugin-console-record/dist/rrweb-plugin-console-record.js
  var rrweb_plugin_console_record_exports = {};
  __export(rrweb_plugin_console_record_exports, {
    PLUGIN_NAME: () => PLUGIN_NAME4,
    getRecordConsolePlugin: () => getRecordConsolePlugin
  });
  function angularZoneUnpatchedAlternative$1(key) {
    var _a22, _b;
    const angularUnpatchedVersionSymbol = (_b = (_a22 = globalThis == null ? void 0 : globalThis.Zone) == null ? void 0 : _a22.__symbol__) == null ? void 0 : _b.call(_a22, key);
    if (angularUnpatchedVersionSymbol && globalThis[angularUnpatchedVersionSymbol]) {
      return globalThis[angularUnpatchedVersionSymbol];
    } else {
      return void 0;
    }
  }
  function getUntaintedPrototype$1(key) {
    if (untaintedBasePrototype$1[key])
      return untaintedBasePrototype$1[key];
    const candidate = angularZoneUnpatchedAlternative$1(key) || globalThis[key];
    const defaultPrototype = candidate.prototype;
    const accessorNames = key in testableAccessors$1 ? testableAccessors$1[key] : void 0;
    const isUntaintedAccessors = Boolean(
      accessorNames && // @ts-expect-error 2345
      accessorNames.every(
        (accessor) => {
          var _a22, _b;
          return Boolean(
            (_b = (_a22 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a22.get) == null ? void 0 : _b.toString().includes("[native code]")
          );
        }
      )
    );
    const methodNames = key in testableMethods$1 ? testableMethods$1[key] : void 0;
    const isUntaintedMethods = Boolean(
      methodNames && methodNames.every(
        // @ts-expect-error 2345
        (method) => {
          var _a22;
          return typeof defaultPrototype[method] === "function" && ((_a22 = defaultPrototype[method]) == null ? void 0 : _a22.toString().includes("[native code]"));
        }
      )
    );
    if (isUntaintedAccessors && isUntaintedMethods) {
      untaintedBasePrototype$1[key] = candidate.prototype;
      return candidate.prototype;
    }
    try {
      const iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      const win = iframeEl.contentWindow;
      if (!win) return candidate.prototype;
      const untaintedObject = win[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype$1[key] = untaintedObject;
    } catch {
      return defaultPrototype;
    }
  }
  function getUntaintedAccessor$1(key, instance, accessor) {
    var _a22;
    const cacheKey = `${key}.${String(accessor)}`;
    if (untaintedAccessorCache$1[cacheKey])
      return untaintedAccessorCache$1[cacheKey].call(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype$1(key);
    const untaintedAccessor = (_a22 = Object.getOwnPropertyDescriptor(
      untaintedPrototype,
      accessor
    )) == null ? void 0 : _a22.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache$1[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  function getUntaintedMethod$1(key, instance, method) {
    const cacheKey = `${key}.${String(method)}`;
    if (untaintedMethodCache$1[cacheKey])
      return untaintedMethodCache$1[cacheKey].bind(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype$1(key);
    const untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache$1[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "childNodes");
  }
  function parentNode$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "parentNode");
  }
  function parentElement$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "parentElement");
  }
  function textContent$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "textContent");
  }
  function contains$1(n22, other) {
    return getUntaintedMethod$1("Node", n22, "contains")(other);
  }
  function getRootNode$1(n22) {
    return getUntaintedMethod$1("Node", n22, "getRootNode")();
  }
  function host$1(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor$1("ShadowRoot", n22, "host");
  }
  function styleSheets$1(n22) {
    return n22.styleSheets;
  }
  function shadowRoot$1(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor$1("Element", n22, "shadowRoot");
  }
  function querySelector$1(n22, selectors) {
    return getUntaintedAccessor$1("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll$1(n22, selectors) {
    return getUntaintedAccessor$1("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor$1() {
    return getUntaintedPrototype$1("MutationObserver").constructor;
  }
  function isShadowRoot2(n22) {
    const hostEl = (
      // anchor and textarea elements also have a `host` property
      // but only shadow roots have a `mode` property
      n22 && "host" in n22 && "mode" in n22 && index$1.host(n22) || null
    );
    return Boolean(
      hostEl && "shadowRoot" in hostEl && index$1.shadowRoot(hostEl) === n22
    );
  }
  function createMirror$2() {
    return new Mirror();
  }
  function classMatchesRegex(node22, regex, checkAncestors) {
    if (!node22) return false;
    if (node22.nodeType !== node22.ELEMENT_NODE) {
      if (!checkAncestors) return false;
      return classMatchesRegex(index$1.parentNode(node22), regex, checkAncestors);
    }
    for (let eIndex = node22.classList.length; eIndex--; ) {
      const className = node22.classList[eIndex];
      if (regex.test(className)) {
        return true;
      }
    }
    if (!checkAncestors) return false;
    return classMatchesRegex(index$1.parentNode(node22), regex, checkAncestors);
  }
  function getDefaultExportFromCjs$1(x2) {
    return x2 && x2.__esModule && Object.prototype.hasOwnProperty.call(x2, "default") ? x2["default"] : x2;
  }
  function getAugmentedNamespace$1(n22) {
    if (Object.prototype.hasOwnProperty.call(n22, "__esModule")) return n22;
    var f2 = n22.default;
    if (typeof f2 == "function") {
      var a2 = function a22() {
        if (this instanceof a22) {
          return Reflect.construct(f2, arguments, this.constructor);
        }
        return f2.apply(this, arguments);
      };
      a2.prototype = f2.prototype;
    } else a2 = {};
    Object.defineProperty(a2, "__esModule", { value: true });
    Object.keys(n22).forEach(function(k2) {
      var d2 = Object.getOwnPropertyDescriptor(n22, k2);
      Object.defineProperty(a2, k2, d2.get ? d2 : {
        enumerable: true,
        get: function() {
          return n22[k2];
        }
      });
    });
    return a2;
  }
  function requirePicocolors_browser$1() {
    if (hasRequiredPicocolors_browser$1) return picocolors_browser$1.exports;
    hasRequiredPicocolors_browser$1 = 1;
    var x2 = String;
    var create = function() {
      return { isColorSupported: false, reset: x2, bold: x2, dim: x2, italic: x2, underline: x2, inverse: x2, hidden: x2, strikethrough: x2, black: x2, red: x2, green: x2, yellow: x2, blue: x2, magenta: x2, cyan: x2, white: x2, gray: x2, bgBlack: x2, bgRed: x2, bgGreen: x2, bgYellow: x2, bgBlue: x2, bgMagenta: x2, bgCyan: x2, bgWhite: x2 };
    };
    picocolors_browser$1.exports = create();
    picocolors_browser$1.exports.createColors = create;
    return picocolors_browser$1.exports;
  }
  function requireCssSyntaxError$1() {
    if (hasRequiredCssSyntaxError$1) return cssSyntaxError$1;
    hasRequiredCssSyntaxError$1 = 1;
    let pico = /* @__PURE__ */ requirePicocolors_browser$1();
    let terminalHighlight = require$$2$1;
    class CssSyntaxError extends Error {
      constructor(message, line2, column, source, file, plugin) {
        super(message);
        this.name = "CssSyntaxError";
        this.reason = message;
        if (file) {
          this.file = file;
        }
        if (source) {
          this.source = source;
        }
        if (plugin) {
          this.plugin = plugin;
        }
        if (typeof line2 !== "undefined" && typeof column !== "undefined") {
          if (typeof line2 === "number") {
            this.line = line2;
            this.column = column;
          } else {
            this.line = line2.line;
            this.column = line2.column;
            this.endLine = column.line;
            this.endColumn = column.column;
          }
        }
        this.setMessage();
        if (Error.captureStackTrace) {
          Error.captureStackTrace(this, CssSyntaxError);
        }
      }
      setMessage() {
        this.message = this.plugin ? this.plugin + ": " : "";
        this.message += this.file ? this.file : "<css input>";
        if (typeof this.line !== "undefined") {
          this.message += ":" + this.line + ":" + this.column;
        }
        this.message += ": " + this.reason;
      }
      showSourceCode(color) {
        if (!this.source) return "";
        let css = this.source;
        if (color == null) color = pico.isColorSupported;
        if (terminalHighlight) {
          if (color) css = terminalHighlight(css);
        }
        let lines = css.split(/\r?\n/);
        let start3 = Math.max(this.line - 3, 0);
        let end = Math.min(this.line + 2, lines.length);
        let maxWidth = String(end).length;
        let mark, aside;
        if (color) {
          let { bold, gray, red } = pico.createColors(true);
          mark = (text) => bold(red(text));
          aside = (text) => gray(text);
        } else {
          mark = aside = (str) => str;
        }
        return lines.slice(start3, end).map((line2, index22) => {
          let number = start3 + 1 + index22;
          let gutter = " " + (" " + number).slice(-maxWidth) + " | ";
          if (number === this.line) {
            let spacing = aside(gutter.replace(/\d/g, " ")) + line2.slice(0, this.column - 1).replace(/[^\t]/g, " ");
            return mark(">") + aside(gutter) + line2 + "\n " + spacing + mark("^");
          }
          return " " + aside(gutter) + line2;
        }).join("\n");
      }
      toString() {
        let code = this.showSourceCode();
        if (code) {
          code = "\n\n" + code + "\n";
        }
        return this.name + ": " + this.message + code;
      }
    }
    cssSyntaxError$1 = CssSyntaxError;
    CssSyntaxError.default = CssSyntaxError;
    return cssSyntaxError$1;
  }
  function requireSymbols$1() {
    if (hasRequiredSymbols$1) return symbols$1;
    hasRequiredSymbols$1 = 1;
    symbols$1.isClean = Symbol("isClean");
    symbols$1.my = Symbol("my");
    return symbols$1;
  }
  function requireStringifier$1() {
    if (hasRequiredStringifier$1) return stringifier$1;
    hasRequiredStringifier$1 = 1;
    const DEFAULT_RAW = {
      after: "\n",
      beforeClose: "\n",
      beforeComment: "\n",
      beforeDecl: "\n",
      beforeOpen: " ",
      beforeRule: "\n",
      colon: ": ",
      commentLeft: " ",
      commentRight: " ",
      emptyBody: "",
      indent: "    ",
      semicolon: false
    };
    function capitalize(str) {
      return str[0].toUpperCase() + str.slice(1);
    }
    class Stringifier {
      constructor(builder) {
        this.builder = builder;
      }
      atrule(node22, semicolon) {
        let name = "@" + node22.name;
        let params = node22.params ? this.rawValue(node22, "params") : "";
        if (typeof node22.raws.afterName !== "undefined") {
          name += node22.raws.afterName;
        } else if (params) {
          name += " ";
        }
        if (node22.nodes) {
          this.block(node22, name + params);
        } else {
          let end = (node22.raws.between || "") + (semicolon ? ";" : "");
          this.builder(name + params + end, node22);
        }
      }
      beforeAfter(node22, detect) {
        let value;
        if (node22.type === "decl") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (node22.type === "comment") {
          value = this.raw(node22, null, "beforeComment");
        } else if (detect === "before") {
          value = this.raw(node22, null, "beforeRule");
        } else {
          value = this.raw(node22, null, "beforeClose");
        }
        let buf = node22.parent;
        let depth = 0;
        while (buf && buf.type !== "root") {
          depth += 1;
          buf = buf.parent;
        }
        if (value.includes("\n")) {
          let indent = this.raw(node22, null, "indent");
          if (indent.length) {
            for (let step = 0; step < depth; step++) value += indent;
          }
        }
        return value;
      }
      block(node22, start3) {
        let between = this.raw(node22, "between", "beforeOpen");
        this.builder(start3 + between + "{", node22, "start");
        let after;
        if (node22.nodes && node22.nodes.length) {
          this.body(node22);
          after = this.raw(node22, "after");
        } else {
          after = this.raw(node22, "after", "emptyBody");
        }
        if (after) this.builder(after);
        this.builder("}", node22, "end");
      }
      body(node22) {
        let last = node22.nodes.length - 1;
        while (last > 0) {
          if (node22.nodes[last].type !== "comment") break;
          last -= 1;
        }
        let semicolon = this.raw(node22, "semicolon");
        for (let i2 = 0; i2 < node22.nodes.length; i2++) {
          let child = node22.nodes[i2];
          let before = this.raw(child, "before");
          if (before) this.builder(before);
          this.stringify(child, last !== i2 || semicolon);
        }
      }
      comment(node22) {
        let left = this.raw(node22, "left", "commentLeft");
        let right = this.raw(node22, "right", "commentRight");
        this.builder("/*" + left + node22.text + right + "*/", node22);
      }
      decl(node22, semicolon) {
        let between = this.raw(node22, "between", "colon");
        let string = node22.prop + between + this.rawValue(node22, "value");
        if (node22.important) {
          string += node22.raws.important || " !important";
        }
        if (semicolon) string += ";";
        this.builder(string, node22);
      }
      document(node22) {
        this.body(node22);
      }
      raw(node22, own, detect) {
        let value;
        if (!detect) detect = own;
        if (own) {
          value = node22.raws[own];
          if (typeof value !== "undefined") return value;
        }
        let parent = node22.parent;
        if (detect === "before") {
          if (!parent || parent.type === "root" && parent.first === node22) {
            return "";
          }
          if (parent && parent.type === "document") {
            return "";
          }
        }
        if (!parent) return DEFAULT_RAW[detect];
        let root22 = node22.root();
        if (!root22.rawCache) root22.rawCache = {};
        if (typeof root22.rawCache[detect] !== "undefined") {
          return root22.rawCache[detect];
        }
        if (detect === "before" || detect === "after") {
          return this.beforeAfter(node22, detect);
        } else {
          let method = "raw" + capitalize(detect);
          if (this[method]) {
            value = this[method](root22, node22);
          } else {
            root22.walk((i2) => {
              value = i2.raws[own];
              if (typeof value !== "undefined") return false;
            });
          }
        }
        if (typeof value === "undefined") value = DEFAULT_RAW[detect];
        root22.rawCache[detect] = value;
        return value;
      }
      rawBeforeClose(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length > 0) {
            if (typeof i2.raws.after !== "undefined") {
              value = i2.raws.after;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawBeforeComment(root22, node22) {
        let value;
        root22.walkComments((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeDecl(root22, node22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeRule");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeOpen(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.type !== "decl") {
            value = i2.raws.between;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawBeforeRule(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && (i2.parent !== root22 || root22.first !== i2)) {
            if (typeof i2.raws.before !== "undefined") {
              value = i2.raws.before;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawColon(root22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.between !== "undefined") {
            value = i2.raws.between.replace(/[^\s:]/g, "");
            return false;
          }
        });
        return value;
      }
      rawEmptyBody(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length === 0) {
            value = i2.raws.after;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawIndent(root22) {
        if (root22.raws.indent) return root22.raws.indent;
        let value;
        root22.walk((i2) => {
          let p2 = i2.parent;
          if (p2 && p2 !== root22 && p2.parent && p2.parent === root22) {
            if (typeof i2.raws.before !== "undefined") {
              let parts = i2.raws.before.split("\n");
              value = parts[parts.length - 1];
              value = value.replace(/\S/g, "");
              return false;
            }
          }
        });
        return value;
      }
      rawSemicolon(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length && i2.last.type === "decl") {
            value = i2.raws.semicolon;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawValue(node22, prop) {
        let value = node22[prop];
        let raw = node22.raws[prop];
        if (raw && raw.value === value) {
          return raw.raw;
        }
        return value;
      }
      root(node22) {
        this.body(node22);
        if (node22.raws.after) this.builder(node22.raws.after);
      }
      rule(node22) {
        this.block(node22, this.rawValue(node22, "selector"));
        if (node22.raws.ownSemicolon) {
          this.builder(node22.raws.ownSemicolon, node22, "end");
        }
      }
      stringify(node22, semicolon) {
        if (!this[node22.type]) {
          throw new Error(
            "Unknown AST node type " + node22.type + ". Maybe you need to change PostCSS stringifier."
          );
        }
        this[node22.type](node22, semicolon);
      }
    }
    stringifier$1 = Stringifier;
    Stringifier.default = Stringifier;
    return stringifier$1;
  }
  function requireStringify$1() {
    if (hasRequiredStringify$1) return stringify_1$1;
    hasRequiredStringify$1 = 1;
    let Stringifier = requireStringifier$1();
    function stringify2(node22, builder) {
      let str = new Stringifier(builder);
      str.stringify(node22);
    }
    stringify_1$1 = stringify2;
    stringify2.default = stringify2;
    return stringify_1$1;
  }
  function requireNode$1() {
    if (hasRequiredNode$1) return node$1;
    hasRequiredNode$1 = 1;
    let { isClean, my } = requireSymbols$1();
    let CssSyntaxError = requireCssSyntaxError$1();
    let Stringifier = requireStringifier$1();
    let stringify2 = requireStringify$1();
    function cloneNode(obj, parent) {
      let cloned = new obj.constructor();
      for (let i2 in obj) {
        if (!Object.prototype.hasOwnProperty.call(obj, i2)) {
          continue;
        }
        if (i2 === "proxyCache") continue;
        let value = obj[i2];
        let type = typeof value;
        if (i2 === "parent" && type === "object") {
          if (parent) cloned[i2] = parent;
        } else if (i2 === "source") {
          cloned[i2] = value;
        } else if (Array.isArray(value)) {
          cloned[i2] = value.map((j) => cloneNode(j, cloned));
        } else {
          if (type === "object" && value !== null) value = cloneNode(value);
          cloned[i2] = value;
        }
      }
      return cloned;
    }
    class Node2 {
      constructor(defaults = {}) {
        this.raws = {};
        this[isClean] = false;
        this[my] = true;
        for (let name in defaults) {
          if (name === "nodes") {
            this.nodes = [];
            for (let node22 of defaults[name]) {
              if (typeof node22.clone === "function") {
                this.append(node22.clone());
              } else {
                this.append(node22);
              }
            }
          } else {
            this[name] = defaults[name];
          }
        }
      }
      addToError(error) {
        error.postcssNode = this;
        if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
          let s2 = this.source;
          error.stack = error.stack.replace(
            /\n\s{4}at /,
            `$&${s2.input.from}:${s2.start.line}:${s2.start.column}$&`
          );
        }
        return error;
      }
      after(add2) {
        this.parent.insertAfter(this, add2);
        return this;
      }
      assign(overrides = {}) {
        for (let name in overrides) {
          this[name] = overrides[name];
        }
        return this;
      }
      before(add2) {
        this.parent.insertBefore(this, add2);
        return this;
      }
      cleanRaws(keepBetween) {
        delete this.raws.before;
        delete this.raws.after;
        if (!keepBetween) delete this.raws.between;
      }
      clone(overrides = {}) {
        let cloned = cloneNode(this);
        for (let name in overrides) {
          cloned[name] = overrides[name];
        }
        return cloned;
      }
      cloneAfter(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertAfter(this, cloned);
        return cloned;
      }
      cloneBefore(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertBefore(this, cloned);
        return cloned;
      }
      error(message, opts = {}) {
        if (this.source) {
          let { end, start: start3 } = this.rangeBy(opts);
          return this.source.input.error(
            message,
            { column: start3.column, line: start3.line },
            { column: end.column, line: end.line },
            opts
          );
        }
        return new CssSyntaxError(message);
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
            prop === "text") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      markDirty() {
        if (this[isClean]) {
          this[isClean] = false;
          let next = this;
          while (next = next.parent) {
            next[isClean] = false;
          }
        }
      }
      next() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 + 1];
      }
      positionBy(opts, stringRepresentation) {
        let pos = this.source.start;
        if (opts.index) {
          pos = this.positionInside(opts.index, stringRepresentation);
        } else if (opts.word) {
          stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) pos = this.positionInside(index22, stringRepresentation);
        }
        return pos;
      }
      positionInside(index22, stringRepresentation) {
        let string = stringRepresentation || this.toString();
        let column = this.source.start.column;
        let line2 = this.source.start.line;
        for (let i2 = 0; i2 < index22; i2++) {
          if (string[i2] === "\n") {
            column = 1;
            line2 += 1;
          } else {
            column += 1;
          }
        }
        return { column, line: line2 };
      }
      prev() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 - 1];
      }
      rangeBy(opts) {
        let start3 = {
          column: this.source.start.column,
          line: this.source.start.line
        };
        let end = this.source.end ? {
          column: this.source.end.column + 1,
          line: this.source.end.line
        } : {
          column: start3.column + 1,
          line: start3.line
        };
        if (opts.word) {
          let stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) {
            start3 = this.positionInside(index22, stringRepresentation);
            end = this.positionInside(index22 + opts.word.length, stringRepresentation);
          }
        } else {
          if (opts.start) {
            start3 = {
              column: opts.start.column,
              line: opts.start.line
            };
          } else if (opts.index) {
            start3 = this.positionInside(opts.index);
          }
          if (opts.end) {
            end = {
              column: opts.end.column,
              line: opts.end.line
            };
          } else if (typeof opts.endIndex === "number") {
            end = this.positionInside(opts.endIndex);
          } else if (opts.index) {
            end = this.positionInside(opts.index + 1);
          }
        }
        if (end.line < start3.line || end.line === start3.line && end.column <= start3.column) {
          end = { column: start3.column + 1, line: start3.line };
        }
        return { end, start: start3 };
      }
      raw(prop, defaultType) {
        let str = new Stringifier();
        return str.raw(this, prop, defaultType);
      }
      remove() {
        if (this.parent) {
          this.parent.removeChild(this);
        }
        this.parent = void 0;
        return this;
      }
      replaceWith(...nodes) {
        if (this.parent) {
          let bookmark = this;
          let foundSelf = false;
          for (let node22 of nodes) {
            if (node22 === this) {
              foundSelf = true;
            } else if (foundSelf) {
              this.parent.insertAfter(bookmark, node22);
              bookmark = node22;
            } else {
              this.parent.insertBefore(bookmark, node22);
            }
          }
          if (!foundSelf) {
            this.remove();
          }
        }
        return this;
      }
      root() {
        let result22 = this;
        while (result22.parent && result22.parent.type !== "document") {
          result22 = result22.parent;
        }
        return result22;
      }
      toJSON(_2, inputs) {
        let fixed = {};
        let emitInputs = inputs == null;
        inputs = inputs || /* @__PURE__ */ new Map();
        let inputsNextIndex = 0;
        for (let name in this) {
          if (!Object.prototype.hasOwnProperty.call(this, name)) {
            continue;
          }
          if (name === "parent" || name === "proxyCache") continue;
          let value = this[name];
          if (Array.isArray(value)) {
            fixed[name] = value.map((i2) => {
              if (typeof i2 === "object" && i2.toJSON) {
                return i2.toJSON(null, inputs);
              } else {
                return i2;
              }
            });
          } else if (typeof value === "object" && value.toJSON) {
            fixed[name] = value.toJSON(null, inputs);
          } else if (name === "source") {
            let inputId = inputs.get(value.input);
            if (inputId == null) {
              inputId = inputsNextIndex;
              inputs.set(value.input, inputsNextIndex);
              inputsNextIndex++;
            }
            fixed[name] = {
              end: value.end,
              inputId,
              start: value.start
            };
          } else {
            fixed[name] = value;
          }
        }
        if (emitInputs) {
          fixed.inputs = [...inputs.keys()].map((input22) => input22.toJSON());
        }
        return fixed;
      }
      toProxy() {
        if (!this.proxyCache) {
          this.proxyCache = new Proxy(this, this.getProxyProcessor());
        }
        return this.proxyCache;
      }
      toString(stringifier22 = stringify2) {
        if (stringifier22.stringify) stringifier22 = stringifier22.stringify;
        let result22 = "";
        stringifier22(this, (i2) => {
          result22 += i2;
        });
        return result22;
      }
      warn(result22, text, opts) {
        let data = { node: this };
        for (let i2 in opts) data[i2] = opts[i2];
        return result22.warn(text, data);
      }
      get proxyOf() {
        return this;
      }
    }
    node$1 = Node2;
    Node2.default = Node2;
    return node$1;
  }
  function requireDeclaration$1() {
    if (hasRequiredDeclaration$1) return declaration$1;
    hasRequiredDeclaration$1 = 1;
    let Node2 = requireNode$1();
    class Declaration extends Node2 {
      constructor(defaults) {
        if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
          defaults = { ...defaults, value: String(defaults.value) };
        }
        super(defaults);
        this.type = "decl";
      }
      get variable() {
        return this.prop.startsWith("--") || this.prop[0] === "$";
      }
    }
    declaration$1 = Declaration;
    Declaration.default = Declaration;
    return declaration$1;
  }
  function requireNonSecure$1() {
    if (hasRequiredNonSecure$1) return nonSecure$1;
    hasRequiredNonSecure$1 = 1;
    let urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
    let customAlphabet = (alphabet, defaultSize = 21) => {
      return (size = defaultSize) => {
        let id2 = "";
        let i2 = size;
        while (i2--) {
          id2 += alphabet[Math.random() * alphabet.length | 0];
        }
        return id2;
      };
    };
    let nanoid = (size = 21) => {
      let id2 = "";
      let i2 = size;
      while (i2--) {
        id2 += urlAlphabet[Math.random() * 64 | 0];
      }
      return id2;
    };
    nonSecure$1 = { nanoid, customAlphabet };
    return nonSecure$1;
  }
  function requirePreviousMap$1() {
    if (hasRequiredPreviousMap$1) return previousMap$1;
    hasRequiredPreviousMap$1 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$1;
    let { existsSync, readFileSync } = require$$2$1;
    let { dirname, join } = require$$2$1;
    function fromBase64(str) {
      if (Buffer) {
        return Buffer.from(str, "base64").toString();
      } else {
        return window.atob(str);
      }
    }
    class PreviousMap {
      constructor(css, opts) {
        if (opts.map === false) return;
        this.loadAnnotation(css);
        this.inline = this.startWith(this.annotation, "data:");
        let prev = opts.map ? opts.map.prev : void 0;
        let text = this.loadMap(opts.from, prev);
        if (!this.mapFile && opts.from) {
          this.mapFile = opts.from;
        }
        if (this.mapFile) this.root = dirname(this.mapFile);
        if (text) this.text = text;
      }
      consumer() {
        if (!this.consumerCache) {
          this.consumerCache = new SourceMapConsumer(this.text);
        }
        return this.consumerCache;
      }
      decodeInline(text) {
        let baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
        let baseUri = /^data:application\/json;base64,/;
        let charsetUri = /^data:application\/json;charset=utf-?8,/;
        let uri = /^data:application\/json,/;
        if (charsetUri.test(text) || uri.test(text)) {
          return decodeURIComponent(text.substr(RegExp.lastMatch.length));
        }
        if (baseCharsetUri.test(text) || baseUri.test(text)) {
          return fromBase64(text.substr(RegExp.lastMatch.length));
        }
        let encoding = text.match(/data:application\/json;([^,]+),/)[1];
        throw new Error("Unsupported source map encoding " + encoding);
      }
      getAnnotationURL(sourceMapString) {
        return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
      }
      isMap(map) {
        if (typeof map !== "object") return false;
        return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
      }
      loadAnnotation(css) {
        let comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
        if (!comments) return;
        let start3 = css.lastIndexOf(comments.pop());
        let end = css.indexOf("*/", start3);
        if (start3 > -1 && end > -1) {
          this.annotation = this.getAnnotationURL(css.substring(start3, end));
        }
      }
      loadFile(path) {
        this.root = dirname(path);
        if (existsSync(path)) {
          this.mapFile = path;
          return readFileSync(path, "utf-8").toString().trim();
        }
      }
      loadMap(file, prev) {
        if (prev === false) return false;
        if (prev) {
          if (typeof prev === "string") {
            return prev;
          } else if (typeof prev === "function") {
            let prevPath = prev(file);
            if (prevPath) {
              let map = this.loadFile(prevPath);
              if (!map) {
                throw new Error(
                  "Unable to load previous source map: " + prevPath.toString()
                );
              }
              return map;
            }
          } else if (prev instanceof SourceMapConsumer) {
            return SourceMapGenerator.fromSourceMap(prev).toString();
          } else if (prev instanceof SourceMapGenerator) {
            return prev.toString();
          } else if (this.isMap(prev)) {
            return JSON.stringify(prev);
          } else {
            throw new Error(
              "Unsupported previous source map format: " + prev.toString()
            );
          }
        } else if (this.inline) {
          return this.decodeInline(this.annotation);
        } else if (this.annotation) {
          let map = this.annotation;
          if (file) map = join(dirname(file), map);
          return this.loadFile(map);
        }
      }
      startWith(string, start3) {
        if (!string) return false;
        return string.substr(0, start3.length) === start3;
      }
      withContent() {
        return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
      }
    }
    previousMap$1 = PreviousMap;
    PreviousMap.default = PreviousMap;
    return previousMap$1;
  }
  function requireInput$1() {
    if (hasRequiredInput$1) return input$1;
    hasRequiredInput$1 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$1;
    let { fileURLToPath, pathToFileURL } = require$$2$1;
    let { isAbsolute, resolve } = require$$2$1;
    let { nanoid } = /* @__PURE__ */ requireNonSecure$1();
    let terminalHighlight = require$$2$1;
    let CssSyntaxError = requireCssSyntaxError$1();
    let PreviousMap = requirePreviousMap$1();
    let fromOffsetCache = Symbol("fromOffsetCache");
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(resolve && isAbsolute);
    class Input {
      constructor(css, opts = {}) {
        if (css === null || typeof css === "undefined" || typeof css === "object" && !css.toString) {
          throw new Error(`PostCSS received ${css} instead of CSS string`);
        }
        this.css = css.toString();
        if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
          this.hasBOM = true;
          this.css = this.css.slice(1);
        } else {
          this.hasBOM = false;
        }
        if (opts.from) {
          if (!pathAvailable || /^\w+:\/\//.test(opts.from) || isAbsolute(opts.from)) {
            this.file = opts.from;
          } else {
            this.file = resolve(opts.from);
          }
        }
        if (pathAvailable && sourceMapAvailable) {
          let map = new PreviousMap(this.css, opts);
          if (map.text) {
            this.map = map;
            let file = map.consumer().file;
            if (!this.file && file) this.file = this.mapResolve(file);
          }
        }
        if (!this.file) {
          this.id = "<input css " + nanoid(6) + ">";
        }
        if (this.map) this.map.file = this.from;
      }
      error(message, line2, column, opts = {}) {
        let result22, endLine, endColumn;
        if (line2 && typeof line2 === "object") {
          let start3 = line2;
          let end = column;
          if (typeof start3.offset === "number") {
            let pos = this.fromOffset(start3.offset);
            line2 = pos.line;
            column = pos.col;
          } else {
            line2 = start3.line;
            column = start3.column;
          }
          if (typeof end.offset === "number") {
            let pos = this.fromOffset(end.offset);
            endLine = pos.line;
            endColumn = pos.col;
          } else {
            endLine = end.line;
            endColumn = end.column;
          }
        } else if (!column) {
          let pos = this.fromOffset(line2);
          line2 = pos.line;
          column = pos.col;
        }
        let origin = this.origin(line2, column, endLine, endColumn);
        if (origin) {
          result22 = new CssSyntaxError(
            message,
            origin.endLine === void 0 ? origin.line : { column: origin.column, line: origin.line },
            origin.endLine === void 0 ? origin.column : { column: origin.endColumn, line: origin.endLine },
            origin.source,
            origin.file,
            opts.plugin
          );
        } else {
          result22 = new CssSyntaxError(
            message,
            endLine === void 0 ? line2 : { column, line: line2 },
            endLine === void 0 ? column : { column: endColumn, line: endLine },
            this.css,
            this.file,
            opts.plugin
          );
        }
        result22.input = { column, endColumn, endLine, line: line2, source: this.css };
        if (this.file) {
          if (pathToFileURL) {
            result22.input.url = pathToFileURL(this.file).toString();
          }
          result22.input.file = this.file;
        }
        return result22;
      }
      fromOffset(offset) {
        let lastLine, lineToIndex;
        if (!this[fromOffsetCache]) {
          let lines = this.css.split("\n");
          lineToIndex = new Array(lines.length);
          let prevIndex = 0;
          for (let i2 = 0, l2 = lines.length; i2 < l2; i2++) {
            lineToIndex[i2] = prevIndex;
            prevIndex += lines[i2].length + 1;
          }
          this[fromOffsetCache] = lineToIndex;
        } else {
          lineToIndex = this[fromOffsetCache];
        }
        lastLine = lineToIndex[lineToIndex.length - 1];
        let min = 0;
        if (offset >= lastLine) {
          min = lineToIndex.length - 1;
        } else {
          let max = lineToIndex.length - 2;
          let mid;
          while (min < max) {
            mid = min + (max - min >> 1);
            if (offset < lineToIndex[mid]) {
              max = mid - 1;
            } else if (offset >= lineToIndex[mid + 1]) {
              min = mid + 1;
            } else {
              min = mid;
              break;
            }
          }
        }
        return {
          col: offset - lineToIndex[min] + 1,
          line: min + 1
        };
      }
      mapResolve(file) {
        if (/^\w+:\/\//.test(file)) {
          return file;
        }
        return resolve(this.map.consumer().sourceRoot || this.map.root || ".", file);
      }
      origin(line2, column, endLine, endColumn) {
        if (!this.map) return false;
        let consumer = this.map.consumer();
        let from = consumer.originalPositionFor({ column, line: line2 });
        if (!from.source) return false;
        let to;
        if (typeof endLine === "number") {
          to = consumer.originalPositionFor({ column: endColumn, line: endLine });
        }
        let fromUrl;
        if (isAbsolute(from.source)) {
          fromUrl = pathToFileURL(from.source);
        } else {
          fromUrl = new URL(
            from.source,
            this.map.consumer().sourceRoot || pathToFileURL(this.map.mapFile)
          );
        }
        let result22 = {
          column: from.column,
          endColumn: to && to.column,
          endLine: to && to.line,
          line: from.line,
          url: fromUrl.toString()
        };
        if (fromUrl.protocol === "file:") {
          if (fileURLToPath) {
            result22.file = fileURLToPath(fromUrl);
          } else {
            throw new Error(`file: protocol is not available in this PostCSS build`);
          }
        }
        let source = consumer.sourceContentFor(from.source);
        if (source) result22.source = source;
        return result22;
      }
      toJSON() {
        let json = {};
        for (let name of ["hasBOM", "css", "file", "id"]) {
          if (this[name] != null) {
            json[name] = this[name];
          }
        }
        if (this.map) {
          json.map = { ...this.map };
          if (json.map.consumerCache) {
            json.map.consumerCache = void 0;
          }
        }
        return json;
      }
      get from() {
        return this.file || this.id;
      }
    }
    input$1 = Input;
    Input.default = Input;
    if (terminalHighlight && terminalHighlight.registerInput) {
      terminalHighlight.registerInput(Input);
    }
    return input$1;
  }
  function requireMapGenerator$1() {
    if (hasRequiredMapGenerator$1) return mapGenerator$1;
    hasRequiredMapGenerator$1 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$1;
    let { dirname, relative, resolve, sep } = require$$2$1;
    let { pathToFileURL } = require$$2$1;
    let Input = requireInput$1();
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(dirname && resolve && relative && sep);
    class MapGenerator {
      constructor(stringify2, root22, opts, cssString) {
        this.stringify = stringify2;
        this.mapOpts = opts.map || {};
        this.root = root22;
        this.opts = opts;
        this.css = cssString;
        this.originalCSS = cssString;
        this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
        this.memoizedFileURLs = /* @__PURE__ */ new Map();
        this.memoizedPaths = /* @__PURE__ */ new Map();
        this.memoizedURLs = /* @__PURE__ */ new Map();
      }
      addAnnotation() {
        let content;
        if (this.isInline()) {
          content = "data:application/json;base64," + this.toBase64(this.map.toString());
        } else if (typeof this.mapOpts.annotation === "string") {
          content = this.mapOpts.annotation;
        } else if (typeof this.mapOpts.annotation === "function") {
          content = this.mapOpts.annotation(this.opts.to, this.root);
        } else {
          content = this.outputFile() + ".map";
        }
        let eol = "\n";
        if (this.css.includes("\r\n")) eol = "\r\n";
        this.css += eol + "/*# sourceMappingURL=" + content + " */";
      }
      applyPrevMaps() {
        for (let prev of this.previous()) {
          let from = this.toUrl(this.path(prev.file));
          let root22 = prev.root || dirname(prev.file);
          let map;
          if (this.mapOpts.sourcesContent === false) {
            map = new SourceMapConsumer(prev.text);
            if (map.sourcesContent) {
              map.sourcesContent = null;
            }
          } else {
            map = prev.consumer();
          }
          this.map.applySourceMap(map, from, this.toUrl(this.path(root22)));
        }
      }
      clearAnnotation() {
        if (this.mapOpts.annotation === false) return;
        if (this.root) {
          let node22;
          for (let i2 = this.root.nodes.length - 1; i2 >= 0; i2--) {
            node22 = this.root.nodes[i2];
            if (node22.type !== "comment") continue;
            if (node22.text.indexOf("# sourceMappingURL=") === 0) {
              this.root.removeChild(i2);
            }
          }
        } else if (this.css) {
          this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
        }
      }
      generate() {
        this.clearAnnotation();
        if (pathAvailable && sourceMapAvailable && this.isMap()) {
          return this.generateMap();
        } else {
          let result22 = "";
          this.stringify(this.root, (i2) => {
            result22 += i2;
          });
          return [result22];
        }
      }
      generateMap() {
        if (this.root) {
          this.generateString();
        } else if (this.previous().length === 1) {
          let prev = this.previous()[0].consumer();
          prev.file = this.outputFile();
          this.map = SourceMapGenerator.fromSourceMap(prev, {
            ignoreInvalidMapping: true
          });
        } else {
          this.map = new SourceMapGenerator({
            file: this.outputFile(),
            ignoreInvalidMapping: true
          });
          this.map.addMapping({
            generated: { column: 0, line: 1 },
            original: { column: 0, line: 1 },
            source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
          });
        }
        if (this.isSourcesContent()) this.setSourcesContent();
        if (this.root && this.previous().length > 0) this.applyPrevMaps();
        if (this.isAnnotation()) this.addAnnotation();
        if (this.isInline()) {
          return [this.css];
        } else {
          return [this.css, this.map];
        }
      }
      generateString() {
        this.css = "";
        this.map = new SourceMapGenerator({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        let line2 = 1;
        let column = 1;
        let noSource = "<no source>";
        let mapping = {
          generated: { column: 0, line: 0 },
          original: { column: 0, line: 0 },
          source: ""
        };
        let lines, last;
        this.stringify(this.root, (str, node22, type) => {
          this.css += str;
          if (node22 && type !== "end") {
            mapping.generated.line = line2;
            mapping.generated.column = column - 1;
            if (node22.source && node22.source.start) {
              mapping.source = this.sourcePath(node22);
              mapping.original.line = node22.source.start.line;
              mapping.original.column = node22.source.start.column - 1;
              this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              this.map.addMapping(mapping);
            }
          }
          lines = str.match(/\n/g);
          if (lines) {
            line2 += lines.length;
            last = str.lastIndexOf("\n");
            column = str.length - last;
          } else {
            column += str.length;
          }
          if (node22 && type !== "start") {
            let p2 = node22.parent || { raws: {} };
            let childless = node22.type === "decl" || node22.type === "atrule" && !node22.nodes;
            if (!childless || node22 !== p2.last || p2.raws.semicolon) {
              if (node22.source && node22.source.end) {
                mapping.source = this.sourcePath(node22);
                mapping.original.line = node22.source.end.line;
                mapping.original.column = node22.source.end.column - 1;
                mapping.generated.line = line2;
                mapping.generated.column = column - 2;
                this.map.addMapping(mapping);
              } else {
                mapping.source = noSource;
                mapping.original.line = 1;
                mapping.original.column = 0;
                mapping.generated.line = line2;
                mapping.generated.column = column - 1;
                this.map.addMapping(mapping);
              }
            }
          }
        });
      }
      isAnnotation() {
        if (this.isInline()) {
          return true;
        }
        if (typeof this.mapOpts.annotation !== "undefined") {
          return this.mapOpts.annotation;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.annotation);
        }
        return true;
      }
      isInline() {
        if (typeof this.mapOpts.inline !== "undefined") {
          return this.mapOpts.inline;
        }
        let annotation = this.mapOpts.annotation;
        if (typeof annotation !== "undefined" && annotation !== true) {
          return false;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.inline);
        }
        return true;
      }
      isMap() {
        if (typeof this.opts.map !== "undefined") {
          return !!this.opts.map;
        }
        return this.previous().length > 0;
      }
      isSourcesContent() {
        if (typeof this.mapOpts.sourcesContent !== "undefined") {
          return this.mapOpts.sourcesContent;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.withContent());
        }
        return true;
      }
      outputFile() {
        if (this.opts.to) {
          return this.path(this.opts.to);
        } else if (this.opts.from) {
          return this.path(this.opts.from);
        } else {
          return "to.css";
        }
      }
      path(file) {
        if (this.mapOpts.absolute) return file;
        if (file.charCodeAt(0) === 60) return file;
        if (/^\w+:\/\//.test(file)) return file;
        let cached = this.memoizedPaths.get(file);
        if (cached) return cached;
        let from = this.opts.to ? dirname(this.opts.to) : ".";
        if (typeof this.mapOpts.annotation === "string") {
          from = dirname(resolve(from, this.mapOpts.annotation));
        }
        let path = relative(from, file);
        this.memoizedPaths.set(file, path);
        return path;
      }
      previous() {
        if (!this.previousMaps) {
          this.previousMaps = [];
          if (this.root) {
            this.root.walk((node22) => {
              if (node22.source && node22.source.input.map) {
                let map = node22.source.input.map;
                if (!this.previousMaps.includes(map)) {
                  this.previousMaps.push(map);
                }
              }
            });
          } else {
            let input22 = new Input(this.originalCSS, this.opts);
            if (input22.map) this.previousMaps.push(input22.map);
          }
        }
        return this.previousMaps;
      }
      setSourcesContent() {
        let already = {};
        if (this.root) {
          this.root.walk((node22) => {
            if (node22.source) {
              let from = node22.source.input.from;
              if (from && !already[from]) {
                already[from] = true;
                let fromUrl = this.usesFileUrls ? this.toFileUrl(from) : this.toUrl(this.path(from));
                this.map.setSourceContent(fromUrl, node22.source.input.css);
              }
            }
          });
        } else if (this.css) {
          let from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
          this.map.setSourceContent(from, this.css);
        }
      }
      sourcePath(node22) {
        if (this.mapOpts.from) {
          return this.toUrl(this.mapOpts.from);
        } else if (this.usesFileUrls) {
          return this.toFileUrl(node22.source.input.from);
        } else {
          return this.toUrl(this.path(node22.source.input.from));
        }
      }
      toBase64(str) {
        if (Buffer) {
          return Buffer.from(str).toString("base64");
        } else {
          return window.btoa(unescape(encodeURIComponent(str)));
        }
      }
      toFileUrl(path) {
        let cached = this.memoizedFileURLs.get(path);
        if (cached) return cached;
        if (pathToFileURL) {
          let fileURL = pathToFileURL(path).toString();
          this.memoizedFileURLs.set(path, fileURL);
          return fileURL;
        } else {
          throw new Error(
            "`map.absolute` option is not available in this PostCSS build"
          );
        }
      }
      toUrl(path) {
        let cached = this.memoizedURLs.get(path);
        if (cached) return cached;
        if (sep === "\\") {
          path = path.replace(/\\/g, "/");
        }
        let url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
        this.memoizedURLs.set(path, url);
        return url;
      }
    }
    mapGenerator$1 = MapGenerator;
    return mapGenerator$1;
  }
  function requireComment$1() {
    if (hasRequiredComment$1) return comment$1;
    hasRequiredComment$1 = 1;
    let Node2 = requireNode$1();
    class Comment extends Node2 {
      constructor(defaults) {
        super(defaults);
        this.type = "comment";
      }
    }
    comment$1 = Comment;
    Comment.default = Comment;
    return comment$1;
  }
  function requireContainer$1() {
    if (hasRequiredContainer$1) return container$1;
    hasRequiredContainer$1 = 1;
    let { isClean, my } = requireSymbols$1();
    let Declaration = requireDeclaration$1();
    let Comment = requireComment$1();
    let Node2 = requireNode$1();
    let parse, Rule, AtRule, Root;
    function cleanSource(nodes) {
      return nodes.map((i2) => {
        if (i2.nodes) i2.nodes = cleanSource(i2.nodes);
        delete i2.source;
        return i2;
      });
    }
    function markDirtyUp(node22) {
      node22[isClean] = false;
      if (node22.proxyOf.nodes) {
        for (let i2 of node22.proxyOf.nodes) {
          markDirtyUp(i2);
        }
      }
    }
    class Container extends Node2 {
      append(...children) {
        for (let child of children) {
          let nodes = this.normalize(child, this.last);
          for (let node22 of nodes) this.proxyOf.nodes.push(node22);
        }
        this.markDirty();
        return this;
      }
      cleanRaws(keepBetween) {
        super.cleanRaws(keepBetween);
        if (this.nodes) {
          for (let node22 of this.nodes) node22.cleanRaws(keepBetween);
        }
      }
      each(callback) {
        if (!this.proxyOf.nodes) return void 0;
        let iterator = this.getIterator();
        let index22, result22;
        while (this.indexes[iterator] < this.proxyOf.nodes.length) {
          index22 = this.indexes[iterator];
          result22 = callback(this.proxyOf.nodes[index22], index22);
          if (result22 === false) break;
          this.indexes[iterator] += 1;
        }
        delete this.indexes[iterator];
        return result22;
      }
      every(condition) {
        return this.nodes.every(condition);
      }
      getIterator() {
        if (!this.lastEach) this.lastEach = 0;
        if (!this.indexes) this.indexes = {};
        this.lastEach += 1;
        let iterator = this.lastEach;
        this.indexes[iterator] = 0;
        return iterator;
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (!node22[prop]) {
              return node22[prop];
            } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
              return (...args) => {
                return node22[prop](
                  ...args.map((i2) => {
                    if (typeof i2 === "function") {
                      return (child, index22) => i2(child.toProxy(), index22);
                    } else {
                      return i2;
                    }
                  })
                );
              };
            } else if (prop === "every" || prop === "some") {
              return (cb) => {
                return node22[prop](
                  (child, ...other) => cb(child.toProxy(), ...other)
                );
              };
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else if (prop === "nodes") {
              return node22.nodes.map((i2) => i2.toProxy());
            } else if (prop === "first" || prop === "last") {
              return node22[prop].toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "name" || prop === "params" || prop === "selector") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      index(child) {
        if (typeof child === "number") return child;
        if (child.proxyOf) child = child.proxyOf;
        return this.proxyOf.nodes.indexOf(child);
      }
      insertAfter(exist, add2) {
        let existIndex = this.index(exist);
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex]).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex + 1, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex < index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      insertBefore(exist, add2) {
        let existIndex = this.index(exist);
        let type = existIndex === 0 ? "prepend" : false;
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex], type).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex <= index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      normalize(nodes, sample) {
        if (typeof nodes === "string") {
          nodes = cleanSource(parse(nodes).nodes);
        } else if (typeof nodes === "undefined") {
          nodes = [];
        } else if (Array.isArray(nodes)) {
          nodes = nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type === "root" && this.type !== "document") {
          nodes = nodes.nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type) {
          nodes = [nodes];
        } else if (nodes.prop) {
          if (typeof nodes.value === "undefined") {
            throw new Error("Value field is missed in node creation");
          } else if (typeof nodes.value !== "string") {
            nodes.value = String(nodes.value);
          }
          nodes = [new Declaration(nodes)];
        } else if (nodes.selector) {
          nodes = [new Rule(nodes)];
        } else if (nodes.name) {
          nodes = [new AtRule(nodes)];
        } else if (nodes.text) {
          nodes = [new Comment(nodes)];
        } else {
          throw new Error("Unknown node type in node creation");
        }
        let processed = nodes.map((i2) => {
          if (!i2[my]) Container.rebuild(i2);
          i2 = i2.proxyOf;
          if (i2.parent) i2.parent.removeChild(i2);
          if (i2[isClean]) markDirtyUp(i2);
          if (typeof i2.raws.before === "undefined") {
            if (sample && typeof sample.raws.before !== "undefined") {
              i2.raws.before = sample.raws.before.replace(/\S/g, "");
            }
          }
          i2.parent = this.proxyOf;
          return i2;
        });
        return processed;
      }
      prepend(...children) {
        children = children.reverse();
        for (let child of children) {
          let nodes = this.normalize(child, this.first, "prepend").reverse();
          for (let node22 of nodes) this.proxyOf.nodes.unshift(node22);
          for (let id2 in this.indexes) {
            this.indexes[id2] = this.indexes[id2] + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      push(child) {
        child.parent = this;
        this.proxyOf.nodes.push(child);
        return this;
      }
      removeAll() {
        for (let node22 of this.proxyOf.nodes) node22.parent = void 0;
        this.proxyOf.nodes = [];
        this.markDirty();
        return this;
      }
      removeChild(child) {
        child = this.index(child);
        this.proxyOf.nodes[child].parent = void 0;
        this.proxyOf.nodes.splice(child, 1);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (index22 >= child) {
            this.indexes[id2] = index22 - 1;
          }
        }
        this.markDirty();
        return this;
      }
      replaceValues(pattern, opts, callback) {
        if (!callback) {
          callback = opts;
          opts = {};
        }
        this.walkDecls((decl) => {
          if (opts.props && !opts.props.includes(decl.prop)) return;
          if (opts.fast && !decl.value.includes(opts.fast)) return;
          decl.value = decl.value.replace(pattern, callback);
        });
        this.markDirty();
        return this;
      }
      some(condition) {
        return this.nodes.some(condition);
      }
      walk(callback) {
        return this.each((child, i2) => {
          let result22;
          try {
            result22 = callback(child, i2);
          } catch (e2) {
            throw child.addToError(e2);
          }
          if (result22 !== false && child.walk) {
            result22 = child.walk(callback);
          }
          return result22;
        });
      }
      walkAtRules(name, callback) {
        if (!callback) {
          callback = name;
          return this.walk((child, i2) => {
            if (child.type === "atrule") {
              return callback(child, i2);
            }
          });
        }
        if (name instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "atrule" && name.test(child.name)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "atrule" && child.name === name) {
            return callback(child, i2);
          }
        });
      }
      walkComments(callback) {
        return this.walk((child, i2) => {
          if (child.type === "comment") {
            return callback(child, i2);
          }
        });
      }
      walkDecls(prop, callback) {
        if (!callback) {
          callback = prop;
          return this.walk((child, i2) => {
            if (child.type === "decl") {
              return callback(child, i2);
            }
          });
        }
        if (prop instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "decl" && prop.test(child.prop)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "decl" && child.prop === prop) {
            return callback(child, i2);
          }
        });
      }
      walkRules(selector2, callback) {
        if (!callback) {
          callback = selector2;
          return this.walk((child, i2) => {
            if (child.type === "rule") {
              return callback(child, i2);
            }
          });
        }
        if (selector2 instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "rule" && selector2.test(child.selector)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "rule" && child.selector === selector2) {
            return callback(child, i2);
          }
        });
      }
      get first() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[0];
      }
      get last() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
      }
    }
    Container.registerParse = (dependant) => {
      parse = dependant;
    };
    Container.registerRule = (dependant) => {
      Rule = dependant;
    };
    Container.registerAtRule = (dependant) => {
      AtRule = dependant;
    };
    Container.registerRoot = (dependant) => {
      Root = dependant;
    };
    container$1 = Container;
    Container.default = Container;
    Container.rebuild = (node22) => {
      if (node22.type === "atrule") {
        Object.setPrototypeOf(node22, AtRule.prototype);
      } else if (node22.type === "rule") {
        Object.setPrototypeOf(node22, Rule.prototype);
      } else if (node22.type === "decl") {
        Object.setPrototypeOf(node22, Declaration.prototype);
      } else if (node22.type === "comment") {
        Object.setPrototypeOf(node22, Comment.prototype);
      } else if (node22.type === "root") {
        Object.setPrototypeOf(node22, Root.prototype);
      }
      node22[my] = true;
      if (node22.nodes) {
        node22.nodes.forEach((child) => {
          Container.rebuild(child);
        });
      }
    };
    return container$1;
  }
  function requireDocument$1() {
    if (hasRequiredDocument$1) return document$1$1;
    hasRequiredDocument$1 = 1;
    let Container = requireContainer$1();
    let LazyResult, Processor;
    class Document2 extends Container {
      constructor(defaults) {
        super({ type: "document", ...defaults });
        if (!this.nodes) {
          this.nodes = [];
        }
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Document2.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Document2.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    document$1$1 = Document2;
    Document2.default = Document2;
    return document$1$1;
  }
  function requireWarnOnce$1() {
    if (hasRequiredWarnOnce$1) return warnOnce$1;
    hasRequiredWarnOnce$1 = 1;
    let printed = {};
    warnOnce$1 = function warnOnce22(message) {
      if (printed[message]) return;
      printed[message] = true;
      if (typeof console !== "undefined" && console.warn) {
        console.warn(message);
      }
    };
    return warnOnce$1;
  }
  function requireWarning$1() {
    if (hasRequiredWarning$1) return warning$1;
    hasRequiredWarning$1 = 1;
    class Warning {
      constructor(text, opts = {}) {
        this.type = "warning";
        this.text = text;
        if (opts.node && opts.node.source) {
          let range = opts.node.rangeBy(opts);
          this.line = range.start.line;
          this.column = range.start.column;
          this.endLine = range.end.line;
          this.endColumn = range.end.column;
        }
        for (let opt in opts) this[opt] = opts[opt];
      }
      toString() {
        if (this.node) {
          return this.node.error(this.text, {
            index: this.index,
            plugin: this.plugin,
            word: this.word
          }).message;
        }
        if (this.plugin) {
          return this.plugin + ": " + this.text;
        }
        return this.text;
      }
    }
    warning$1 = Warning;
    Warning.default = Warning;
    return warning$1;
  }
  function requireResult$1() {
    if (hasRequiredResult$1) return result$1;
    hasRequiredResult$1 = 1;
    let Warning = requireWarning$1();
    class Result {
      constructor(processor22, root22, opts) {
        this.processor = processor22;
        this.messages = [];
        this.root = root22;
        this.opts = opts;
        this.css = void 0;
        this.map = void 0;
      }
      toString() {
        return this.css;
      }
      warn(text, opts = {}) {
        if (!opts.plugin) {
          if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
            opts.plugin = this.lastPlugin.postcssPlugin;
          }
        }
        let warning22 = new Warning(text, opts);
        this.messages.push(warning22);
        return warning22;
      }
      warnings() {
        return this.messages.filter((i2) => i2.type === "warning");
      }
      get content() {
        return this.css;
      }
    }
    result$1 = Result;
    Result.default = Result;
    return result$1;
  }
  function requireTokenize$1() {
    if (hasRequiredTokenize$1) return tokenize$1;
    hasRequiredTokenize$1 = 1;
    const SINGLE_QUOTE = "'".charCodeAt(0);
    const DOUBLE_QUOTE = '"'.charCodeAt(0);
    const BACKSLASH = "\\".charCodeAt(0);
    const SLASH = "/".charCodeAt(0);
    const NEWLINE = "\n".charCodeAt(0);
    const SPACE = " ".charCodeAt(0);
    const FEED = "\f".charCodeAt(0);
    const TAB = "	".charCodeAt(0);
    const CR = "\r".charCodeAt(0);
    const OPEN_SQUARE = "[".charCodeAt(0);
    const CLOSE_SQUARE = "]".charCodeAt(0);
    const OPEN_PARENTHESES = "(".charCodeAt(0);
    const CLOSE_PARENTHESES = ")".charCodeAt(0);
    const OPEN_CURLY = "{".charCodeAt(0);
    const CLOSE_CURLY = "}".charCodeAt(0);
    const SEMICOLON = ";".charCodeAt(0);
    const ASTERISK = "*".charCodeAt(0);
    const COLON = ":".charCodeAt(0);
    const AT = "@".charCodeAt(0);
    const RE_AT_END = /[\t\n\f\r "#'()/;[\\\]{}]/g;
    const RE_WORD_END = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
    const RE_BAD_BRACKET = /.[\r\n"'(/\\]/;
    const RE_HEX_ESCAPE = /[\da-f]/i;
    tokenize$1 = function tokenizer(input22, options = {}) {
      let css = input22.css.valueOf();
      let ignore = options.ignoreErrors;
      let code, next, quote, content, escape2;
      let escaped, escapePos, prev, n22, currentToken;
      let length = css.length;
      let pos = 0;
      let buffer = [];
      let returned = [];
      function position() {
        return pos;
      }
      function unclosed(what) {
        throw input22.error("Unclosed " + what, pos);
      }
      function endOfFile() {
        return returned.length === 0 && pos >= length;
      }
      function nextToken(opts) {
        if (returned.length) return returned.pop();
        if (pos >= length) return;
        let ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
        code = css.charCodeAt(pos);
        switch (code) {
          case NEWLINE:
          case SPACE:
          case TAB:
          case CR:
          case FEED: {
            next = pos;
            do {
              next += 1;
              code = css.charCodeAt(next);
            } while (code === SPACE || code === NEWLINE || code === TAB || code === CR || code === FEED);
            currentToken = ["space", css.slice(pos, next)];
            pos = next - 1;
            break;
          }
          case OPEN_SQUARE:
          case CLOSE_SQUARE:
          case OPEN_CURLY:
          case CLOSE_CURLY:
          case COLON:
          case SEMICOLON:
          case CLOSE_PARENTHESES: {
            let controlChar = String.fromCharCode(code);
            currentToken = [controlChar, controlChar, pos];
            break;
          }
          case OPEN_PARENTHESES: {
            prev = buffer.length ? buffer.pop()[1] : "";
            n22 = css.charCodeAt(pos + 1);
            if (prev === "url" && n22 !== SINGLE_QUOTE && n22 !== DOUBLE_QUOTE && n22 !== SPACE && n22 !== NEWLINE && n22 !== TAB && n22 !== FEED && n22 !== CR) {
              next = pos;
              do {
                escaped = false;
                next = css.indexOf(")", next + 1);
                if (next === -1) {
                  if (ignore || ignoreUnclosed) {
                    next = pos;
                    break;
                  } else {
                    unclosed("bracket");
                  }
                }
                escapePos = next;
                while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                  escapePos -= 1;
                  escaped = !escaped;
                }
              } while (escaped);
              currentToken = ["brackets", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              next = css.indexOf(")", pos + 1);
              content = css.slice(pos, next + 1);
              if (next === -1 || RE_BAD_BRACKET.test(content)) {
                currentToken = ["(", "(", pos];
              } else {
                currentToken = ["brackets", content, pos, next];
                pos = next;
              }
            }
            break;
          }
          case SINGLE_QUOTE:
          case DOUBLE_QUOTE: {
            quote = code === SINGLE_QUOTE ? "'" : '"';
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(quote, next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos + 1;
                  break;
                } else {
                  unclosed("string");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = ["string", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case AT: {
            RE_AT_END.lastIndex = pos + 1;
            RE_AT_END.test(css);
            if (RE_AT_END.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_AT_END.lastIndex - 2;
            }
            currentToken = ["at-word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case BACKSLASH: {
            next = pos;
            escape2 = true;
            while (css.charCodeAt(next + 1) === BACKSLASH) {
              next += 1;
              escape2 = !escape2;
            }
            code = css.charCodeAt(next + 1);
            if (escape2 && code !== SLASH && code !== SPACE && code !== NEWLINE && code !== TAB && code !== CR && code !== FEED) {
              next += 1;
              if (RE_HEX_ESCAPE.test(css.charAt(next))) {
                while (RE_HEX_ESCAPE.test(css.charAt(next + 1))) {
                  next += 1;
                }
                if (css.charCodeAt(next + 1) === SPACE) {
                  next += 1;
                }
              }
            }
            currentToken = ["word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          default: {
            if (code === SLASH && css.charCodeAt(pos + 1) === ASTERISK) {
              next = css.indexOf("*/", pos + 2) + 1;
              if (next === 0) {
                if (ignore || ignoreUnclosed) {
                  next = css.length;
                } else {
                  unclosed("comment");
                }
              }
              currentToken = ["comment", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              RE_WORD_END.lastIndex = pos + 1;
              RE_WORD_END.test(css);
              if (RE_WORD_END.lastIndex === 0) {
                next = css.length - 1;
              } else {
                next = RE_WORD_END.lastIndex - 2;
              }
              currentToken = ["word", css.slice(pos, next + 1), pos, next];
              buffer.push(currentToken);
              pos = next;
            }
            break;
          }
        }
        pos++;
        return currentToken;
      }
      function back(token) {
        returned.push(token);
      }
      return {
        back,
        endOfFile,
        nextToken,
        position
      };
    };
    return tokenize$1;
  }
  function requireAtRule$1() {
    if (hasRequiredAtRule$1) return atRule$1;
    hasRequiredAtRule$1 = 1;
    let Container = requireContainer$1();
    class AtRule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "atrule";
      }
      append(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.append(...children);
      }
      prepend(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.prepend(...children);
      }
    }
    atRule$1 = AtRule;
    AtRule.default = AtRule;
    Container.registerAtRule(AtRule);
    return atRule$1;
  }
  function requireRoot$1() {
    if (hasRequiredRoot$1) return root$1;
    hasRequiredRoot$1 = 1;
    let Container = requireContainer$1();
    let LazyResult, Processor;
    class Root extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "root";
        if (!this.nodes) this.nodes = [];
      }
      normalize(child, sample, type) {
        let nodes = super.normalize(child);
        if (sample) {
          if (type === "prepend") {
            if (this.nodes.length > 1) {
              sample.raws.before = this.nodes[1].raws.before;
            } else {
              delete sample.raws.before;
            }
          } else if (this.first !== sample) {
            for (let node22 of nodes) {
              node22.raws.before = sample.raws.before;
            }
          }
        }
        return nodes;
      }
      removeChild(child, ignore) {
        let index22 = this.index(child);
        if (!ignore && index22 === 0 && this.nodes.length > 1) {
          this.nodes[1].raws.before = this.nodes[index22].raws.before;
        }
        return super.removeChild(child);
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Root.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Root.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    root$1 = Root;
    Root.default = Root;
    Container.registerRoot(Root);
    return root$1;
  }
  function requireList$1() {
    if (hasRequiredList$1) return list_1$1;
    hasRequiredList$1 = 1;
    let list = {
      comma(string) {
        return list.split(string, [","], true);
      },
      space(string) {
        let spaces = [" ", "\n", "	"];
        return list.split(string, spaces);
      },
      split(string, separators, last) {
        let array = [];
        let current = "";
        let split = false;
        let func = 0;
        let inQuote = false;
        let prevQuote = "";
        let escape2 = false;
        for (let letter of string) {
          if (escape2) {
            escape2 = false;
          } else if (letter === "\\") {
            escape2 = true;
          } else if (inQuote) {
            if (letter === prevQuote) {
              inQuote = false;
            }
          } else if (letter === '"' || letter === "'") {
            inQuote = true;
            prevQuote = letter;
          } else if (letter === "(") {
            func += 1;
          } else if (letter === ")") {
            if (func > 0) func -= 1;
          } else if (func === 0) {
            if (separators.includes(letter)) split = true;
          }
          if (split) {
            if (current !== "") array.push(current.trim());
            current = "";
            split = false;
          } else {
            current += letter;
          }
        }
        if (last || current !== "") array.push(current.trim());
        return array;
      }
    };
    list_1$1 = list;
    list.default = list;
    return list_1$1;
  }
  function requireRule$1() {
    if (hasRequiredRule$1) return rule$1;
    hasRequiredRule$1 = 1;
    let Container = requireContainer$1();
    let list = requireList$1();
    class Rule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "rule";
        if (!this.nodes) this.nodes = [];
      }
      get selectors() {
        return list.comma(this.selector);
      }
      set selectors(values) {
        let match = this.selector ? this.selector.match(/,\s*/) : null;
        let sep = match ? match[0] : "," + this.raw("between", "beforeOpen");
        this.selector = values.join(sep);
      }
    }
    rule$1 = Rule;
    Rule.default = Rule;
    Container.registerRule(Rule);
    return rule$1;
  }
  function requireParser$1() {
    if (hasRequiredParser$1) return parser$1;
    hasRequiredParser$1 = 1;
    let Declaration = requireDeclaration$1();
    let tokenizer = requireTokenize$1();
    let Comment = requireComment$1();
    let AtRule = requireAtRule$1();
    let Root = requireRoot$1();
    let Rule = requireRule$1();
    const SAFE_COMMENT_NEIGHBOR = {
      empty: true,
      space: true
    };
    function findLastWithPosition(tokens) {
      for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
        let token = tokens[i2];
        let pos = token[3] || token[2];
        if (pos) return pos;
      }
    }
    class Parser {
      constructor(input22) {
        this.input = input22;
        this.root = new Root();
        this.current = this.root;
        this.spaces = "";
        this.semicolon = false;
        this.createTokenizer();
        this.root.source = { input: input22, start: { column: 1, line: 1, offset: 0 } };
      }
      atrule(token) {
        let node22 = new AtRule();
        node22.name = token[1].slice(1);
        if (node22.name === "") {
          this.unnamedAtrule(node22, token);
        }
        this.init(node22, token[2]);
        let type;
        let prev;
        let shift;
        let last = false;
        let open = false;
        let params = [];
        let brackets = [];
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          type = token[0];
          if (type === "(" || type === "[") {
            brackets.push(type === "(" ? ")" : "]");
          } else if (type === "{" && brackets.length > 0) {
            brackets.push("}");
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
          }
          if (brackets.length === 0) {
            if (type === ";") {
              node22.source.end = this.getPosition(token[2]);
              node22.source.end.offset++;
              this.semicolon = true;
              break;
            } else if (type === "{") {
              open = true;
              break;
            } else if (type === "}") {
              if (params.length > 0) {
                shift = params.length - 1;
                prev = params[shift];
                while (prev && prev[0] === "space") {
                  prev = params[--shift];
                }
                if (prev) {
                  node22.source.end = this.getPosition(prev[3] || prev[2]);
                  node22.source.end.offset++;
                }
              }
              this.end(token);
              break;
            } else {
              params.push(token);
            }
          } else {
            params.push(token);
          }
          if (this.tokenizer.endOfFile()) {
            last = true;
            break;
          }
        }
        node22.raws.between = this.spacesAndCommentsFromEnd(params);
        if (params.length) {
          node22.raws.afterName = this.spacesAndCommentsFromStart(params);
          this.raw(node22, "params", params);
          if (last) {
            token = params[params.length - 1];
            node22.source.end = this.getPosition(token[3] || token[2]);
            node22.source.end.offset++;
            this.spaces = node22.raws.between;
            node22.raws.between = "";
          }
        } else {
          node22.raws.afterName = "";
          node22.params = "";
        }
        if (open) {
          node22.nodes = [];
          this.current = node22;
        }
      }
      checkMissedSemicolon(tokens) {
        let colon = this.colon(tokens);
        if (colon === false) return;
        let founded = 0;
        let token;
        for (let j = colon - 1; j >= 0; j--) {
          token = tokens[j];
          if (token[0] !== "space") {
            founded += 1;
            if (founded === 2) break;
          }
        }
        throw this.input.error(
          "Missed semicolon",
          token[0] === "word" ? token[3] + 1 : token[2]
        );
      }
      colon(tokens) {
        let brackets = 0;
        let token, type, prev;
        for (let [i2, element] of tokens.entries()) {
          token = element;
          type = token[0];
          if (type === "(") {
            brackets += 1;
          }
          if (type === ")") {
            brackets -= 1;
          }
          if (brackets === 0 && type === ":") {
            if (!prev) {
              this.doubleColon(token);
            } else if (prev[0] === "word" && prev[1] === "progid") {
              continue;
            } else {
              return i2;
            }
          }
          prev = token;
        }
        return false;
      }
      comment(token) {
        let node22 = new Comment();
        this.init(node22, token[2]);
        node22.source.end = this.getPosition(token[3] || token[2]);
        node22.source.end.offset++;
        let text = token[1].slice(2, -2);
        if (/^\s*$/.test(text)) {
          node22.text = "";
          node22.raws.left = text;
          node22.raws.right = "";
        } else {
          let match = text.match(/^(\s*)([^]*\S)(\s*)$/);
          node22.text = match[2];
          node22.raws.left = match[1];
          node22.raws.right = match[3];
        }
      }
      createTokenizer() {
        this.tokenizer = tokenizer(this.input);
      }
      decl(tokens, customProperty) {
        let node22 = new Declaration();
        this.init(node22, tokens[0][2]);
        let last = tokens[tokens.length - 1];
        if (last[0] === ";") {
          this.semicolon = true;
          tokens.pop();
        }
        node22.source.end = this.getPosition(
          last[3] || last[2] || findLastWithPosition(tokens)
        );
        node22.source.end.offset++;
        while (tokens[0][0] !== "word") {
          if (tokens.length === 1) this.unknownWord(tokens);
          node22.raws.before += tokens.shift()[1];
        }
        node22.source.start = this.getPosition(tokens[0][2]);
        node22.prop = "";
        while (tokens.length) {
          let type = tokens[0][0];
          if (type === ":" || type === "space" || type === "comment") {
            break;
          }
          node22.prop += tokens.shift()[1];
        }
        node22.raws.between = "";
        let token;
        while (tokens.length) {
          token = tokens.shift();
          if (token[0] === ":") {
            node22.raws.between += token[1];
            break;
          } else {
            if (token[0] === "word" && /\w/.test(token[1])) {
              this.unknownWord([token]);
            }
            node22.raws.between += token[1];
          }
        }
        if (node22.prop[0] === "_" || node22.prop[0] === "*") {
          node22.raws.before += node22.prop[0];
          node22.prop = node22.prop.slice(1);
        }
        let firstSpaces = [];
        let next;
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          firstSpaces.push(tokens.shift());
        }
        this.precheckMissedSemicolon(tokens);
        for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
          token = tokens[i2];
          if (token[1].toLowerCase() === "!important") {
            node22.important = true;
            let string = this.stringFrom(tokens, i2);
            string = this.spacesFromEnd(tokens) + string;
            if (string !== " !important") node22.raws.important = string;
            break;
          } else if (token[1].toLowerCase() === "important") {
            let cache = tokens.slice(0);
            let str = "";
            for (let j = i2; j > 0; j--) {
              let type = cache[j][0];
              if (str.trim().indexOf("!") === 0 && type !== "space") {
                break;
              }
              str = cache.pop()[1] + str;
            }
            if (str.trim().indexOf("!") === 0) {
              node22.important = true;
              node22.raws.important = str;
              tokens = cache;
            }
          }
          if (token[0] !== "space" && token[0] !== "comment") {
            break;
          }
        }
        let hasWord = tokens.some((i2) => i2[0] !== "space" && i2[0] !== "comment");
        if (hasWord) {
          node22.raws.between += firstSpaces.map((i2) => i2[1]).join("");
          firstSpaces = [];
        }
        this.raw(node22, "value", firstSpaces.concat(tokens), customProperty);
        if (node22.value.includes(":") && !customProperty) {
          this.checkMissedSemicolon(tokens);
        }
      }
      doubleColon(token) {
        throw this.input.error(
          "Double colon",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
      emptyRule(token) {
        let node22 = new Rule();
        this.init(node22, token[2]);
        node22.selector = "";
        node22.raws.between = "";
        this.current = node22;
      }
      end(token) {
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.semicolon = false;
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.spaces = "";
        if (this.current.parent) {
          this.current.source.end = this.getPosition(token[2]);
          this.current.source.end.offset++;
          this.current = this.current.parent;
        } else {
          this.unexpectedClose(token);
        }
      }
      endFile() {
        if (this.current.parent) this.unclosedBlock();
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.root.source.end = this.getPosition(this.tokenizer.position());
      }
      freeSemicolon(token) {
        this.spaces += token[1];
        if (this.current.nodes) {
          let prev = this.current.nodes[this.current.nodes.length - 1];
          if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
            prev.raws.ownSemicolon = this.spaces;
            this.spaces = "";
          }
        }
      }
      // Helpers
      getPosition(offset) {
        let pos = this.input.fromOffset(offset);
        return {
          column: pos.col,
          line: pos.line,
          offset
        };
      }
      init(node22, offset) {
        this.current.push(node22);
        node22.source = {
          input: this.input,
          start: this.getPosition(offset)
        };
        node22.raws.before = this.spaces;
        this.spaces = "";
        if (node22.type !== "comment") this.semicolon = false;
      }
      other(start3) {
        let end = false;
        let type = null;
        let colon = false;
        let bracket = null;
        let brackets = [];
        let customProperty = start3[1].startsWith("--");
        let tokens = [];
        let token = start3;
        while (token) {
          type = token[0];
          tokens.push(token);
          if (type === "(" || type === "[") {
            if (!bracket) bracket = token;
            brackets.push(type === "(" ? ")" : "]");
          } else if (customProperty && colon && type === "{") {
            if (!bracket) bracket = token;
            brackets.push("}");
          } else if (brackets.length === 0) {
            if (type === ";") {
              if (colon) {
                this.decl(tokens, customProperty);
                return;
              } else {
                break;
              }
            } else if (type === "{") {
              this.rule(tokens);
              return;
            } else if (type === "}") {
              this.tokenizer.back(tokens.pop());
              end = true;
              break;
            } else if (type === ":") {
              colon = true;
            }
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
            if (brackets.length === 0) bracket = null;
          }
          token = this.tokenizer.nextToken();
        }
        if (this.tokenizer.endOfFile()) end = true;
        if (brackets.length > 0) this.unclosedBracket(bracket);
        if (end && colon) {
          if (!customProperty) {
            while (tokens.length) {
              token = tokens[tokens.length - 1][0];
              if (token !== "space" && token !== "comment") break;
              this.tokenizer.back(tokens.pop());
            }
          }
          this.decl(tokens, customProperty);
        } else {
          this.unknownWord(tokens);
        }
      }
      parse() {
        let token;
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          switch (token[0]) {
            case "space":
              this.spaces += token[1];
              break;
            case ";":
              this.freeSemicolon(token);
              break;
            case "}":
              this.end(token);
              break;
            case "comment":
              this.comment(token);
              break;
            case "at-word":
              this.atrule(token);
              break;
            case "{":
              this.emptyRule(token);
              break;
            default:
              this.other(token);
              break;
          }
        }
        this.endFile();
      }
      precheckMissedSemicolon() {
      }
      raw(node22, prop, tokens, customProperty) {
        let token, type;
        let length = tokens.length;
        let value = "";
        let clean = true;
        let next, prev;
        for (let i2 = 0; i2 < length; i2 += 1) {
          token = tokens[i2];
          type = token[0];
          if (type === "space" && i2 === length - 1 && !customProperty) {
            clean = false;
          } else if (type === "comment") {
            prev = tokens[i2 - 1] ? tokens[i2 - 1][0] : "empty";
            next = tokens[i2 + 1] ? tokens[i2 + 1][0] : "empty";
            if (!SAFE_COMMENT_NEIGHBOR[prev] && !SAFE_COMMENT_NEIGHBOR[next]) {
              if (value.slice(-1) === ",") {
                clean = false;
              } else {
                value += token[1];
              }
            } else {
              clean = false;
            }
          } else {
            value += token[1];
          }
        }
        if (!clean) {
          let raw = tokens.reduce((all, i2) => all + i2[1], "");
          node22.raws[prop] = { raw, value };
        }
        node22[prop] = value;
      }
      rule(tokens) {
        tokens.pop();
        let node22 = new Rule();
        this.init(node22, tokens[0][2]);
        node22.raws.between = this.spacesAndCommentsFromEnd(tokens);
        this.raw(node22, "selector", tokens);
        this.current = node22;
      }
      spacesAndCommentsFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space" && lastTokenType !== "comment") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      // Errors
      spacesAndCommentsFromStart(tokens) {
        let next;
        let spaces = "";
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          spaces += tokens.shift()[1];
        }
        return spaces;
      }
      spacesFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      stringFrom(tokens, from) {
        let result22 = "";
        for (let i2 = from; i2 < tokens.length; i2++) {
          result22 += tokens[i2][1];
        }
        tokens.splice(from, tokens.length - from);
        return result22;
      }
      unclosedBlock() {
        let pos = this.current.source.start;
        throw this.input.error("Unclosed block", pos.line, pos.column);
      }
      unclosedBracket(bracket) {
        throw this.input.error(
          "Unclosed bracket",
          { offset: bracket[2] },
          { offset: bracket[2] + 1 }
        );
      }
      unexpectedClose(token) {
        throw this.input.error(
          "Unexpected }",
          { offset: token[2] },
          { offset: token[2] + 1 }
        );
      }
      unknownWord(tokens) {
        throw this.input.error(
          "Unknown word",
          { offset: tokens[0][2] },
          { offset: tokens[0][2] + tokens[0][1].length }
        );
      }
      unnamedAtrule(node22, token) {
        throw this.input.error(
          "At-rule without name",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
    }
    parser$1 = Parser;
    return parser$1;
  }
  function requireParse$1() {
    if (hasRequiredParse$1) return parse_1$1;
    hasRequiredParse$1 = 1;
    let Container = requireContainer$1();
    let Parser = requireParser$1();
    let Input = requireInput$1();
    function parse(css, opts) {
      let input22 = new Input(css, opts);
      let parser22 = new Parser(input22);
      try {
        parser22.parse();
      } catch (e2) {
        if (true) {
          if (e2.name === "CssSyntaxError" && opts && opts.from) {
            if (/\.scss$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
            } else if (/\.sass/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
            } else if (/\.less$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
            }
          }
        }
        throw e2;
      }
      return parser22.root;
    }
    parse_1$1 = parse;
    parse.default = parse;
    Container.registerParse(parse);
    return parse_1$1;
  }
  function requireLazyResult$1() {
    if (hasRequiredLazyResult$1) return lazyResult$1;
    hasRequiredLazyResult$1 = 1;
    let { isClean, my } = requireSymbols$1();
    let MapGenerator = requireMapGenerator$1();
    let stringify2 = requireStringify$1();
    let Container = requireContainer$1();
    let Document2 = requireDocument$1();
    let warnOnce22 = requireWarnOnce$1();
    let Result = requireResult$1();
    let parse = requireParse$1();
    let Root = requireRoot$1();
    const TYPE_TO_CLASS_NAME = {
      atrule: "AtRule",
      comment: "Comment",
      decl: "Declaration",
      document: "Document",
      root: "Root",
      rule: "Rule"
    };
    const PLUGIN_PROPS = {
      AtRule: true,
      AtRuleExit: true,
      Comment: true,
      CommentExit: true,
      Declaration: true,
      DeclarationExit: true,
      Document: true,
      DocumentExit: true,
      Once: true,
      OnceExit: true,
      postcssPlugin: true,
      prepare: true,
      Root: true,
      RootExit: true,
      Rule: true,
      RuleExit: true
    };
    const NOT_VISITORS = {
      Once: true,
      postcssPlugin: true,
      prepare: true
    };
    const CHILDREN = 0;
    function isPromise(obj) {
      return typeof obj === "object" && typeof obj.then === "function";
    }
    function getEvents(node22) {
      let key = false;
      let type = TYPE_TO_CLASS_NAME[node22.type];
      if (node22.type === "decl") {
        key = node22.prop.toLowerCase();
      } else if (node22.type === "atrule") {
        key = node22.name.toLowerCase();
      }
      if (key && node22.append) {
        return [
          type,
          type + "-" + key,
          CHILDREN,
          type + "Exit",
          type + "Exit-" + key
        ];
      } else if (key) {
        return [type, type + "-" + key, type + "Exit", type + "Exit-" + key];
      } else if (node22.append) {
        return [type, CHILDREN, type + "Exit"];
      } else {
        return [type, type + "Exit"];
      }
    }
    function toStack(node22) {
      let events;
      if (node22.type === "document") {
        events = ["Document", CHILDREN, "DocumentExit"];
      } else if (node22.type === "root") {
        events = ["Root", CHILDREN, "RootExit"];
      } else {
        events = getEvents(node22);
      }
      return {
        eventIndex: 0,
        events,
        iterator: 0,
        node: node22,
        visitorIndex: 0,
        visitors: []
      };
    }
    function cleanMarks(node22) {
      node22[isClean] = false;
      if (node22.nodes) node22.nodes.forEach((i2) => cleanMarks(i2));
      return node22;
    }
    let postcss22 = {};
    class LazyResult {
      constructor(processor22, css, opts) {
        this.stringified = false;
        this.processed = false;
        let root22;
        if (typeof css === "object" && css !== null && (css.type === "root" || css.type === "document")) {
          root22 = cleanMarks(css);
        } else if (css instanceof LazyResult || css instanceof Result) {
          root22 = cleanMarks(css.root);
          if (css.map) {
            if (typeof opts.map === "undefined") opts.map = {};
            if (!opts.map.inline) opts.map.inline = false;
            opts.map.prev = css.map;
          }
        } else {
          let parser22 = parse;
          if (opts.syntax) parser22 = opts.syntax.parse;
          if (opts.parser) parser22 = opts.parser;
          if (parser22.parse) parser22 = parser22.parse;
          try {
            root22 = parser22(css, opts);
          } catch (error) {
            this.processed = true;
            this.error = error;
          }
          if (root22 && !root22[my]) {
            Container.rebuild(root22);
          }
        }
        this.result = new Result(processor22, root22, opts);
        this.helpers = { ...postcss22, postcss: postcss22, result: this.result };
        this.plugins = this.processor.plugins.map((plugin) => {
          if (typeof plugin === "object" && plugin.prepare) {
            return { ...plugin, ...plugin.prepare(this.result) };
          } else {
            return plugin;
          }
        });
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        if (this.processed) return Promise.resolve(this.result);
        if (!this.processing) {
          this.processing = this.runAsync();
        }
        return this.processing;
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      getAsyncError() {
        throw new Error("Use process(css).then(cb) to work with async plugins");
      }
      handleError(error, node22) {
        let plugin = this.result.lastPlugin;
        try {
          if (node22) node22.addToError(error);
          this.error = error;
          if (error.name === "CssSyntaxError" && !error.plugin) {
            error.plugin = plugin.postcssPlugin;
            error.setMessage();
          } else if (plugin.postcssVersion) {
            if (true) {
              let pluginName = plugin.postcssPlugin;
              let pluginVer = plugin.postcssVersion;
              let runtimeVer = this.result.processor.version;
              let a2 = pluginVer.split(".");
              let b2 = runtimeVer.split(".");
              if (a2[0] !== b2[0] || parseInt(a2[1]) > parseInt(b2[1])) {
                console.error(
                  "Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below."
                );
              }
            }
          }
        } catch (err) {
          if (console && console.error) console.error(err);
        }
        return error;
      }
      prepareVisitors() {
        this.listeners = {};
        let add2 = (plugin, type, cb) => {
          if (!this.listeners[type]) this.listeners[type] = [];
          this.listeners[type].push([plugin, cb]);
        };
        for (let plugin of this.plugins) {
          if (typeof plugin === "object") {
            for (let event in plugin) {
              if (!PLUGIN_PROPS[event] && /^[A-Z]/.test(event)) {
                throw new Error(
                  `Unknown event ${event} in ${plugin.postcssPlugin}. Try to update PostCSS (${this.processor.version} now).`
                );
              }
              if (!NOT_VISITORS[event]) {
                if (typeof plugin[event] === "object") {
                  for (let filter in plugin[event]) {
                    if (filter === "*") {
                      add2(plugin, event, plugin[event][filter]);
                    } else {
                      add2(
                        plugin,
                        event + "-" + filter.toLowerCase(),
                        plugin[event][filter]
                      );
                    }
                  }
                } else if (typeof plugin[event] === "function") {
                  add2(plugin, event, plugin[event]);
                }
              }
            }
          }
        }
        this.hasListener = Object.keys(this.listeners).length > 0;
      }
      async runAsync() {
        this.plugin = 0;
        for (let i2 = 0; i2 < this.plugins.length; i2++) {
          let plugin = this.plugins[i2];
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            try {
              await promise;
            } catch (error) {
              throw this.handleError(error);
            }
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            let stack = [toStack(root22)];
            while (stack.length > 0) {
              let promise = this.visitTick(stack);
              if (isPromise(promise)) {
                try {
                  await promise;
                } catch (e2) {
                  let node22 = stack[stack.length - 1].node;
                  throw this.handleError(e2, node22);
                }
              }
            }
          }
          if (this.listeners.OnceExit) {
            for (let [plugin, visitor] of this.listeners.OnceExit) {
              this.result.lastPlugin = plugin;
              try {
                if (root22.type === "document") {
                  let roots = root22.nodes.map(
                    (subRoot) => visitor(subRoot, this.helpers)
                  );
                  await Promise.all(roots);
                } else {
                  await visitor(root22, this.helpers);
                }
              } catch (e2) {
                throw this.handleError(e2);
              }
            }
          }
        }
        this.processed = true;
        return this.stringify();
      }
      runOnRoot(plugin) {
        this.result.lastPlugin = plugin;
        try {
          if (typeof plugin === "object" && plugin.Once) {
            if (this.result.root.type === "document") {
              let roots = this.result.root.nodes.map(
                (root22) => plugin.Once(root22, this.helpers)
              );
              if (isPromise(roots[0])) {
                return Promise.all(roots);
              }
              return roots;
            }
            return plugin.Once(this.result.root, this.helpers);
          } else if (typeof plugin === "function") {
            return plugin(this.result.root, this.result);
          }
        } catch (error) {
          throw this.handleError(error);
        }
      }
      stringify() {
        if (this.error) throw this.error;
        if (this.stringified) return this.result;
        this.stringified = true;
        this.sync();
        let opts = this.result.opts;
        let str = stringify2;
        if (opts.syntax) str = opts.syntax.stringify;
        if (opts.stringifier) str = opts.stringifier;
        if (str.stringify) str = str.stringify;
        let map = new MapGenerator(str, this.result.root, this.result.opts);
        let data = map.generate();
        this.result.css = data[0];
        this.result.map = data[1];
        return this.result;
      }
      sync() {
        if (this.error) throw this.error;
        if (this.processed) return this.result;
        this.processed = true;
        if (this.processing) {
          throw this.getAsyncError();
        }
        for (let plugin of this.plugins) {
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            this.walkSync(root22);
          }
          if (this.listeners.OnceExit) {
            if (root22.type === "document") {
              for (let subRoot of root22.nodes) {
                this.visitSync(this.listeners.OnceExit, subRoot);
              }
            } else {
              this.visitSync(this.listeners.OnceExit, root22);
            }
          }
        }
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this.opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this.css;
      }
      visitSync(visitors, node22) {
        for (let [plugin, visitor] of visitors) {
          this.result.lastPlugin = plugin;
          let promise;
          try {
            promise = visitor(node22, this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22.proxyOf);
          }
          if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
            return true;
          }
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
      }
      visitTick(stack) {
        let visit2 = stack[stack.length - 1];
        let { node: node22, visitors } = visit2;
        if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
          stack.pop();
          return;
        }
        if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
          let [plugin, visitor] = visitors[visit2.visitorIndex];
          visit2.visitorIndex += 1;
          if (visit2.visitorIndex === visitors.length) {
            visit2.visitors = [];
            visit2.visitorIndex = 0;
          }
          this.result.lastPlugin = plugin;
          try {
            return visitor(node22.toProxy(), this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22);
          }
        }
        if (visit2.iterator !== 0) {
          let iterator = visit2.iterator;
          let child;
          while (child = node22.nodes[node22.indexes[iterator]]) {
            node22.indexes[iterator] += 1;
            if (!child[isClean]) {
              child[isClean] = true;
              stack.push(toStack(child));
              return;
            }
          }
          visit2.iterator = 0;
          delete node22.indexes[iterator];
        }
        let events = visit2.events;
        while (visit2.eventIndex < events.length) {
          let event = events[visit2.eventIndex];
          visit2.eventIndex += 1;
          if (event === CHILDREN) {
            if (node22.nodes && node22.nodes.length) {
              node22[isClean] = true;
              visit2.iterator = node22.getIterator();
            }
            return;
          } else if (this.listeners[event]) {
            visit2.visitors = this.listeners[event];
            return;
          }
        }
        stack.pop();
      }
      walkSync(node22) {
        node22[isClean] = true;
        let events = getEvents(node22);
        for (let event of events) {
          if (event === CHILDREN) {
            if (node22.nodes) {
              node22.each((child) => {
                if (!child[isClean]) this.walkSync(child);
              });
            }
          } else {
            let visitors = this.listeners[event];
            if (visitors) {
              if (this.visitSync(visitors, node22.toProxy())) return;
            }
          }
        }
      }
      warnings() {
        return this.sync().warnings();
      }
      get content() {
        return this.stringify().content;
      }
      get css() {
        return this.stringify().css;
      }
      get map() {
        return this.stringify().map;
      }
      get messages() {
        return this.sync().messages;
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        return this.sync().root;
      }
      get [Symbol.toStringTag]() {
        return "LazyResult";
      }
    }
    LazyResult.registerPostcss = (dependant) => {
      postcss22 = dependant;
    };
    lazyResult$1 = LazyResult;
    LazyResult.default = LazyResult;
    Root.registerLazyResult(LazyResult);
    Document2.registerLazyResult(LazyResult);
    return lazyResult$1;
  }
  function requireNoWorkResult$1() {
    if (hasRequiredNoWorkResult$1) return noWorkResult$1;
    hasRequiredNoWorkResult$1 = 1;
    let MapGenerator = requireMapGenerator$1();
    let stringify2 = requireStringify$1();
    let warnOnce22 = requireWarnOnce$1();
    let parse = requireParse$1();
    const Result = requireResult$1();
    class NoWorkResult {
      constructor(processor22, css, opts) {
        css = css.toString();
        this.stringified = false;
        this._processor = processor22;
        this._css = css;
        this._opts = opts;
        this._map = void 0;
        let root22;
        let str = stringify2;
        this.result = new Result(this._processor, root22, this._opts);
        this.result.css = css;
        let self2 = this;
        Object.defineProperty(this.result, "root", {
          get() {
            return self2.root;
          }
        });
        let map = new MapGenerator(str, root22, this._opts, css);
        if (map.isMap()) {
          let [generatedCSS, generatedMap] = map.generate();
          if (generatedCSS) {
            this.result.css = generatedCSS;
          }
          if (generatedMap) {
            this.result.map = generatedMap;
          }
        } else {
          map.clearAnnotation();
          this.result.css = map.css;
        }
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        return Promise.resolve(this.result);
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      sync() {
        if (this.error) throw this.error;
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this._opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this._css;
      }
      warnings() {
        return [];
      }
      get content() {
        return this.result.css;
      }
      get css() {
        return this.result.css;
      }
      get map() {
        return this.result.map;
      }
      get messages() {
        return [];
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        if (this._root) {
          return this._root;
        }
        let root22;
        let parser22 = parse;
        try {
          root22 = parser22(this._css, this._opts);
        } catch (error) {
          this.error = error;
        }
        if (this.error) {
          throw this.error;
        } else {
          this._root = root22;
          return root22;
        }
      }
      get [Symbol.toStringTag]() {
        return "NoWorkResult";
      }
    }
    noWorkResult$1 = NoWorkResult;
    NoWorkResult.default = NoWorkResult;
    return noWorkResult$1;
  }
  function requireProcessor$1() {
    if (hasRequiredProcessor$1) return processor$1;
    hasRequiredProcessor$1 = 1;
    let NoWorkResult = requireNoWorkResult$1();
    let LazyResult = requireLazyResult$1();
    let Document2 = requireDocument$1();
    let Root = requireRoot$1();
    class Processor {
      constructor(plugins = []) {
        this.version = "8.4.38";
        this.plugins = this.normalize(plugins);
      }
      normalize(plugins) {
        let normalized = [];
        for (let i2 of plugins) {
          if (i2.postcss === true) {
            i2 = i2();
          } else if (i2.postcss) {
            i2 = i2.postcss;
          }
          if (typeof i2 === "object" && Array.isArray(i2.plugins)) {
            normalized = normalized.concat(i2.plugins);
          } else if (typeof i2 === "object" && i2.postcssPlugin) {
            normalized.push(i2);
          } else if (typeof i2 === "function") {
            normalized.push(i2);
          } else if (typeof i2 === "object" && (i2.parse || i2.stringify)) {
            if (true) {
              throw new Error(
                "PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation."
              );
            }
          } else {
            throw new Error(i2 + " is not a PostCSS plugin");
          }
        }
        return normalized;
      }
      process(css, opts = {}) {
        if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
          return new NoWorkResult(this, css, opts);
        } else {
          return new LazyResult(this, css, opts);
        }
      }
      use(plugin) {
        this.plugins = this.plugins.concat(this.normalize([plugin]));
        return this;
      }
    }
    processor$1 = Processor;
    Processor.default = Processor;
    Root.registerProcessor(Processor);
    Document2.registerProcessor(Processor);
    return processor$1;
  }
  function requireFromJSON$1() {
    if (hasRequiredFromJSON$1) return fromJSON_1$1;
    hasRequiredFromJSON$1 = 1;
    let Declaration = requireDeclaration$1();
    let PreviousMap = requirePreviousMap$1();
    let Comment = requireComment$1();
    let AtRule = requireAtRule$1();
    let Input = requireInput$1();
    let Root = requireRoot$1();
    let Rule = requireRule$1();
    function fromJSON(json, inputs) {
      if (Array.isArray(json)) return json.map((n22) => fromJSON(n22));
      let { inputs: ownInputs, ...defaults } = json;
      if (ownInputs) {
        inputs = [];
        for (let input22 of ownInputs) {
          let inputHydrated = { ...input22, __proto__: Input.prototype };
          if (inputHydrated.map) {
            inputHydrated.map = {
              ...inputHydrated.map,
              __proto__: PreviousMap.prototype
            };
          }
          inputs.push(inputHydrated);
        }
      }
      if (defaults.nodes) {
        defaults.nodes = json.nodes.map((n22) => fromJSON(n22, inputs));
      }
      if (defaults.source) {
        let { inputId, ...source } = defaults.source;
        defaults.source = source;
        if (inputId != null) {
          defaults.source.input = inputs[inputId];
        }
      }
      if (defaults.type === "root") {
        return new Root(defaults);
      } else if (defaults.type === "decl") {
        return new Declaration(defaults);
      } else if (defaults.type === "rule") {
        return new Rule(defaults);
      } else if (defaults.type === "comment") {
        return new Comment(defaults);
      } else if (defaults.type === "atrule") {
        return new AtRule(defaults);
      } else {
        throw new Error("Unknown node type: " + json.type);
      }
    }
    fromJSON_1$1 = fromJSON;
    fromJSON.default = fromJSON;
    return fromJSON_1$1;
  }
  function requirePostcss$1() {
    if (hasRequiredPostcss$1) return postcss_1$1;
    hasRequiredPostcss$1 = 1;
    let CssSyntaxError = requireCssSyntaxError$1();
    let Declaration = requireDeclaration$1();
    let LazyResult = requireLazyResult$1();
    let Container = requireContainer$1();
    let Processor = requireProcessor$1();
    let stringify2 = requireStringify$1();
    let fromJSON = requireFromJSON$1();
    let Document2 = requireDocument$1();
    let Warning = requireWarning$1();
    let Comment = requireComment$1();
    let AtRule = requireAtRule$1();
    let Result = requireResult$1();
    let Input = requireInput$1();
    let parse = requireParse$1();
    let list = requireList$1();
    let Rule = requireRule$1();
    let Root = requireRoot$1();
    let Node2 = requireNode$1();
    function postcss22(...plugins) {
      if (plugins.length === 1 && Array.isArray(plugins[0])) {
        plugins = plugins[0];
      }
      return new Processor(plugins);
    }
    postcss22.plugin = function plugin(name, initializer) {
      let warningPrinted = false;
      function creator(...args) {
        if (console && console.warn && !warningPrinted) {
          warningPrinted = true;
          console.warn(
            name + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration"
          );
          if (process.env.LANG && process.env.LANG.startsWith("cn")) {
            console.warn(
              name + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226"
            );
          }
        }
        let transformer = initializer(...args);
        transformer.postcssPlugin = name;
        transformer.postcssVersion = new Processor().version;
        return transformer;
      }
      let cache;
      Object.defineProperty(creator, "postcss", {
        get() {
          if (!cache) cache = creator();
          return cache;
        }
      });
      creator.process = function(css, processOpts, pluginOpts) {
        return postcss22([creator(pluginOpts)]).process(css, processOpts);
      };
      return creator;
    };
    postcss22.stringify = stringify2;
    postcss22.parse = parse;
    postcss22.fromJSON = fromJSON;
    postcss22.list = list;
    postcss22.comment = (defaults) => new Comment(defaults);
    postcss22.atRule = (defaults) => new AtRule(defaults);
    postcss22.decl = (defaults) => new Declaration(defaults);
    postcss22.rule = (defaults) => new Rule(defaults);
    postcss22.root = (defaults) => new Root(defaults);
    postcss22.document = (defaults) => new Document2(defaults);
    postcss22.CssSyntaxError = CssSyntaxError;
    postcss22.Declaration = Declaration;
    postcss22.Container = Container;
    postcss22.Processor = Processor;
    postcss22.Document = Document2;
    postcss22.Comment = Comment;
    postcss22.Warning = Warning;
    postcss22.AtRule = AtRule;
    postcss22.Result = Result;
    postcss22.Input = Input;
    postcss22.Rule = Rule;
    postcss22.Root = Root;
    postcss22.Node = Node2;
    LazyResult.registerPostcss(postcss22);
    postcss_1$1 = postcss22;
    postcss22.default = postcss22;
    return postcss_1$1;
  }
  function getDefaultExportFromCjs(x2) {
    return x2 && x2.__esModule && Object.prototype.hasOwnProperty.call(x2, "default") ? x2["default"] : x2;
  }
  function getAugmentedNamespace(n22) {
    if (Object.prototype.hasOwnProperty.call(n22, "__esModule")) return n22;
    var f2 = n22.default;
    if (typeof f2 == "function") {
      var a2 = function a22() {
        if (this instanceof a22) {
          return Reflect.construct(f2, arguments, this.constructor);
        }
        return f2.apply(this, arguments);
      };
      a2.prototype = f2.prototype;
    } else a2 = {};
    Object.defineProperty(a2, "__esModule", { value: true });
    Object.keys(n22).forEach(function(k2) {
      var d2 = Object.getOwnPropertyDescriptor(n22, k2);
      Object.defineProperty(a2, k2, d2.get ? d2 : {
        enumerable: true,
        get: function() {
          return n22[k2];
        }
      });
    });
    return a2;
  }
  function requirePicocolors_browser() {
    if (hasRequiredPicocolors_browser) return picocolors_browser.exports;
    hasRequiredPicocolors_browser = 1;
    var x2 = String;
    var create = function() {
      return { isColorSupported: false, reset: x2, bold: x2, dim: x2, italic: x2, underline: x2, inverse: x2, hidden: x2, strikethrough: x2, black: x2, red: x2, green: x2, yellow: x2, blue: x2, magenta: x2, cyan: x2, white: x2, gray: x2, bgBlack: x2, bgRed: x2, bgGreen: x2, bgYellow: x2, bgBlue: x2, bgMagenta: x2, bgCyan: x2, bgWhite: x2 };
    };
    picocolors_browser.exports = create();
    picocolors_browser.exports.createColors = create;
    return picocolors_browser.exports;
  }
  function requireCssSyntaxError() {
    if (hasRequiredCssSyntaxError) return cssSyntaxError;
    hasRequiredCssSyntaxError = 1;
    let pico = /* @__PURE__ */ requirePicocolors_browser();
    let terminalHighlight = require$$2;
    class CssSyntaxError extends Error {
      constructor(message, line2, column, source, file, plugin) {
        super(message);
        this.name = "CssSyntaxError";
        this.reason = message;
        if (file) {
          this.file = file;
        }
        if (source) {
          this.source = source;
        }
        if (plugin) {
          this.plugin = plugin;
        }
        if (typeof line2 !== "undefined" && typeof column !== "undefined") {
          if (typeof line2 === "number") {
            this.line = line2;
            this.column = column;
          } else {
            this.line = line2.line;
            this.column = line2.column;
            this.endLine = column.line;
            this.endColumn = column.column;
          }
        }
        this.setMessage();
        if (Error.captureStackTrace) {
          Error.captureStackTrace(this, CssSyntaxError);
        }
      }
      setMessage() {
        this.message = this.plugin ? this.plugin + ": " : "";
        this.message += this.file ? this.file : "<css input>";
        if (typeof this.line !== "undefined") {
          this.message += ":" + this.line + ":" + this.column;
        }
        this.message += ": " + this.reason;
      }
      showSourceCode(color) {
        if (!this.source) return "";
        let css = this.source;
        if (color == null) color = pico.isColorSupported;
        if (terminalHighlight) {
          if (color) css = terminalHighlight(css);
        }
        let lines = css.split(/\r?\n/);
        let start3 = Math.max(this.line - 3, 0);
        let end = Math.min(this.line + 2, lines.length);
        let maxWidth = String(end).length;
        let mark, aside;
        if (color) {
          let { bold, gray, red } = pico.createColors(true);
          mark = (text) => bold(red(text));
          aside = (text) => gray(text);
        } else {
          mark = aside = (str) => str;
        }
        return lines.slice(start3, end).map((line2, index22) => {
          let number = start3 + 1 + index22;
          let gutter = " " + (" " + number).slice(-maxWidth) + " | ";
          if (number === this.line) {
            let spacing = aside(gutter.replace(/\d/g, " ")) + line2.slice(0, this.column - 1).replace(/[^\t]/g, " ");
            return mark(">") + aside(gutter) + line2 + "\n " + spacing + mark("^");
          }
          return " " + aside(gutter) + line2;
        }).join("\n");
      }
      toString() {
        let code = this.showSourceCode();
        if (code) {
          code = "\n\n" + code + "\n";
        }
        return this.name + ": " + this.message + code;
      }
    }
    cssSyntaxError = CssSyntaxError;
    CssSyntaxError.default = CssSyntaxError;
    return cssSyntaxError;
  }
  function requireSymbols() {
    if (hasRequiredSymbols) return symbols;
    hasRequiredSymbols = 1;
    symbols.isClean = Symbol("isClean");
    symbols.my = Symbol("my");
    return symbols;
  }
  function requireStringifier() {
    if (hasRequiredStringifier) return stringifier;
    hasRequiredStringifier = 1;
    const DEFAULT_RAW = {
      after: "\n",
      beforeClose: "\n",
      beforeComment: "\n",
      beforeDecl: "\n",
      beforeOpen: " ",
      beforeRule: "\n",
      colon: ": ",
      commentLeft: " ",
      commentRight: " ",
      emptyBody: "",
      indent: "    ",
      semicolon: false
    };
    function capitalize(str) {
      return str[0].toUpperCase() + str.slice(1);
    }
    class Stringifier {
      constructor(builder) {
        this.builder = builder;
      }
      atrule(node22, semicolon) {
        let name = "@" + node22.name;
        let params = node22.params ? this.rawValue(node22, "params") : "";
        if (typeof node22.raws.afterName !== "undefined") {
          name += node22.raws.afterName;
        } else if (params) {
          name += " ";
        }
        if (node22.nodes) {
          this.block(node22, name + params);
        } else {
          let end = (node22.raws.between || "") + (semicolon ? ";" : "");
          this.builder(name + params + end, node22);
        }
      }
      beforeAfter(node22, detect) {
        let value;
        if (node22.type === "decl") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (node22.type === "comment") {
          value = this.raw(node22, null, "beforeComment");
        } else if (detect === "before") {
          value = this.raw(node22, null, "beforeRule");
        } else {
          value = this.raw(node22, null, "beforeClose");
        }
        let buf = node22.parent;
        let depth = 0;
        while (buf && buf.type !== "root") {
          depth += 1;
          buf = buf.parent;
        }
        if (value.includes("\n")) {
          let indent = this.raw(node22, null, "indent");
          if (indent.length) {
            for (let step = 0; step < depth; step++) value += indent;
          }
        }
        return value;
      }
      block(node22, start3) {
        let between = this.raw(node22, "between", "beforeOpen");
        this.builder(start3 + between + "{", node22, "start");
        let after;
        if (node22.nodes && node22.nodes.length) {
          this.body(node22);
          after = this.raw(node22, "after");
        } else {
          after = this.raw(node22, "after", "emptyBody");
        }
        if (after) this.builder(after);
        this.builder("}", node22, "end");
      }
      body(node22) {
        let last = node22.nodes.length - 1;
        while (last > 0) {
          if (node22.nodes[last].type !== "comment") break;
          last -= 1;
        }
        let semicolon = this.raw(node22, "semicolon");
        for (let i2 = 0; i2 < node22.nodes.length; i2++) {
          let child = node22.nodes[i2];
          let before = this.raw(child, "before");
          if (before) this.builder(before);
          this.stringify(child, last !== i2 || semicolon);
        }
      }
      comment(node22) {
        let left = this.raw(node22, "left", "commentLeft");
        let right = this.raw(node22, "right", "commentRight");
        this.builder("/*" + left + node22.text + right + "*/", node22);
      }
      decl(node22, semicolon) {
        let between = this.raw(node22, "between", "colon");
        let string = node22.prop + between + this.rawValue(node22, "value");
        if (node22.important) {
          string += node22.raws.important || " !important";
        }
        if (semicolon) string += ";";
        this.builder(string, node22);
      }
      document(node22) {
        this.body(node22);
      }
      raw(node22, own, detect) {
        let value;
        if (!detect) detect = own;
        if (own) {
          value = node22.raws[own];
          if (typeof value !== "undefined") return value;
        }
        let parent = node22.parent;
        if (detect === "before") {
          if (!parent || parent.type === "root" && parent.first === node22) {
            return "";
          }
          if (parent && parent.type === "document") {
            return "";
          }
        }
        if (!parent) return DEFAULT_RAW[detect];
        let root22 = node22.root();
        if (!root22.rawCache) root22.rawCache = {};
        if (typeof root22.rawCache[detect] !== "undefined") {
          return root22.rawCache[detect];
        }
        if (detect === "before" || detect === "after") {
          return this.beforeAfter(node22, detect);
        } else {
          let method = "raw" + capitalize(detect);
          if (this[method]) {
            value = this[method](root22, node22);
          } else {
            root22.walk((i2) => {
              value = i2.raws[own];
              if (typeof value !== "undefined") return false;
            });
          }
        }
        if (typeof value === "undefined") value = DEFAULT_RAW[detect];
        root22.rawCache[detect] = value;
        return value;
      }
      rawBeforeClose(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length > 0) {
            if (typeof i2.raws.after !== "undefined") {
              value = i2.raws.after;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawBeforeComment(root22, node22) {
        let value;
        root22.walkComments((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeDecl(root22, node22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeRule");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeOpen(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.type !== "decl") {
            value = i2.raws.between;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawBeforeRule(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && (i2.parent !== root22 || root22.first !== i2)) {
            if (typeof i2.raws.before !== "undefined") {
              value = i2.raws.before;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawColon(root22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.between !== "undefined") {
            value = i2.raws.between.replace(/[^\s:]/g, "");
            return false;
          }
        });
        return value;
      }
      rawEmptyBody(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length === 0) {
            value = i2.raws.after;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawIndent(root22) {
        if (root22.raws.indent) return root22.raws.indent;
        let value;
        root22.walk((i2) => {
          let p2 = i2.parent;
          if (p2 && p2 !== root22 && p2.parent && p2.parent === root22) {
            if (typeof i2.raws.before !== "undefined") {
              let parts = i2.raws.before.split("\n");
              value = parts[parts.length - 1];
              value = value.replace(/\S/g, "");
              return false;
            }
          }
        });
        return value;
      }
      rawSemicolon(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length && i2.last.type === "decl") {
            value = i2.raws.semicolon;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawValue(node22, prop) {
        let value = node22[prop];
        let raw = node22.raws[prop];
        if (raw && raw.value === value) {
          return raw.raw;
        }
        return value;
      }
      root(node22) {
        this.body(node22);
        if (node22.raws.after) this.builder(node22.raws.after);
      }
      rule(node22) {
        this.block(node22, this.rawValue(node22, "selector"));
        if (node22.raws.ownSemicolon) {
          this.builder(node22.raws.ownSemicolon, node22, "end");
        }
      }
      stringify(node22, semicolon) {
        if (!this[node22.type]) {
          throw new Error(
            "Unknown AST node type " + node22.type + ". Maybe you need to change PostCSS stringifier."
          );
        }
        this[node22.type](node22, semicolon);
      }
    }
    stringifier = Stringifier;
    Stringifier.default = Stringifier;
    return stringifier;
  }
  function requireStringify() {
    if (hasRequiredStringify) return stringify_1;
    hasRequiredStringify = 1;
    let Stringifier = requireStringifier();
    function stringify2(node22, builder) {
      let str = new Stringifier(builder);
      str.stringify(node22);
    }
    stringify_1 = stringify2;
    stringify2.default = stringify2;
    return stringify_1;
  }
  function requireNode() {
    if (hasRequiredNode) return node;
    hasRequiredNode = 1;
    let { isClean, my } = requireSymbols();
    let CssSyntaxError = requireCssSyntaxError();
    let Stringifier = requireStringifier();
    let stringify2 = requireStringify();
    function cloneNode(obj, parent) {
      let cloned = new obj.constructor();
      for (let i2 in obj) {
        if (!Object.prototype.hasOwnProperty.call(obj, i2)) {
          continue;
        }
        if (i2 === "proxyCache") continue;
        let value = obj[i2];
        let type = typeof value;
        if (i2 === "parent" && type === "object") {
          if (parent) cloned[i2] = parent;
        } else if (i2 === "source") {
          cloned[i2] = value;
        } else if (Array.isArray(value)) {
          cloned[i2] = value.map((j) => cloneNode(j, cloned));
        } else {
          if (type === "object" && value !== null) value = cloneNode(value);
          cloned[i2] = value;
        }
      }
      return cloned;
    }
    class Node2 {
      constructor(defaults = {}) {
        this.raws = {};
        this[isClean] = false;
        this[my] = true;
        for (let name in defaults) {
          if (name === "nodes") {
            this.nodes = [];
            for (let node22 of defaults[name]) {
              if (typeof node22.clone === "function") {
                this.append(node22.clone());
              } else {
                this.append(node22);
              }
            }
          } else {
            this[name] = defaults[name];
          }
        }
      }
      addToError(error) {
        error.postcssNode = this;
        if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
          let s2 = this.source;
          error.stack = error.stack.replace(
            /\n\s{4}at /,
            `$&${s2.input.from}:${s2.start.line}:${s2.start.column}$&`
          );
        }
        return error;
      }
      after(add2) {
        this.parent.insertAfter(this, add2);
        return this;
      }
      assign(overrides = {}) {
        for (let name in overrides) {
          this[name] = overrides[name];
        }
        return this;
      }
      before(add2) {
        this.parent.insertBefore(this, add2);
        return this;
      }
      cleanRaws(keepBetween) {
        delete this.raws.before;
        delete this.raws.after;
        if (!keepBetween) delete this.raws.between;
      }
      clone(overrides = {}) {
        let cloned = cloneNode(this);
        for (let name in overrides) {
          cloned[name] = overrides[name];
        }
        return cloned;
      }
      cloneAfter(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertAfter(this, cloned);
        return cloned;
      }
      cloneBefore(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertBefore(this, cloned);
        return cloned;
      }
      error(message, opts = {}) {
        if (this.source) {
          let { end, start: start3 } = this.rangeBy(opts);
          return this.source.input.error(
            message,
            { column: start3.column, line: start3.line },
            { column: end.column, line: end.line },
            opts
          );
        }
        return new CssSyntaxError(message);
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
            prop === "text") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      markDirty() {
        if (this[isClean]) {
          this[isClean] = false;
          let next = this;
          while (next = next.parent) {
            next[isClean] = false;
          }
        }
      }
      next() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 + 1];
      }
      positionBy(opts, stringRepresentation) {
        let pos = this.source.start;
        if (opts.index) {
          pos = this.positionInside(opts.index, stringRepresentation);
        } else if (opts.word) {
          stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) pos = this.positionInside(index22, stringRepresentation);
        }
        return pos;
      }
      positionInside(index22, stringRepresentation) {
        let string = stringRepresentation || this.toString();
        let column = this.source.start.column;
        let line2 = this.source.start.line;
        for (let i2 = 0; i2 < index22; i2++) {
          if (string[i2] === "\n") {
            column = 1;
            line2 += 1;
          } else {
            column += 1;
          }
        }
        return { column, line: line2 };
      }
      prev() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 - 1];
      }
      rangeBy(opts) {
        let start3 = {
          column: this.source.start.column,
          line: this.source.start.line
        };
        let end = this.source.end ? {
          column: this.source.end.column + 1,
          line: this.source.end.line
        } : {
          column: start3.column + 1,
          line: start3.line
        };
        if (opts.word) {
          let stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) {
            start3 = this.positionInside(index22, stringRepresentation);
            end = this.positionInside(index22 + opts.word.length, stringRepresentation);
          }
        } else {
          if (opts.start) {
            start3 = {
              column: opts.start.column,
              line: opts.start.line
            };
          } else if (opts.index) {
            start3 = this.positionInside(opts.index);
          }
          if (opts.end) {
            end = {
              column: opts.end.column,
              line: opts.end.line
            };
          } else if (typeof opts.endIndex === "number") {
            end = this.positionInside(opts.endIndex);
          } else if (opts.index) {
            end = this.positionInside(opts.index + 1);
          }
        }
        if (end.line < start3.line || end.line === start3.line && end.column <= start3.column) {
          end = { column: start3.column + 1, line: start3.line };
        }
        return { end, start: start3 };
      }
      raw(prop, defaultType) {
        let str = new Stringifier();
        return str.raw(this, prop, defaultType);
      }
      remove() {
        if (this.parent) {
          this.parent.removeChild(this);
        }
        this.parent = void 0;
        return this;
      }
      replaceWith(...nodes) {
        if (this.parent) {
          let bookmark = this;
          let foundSelf = false;
          for (let node22 of nodes) {
            if (node22 === this) {
              foundSelf = true;
            } else if (foundSelf) {
              this.parent.insertAfter(bookmark, node22);
              bookmark = node22;
            } else {
              this.parent.insertBefore(bookmark, node22);
            }
          }
          if (!foundSelf) {
            this.remove();
          }
        }
        return this;
      }
      root() {
        let result22 = this;
        while (result22.parent && result22.parent.type !== "document") {
          result22 = result22.parent;
        }
        return result22;
      }
      toJSON(_2, inputs) {
        let fixed = {};
        let emitInputs = inputs == null;
        inputs = inputs || /* @__PURE__ */ new Map();
        let inputsNextIndex = 0;
        for (let name in this) {
          if (!Object.prototype.hasOwnProperty.call(this, name)) {
            continue;
          }
          if (name === "parent" || name === "proxyCache") continue;
          let value = this[name];
          if (Array.isArray(value)) {
            fixed[name] = value.map((i2) => {
              if (typeof i2 === "object" && i2.toJSON) {
                return i2.toJSON(null, inputs);
              } else {
                return i2;
              }
            });
          } else if (typeof value === "object" && value.toJSON) {
            fixed[name] = value.toJSON(null, inputs);
          } else if (name === "source") {
            let inputId = inputs.get(value.input);
            if (inputId == null) {
              inputId = inputsNextIndex;
              inputs.set(value.input, inputsNextIndex);
              inputsNextIndex++;
            }
            fixed[name] = {
              end: value.end,
              inputId,
              start: value.start
            };
          } else {
            fixed[name] = value;
          }
        }
        if (emitInputs) {
          fixed.inputs = [...inputs.keys()].map((input22) => input22.toJSON());
        }
        return fixed;
      }
      toProxy() {
        if (!this.proxyCache) {
          this.proxyCache = new Proxy(this, this.getProxyProcessor());
        }
        return this.proxyCache;
      }
      toString(stringifier22 = stringify2) {
        if (stringifier22.stringify) stringifier22 = stringifier22.stringify;
        let result22 = "";
        stringifier22(this, (i2) => {
          result22 += i2;
        });
        return result22;
      }
      warn(result22, text, opts) {
        let data = { node: this };
        for (let i2 in opts) data[i2] = opts[i2];
        return result22.warn(text, data);
      }
      get proxyOf() {
        return this;
      }
    }
    node = Node2;
    Node2.default = Node2;
    return node;
  }
  function requireDeclaration() {
    if (hasRequiredDeclaration) return declaration;
    hasRequiredDeclaration = 1;
    let Node2 = requireNode();
    class Declaration extends Node2 {
      constructor(defaults) {
        if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
          defaults = { ...defaults, value: String(defaults.value) };
        }
        super(defaults);
        this.type = "decl";
      }
      get variable() {
        return this.prop.startsWith("--") || this.prop[0] === "$";
      }
    }
    declaration = Declaration;
    Declaration.default = Declaration;
    return declaration;
  }
  function requireNonSecure() {
    if (hasRequiredNonSecure) return nonSecure;
    hasRequiredNonSecure = 1;
    let urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
    let customAlphabet = (alphabet, defaultSize = 21) => {
      return (size = defaultSize) => {
        let id2 = "";
        let i2 = size;
        while (i2--) {
          id2 += alphabet[Math.random() * alphabet.length | 0];
        }
        return id2;
      };
    };
    let nanoid = (size = 21) => {
      let id2 = "";
      let i2 = size;
      while (i2--) {
        id2 += urlAlphabet[Math.random() * 64 | 0];
      }
      return id2;
    };
    nonSecure = { nanoid, customAlphabet };
    return nonSecure;
  }
  function requirePreviousMap() {
    if (hasRequiredPreviousMap) return previousMap;
    hasRequiredPreviousMap = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2;
    let { existsSync, readFileSync } = require$$2;
    let { dirname, join } = require$$2;
    function fromBase64(str) {
      if (Buffer) {
        return Buffer.from(str, "base64").toString();
      } else {
        return window.atob(str);
      }
    }
    class PreviousMap {
      constructor(css, opts) {
        if (opts.map === false) return;
        this.loadAnnotation(css);
        this.inline = this.startWith(this.annotation, "data:");
        let prev = opts.map ? opts.map.prev : void 0;
        let text = this.loadMap(opts.from, prev);
        if (!this.mapFile && opts.from) {
          this.mapFile = opts.from;
        }
        if (this.mapFile) this.root = dirname(this.mapFile);
        if (text) this.text = text;
      }
      consumer() {
        if (!this.consumerCache) {
          this.consumerCache = new SourceMapConsumer(this.text);
        }
        return this.consumerCache;
      }
      decodeInline(text) {
        let baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
        let baseUri = /^data:application\/json;base64,/;
        let charsetUri = /^data:application\/json;charset=utf-?8,/;
        let uri = /^data:application\/json,/;
        if (charsetUri.test(text) || uri.test(text)) {
          return decodeURIComponent(text.substr(RegExp.lastMatch.length));
        }
        if (baseCharsetUri.test(text) || baseUri.test(text)) {
          return fromBase64(text.substr(RegExp.lastMatch.length));
        }
        let encoding = text.match(/data:application\/json;([^,]+),/)[1];
        throw new Error("Unsupported source map encoding " + encoding);
      }
      getAnnotationURL(sourceMapString) {
        return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
      }
      isMap(map) {
        if (typeof map !== "object") return false;
        return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
      }
      loadAnnotation(css) {
        let comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
        if (!comments) return;
        let start3 = css.lastIndexOf(comments.pop());
        let end = css.indexOf("*/", start3);
        if (start3 > -1 && end > -1) {
          this.annotation = this.getAnnotationURL(css.substring(start3, end));
        }
      }
      loadFile(path) {
        this.root = dirname(path);
        if (existsSync(path)) {
          this.mapFile = path;
          return readFileSync(path, "utf-8").toString().trim();
        }
      }
      loadMap(file, prev) {
        if (prev === false) return false;
        if (prev) {
          if (typeof prev === "string") {
            return prev;
          } else if (typeof prev === "function") {
            let prevPath = prev(file);
            if (prevPath) {
              let map = this.loadFile(prevPath);
              if (!map) {
                throw new Error(
                  "Unable to load previous source map: " + prevPath.toString()
                );
              }
              return map;
            }
          } else if (prev instanceof SourceMapConsumer) {
            return SourceMapGenerator.fromSourceMap(prev).toString();
          } else if (prev instanceof SourceMapGenerator) {
            return prev.toString();
          } else if (this.isMap(prev)) {
            return JSON.stringify(prev);
          } else {
            throw new Error(
              "Unsupported previous source map format: " + prev.toString()
            );
          }
        } else if (this.inline) {
          return this.decodeInline(this.annotation);
        } else if (this.annotation) {
          let map = this.annotation;
          if (file) map = join(dirname(file), map);
          return this.loadFile(map);
        }
      }
      startWith(string, start3) {
        if (!string) return false;
        return string.substr(0, start3.length) === start3;
      }
      withContent() {
        return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
      }
    }
    previousMap = PreviousMap;
    PreviousMap.default = PreviousMap;
    return previousMap;
  }
  function requireInput() {
    if (hasRequiredInput) return input;
    hasRequiredInput = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2;
    let { fileURLToPath, pathToFileURL } = require$$2;
    let { isAbsolute, resolve } = require$$2;
    let { nanoid } = /* @__PURE__ */ requireNonSecure();
    let terminalHighlight = require$$2;
    let CssSyntaxError = requireCssSyntaxError();
    let PreviousMap = requirePreviousMap();
    let fromOffsetCache = Symbol("fromOffsetCache");
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(resolve && isAbsolute);
    class Input {
      constructor(css, opts = {}) {
        if (css === null || typeof css === "undefined" || typeof css === "object" && !css.toString) {
          throw new Error(`PostCSS received ${css} instead of CSS string`);
        }
        this.css = css.toString();
        if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
          this.hasBOM = true;
          this.css = this.css.slice(1);
        } else {
          this.hasBOM = false;
        }
        if (opts.from) {
          if (!pathAvailable || /^\w+:\/\//.test(opts.from) || isAbsolute(opts.from)) {
            this.file = opts.from;
          } else {
            this.file = resolve(opts.from);
          }
        }
        if (pathAvailable && sourceMapAvailable) {
          let map = new PreviousMap(this.css, opts);
          if (map.text) {
            this.map = map;
            let file = map.consumer().file;
            if (!this.file && file) this.file = this.mapResolve(file);
          }
        }
        if (!this.file) {
          this.id = "<input css " + nanoid(6) + ">";
        }
        if (this.map) this.map.file = this.from;
      }
      error(message, line2, column, opts = {}) {
        let result22, endLine, endColumn;
        if (line2 && typeof line2 === "object") {
          let start3 = line2;
          let end = column;
          if (typeof start3.offset === "number") {
            let pos = this.fromOffset(start3.offset);
            line2 = pos.line;
            column = pos.col;
          } else {
            line2 = start3.line;
            column = start3.column;
          }
          if (typeof end.offset === "number") {
            let pos = this.fromOffset(end.offset);
            endLine = pos.line;
            endColumn = pos.col;
          } else {
            endLine = end.line;
            endColumn = end.column;
          }
        } else if (!column) {
          let pos = this.fromOffset(line2);
          line2 = pos.line;
          column = pos.col;
        }
        let origin = this.origin(line2, column, endLine, endColumn);
        if (origin) {
          result22 = new CssSyntaxError(
            message,
            origin.endLine === void 0 ? origin.line : { column: origin.column, line: origin.line },
            origin.endLine === void 0 ? origin.column : { column: origin.endColumn, line: origin.endLine },
            origin.source,
            origin.file,
            opts.plugin
          );
        } else {
          result22 = new CssSyntaxError(
            message,
            endLine === void 0 ? line2 : { column, line: line2 },
            endLine === void 0 ? column : { column: endColumn, line: endLine },
            this.css,
            this.file,
            opts.plugin
          );
        }
        result22.input = { column, endColumn, endLine, line: line2, source: this.css };
        if (this.file) {
          if (pathToFileURL) {
            result22.input.url = pathToFileURL(this.file).toString();
          }
          result22.input.file = this.file;
        }
        return result22;
      }
      fromOffset(offset) {
        let lastLine, lineToIndex;
        if (!this[fromOffsetCache]) {
          let lines = this.css.split("\n");
          lineToIndex = new Array(lines.length);
          let prevIndex = 0;
          for (let i2 = 0, l2 = lines.length; i2 < l2; i2++) {
            lineToIndex[i2] = prevIndex;
            prevIndex += lines[i2].length + 1;
          }
          this[fromOffsetCache] = lineToIndex;
        } else {
          lineToIndex = this[fromOffsetCache];
        }
        lastLine = lineToIndex[lineToIndex.length - 1];
        let min = 0;
        if (offset >= lastLine) {
          min = lineToIndex.length - 1;
        } else {
          let max = lineToIndex.length - 2;
          let mid;
          while (min < max) {
            mid = min + (max - min >> 1);
            if (offset < lineToIndex[mid]) {
              max = mid - 1;
            } else if (offset >= lineToIndex[mid + 1]) {
              min = mid + 1;
            } else {
              min = mid;
              break;
            }
          }
        }
        return {
          col: offset - lineToIndex[min] + 1,
          line: min + 1
        };
      }
      mapResolve(file) {
        if (/^\w+:\/\//.test(file)) {
          return file;
        }
        return resolve(this.map.consumer().sourceRoot || this.map.root || ".", file);
      }
      origin(line2, column, endLine, endColumn) {
        if (!this.map) return false;
        let consumer = this.map.consumer();
        let from = consumer.originalPositionFor({ column, line: line2 });
        if (!from.source) return false;
        let to;
        if (typeof endLine === "number") {
          to = consumer.originalPositionFor({ column: endColumn, line: endLine });
        }
        let fromUrl;
        if (isAbsolute(from.source)) {
          fromUrl = pathToFileURL(from.source);
        } else {
          fromUrl = new URL(
            from.source,
            this.map.consumer().sourceRoot || pathToFileURL(this.map.mapFile)
          );
        }
        let result22 = {
          column: from.column,
          endColumn: to && to.column,
          endLine: to && to.line,
          line: from.line,
          url: fromUrl.toString()
        };
        if (fromUrl.protocol === "file:") {
          if (fileURLToPath) {
            result22.file = fileURLToPath(fromUrl);
          } else {
            throw new Error(`file: protocol is not available in this PostCSS build`);
          }
        }
        let source = consumer.sourceContentFor(from.source);
        if (source) result22.source = source;
        return result22;
      }
      toJSON() {
        let json = {};
        for (let name of ["hasBOM", "css", "file", "id"]) {
          if (this[name] != null) {
            json[name] = this[name];
          }
        }
        if (this.map) {
          json.map = { ...this.map };
          if (json.map.consumerCache) {
            json.map.consumerCache = void 0;
          }
        }
        return json;
      }
      get from() {
        return this.file || this.id;
      }
    }
    input = Input;
    Input.default = Input;
    if (terminalHighlight && terminalHighlight.registerInput) {
      terminalHighlight.registerInput(Input);
    }
    return input;
  }
  function requireMapGenerator() {
    if (hasRequiredMapGenerator) return mapGenerator;
    hasRequiredMapGenerator = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2;
    let { dirname, relative, resolve, sep } = require$$2;
    let { pathToFileURL } = require$$2;
    let Input = requireInput();
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(dirname && resolve && relative && sep);
    class MapGenerator {
      constructor(stringify2, root22, opts, cssString) {
        this.stringify = stringify2;
        this.mapOpts = opts.map || {};
        this.root = root22;
        this.opts = opts;
        this.css = cssString;
        this.originalCSS = cssString;
        this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
        this.memoizedFileURLs = /* @__PURE__ */ new Map();
        this.memoizedPaths = /* @__PURE__ */ new Map();
        this.memoizedURLs = /* @__PURE__ */ new Map();
      }
      addAnnotation() {
        let content;
        if (this.isInline()) {
          content = "data:application/json;base64," + this.toBase64(this.map.toString());
        } else if (typeof this.mapOpts.annotation === "string") {
          content = this.mapOpts.annotation;
        } else if (typeof this.mapOpts.annotation === "function") {
          content = this.mapOpts.annotation(this.opts.to, this.root);
        } else {
          content = this.outputFile() + ".map";
        }
        let eol = "\n";
        if (this.css.includes("\r\n")) eol = "\r\n";
        this.css += eol + "/*# sourceMappingURL=" + content + " */";
      }
      applyPrevMaps() {
        for (let prev of this.previous()) {
          let from = this.toUrl(this.path(prev.file));
          let root22 = prev.root || dirname(prev.file);
          let map;
          if (this.mapOpts.sourcesContent === false) {
            map = new SourceMapConsumer(prev.text);
            if (map.sourcesContent) {
              map.sourcesContent = null;
            }
          } else {
            map = prev.consumer();
          }
          this.map.applySourceMap(map, from, this.toUrl(this.path(root22)));
        }
      }
      clearAnnotation() {
        if (this.mapOpts.annotation === false) return;
        if (this.root) {
          let node22;
          for (let i2 = this.root.nodes.length - 1; i2 >= 0; i2--) {
            node22 = this.root.nodes[i2];
            if (node22.type !== "comment") continue;
            if (node22.text.indexOf("# sourceMappingURL=") === 0) {
              this.root.removeChild(i2);
            }
          }
        } else if (this.css) {
          this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
        }
      }
      generate() {
        this.clearAnnotation();
        if (pathAvailable && sourceMapAvailable && this.isMap()) {
          return this.generateMap();
        } else {
          let result22 = "";
          this.stringify(this.root, (i2) => {
            result22 += i2;
          });
          return [result22];
        }
      }
      generateMap() {
        if (this.root) {
          this.generateString();
        } else if (this.previous().length === 1) {
          let prev = this.previous()[0].consumer();
          prev.file = this.outputFile();
          this.map = SourceMapGenerator.fromSourceMap(prev, {
            ignoreInvalidMapping: true
          });
        } else {
          this.map = new SourceMapGenerator({
            file: this.outputFile(),
            ignoreInvalidMapping: true
          });
          this.map.addMapping({
            generated: { column: 0, line: 1 },
            original: { column: 0, line: 1 },
            source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
          });
        }
        if (this.isSourcesContent()) this.setSourcesContent();
        if (this.root && this.previous().length > 0) this.applyPrevMaps();
        if (this.isAnnotation()) this.addAnnotation();
        if (this.isInline()) {
          return [this.css];
        } else {
          return [this.css, this.map];
        }
      }
      generateString() {
        this.css = "";
        this.map = new SourceMapGenerator({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        let line2 = 1;
        let column = 1;
        let noSource = "<no source>";
        let mapping = {
          generated: { column: 0, line: 0 },
          original: { column: 0, line: 0 },
          source: ""
        };
        let lines, last;
        this.stringify(this.root, (str, node22, type) => {
          this.css += str;
          if (node22 && type !== "end") {
            mapping.generated.line = line2;
            mapping.generated.column = column - 1;
            if (node22.source && node22.source.start) {
              mapping.source = this.sourcePath(node22);
              mapping.original.line = node22.source.start.line;
              mapping.original.column = node22.source.start.column - 1;
              this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              this.map.addMapping(mapping);
            }
          }
          lines = str.match(/\n/g);
          if (lines) {
            line2 += lines.length;
            last = str.lastIndexOf("\n");
            column = str.length - last;
          } else {
            column += str.length;
          }
          if (node22 && type !== "start") {
            let p2 = node22.parent || { raws: {} };
            let childless = node22.type === "decl" || node22.type === "atrule" && !node22.nodes;
            if (!childless || node22 !== p2.last || p2.raws.semicolon) {
              if (node22.source && node22.source.end) {
                mapping.source = this.sourcePath(node22);
                mapping.original.line = node22.source.end.line;
                mapping.original.column = node22.source.end.column - 1;
                mapping.generated.line = line2;
                mapping.generated.column = column - 2;
                this.map.addMapping(mapping);
              } else {
                mapping.source = noSource;
                mapping.original.line = 1;
                mapping.original.column = 0;
                mapping.generated.line = line2;
                mapping.generated.column = column - 1;
                this.map.addMapping(mapping);
              }
            }
          }
        });
      }
      isAnnotation() {
        if (this.isInline()) {
          return true;
        }
        if (typeof this.mapOpts.annotation !== "undefined") {
          return this.mapOpts.annotation;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.annotation);
        }
        return true;
      }
      isInline() {
        if (typeof this.mapOpts.inline !== "undefined") {
          return this.mapOpts.inline;
        }
        let annotation = this.mapOpts.annotation;
        if (typeof annotation !== "undefined" && annotation !== true) {
          return false;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.inline);
        }
        return true;
      }
      isMap() {
        if (typeof this.opts.map !== "undefined") {
          return !!this.opts.map;
        }
        return this.previous().length > 0;
      }
      isSourcesContent() {
        if (typeof this.mapOpts.sourcesContent !== "undefined") {
          return this.mapOpts.sourcesContent;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.withContent());
        }
        return true;
      }
      outputFile() {
        if (this.opts.to) {
          return this.path(this.opts.to);
        } else if (this.opts.from) {
          return this.path(this.opts.from);
        } else {
          return "to.css";
        }
      }
      path(file) {
        if (this.mapOpts.absolute) return file;
        if (file.charCodeAt(0) === 60) return file;
        if (/^\w+:\/\//.test(file)) return file;
        let cached = this.memoizedPaths.get(file);
        if (cached) return cached;
        let from = this.opts.to ? dirname(this.opts.to) : ".";
        if (typeof this.mapOpts.annotation === "string") {
          from = dirname(resolve(from, this.mapOpts.annotation));
        }
        let path = relative(from, file);
        this.memoizedPaths.set(file, path);
        return path;
      }
      previous() {
        if (!this.previousMaps) {
          this.previousMaps = [];
          if (this.root) {
            this.root.walk((node22) => {
              if (node22.source && node22.source.input.map) {
                let map = node22.source.input.map;
                if (!this.previousMaps.includes(map)) {
                  this.previousMaps.push(map);
                }
              }
            });
          } else {
            let input22 = new Input(this.originalCSS, this.opts);
            if (input22.map) this.previousMaps.push(input22.map);
          }
        }
        return this.previousMaps;
      }
      setSourcesContent() {
        let already = {};
        if (this.root) {
          this.root.walk((node22) => {
            if (node22.source) {
              let from = node22.source.input.from;
              if (from && !already[from]) {
                already[from] = true;
                let fromUrl = this.usesFileUrls ? this.toFileUrl(from) : this.toUrl(this.path(from));
                this.map.setSourceContent(fromUrl, node22.source.input.css);
              }
            }
          });
        } else if (this.css) {
          let from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
          this.map.setSourceContent(from, this.css);
        }
      }
      sourcePath(node22) {
        if (this.mapOpts.from) {
          return this.toUrl(this.mapOpts.from);
        } else if (this.usesFileUrls) {
          return this.toFileUrl(node22.source.input.from);
        } else {
          return this.toUrl(this.path(node22.source.input.from));
        }
      }
      toBase64(str) {
        if (Buffer) {
          return Buffer.from(str).toString("base64");
        } else {
          return window.btoa(unescape(encodeURIComponent(str)));
        }
      }
      toFileUrl(path) {
        let cached = this.memoizedFileURLs.get(path);
        if (cached) return cached;
        if (pathToFileURL) {
          let fileURL = pathToFileURL(path).toString();
          this.memoizedFileURLs.set(path, fileURL);
          return fileURL;
        } else {
          throw new Error(
            "`map.absolute` option is not available in this PostCSS build"
          );
        }
      }
      toUrl(path) {
        let cached = this.memoizedURLs.get(path);
        if (cached) return cached;
        if (sep === "\\") {
          path = path.replace(/\\/g, "/");
        }
        let url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
        this.memoizedURLs.set(path, url);
        return url;
      }
    }
    mapGenerator = MapGenerator;
    return mapGenerator;
  }
  function requireComment() {
    if (hasRequiredComment) return comment;
    hasRequiredComment = 1;
    let Node2 = requireNode();
    class Comment extends Node2 {
      constructor(defaults) {
        super(defaults);
        this.type = "comment";
      }
    }
    comment = Comment;
    Comment.default = Comment;
    return comment;
  }
  function requireContainer() {
    if (hasRequiredContainer) return container;
    hasRequiredContainer = 1;
    let { isClean, my } = requireSymbols();
    let Declaration = requireDeclaration();
    let Comment = requireComment();
    let Node2 = requireNode();
    let parse, Rule, AtRule, Root;
    function cleanSource(nodes) {
      return nodes.map((i2) => {
        if (i2.nodes) i2.nodes = cleanSource(i2.nodes);
        delete i2.source;
        return i2;
      });
    }
    function markDirtyUp(node22) {
      node22[isClean] = false;
      if (node22.proxyOf.nodes) {
        for (let i2 of node22.proxyOf.nodes) {
          markDirtyUp(i2);
        }
      }
    }
    class Container extends Node2 {
      append(...children) {
        for (let child of children) {
          let nodes = this.normalize(child, this.last);
          for (let node22 of nodes) this.proxyOf.nodes.push(node22);
        }
        this.markDirty();
        return this;
      }
      cleanRaws(keepBetween) {
        super.cleanRaws(keepBetween);
        if (this.nodes) {
          for (let node22 of this.nodes) node22.cleanRaws(keepBetween);
        }
      }
      each(callback) {
        if (!this.proxyOf.nodes) return void 0;
        let iterator = this.getIterator();
        let index22, result22;
        while (this.indexes[iterator] < this.proxyOf.nodes.length) {
          index22 = this.indexes[iterator];
          result22 = callback(this.proxyOf.nodes[index22], index22);
          if (result22 === false) break;
          this.indexes[iterator] += 1;
        }
        delete this.indexes[iterator];
        return result22;
      }
      every(condition) {
        return this.nodes.every(condition);
      }
      getIterator() {
        if (!this.lastEach) this.lastEach = 0;
        if (!this.indexes) this.indexes = {};
        this.lastEach += 1;
        let iterator = this.lastEach;
        this.indexes[iterator] = 0;
        return iterator;
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (!node22[prop]) {
              return node22[prop];
            } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
              return (...args) => {
                return node22[prop](
                  ...args.map((i2) => {
                    if (typeof i2 === "function") {
                      return (child, index22) => i2(child.toProxy(), index22);
                    } else {
                      return i2;
                    }
                  })
                );
              };
            } else if (prop === "every" || prop === "some") {
              return (cb) => {
                return node22[prop](
                  (child, ...other) => cb(child.toProxy(), ...other)
                );
              };
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else if (prop === "nodes") {
              return node22.nodes.map((i2) => i2.toProxy());
            } else if (prop === "first" || prop === "last") {
              return node22[prop].toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "name" || prop === "params" || prop === "selector") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      index(child) {
        if (typeof child === "number") return child;
        if (child.proxyOf) child = child.proxyOf;
        return this.proxyOf.nodes.indexOf(child);
      }
      insertAfter(exist, add2) {
        let existIndex = this.index(exist);
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex]).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex + 1, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex < index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      insertBefore(exist, add2) {
        let existIndex = this.index(exist);
        let type = existIndex === 0 ? "prepend" : false;
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex], type).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex <= index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      normalize(nodes, sample) {
        if (typeof nodes === "string") {
          nodes = cleanSource(parse(nodes).nodes);
        } else if (typeof nodes === "undefined") {
          nodes = [];
        } else if (Array.isArray(nodes)) {
          nodes = nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type === "root" && this.type !== "document") {
          nodes = nodes.nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type) {
          nodes = [nodes];
        } else if (nodes.prop) {
          if (typeof nodes.value === "undefined") {
            throw new Error("Value field is missed in node creation");
          } else if (typeof nodes.value !== "string") {
            nodes.value = String(nodes.value);
          }
          nodes = [new Declaration(nodes)];
        } else if (nodes.selector) {
          nodes = [new Rule(nodes)];
        } else if (nodes.name) {
          nodes = [new AtRule(nodes)];
        } else if (nodes.text) {
          nodes = [new Comment(nodes)];
        } else {
          throw new Error("Unknown node type in node creation");
        }
        let processed = nodes.map((i2) => {
          if (!i2[my]) Container.rebuild(i2);
          i2 = i2.proxyOf;
          if (i2.parent) i2.parent.removeChild(i2);
          if (i2[isClean]) markDirtyUp(i2);
          if (typeof i2.raws.before === "undefined") {
            if (sample && typeof sample.raws.before !== "undefined") {
              i2.raws.before = sample.raws.before.replace(/\S/g, "");
            }
          }
          i2.parent = this.proxyOf;
          return i2;
        });
        return processed;
      }
      prepend(...children) {
        children = children.reverse();
        for (let child of children) {
          let nodes = this.normalize(child, this.first, "prepend").reverse();
          for (let node22 of nodes) this.proxyOf.nodes.unshift(node22);
          for (let id2 in this.indexes) {
            this.indexes[id2] = this.indexes[id2] + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      push(child) {
        child.parent = this;
        this.proxyOf.nodes.push(child);
        return this;
      }
      removeAll() {
        for (let node22 of this.proxyOf.nodes) node22.parent = void 0;
        this.proxyOf.nodes = [];
        this.markDirty();
        return this;
      }
      removeChild(child) {
        child = this.index(child);
        this.proxyOf.nodes[child].parent = void 0;
        this.proxyOf.nodes.splice(child, 1);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (index22 >= child) {
            this.indexes[id2] = index22 - 1;
          }
        }
        this.markDirty();
        return this;
      }
      replaceValues(pattern, opts, callback) {
        if (!callback) {
          callback = opts;
          opts = {};
        }
        this.walkDecls((decl) => {
          if (opts.props && !opts.props.includes(decl.prop)) return;
          if (opts.fast && !decl.value.includes(opts.fast)) return;
          decl.value = decl.value.replace(pattern, callback);
        });
        this.markDirty();
        return this;
      }
      some(condition) {
        return this.nodes.some(condition);
      }
      walk(callback) {
        return this.each((child, i2) => {
          let result22;
          try {
            result22 = callback(child, i2);
          } catch (e2) {
            throw child.addToError(e2);
          }
          if (result22 !== false && child.walk) {
            result22 = child.walk(callback);
          }
          return result22;
        });
      }
      walkAtRules(name, callback) {
        if (!callback) {
          callback = name;
          return this.walk((child, i2) => {
            if (child.type === "atrule") {
              return callback(child, i2);
            }
          });
        }
        if (name instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "atrule" && name.test(child.name)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "atrule" && child.name === name) {
            return callback(child, i2);
          }
        });
      }
      walkComments(callback) {
        return this.walk((child, i2) => {
          if (child.type === "comment") {
            return callback(child, i2);
          }
        });
      }
      walkDecls(prop, callback) {
        if (!callback) {
          callback = prop;
          return this.walk((child, i2) => {
            if (child.type === "decl") {
              return callback(child, i2);
            }
          });
        }
        if (prop instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "decl" && prop.test(child.prop)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "decl" && child.prop === prop) {
            return callback(child, i2);
          }
        });
      }
      walkRules(selector2, callback) {
        if (!callback) {
          callback = selector2;
          return this.walk((child, i2) => {
            if (child.type === "rule") {
              return callback(child, i2);
            }
          });
        }
        if (selector2 instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "rule" && selector2.test(child.selector)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "rule" && child.selector === selector2) {
            return callback(child, i2);
          }
        });
      }
      get first() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[0];
      }
      get last() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
      }
    }
    Container.registerParse = (dependant) => {
      parse = dependant;
    };
    Container.registerRule = (dependant) => {
      Rule = dependant;
    };
    Container.registerAtRule = (dependant) => {
      AtRule = dependant;
    };
    Container.registerRoot = (dependant) => {
      Root = dependant;
    };
    container = Container;
    Container.default = Container;
    Container.rebuild = (node22) => {
      if (node22.type === "atrule") {
        Object.setPrototypeOf(node22, AtRule.prototype);
      } else if (node22.type === "rule") {
        Object.setPrototypeOf(node22, Rule.prototype);
      } else if (node22.type === "decl") {
        Object.setPrototypeOf(node22, Declaration.prototype);
      } else if (node22.type === "comment") {
        Object.setPrototypeOf(node22, Comment.prototype);
      } else if (node22.type === "root") {
        Object.setPrototypeOf(node22, Root.prototype);
      }
      node22[my] = true;
      if (node22.nodes) {
        node22.nodes.forEach((child) => {
          Container.rebuild(child);
        });
      }
    };
    return container;
  }
  function requireDocument() {
    if (hasRequiredDocument) return document$1;
    hasRequiredDocument = 1;
    let Container = requireContainer();
    let LazyResult, Processor;
    class Document2 extends Container {
      constructor(defaults) {
        super({ type: "document", ...defaults });
        if (!this.nodes) {
          this.nodes = [];
        }
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Document2.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Document2.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    document$1 = Document2;
    Document2.default = Document2;
    return document$1;
  }
  function requireWarnOnce() {
    if (hasRequiredWarnOnce) return warnOnce;
    hasRequiredWarnOnce = 1;
    let printed = {};
    warnOnce = function warnOnce22(message) {
      if (printed[message]) return;
      printed[message] = true;
      if (typeof console !== "undefined" && console.warn) {
        console.warn(message);
      }
    };
    return warnOnce;
  }
  function requireWarning() {
    if (hasRequiredWarning) return warning;
    hasRequiredWarning = 1;
    class Warning {
      constructor(text, opts = {}) {
        this.type = "warning";
        this.text = text;
        if (opts.node && opts.node.source) {
          let range = opts.node.rangeBy(opts);
          this.line = range.start.line;
          this.column = range.start.column;
          this.endLine = range.end.line;
          this.endColumn = range.end.column;
        }
        for (let opt in opts) this[opt] = opts[opt];
      }
      toString() {
        if (this.node) {
          return this.node.error(this.text, {
            index: this.index,
            plugin: this.plugin,
            word: this.word
          }).message;
        }
        if (this.plugin) {
          return this.plugin + ": " + this.text;
        }
        return this.text;
      }
    }
    warning = Warning;
    Warning.default = Warning;
    return warning;
  }
  function requireResult() {
    if (hasRequiredResult) return result;
    hasRequiredResult = 1;
    let Warning = requireWarning();
    class Result {
      constructor(processor22, root22, opts) {
        this.processor = processor22;
        this.messages = [];
        this.root = root22;
        this.opts = opts;
        this.css = void 0;
        this.map = void 0;
      }
      toString() {
        return this.css;
      }
      warn(text, opts = {}) {
        if (!opts.plugin) {
          if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
            opts.plugin = this.lastPlugin.postcssPlugin;
          }
        }
        let warning22 = new Warning(text, opts);
        this.messages.push(warning22);
        return warning22;
      }
      warnings() {
        return this.messages.filter((i2) => i2.type === "warning");
      }
      get content() {
        return this.css;
      }
    }
    result = Result;
    Result.default = Result;
    return result;
  }
  function requireTokenize() {
    if (hasRequiredTokenize) return tokenize;
    hasRequiredTokenize = 1;
    const SINGLE_QUOTE = "'".charCodeAt(0);
    const DOUBLE_QUOTE = '"'.charCodeAt(0);
    const BACKSLASH = "\\".charCodeAt(0);
    const SLASH = "/".charCodeAt(0);
    const NEWLINE = "\n".charCodeAt(0);
    const SPACE = " ".charCodeAt(0);
    const FEED = "\f".charCodeAt(0);
    const TAB = "	".charCodeAt(0);
    const CR = "\r".charCodeAt(0);
    const OPEN_SQUARE = "[".charCodeAt(0);
    const CLOSE_SQUARE = "]".charCodeAt(0);
    const OPEN_PARENTHESES = "(".charCodeAt(0);
    const CLOSE_PARENTHESES = ")".charCodeAt(0);
    const OPEN_CURLY = "{".charCodeAt(0);
    const CLOSE_CURLY = "}".charCodeAt(0);
    const SEMICOLON = ";".charCodeAt(0);
    const ASTERISK = "*".charCodeAt(0);
    const COLON = ":".charCodeAt(0);
    const AT = "@".charCodeAt(0);
    const RE_AT_END = /[\t\n\f\r "#'()/;[\\\]{}]/g;
    const RE_WORD_END = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
    const RE_BAD_BRACKET = /.[\r\n"'(/\\]/;
    const RE_HEX_ESCAPE = /[\da-f]/i;
    tokenize = function tokenizer(input22, options = {}) {
      let css = input22.css.valueOf();
      let ignore = options.ignoreErrors;
      let code, next, quote, content, escape2;
      let escaped, escapePos, prev, n22, currentToken;
      let length = css.length;
      let pos = 0;
      let buffer = [];
      let returned = [];
      function position() {
        return pos;
      }
      function unclosed(what) {
        throw input22.error("Unclosed " + what, pos);
      }
      function endOfFile() {
        return returned.length === 0 && pos >= length;
      }
      function nextToken(opts) {
        if (returned.length) return returned.pop();
        if (pos >= length) return;
        let ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
        code = css.charCodeAt(pos);
        switch (code) {
          case NEWLINE:
          case SPACE:
          case TAB:
          case CR:
          case FEED: {
            next = pos;
            do {
              next += 1;
              code = css.charCodeAt(next);
            } while (code === SPACE || code === NEWLINE || code === TAB || code === CR || code === FEED);
            currentToken = ["space", css.slice(pos, next)];
            pos = next - 1;
            break;
          }
          case OPEN_SQUARE:
          case CLOSE_SQUARE:
          case OPEN_CURLY:
          case CLOSE_CURLY:
          case COLON:
          case SEMICOLON:
          case CLOSE_PARENTHESES: {
            let controlChar = String.fromCharCode(code);
            currentToken = [controlChar, controlChar, pos];
            break;
          }
          case OPEN_PARENTHESES: {
            prev = buffer.length ? buffer.pop()[1] : "";
            n22 = css.charCodeAt(pos + 1);
            if (prev === "url" && n22 !== SINGLE_QUOTE && n22 !== DOUBLE_QUOTE && n22 !== SPACE && n22 !== NEWLINE && n22 !== TAB && n22 !== FEED && n22 !== CR) {
              next = pos;
              do {
                escaped = false;
                next = css.indexOf(")", next + 1);
                if (next === -1) {
                  if (ignore || ignoreUnclosed) {
                    next = pos;
                    break;
                  } else {
                    unclosed("bracket");
                  }
                }
                escapePos = next;
                while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                  escapePos -= 1;
                  escaped = !escaped;
                }
              } while (escaped);
              currentToken = ["brackets", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              next = css.indexOf(")", pos + 1);
              content = css.slice(pos, next + 1);
              if (next === -1 || RE_BAD_BRACKET.test(content)) {
                currentToken = ["(", "(", pos];
              } else {
                currentToken = ["brackets", content, pos, next];
                pos = next;
              }
            }
            break;
          }
          case SINGLE_QUOTE:
          case DOUBLE_QUOTE: {
            quote = code === SINGLE_QUOTE ? "'" : '"';
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(quote, next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos + 1;
                  break;
                } else {
                  unclosed("string");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = ["string", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case AT: {
            RE_AT_END.lastIndex = pos + 1;
            RE_AT_END.test(css);
            if (RE_AT_END.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_AT_END.lastIndex - 2;
            }
            currentToken = ["at-word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case BACKSLASH: {
            next = pos;
            escape2 = true;
            while (css.charCodeAt(next + 1) === BACKSLASH) {
              next += 1;
              escape2 = !escape2;
            }
            code = css.charCodeAt(next + 1);
            if (escape2 && code !== SLASH && code !== SPACE && code !== NEWLINE && code !== TAB && code !== CR && code !== FEED) {
              next += 1;
              if (RE_HEX_ESCAPE.test(css.charAt(next))) {
                while (RE_HEX_ESCAPE.test(css.charAt(next + 1))) {
                  next += 1;
                }
                if (css.charCodeAt(next + 1) === SPACE) {
                  next += 1;
                }
              }
            }
            currentToken = ["word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          default: {
            if (code === SLASH && css.charCodeAt(pos + 1) === ASTERISK) {
              next = css.indexOf("*/", pos + 2) + 1;
              if (next === 0) {
                if (ignore || ignoreUnclosed) {
                  next = css.length;
                } else {
                  unclosed("comment");
                }
              }
              currentToken = ["comment", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              RE_WORD_END.lastIndex = pos + 1;
              RE_WORD_END.test(css);
              if (RE_WORD_END.lastIndex === 0) {
                next = css.length - 1;
              } else {
                next = RE_WORD_END.lastIndex - 2;
              }
              currentToken = ["word", css.slice(pos, next + 1), pos, next];
              buffer.push(currentToken);
              pos = next;
            }
            break;
          }
        }
        pos++;
        return currentToken;
      }
      function back(token) {
        returned.push(token);
      }
      return {
        back,
        endOfFile,
        nextToken,
        position
      };
    };
    return tokenize;
  }
  function requireAtRule() {
    if (hasRequiredAtRule) return atRule;
    hasRequiredAtRule = 1;
    let Container = requireContainer();
    class AtRule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "atrule";
      }
      append(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.append(...children);
      }
      prepend(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.prepend(...children);
      }
    }
    atRule = AtRule;
    AtRule.default = AtRule;
    Container.registerAtRule(AtRule);
    return atRule;
  }
  function requireRoot() {
    if (hasRequiredRoot) return root;
    hasRequiredRoot = 1;
    let Container = requireContainer();
    let LazyResult, Processor;
    class Root extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "root";
        if (!this.nodes) this.nodes = [];
      }
      normalize(child, sample, type) {
        let nodes = super.normalize(child);
        if (sample) {
          if (type === "prepend") {
            if (this.nodes.length > 1) {
              sample.raws.before = this.nodes[1].raws.before;
            } else {
              delete sample.raws.before;
            }
          } else if (this.first !== sample) {
            for (let node22 of nodes) {
              node22.raws.before = sample.raws.before;
            }
          }
        }
        return nodes;
      }
      removeChild(child, ignore) {
        let index22 = this.index(child);
        if (!ignore && index22 === 0 && this.nodes.length > 1) {
          this.nodes[1].raws.before = this.nodes[index22].raws.before;
        }
        return super.removeChild(child);
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Root.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Root.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    root = Root;
    Root.default = Root;
    Container.registerRoot(Root);
    return root;
  }
  function requireList() {
    if (hasRequiredList) return list_1;
    hasRequiredList = 1;
    let list = {
      comma(string) {
        return list.split(string, [","], true);
      },
      space(string) {
        let spaces = [" ", "\n", "	"];
        return list.split(string, spaces);
      },
      split(string, separators, last) {
        let array = [];
        let current = "";
        let split = false;
        let func = 0;
        let inQuote = false;
        let prevQuote = "";
        let escape2 = false;
        for (let letter of string) {
          if (escape2) {
            escape2 = false;
          } else if (letter === "\\") {
            escape2 = true;
          } else if (inQuote) {
            if (letter === prevQuote) {
              inQuote = false;
            }
          } else if (letter === '"' || letter === "'") {
            inQuote = true;
            prevQuote = letter;
          } else if (letter === "(") {
            func += 1;
          } else if (letter === ")") {
            if (func > 0) func -= 1;
          } else if (func === 0) {
            if (separators.includes(letter)) split = true;
          }
          if (split) {
            if (current !== "") array.push(current.trim());
            current = "";
            split = false;
          } else {
            current += letter;
          }
        }
        if (last || current !== "") array.push(current.trim());
        return array;
      }
    };
    list_1 = list;
    list.default = list;
    return list_1;
  }
  function requireRule() {
    if (hasRequiredRule) return rule;
    hasRequiredRule = 1;
    let Container = requireContainer();
    let list = requireList();
    class Rule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "rule";
        if (!this.nodes) this.nodes = [];
      }
      get selectors() {
        return list.comma(this.selector);
      }
      set selectors(values) {
        let match = this.selector ? this.selector.match(/,\s*/) : null;
        let sep = match ? match[0] : "," + this.raw("between", "beforeOpen");
        this.selector = values.join(sep);
      }
    }
    rule = Rule;
    Rule.default = Rule;
    Container.registerRule(Rule);
    return rule;
  }
  function requireParser() {
    if (hasRequiredParser) return parser;
    hasRequiredParser = 1;
    let Declaration = requireDeclaration();
    let tokenizer = requireTokenize();
    let Comment = requireComment();
    let AtRule = requireAtRule();
    let Root = requireRoot();
    let Rule = requireRule();
    const SAFE_COMMENT_NEIGHBOR = {
      empty: true,
      space: true
    };
    function findLastWithPosition(tokens) {
      for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
        let token = tokens[i2];
        let pos = token[3] || token[2];
        if (pos) return pos;
      }
    }
    class Parser {
      constructor(input22) {
        this.input = input22;
        this.root = new Root();
        this.current = this.root;
        this.spaces = "";
        this.semicolon = false;
        this.createTokenizer();
        this.root.source = { input: input22, start: { column: 1, line: 1, offset: 0 } };
      }
      atrule(token) {
        let node22 = new AtRule();
        node22.name = token[1].slice(1);
        if (node22.name === "") {
          this.unnamedAtrule(node22, token);
        }
        this.init(node22, token[2]);
        let type;
        let prev;
        let shift;
        let last = false;
        let open = false;
        let params = [];
        let brackets = [];
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          type = token[0];
          if (type === "(" || type === "[") {
            brackets.push(type === "(" ? ")" : "]");
          } else if (type === "{" && brackets.length > 0) {
            brackets.push("}");
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
          }
          if (brackets.length === 0) {
            if (type === ";") {
              node22.source.end = this.getPosition(token[2]);
              node22.source.end.offset++;
              this.semicolon = true;
              break;
            } else if (type === "{") {
              open = true;
              break;
            } else if (type === "}") {
              if (params.length > 0) {
                shift = params.length - 1;
                prev = params[shift];
                while (prev && prev[0] === "space") {
                  prev = params[--shift];
                }
                if (prev) {
                  node22.source.end = this.getPosition(prev[3] || prev[2]);
                  node22.source.end.offset++;
                }
              }
              this.end(token);
              break;
            } else {
              params.push(token);
            }
          } else {
            params.push(token);
          }
          if (this.tokenizer.endOfFile()) {
            last = true;
            break;
          }
        }
        node22.raws.between = this.spacesAndCommentsFromEnd(params);
        if (params.length) {
          node22.raws.afterName = this.spacesAndCommentsFromStart(params);
          this.raw(node22, "params", params);
          if (last) {
            token = params[params.length - 1];
            node22.source.end = this.getPosition(token[3] || token[2]);
            node22.source.end.offset++;
            this.spaces = node22.raws.between;
            node22.raws.between = "";
          }
        } else {
          node22.raws.afterName = "";
          node22.params = "";
        }
        if (open) {
          node22.nodes = [];
          this.current = node22;
        }
      }
      checkMissedSemicolon(tokens) {
        let colon = this.colon(tokens);
        if (colon === false) return;
        let founded = 0;
        let token;
        for (let j = colon - 1; j >= 0; j--) {
          token = tokens[j];
          if (token[0] !== "space") {
            founded += 1;
            if (founded === 2) break;
          }
        }
        throw this.input.error(
          "Missed semicolon",
          token[0] === "word" ? token[3] + 1 : token[2]
        );
      }
      colon(tokens) {
        let brackets = 0;
        let token, type, prev;
        for (let [i2, element] of tokens.entries()) {
          token = element;
          type = token[0];
          if (type === "(") {
            brackets += 1;
          }
          if (type === ")") {
            brackets -= 1;
          }
          if (brackets === 0 && type === ":") {
            if (!prev) {
              this.doubleColon(token);
            } else if (prev[0] === "word" && prev[1] === "progid") {
              continue;
            } else {
              return i2;
            }
          }
          prev = token;
        }
        return false;
      }
      comment(token) {
        let node22 = new Comment();
        this.init(node22, token[2]);
        node22.source.end = this.getPosition(token[3] || token[2]);
        node22.source.end.offset++;
        let text = token[1].slice(2, -2);
        if (/^\s*$/.test(text)) {
          node22.text = "";
          node22.raws.left = text;
          node22.raws.right = "";
        } else {
          let match = text.match(/^(\s*)([^]*\S)(\s*)$/);
          node22.text = match[2];
          node22.raws.left = match[1];
          node22.raws.right = match[3];
        }
      }
      createTokenizer() {
        this.tokenizer = tokenizer(this.input);
      }
      decl(tokens, customProperty) {
        let node22 = new Declaration();
        this.init(node22, tokens[0][2]);
        let last = tokens[tokens.length - 1];
        if (last[0] === ";") {
          this.semicolon = true;
          tokens.pop();
        }
        node22.source.end = this.getPosition(
          last[3] || last[2] || findLastWithPosition(tokens)
        );
        node22.source.end.offset++;
        while (tokens[0][0] !== "word") {
          if (tokens.length === 1) this.unknownWord(tokens);
          node22.raws.before += tokens.shift()[1];
        }
        node22.source.start = this.getPosition(tokens[0][2]);
        node22.prop = "";
        while (tokens.length) {
          let type = tokens[0][0];
          if (type === ":" || type === "space" || type === "comment") {
            break;
          }
          node22.prop += tokens.shift()[1];
        }
        node22.raws.between = "";
        let token;
        while (tokens.length) {
          token = tokens.shift();
          if (token[0] === ":") {
            node22.raws.between += token[1];
            break;
          } else {
            if (token[0] === "word" && /\w/.test(token[1])) {
              this.unknownWord([token]);
            }
            node22.raws.between += token[1];
          }
        }
        if (node22.prop[0] === "_" || node22.prop[0] === "*") {
          node22.raws.before += node22.prop[0];
          node22.prop = node22.prop.slice(1);
        }
        let firstSpaces = [];
        let next;
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          firstSpaces.push(tokens.shift());
        }
        this.precheckMissedSemicolon(tokens);
        for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
          token = tokens[i2];
          if (token[1].toLowerCase() === "!important") {
            node22.important = true;
            let string = this.stringFrom(tokens, i2);
            string = this.spacesFromEnd(tokens) + string;
            if (string !== " !important") node22.raws.important = string;
            break;
          } else if (token[1].toLowerCase() === "important") {
            let cache = tokens.slice(0);
            let str = "";
            for (let j = i2; j > 0; j--) {
              let type = cache[j][0];
              if (str.trim().indexOf("!") === 0 && type !== "space") {
                break;
              }
              str = cache.pop()[1] + str;
            }
            if (str.trim().indexOf("!") === 0) {
              node22.important = true;
              node22.raws.important = str;
              tokens = cache;
            }
          }
          if (token[0] !== "space" && token[0] !== "comment") {
            break;
          }
        }
        let hasWord = tokens.some((i2) => i2[0] !== "space" && i2[0] !== "comment");
        if (hasWord) {
          node22.raws.between += firstSpaces.map((i2) => i2[1]).join("");
          firstSpaces = [];
        }
        this.raw(node22, "value", firstSpaces.concat(tokens), customProperty);
        if (node22.value.includes(":") && !customProperty) {
          this.checkMissedSemicolon(tokens);
        }
      }
      doubleColon(token) {
        throw this.input.error(
          "Double colon",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
      emptyRule(token) {
        let node22 = new Rule();
        this.init(node22, token[2]);
        node22.selector = "";
        node22.raws.between = "";
        this.current = node22;
      }
      end(token) {
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.semicolon = false;
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.spaces = "";
        if (this.current.parent) {
          this.current.source.end = this.getPosition(token[2]);
          this.current.source.end.offset++;
          this.current = this.current.parent;
        } else {
          this.unexpectedClose(token);
        }
      }
      endFile() {
        if (this.current.parent) this.unclosedBlock();
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.root.source.end = this.getPosition(this.tokenizer.position());
      }
      freeSemicolon(token) {
        this.spaces += token[1];
        if (this.current.nodes) {
          let prev = this.current.nodes[this.current.nodes.length - 1];
          if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
            prev.raws.ownSemicolon = this.spaces;
            this.spaces = "";
          }
        }
      }
      // Helpers
      getPosition(offset) {
        let pos = this.input.fromOffset(offset);
        return {
          column: pos.col,
          line: pos.line,
          offset
        };
      }
      init(node22, offset) {
        this.current.push(node22);
        node22.source = {
          input: this.input,
          start: this.getPosition(offset)
        };
        node22.raws.before = this.spaces;
        this.spaces = "";
        if (node22.type !== "comment") this.semicolon = false;
      }
      other(start3) {
        let end = false;
        let type = null;
        let colon = false;
        let bracket = null;
        let brackets = [];
        let customProperty = start3[1].startsWith("--");
        let tokens = [];
        let token = start3;
        while (token) {
          type = token[0];
          tokens.push(token);
          if (type === "(" || type === "[") {
            if (!bracket) bracket = token;
            brackets.push(type === "(" ? ")" : "]");
          } else if (customProperty && colon && type === "{") {
            if (!bracket) bracket = token;
            brackets.push("}");
          } else if (brackets.length === 0) {
            if (type === ";") {
              if (colon) {
                this.decl(tokens, customProperty);
                return;
              } else {
                break;
              }
            } else if (type === "{") {
              this.rule(tokens);
              return;
            } else if (type === "}") {
              this.tokenizer.back(tokens.pop());
              end = true;
              break;
            } else if (type === ":") {
              colon = true;
            }
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
            if (brackets.length === 0) bracket = null;
          }
          token = this.tokenizer.nextToken();
        }
        if (this.tokenizer.endOfFile()) end = true;
        if (brackets.length > 0) this.unclosedBracket(bracket);
        if (end && colon) {
          if (!customProperty) {
            while (tokens.length) {
              token = tokens[tokens.length - 1][0];
              if (token !== "space" && token !== "comment") break;
              this.tokenizer.back(tokens.pop());
            }
          }
          this.decl(tokens, customProperty);
        } else {
          this.unknownWord(tokens);
        }
      }
      parse() {
        let token;
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          switch (token[0]) {
            case "space":
              this.spaces += token[1];
              break;
            case ";":
              this.freeSemicolon(token);
              break;
            case "}":
              this.end(token);
              break;
            case "comment":
              this.comment(token);
              break;
            case "at-word":
              this.atrule(token);
              break;
            case "{":
              this.emptyRule(token);
              break;
            default:
              this.other(token);
              break;
          }
        }
        this.endFile();
      }
      precheckMissedSemicolon() {
      }
      raw(node22, prop, tokens, customProperty) {
        let token, type;
        let length = tokens.length;
        let value = "";
        let clean = true;
        let next, prev;
        for (let i2 = 0; i2 < length; i2 += 1) {
          token = tokens[i2];
          type = token[0];
          if (type === "space" && i2 === length - 1 && !customProperty) {
            clean = false;
          } else if (type === "comment") {
            prev = tokens[i2 - 1] ? tokens[i2 - 1][0] : "empty";
            next = tokens[i2 + 1] ? tokens[i2 + 1][0] : "empty";
            if (!SAFE_COMMENT_NEIGHBOR[prev] && !SAFE_COMMENT_NEIGHBOR[next]) {
              if (value.slice(-1) === ",") {
                clean = false;
              } else {
                value += token[1];
              }
            } else {
              clean = false;
            }
          } else {
            value += token[1];
          }
        }
        if (!clean) {
          let raw = tokens.reduce((all, i2) => all + i2[1], "");
          node22.raws[prop] = { raw, value };
        }
        node22[prop] = value;
      }
      rule(tokens) {
        tokens.pop();
        let node22 = new Rule();
        this.init(node22, tokens[0][2]);
        node22.raws.between = this.spacesAndCommentsFromEnd(tokens);
        this.raw(node22, "selector", tokens);
        this.current = node22;
      }
      spacesAndCommentsFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space" && lastTokenType !== "comment") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      // Errors
      spacesAndCommentsFromStart(tokens) {
        let next;
        let spaces = "";
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          spaces += tokens.shift()[1];
        }
        return spaces;
      }
      spacesFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      stringFrom(tokens, from) {
        let result22 = "";
        for (let i2 = from; i2 < tokens.length; i2++) {
          result22 += tokens[i2][1];
        }
        tokens.splice(from, tokens.length - from);
        return result22;
      }
      unclosedBlock() {
        let pos = this.current.source.start;
        throw this.input.error("Unclosed block", pos.line, pos.column);
      }
      unclosedBracket(bracket) {
        throw this.input.error(
          "Unclosed bracket",
          { offset: bracket[2] },
          { offset: bracket[2] + 1 }
        );
      }
      unexpectedClose(token) {
        throw this.input.error(
          "Unexpected }",
          { offset: token[2] },
          { offset: token[2] + 1 }
        );
      }
      unknownWord(tokens) {
        throw this.input.error(
          "Unknown word",
          { offset: tokens[0][2] },
          { offset: tokens[0][2] + tokens[0][1].length }
        );
      }
      unnamedAtrule(node22, token) {
        throw this.input.error(
          "At-rule without name",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
    }
    parser = Parser;
    return parser;
  }
  function requireParse() {
    if (hasRequiredParse) return parse_1;
    hasRequiredParse = 1;
    let Container = requireContainer();
    let Parser = requireParser();
    let Input = requireInput();
    function parse(css, opts) {
      let input22 = new Input(css, opts);
      let parser22 = new Parser(input22);
      try {
        parser22.parse();
      } catch (e2) {
        if (true) {
          if (e2.name === "CssSyntaxError" && opts && opts.from) {
            if (/\.scss$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
            } else if (/\.sass/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
            } else if (/\.less$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
            }
          }
        }
        throw e2;
      }
      return parser22.root;
    }
    parse_1 = parse;
    parse.default = parse;
    Container.registerParse(parse);
    return parse_1;
  }
  function requireLazyResult() {
    if (hasRequiredLazyResult) return lazyResult;
    hasRequiredLazyResult = 1;
    let { isClean, my } = requireSymbols();
    let MapGenerator = requireMapGenerator();
    let stringify2 = requireStringify();
    let Container = requireContainer();
    let Document2 = requireDocument();
    let warnOnce22 = requireWarnOnce();
    let Result = requireResult();
    let parse = requireParse();
    let Root = requireRoot();
    const TYPE_TO_CLASS_NAME = {
      atrule: "AtRule",
      comment: "Comment",
      decl: "Declaration",
      document: "Document",
      root: "Root",
      rule: "Rule"
    };
    const PLUGIN_PROPS = {
      AtRule: true,
      AtRuleExit: true,
      Comment: true,
      CommentExit: true,
      Declaration: true,
      DeclarationExit: true,
      Document: true,
      DocumentExit: true,
      Once: true,
      OnceExit: true,
      postcssPlugin: true,
      prepare: true,
      Root: true,
      RootExit: true,
      Rule: true,
      RuleExit: true
    };
    const NOT_VISITORS = {
      Once: true,
      postcssPlugin: true,
      prepare: true
    };
    const CHILDREN = 0;
    function isPromise(obj) {
      return typeof obj === "object" && typeof obj.then === "function";
    }
    function getEvents(node22) {
      let key = false;
      let type = TYPE_TO_CLASS_NAME[node22.type];
      if (node22.type === "decl") {
        key = node22.prop.toLowerCase();
      } else if (node22.type === "atrule") {
        key = node22.name.toLowerCase();
      }
      if (key && node22.append) {
        return [
          type,
          type + "-" + key,
          CHILDREN,
          type + "Exit",
          type + "Exit-" + key
        ];
      } else if (key) {
        return [type, type + "-" + key, type + "Exit", type + "Exit-" + key];
      } else if (node22.append) {
        return [type, CHILDREN, type + "Exit"];
      } else {
        return [type, type + "Exit"];
      }
    }
    function toStack(node22) {
      let events;
      if (node22.type === "document") {
        events = ["Document", CHILDREN, "DocumentExit"];
      } else if (node22.type === "root") {
        events = ["Root", CHILDREN, "RootExit"];
      } else {
        events = getEvents(node22);
      }
      return {
        eventIndex: 0,
        events,
        iterator: 0,
        node: node22,
        visitorIndex: 0,
        visitors: []
      };
    }
    function cleanMarks(node22) {
      node22[isClean] = false;
      if (node22.nodes) node22.nodes.forEach((i2) => cleanMarks(i2));
      return node22;
    }
    let postcss22 = {};
    class LazyResult {
      constructor(processor22, css, opts) {
        this.stringified = false;
        this.processed = false;
        let root22;
        if (typeof css === "object" && css !== null && (css.type === "root" || css.type === "document")) {
          root22 = cleanMarks(css);
        } else if (css instanceof LazyResult || css instanceof Result) {
          root22 = cleanMarks(css.root);
          if (css.map) {
            if (typeof opts.map === "undefined") opts.map = {};
            if (!opts.map.inline) opts.map.inline = false;
            opts.map.prev = css.map;
          }
        } else {
          let parser22 = parse;
          if (opts.syntax) parser22 = opts.syntax.parse;
          if (opts.parser) parser22 = opts.parser;
          if (parser22.parse) parser22 = parser22.parse;
          try {
            root22 = parser22(css, opts);
          } catch (error) {
            this.processed = true;
            this.error = error;
          }
          if (root22 && !root22[my]) {
            Container.rebuild(root22);
          }
        }
        this.result = new Result(processor22, root22, opts);
        this.helpers = { ...postcss22, postcss: postcss22, result: this.result };
        this.plugins = this.processor.plugins.map((plugin) => {
          if (typeof plugin === "object" && plugin.prepare) {
            return { ...plugin, ...plugin.prepare(this.result) };
          } else {
            return plugin;
          }
        });
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        if (this.processed) return Promise.resolve(this.result);
        if (!this.processing) {
          this.processing = this.runAsync();
        }
        return this.processing;
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      getAsyncError() {
        throw new Error("Use process(css).then(cb) to work with async plugins");
      }
      handleError(error, node22) {
        let plugin = this.result.lastPlugin;
        try {
          if (node22) node22.addToError(error);
          this.error = error;
          if (error.name === "CssSyntaxError" && !error.plugin) {
            error.plugin = plugin.postcssPlugin;
            error.setMessage();
          } else if (plugin.postcssVersion) {
            if (true) {
              let pluginName = plugin.postcssPlugin;
              let pluginVer = plugin.postcssVersion;
              let runtimeVer = this.result.processor.version;
              let a2 = pluginVer.split(".");
              let b2 = runtimeVer.split(".");
              if (a2[0] !== b2[0] || parseInt(a2[1]) > parseInt(b2[1])) {
                console.error(
                  "Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below."
                );
              }
            }
          }
        } catch (err) {
          if (console && console.error) console.error(err);
        }
        return error;
      }
      prepareVisitors() {
        this.listeners = {};
        let add2 = (plugin, type, cb) => {
          if (!this.listeners[type]) this.listeners[type] = [];
          this.listeners[type].push([plugin, cb]);
        };
        for (let plugin of this.plugins) {
          if (typeof plugin === "object") {
            for (let event in plugin) {
              if (!PLUGIN_PROPS[event] && /^[A-Z]/.test(event)) {
                throw new Error(
                  `Unknown event ${event} in ${plugin.postcssPlugin}. Try to update PostCSS (${this.processor.version} now).`
                );
              }
              if (!NOT_VISITORS[event]) {
                if (typeof plugin[event] === "object") {
                  for (let filter in plugin[event]) {
                    if (filter === "*") {
                      add2(plugin, event, plugin[event][filter]);
                    } else {
                      add2(
                        plugin,
                        event + "-" + filter.toLowerCase(),
                        plugin[event][filter]
                      );
                    }
                  }
                } else if (typeof plugin[event] === "function") {
                  add2(plugin, event, plugin[event]);
                }
              }
            }
          }
        }
        this.hasListener = Object.keys(this.listeners).length > 0;
      }
      async runAsync() {
        this.plugin = 0;
        for (let i2 = 0; i2 < this.plugins.length; i2++) {
          let plugin = this.plugins[i2];
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            try {
              await promise;
            } catch (error) {
              throw this.handleError(error);
            }
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            let stack = [toStack(root22)];
            while (stack.length > 0) {
              let promise = this.visitTick(stack);
              if (isPromise(promise)) {
                try {
                  await promise;
                } catch (e2) {
                  let node22 = stack[stack.length - 1].node;
                  throw this.handleError(e2, node22);
                }
              }
            }
          }
          if (this.listeners.OnceExit) {
            for (let [plugin, visitor] of this.listeners.OnceExit) {
              this.result.lastPlugin = plugin;
              try {
                if (root22.type === "document") {
                  let roots = root22.nodes.map(
                    (subRoot) => visitor(subRoot, this.helpers)
                  );
                  await Promise.all(roots);
                } else {
                  await visitor(root22, this.helpers);
                }
              } catch (e2) {
                throw this.handleError(e2);
              }
            }
          }
        }
        this.processed = true;
        return this.stringify();
      }
      runOnRoot(plugin) {
        this.result.lastPlugin = plugin;
        try {
          if (typeof plugin === "object" && plugin.Once) {
            if (this.result.root.type === "document") {
              let roots = this.result.root.nodes.map(
                (root22) => plugin.Once(root22, this.helpers)
              );
              if (isPromise(roots[0])) {
                return Promise.all(roots);
              }
              return roots;
            }
            return plugin.Once(this.result.root, this.helpers);
          } else if (typeof plugin === "function") {
            return plugin(this.result.root, this.result);
          }
        } catch (error) {
          throw this.handleError(error);
        }
      }
      stringify() {
        if (this.error) throw this.error;
        if (this.stringified) return this.result;
        this.stringified = true;
        this.sync();
        let opts = this.result.opts;
        let str = stringify2;
        if (opts.syntax) str = opts.syntax.stringify;
        if (opts.stringifier) str = opts.stringifier;
        if (str.stringify) str = str.stringify;
        let map = new MapGenerator(str, this.result.root, this.result.opts);
        let data = map.generate();
        this.result.css = data[0];
        this.result.map = data[1];
        return this.result;
      }
      sync() {
        if (this.error) throw this.error;
        if (this.processed) return this.result;
        this.processed = true;
        if (this.processing) {
          throw this.getAsyncError();
        }
        for (let plugin of this.plugins) {
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            this.walkSync(root22);
          }
          if (this.listeners.OnceExit) {
            if (root22.type === "document") {
              for (let subRoot of root22.nodes) {
                this.visitSync(this.listeners.OnceExit, subRoot);
              }
            } else {
              this.visitSync(this.listeners.OnceExit, root22);
            }
          }
        }
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this.opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this.css;
      }
      visitSync(visitors, node22) {
        for (let [plugin, visitor] of visitors) {
          this.result.lastPlugin = plugin;
          let promise;
          try {
            promise = visitor(node22, this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22.proxyOf);
          }
          if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
            return true;
          }
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
      }
      visitTick(stack) {
        let visit2 = stack[stack.length - 1];
        let { node: node22, visitors } = visit2;
        if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
          stack.pop();
          return;
        }
        if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
          let [plugin, visitor] = visitors[visit2.visitorIndex];
          visit2.visitorIndex += 1;
          if (visit2.visitorIndex === visitors.length) {
            visit2.visitors = [];
            visit2.visitorIndex = 0;
          }
          this.result.lastPlugin = plugin;
          try {
            return visitor(node22.toProxy(), this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22);
          }
        }
        if (visit2.iterator !== 0) {
          let iterator = visit2.iterator;
          let child;
          while (child = node22.nodes[node22.indexes[iterator]]) {
            node22.indexes[iterator] += 1;
            if (!child[isClean]) {
              child[isClean] = true;
              stack.push(toStack(child));
              return;
            }
          }
          visit2.iterator = 0;
          delete node22.indexes[iterator];
        }
        let events = visit2.events;
        while (visit2.eventIndex < events.length) {
          let event = events[visit2.eventIndex];
          visit2.eventIndex += 1;
          if (event === CHILDREN) {
            if (node22.nodes && node22.nodes.length) {
              node22[isClean] = true;
              visit2.iterator = node22.getIterator();
            }
            return;
          } else if (this.listeners[event]) {
            visit2.visitors = this.listeners[event];
            return;
          }
        }
        stack.pop();
      }
      walkSync(node22) {
        node22[isClean] = true;
        let events = getEvents(node22);
        for (let event of events) {
          if (event === CHILDREN) {
            if (node22.nodes) {
              node22.each((child) => {
                if (!child[isClean]) this.walkSync(child);
              });
            }
          } else {
            let visitors = this.listeners[event];
            if (visitors) {
              if (this.visitSync(visitors, node22.toProxy())) return;
            }
          }
        }
      }
      warnings() {
        return this.sync().warnings();
      }
      get content() {
        return this.stringify().content;
      }
      get css() {
        return this.stringify().css;
      }
      get map() {
        return this.stringify().map;
      }
      get messages() {
        return this.sync().messages;
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        return this.sync().root;
      }
      get [Symbol.toStringTag]() {
        return "LazyResult";
      }
    }
    LazyResult.registerPostcss = (dependant) => {
      postcss22 = dependant;
    };
    lazyResult = LazyResult;
    LazyResult.default = LazyResult;
    Root.registerLazyResult(LazyResult);
    Document2.registerLazyResult(LazyResult);
    return lazyResult;
  }
  function requireNoWorkResult() {
    if (hasRequiredNoWorkResult) return noWorkResult;
    hasRequiredNoWorkResult = 1;
    let MapGenerator = requireMapGenerator();
    let stringify2 = requireStringify();
    let warnOnce22 = requireWarnOnce();
    let parse = requireParse();
    const Result = requireResult();
    class NoWorkResult {
      constructor(processor22, css, opts) {
        css = css.toString();
        this.stringified = false;
        this._processor = processor22;
        this._css = css;
        this._opts = opts;
        this._map = void 0;
        let root22;
        let str = stringify2;
        this.result = new Result(this._processor, root22, this._opts);
        this.result.css = css;
        let self2 = this;
        Object.defineProperty(this.result, "root", {
          get() {
            return self2.root;
          }
        });
        let map = new MapGenerator(str, root22, this._opts, css);
        if (map.isMap()) {
          let [generatedCSS, generatedMap] = map.generate();
          if (generatedCSS) {
            this.result.css = generatedCSS;
          }
          if (generatedMap) {
            this.result.map = generatedMap;
          }
        } else {
          map.clearAnnotation();
          this.result.css = map.css;
        }
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        return Promise.resolve(this.result);
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      sync() {
        if (this.error) throw this.error;
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this._opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this._css;
      }
      warnings() {
        return [];
      }
      get content() {
        return this.result.css;
      }
      get css() {
        return this.result.css;
      }
      get map() {
        return this.result.map;
      }
      get messages() {
        return [];
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        if (this._root) {
          return this._root;
        }
        let root22;
        let parser22 = parse;
        try {
          root22 = parser22(this._css, this._opts);
        } catch (error) {
          this.error = error;
        }
        if (this.error) {
          throw this.error;
        } else {
          this._root = root22;
          return root22;
        }
      }
      get [Symbol.toStringTag]() {
        return "NoWorkResult";
      }
    }
    noWorkResult = NoWorkResult;
    NoWorkResult.default = NoWorkResult;
    return noWorkResult;
  }
  function requireProcessor() {
    if (hasRequiredProcessor) return processor;
    hasRequiredProcessor = 1;
    let NoWorkResult = requireNoWorkResult();
    let LazyResult = requireLazyResult();
    let Document2 = requireDocument();
    let Root = requireRoot();
    class Processor {
      constructor(plugins = []) {
        this.version = "8.4.38";
        this.plugins = this.normalize(plugins);
      }
      normalize(plugins) {
        let normalized = [];
        for (let i2 of plugins) {
          if (i2.postcss === true) {
            i2 = i2();
          } else if (i2.postcss) {
            i2 = i2.postcss;
          }
          if (typeof i2 === "object" && Array.isArray(i2.plugins)) {
            normalized = normalized.concat(i2.plugins);
          } else if (typeof i2 === "object" && i2.postcssPlugin) {
            normalized.push(i2);
          } else if (typeof i2 === "function") {
            normalized.push(i2);
          } else if (typeof i2 === "object" && (i2.parse || i2.stringify)) {
            if (true) {
              throw new Error(
                "PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation."
              );
            }
          } else {
            throw new Error(i2 + " is not a PostCSS plugin");
          }
        }
        return normalized;
      }
      process(css, opts = {}) {
        if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
          return new NoWorkResult(this, css, opts);
        } else {
          return new LazyResult(this, css, opts);
        }
      }
      use(plugin) {
        this.plugins = this.plugins.concat(this.normalize([plugin]));
        return this;
      }
    }
    processor = Processor;
    Processor.default = Processor;
    Root.registerProcessor(Processor);
    Document2.registerProcessor(Processor);
    return processor;
  }
  function requireFromJSON() {
    if (hasRequiredFromJSON) return fromJSON_1;
    hasRequiredFromJSON = 1;
    let Declaration = requireDeclaration();
    let PreviousMap = requirePreviousMap();
    let Comment = requireComment();
    let AtRule = requireAtRule();
    let Input = requireInput();
    let Root = requireRoot();
    let Rule = requireRule();
    function fromJSON(json, inputs) {
      if (Array.isArray(json)) return json.map((n22) => fromJSON(n22));
      let { inputs: ownInputs, ...defaults } = json;
      if (ownInputs) {
        inputs = [];
        for (let input22 of ownInputs) {
          let inputHydrated = { ...input22, __proto__: Input.prototype };
          if (inputHydrated.map) {
            inputHydrated.map = {
              ...inputHydrated.map,
              __proto__: PreviousMap.prototype
            };
          }
          inputs.push(inputHydrated);
        }
      }
      if (defaults.nodes) {
        defaults.nodes = json.nodes.map((n22) => fromJSON(n22, inputs));
      }
      if (defaults.source) {
        let { inputId, ...source } = defaults.source;
        defaults.source = source;
        if (inputId != null) {
          defaults.source.input = inputs[inputId];
        }
      }
      if (defaults.type === "root") {
        return new Root(defaults);
      } else if (defaults.type === "decl") {
        return new Declaration(defaults);
      } else if (defaults.type === "rule") {
        return new Rule(defaults);
      } else if (defaults.type === "comment") {
        return new Comment(defaults);
      } else if (defaults.type === "atrule") {
        return new AtRule(defaults);
      } else {
        throw new Error("Unknown node type: " + json.type);
      }
    }
    fromJSON_1 = fromJSON;
    fromJSON.default = fromJSON;
    return fromJSON_1;
  }
  function requirePostcss() {
    if (hasRequiredPostcss) return postcss_1;
    hasRequiredPostcss = 1;
    let CssSyntaxError = requireCssSyntaxError();
    let Declaration = requireDeclaration();
    let LazyResult = requireLazyResult();
    let Container = requireContainer();
    let Processor = requireProcessor();
    let stringify2 = requireStringify();
    let fromJSON = requireFromJSON();
    let Document2 = requireDocument();
    let Warning = requireWarning();
    let Comment = requireComment();
    let AtRule = requireAtRule();
    let Result = requireResult();
    let Input = requireInput();
    let parse = requireParse();
    let list = requireList();
    let Rule = requireRule();
    let Root = requireRoot();
    let Node2 = requireNode();
    function postcss22(...plugins) {
      if (plugins.length === 1 && Array.isArray(plugins[0])) {
        plugins = plugins[0];
      }
      return new Processor(plugins);
    }
    postcss22.plugin = function plugin(name, initializer) {
      let warningPrinted = false;
      function creator(...args) {
        if (console && console.warn && !warningPrinted) {
          warningPrinted = true;
          console.warn(
            name + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration"
          );
          if (process.env.LANG && process.env.LANG.startsWith("cn")) {
            console.warn(
              name + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226"
            );
          }
        }
        let transformer = initializer(...args);
        transformer.postcssPlugin = name;
        transformer.postcssVersion = new Processor().version;
        return transformer;
      }
      let cache;
      Object.defineProperty(creator, "postcss", {
        get() {
          if (!cache) cache = creator();
          return cache;
        }
      });
      creator.process = function(css, processOpts, pluginOpts) {
        return postcss22([creator(pluginOpts)]).process(css, processOpts);
      };
      return creator;
    };
    postcss22.stringify = stringify2;
    postcss22.parse = parse;
    postcss22.fromJSON = fromJSON;
    postcss22.list = list;
    postcss22.comment = (defaults) => new Comment(defaults);
    postcss22.atRule = (defaults) => new AtRule(defaults);
    postcss22.decl = (defaults) => new Declaration(defaults);
    postcss22.rule = (defaults) => new Rule(defaults);
    postcss22.root = (defaults) => new Root(defaults);
    postcss22.document = (defaults) => new Document2(defaults);
    postcss22.CssSyntaxError = CssSyntaxError;
    postcss22.Declaration = Declaration;
    postcss22.Container = Container;
    postcss22.Processor = Processor;
    postcss22.Document = Document2;
    postcss22.Comment = Comment;
    postcss22.Warning = Warning;
    postcss22.AtRule = AtRule;
    postcss22.Result = Result;
    postcss22.Input = Input;
    postcss22.Rule = Rule;
    postcss22.Root = Root;
    postcss22.Node = Node2;
    LazyResult.registerPostcss(postcss22);
    postcss_1 = postcss22;
    postcss22.default = postcss22;
    return postcss_1;
  }
  function angularZoneUnpatchedAlternative(key) {
    var _a22, _b;
    const angularUnpatchedVersionSymbol = (_b = (_a22 = globalThis == null ? void 0 : globalThis.Zone) == null ? void 0 : _a22.__symbol__) == null ? void 0 : _b.call(_a22, key);
    if (angularUnpatchedVersionSymbol && globalThis[angularUnpatchedVersionSymbol]) {
      return globalThis[angularUnpatchedVersionSymbol];
    } else {
      return void 0;
    }
  }
  function getUntaintedPrototype(key) {
    if (untaintedBasePrototype[key])
      return untaintedBasePrototype[key];
    const candidate = angularZoneUnpatchedAlternative(key) || globalThis[key];
    const defaultPrototype = candidate.prototype;
    const accessorNames = key in testableAccessors ? testableAccessors[key] : void 0;
    const isUntaintedAccessors = Boolean(
      accessorNames && // @ts-expect-error 2345
      accessorNames.every(
        (accessor) => {
          var _a22, _b;
          return Boolean(
            (_b = (_a22 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a22.get) == null ? void 0 : _b.toString().includes("[native code]")
          );
        }
      )
    );
    const methodNames = key in testableMethods ? testableMethods[key] : void 0;
    const isUntaintedMethods = Boolean(
      methodNames && methodNames.every(
        // @ts-expect-error 2345
        (method) => {
          var _a22;
          return typeof defaultPrototype[method] === "function" && ((_a22 = defaultPrototype[method]) == null ? void 0 : _a22.toString().includes("[native code]"));
        }
      )
    );
    if (isUntaintedAccessors && isUntaintedMethods) {
      untaintedBasePrototype[key] = candidate.prototype;
      return candidate.prototype;
    }
    try {
      const iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      const win = iframeEl.contentWindow;
      if (!win) return candidate.prototype;
      const untaintedObject = win[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype[key] = untaintedObject;
    } catch {
      return defaultPrototype;
    }
  }
  function getUntaintedAccessor(key, instance, accessor) {
    var _a22;
    const cacheKey = `${key}.${String(accessor)}`;
    if (untaintedAccessorCache[cacheKey])
      return untaintedAccessorCache[cacheKey].call(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype(key);
    const untaintedAccessor = (_a22 = Object.getOwnPropertyDescriptor(
      untaintedPrototype,
      accessor
    )) == null ? void 0 : _a22.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  function getUntaintedMethod(key, instance, method) {
    const cacheKey = `${key}.${String(method)}`;
    if (untaintedMethodCache[cacheKey])
      return untaintedMethodCache[cacheKey].bind(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype(key);
    const untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes(n22) {
    return getUntaintedAccessor("Node", n22, "childNodes");
  }
  function parentNode(n22) {
    return getUntaintedAccessor("Node", n22, "parentNode");
  }
  function parentElement(n22) {
    return getUntaintedAccessor("Node", n22, "parentElement");
  }
  function textContent(n22) {
    return getUntaintedAccessor("Node", n22, "textContent");
  }
  function contains(n22, other) {
    return getUntaintedMethod("Node", n22, "contains")(other);
  }
  function getRootNode(n22) {
    return getUntaintedMethod("Node", n22, "getRootNode")();
  }
  function host(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor("ShadowRoot", n22, "host");
  }
  function styleSheets(n22) {
    return n22.styleSheets;
  }
  function shadowRoot(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor("Element", n22, "shadowRoot");
  }
  function querySelector(n22, selectors) {
    return getUntaintedAccessor("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll(n22, selectors) {
    return getUntaintedAccessor("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor() {
    return getUntaintedPrototype("MutationObserver").constructor;
  }
  function on(type, fn, target = document) {
    const options = { capture: true, passive: true };
    target.addEventListener(type, fn, options);
    return () => target.removeEventListener(type, fn, options);
  }
  function throttle(func, wait, options = {}) {
    let timeout = null;
    let previous = 0;
    return function(...args) {
      const now = Date.now();
      if (!previous && options.leading === false) {
        previous = now;
      }
      const remaining = wait - (now - previous);
      const context = this;
      if (remaining <= 0 || remaining > wait) {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
        previous = now;
        func.apply(context, args);
      } else if (!timeout && options.trailing !== false) {
        timeout = setTimeout(() => {
          previous = options.leading === false ? 0 : Date.now();
          timeout = null;
          func.apply(context, args);
        }, remaining);
      }
    };
  }
  function hookSetter(target, key, d2, isRevoked, win = window) {
    const original = win.Object.getOwnPropertyDescriptor(target, key);
    win.Object.defineProperty(
      target,
      key,
      isRevoked ? d2 : {
        set(value) {
          setTimeout(() => {
            d2.set.call(this, value);
          }, 0);
          if (original && original.set) {
            original.set.call(this, value);
          }
        }
      }
    );
    return () => hookSetter(target, key, original || {}, true);
  }
  function patch(source, name, replacement) {
    try {
      if (!(name in source)) {
        return () => {
        };
      }
      const original = source[name];
      const wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name] = wrapped;
      return () => {
        source[name] = original;
      };
    } catch {
      return () => {
      };
    }
  }
  function getWindowScroll(win) {
    var _a22, _b, _c, _d;
    const doc = win.document;
    return {
      left: doc.scrollingElement ? doc.scrollingElement.scrollLeft : win.pageXOffset !== void 0 ? win.pageXOffset : doc.documentElement.scrollLeft || (doc == null ? void 0 : doc.body) && ((_a22 = index2.parentElement(doc.body)) == null ? void 0 : _a22.scrollLeft) || ((_b = doc == null ? void 0 : doc.body) == null ? void 0 : _b.scrollLeft) || 0,
      top: doc.scrollingElement ? doc.scrollingElement.scrollTop : win.pageYOffset !== void 0 ? win.pageYOffset : (doc == null ? void 0 : doc.documentElement.scrollTop) || (doc == null ? void 0 : doc.body) && ((_c = index2.parentElement(doc.body)) == null ? void 0 : _c.scrollTop) || ((_d = doc == null ? void 0 : doc.body) == null ? void 0 : _d.scrollTop) || 0
    };
  }
  function getWindowHeight() {
    return window.innerHeight || document.documentElement && document.documentElement.clientHeight || document.body && document.body.clientHeight;
  }
  function getWindowWidth() {
    return window.innerWidth || document.documentElement && document.documentElement.clientWidth || document.body && document.body.clientWidth;
  }
  function closestElementOfNode(node22) {
    if (!node22) {
      return null;
    }
    const el = node22.nodeType === node22.ELEMENT_NODE ? node22 : index2.parentElement(node22);
    return el;
  }
  function isBlocked(node22, blockClass, blockSelector, checkAncestors) {
    if (!node22) {
      return false;
    }
    const el = closestElementOfNode(node22);
    if (!el) {
      return false;
    }
    try {
      if (typeof blockClass === "string") {
        if (el.classList.contains(blockClass)) return true;
        if (checkAncestors && el.closest("." + blockClass) !== null) return true;
      } else {
        if (classMatchesRegex(el, blockClass, checkAncestors)) return true;
      }
    } catch (e2) {
    }
    if (blockSelector) {
      if (el.matches(blockSelector)) return true;
      if (checkAncestors && el.closest(blockSelector) !== null) return true;
    }
    return false;
  }
  function isSerialized(n22, mirror2) {
    return mirror2.getId(n22) !== -1;
  }
  function isIgnored(n22, mirror2, slimDOMOptions) {
    if (n22.tagName === "TITLE" && slimDOMOptions.headTitleMutations) {
      return true;
    }
    return mirror2.getId(n22) === IGNORED_NODE;
  }
  function isAncestorRemoved(target, mirror2) {
    if (isShadowRoot2(target)) {
      return false;
    }
    const id2 = mirror2.getId(target);
    if (!mirror2.has(id2)) {
      return true;
    }
    const parent = index2.parentNode(target);
    if (parent && parent.nodeType === target.DOCUMENT_NODE) {
      return false;
    }
    if (!parent) {
      return true;
    }
    return isAncestorRemoved(parent, mirror2);
  }
  function legacy_isTouchEvent(event) {
    return Boolean(event.changedTouches);
  }
  function polyfill$1(win = window) {
    if ("NodeList" in win && !win.NodeList.prototype.forEach) {
      win.NodeList.prototype.forEach = Array.prototype.forEach;
    }
    if ("DOMTokenList" in win && !win.DOMTokenList.prototype.forEach) {
      win.DOMTokenList.prototype.forEach = Array.prototype.forEach;
    }
  }
  function queueToResolveTrees(queue) {
    const queueNodeMap = {};
    const putIntoMap = (m2, parent) => {
      const nodeInTree = {
        value: m2,
        parent,
        children: []
      };
      queueNodeMap[m2.node.id] = nodeInTree;
      return nodeInTree;
    };
    const queueNodeTrees = [];
    for (const mutation of queue) {
      const { nextId, parentId } = mutation;
      if (nextId && nextId in queueNodeMap) {
        const nextInTree = queueNodeMap[nextId];
        if (nextInTree.parent) {
          const idx = nextInTree.parent.children.indexOf(nextInTree);
          nextInTree.parent.children.splice(
            idx,
            0,
            putIntoMap(mutation, nextInTree.parent)
          );
        } else {
          const idx = queueNodeTrees.indexOf(nextInTree);
          queueNodeTrees.splice(idx, 0, putIntoMap(mutation, null));
        }
        continue;
      }
      if (parentId in queueNodeMap) {
        const parentInTree = queueNodeMap[parentId];
        parentInTree.children.push(putIntoMap(mutation, parentInTree));
        continue;
      }
      queueNodeTrees.push(putIntoMap(mutation, null));
    }
    return queueNodeTrees;
  }
  function iterateResolveTree(tree, cb) {
    cb(tree.value);
    for (let i2 = tree.children.length - 1; i2 >= 0; i2--) {
      iterateResolveTree(tree.children[i2], cb);
    }
  }
  function isSerializedIframe(n22, mirror2) {
    return Boolean(n22.nodeName === "IFRAME" && mirror2.getMeta(n22));
  }
  function isSerializedStylesheet(n22, mirror2) {
    return Boolean(
      n22.nodeName === "LINK" && n22.nodeType === n22.ELEMENT_NODE && n22.getAttribute && n22.getAttribute("rel") === "stylesheet" && mirror2.getMeta(n22)
    );
  }
  function getBaseDimension(node22, rootIframe) {
    var _a22, _b;
    const frameElement = (_b = (_a22 = node22.ownerDocument) == null ? void 0 : _a22.defaultView) == null ? void 0 : _b.frameElement;
    if (!frameElement || frameElement === rootIframe) {
      return {
        x: 0,
        y: 0,
        relativeScale: 1,
        absoluteScale: 1
      };
    }
    const frameDimension = frameElement.getBoundingClientRect();
    const frameBaseDimension = getBaseDimension(frameElement, rootIframe);
    const relativeScale = frameDimension.height / frameElement.clientHeight;
    return {
      x: frameDimension.x * frameBaseDimension.relativeScale + frameBaseDimension.x,
      y: frameDimension.y * frameBaseDimension.relativeScale + frameBaseDimension.y,
      relativeScale,
      absoluteScale: frameBaseDimension.absoluteScale * relativeScale
    };
  }
  function hasShadowRoot(n22) {
    if (!n22) return false;
    if (n22 instanceof BaseRRNode && "shadowRoot" in n22) {
      return Boolean(n22.shadowRoot);
    }
    return Boolean(index2.shadowRoot(n22));
  }
  function getNestedRule(rules2, position) {
    const rule22 = rules2[position[0]];
    if (position.length === 1) {
      return rule22;
    } else {
      return getNestedRule(
        rule22.cssRules[position[1]].cssRules,
        position.slice(2)
      );
    }
  }
  function getPositionsAndIndex(nestedIndex) {
    const positions = [...nestedIndex];
    const index22 = positions.pop();
    return { positions, index: index22 };
  }
  function uniqueTextMutations(mutations) {
    const idSet = /* @__PURE__ */ new Set();
    const uniqueMutations = [];
    for (let i2 = mutations.length; i2--; ) {
      const mutation = mutations[i2];
      if (!idSet.has(mutation.id)) {
        uniqueMutations.push(mutation);
        idSet.add(mutation.id);
      }
    }
    return uniqueMutations;
  }
  function getShadowHost(n22) {
    var _a22;
    let shadowHost = null;
    if ("getRootNode" in n22 && ((_a22 = index2.getRootNode(n22)) == null ? void 0 : _a22.nodeType) === Node.DOCUMENT_FRAGMENT_NODE && index2.host(index2.getRootNode(n22)))
      shadowHost = index2.host(index2.getRootNode(n22));
    return shadowHost;
  }
  function getRootShadowHost(n22) {
    let rootShadowHost = n22;
    let shadowHost;
    while (shadowHost = getShadowHost(rootShadowHost))
      rootShadowHost = shadowHost;
    return rootShadowHost;
  }
  function shadowHostInDom(n22) {
    const doc = n22.ownerDocument;
    if (!doc) return false;
    const shadowHost = getRootShadowHost(n22);
    return index2.contains(doc, shadowHost);
  }
  function inDom(n22) {
    const doc = n22.ownerDocument;
    if (!doc) return false;
    return index2.contains(doc, n22) || shadowHostInDom(n22);
  }
  function pathToSelector(node22) {
    if (!node22 || !node22.outerHTML) {
      return "";
    }
    let path = "";
    while (node22.parentElement) {
      let name = node22.localName;
      if (!name) {
        break;
      }
      name = name.toLowerCase();
      const parent = node22.parentElement;
      const domSiblings = [];
      if (parent.children && parent.children.length > 0) {
        for (let i2 = 0; i2 < parent.children.length; i2++) {
          const sibling = parent.children[i2];
          if (sibling.localName && sibling.localName.toLowerCase) {
            if (sibling.localName.toLowerCase() === name) {
              domSiblings.push(sibling);
            }
          }
        }
      }
      if (domSiblings.length > 1) {
        name += `:eq(${domSiblings.indexOf(node22)})`;
      }
      path = name + (path ? ">" + path : "");
      node22 = parent;
    }
    return path;
  }
  function isObject(obj) {
    return Object.prototype.toString.call(obj) === "[object Object]";
  }
  function isObjTooDeep(obj, limit) {
    if (limit === 0) {
      return true;
    }
    const keys = Object.keys(obj);
    for (const key of keys) {
      if (isObject(obj[key]) && isObjTooDeep(obj[key], limit - 1)) {
        return true;
      }
    }
    return false;
  }
  function stringify(obj, stringifyOptions) {
    const options = {
      numOfKeysLimit: 50,
      depthOfLimit: 4
    };
    Object.assign(options, stringifyOptions);
    const stack = [];
    const keys = [];
    return JSON.stringify(
      obj,
      function(key, value) {
        if (stack.length > 0) {
          const thisPos = stack.indexOf(this);
          ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
          ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
          if (~stack.indexOf(value)) {
            if (stack[0] === value) {
              value = "[Circular ~]";
            } else {
              value = "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
            }
          }
        } else {
          stack.push(value);
        }
        if (value === null) return value;
        if (value === void 0) return "undefined";
        if (shouldIgnore(value)) {
          return toString(value);
        }
        if (typeof value === "bigint") {
          return value.toString() + "n";
        }
        if (value instanceof Event) {
          const eventResult = {};
          for (const eventKey in value) {
            const eventValue = value[eventKey];
            if (Array.isArray(eventValue)) {
              eventResult[eventKey] = pathToSelector(
                eventValue.length ? eventValue[0] : null
              );
            } else {
              eventResult[eventKey] = eventValue;
            }
          }
          return eventResult;
        } else if (value instanceof Node) {
          if (value instanceof HTMLElement) {
            return value ? value.outerHTML : "";
          }
          return value.nodeName;
        } else if (value instanceof Error) {
          return value.stack ? value.stack + "\nEnd of stack for Error object" : value.name + ": " + value.message;
        }
        return value;
      }
    );
    function shouldIgnore(_obj) {
      if (isObject(_obj) && Object.keys(_obj).length > options.numOfKeysLimit) {
        return true;
      }
      if (typeof _obj === "function") {
        return true;
      }
      if (isObject(_obj) && isObjTooDeep(_obj, options.depthOfLimit)) {
        return true;
      }
      return false;
    }
    function toString(_obj) {
      let str = _obj.toString();
      if (options.stringLengthLimit && str.length > options.stringLengthLimit) {
        str = `${str.slice(0, options.stringLengthLimit)}...`;
      }
      return str;
    }
  }
  function initLogObserver(cb, win, options) {
    const logOptions = options ? Object.assign({}, defaultLogOptions, options) : defaultLogOptions;
    const loggerType = logOptions.logger;
    if (!loggerType) {
      return () => {
      };
    }
    let logger;
    if (typeof loggerType === "string") {
      logger = win[loggerType];
    } else {
      logger = loggerType;
    }
    let logCount = 0;
    let inStack = false;
    const cancelHandlers = [];
    if (logOptions.level.includes("error")) {
      const errorHandler2 = (event) => {
        const message = event.message, error = event.error;
        const trace = ErrorStackParser.parse(error).map(
          (stackFrame) => stackFrame.toString()
        );
        const payload = [stringify(message, logOptions.stringifyOptions)];
        cb({
          level: "error",
          trace,
          payload
        });
      };
      win.addEventListener("error", errorHandler2);
      cancelHandlers.push(() => {
        win.removeEventListener("error", errorHandler2);
      });
      const unhandledrejectionHandler = (event) => {
        let error;
        let payload;
        if (event.reason instanceof Error) {
          error = event.reason;
          payload = [
            stringify(
              `Uncaught (in promise) ${error.name}: ${error.message}`,
              logOptions.stringifyOptions
            )
          ];
        } else {
          error = new Error();
          payload = [
            stringify("Uncaught (in promise)", logOptions.stringifyOptions),
            stringify(event.reason, logOptions.stringifyOptions)
          ];
        }
        const trace = ErrorStackParser.parse(error).map(
          (stackFrame) => stackFrame.toString()
        );
        cb({
          level: "error",
          trace,
          payload
        });
      };
      win.addEventListener("unhandledrejection", unhandledrejectionHandler);
      cancelHandlers.push(() => {
        win.removeEventListener("unhandledrejection", unhandledrejectionHandler);
      });
    }
    for (const levelType of logOptions.level) {
      cancelHandlers.push(replace(logger, levelType));
    }
    return () => {
      cancelHandlers.forEach((h2) => h2());
    };
    function replace(_logger, level) {
      if (!_logger[level]) {
        return () => {
        };
      }
      return utils.patch(
        _logger,
        level,
        (original) => {
          return (...args) => {
            original.apply(this, args);
            if (level === "assert" && !!args[0]) {
              return;
            }
            if (inStack) {
              return;
            }
            inStack = true;
            try {
              const trace = ErrorStackParser.parse(new Error()).map((stackFrame) => stackFrame.toString()).splice(1);
              const argsForPayload = level === "assert" ? args.slice(1) : args;
              const payload = argsForPayload.map(
                (s2) => stringify(s2, logOptions.stringifyOptions)
              );
              logCount++;
              if (logCount < logOptions.lengthThreshold) {
                cb({
                  level,
                  trace,
                  payload
                });
              } else if (logCount === logOptions.lengthThreshold) {
                cb({
                  level: "warn",
                  trace: [],
                  payload: [
                    stringify("The number of log records reached the threshold.")
                  ]
                });
              }
            } catch (error) {
              original("rrweb logger error:", error, ...args);
            } finally {
              inStack = false;
            }
          };
        }
      );
    }
  }
  var __defProp2, __defNormalProp2, __publicField2, __defProp22, __defNormalProp22, __publicField22, _a2, __defProp$1, __defNormalProp$1, __publicField$1, testableAccessors$1, testableMethods$1, untaintedBasePrototype$1, untaintedAccessorCache$1, untaintedMethodCache$1, index$1, Mirror, IGNORED_NODE, picocolors_browser$1, hasRequiredPicocolors_browser$1, __viteBrowserExternal$2, __viteBrowserExternal$1$1, require$$2$1, cssSyntaxError$1, hasRequiredCssSyntaxError$1, symbols$1, hasRequiredSymbols$1, stringifier$1, hasRequiredStringifier$1, stringify_1$1, hasRequiredStringify$1, node$1, hasRequiredNode$1, declaration$1, hasRequiredDeclaration$1, nonSecure$1, hasRequiredNonSecure$1, previousMap$1, hasRequiredPreviousMap$1, input$1, hasRequiredInput$1, mapGenerator$1, hasRequiredMapGenerator$1, comment$1, hasRequiredComment$1, container$1, hasRequiredContainer$1, document$1$1, hasRequiredDocument$1, warnOnce$1, hasRequiredWarnOnce$1, warning$1, hasRequiredWarning$1, result$1, hasRequiredResult$1, tokenize$1, hasRequiredTokenize$1, atRule$1, hasRequiredAtRule$1, root$1, hasRequiredRoot$1, list_1$1, hasRequiredList$1, rule$1, hasRequiredRule$1, parser$1, hasRequiredParser$1, parse_1$1, hasRequiredParse$1, lazyResult$1, hasRequiredLazyResult$1, noWorkResult$1, hasRequiredNoWorkResult$1, processor$1, hasRequiredProcessor$1, fromJSON_1$1, hasRequiredFromJSON$1, postcss_1$1, hasRequiredPostcss$1, postcssExports$1, postcss$1, __defProp222, __defNormalProp222, __publicField222, picocolors_browser, hasRequiredPicocolors_browser, __viteBrowserExternal, __viteBrowserExternal$1, require$$2, cssSyntaxError, hasRequiredCssSyntaxError, symbols, hasRequiredSymbols, stringifier, hasRequiredStringifier, stringify_1, hasRequiredStringify, node, hasRequiredNode, declaration, hasRequiredDeclaration, nonSecure, hasRequiredNonSecure, previousMap, hasRequiredPreviousMap, input, hasRequiredInput, mapGenerator, hasRequiredMapGenerator, comment, hasRequiredComment, container, hasRequiredContainer, document$1, hasRequiredDocument, warnOnce, hasRequiredWarnOnce, warning, hasRequiredWarning, result, hasRequiredResult, tokenize, hasRequiredTokenize, atRule, hasRequiredAtRule, root, hasRequiredRoot, list_1, hasRequiredList, rule, hasRequiredRule, parser, hasRequiredParser, parse_1, hasRequiredParse, lazyResult, hasRequiredLazyResult, noWorkResult, hasRequiredNoWorkResult, processor, hasRequiredProcessor, fromJSON_1, hasRequiredFromJSON, postcss_1, hasRequiredPostcss, postcssExports, postcss, BaseRRNode, testableAccessors, testableMethods, untaintedBasePrototype, untaintedAccessorCache, untaintedMethodCache, index2, DEPARTED_MIRROR_ACCESS_WARNING, _mirror, nowTimestamp, StyleSheetMirror, utils, chars, lookup, i$1, jsContent, n2, StackFrame, FIREFOX_SAFARI_STACK_REGEXP, CHROME_IE_STACK_REGEXP, SAFARI_NATIVE_CODE_REGEXP, ErrorStackParser, defaultLogOptions, PLUGIN_NAME4, getRecordConsolePlugin;
  var init_rrweb_plugin_console_record = __esm({
    "node_modules/@amplitude/rrweb-plugin-console-record/dist/rrweb-plugin-console-record.js"() {
      __defProp2 = Object.defineProperty;
      __defNormalProp2 = (obj, key, value) => key in obj ? __defProp2(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField2 = (obj, key, value) => __defNormalProp2(obj, typeof key !== "symbol" ? key + "" : key, value);
      __defProp22 = Object.defineProperty;
      __defNormalProp22 = (obj, key, value) => key in obj ? __defProp22(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField22 = (obj, key, value) => __defNormalProp22(obj, typeof key !== "symbol" ? key + "" : key, value);
      __defProp$1 = Object.defineProperty;
      __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField$1 = (obj, key, value) => __defNormalProp$1(obj, typeof key !== "symbol" ? key + "" : key, value);
      testableAccessors$1 = {
        Node: ["childNodes", "parentNode", "parentElement", "textContent"],
        ShadowRoot: ["host", "styleSheets"],
        Element: ["shadowRoot", "querySelector", "querySelectorAll"],
        MutationObserver: []
      };
      testableMethods$1 = {
        Node: ["contains", "getRootNode"],
        ShadowRoot: ["getSelection"],
        Element: [],
        MutationObserver: ["constructor"]
      };
      untaintedBasePrototype$1 = {};
      untaintedAccessorCache$1 = {};
      untaintedMethodCache$1 = {};
      index$1 = {
        childNodes: childNodes$1,
        parentNode: parentNode$1,
        parentElement: parentElement$1,
        textContent: textContent$1,
        contains: contains$1,
        getRootNode: getRootNode$1,
        host: host$1,
        styleSheets: styleSheets$1,
        shadowRoot: shadowRoot$1,
        querySelector: querySelector$1,
        querySelectorAll: querySelectorAll$1,
        mutationObserver: mutationObserverCtor$1
      };
      Mirror = class {
        constructor() {
          __publicField$1(this, "idNodeMap", /* @__PURE__ */ new Map());
          __publicField$1(this, "nodeMetaMap", /* @__PURE__ */ new WeakMap());
        }
        getId(n22) {
          var _a22;
          if (!n22) return -1;
          const id2 = (_a22 = this.getMeta(n22)) == null ? void 0 : _a22.id;
          return id2 != null ? id2 : -1;
        }
        getNode(id2) {
          return this.idNodeMap.get(id2) || null;
        }
        getIds() {
          return Array.from(this.idNodeMap.keys());
        }
        getMeta(n22) {
          return this.nodeMetaMap.get(n22) || null;
        }
        // removes the node from idNodeMap
        // doesn't remove the node from nodeMetaMap
        removeNodeFromMap(n22) {
          const id2 = this.getId(n22);
          this.idNodeMap.delete(id2);
          if (n22.childNodes) {
            n22.childNodes.forEach(
              (childNode) => this.removeNodeFromMap(childNode)
            );
          }
        }
        has(id2) {
          return this.idNodeMap.has(id2);
        }
        hasNode(node22) {
          return this.nodeMetaMap.has(node22);
        }
        add(n22, meta) {
          const id2 = meta.id;
          this.idNodeMap.set(id2, n22);
          this.nodeMetaMap.set(n22, meta);
        }
        replace(id2, n22) {
          const oldNode = this.getNode(id2);
          if (oldNode) {
            const meta = this.nodeMetaMap.get(oldNode);
            if (meta) this.nodeMetaMap.set(n22, meta);
          }
          this.idNodeMap.set(id2, n22);
        }
        reset() {
          this.idNodeMap = /* @__PURE__ */ new Map();
          this.nodeMetaMap = /* @__PURE__ */ new WeakMap();
        }
      };
      IGNORED_NODE = -2;
      picocolors_browser$1 = { exports: {} };
      __viteBrowserExternal$2 = {};
      __viteBrowserExternal$1$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
        __proto__: null,
        default: __viteBrowserExternal$2
      }, Symbol.toStringTag, { value: "Module" }));
      require$$2$1 = /* @__PURE__ */ getAugmentedNamespace$1(__viteBrowserExternal$1$1);
      symbols$1 = {};
      postcssExports$1 = requirePostcss$1();
      postcss$1 = /* @__PURE__ */ getDefaultExportFromCjs$1(postcssExports$1);
      postcss$1.stringify;
      postcss$1.fromJSON;
      postcss$1.plugin;
      postcss$1.parse;
      postcss$1.list;
      postcss$1.document;
      postcss$1.comment;
      postcss$1.atRule;
      postcss$1.rule;
      postcss$1.decl;
      postcss$1.root;
      postcss$1.CssSyntaxError;
      postcss$1.Declaration;
      postcss$1.Container;
      postcss$1.Processor;
      postcss$1.Document;
      postcss$1.Comment;
      postcss$1.Warning;
      postcss$1.AtRule;
      postcss$1.Result;
      postcss$1.Input;
      postcss$1.Rule;
      postcss$1.Root;
      postcss$1.Node;
      __defProp222 = Object.defineProperty;
      __defNormalProp222 = (obj, key, value) => key in obj ? __defProp222(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField222 = (obj, key, value) => __defNormalProp222(obj, typeof key !== "symbol" ? key + "" : key, value);
      picocolors_browser = { exports: {} };
      __viteBrowserExternal = {};
      __viteBrowserExternal$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
        __proto__: null,
        default: __viteBrowserExternal
      }, Symbol.toStringTag, { value: "Module" }));
      require$$2 = /* @__PURE__ */ getAugmentedNamespace(__viteBrowserExternal$1);
      symbols = {};
      postcssExports = requirePostcss();
      postcss = /* @__PURE__ */ getDefaultExportFromCjs(postcssExports);
      postcss.stringify;
      postcss.fromJSON;
      postcss.plugin;
      postcss.parse;
      postcss.list;
      postcss.document;
      postcss.comment;
      postcss.atRule;
      postcss.rule;
      postcss.decl;
      postcss.root;
      postcss.CssSyntaxError;
      postcss.Declaration;
      postcss.Container;
      postcss.Processor;
      postcss.Document;
      postcss.Comment;
      postcss.Warning;
      postcss.AtRule;
      postcss.Result;
      postcss.Input;
      postcss.Rule;
      postcss.Root;
      postcss.Node;
      BaseRRNode = class _BaseRRNode {
        // eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
        constructor(..._args) {
          __publicField222(this, "parentElement", null);
          __publicField222(this, "parentNode", null);
          __publicField222(this, "ownerDocument");
          __publicField222(this, "firstChild", null);
          __publicField222(this, "lastChild", null);
          __publicField222(this, "previousSibling", null);
          __publicField222(this, "nextSibling", null);
          __publicField222(this, "ELEMENT_NODE", 1);
          __publicField222(this, "TEXT_NODE", 3);
          __publicField222(this, "nodeType");
          __publicField222(this, "nodeName");
          __publicField222(this, "RRNodeType");
        }
        get childNodes() {
          const childNodes22 = [];
          let childIterator = this.firstChild;
          while (childIterator) {
            childNodes22.push(childIterator);
            childIterator = childIterator.nextSibling;
          }
          return childNodes22;
        }
        contains(node22) {
          if (!(node22 instanceof _BaseRRNode)) return false;
          else if (node22.ownerDocument !== this.ownerDocument) return false;
          else if (node22 === this) return true;
          while (node22.parentNode) {
            if (node22.parentNode === this) return true;
            node22 = node22.parentNode;
          }
          return false;
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        appendChild(_newChild) {
          throw new Error(
            `RRDomException: Failed to execute 'appendChild' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        insertBefore(_newChild, _refChild) {
          throw new Error(
            `RRDomException: Failed to execute 'insertBefore' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        removeChild(_node) {
          throw new Error(
            `RRDomException: Failed to execute 'removeChild' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        toString() {
          return "RRNode";
        }
      };
      testableAccessors = {
        Node: ["childNodes", "parentNode", "parentElement", "textContent"],
        ShadowRoot: ["host", "styleSheets"],
        Element: ["shadowRoot", "querySelector", "querySelectorAll"],
        MutationObserver: []
      };
      testableMethods = {
        Node: ["contains", "getRootNode"],
        ShadowRoot: ["getSelection"],
        Element: [],
        MutationObserver: ["constructor"]
      };
      untaintedBasePrototype = {};
      untaintedAccessorCache = {};
      untaintedMethodCache = {};
      index2 = {
        childNodes,
        parentNode,
        parentElement,
        textContent,
        contains,
        getRootNode,
        host,
        styleSheets,
        shadowRoot,
        querySelector,
        querySelectorAll,
        mutationObserver: mutationObserverCtor
      };
      DEPARTED_MIRROR_ACCESS_WARNING = "Please stop import mirror directly. Instead of that,\r\nnow you can use replayer.getMirror() to access the mirror instance of a replayer,\r\nor you can use record.mirror to access the mirror instance during recording.";
      _mirror = {
        map: {},
        getId() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
          return -1;
        },
        getNode() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
          return null;
        },
        removeNodeFromMap() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
        },
        has() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
          return false;
        },
        reset() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
        }
      };
      if (typeof window !== "undefined" && window.Proxy && window.Reflect) {
        _mirror = new Proxy(_mirror, {
          get(target, prop, receiver) {
            if (prop === "map") {
              console.error(DEPARTED_MIRROR_ACCESS_WARNING);
            }
            return Reflect.get(target, prop, receiver);
          }
        });
      }
      nowTimestamp = Date.now;
      if (!/* @__PURE__ */ /[1-9][0-9]{12}/.test(Date.now().toString())) {
        nowTimestamp = () => (/* @__PURE__ */ new Date()).getTime();
      }
      StyleSheetMirror = class {
        constructor() {
          __publicField22(this, "id", 1);
          __publicField22(this, "styleIDMap", /* @__PURE__ */ new WeakMap());
          __publicField22(this, "idStyleMap", /* @__PURE__ */ new Map());
        }
        getId(stylesheet) {
          var _a4;
          return (_a4 = this.styleIDMap.get(stylesheet)) != null ? _a4 : -1;
        }
        has(stylesheet) {
          return this.styleIDMap.has(stylesheet);
        }
        /**
         * @returns If the stylesheet is in the mirror, returns the id of the stylesheet. If not, return the new assigned id.
         */
        add(stylesheet, id2) {
          if (this.has(stylesheet)) return this.getId(stylesheet);
          let newId;
          if (id2 === void 0) {
            newId = this.id++;
          } else newId = id2;
          this.styleIDMap.set(stylesheet, newId);
          this.idStyleMap.set(newId, stylesheet);
          return newId;
        }
        getStyle(id2) {
          return this.idStyleMap.get(id2) || null;
        }
        reset() {
          this.styleIDMap = /* @__PURE__ */ new WeakMap();
          this.idStyleMap = /* @__PURE__ */ new Map();
          this.id = 1;
        }
        generateId() {
          return this.id++;
        }
      };
      utils = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
        __proto__: null,
        StyleSheetMirror,
        get _mirror() {
          return _mirror;
        },
        closestElementOfNode,
        getBaseDimension,
        getNestedRule,
        getPositionsAndIndex,
        getRootShadowHost,
        getShadowHost,
        getWindowHeight,
        getWindowScroll,
        getWindowWidth,
        hasShadowRoot,
        hookSetter,
        inDom,
        isAncestorRemoved,
        isBlocked,
        isIgnored,
        isSerialized,
        isSerializedIframe,
        isSerializedStylesheet,
        iterateResolveTree,
        legacy_isTouchEvent,
        get nowTimestamp() {
          return nowTimestamp;
        },
        on,
        patch,
        polyfill: polyfill$1,
        queueToResolveTrees,
        shadowHostInDom,
        throttle,
        uniqueTextMutations
      }, Symbol.toStringTag, { value: "Module" }));
      chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
      lookup = typeof Uint8Array === "undefined" ? [] : new Uint8Array(256);
      for (i$1 = 0; i$1 < chars.length; i$1++) {
        lookup[chars.charCodeAt(i$1)] = i$1;
      }
      jsContent = '(function() {\n  "use strict";\n  var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";\n  var lookup = typeof Uint8Array === "undefined" ? [] : new Uint8Array(256);\n  for (var i = 0; i < chars.length; i++) {\n    lookup[chars.charCodeAt(i)] = i;\n  }\n  var encode = function(arraybuffer) {\n    var bytes = new Uint8Array(arraybuffer), i2, len = bytes.length, base64 = "";\n    for (i2 = 0; i2 < len; i2 += 3) {\n      base64 += chars[bytes[i2] >> 2];\n      base64 += chars[(bytes[i2] & 3) << 4 | bytes[i2 + 1] >> 4];\n      base64 += chars[(bytes[i2 + 1] & 15) << 2 | bytes[i2 + 2] >> 6];\n      base64 += chars[bytes[i2 + 2] & 63];\n    }\n    if (len % 3 === 2) {\n      base64 = base64.substring(0, base64.length - 1) + "=";\n    } else if (len % 3 === 1) {\n      base64 = base64.substring(0, base64.length - 2) + "==";\n    }\n    return base64;\n  };\n  const lastBlobMap = /* @__PURE__ */ new Map();\n  const transparentBlobMap = /* @__PURE__ */ new Map();\n  async function getTransparentBlobFor(width, height, dataURLOptions) {\n    const id = `${width}-${height}`;\n    if ("OffscreenCanvas" in globalThis) {\n      if (transparentBlobMap.has(id)) return transparentBlobMap.get(id);\n      const offscreen = new OffscreenCanvas(width, height);\n      offscreen.getContext("2d");\n      const blob = await offscreen.convertToBlob(dataURLOptions);\n      const arrayBuffer = await blob.arrayBuffer();\n      const base64 = encode(arrayBuffer);\n      transparentBlobMap.set(id, base64);\n      return base64;\n    } else {\n      return "";\n    }\n  }\n  const worker = self;\n  worker.onmessage = async function(e) {\n    if ("OffscreenCanvas" in globalThis) {\n      const { id, bitmap, width, height, dataURLOptions } = e.data;\n      const transparentBase64 = getTransparentBlobFor(\n        width,\n        height,\n        dataURLOptions\n      );\n      const offscreen = new OffscreenCanvas(width, height);\n      const ctx = offscreen.getContext("2d");\n      ctx.drawImage(bitmap, 0, 0);\n      bitmap.close();\n      const blob = await offscreen.convertToBlob(dataURLOptions);\n      const type = blob.type;\n      const arrayBuffer = await blob.arrayBuffer();\n      const base64 = encode(arrayBuffer);\n      if (!lastBlobMap.has(id) && await transparentBase64 === base64) {\n        lastBlobMap.set(id, base64);\n        return worker.postMessage({ id });\n      }\n      if (lastBlobMap.get(id) === base64) return worker.postMessage({ id });\n      worker.postMessage({\n        id,\n        type,\n        base64,\n        width,\n        height\n      });\n      lastBlobMap.set(id, base64);\n    } else {\n      return worker.postMessage({ id: e.data.id });\n    }\n  };\n})();\n//# sourceMappingURL=image-bitmap-data-url-worker-IJpC7g_b.js.map\n';
      typeof self !== "undefined" && self.Blob && new Blob([jsContent], { type: "text/javascript;charset=utf-8" });
      try {
        if (Array.from([1], (x2) => x2 * 2)[0] !== 2) {
          const cleanFrame = document.createElement("iframe");
          document.body.appendChild(cleanFrame);
          Array.from = ((_a2 = cleanFrame.contentWindow) == null ? void 0 : _a2.Array.from) || Array.from;
          document.body.removeChild(cleanFrame);
        }
      } catch (err) {
        console.debug("Unable to override Array.from", err);
      }
      createMirror$2();
      !(function(t2) {
        t2[t2.NotStarted = 0] = "NotStarted", t2[t2.Running = 1] = "Running", t2[t2.Stopped = 2] = "Stopped";
      })(n2 || (n2 = {}));
      StackFrame = class {
        constructor(obj) {
          __publicField2(this, "fileName");
          __publicField2(this, "functionName");
          __publicField2(this, "lineNumber");
          __publicField2(this, "columnNumber");
          this.fileName = obj.fileName || "";
          this.functionName = obj.functionName || "";
          this.lineNumber = obj.lineNumber;
          this.columnNumber = obj.columnNumber;
        }
        toString() {
          const lineNumber = this.lineNumber || "";
          const columnNumber = this.columnNumber || "";
          if (this.functionName)
            return `${this.functionName} (${this.fileName}:${lineNumber}:${columnNumber})`;
          return `${this.fileName}:${lineNumber}:${columnNumber}`;
        }
      };
      FIREFOX_SAFARI_STACK_REGEXP = /(^|@)\S+:\d+/;
      CHROME_IE_STACK_REGEXP = /^\s*at .*(\S+:\d+|\(native\))/m;
      SAFARI_NATIVE_CODE_REGEXP = /^(eval@)?(\[native code])?$/;
      ErrorStackParser = {
        /**
         * Given an Error object, extract the most information from it.
         */
        parse: function(error) {
          if (!error) {
            return [];
          }
          if (
            // eslint-disable-next-line @typescript-eslint/ban-ts-comment
            // @ts-ignore
            typeof error.stacktrace !== "undefined" || // eslint-disable-next-line @typescript-eslint/ban-ts-comment
            // @ts-ignore
            typeof error["opera#sourceloc"] !== "undefined"
          ) {
            return this.parseOpera(
              error
            );
          } else if (error.stack && error.stack.match(CHROME_IE_STACK_REGEXP)) {
            return this.parseV8OrIE(error);
          } else if (error.stack) {
            return this.parseFFOrSafari(error);
          } else {
            console.warn(
              "[console-record-plugin]: Failed to parse error object:",
              error
            );
            return [];
          }
        },
        // Separate line and column numbers from a string of the form: (URI:Line:Column)
        extractLocation: function(urlLike) {
          if (urlLike.indexOf(":") === -1) {
            return [urlLike];
          }
          const regExp = /(.+?)(?::(\d+))?(?::(\d+))?$/;
          const parts = regExp.exec(urlLike.replace(/[()]/g, ""));
          if (!parts) throw new Error(`Cannot parse given url: ${urlLike}`);
          return [parts[1], parts[2] || void 0, parts[3] || void 0];
        },
        parseV8OrIE: function(error) {
          const filtered = error.stack.split("\n").filter(function(line2) {
            return !!line2.match(CHROME_IE_STACK_REGEXP);
          }, this);
          return filtered.map(function(line2) {
            if (line2.indexOf("(eval ") > -1) {
              line2 = line2.replace(/eval code/g, "eval").replace(/(\(eval at [^()]*)|(\),.*$)/g, "");
            }
            let sanitizedLine = line2.replace(/^\s+/, "").replace(/\(eval code/g, "(");
            const location2 = sanitizedLine.match(/ (\((.+):(\d+):(\d+)\)$)/);
            sanitizedLine = location2 ? sanitizedLine.replace(location2[0], "") : sanitizedLine;
            const tokens = sanitizedLine.split(/\s+/).slice(1);
            const locationParts = this.extractLocation(
              location2 ? location2[1] : tokens.pop()
            );
            const functionName = tokens.join(" ") || void 0;
            const fileName = ["eval", "<anonymous>"].indexOf(locationParts[0]) > -1 ? void 0 : locationParts[0];
            return new StackFrame({
              functionName,
              fileName,
              lineNumber: locationParts[1],
              columnNumber: locationParts[2]
            });
          }, this);
        },
        parseFFOrSafari: function(error) {
          const filtered = error.stack.split("\n").filter(function(line2) {
            return !line2.match(SAFARI_NATIVE_CODE_REGEXP);
          }, this);
          return filtered.map(function(line2) {
            if (line2.indexOf(" > eval") > -1) {
              line2 = line2.replace(
                / line (\d+)(?: > eval line \d+)* > eval:\d+:\d+/g,
                ":$1"
              );
            }
            if (line2.indexOf("@") === -1 && line2.indexOf(":") === -1) {
              return new StackFrame({
                functionName: line2
              });
            } else {
              const functionNameRegex = /((.*".+"[^@]*)?[^@]*)(?:@)/;
              const matches = line2.match(functionNameRegex);
              const functionName = matches && matches[1] ? matches[1] : void 0;
              const locationParts = this.extractLocation(
                line2.replace(functionNameRegex, "")
              );
              return new StackFrame({
                functionName,
                fileName: locationParts[0],
                lineNumber: locationParts[1],
                columnNumber: locationParts[2]
              });
            }
          }, this);
        },
        parseOpera: function(e2) {
          if (!e2.stacktrace || e2.message.indexOf("\n") > -1 && e2.message.split("\n").length > e2.stacktrace.split("\n").length) {
            return this.parseOpera9(e2);
          } else if (!e2.stack) {
            return this.parseOpera10(e2);
          } else {
            return this.parseOpera11(e2);
          }
        },
        parseOpera9: function(e2) {
          const lineRE = /Line (\d+).*script (?:in )?(\S+)/i;
          const lines = e2.message.split("\n");
          const result22 = [];
          for (let i2 = 2, len = lines.length; i2 < len; i2 += 2) {
            const match = lineRE.exec(lines[i2]);
            if (match) {
              result22.push(
                new StackFrame({
                  fileName: match[2],
                  lineNumber: parseFloat(match[1])
                })
              );
            }
          }
          return result22;
        },
        parseOpera10: function(e2) {
          const lineRE = /Line (\d+).*script (?:in )?(\S+)(?:: In function (\S+))?$/i;
          const lines = e2.stacktrace.split("\n");
          const result22 = [];
          for (let i2 = 0, len = lines.length; i2 < len; i2 += 2) {
            const match = lineRE.exec(lines[i2]);
            if (match) {
              result22.push(
                new StackFrame({
                  functionName: match[3] || void 0,
                  fileName: match[2],
                  lineNumber: parseFloat(match[1])
                })
              );
            }
          }
          return result22;
        },
        // Opera 10.65+ Error.stack very similar to FF/Safari
        parseOpera11: function(error) {
          const filtered = error.stack.split("\n").filter(function(line2) {
            return !!line2.match(FIREFOX_SAFARI_STACK_REGEXP) && !line2.match(/^Error created at/);
          }, this);
          return filtered.map(function(line2) {
            const tokens = line2.split("@");
            const locationParts = this.extractLocation(tokens.pop());
            const functionCall = tokens.shift() || "";
            const functionName = functionCall.replace(/<anonymous function(: (\w+))?>/, "$2").replace(/\([^)]*\)/g, "") || void 0;
            return new StackFrame({
              functionName,
              fileName: locationParts[0],
              lineNumber: locationParts[1],
              columnNumber: locationParts[2]
            });
          }, this);
        }
      };
      defaultLogOptions = {
        level: [
          "assert",
          "clear",
          "count",
          "countReset",
          "debug",
          "dir",
          "dirxml",
          "error",
          "group",
          "groupCollapsed",
          "groupEnd",
          "info",
          "log",
          "table",
          "time",
          "timeEnd",
          "timeLog",
          "trace",
          "warn"
        ],
        lengthThreshold: 1e3,
        logger: "console"
      };
      PLUGIN_NAME4 = "rrweb/console@1";
      getRecordConsolePlugin = (options) => ({
        name: PLUGIN_NAME4,
        observer: initLogObserver,
        options
      });
    }
  });

  // node_modules/@amplitude/rrweb-record/dist/rrweb-record.js
  var rrweb_record_exports = {};
  __export(rrweb_record_exports, {
    record: () => record
  });
  function angularZoneUnpatchedAlternative$12(key) {
    var _a22, _b;
    const angularUnpatchedVersionSymbol = (_b = (_a22 = globalThis == null ? void 0 : globalThis.Zone) == null ? void 0 : _a22.__symbol__) == null ? void 0 : _b.call(_a22, key);
    if (angularUnpatchedVersionSymbol && globalThis[angularUnpatchedVersionSymbol]) {
      return globalThis[angularUnpatchedVersionSymbol];
    } else {
      return void 0;
    }
  }
  function getUntaintedPrototype$12(key) {
    if (untaintedBasePrototype$12[key])
      return untaintedBasePrototype$12[key];
    const candidate = angularZoneUnpatchedAlternative$12(key) || globalThis[key];
    const defaultPrototype = candidate.prototype;
    const accessorNames = key in testableAccessors$12 ? testableAccessors$12[key] : void 0;
    const isUntaintedAccessors = Boolean(
      accessorNames && // @ts-expect-error 2345
      accessorNames.every(
        (accessor) => {
          var _a22, _b;
          return Boolean(
            (_b = (_a22 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a22.get) == null ? void 0 : _b.toString().includes("[native code]")
          );
        }
      )
    );
    const methodNames = key in testableMethods$12 ? testableMethods$12[key] : void 0;
    const isUntaintedMethods = Boolean(
      methodNames && methodNames.every(
        // @ts-expect-error 2345
        (method) => {
          var _a22;
          return typeof defaultPrototype[method] === "function" && ((_a22 = defaultPrototype[method]) == null ? void 0 : _a22.toString().includes("[native code]"));
        }
      )
    );
    if (isUntaintedAccessors && isUntaintedMethods) {
      untaintedBasePrototype$12[key] = candidate.prototype;
      return candidate.prototype;
    }
    try {
      const iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      const win = iframeEl.contentWindow;
      if (!win) return candidate.prototype;
      const untaintedObject = win[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype$12[key] = untaintedObject;
    } catch {
      return defaultPrototype;
    }
  }
  function getUntaintedAccessor$12(key, instance, accessor) {
    var _a22;
    const cacheKey = `${key}.${String(accessor)}`;
    if (untaintedAccessorCache$12[cacheKey])
      return untaintedAccessorCache$12[cacheKey].call(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype$12(key);
    const untaintedAccessor = (_a22 = Object.getOwnPropertyDescriptor(
      untaintedPrototype,
      accessor
    )) == null ? void 0 : _a22.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache$12[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  function getUntaintedMethod$12(key, instance, method) {
    const cacheKey = `${key}.${String(method)}`;
    if (untaintedMethodCache$12[cacheKey])
      return untaintedMethodCache$12[cacheKey].bind(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype$12(key);
    const untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache$12[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes$12(n22) {
    return getUntaintedAccessor$12("Node", n22, "childNodes");
  }
  function parentNode$12(n22) {
    return getUntaintedAccessor$12("Node", n22, "parentNode");
  }
  function parentElement$12(n22) {
    return getUntaintedAccessor$12("Node", n22, "parentElement");
  }
  function textContent$12(n22) {
    return getUntaintedAccessor$12("Node", n22, "textContent");
  }
  function contains$12(n22, other) {
    return getUntaintedMethod$12("Node", n22, "contains")(other);
  }
  function getRootNode$12(n22) {
    return getUntaintedMethod$12("Node", n22, "getRootNode")();
  }
  function host$12(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor$12("ShadowRoot", n22, "host");
  }
  function styleSheets$12(n22) {
    return n22.styleSheets;
  }
  function shadowRoot$12(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor$12("Element", n22, "shadowRoot");
  }
  function querySelector$12(n22, selectors) {
    return getUntaintedAccessor$12("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll$12(n22, selectors) {
    return getUntaintedAccessor$12("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor$12() {
    return getUntaintedPrototype$12("MutationObserver").constructor;
  }
  function isElement(n22) {
    return n22.nodeType === n22.ELEMENT_NODE;
  }
  function isShadowRoot3(n22) {
    const hostEl = (
      // anchor and textarea elements also have a `host` property
      // but only shadow roots have a `mode` property
      n22 && "host" in n22 && "mode" in n22 && index$12.host(n22) || null
    );
    return Boolean(
      hostEl && "shadowRoot" in hostEl && index$12.shadowRoot(hostEl) === n22
    );
  }
  function isNativeShadowDom(shadowRoot22) {
    return Object.prototype.toString.call(shadowRoot22) === "[object ShadowRoot]";
  }
  function fixBrowserCompatibilityIssuesInCSS(cssText) {
    if (cssText.includes(" background-clip: text;") && !cssText.includes(" -webkit-background-clip: text;")) {
      cssText = cssText.replace(
        /\sbackground-clip:\s*text;/g,
        " -webkit-background-clip: text; background-clip: text;"
      );
    }
    return cssText;
  }
  function escapeImportStatement(rule22) {
    const { cssText } = rule22;
    if (cssText.split('"').length < 3) return cssText;
    const statement = ["@import", `url(${JSON.stringify(rule22.href)})`];
    if (rule22.layerName === "") {
      statement.push(`layer`);
    } else if (rule22.layerName) {
      statement.push(`layer(${rule22.layerName})`);
    }
    if (rule22.supportsText) {
      statement.push(`supports(${rule22.supportsText})`);
    }
    if (rule22.media.length) {
      statement.push(rule22.media.mediaText);
    }
    return statement.join(" ") + ";";
  }
  function stringifyStylesheet(s2) {
    try {
      const rules2 = s2.rules || s2.cssRules;
      if (!rules2) {
        return null;
      }
      let sheetHref = s2.href;
      if (!sheetHref && s2.ownerNode && s2.ownerNode.ownerDocument) {
        sheetHref = s2.ownerNode.ownerDocument.location.href;
      }
      const stringifiedRules = Array.from(
        rules2,
        (rule22) => stringifyRule(rule22, sheetHref)
      ).join("");
      return fixBrowserCompatibilityIssuesInCSS(stringifiedRules);
    } catch (error) {
      return null;
    }
  }
  function stringifyRule(rule22, sheetHref) {
    if (isCSSImportRule(rule22)) {
      let importStringified;
      try {
        importStringified = // for same-origin stylesheets,
        // we can access the imported stylesheet rules directly
        stringifyStylesheet(rule22.styleSheet) || // work around browser issues with the raw string `@import url(...)` statement
        escapeImportStatement(rule22);
      } catch (error) {
        importStringified = rule22.cssText;
      }
      if (rule22.styleSheet.href) {
        return absolutifyURLs(importStringified, rule22.styleSheet.href);
      }
      return importStringified;
    } else {
      let ruleStringified = rule22.cssText;
      if (isCSSStyleRule(rule22) && rule22.selectorText.includes(":")) {
        ruleStringified = fixSafariColons(ruleStringified);
      }
      if (sheetHref) {
        return absolutifyURLs(ruleStringified, sheetHref);
      }
      return ruleStringified;
    }
  }
  function fixSafariColons(cssStringified) {
    const regex = /(\[(?:[\w-]+)[^\\])(:(?:[\w-]+)\])/gm;
    return cssStringified.replace(regex, "$1\\$2");
  }
  function isCSSImportRule(rule22) {
    return "styleSheet" in rule22;
  }
  function isCSSStyleRule(rule22) {
    return "selectorText" in rule22;
  }
  function createMirror$22() {
    return new Mirror2();
  }
  function maskInputValue({
    element,
    maskInputOptions,
    tagName: tagName2,
    type,
    value,
    maskInputFn
  }) {
    let text = value || "";
    const actualType = type && toLowerCase2(type);
    if (maskInputOptions[tagName2.toLowerCase()] || actualType && maskInputOptions[actualType]) {
      if (maskInputFn) {
        text = maskInputFn(text, element);
      } else {
        text = "*".repeat(text.length);
      }
    }
    return text;
  }
  function toLowerCase2(str) {
    return str.toLowerCase();
  }
  function is2DCanvasBlank(canvas) {
    const ctx = canvas.getContext("2d");
    if (!ctx) return true;
    const chunkSize = 50;
    for (let x2 = 0; x2 < canvas.width; x2 += chunkSize) {
      for (let y2 = 0; y2 < canvas.height; y2 += chunkSize) {
        const getImageData = ctx.getImageData;
        const originalGetImageData = ORIGINAL_ATTRIBUTE_NAME in getImageData ? getImageData[ORIGINAL_ATTRIBUTE_NAME] : getImageData;
        const pixelBuffer = new Uint32Array(
          // eslint-disable-next-line @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
          originalGetImageData.call(
            ctx,
            x2,
            y2,
            Math.min(chunkSize, canvas.width - x2),
            Math.min(chunkSize, canvas.height - y2)
          ).data.buffer
        );
        if (pixelBuffer.some((pixel) => pixel !== 0)) return false;
      }
    }
    return true;
  }
  function getInputType2(element) {
    const type = element.type;
    return element.hasAttribute("data-rr-is-password") ? "password" : type ? (
      // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion
      toLowerCase2(type)
    ) : null;
  }
  function extractFileExtension(path, baseURL) {
    var _a4;
    let url;
    try {
      url = new URL(path, baseURL != null ? baseURL : window.location.href);
    } catch (err) {
      return null;
    }
    const regex = /\.([0-9a-z]+)(?:$)/i;
    const match = url.pathname.match(regex);
    return (_a4 = match == null ? void 0 : match[1]) != null ? _a4 : null;
  }
  function extractOrigin(url) {
    let origin = "";
    if (url.indexOf("//") > -1) {
      origin = url.split("/").slice(0, 3).join("/");
    } else {
      origin = url.split("/")[0];
    }
    origin = origin.split("?")[0];
    return origin;
  }
  function absolutifyURLs(cssText, href) {
    return (cssText || "").replace(
      URL_IN_CSS_REF,
      (origin, quote1, path1, quote2, path2, path3) => {
        const filePath = path1 || path2 || path3;
        const maybeQuote = quote1 || quote2 || "";
        if (!filePath) {
          return origin;
        }
        if (URL_PROTOCOL_MATCH.test(filePath) || URL_WWW_MATCH.test(filePath)) {
          return `url(${maybeQuote}${filePath}${maybeQuote})`;
        }
        if (DATA_URI.test(filePath)) {
          return `url(${maybeQuote}${filePath}${maybeQuote})`;
        }
        if (filePath[0] === "/") {
          return `url(${maybeQuote}${extractOrigin(href) + filePath}${maybeQuote})`;
        }
        const stack = href.split("/");
        const parts = filePath.split("/");
        stack.pop();
        for (const part of parts) {
          if (part === ".") {
            continue;
          } else if (part === "..") {
            stack.pop();
          } else {
            stack.push(part);
          }
        }
        return `url(${maybeQuote}${stack.join("/")}${maybeQuote})`;
      }
    );
  }
  function normalizeCssString(cssText, _testNoPxNorm = false) {
    if (_testNoPxNorm) {
      return cssText.replace(/(\/\*[^*]*\*\/)|[\s;]/g, "");
    } else {
      return cssText.replace(/(\/\*[^*]*\*\/)|[\s;]/g, "").replace(/0px/g, "0");
    }
  }
  function splitCssText(cssText, style, _testNoPxNorm = false) {
    const childNodes22 = Array.from(style.childNodes);
    const splits = [];
    let iterCount = 0;
    if (childNodes22.length > 1 && cssText && typeof cssText === "string") {
      let cssTextNorm = normalizeCssString(cssText, _testNoPxNorm);
      const normFactor = cssTextNorm.length / cssText.length;
      for (let i2 = 1; i2 < childNodes22.length; i2++) {
        if (childNodes22[i2].textContent && typeof childNodes22[i2].textContent === "string") {
          const textContentNorm = normalizeCssString(
            childNodes22[i2].textContent,
            _testNoPxNorm
          );
          const jLimit = 100;
          let j = 3;
          for (; j < textContentNorm.length; j++) {
            if (
              // keep consuming css identifiers (to get a decent chunk more quickly)
              textContentNorm[j].match(/[a-zA-Z0-9]/) || // substring needs to be unique to this section
              textContentNorm.indexOf(textContentNorm.substring(0, j), 1) !== -1
            ) {
              continue;
            }
            break;
          }
          for (; j < textContentNorm.length; j++) {
            let startSubstring = textContentNorm.substring(0, j);
            let cssNormSplits = cssTextNorm.split(startSubstring);
            let splitNorm = -1;
            if (cssNormSplits.length === 2) {
              splitNorm = cssNormSplits[0].length;
            } else if (cssNormSplits.length > 2 && cssNormSplits[0] === "" && childNodes22[i2 - 1].textContent !== "") {
              splitNorm = cssTextNorm.indexOf(startSubstring, 1);
            } else if (cssNormSplits.length === 1) {
              startSubstring = startSubstring.substring(
                0,
                startSubstring.length - 1
              );
              cssNormSplits = cssTextNorm.split(startSubstring);
              if (cssNormSplits.length <= 1) {
                splits.push(cssText);
                return splits;
              }
              j = jLimit + 1;
            } else if (j === textContentNorm.length - 1) {
              splitNorm = cssTextNorm.indexOf(startSubstring);
            }
            if (cssNormSplits.length >= 2 && j > jLimit) {
              const prevTextContent = childNodes22[i2 - 1].textContent;
              if (prevTextContent && typeof prevTextContent === "string") {
                const prevMinLength = normalizeCssString(prevTextContent).length;
                splitNorm = cssTextNorm.indexOf(startSubstring, prevMinLength);
              }
              if (splitNorm === -1) {
                splitNorm = cssNormSplits[0].length;
              }
            }
            if (splitNorm !== -1) {
              let k2 = Math.floor(splitNorm / normFactor);
              for (; k2 > 0 && k2 < cssText.length; ) {
                iterCount += 1;
                if (iterCount > 50 * childNodes22.length) {
                  splits.push(cssText);
                  return splits;
                }
                const normPart = normalizeCssString(
                  cssText.substring(0, k2),
                  _testNoPxNorm
                );
                if (normPart.length === splitNorm) {
                  splits.push(cssText.substring(0, k2));
                  cssText = cssText.substring(k2);
                  cssTextNorm = cssTextNorm.substring(splitNorm);
                  break;
                } else if (normPart.length < splitNorm) {
                  k2 += Math.max(
                    1,
                    Math.floor((splitNorm - normPart.length) / normFactor)
                  );
                } else {
                  k2 -= Math.max(
                    1,
                    Math.floor((normPart.length - splitNorm) * normFactor)
                  );
                }
              }
              break;
            }
          }
        }
      }
    }
    splits.push(cssText);
    return splits;
  }
  function markCssSplits(cssText, style) {
    return splitCssText(cssText, style).join("/* rr_split */");
  }
  function genId() {
    return _id++;
  }
  function getValidTagName$1(element) {
    if (element instanceof HTMLFormElement) {
      return "form";
    }
    const processedTagName = toLowerCase2(element.tagName);
    if (tagNameRegex.test(processedTagName)) {
      return "div";
    }
    return processedTagName;
  }
  function getAbsoluteSrcsetString(doc, attributeValue) {
    if (attributeValue.trim() === "") {
      return attributeValue;
    }
    let pos = 0;
    function collectCharacters(regEx) {
      let chars22;
      const match = regEx.exec(attributeValue.substring(pos));
      if (match) {
        chars22 = match[0];
        pos += chars22.length;
        return chars22;
      }
      return "";
    }
    const output = [];
    while (true) {
      collectCharacters(SRCSET_COMMAS_OR_SPACES);
      if (pos >= attributeValue.length) {
        break;
      }
      let url = collectCharacters(SRCSET_NOT_SPACES);
      if (url.slice(-1) === ",") {
        url = absoluteToDoc(doc, url.substring(0, url.length - 1));
        output.push(url);
      } else {
        let descriptorsStr = "";
        url = absoluteToDoc(doc, url);
        let inParens = false;
        while (true) {
          const c2 = attributeValue.charAt(pos);
          if (c2 === "") {
            output.push((url + descriptorsStr).trim());
            break;
          } else if (!inParens) {
            if (c2 === ",") {
              pos += 1;
              output.push((url + descriptorsStr).trim());
              break;
            } else if (c2 === "(") {
              inParens = true;
            }
          } else {
            if (c2 === ")") {
              inParens = false;
            }
          }
          descriptorsStr += c2;
          pos += 1;
        }
      }
    }
    return output.join(", ");
  }
  function absoluteToDoc(doc, attributeValue) {
    if (!attributeValue || attributeValue.trim() === "") {
      return attributeValue;
    }
    return getHref(doc, attributeValue);
  }
  function isSVGElement(el) {
    return Boolean(el.tagName === "svg" || el.ownerSVGElement);
  }
  function getHref(doc, customHref) {
    let a2 = cachedDocument.get(doc);
    if (!a2) {
      a2 = doc.createElement("a");
      cachedDocument.set(doc, a2);
    }
    if (!customHref) {
      customHref = "";
    } else if (customHref.startsWith("blob:") || customHref.startsWith("data:")) {
      return customHref;
    }
    a2.setAttribute("href", customHref);
    return a2.href;
  }
  function transformAttribute(doc, tagName2, name, value) {
    if (!value) {
      return value;
    }
    if (name === "src" || name === "href" && !(tagName2 === "use" && value[0] === "#")) {
      return absoluteToDoc(doc, value);
    } else if (name === "xlink:href" && value[0] !== "#") {
      return absoluteToDoc(doc, value);
    } else if (name === "background" && (tagName2 === "table" || tagName2 === "td" || tagName2 === "th")) {
      return absoluteToDoc(doc, value);
    } else if (name === "srcset") {
      return getAbsoluteSrcsetString(doc, value);
    } else if (name === "style") {
      return absolutifyURLs(value, getHref(doc));
    } else if (tagName2 === "object" && name === "data") {
      return absoluteToDoc(doc, value);
    }
    return value;
  }
  function ignoreAttribute(tagName2, name, _value) {
    return (tagName2 === "video" || tagName2 === "audio") && name === "autoplay";
  }
  function _isBlockedElement(element, blockClass, blockSelector) {
    try {
      if (typeof blockClass === "string") {
        if (element.classList.contains(blockClass)) {
          return true;
        }
      } else {
        for (let eIndex = element.classList.length; eIndex--; ) {
          const className = element.classList[eIndex];
          if (blockClass.test(className)) {
            return true;
          }
        }
      }
      if (blockSelector) {
        return element.matches(blockSelector);
      }
    } catch (e2) {
    }
    return false;
  }
  function classMatchesRegex2(node22, regex, checkAncestors) {
    if (!node22) return false;
    if (node22.nodeType !== node22.ELEMENT_NODE) {
      if (!checkAncestors) return false;
      return classMatchesRegex2(index$12.parentNode(node22), regex, checkAncestors);
    }
    for (let eIndex = node22.classList.length; eIndex--; ) {
      const className = node22.classList[eIndex];
      if (regex.test(className)) {
        return true;
      }
    }
    if (!checkAncestors) return false;
    return classMatchesRegex2(index$12.parentNode(node22), regex, checkAncestors);
  }
  function needMaskingText(node22, maskTextClass, maskTextSelector, checkAncestors) {
    let el;
    if (isElement(node22)) {
      el = node22;
      if (!index$12.childNodes(el).length) {
        return false;
      }
    } else if (index$12.parentElement(node22) === null) {
      return false;
    } else {
      el = index$12.parentElement(node22);
    }
    try {
      if (typeof maskTextClass === "string") {
        if (checkAncestors) {
          if (el.closest(`.${maskTextClass}`)) return true;
        } else {
          if (el.classList.contains(maskTextClass)) return true;
        }
      } else {
        if (classMatchesRegex2(el, maskTextClass, checkAncestors)) return true;
      }
      if (maskTextSelector) {
        if (checkAncestors) {
          if (el.closest(maskTextSelector)) return true;
        } else {
          if (el.matches(maskTextSelector)) return true;
        }
      }
    } catch (e2) {
    }
    return false;
  }
  function onceIframeLoaded(iframeEl, listener, iframeLoadTimeout) {
    const win = iframeEl.contentWindow;
    if (!win) {
      return;
    }
    let fired = false;
    let readyState;
    try {
      readyState = win.document.readyState;
    } catch (error) {
      return;
    }
    if (readyState !== "complete") {
      const timer = setTimeout(() => {
        if (!fired) {
          listener();
          fired = true;
        }
      }, iframeLoadTimeout);
      iframeEl.addEventListener("load", () => {
        clearTimeout(timer);
        fired = true;
        listener();
      });
      return;
    }
    const blankUrl = "about:blank";
    if (win.location.href !== blankUrl || iframeEl.src === blankUrl || iframeEl.src === "") {
      setTimeout(listener, 0);
      return iframeEl.addEventListener("load", listener);
    }
    iframeEl.addEventListener("load", listener);
  }
  function onceStylesheetLoaded(link, listener, styleSheetLoadTimeout) {
    let fired = false;
    let styleSheetLoaded;
    try {
      styleSheetLoaded = link.sheet;
    } catch (error) {
      return;
    }
    if (styleSheetLoaded) return;
    const timer = setTimeout(() => {
      if (!fired) {
        listener();
        fired = true;
      }
    }, styleSheetLoadTimeout);
    link.addEventListener("load", () => {
      clearTimeout(timer);
      fired = true;
      listener();
    });
  }
  function serializeNode(n22, options) {
    const {
      doc,
      mirror: mirror2,
      blockClass,
      blockSelector,
      needsMask,
      inlineStylesheet,
      maskInputOptions = {},
      maskTextFn,
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      dataURLOptions = {},
      inlineImages,
      recordCanvas,
      keepIframeSrcFn,
      newlyAddedElement = false,
      cssCaptured = false,
      applyBackgroundColorToBlockedElements = false
    } = options;
    const rootId = getRootId(doc, mirror2);
    switch (n22.nodeType) {
      case n22.DOCUMENT_NODE:
        if (n22.compatMode !== "CSS1Compat") {
          return {
            type: NodeType$3.Document,
            childNodes: [],
            compatMode: n22.compatMode
            // probably "BackCompat"
          };
        } else {
          return {
            type: NodeType$3.Document,
            childNodes: []
          };
        }
      case n22.DOCUMENT_TYPE_NODE:
        return {
          type: NodeType$3.DocumentType,
          name: n22.name,
          publicId: n22.publicId,
          systemId: n22.systemId,
          rootId
        };
      case n22.ELEMENT_NODE:
        return serializeElementNode(n22, {
          doc,
          blockClass,
          blockSelector,
          inlineStylesheet,
          maskInputOptions,
          maskInputFn,
          maskAttributeFn: maskAttributeFn2,
          dataURLOptions,
          inlineImages,
          recordCanvas,
          keepIframeSrcFn,
          newlyAddedElement,
          rootId,
          applyBackgroundColorToBlockedElements
        });
      case n22.TEXT_NODE:
        return serializeTextNode(n22, {
          doc,
          needsMask,
          maskTextFn,
          rootId,
          cssCaptured
        });
      case n22.CDATA_SECTION_NODE:
        return {
          type: NodeType$3.CDATA,
          textContent: "",
          rootId
        };
      case n22.COMMENT_NODE:
        return {
          type: NodeType$3.Comment,
          textContent: index$12.textContent(n22) || "",
          rootId
        };
      default:
        return false;
    }
  }
  function getRootId(doc, mirror2) {
    if (!mirror2.hasNode(doc)) return void 0;
    const docId = mirror2.getId(doc);
    return docId === 1 ? void 0 : docId;
  }
  function serializeTextNode(n22, options) {
    const { needsMask, maskTextFn, rootId, cssCaptured } = options;
    const parent = index$12.parentNode(n22);
    const parentTagName = parent && parent.tagName;
    let textContent22 = "";
    const isStyle = parentTagName === "STYLE" ? true : void 0;
    const isScript = parentTagName === "SCRIPT" ? true : void 0;
    if (isScript) {
      textContent22 = "SCRIPT_PLACEHOLDER";
    } else if (!cssCaptured) {
      textContent22 = index$12.textContent(n22);
      if (isStyle && textContent22) {
        textContent22 = absolutifyURLs(textContent22, getHref(options.doc));
      }
    }
    if (!isStyle && !isScript && textContent22 && needsMask) {
      textContent22 = maskTextFn ? maskTextFn(textContent22, index$12.parentElement(n22)) : textContent22.replace(/[\S]/g, "*");
    }
    return {
      type: NodeType$3.Text,
      textContent: textContent22 || "",
      rootId
    };
  }
  function serializeElementNode(n22, options) {
    const {
      doc,
      blockClass,
      blockSelector,
      inlineStylesheet,
      maskInputOptions = {},
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      dataURLOptions = {},
      inlineImages,
      recordCanvas,
      keepIframeSrcFn,
      newlyAddedElement = false,
      rootId,
      applyBackgroundColorToBlockedElements = false
    } = options;
    const needBlock = _isBlockedElement(n22, blockClass, blockSelector);
    const tagName2 = getValidTagName$1(n22);
    let attributes = {};
    const len = n22.attributes.length;
    for (let i2 = 0; i2 < len; i2++) {
      const attr2 = n22.attributes[i2];
      if (!ignoreAttribute(tagName2, attr2.name, attr2.value)) {
        const transformedValue = transformAttribute(
          doc,
          tagName2,
          toLowerCase2(attr2.name),
          attr2.value
        );
        if (maskAttributeFn2 && transformedValue !== null) {
          const maskedValue = maskAttributeFn2(attr2.name, transformedValue, n22);
          if (maskedValue !== null) {
            attributes[attr2.name] = maskedValue;
          }
        } else {
          attributes[attr2.name] = transformedValue;
        }
      }
    }
    if (tagName2 === "link" && inlineStylesheet) {
      const stylesheet = Array.from(doc.styleSheets).find((s2) => {
        return s2.href === n22.href;
      });
      let cssText = null;
      if (stylesheet) {
        cssText = stringifyStylesheet(stylesheet);
      }
      if (cssText) {
        delete attributes.rel;
        delete attributes.href;
        attributes._cssText = cssText;
      }
    }
    if (tagName2 === "style" && n22.sheet) {
      let cssText = stringifyStylesheet(
        n22.sheet
      );
      if (cssText) {
        if (n22.childNodes.length > 1) {
          cssText = markCssSplits(cssText, n22);
        }
        attributes._cssText = cssText;
      }
    }
    if (tagName2 === "input" || tagName2 === "textarea" || tagName2 === "select") {
      const value = n22.value;
      const checked = n22.checked;
      if (attributes.type !== "radio" && attributes.type !== "checkbox" && attributes.type !== "submit" && attributes.type !== "button" && value) {
        attributes.value = maskInputValue({
          element: n22,
          type: getInputType2(n22),
          tagName: tagName2,
          value,
          maskInputOptions,
          maskInputFn
        });
      } else if (checked) {
        attributes.checked = checked;
      }
    }
    if (tagName2 === "option") {
      if (n22.selected && !maskInputOptions["select"]) {
        attributes.selected = true;
      } else {
        delete attributes.selected;
      }
    }
    if (tagName2 === "dialog" && n22.open) {
      attributes.rr_open_mode = n22.matches("dialog:modal") ? "modal" : "non-modal";
    }
    if (tagName2 === "canvas" && recordCanvas) {
      if (n22.__context === "2d") {
        if (!is2DCanvasBlank(n22)) {
          attributes.rr_dataURL = n22.toDataURL(
            dataURLOptions.type,
            dataURLOptions.quality
          );
        }
      } else if (!("__context" in n22)) {
        const canvasDataURL = n22.toDataURL(
          dataURLOptions.type,
          dataURLOptions.quality
        );
        const blankCanvas = doc.createElement("canvas");
        blankCanvas.width = n22.width;
        blankCanvas.height = n22.height;
        const blankCanvasDataURL = blankCanvas.toDataURL(
          dataURLOptions.type,
          dataURLOptions.quality
        );
        if (canvasDataURL !== blankCanvasDataURL) {
          attributes.rr_dataURL = canvasDataURL;
        }
      }
    }
    if (tagName2 === "img" && inlineImages) {
      if (!canvasService) {
        canvasService = doc.createElement("canvas");
        canvasCtx = canvasService.getContext("2d");
      }
      const image = n22;
      const imageSrc = image.currentSrc || image.getAttribute("src") || "<unknown-src>";
      const priorCrossOrigin = image.crossOrigin;
      const recordInlineImage = () => {
        image.removeEventListener("load", recordInlineImage);
        try {
          canvasService.width = image.naturalWidth;
          canvasService.height = image.naturalHeight;
          canvasCtx.drawImage(image, 0, 0);
          attributes.rr_dataURL = canvasService.toDataURL(
            dataURLOptions.type,
            dataURLOptions.quality
          );
        } catch (err) {
          if (image.crossOrigin !== "anonymous") {
            image.crossOrigin = "anonymous";
            if (image.complete && image.naturalWidth !== 0)
              recordInlineImage();
            else image.addEventListener("load", recordInlineImage);
            return;
          } else {
            console.warn(
              `Cannot inline img src=${imageSrc}! Error: ${err}`
            );
          }
        }
        if (image.crossOrigin === "anonymous") {
          priorCrossOrigin ? attributes.crossOrigin = priorCrossOrigin : image.removeAttribute("crossorigin");
        }
      };
      if (image.complete && image.naturalWidth !== 0) recordInlineImage();
      else image.addEventListener("load", recordInlineImage);
    }
    if (tagName2 === "audio" || tagName2 === "video") {
      const mediaAttributes = attributes;
      mediaAttributes.rr_mediaState = n22.paused ? "paused" : "played";
      mediaAttributes.rr_mediaCurrentTime = n22.currentTime;
      mediaAttributes.rr_mediaPlaybackRate = n22.playbackRate;
      mediaAttributes.rr_mediaMuted = n22.muted;
      mediaAttributes.rr_mediaLoop = n22.loop;
      mediaAttributes.rr_mediaVolume = n22.volume;
    }
    if (!newlyAddedElement) {
      if (n22.scrollLeft) {
        attributes.rr_scrollLeft = n22.scrollLeft;
      }
      if (n22.scrollTop) {
        attributes.rr_scrollTop = n22.scrollTop;
      }
    }
    if (needBlock) {
      const { width, height } = n22.getBoundingClientRect();
      attributes = {
        class: attributes.class,
        rr_width: `${width}px`,
        rr_height: `${height}px`,
        ...applyBackgroundColorToBlockedElements ? { rr_background_color: _DEFAULT_BLOCKED_ELEMENT_BACKGROUND_COLOR } : {}
      };
    }
    if (tagName2 === "iframe" && !keepIframeSrcFn(attributes.src)) {
      if (!n22.contentDocument) {
        attributes.rr_src = attributes.src;
      }
      delete attributes.src;
    }
    let isCustomElement;
    try {
      if (customElements.get(tagName2)) isCustomElement = true;
    } catch (e2) {
    }
    return {
      type: NodeType$3.Element,
      tagName: tagName2,
      attributes,
      childNodes: [],
      isSVG: isSVGElement(n22) || void 0,
      needBlock,
      rootId,
      isCustom: isCustomElement
    };
  }
  function lowerIfExists(maybeAttr) {
    if (maybeAttr === void 0 || maybeAttr === null) {
      return "";
    } else {
      return maybeAttr.toLowerCase();
    }
  }
  function slimDOMExcluded(sn, slimDOMOptions) {
    if (slimDOMOptions.comment && sn.type === NodeType$3.Comment) {
      return true;
    } else if (sn.type === NodeType$3.Element) {
      if (slimDOMOptions.script && // script tag
      (sn.tagName === "script" || // (module)preload link
      sn.tagName === "link" && (sn.attributes.rel === "preload" || sn.attributes.rel === "modulepreload") && sn.attributes.as === "script" || // prefetch link
      sn.tagName === "link" && sn.attributes.rel === "prefetch" && typeof sn.attributes.href === "string" && extractFileExtension(sn.attributes.href) === "js")) {
        return true;
      } else if (slimDOMOptions.headFavicon && (sn.tagName === "link" && sn.attributes.rel === "shortcut icon" || sn.tagName === "meta" && (lowerIfExists(sn.attributes.name).match(
        /^msapplication-tile(image|color)$/
      ) || lowerIfExists(sn.attributes.name) === "application-name" || lowerIfExists(sn.attributes.rel) === "icon" || lowerIfExists(sn.attributes.rel) === "apple-touch-icon" || lowerIfExists(sn.attributes.rel) === "shortcut icon"))) {
        return true;
      } else if (sn.tagName === "meta") {
        if (slimDOMOptions.headMetaDescKeywords && lowerIfExists(sn.attributes.name).match(/^description|keywords$/)) {
          return true;
        } else if (slimDOMOptions.headMetaSocial && (lowerIfExists(sn.attributes.property).match(/^(og|twitter|fb):/) || // og = opengraph (facebook)
        lowerIfExists(sn.attributes.name).match(/^(og|twitter):/) || lowerIfExists(sn.attributes.name) === "pinterest")) {
          return true;
        } else if (slimDOMOptions.headMetaRobots && (lowerIfExists(sn.attributes.name) === "robots" || lowerIfExists(sn.attributes.name) === "googlebot" || lowerIfExists(sn.attributes.name) === "bingbot")) {
          return true;
        } else if (slimDOMOptions.headMetaHttpEquiv && sn.attributes["http-equiv"] !== void 0) {
          return true;
        } else if (slimDOMOptions.headMetaAuthorship && (lowerIfExists(sn.attributes.name) === "author" || lowerIfExists(sn.attributes.name) === "generator" || lowerIfExists(sn.attributes.name) === "framework" || lowerIfExists(sn.attributes.name) === "publisher" || lowerIfExists(sn.attributes.name) === "progid" || lowerIfExists(sn.attributes.property).match(/^article:/) || lowerIfExists(sn.attributes.property).match(/^product:/))) {
          return true;
        } else if (slimDOMOptions.headMetaVerification && (lowerIfExists(sn.attributes.name) === "google-site-verification" || lowerIfExists(sn.attributes.name) === "yandex-verification" || lowerIfExists(sn.attributes.name) === "csrf-token" || lowerIfExists(sn.attributes.name) === "p:domain_verify" || lowerIfExists(sn.attributes.name) === "verify-v1" || lowerIfExists(sn.attributes.name) === "verification" || lowerIfExists(sn.attributes.name) === "shopify-checkout-api-token")) {
          return true;
        }
      }
    }
    return false;
  }
  function serializeAdoptedStyleSheets(sheets, onAdoptedStyleSheet, emittedStyleIds) {
    return Array.from(sheets).map((sheet) => {
      const styleId = onAdoptedStyleSheet(sheet);
      let rules2 = [];
      if (!emittedStyleIds.has(styleId)) {
        emittedStyleIds.add(styleId);
        try {
          rules2 = Array.from(sheet.cssRules, (rule22, index22) => ({
            rule: stringifyRule(rule22, sheet.href),
            index: index22
          }));
        } catch (e2) {
          if (e2 instanceof DOMException && e2.name === "SecurityError") ;
          else {
            throw e2;
          }
        }
      }
      return { styleId, rules: rules2 };
    });
  }
  function serializeNodeWithId(n22, options) {
    var _a4;
    var _a22, _b;
    const {
      doc,
      mirror: mirror2,
      blockClass,
      blockSelector,
      maskTextClass,
      maskTextSelector,
      skipChild = false,
      inlineStylesheet = true,
      maskInputOptions = {},
      maskTextFn,
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      slimDOMOptions,
      dataURLOptions = {},
      inlineImages = false,
      recordCanvas = false,
      onSerialize,
      onAdoptedStyleSheet,
      onIframeLoad,
      iframeLoadTimeout = 5e3,
      onStylesheetLoad,
      stylesheetLoadTimeout = 5e3,
      keepIframeSrcFn = () => false,
      newlyAddedElement = false,
      cssCaptured = false,
      applyBackgroundColorToBlockedElements = false
    } = options;
    const emittedStyleIds = (_a4 = options._emittedStyleIds) != null ? _a4 : /* @__PURE__ */ new Set();
    let { needsMask } = options;
    let { preserveWhiteSpace = true } = options;
    if (!needsMask) {
      const checkAncestors = needsMask === void 0;
      needsMask = needMaskingText(
        n22,
        maskTextClass,
        maskTextSelector,
        checkAncestors
      );
    }
    const _serializedNode = serializeNode(n22, {
      doc,
      mirror: mirror2,
      blockClass,
      blockSelector,
      needsMask,
      inlineStylesheet,
      maskInputOptions,
      maskTextFn,
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      dataURLOptions,
      inlineImages,
      recordCanvas,
      keepIframeSrcFn,
      newlyAddedElement,
      cssCaptured,
      applyBackgroundColorToBlockedElements
    });
    if (!_serializedNode) {
      console.warn(n22, "not serialized");
      return null;
    }
    let id2;
    if (mirror2.hasNode(n22)) {
      id2 = mirror2.getId(n22);
    } else if (slimDOMExcluded(_serializedNode, slimDOMOptions) || !preserveWhiteSpace && _serializedNode.type === NodeType$3.Text && !_serializedNode.textContent.replace(/^\s+|\s+$/gm, "").length) {
      id2 = IGNORED_NODE2;
    } else {
      id2 = genId();
    }
    const serializedNode = Object.assign(_serializedNode, { id: id2 });
    mirror2.add(n22, serializedNode);
    if (id2 === IGNORED_NODE2) {
      return null;
    }
    if (onSerialize) {
      onSerialize(n22);
    }
    let recordChild = !skipChild;
    if (serializedNode.type === NodeType$3.Element) {
      recordChild = recordChild && !serializedNode.needBlock;
      delete serializedNode.needBlock;
      const shadowRootEl = index$12.shadowRoot(n22);
      if (shadowRootEl && isNativeShadowDom(shadowRootEl)) {
        serializedNode.isShadowHost = true;
        if (onAdoptedStyleSheet && ((_a22 = shadowRootEl.adoptedStyleSheets) == null ? void 0 : _a22.length)) {
          serializedNode.adoptedStyleSheets = serializeAdoptedStyleSheets(
            shadowRootEl.adoptedStyleSheets,
            onAdoptedStyleSheet,
            emittedStyleIds
          );
        }
      }
    }
    if (serializedNode.type === NodeType$3.Document && onAdoptedStyleSheet) {
      const doc2 = n22;
      if ((_b = doc2.adoptedStyleSheets) == null ? void 0 : _b.length) {
        serializedNode.adoptedStyleSheets = serializeAdoptedStyleSheets(
          doc2.adoptedStyleSheets,
          onAdoptedStyleSheet,
          emittedStyleIds
        );
      }
    }
    if ((serializedNode.type === NodeType$3.Document || serializedNode.type === NodeType$3.Element) && recordChild) {
      if (slimDOMOptions.headWhitespace && serializedNode.type === NodeType$3.Element && serializedNode.tagName === "head") {
        preserveWhiteSpace = false;
      }
      const bypassOptions = {
        doc,
        mirror: mirror2,
        blockClass,
        blockSelector,
        needsMask,
        maskTextClass,
        maskTextSelector,
        skipChild,
        inlineStylesheet,
        maskInputOptions,
        maskTextFn,
        maskInputFn,
        maskAttributeFn: maskAttributeFn2,
        slimDOMOptions,
        dataURLOptions,
        inlineImages,
        recordCanvas,
        preserveWhiteSpace,
        onSerialize,
        onAdoptedStyleSheet,
        _emittedStyleIds: emittedStyleIds,
        onIframeLoad,
        iframeLoadTimeout,
        onStylesheetLoad,
        stylesheetLoadTimeout,
        keepIframeSrcFn,
        cssCaptured: false,
        applyBackgroundColorToBlockedElements
      };
      if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "textarea" && serializedNode.attributes.value !== void 0) ;
      else {
        if (serializedNode.type === NodeType$3.Element && serializedNode.attributes._cssText !== void 0 && typeof serializedNode.attributes._cssText === "string") {
          bypassOptions.cssCaptured = true;
        }
        for (const childN of Array.from(index$12.childNodes(n22))) {
          const serializedChildNode = serializeNodeWithId(childN, bypassOptions);
          if (serializedChildNode) {
            serializedNode.childNodes.push(serializedChildNode);
          }
        }
      }
      let shadowRootEl = null;
      if (isElement(n22) && (shadowRootEl = index$12.shadowRoot(n22))) {
        for (const childN of Array.from(index$12.childNodes(shadowRootEl))) {
          const serializedChildNode = serializeNodeWithId(childN, bypassOptions);
          if (serializedChildNode) {
            isNativeShadowDom(shadowRootEl) && (serializedChildNode.isShadow = true);
            serializedNode.childNodes.push(serializedChildNode);
          }
        }
      }
    }
    const parent = index$12.parentNode(n22);
    if (parent && isShadowRoot3(parent) && isNativeShadowDom(parent)) {
      serializedNode.isShadow = true;
    }
    if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "iframe") {
      onceIframeLoaded(
        n22,
        () => {
          const iframeDoc = n22.contentDocument;
          if (iframeDoc && onIframeLoad) {
            const serializedIframeNode = serializeNodeWithId(iframeDoc, {
              doc: iframeDoc,
              mirror: mirror2,
              blockClass,
              blockSelector,
              needsMask,
              maskTextClass,
              maskTextSelector,
              skipChild: false,
              inlineStylesheet,
              maskInputOptions,
              maskTextFn,
              maskInputFn,
              maskAttributeFn: maskAttributeFn2,
              slimDOMOptions,
              dataURLOptions,
              inlineImages,
              recordCanvas,
              preserveWhiteSpace,
              onSerialize,
              onAdoptedStyleSheet,
              onIframeLoad,
              iframeLoadTimeout,
              onStylesheetLoad,
              stylesheetLoadTimeout,
              keepIframeSrcFn
            });
            if (serializedIframeNode) {
              onIframeLoad(
                n22,
                serializedIframeNode
              );
            }
          }
        },
        iframeLoadTimeout
      );
    }
    if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "link" && typeof serializedNode.attributes.rel === "string" && (serializedNode.attributes.rel === "stylesheet" || serializedNode.attributes.rel === "preload" && typeof serializedNode.attributes.href === "string" && extractFileExtension(serializedNode.attributes.href) === "css")) {
      onceStylesheetLoaded(
        n22,
        () => {
          if (onStylesheetLoad) {
            const serializedLinkNode = serializeNodeWithId(n22, {
              doc,
              mirror: mirror2,
              blockClass,
              blockSelector,
              needsMask,
              maskTextClass,
              maskTextSelector,
              skipChild: false,
              inlineStylesheet,
              maskInputOptions,
              maskTextFn,
              maskInputFn,
              maskAttributeFn: maskAttributeFn2,
              slimDOMOptions,
              dataURLOptions,
              inlineImages,
              recordCanvas,
              preserveWhiteSpace,
              onSerialize,
              onIframeLoad,
              iframeLoadTimeout,
              onStylesheetLoad,
              stylesheetLoadTimeout,
              keepIframeSrcFn
            });
            if (serializedLinkNode) {
              onStylesheetLoad(
                n22,
                serializedLinkNode
              );
            }
          }
        },
        stylesheetLoadTimeout
      );
    }
    return serializedNode;
  }
  function snapshot(n22, options) {
    const {
      mirror: mirror2 = new Mirror2(),
      blockClass = "rr-block",
      blockSelector = null,
      maskTextClass = "rr-mask",
      maskTextSelector = null,
      inlineStylesheet = true,
      inlineImages = false,
      recordCanvas = false,
      maskAllInputs = false,
      maskTextFn,
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      slimDOM = false,
      dataURLOptions,
      preserveWhiteSpace,
      onSerialize,
      onAdoptedStyleSheet,
      onIframeLoad,
      iframeLoadTimeout,
      onStylesheetLoad,
      stylesheetLoadTimeout,
      keepIframeSrcFn = () => false,
      applyBackgroundColorToBlockedElements = false
    } = options || {};
    const maskInputOptions = maskAllInputs === true ? {
      color: true,
      date: true,
      "datetime-local": true,
      email: true,
      month: true,
      number: true,
      range: true,
      search: true,
      tel: true,
      text: true,
      time: true,
      url: true,
      week: true,
      textarea: true,
      select: true,
      password: true
    } : maskAllInputs === false ? {
      password: true
    } : maskAllInputs;
    const slimDOMOptions = slimDOM === true || slimDOM === "all" ? (
      // if true: set of sensible options that should not throw away any information
      {
        script: true,
        comment: true,
        headFavicon: true,
        headWhitespace: true,
        headMetaDescKeywords: slimDOM === "all",
        // destructive
        headMetaSocial: true,
        headMetaRobots: true,
        headMetaHttpEquiv: true,
        headMetaAuthorship: true,
        headMetaVerification: true
      }
    ) : slimDOM === false ? {} : slimDOM;
    return serializeNodeWithId(n22, {
      doc: n22,
      mirror: mirror2,
      blockClass,
      blockSelector,
      maskTextClass,
      maskTextSelector,
      skipChild: false,
      inlineStylesheet,
      maskInputOptions,
      maskTextFn,
      maskInputFn,
      maskAttributeFn: maskAttributeFn2,
      slimDOMOptions,
      dataURLOptions,
      inlineImages,
      recordCanvas,
      preserveWhiteSpace,
      onSerialize,
      onAdoptedStyleSheet,
      onIframeLoad,
      iframeLoadTimeout,
      onStylesheetLoad,
      stylesheetLoadTimeout,
      keepIframeSrcFn,
      newlyAddedElement: false,
      applyBackgroundColorToBlockedElements
    });
  }
  function getDefaultExportFromCjs$12(x2) {
    return x2 && x2.__esModule && Object.prototype.hasOwnProperty.call(x2, "default") ? x2["default"] : x2;
  }
  function getAugmentedNamespace$12(n22) {
    if (Object.prototype.hasOwnProperty.call(n22, "__esModule")) return n22;
    var f2 = n22.default;
    if (typeof f2 == "function") {
      var a2 = function a22() {
        if (this instanceof a22) {
          return Reflect.construct(f2, arguments, this.constructor);
        }
        return f2.apply(this, arguments);
      };
      a2.prototype = f2.prototype;
    } else a2 = {};
    Object.defineProperty(a2, "__esModule", { value: true });
    Object.keys(n22).forEach(function(k2) {
      var d2 = Object.getOwnPropertyDescriptor(n22, k2);
      Object.defineProperty(a2, k2, d2.get ? d2 : {
        enumerable: true,
        get: function() {
          return n22[k2];
        }
      });
    });
    return a2;
  }
  function requirePicocolors_browser$12() {
    if (hasRequiredPicocolors_browser$12) return picocolors_browser$12.exports;
    hasRequiredPicocolors_browser$12 = 1;
    var x2 = String;
    var create = function() {
      return { isColorSupported: false, reset: x2, bold: x2, dim: x2, italic: x2, underline: x2, inverse: x2, hidden: x2, strikethrough: x2, black: x2, red: x2, green: x2, yellow: x2, blue: x2, magenta: x2, cyan: x2, white: x2, gray: x2, bgBlack: x2, bgRed: x2, bgGreen: x2, bgYellow: x2, bgBlue: x2, bgMagenta: x2, bgCyan: x2, bgWhite: x2 };
    };
    picocolors_browser$12.exports = create();
    picocolors_browser$12.exports.createColors = create;
    return picocolors_browser$12.exports;
  }
  function requireCssSyntaxError$12() {
    if (hasRequiredCssSyntaxError$12) return cssSyntaxError$12;
    hasRequiredCssSyntaxError$12 = 1;
    let pico = /* @__PURE__ */ requirePicocolors_browser$12();
    let terminalHighlight = require$$2$12;
    class CssSyntaxError extends Error {
      constructor(message, line2, column, source, file, plugin) {
        super(message);
        this.name = "CssSyntaxError";
        this.reason = message;
        if (file) {
          this.file = file;
        }
        if (source) {
          this.source = source;
        }
        if (plugin) {
          this.plugin = plugin;
        }
        if (typeof line2 !== "undefined" && typeof column !== "undefined") {
          if (typeof line2 === "number") {
            this.line = line2;
            this.column = column;
          } else {
            this.line = line2.line;
            this.column = line2.column;
            this.endLine = column.line;
            this.endColumn = column.column;
          }
        }
        this.setMessage();
        if (Error.captureStackTrace) {
          Error.captureStackTrace(this, CssSyntaxError);
        }
      }
      setMessage() {
        this.message = this.plugin ? this.plugin + ": " : "";
        this.message += this.file ? this.file : "<css input>";
        if (typeof this.line !== "undefined") {
          this.message += ":" + this.line + ":" + this.column;
        }
        this.message += ": " + this.reason;
      }
      showSourceCode(color) {
        if (!this.source) return "";
        let css = this.source;
        if (color == null) color = pico.isColorSupported;
        if (terminalHighlight) {
          if (color) css = terminalHighlight(css);
        }
        let lines = css.split(/\r?\n/);
        let start3 = Math.max(this.line - 3, 0);
        let end = Math.min(this.line + 2, lines.length);
        let maxWidth = String(end).length;
        let mark, aside;
        if (color) {
          let { bold, gray, red } = pico.createColors(true);
          mark = (text) => bold(red(text));
          aside = (text) => gray(text);
        } else {
          mark = aside = (str) => str;
        }
        return lines.slice(start3, end).map((line2, index22) => {
          let number = start3 + 1 + index22;
          let gutter = " " + (" " + number).slice(-maxWidth) + " | ";
          if (number === this.line) {
            let spacing = aside(gutter.replace(/\d/g, " ")) + line2.slice(0, this.column - 1).replace(/[^\t]/g, " ");
            return mark(">") + aside(gutter) + line2 + "\n " + spacing + mark("^");
          }
          return " " + aside(gutter) + line2;
        }).join("\n");
      }
      toString() {
        let code = this.showSourceCode();
        if (code) {
          code = "\n\n" + code + "\n";
        }
        return this.name + ": " + this.message + code;
      }
    }
    cssSyntaxError$12 = CssSyntaxError;
    CssSyntaxError.default = CssSyntaxError;
    return cssSyntaxError$12;
  }
  function requireSymbols$12() {
    if (hasRequiredSymbols$12) return symbols$12;
    hasRequiredSymbols$12 = 1;
    symbols$12.isClean = Symbol("isClean");
    symbols$12.my = Symbol("my");
    return symbols$12;
  }
  function requireStringifier$12() {
    if (hasRequiredStringifier$12) return stringifier$12;
    hasRequiredStringifier$12 = 1;
    const DEFAULT_RAW = {
      after: "\n",
      beforeClose: "\n",
      beforeComment: "\n",
      beforeDecl: "\n",
      beforeOpen: " ",
      beforeRule: "\n",
      colon: ": ",
      commentLeft: " ",
      commentRight: " ",
      emptyBody: "",
      indent: "    ",
      semicolon: false
    };
    function capitalize(str) {
      return str[0].toUpperCase() + str.slice(1);
    }
    class Stringifier {
      constructor(builder) {
        this.builder = builder;
      }
      atrule(node22, semicolon) {
        let name = "@" + node22.name;
        let params = node22.params ? this.rawValue(node22, "params") : "";
        if (typeof node22.raws.afterName !== "undefined") {
          name += node22.raws.afterName;
        } else if (params) {
          name += " ";
        }
        if (node22.nodes) {
          this.block(node22, name + params);
        } else {
          let end = (node22.raws.between || "") + (semicolon ? ";" : "");
          this.builder(name + params + end, node22);
        }
      }
      beforeAfter(node22, detect) {
        let value;
        if (node22.type === "decl") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (node22.type === "comment") {
          value = this.raw(node22, null, "beforeComment");
        } else if (detect === "before") {
          value = this.raw(node22, null, "beforeRule");
        } else {
          value = this.raw(node22, null, "beforeClose");
        }
        let buf = node22.parent;
        let depth = 0;
        while (buf && buf.type !== "root") {
          depth += 1;
          buf = buf.parent;
        }
        if (value.includes("\n")) {
          let indent = this.raw(node22, null, "indent");
          if (indent.length) {
            for (let step = 0; step < depth; step++) value += indent;
          }
        }
        return value;
      }
      block(node22, start3) {
        let between = this.raw(node22, "between", "beforeOpen");
        this.builder(start3 + between + "{", node22, "start");
        let after;
        if (node22.nodes && node22.nodes.length) {
          this.body(node22);
          after = this.raw(node22, "after");
        } else {
          after = this.raw(node22, "after", "emptyBody");
        }
        if (after) this.builder(after);
        this.builder("}", node22, "end");
      }
      body(node22) {
        let last = node22.nodes.length - 1;
        while (last > 0) {
          if (node22.nodes[last].type !== "comment") break;
          last -= 1;
        }
        let semicolon = this.raw(node22, "semicolon");
        for (let i2 = 0; i2 < node22.nodes.length; i2++) {
          let child = node22.nodes[i2];
          let before = this.raw(child, "before");
          if (before) this.builder(before);
          this.stringify(child, last !== i2 || semicolon);
        }
      }
      comment(node22) {
        let left = this.raw(node22, "left", "commentLeft");
        let right = this.raw(node22, "right", "commentRight");
        this.builder("/*" + left + node22.text + right + "*/", node22);
      }
      decl(node22, semicolon) {
        let between = this.raw(node22, "between", "colon");
        let string = node22.prop + between + this.rawValue(node22, "value");
        if (node22.important) {
          string += node22.raws.important || " !important";
        }
        if (semicolon) string += ";";
        this.builder(string, node22);
      }
      document(node22) {
        this.body(node22);
      }
      raw(node22, own, detect) {
        let value;
        if (!detect) detect = own;
        if (own) {
          value = node22.raws[own];
          if (typeof value !== "undefined") return value;
        }
        let parent = node22.parent;
        if (detect === "before") {
          if (!parent || parent.type === "root" && parent.first === node22) {
            return "";
          }
          if (parent && parent.type === "document") {
            return "";
          }
        }
        if (!parent) return DEFAULT_RAW[detect];
        let root22 = node22.root();
        if (!root22.rawCache) root22.rawCache = {};
        if (typeof root22.rawCache[detect] !== "undefined") {
          return root22.rawCache[detect];
        }
        if (detect === "before" || detect === "after") {
          return this.beforeAfter(node22, detect);
        } else {
          let method = "raw" + capitalize(detect);
          if (this[method]) {
            value = this[method](root22, node22);
          } else {
            root22.walk((i2) => {
              value = i2.raws[own];
              if (typeof value !== "undefined") return false;
            });
          }
        }
        if (typeof value === "undefined") value = DEFAULT_RAW[detect];
        root22.rawCache[detect] = value;
        return value;
      }
      rawBeforeClose(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length > 0) {
            if (typeof i2.raws.after !== "undefined") {
              value = i2.raws.after;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawBeforeComment(root22, node22) {
        let value;
        root22.walkComments((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeDecl(root22, node22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeRule");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeOpen(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.type !== "decl") {
            value = i2.raws.between;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawBeforeRule(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && (i2.parent !== root22 || root22.first !== i2)) {
            if (typeof i2.raws.before !== "undefined") {
              value = i2.raws.before;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawColon(root22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.between !== "undefined") {
            value = i2.raws.between.replace(/[^\s:]/g, "");
            return false;
          }
        });
        return value;
      }
      rawEmptyBody(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length === 0) {
            value = i2.raws.after;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawIndent(root22) {
        if (root22.raws.indent) return root22.raws.indent;
        let value;
        root22.walk((i2) => {
          let p2 = i2.parent;
          if (p2 && p2 !== root22 && p2.parent && p2.parent === root22) {
            if (typeof i2.raws.before !== "undefined") {
              let parts = i2.raws.before.split("\n");
              value = parts[parts.length - 1];
              value = value.replace(/\S/g, "");
              return false;
            }
          }
        });
        return value;
      }
      rawSemicolon(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length && i2.last.type === "decl") {
            value = i2.raws.semicolon;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawValue(node22, prop) {
        let value = node22[prop];
        let raw = node22.raws[prop];
        if (raw && raw.value === value) {
          return raw.raw;
        }
        return value;
      }
      root(node22) {
        this.body(node22);
        if (node22.raws.after) this.builder(node22.raws.after);
      }
      rule(node22) {
        this.block(node22, this.rawValue(node22, "selector"));
        if (node22.raws.ownSemicolon) {
          this.builder(node22.raws.ownSemicolon, node22, "end");
        }
      }
      stringify(node22, semicolon) {
        if (!this[node22.type]) {
          throw new Error(
            "Unknown AST node type " + node22.type + ". Maybe you need to change PostCSS stringifier."
          );
        }
        this[node22.type](node22, semicolon);
      }
    }
    stringifier$12 = Stringifier;
    Stringifier.default = Stringifier;
    return stringifier$12;
  }
  function requireStringify$12() {
    if (hasRequiredStringify$12) return stringify_1$12;
    hasRequiredStringify$12 = 1;
    let Stringifier = requireStringifier$12();
    function stringify2(node22, builder) {
      let str = new Stringifier(builder);
      str.stringify(node22);
    }
    stringify_1$12 = stringify2;
    stringify2.default = stringify2;
    return stringify_1$12;
  }
  function requireNode$12() {
    if (hasRequiredNode$12) return node$12;
    hasRequiredNode$12 = 1;
    let { isClean, my } = requireSymbols$12();
    let CssSyntaxError = requireCssSyntaxError$12();
    let Stringifier = requireStringifier$12();
    let stringify2 = requireStringify$12();
    function cloneNode(obj, parent) {
      let cloned = new obj.constructor();
      for (let i2 in obj) {
        if (!Object.prototype.hasOwnProperty.call(obj, i2)) {
          continue;
        }
        if (i2 === "proxyCache") continue;
        let value = obj[i2];
        let type = typeof value;
        if (i2 === "parent" && type === "object") {
          if (parent) cloned[i2] = parent;
        } else if (i2 === "source") {
          cloned[i2] = value;
        } else if (Array.isArray(value)) {
          cloned[i2] = value.map((j) => cloneNode(j, cloned));
        } else {
          if (type === "object" && value !== null) value = cloneNode(value);
          cloned[i2] = value;
        }
      }
      return cloned;
    }
    class Node2 {
      constructor(defaults = {}) {
        this.raws = {};
        this[isClean] = false;
        this[my] = true;
        for (let name in defaults) {
          if (name === "nodes") {
            this.nodes = [];
            for (let node22 of defaults[name]) {
              if (typeof node22.clone === "function") {
                this.append(node22.clone());
              } else {
                this.append(node22);
              }
            }
          } else {
            this[name] = defaults[name];
          }
        }
      }
      addToError(error) {
        error.postcssNode = this;
        if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
          let s2 = this.source;
          error.stack = error.stack.replace(
            /\n\s{4}at /,
            `$&${s2.input.from}:${s2.start.line}:${s2.start.column}$&`
          );
        }
        return error;
      }
      after(add2) {
        this.parent.insertAfter(this, add2);
        return this;
      }
      assign(overrides = {}) {
        for (let name in overrides) {
          this[name] = overrides[name];
        }
        return this;
      }
      before(add2) {
        this.parent.insertBefore(this, add2);
        return this;
      }
      cleanRaws(keepBetween) {
        delete this.raws.before;
        delete this.raws.after;
        if (!keepBetween) delete this.raws.between;
      }
      clone(overrides = {}) {
        let cloned = cloneNode(this);
        for (let name in overrides) {
          cloned[name] = overrides[name];
        }
        return cloned;
      }
      cloneAfter(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertAfter(this, cloned);
        return cloned;
      }
      cloneBefore(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertBefore(this, cloned);
        return cloned;
      }
      error(message, opts = {}) {
        if (this.source) {
          let { end, start: start3 } = this.rangeBy(opts);
          return this.source.input.error(
            message,
            { column: start3.column, line: start3.line },
            { column: end.column, line: end.line },
            opts
          );
        }
        return new CssSyntaxError(message);
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
            prop === "text") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      markDirty() {
        if (this[isClean]) {
          this[isClean] = false;
          let next = this;
          while (next = next.parent) {
            next[isClean] = false;
          }
        }
      }
      next() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 + 1];
      }
      positionBy(opts, stringRepresentation) {
        let pos = this.source.start;
        if (opts.index) {
          pos = this.positionInside(opts.index, stringRepresentation);
        } else if (opts.word) {
          stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) pos = this.positionInside(index22, stringRepresentation);
        }
        return pos;
      }
      positionInside(index22, stringRepresentation) {
        let string = stringRepresentation || this.toString();
        let column = this.source.start.column;
        let line2 = this.source.start.line;
        for (let i2 = 0; i2 < index22; i2++) {
          if (string[i2] === "\n") {
            column = 1;
            line2 += 1;
          } else {
            column += 1;
          }
        }
        return { column, line: line2 };
      }
      prev() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 - 1];
      }
      rangeBy(opts) {
        let start3 = {
          column: this.source.start.column,
          line: this.source.start.line
        };
        let end = this.source.end ? {
          column: this.source.end.column + 1,
          line: this.source.end.line
        } : {
          column: start3.column + 1,
          line: start3.line
        };
        if (opts.word) {
          let stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) {
            start3 = this.positionInside(index22, stringRepresentation);
            end = this.positionInside(index22 + opts.word.length, stringRepresentation);
          }
        } else {
          if (opts.start) {
            start3 = {
              column: opts.start.column,
              line: opts.start.line
            };
          } else if (opts.index) {
            start3 = this.positionInside(opts.index);
          }
          if (opts.end) {
            end = {
              column: opts.end.column,
              line: opts.end.line
            };
          } else if (typeof opts.endIndex === "number") {
            end = this.positionInside(opts.endIndex);
          } else if (opts.index) {
            end = this.positionInside(opts.index + 1);
          }
        }
        if (end.line < start3.line || end.line === start3.line && end.column <= start3.column) {
          end = { column: start3.column + 1, line: start3.line };
        }
        return { end, start: start3 };
      }
      raw(prop, defaultType) {
        let str = new Stringifier();
        return str.raw(this, prop, defaultType);
      }
      remove() {
        if (this.parent) {
          this.parent.removeChild(this);
        }
        this.parent = void 0;
        return this;
      }
      replaceWith(...nodes) {
        if (this.parent) {
          let bookmark = this;
          let foundSelf = false;
          for (let node22 of nodes) {
            if (node22 === this) {
              foundSelf = true;
            } else if (foundSelf) {
              this.parent.insertAfter(bookmark, node22);
              bookmark = node22;
            } else {
              this.parent.insertBefore(bookmark, node22);
            }
          }
          if (!foundSelf) {
            this.remove();
          }
        }
        return this;
      }
      root() {
        let result22 = this;
        while (result22.parent && result22.parent.type !== "document") {
          result22 = result22.parent;
        }
        return result22;
      }
      toJSON(_2, inputs) {
        let fixed = {};
        let emitInputs = inputs == null;
        inputs = inputs || /* @__PURE__ */ new Map();
        let inputsNextIndex = 0;
        for (let name in this) {
          if (!Object.prototype.hasOwnProperty.call(this, name)) {
            continue;
          }
          if (name === "parent" || name === "proxyCache") continue;
          let value = this[name];
          if (Array.isArray(value)) {
            fixed[name] = value.map((i2) => {
              if (typeof i2 === "object" && i2.toJSON) {
                return i2.toJSON(null, inputs);
              } else {
                return i2;
              }
            });
          } else if (typeof value === "object" && value.toJSON) {
            fixed[name] = value.toJSON(null, inputs);
          } else if (name === "source") {
            let inputId = inputs.get(value.input);
            if (inputId == null) {
              inputId = inputsNextIndex;
              inputs.set(value.input, inputsNextIndex);
              inputsNextIndex++;
            }
            fixed[name] = {
              end: value.end,
              inputId,
              start: value.start
            };
          } else {
            fixed[name] = value;
          }
        }
        if (emitInputs) {
          fixed.inputs = [...inputs.keys()].map((input22) => input22.toJSON());
        }
        return fixed;
      }
      toProxy() {
        if (!this.proxyCache) {
          this.proxyCache = new Proxy(this, this.getProxyProcessor());
        }
        return this.proxyCache;
      }
      toString(stringifier22 = stringify2) {
        if (stringifier22.stringify) stringifier22 = stringifier22.stringify;
        let result22 = "";
        stringifier22(this, (i2) => {
          result22 += i2;
        });
        return result22;
      }
      warn(result22, text, opts) {
        let data = { node: this };
        for (let i2 in opts) data[i2] = opts[i2];
        return result22.warn(text, data);
      }
      get proxyOf() {
        return this;
      }
    }
    node$12 = Node2;
    Node2.default = Node2;
    return node$12;
  }
  function requireDeclaration$12() {
    if (hasRequiredDeclaration$12) return declaration$12;
    hasRequiredDeclaration$12 = 1;
    let Node2 = requireNode$12();
    class Declaration extends Node2 {
      constructor(defaults) {
        if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
          defaults = { ...defaults, value: String(defaults.value) };
        }
        super(defaults);
        this.type = "decl";
      }
      get variable() {
        return this.prop.startsWith("--") || this.prop[0] === "$";
      }
    }
    declaration$12 = Declaration;
    Declaration.default = Declaration;
    return declaration$12;
  }
  function requireNonSecure$12() {
    if (hasRequiredNonSecure$12) return nonSecure$12;
    hasRequiredNonSecure$12 = 1;
    let urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
    let customAlphabet = (alphabet, defaultSize = 21) => {
      return (size = defaultSize) => {
        let id2 = "";
        let i2 = size;
        while (i2--) {
          id2 += alphabet[Math.random() * alphabet.length | 0];
        }
        return id2;
      };
    };
    let nanoid = (size = 21) => {
      let id2 = "";
      let i2 = size;
      while (i2--) {
        id2 += urlAlphabet[Math.random() * 64 | 0];
      }
      return id2;
    };
    nonSecure$12 = { nanoid, customAlphabet };
    return nonSecure$12;
  }
  function requirePreviousMap$12() {
    if (hasRequiredPreviousMap$12) return previousMap$12;
    hasRequiredPreviousMap$12 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$12;
    let { existsSync, readFileSync } = require$$2$12;
    let { dirname, join } = require$$2$12;
    function fromBase64(str) {
      if (Buffer) {
        return Buffer.from(str, "base64").toString();
      } else {
        return window.atob(str);
      }
    }
    class PreviousMap {
      constructor(css, opts) {
        if (opts.map === false) return;
        this.loadAnnotation(css);
        this.inline = this.startWith(this.annotation, "data:");
        let prev = opts.map ? opts.map.prev : void 0;
        let text = this.loadMap(opts.from, prev);
        if (!this.mapFile && opts.from) {
          this.mapFile = opts.from;
        }
        if (this.mapFile) this.root = dirname(this.mapFile);
        if (text) this.text = text;
      }
      consumer() {
        if (!this.consumerCache) {
          this.consumerCache = new SourceMapConsumer(this.text);
        }
        return this.consumerCache;
      }
      decodeInline(text) {
        let baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
        let baseUri = /^data:application\/json;base64,/;
        let charsetUri = /^data:application\/json;charset=utf-?8,/;
        let uri = /^data:application\/json,/;
        if (charsetUri.test(text) || uri.test(text)) {
          return decodeURIComponent(text.substr(RegExp.lastMatch.length));
        }
        if (baseCharsetUri.test(text) || baseUri.test(text)) {
          return fromBase64(text.substr(RegExp.lastMatch.length));
        }
        let encoding = text.match(/data:application\/json;([^,]+),/)[1];
        throw new Error("Unsupported source map encoding " + encoding);
      }
      getAnnotationURL(sourceMapString) {
        return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
      }
      isMap(map) {
        if (typeof map !== "object") return false;
        return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
      }
      loadAnnotation(css) {
        let comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
        if (!comments) return;
        let start3 = css.lastIndexOf(comments.pop());
        let end = css.indexOf("*/", start3);
        if (start3 > -1 && end > -1) {
          this.annotation = this.getAnnotationURL(css.substring(start3, end));
        }
      }
      loadFile(path) {
        this.root = dirname(path);
        if (existsSync(path)) {
          this.mapFile = path;
          return readFileSync(path, "utf-8").toString().trim();
        }
      }
      loadMap(file, prev) {
        if (prev === false) return false;
        if (prev) {
          if (typeof prev === "string") {
            return prev;
          } else if (typeof prev === "function") {
            let prevPath = prev(file);
            if (prevPath) {
              let map = this.loadFile(prevPath);
              if (!map) {
                throw new Error(
                  "Unable to load previous source map: " + prevPath.toString()
                );
              }
              return map;
            }
          } else if (prev instanceof SourceMapConsumer) {
            return SourceMapGenerator.fromSourceMap(prev).toString();
          } else if (prev instanceof SourceMapGenerator) {
            return prev.toString();
          } else if (this.isMap(prev)) {
            return JSON.stringify(prev);
          } else {
            throw new Error(
              "Unsupported previous source map format: " + prev.toString()
            );
          }
        } else if (this.inline) {
          return this.decodeInline(this.annotation);
        } else if (this.annotation) {
          let map = this.annotation;
          if (file) map = join(dirname(file), map);
          return this.loadFile(map);
        }
      }
      startWith(string, start3) {
        if (!string) return false;
        return string.substr(0, start3.length) === start3;
      }
      withContent() {
        return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
      }
    }
    previousMap$12 = PreviousMap;
    PreviousMap.default = PreviousMap;
    return previousMap$12;
  }
  function requireInput$12() {
    if (hasRequiredInput$12) return input$12;
    hasRequiredInput$12 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$12;
    let { fileURLToPath, pathToFileURL } = require$$2$12;
    let { isAbsolute, resolve } = require$$2$12;
    let { nanoid } = /* @__PURE__ */ requireNonSecure$12();
    let terminalHighlight = require$$2$12;
    let CssSyntaxError = requireCssSyntaxError$12();
    let PreviousMap = requirePreviousMap$12();
    let fromOffsetCache = Symbol("fromOffsetCache");
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(resolve && isAbsolute);
    class Input {
      constructor(css, opts = {}) {
        if (css === null || typeof css === "undefined" || typeof css === "object" && !css.toString) {
          throw new Error(`PostCSS received ${css} instead of CSS string`);
        }
        this.css = css.toString();
        if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
          this.hasBOM = true;
          this.css = this.css.slice(1);
        } else {
          this.hasBOM = false;
        }
        if (opts.from) {
          if (!pathAvailable || /^\w+:\/\//.test(opts.from) || isAbsolute(opts.from)) {
            this.file = opts.from;
          } else {
            this.file = resolve(opts.from);
          }
        }
        if (pathAvailable && sourceMapAvailable) {
          let map = new PreviousMap(this.css, opts);
          if (map.text) {
            this.map = map;
            let file = map.consumer().file;
            if (!this.file && file) this.file = this.mapResolve(file);
          }
        }
        if (!this.file) {
          this.id = "<input css " + nanoid(6) + ">";
        }
        if (this.map) this.map.file = this.from;
      }
      error(message, line2, column, opts = {}) {
        let result22, endLine, endColumn;
        if (line2 && typeof line2 === "object") {
          let start3 = line2;
          let end = column;
          if (typeof start3.offset === "number") {
            let pos = this.fromOffset(start3.offset);
            line2 = pos.line;
            column = pos.col;
          } else {
            line2 = start3.line;
            column = start3.column;
          }
          if (typeof end.offset === "number") {
            let pos = this.fromOffset(end.offset);
            endLine = pos.line;
            endColumn = pos.col;
          } else {
            endLine = end.line;
            endColumn = end.column;
          }
        } else if (!column) {
          let pos = this.fromOffset(line2);
          line2 = pos.line;
          column = pos.col;
        }
        let origin = this.origin(line2, column, endLine, endColumn);
        if (origin) {
          result22 = new CssSyntaxError(
            message,
            origin.endLine === void 0 ? origin.line : { column: origin.column, line: origin.line },
            origin.endLine === void 0 ? origin.column : { column: origin.endColumn, line: origin.endLine },
            origin.source,
            origin.file,
            opts.plugin
          );
        } else {
          result22 = new CssSyntaxError(
            message,
            endLine === void 0 ? line2 : { column, line: line2 },
            endLine === void 0 ? column : { column: endColumn, line: endLine },
            this.css,
            this.file,
            opts.plugin
          );
        }
        result22.input = { column, endColumn, endLine, line: line2, source: this.css };
        if (this.file) {
          if (pathToFileURL) {
            result22.input.url = pathToFileURL(this.file).toString();
          }
          result22.input.file = this.file;
        }
        return result22;
      }
      fromOffset(offset) {
        let lastLine, lineToIndex;
        if (!this[fromOffsetCache]) {
          let lines = this.css.split("\n");
          lineToIndex = new Array(lines.length);
          let prevIndex = 0;
          for (let i2 = 0, l2 = lines.length; i2 < l2; i2++) {
            lineToIndex[i2] = prevIndex;
            prevIndex += lines[i2].length + 1;
          }
          this[fromOffsetCache] = lineToIndex;
        } else {
          lineToIndex = this[fromOffsetCache];
        }
        lastLine = lineToIndex[lineToIndex.length - 1];
        let min = 0;
        if (offset >= lastLine) {
          min = lineToIndex.length - 1;
        } else {
          let max = lineToIndex.length - 2;
          let mid;
          while (min < max) {
            mid = min + (max - min >> 1);
            if (offset < lineToIndex[mid]) {
              max = mid - 1;
            } else if (offset >= lineToIndex[mid + 1]) {
              min = mid + 1;
            } else {
              min = mid;
              break;
            }
          }
        }
        return {
          col: offset - lineToIndex[min] + 1,
          line: min + 1
        };
      }
      mapResolve(file) {
        if (/^\w+:\/\//.test(file)) {
          return file;
        }
        return resolve(this.map.consumer().sourceRoot || this.map.root || ".", file);
      }
      origin(line2, column, endLine, endColumn) {
        if (!this.map) return false;
        let consumer = this.map.consumer();
        let from = consumer.originalPositionFor({ column, line: line2 });
        if (!from.source) return false;
        let to;
        if (typeof endLine === "number") {
          to = consumer.originalPositionFor({ column: endColumn, line: endLine });
        }
        let fromUrl;
        if (isAbsolute(from.source)) {
          fromUrl = pathToFileURL(from.source);
        } else {
          fromUrl = new URL(
            from.source,
            this.map.consumer().sourceRoot || pathToFileURL(this.map.mapFile)
          );
        }
        let result22 = {
          column: from.column,
          endColumn: to && to.column,
          endLine: to && to.line,
          line: from.line,
          url: fromUrl.toString()
        };
        if (fromUrl.protocol === "file:") {
          if (fileURLToPath) {
            result22.file = fileURLToPath(fromUrl);
          } else {
            throw new Error(`file: protocol is not available in this PostCSS build`);
          }
        }
        let source = consumer.sourceContentFor(from.source);
        if (source) result22.source = source;
        return result22;
      }
      toJSON() {
        let json = {};
        for (let name of ["hasBOM", "css", "file", "id"]) {
          if (this[name] != null) {
            json[name] = this[name];
          }
        }
        if (this.map) {
          json.map = { ...this.map };
          if (json.map.consumerCache) {
            json.map.consumerCache = void 0;
          }
        }
        return json;
      }
      get from() {
        return this.file || this.id;
      }
    }
    input$12 = Input;
    Input.default = Input;
    if (terminalHighlight && terminalHighlight.registerInput) {
      terminalHighlight.registerInput(Input);
    }
    return input$12;
  }
  function requireMapGenerator$12() {
    if (hasRequiredMapGenerator$12) return mapGenerator$12;
    hasRequiredMapGenerator$12 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$2$12;
    let { dirname, relative, resolve, sep } = require$$2$12;
    let { pathToFileURL } = require$$2$12;
    let Input = requireInput$12();
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(dirname && resolve && relative && sep);
    class MapGenerator {
      constructor(stringify2, root22, opts, cssString) {
        this.stringify = stringify2;
        this.mapOpts = opts.map || {};
        this.root = root22;
        this.opts = opts;
        this.css = cssString;
        this.originalCSS = cssString;
        this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
        this.memoizedFileURLs = /* @__PURE__ */ new Map();
        this.memoizedPaths = /* @__PURE__ */ new Map();
        this.memoizedURLs = /* @__PURE__ */ new Map();
      }
      addAnnotation() {
        let content;
        if (this.isInline()) {
          content = "data:application/json;base64," + this.toBase64(this.map.toString());
        } else if (typeof this.mapOpts.annotation === "string") {
          content = this.mapOpts.annotation;
        } else if (typeof this.mapOpts.annotation === "function") {
          content = this.mapOpts.annotation(this.opts.to, this.root);
        } else {
          content = this.outputFile() + ".map";
        }
        let eol = "\n";
        if (this.css.includes("\r\n")) eol = "\r\n";
        this.css += eol + "/*# sourceMappingURL=" + content + " */";
      }
      applyPrevMaps() {
        for (let prev of this.previous()) {
          let from = this.toUrl(this.path(prev.file));
          let root22 = prev.root || dirname(prev.file);
          let map;
          if (this.mapOpts.sourcesContent === false) {
            map = new SourceMapConsumer(prev.text);
            if (map.sourcesContent) {
              map.sourcesContent = null;
            }
          } else {
            map = prev.consumer();
          }
          this.map.applySourceMap(map, from, this.toUrl(this.path(root22)));
        }
      }
      clearAnnotation() {
        if (this.mapOpts.annotation === false) return;
        if (this.root) {
          let node22;
          for (let i2 = this.root.nodes.length - 1; i2 >= 0; i2--) {
            node22 = this.root.nodes[i2];
            if (node22.type !== "comment") continue;
            if (node22.text.indexOf("# sourceMappingURL=") === 0) {
              this.root.removeChild(i2);
            }
          }
        } else if (this.css) {
          this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
        }
      }
      generate() {
        this.clearAnnotation();
        if (pathAvailable && sourceMapAvailable && this.isMap()) {
          return this.generateMap();
        } else {
          let result22 = "";
          this.stringify(this.root, (i2) => {
            result22 += i2;
          });
          return [result22];
        }
      }
      generateMap() {
        if (this.root) {
          this.generateString();
        } else if (this.previous().length === 1) {
          let prev = this.previous()[0].consumer();
          prev.file = this.outputFile();
          this.map = SourceMapGenerator.fromSourceMap(prev, {
            ignoreInvalidMapping: true
          });
        } else {
          this.map = new SourceMapGenerator({
            file: this.outputFile(),
            ignoreInvalidMapping: true
          });
          this.map.addMapping({
            generated: { column: 0, line: 1 },
            original: { column: 0, line: 1 },
            source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
          });
        }
        if (this.isSourcesContent()) this.setSourcesContent();
        if (this.root && this.previous().length > 0) this.applyPrevMaps();
        if (this.isAnnotation()) this.addAnnotation();
        if (this.isInline()) {
          return [this.css];
        } else {
          return [this.css, this.map];
        }
      }
      generateString() {
        this.css = "";
        this.map = new SourceMapGenerator({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        let line2 = 1;
        let column = 1;
        let noSource = "<no source>";
        let mapping = {
          generated: { column: 0, line: 0 },
          original: { column: 0, line: 0 },
          source: ""
        };
        let lines, last;
        this.stringify(this.root, (str, node22, type) => {
          this.css += str;
          if (node22 && type !== "end") {
            mapping.generated.line = line2;
            mapping.generated.column = column - 1;
            if (node22.source && node22.source.start) {
              mapping.source = this.sourcePath(node22);
              mapping.original.line = node22.source.start.line;
              mapping.original.column = node22.source.start.column - 1;
              this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              this.map.addMapping(mapping);
            }
          }
          lines = str.match(/\n/g);
          if (lines) {
            line2 += lines.length;
            last = str.lastIndexOf("\n");
            column = str.length - last;
          } else {
            column += str.length;
          }
          if (node22 && type !== "start") {
            let p2 = node22.parent || { raws: {} };
            let childless = node22.type === "decl" || node22.type === "atrule" && !node22.nodes;
            if (!childless || node22 !== p2.last || p2.raws.semicolon) {
              if (node22.source && node22.source.end) {
                mapping.source = this.sourcePath(node22);
                mapping.original.line = node22.source.end.line;
                mapping.original.column = node22.source.end.column - 1;
                mapping.generated.line = line2;
                mapping.generated.column = column - 2;
                this.map.addMapping(mapping);
              } else {
                mapping.source = noSource;
                mapping.original.line = 1;
                mapping.original.column = 0;
                mapping.generated.line = line2;
                mapping.generated.column = column - 1;
                this.map.addMapping(mapping);
              }
            }
          }
        });
      }
      isAnnotation() {
        if (this.isInline()) {
          return true;
        }
        if (typeof this.mapOpts.annotation !== "undefined") {
          return this.mapOpts.annotation;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.annotation);
        }
        return true;
      }
      isInline() {
        if (typeof this.mapOpts.inline !== "undefined") {
          return this.mapOpts.inline;
        }
        let annotation = this.mapOpts.annotation;
        if (typeof annotation !== "undefined" && annotation !== true) {
          return false;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.inline);
        }
        return true;
      }
      isMap() {
        if (typeof this.opts.map !== "undefined") {
          return !!this.opts.map;
        }
        return this.previous().length > 0;
      }
      isSourcesContent() {
        if (typeof this.mapOpts.sourcesContent !== "undefined") {
          return this.mapOpts.sourcesContent;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.withContent());
        }
        return true;
      }
      outputFile() {
        if (this.opts.to) {
          return this.path(this.opts.to);
        } else if (this.opts.from) {
          return this.path(this.opts.from);
        } else {
          return "to.css";
        }
      }
      path(file) {
        if (this.mapOpts.absolute) return file;
        if (file.charCodeAt(0) === 60) return file;
        if (/^\w+:\/\//.test(file)) return file;
        let cached = this.memoizedPaths.get(file);
        if (cached) return cached;
        let from = this.opts.to ? dirname(this.opts.to) : ".";
        if (typeof this.mapOpts.annotation === "string") {
          from = dirname(resolve(from, this.mapOpts.annotation));
        }
        let path = relative(from, file);
        this.memoizedPaths.set(file, path);
        return path;
      }
      previous() {
        if (!this.previousMaps) {
          this.previousMaps = [];
          if (this.root) {
            this.root.walk((node22) => {
              if (node22.source && node22.source.input.map) {
                let map = node22.source.input.map;
                if (!this.previousMaps.includes(map)) {
                  this.previousMaps.push(map);
                }
              }
            });
          } else {
            let input22 = new Input(this.originalCSS, this.opts);
            if (input22.map) this.previousMaps.push(input22.map);
          }
        }
        return this.previousMaps;
      }
      setSourcesContent() {
        let already = {};
        if (this.root) {
          this.root.walk((node22) => {
            if (node22.source) {
              let from = node22.source.input.from;
              if (from && !already[from]) {
                already[from] = true;
                let fromUrl = this.usesFileUrls ? this.toFileUrl(from) : this.toUrl(this.path(from));
                this.map.setSourceContent(fromUrl, node22.source.input.css);
              }
            }
          });
        } else if (this.css) {
          let from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
          this.map.setSourceContent(from, this.css);
        }
      }
      sourcePath(node22) {
        if (this.mapOpts.from) {
          return this.toUrl(this.mapOpts.from);
        } else if (this.usesFileUrls) {
          return this.toFileUrl(node22.source.input.from);
        } else {
          return this.toUrl(this.path(node22.source.input.from));
        }
      }
      toBase64(str) {
        if (Buffer) {
          return Buffer.from(str).toString("base64");
        } else {
          return window.btoa(unescape(encodeURIComponent(str)));
        }
      }
      toFileUrl(path) {
        let cached = this.memoizedFileURLs.get(path);
        if (cached) return cached;
        if (pathToFileURL) {
          let fileURL = pathToFileURL(path).toString();
          this.memoizedFileURLs.set(path, fileURL);
          return fileURL;
        } else {
          throw new Error(
            "`map.absolute` option is not available in this PostCSS build"
          );
        }
      }
      toUrl(path) {
        let cached = this.memoizedURLs.get(path);
        if (cached) return cached;
        if (sep === "\\") {
          path = path.replace(/\\/g, "/");
        }
        let url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
        this.memoizedURLs.set(path, url);
        return url;
      }
    }
    mapGenerator$12 = MapGenerator;
    return mapGenerator$12;
  }
  function requireComment$12() {
    if (hasRequiredComment$12) return comment$12;
    hasRequiredComment$12 = 1;
    let Node2 = requireNode$12();
    class Comment extends Node2 {
      constructor(defaults) {
        super(defaults);
        this.type = "comment";
      }
    }
    comment$12 = Comment;
    Comment.default = Comment;
    return comment$12;
  }
  function requireContainer$12() {
    if (hasRequiredContainer$12) return container$12;
    hasRequiredContainer$12 = 1;
    let { isClean, my } = requireSymbols$12();
    let Declaration = requireDeclaration$12();
    let Comment = requireComment$12();
    let Node2 = requireNode$12();
    let parse, Rule, AtRule, Root;
    function cleanSource(nodes) {
      return nodes.map((i2) => {
        if (i2.nodes) i2.nodes = cleanSource(i2.nodes);
        delete i2.source;
        return i2;
      });
    }
    function markDirtyUp(node22) {
      node22[isClean] = false;
      if (node22.proxyOf.nodes) {
        for (let i2 of node22.proxyOf.nodes) {
          markDirtyUp(i2);
        }
      }
    }
    class Container extends Node2 {
      append(...children) {
        for (let child of children) {
          let nodes = this.normalize(child, this.last);
          for (let node22 of nodes) this.proxyOf.nodes.push(node22);
        }
        this.markDirty();
        return this;
      }
      cleanRaws(keepBetween) {
        super.cleanRaws(keepBetween);
        if (this.nodes) {
          for (let node22 of this.nodes) node22.cleanRaws(keepBetween);
        }
      }
      each(callback) {
        if (!this.proxyOf.nodes) return void 0;
        let iterator = this.getIterator();
        let index22, result22;
        while (this.indexes[iterator] < this.proxyOf.nodes.length) {
          index22 = this.indexes[iterator];
          result22 = callback(this.proxyOf.nodes[index22], index22);
          if (result22 === false) break;
          this.indexes[iterator] += 1;
        }
        delete this.indexes[iterator];
        return result22;
      }
      every(condition) {
        return this.nodes.every(condition);
      }
      getIterator() {
        if (!this.lastEach) this.lastEach = 0;
        if (!this.indexes) this.indexes = {};
        this.lastEach += 1;
        let iterator = this.lastEach;
        this.indexes[iterator] = 0;
        return iterator;
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (!node22[prop]) {
              return node22[prop];
            } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
              return (...args) => {
                return node22[prop](
                  ...args.map((i2) => {
                    if (typeof i2 === "function") {
                      return (child, index22) => i2(child.toProxy(), index22);
                    } else {
                      return i2;
                    }
                  })
                );
              };
            } else if (prop === "every" || prop === "some") {
              return (cb) => {
                return node22[prop](
                  (child, ...other) => cb(child.toProxy(), ...other)
                );
              };
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else if (prop === "nodes") {
              return node22.nodes.map((i2) => i2.toProxy());
            } else if (prop === "first" || prop === "last") {
              return node22[prop].toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "name" || prop === "params" || prop === "selector") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      index(child) {
        if (typeof child === "number") return child;
        if (child.proxyOf) child = child.proxyOf;
        return this.proxyOf.nodes.indexOf(child);
      }
      insertAfter(exist, add2) {
        let existIndex = this.index(exist);
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex]).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex + 1, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex < index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      insertBefore(exist, add2) {
        let existIndex = this.index(exist);
        let type = existIndex === 0 ? "prepend" : false;
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex], type).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex <= index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      normalize(nodes, sample) {
        if (typeof nodes === "string") {
          nodes = cleanSource(parse(nodes).nodes);
        } else if (typeof nodes === "undefined") {
          nodes = [];
        } else if (Array.isArray(nodes)) {
          nodes = nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type === "root" && this.type !== "document") {
          nodes = nodes.nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type) {
          nodes = [nodes];
        } else if (nodes.prop) {
          if (typeof nodes.value === "undefined") {
            throw new Error("Value field is missed in node creation");
          } else if (typeof nodes.value !== "string") {
            nodes.value = String(nodes.value);
          }
          nodes = [new Declaration(nodes)];
        } else if (nodes.selector) {
          nodes = [new Rule(nodes)];
        } else if (nodes.name) {
          nodes = [new AtRule(nodes)];
        } else if (nodes.text) {
          nodes = [new Comment(nodes)];
        } else {
          throw new Error("Unknown node type in node creation");
        }
        let processed = nodes.map((i2) => {
          if (!i2[my]) Container.rebuild(i2);
          i2 = i2.proxyOf;
          if (i2.parent) i2.parent.removeChild(i2);
          if (i2[isClean]) markDirtyUp(i2);
          if (typeof i2.raws.before === "undefined") {
            if (sample && typeof sample.raws.before !== "undefined") {
              i2.raws.before = sample.raws.before.replace(/\S/g, "");
            }
          }
          i2.parent = this.proxyOf;
          return i2;
        });
        return processed;
      }
      prepend(...children) {
        children = children.reverse();
        for (let child of children) {
          let nodes = this.normalize(child, this.first, "prepend").reverse();
          for (let node22 of nodes) this.proxyOf.nodes.unshift(node22);
          for (let id2 in this.indexes) {
            this.indexes[id2] = this.indexes[id2] + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      push(child) {
        child.parent = this;
        this.proxyOf.nodes.push(child);
        return this;
      }
      removeAll() {
        for (let node22 of this.proxyOf.nodes) node22.parent = void 0;
        this.proxyOf.nodes = [];
        this.markDirty();
        return this;
      }
      removeChild(child) {
        child = this.index(child);
        this.proxyOf.nodes[child].parent = void 0;
        this.proxyOf.nodes.splice(child, 1);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (index22 >= child) {
            this.indexes[id2] = index22 - 1;
          }
        }
        this.markDirty();
        return this;
      }
      replaceValues(pattern, opts, callback) {
        if (!callback) {
          callback = opts;
          opts = {};
        }
        this.walkDecls((decl) => {
          if (opts.props && !opts.props.includes(decl.prop)) return;
          if (opts.fast && !decl.value.includes(opts.fast)) return;
          decl.value = decl.value.replace(pattern, callback);
        });
        this.markDirty();
        return this;
      }
      some(condition) {
        return this.nodes.some(condition);
      }
      walk(callback) {
        return this.each((child, i2) => {
          let result22;
          try {
            result22 = callback(child, i2);
          } catch (e2) {
            throw child.addToError(e2);
          }
          if (result22 !== false && child.walk) {
            result22 = child.walk(callback);
          }
          return result22;
        });
      }
      walkAtRules(name, callback) {
        if (!callback) {
          callback = name;
          return this.walk((child, i2) => {
            if (child.type === "atrule") {
              return callback(child, i2);
            }
          });
        }
        if (name instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "atrule" && name.test(child.name)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "atrule" && child.name === name) {
            return callback(child, i2);
          }
        });
      }
      walkComments(callback) {
        return this.walk((child, i2) => {
          if (child.type === "comment") {
            return callback(child, i2);
          }
        });
      }
      walkDecls(prop, callback) {
        if (!callback) {
          callback = prop;
          return this.walk((child, i2) => {
            if (child.type === "decl") {
              return callback(child, i2);
            }
          });
        }
        if (prop instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "decl" && prop.test(child.prop)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "decl" && child.prop === prop) {
            return callback(child, i2);
          }
        });
      }
      walkRules(selector2, callback) {
        if (!callback) {
          callback = selector2;
          return this.walk((child, i2) => {
            if (child.type === "rule") {
              return callback(child, i2);
            }
          });
        }
        if (selector2 instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "rule" && selector2.test(child.selector)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "rule" && child.selector === selector2) {
            return callback(child, i2);
          }
        });
      }
      get first() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[0];
      }
      get last() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
      }
    }
    Container.registerParse = (dependant) => {
      parse = dependant;
    };
    Container.registerRule = (dependant) => {
      Rule = dependant;
    };
    Container.registerAtRule = (dependant) => {
      AtRule = dependant;
    };
    Container.registerRoot = (dependant) => {
      Root = dependant;
    };
    container$12 = Container;
    Container.default = Container;
    Container.rebuild = (node22) => {
      if (node22.type === "atrule") {
        Object.setPrototypeOf(node22, AtRule.prototype);
      } else if (node22.type === "rule") {
        Object.setPrototypeOf(node22, Rule.prototype);
      } else if (node22.type === "decl") {
        Object.setPrototypeOf(node22, Declaration.prototype);
      } else if (node22.type === "comment") {
        Object.setPrototypeOf(node22, Comment.prototype);
      } else if (node22.type === "root") {
        Object.setPrototypeOf(node22, Root.prototype);
      }
      node22[my] = true;
      if (node22.nodes) {
        node22.nodes.forEach((child) => {
          Container.rebuild(child);
        });
      }
    };
    return container$12;
  }
  function requireDocument$12() {
    if (hasRequiredDocument$12) return document$1$12;
    hasRequiredDocument$12 = 1;
    let Container = requireContainer$12();
    let LazyResult, Processor;
    class Document2 extends Container {
      constructor(defaults) {
        super({ type: "document", ...defaults });
        if (!this.nodes) {
          this.nodes = [];
        }
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Document2.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Document2.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    document$1$12 = Document2;
    Document2.default = Document2;
    return document$1$12;
  }
  function requireWarnOnce$12() {
    if (hasRequiredWarnOnce$12) return warnOnce$12;
    hasRequiredWarnOnce$12 = 1;
    let printed = {};
    warnOnce$12 = function warnOnce22(message) {
      if (printed[message]) return;
      printed[message] = true;
      if (typeof console !== "undefined" && console.warn) {
        console.warn(message);
      }
    };
    return warnOnce$12;
  }
  function requireWarning$12() {
    if (hasRequiredWarning$12) return warning$12;
    hasRequiredWarning$12 = 1;
    class Warning {
      constructor(text, opts = {}) {
        this.type = "warning";
        this.text = text;
        if (opts.node && opts.node.source) {
          let range = opts.node.rangeBy(opts);
          this.line = range.start.line;
          this.column = range.start.column;
          this.endLine = range.end.line;
          this.endColumn = range.end.column;
        }
        for (let opt in opts) this[opt] = opts[opt];
      }
      toString() {
        if (this.node) {
          return this.node.error(this.text, {
            index: this.index,
            plugin: this.plugin,
            word: this.word
          }).message;
        }
        if (this.plugin) {
          return this.plugin + ": " + this.text;
        }
        return this.text;
      }
    }
    warning$12 = Warning;
    Warning.default = Warning;
    return warning$12;
  }
  function requireResult$12() {
    if (hasRequiredResult$12) return result$12;
    hasRequiredResult$12 = 1;
    let Warning = requireWarning$12();
    class Result {
      constructor(processor22, root22, opts) {
        this.processor = processor22;
        this.messages = [];
        this.root = root22;
        this.opts = opts;
        this.css = void 0;
        this.map = void 0;
      }
      toString() {
        return this.css;
      }
      warn(text, opts = {}) {
        if (!opts.plugin) {
          if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
            opts.plugin = this.lastPlugin.postcssPlugin;
          }
        }
        let warning22 = new Warning(text, opts);
        this.messages.push(warning22);
        return warning22;
      }
      warnings() {
        return this.messages.filter((i2) => i2.type === "warning");
      }
      get content() {
        return this.css;
      }
    }
    result$12 = Result;
    Result.default = Result;
    return result$12;
  }
  function requireTokenize$12() {
    if (hasRequiredTokenize$12) return tokenize$12;
    hasRequiredTokenize$12 = 1;
    const SINGLE_QUOTE = "'".charCodeAt(0);
    const DOUBLE_QUOTE = '"'.charCodeAt(0);
    const BACKSLASH = "\\".charCodeAt(0);
    const SLASH = "/".charCodeAt(0);
    const NEWLINE = "\n".charCodeAt(0);
    const SPACE = " ".charCodeAt(0);
    const FEED = "\f".charCodeAt(0);
    const TAB = "	".charCodeAt(0);
    const CR = "\r".charCodeAt(0);
    const OPEN_SQUARE = "[".charCodeAt(0);
    const CLOSE_SQUARE = "]".charCodeAt(0);
    const OPEN_PARENTHESES = "(".charCodeAt(0);
    const CLOSE_PARENTHESES = ")".charCodeAt(0);
    const OPEN_CURLY = "{".charCodeAt(0);
    const CLOSE_CURLY = "}".charCodeAt(0);
    const SEMICOLON = ";".charCodeAt(0);
    const ASTERISK = "*".charCodeAt(0);
    const COLON = ":".charCodeAt(0);
    const AT = "@".charCodeAt(0);
    const RE_AT_END = /[\t\n\f\r "#'()/;[\\\]{}]/g;
    const RE_WORD_END = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
    const RE_BAD_BRACKET = /.[\r\n"'(/\\]/;
    const RE_HEX_ESCAPE = /[\da-f]/i;
    tokenize$12 = function tokenizer(input22, options = {}) {
      let css = input22.css.valueOf();
      let ignore = options.ignoreErrors;
      let code, next, quote, content, escape2;
      let escaped, escapePos, prev, n22, currentToken;
      let length = css.length;
      let pos = 0;
      let buffer = [];
      let returned = [];
      function position() {
        return pos;
      }
      function unclosed(what) {
        throw input22.error("Unclosed " + what, pos);
      }
      function endOfFile() {
        return returned.length === 0 && pos >= length;
      }
      function nextToken(opts) {
        if (returned.length) return returned.pop();
        if (pos >= length) return;
        let ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
        code = css.charCodeAt(pos);
        switch (code) {
          case NEWLINE:
          case SPACE:
          case TAB:
          case CR:
          case FEED: {
            next = pos;
            do {
              next += 1;
              code = css.charCodeAt(next);
            } while (code === SPACE || code === NEWLINE || code === TAB || code === CR || code === FEED);
            currentToken = ["space", css.slice(pos, next)];
            pos = next - 1;
            break;
          }
          case OPEN_SQUARE:
          case CLOSE_SQUARE:
          case OPEN_CURLY:
          case CLOSE_CURLY:
          case COLON:
          case SEMICOLON:
          case CLOSE_PARENTHESES: {
            let controlChar = String.fromCharCode(code);
            currentToken = [controlChar, controlChar, pos];
            break;
          }
          case OPEN_PARENTHESES: {
            prev = buffer.length ? buffer.pop()[1] : "";
            n22 = css.charCodeAt(pos + 1);
            if (prev === "url" && n22 !== SINGLE_QUOTE && n22 !== DOUBLE_QUOTE && n22 !== SPACE && n22 !== NEWLINE && n22 !== TAB && n22 !== FEED && n22 !== CR) {
              next = pos;
              do {
                escaped = false;
                next = css.indexOf(")", next + 1);
                if (next === -1) {
                  if (ignore || ignoreUnclosed) {
                    next = pos;
                    break;
                  } else {
                    unclosed("bracket");
                  }
                }
                escapePos = next;
                while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                  escapePos -= 1;
                  escaped = !escaped;
                }
              } while (escaped);
              currentToken = ["brackets", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              next = css.indexOf(")", pos + 1);
              content = css.slice(pos, next + 1);
              if (next === -1 || RE_BAD_BRACKET.test(content)) {
                currentToken = ["(", "(", pos];
              } else {
                currentToken = ["brackets", content, pos, next];
                pos = next;
              }
            }
            break;
          }
          case SINGLE_QUOTE:
          case DOUBLE_QUOTE: {
            quote = code === SINGLE_QUOTE ? "'" : '"';
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(quote, next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos + 1;
                  break;
                } else {
                  unclosed("string");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = ["string", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case AT: {
            RE_AT_END.lastIndex = pos + 1;
            RE_AT_END.test(css);
            if (RE_AT_END.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_AT_END.lastIndex - 2;
            }
            currentToken = ["at-word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case BACKSLASH: {
            next = pos;
            escape2 = true;
            while (css.charCodeAt(next + 1) === BACKSLASH) {
              next += 1;
              escape2 = !escape2;
            }
            code = css.charCodeAt(next + 1);
            if (escape2 && code !== SLASH && code !== SPACE && code !== NEWLINE && code !== TAB && code !== CR && code !== FEED) {
              next += 1;
              if (RE_HEX_ESCAPE.test(css.charAt(next))) {
                while (RE_HEX_ESCAPE.test(css.charAt(next + 1))) {
                  next += 1;
                }
                if (css.charCodeAt(next + 1) === SPACE) {
                  next += 1;
                }
              }
            }
            currentToken = ["word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          default: {
            if (code === SLASH && css.charCodeAt(pos + 1) === ASTERISK) {
              next = css.indexOf("*/", pos + 2) + 1;
              if (next === 0) {
                if (ignore || ignoreUnclosed) {
                  next = css.length;
                } else {
                  unclosed("comment");
                }
              }
              currentToken = ["comment", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              RE_WORD_END.lastIndex = pos + 1;
              RE_WORD_END.test(css);
              if (RE_WORD_END.lastIndex === 0) {
                next = css.length - 1;
              } else {
                next = RE_WORD_END.lastIndex - 2;
              }
              currentToken = ["word", css.slice(pos, next + 1), pos, next];
              buffer.push(currentToken);
              pos = next;
            }
            break;
          }
        }
        pos++;
        return currentToken;
      }
      function back(token) {
        returned.push(token);
      }
      return {
        back,
        endOfFile,
        nextToken,
        position
      };
    };
    return tokenize$12;
  }
  function requireAtRule$12() {
    if (hasRequiredAtRule$12) return atRule$12;
    hasRequiredAtRule$12 = 1;
    let Container = requireContainer$12();
    class AtRule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "atrule";
      }
      append(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.append(...children);
      }
      prepend(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.prepend(...children);
      }
    }
    atRule$12 = AtRule;
    AtRule.default = AtRule;
    Container.registerAtRule(AtRule);
    return atRule$12;
  }
  function requireRoot$12() {
    if (hasRequiredRoot$12) return root$12;
    hasRequiredRoot$12 = 1;
    let Container = requireContainer$12();
    let LazyResult, Processor;
    class Root extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "root";
        if (!this.nodes) this.nodes = [];
      }
      normalize(child, sample, type) {
        let nodes = super.normalize(child);
        if (sample) {
          if (type === "prepend") {
            if (this.nodes.length > 1) {
              sample.raws.before = this.nodes[1].raws.before;
            } else {
              delete sample.raws.before;
            }
          } else if (this.first !== sample) {
            for (let node22 of nodes) {
              node22.raws.before = sample.raws.before;
            }
          }
        }
        return nodes;
      }
      removeChild(child, ignore) {
        let index22 = this.index(child);
        if (!ignore && index22 === 0 && this.nodes.length > 1) {
          this.nodes[1].raws.before = this.nodes[index22].raws.before;
        }
        return super.removeChild(child);
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Root.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Root.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    root$12 = Root;
    Root.default = Root;
    Container.registerRoot(Root);
    return root$12;
  }
  function requireList$12() {
    if (hasRequiredList$12) return list_1$12;
    hasRequiredList$12 = 1;
    let list = {
      comma(string) {
        return list.split(string, [","], true);
      },
      space(string) {
        let spaces = [" ", "\n", "	"];
        return list.split(string, spaces);
      },
      split(string, separators, last) {
        let array = [];
        let current = "";
        let split = false;
        let func = 0;
        let inQuote = false;
        let prevQuote = "";
        let escape2 = false;
        for (let letter of string) {
          if (escape2) {
            escape2 = false;
          } else if (letter === "\\") {
            escape2 = true;
          } else if (inQuote) {
            if (letter === prevQuote) {
              inQuote = false;
            }
          } else if (letter === '"' || letter === "'") {
            inQuote = true;
            prevQuote = letter;
          } else if (letter === "(") {
            func += 1;
          } else if (letter === ")") {
            if (func > 0) func -= 1;
          } else if (func === 0) {
            if (separators.includes(letter)) split = true;
          }
          if (split) {
            if (current !== "") array.push(current.trim());
            current = "";
            split = false;
          } else {
            current += letter;
          }
        }
        if (last || current !== "") array.push(current.trim());
        return array;
      }
    };
    list_1$12 = list;
    list.default = list;
    return list_1$12;
  }
  function requireRule$12() {
    if (hasRequiredRule$12) return rule$12;
    hasRequiredRule$12 = 1;
    let Container = requireContainer$12();
    let list = requireList$12();
    class Rule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "rule";
        if (!this.nodes) this.nodes = [];
      }
      get selectors() {
        return list.comma(this.selector);
      }
      set selectors(values) {
        let match = this.selector ? this.selector.match(/,\s*/) : null;
        let sep = match ? match[0] : "," + this.raw("between", "beforeOpen");
        this.selector = values.join(sep);
      }
    }
    rule$12 = Rule;
    Rule.default = Rule;
    Container.registerRule(Rule);
    return rule$12;
  }
  function requireParser$12() {
    if (hasRequiredParser$12) return parser$12;
    hasRequiredParser$12 = 1;
    let Declaration = requireDeclaration$12();
    let tokenizer = requireTokenize$12();
    let Comment = requireComment$12();
    let AtRule = requireAtRule$12();
    let Root = requireRoot$12();
    let Rule = requireRule$12();
    const SAFE_COMMENT_NEIGHBOR = {
      empty: true,
      space: true
    };
    function findLastWithPosition(tokens) {
      for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
        let token = tokens[i2];
        let pos = token[3] || token[2];
        if (pos) return pos;
      }
    }
    class Parser {
      constructor(input22) {
        this.input = input22;
        this.root = new Root();
        this.current = this.root;
        this.spaces = "";
        this.semicolon = false;
        this.createTokenizer();
        this.root.source = { input: input22, start: { column: 1, line: 1, offset: 0 } };
      }
      atrule(token) {
        let node22 = new AtRule();
        node22.name = token[1].slice(1);
        if (node22.name === "") {
          this.unnamedAtrule(node22, token);
        }
        this.init(node22, token[2]);
        let type;
        let prev;
        let shift;
        let last = false;
        let open = false;
        let params = [];
        let brackets = [];
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          type = token[0];
          if (type === "(" || type === "[") {
            brackets.push(type === "(" ? ")" : "]");
          } else if (type === "{" && brackets.length > 0) {
            brackets.push("}");
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
          }
          if (brackets.length === 0) {
            if (type === ";") {
              node22.source.end = this.getPosition(token[2]);
              node22.source.end.offset++;
              this.semicolon = true;
              break;
            } else if (type === "{") {
              open = true;
              break;
            } else if (type === "}") {
              if (params.length > 0) {
                shift = params.length - 1;
                prev = params[shift];
                while (prev && prev[0] === "space") {
                  prev = params[--shift];
                }
                if (prev) {
                  node22.source.end = this.getPosition(prev[3] || prev[2]);
                  node22.source.end.offset++;
                }
              }
              this.end(token);
              break;
            } else {
              params.push(token);
            }
          } else {
            params.push(token);
          }
          if (this.tokenizer.endOfFile()) {
            last = true;
            break;
          }
        }
        node22.raws.between = this.spacesAndCommentsFromEnd(params);
        if (params.length) {
          node22.raws.afterName = this.spacesAndCommentsFromStart(params);
          this.raw(node22, "params", params);
          if (last) {
            token = params[params.length - 1];
            node22.source.end = this.getPosition(token[3] || token[2]);
            node22.source.end.offset++;
            this.spaces = node22.raws.between;
            node22.raws.between = "";
          }
        } else {
          node22.raws.afterName = "";
          node22.params = "";
        }
        if (open) {
          node22.nodes = [];
          this.current = node22;
        }
      }
      checkMissedSemicolon(tokens) {
        let colon = this.colon(tokens);
        if (colon === false) return;
        let founded = 0;
        let token;
        for (let j = colon - 1; j >= 0; j--) {
          token = tokens[j];
          if (token[0] !== "space") {
            founded += 1;
            if (founded === 2) break;
          }
        }
        throw this.input.error(
          "Missed semicolon",
          token[0] === "word" ? token[3] + 1 : token[2]
        );
      }
      colon(tokens) {
        let brackets = 0;
        let token, type, prev;
        for (let [i2, element] of tokens.entries()) {
          token = element;
          type = token[0];
          if (type === "(") {
            brackets += 1;
          }
          if (type === ")") {
            brackets -= 1;
          }
          if (brackets === 0 && type === ":") {
            if (!prev) {
              this.doubleColon(token);
            } else if (prev[0] === "word" && prev[1] === "progid") {
              continue;
            } else {
              return i2;
            }
          }
          prev = token;
        }
        return false;
      }
      comment(token) {
        let node22 = new Comment();
        this.init(node22, token[2]);
        node22.source.end = this.getPosition(token[3] || token[2]);
        node22.source.end.offset++;
        let text = token[1].slice(2, -2);
        if (/^\s*$/.test(text)) {
          node22.text = "";
          node22.raws.left = text;
          node22.raws.right = "";
        } else {
          let match = text.match(/^(\s*)([^]*\S)(\s*)$/);
          node22.text = match[2];
          node22.raws.left = match[1];
          node22.raws.right = match[3];
        }
      }
      createTokenizer() {
        this.tokenizer = tokenizer(this.input);
      }
      decl(tokens, customProperty) {
        let node22 = new Declaration();
        this.init(node22, tokens[0][2]);
        let last = tokens[tokens.length - 1];
        if (last[0] === ";") {
          this.semicolon = true;
          tokens.pop();
        }
        node22.source.end = this.getPosition(
          last[3] || last[2] || findLastWithPosition(tokens)
        );
        node22.source.end.offset++;
        while (tokens[0][0] !== "word") {
          if (tokens.length === 1) this.unknownWord(tokens);
          node22.raws.before += tokens.shift()[1];
        }
        node22.source.start = this.getPosition(tokens[0][2]);
        node22.prop = "";
        while (tokens.length) {
          let type = tokens[0][0];
          if (type === ":" || type === "space" || type === "comment") {
            break;
          }
          node22.prop += tokens.shift()[1];
        }
        node22.raws.between = "";
        let token;
        while (tokens.length) {
          token = tokens.shift();
          if (token[0] === ":") {
            node22.raws.between += token[1];
            break;
          } else {
            if (token[0] === "word" && /\w/.test(token[1])) {
              this.unknownWord([token]);
            }
            node22.raws.between += token[1];
          }
        }
        if (node22.prop[0] === "_" || node22.prop[0] === "*") {
          node22.raws.before += node22.prop[0];
          node22.prop = node22.prop.slice(1);
        }
        let firstSpaces = [];
        let next;
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          firstSpaces.push(tokens.shift());
        }
        this.precheckMissedSemicolon(tokens);
        for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
          token = tokens[i2];
          if (token[1].toLowerCase() === "!important") {
            node22.important = true;
            let string = this.stringFrom(tokens, i2);
            string = this.spacesFromEnd(tokens) + string;
            if (string !== " !important") node22.raws.important = string;
            break;
          } else if (token[1].toLowerCase() === "important") {
            let cache = tokens.slice(0);
            let str = "";
            for (let j = i2; j > 0; j--) {
              let type = cache[j][0];
              if (str.trim().indexOf("!") === 0 && type !== "space") {
                break;
              }
              str = cache.pop()[1] + str;
            }
            if (str.trim().indexOf("!") === 0) {
              node22.important = true;
              node22.raws.important = str;
              tokens = cache;
            }
          }
          if (token[0] !== "space" && token[0] !== "comment") {
            break;
          }
        }
        let hasWord = tokens.some((i2) => i2[0] !== "space" && i2[0] !== "comment");
        if (hasWord) {
          node22.raws.between += firstSpaces.map((i2) => i2[1]).join("");
          firstSpaces = [];
        }
        this.raw(node22, "value", firstSpaces.concat(tokens), customProperty);
        if (node22.value.includes(":") && !customProperty) {
          this.checkMissedSemicolon(tokens);
        }
      }
      doubleColon(token) {
        throw this.input.error(
          "Double colon",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
      emptyRule(token) {
        let node22 = new Rule();
        this.init(node22, token[2]);
        node22.selector = "";
        node22.raws.between = "";
        this.current = node22;
      }
      end(token) {
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.semicolon = false;
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.spaces = "";
        if (this.current.parent) {
          this.current.source.end = this.getPosition(token[2]);
          this.current.source.end.offset++;
          this.current = this.current.parent;
        } else {
          this.unexpectedClose(token);
        }
      }
      endFile() {
        if (this.current.parent) this.unclosedBlock();
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.root.source.end = this.getPosition(this.tokenizer.position());
      }
      freeSemicolon(token) {
        this.spaces += token[1];
        if (this.current.nodes) {
          let prev = this.current.nodes[this.current.nodes.length - 1];
          if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
            prev.raws.ownSemicolon = this.spaces;
            this.spaces = "";
          }
        }
      }
      // Helpers
      getPosition(offset) {
        let pos = this.input.fromOffset(offset);
        return {
          column: pos.col,
          line: pos.line,
          offset
        };
      }
      init(node22, offset) {
        this.current.push(node22);
        node22.source = {
          input: this.input,
          start: this.getPosition(offset)
        };
        node22.raws.before = this.spaces;
        this.spaces = "";
        if (node22.type !== "comment") this.semicolon = false;
      }
      other(start3) {
        let end = false;
        let type = null;
        let colon = false;
        let bracket = null;
        let brackets = [];
        let customProperty = start3[1].startsWith("--");
        let tokens = [];
        let token = start3;
        while (token) {
          type = token[0];
          tokens.push(token);
          if (type === "(" || type === "[") {
            if (!bracket) bracket = token;
            brackets.push(type === "(" ? ")" : "]");
          } else if (customProperty && colon && type === "{") {
            if (!bracket) bracket = token;
            brackets.push("}");
          } else if (brackets.length === 0) {
            if (type === ";") {
              if (colon) {
                this.decl(tokens, customProperty);
                return;
              } else {
                break;
              }
            } else if (type === "{") {
              this.rule(tokens);
              return;
            } else if (type === "}") {
              this.tokenizer.back(tokens.pop());
              end = true;
              break;
            } else if (type === ":") {
              colon = true;
            }
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
            if (brackets.length === 0) bracket = null;
          }
          token = this.tokenizer.nextToken();
        }
        if (this.tokenizer.endOfFile()) end = true;
        if (brackets.length > 0) this.unclosedBracket(bracket);
        if (end && colon) {
          if (!customProperty) {
            while (tokens.length) {
              token = tokens[tokens.length - 1][0];
              if (token !== "space" && token !== "comment") break;
              this.tokenizer.back(tokens.pop());
            }
          }
          this.decl(tokens, customProperty);
        } else {
          this.unknownWord(tokens);
        }
      }
      parse() {
        let token;
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          switch (token[0]) {
            case "space":
              this.spaces += token[1];
              break;
            case ";":
              this.freeSemicolon(token);
              break;
            case "}":
              this.end(token);
              break;
            case "comment":
              this.comment(token);
              break;
            case "at-word":
              this.atrule(token);
              break;
            case "{":
              this.emptyRule(token);
              break;
            default:
              this.other(token);
              break;
          }
        }
        this.endFile();
      }
      precheckMissedSemicolon() {
      }
      raw(node22, prop, tokens, customProperty) {
        let token, type;
        let length = tokens.length;
        let value = "";
        let clean = true;
        let next, prev;
        for (let i2 = 0; i2 < length; i2 += 1) {
          token = tokens[i2];
          type = token[0];
          if (type === "space" && i2 === length - 1 && !customProperty) {
            clean = false;
          } else if (type === "comment") {
            prev = tokens[i2 - 1] ? tokens[i2 - 1][0] : "empty";
            next = tokens[i2 + 1] ? tokens[i2 + 1][0] : "empty";
            if (!SAFE_COMMENT_NEIGHBOR[prev] && !SAFE_COMMENT_NEIGHBOR[next]) {
              if (value.slice(-1) === ",") {
                clean = false;
              } else {
                value += token[1];
              }
            } else {
              clean = false;
            }
          } else {
            value += token[1];
          }
        }
        if (!clean) {
          let raw = tokens.reduce((all, i2) => all + i2[1], "");
          node22.raws[prop] = { raw, value };
        }
        node22[prop] = value;
      }
      rule(tokens) {
        tokens.pop();
        let node22 = new Rule();
        this.init(node22, tokens[0][2]);
        node22.raws.between = this.spacesAndCommentsFromEnd(tokens);
        this.raw(node22, "selector", tokens);
        this.current = node22;
      }
      spacesAndCommentsFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space" && lastTokenType !== "comment") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      // Errors
      spacesAndCommentsFromStart(tokens) {
        let next;
        let spaces = "";
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          spaces += tokens.shift()[1];
        }
        return spaces;
      }
      spacesFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      stringFrom(tokens, from) {
        let result22 = "";
        for (let i2 = from; i2 < tokens.length; i2++) {
          result22 += tokens[i2][1];
        }
        tokens.splice(from, tokens.length - from);
        return result22;
      }
      unclosedBlock() {
        let pos = this.current.source.start;
        throw this.input.error("Unclosed block", pos.line, pos.column);
      }
      unclosedBracket(bracket) {
        throw this.input.error(
          "Unclosed bracket",
          { offset: bracket[2] },
          { offset: bracket[2] + 1 }
        );
      }
      unexpectedClose(token) {
        throw this.input.error(
          "Unexpected }",
          { offset: token[2] },
          { offset: token[2] + 1 }
        );
      }
      unknownWord(tokens) {
        throw this.input.error(
          "Unknown word",
          { offset: tokens[0][2] },
          { offset: tokens[0][2] + tokens[0][1].length }
        );
      }
      unnamedAtrule(node22, token) {
        throw this.input.error(
          "At-rule without name",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
    }
    parser$12 = Parser;
    return parser$12;
  }
  function requireParse$12() {
    if (hasRequiredParse$12) return parse_1$12;
    hasRequiredParse$12 = 1;
    let Container = requireContainer$12();
    let Parser = requireParser$12();
    let Input = requireInput$12();
    function parse(css, opts) {
      let input22 = new Input(css, opts);
      let parser22 = new Parser(input22);
      try {
        parser22.parse();
      } catch (e2) {
        if (true) {
          if (e2.name === "CssSyntaxError" && opts && opts.from) {
            if (/\.scss$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
            } else if (/\.sass/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
            } else if (/\.less$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
            }
          }
        }
        throw e2;
      }
      return parser22.root;
    }
    parse_1$12 = parse;
    parse.default = parse;
    Container.registerParse(parse);
    return parse_1$12;
  }
  function requireLazyResult$12() {
    if (hasRequiredLazyResult$12) return lazyResult$12;
    hasRequiredLazyResult$12 = 1;
    let { isClean, my } = requireSymbols$12();
    let MapGenerator = requireMapGenerator$12();
    let stringify2 = requireStringify$12();
    let Container = requireContainer$12();
    let Document2 = requireDocument$12();
    let warnOnce22 = requireWarnOnce$12();
    let Result = requireResult$12();
    let parse = requireParse$12();
    let Root = requireRoot$12();
    const TYPE_TO_CLASS_NAME = {
      atrule: "AtRule",
      comment: "Comment",
      decl: "Declaration",
      document: "Document",
      root: "Root",
      rule: "Rule"
    };
    const PLUGIN_PROPS = {
      AtRule: true,
      AtRuleExit: true,
      Comment: true,
      CommentExit: true,
      Declaration: true,
      DeclarationExit: true,
      Document: true,
      DocumentExit: true,
      Once: true,
      OnceExit: true,
      postcssPlugin: true,
      prepare: true,
      Root: true,
      RootExit: true,
      Rule: true,
      RuleExit: true
    };
    const NOT_VISITORS = {
      Once: true,
      postcssPlugin: true,
      prepare: true
    };
    const CHILDREN = 0;
    function isPromise(obj) {
      return typeof obj === "object" && typeof obj.then === "function";
    }
    function getEvents(node22) {
      let key = false;
      let type = TYPE_TO_CLASS_NAME[node22.type];
      if (node22.type === "decl") {
        key = node22.prop.toLowerCase();
      } else if (node22.type === "atrule") {
        key = node22.name.toLowerCase();
      }
      if (key && node22.append) {
        return [
          type,
          type + "-" + key,
          CHILDREN,
          type + "Exit",
          type + "Exit-" + key
        ];
      } else if (key) {
        return [type, type + "-" + key, type + "Exit", type + "Exit-" + key];
      } else if (node22.append) {
        return [type, CHILDREN, type + "Exit"];
      } else {
        return [type, type + "Exit"];
      }
    }
    function toStack(node22) {
      let events;
      if (node22.type === "document") {
        events = ["Document", CHILDREN, "DocumentExit"];
      } else if (node22.type === "root") {
        events = ["Root", CHILDREN, "RootExit"];
      } else {
        events = getEvents(node22);
      }
      return {
        eventIndex: 0,
        events,
        iterator: 0,
        node: node22,
        visitorIndex: 0,
        visitors: []
      };
    }
    function cleanMarks(node22) {
      node22[isClean] = false;
      if (node22.nodes) node22.nodes.forEach((i2) => cleanMarks(i2));
      return node22;
    }
    let postcss22 = {};
    class LazyResult {
      constructor(processor22, css, opts) {
        this.stringified = false;
        this.processed = false;
        let root22;
        if (typeof css === "object" && css !== null && (css.type === "root" || css.type === "document")) {
          root22 = cleanMarks(css);
        } else if (css instanceof LazyResult || css instanceof Result) {
          root22 = cleanMarks(css.root);
          if (css.map) {
            if (typeof opts.map === "undefined") opts.map = {};
            if (!opts.map.inline) opts.map.inline = false;
            opts.map.prev = css.map;
          }
        } else {
          let parser22 = parse;
          if (opts.syntax) parser22 = opts.syntax.parse;
          if (opts.parser) parser22 = opts.parser;
          if (parser22.parse) parser22 = parser22.parse;
          try {
            root22 = parser22(css, opts);
          } catch (error) {
            this.processed = true;
            this.error = error;
          }
          if (root22 && !root22[my]) {
            Container.rebuild(root22);
          }
        }
        this.result = new Result(processor22, root22, opts);
        this.helpers = { ...postcss22, postcss: postcss22, result: this.result };
        this.plugins = this.processor.plugins.map((plugin) => {
          if (typeof plugin === "object" && plugin.prepare) {
            return { ...plugin, ...plugin.prepare(this.result) };
          } else {
            return plugin;
          }
        });
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        if (this.processed) return Promise.resolve(this.result);
        if (!this.processing) {
          this.processing = this.runAsync();
        }
        return this.processing;
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      getAsyncError() {
        throw new Error("Use process(css).then(cb) to work with async plugins");
      }
      handleError(error, node22) {
        let plugin = this.result.lastPlugin;
        try {
          if (node22) node22.addToError(error);
          this.error = error;
          if (error.name === "CssSyntaxError" && !error.plugin) {
            error.plugin = plugin.postcssPlugin;
            error.setMessage();
          } else if (plugin.postcssVersion) {
            if (true) {
              let pluginName = plugin.postcssPlugin;
              let pluginVer = plugin.postcssVersion;
              let runtimeVer = this.result.processor.version;
              let a2 = pluginVer.split(".");
              let b2 = runtimeVer.split(".");
              if (a2[0] !== b2[0] || parseInt(a2[1]) > parseInt(b2[1])) {
                console.error(
                  "Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below."
                );
              }
            }
          }
        } catch (err) {
          if (console && console.error) console.error(err);
        }
        return error;
      }
      prepareVisitors() {
        this.listeners = {};
        let add2 = (plugin, type, cb) => {
          if (!this.listeners[type]) this.listeners[type] = [];
          this.listeners[type].push([plugin, cb]);
        };
        for (let plugin of this.plugins) {
          if (typeof plugin === "object") {
            for (let event in plugin) {
              if (!PLUGIN_PROPS[event] && /^[A-Z]/.test(event)) {
                throw new Error(
                  `Unknown event ${event} in ${plugin.postcssPlugin}. Try to update PostCSS (${this.processor.version} now).`
                );
              }
              if (!NOT_VISITORS[event]) {
                if (typeof plugin[event] === "object") {
                  for (let filter in plugin[event]) {
                    if (filter === "*") {
                      add2(plugin, event, plugin[event][filter]);
                    } else {
                      add2(
                        plugin,
                        event + "-" + filter.toLowerCase(),
                        plugin[event][filter]
                      );
                    }
                  }
                } else if (typeof plugin[event] === "function") {
                  add2(plugin, event, plugin[event]);
                }
              }
            }
          }
        }
        this.hasListener = Object.keys(this.listeners).length > 0;
      }
      async runAsync() {
        this.plugin = 0;
        for (let i2 = 0; i2 < this.plugins.length; i2++) {
          let plugin = this.plugins[i2];
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            try {
              await promise;
            } catch (error) {
              throw this.handleError(error);
            }
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            let stack = [toStack(root22)];
            while (stack.length > 0) {
              let promise = this.visitTick(stack);
              if (isPromise(promise)) {
                try {
                  await promise;
                } catch (e2) {
                  let node22 = stack[stack.length - 1].node;
                  throw this.handleError(e2, node22);
                }
              }
            }
          }
          if (this.listeners.OnceExit) {
            for (let [plugin, visitor] of this.listeners.OnceExit) {
              this.result.lastPlugin = plugin;
              try {
                if (root22.type === "document") {
                  let roots = root22.nodes.map(
                    (subRoot) => visitor(subRoot, this.helpers)
                  );
                  await Promise.all(roots);
                } else {
                  await visitor(root22, this.helpers);
                }
              } catch (e2) {
                throw this.handleError(e2);
              }
            }
          }
        }
        this.processed = true;
        return this.stringify();
      }
      runOnRoot(plugin) {
        this.result.lastPlugin = plugin;
        try {
          if (typeof plugin === "object" && plugin.Once) {
            if (this.result.root.type === "document") {
              let roots = this.result.root.nodes.map(
                (root22) => plugin.Once(root22, this.helpers)
              );
              if (isPromise(roots[0])) {
                return Promise.all(roots);
              }
              return roots;
            }
            return plugin.Once(this.result.root, this.helpers);
          } else if (typeof plugin === "function") {
            return plugin(this.result.root, this.result);
          }
        } catch (error) {
          throw this.handleError(error);
        }
      }
      stringify() {
        if (this.error) throw this.error;
        if (this.stringified) return this.result;
        this.stringified = true;
        this.sync();
        let opts = this.result.opts;
        let str = stringify2;
        if (opts.syntax) str = opts.syntax.stringify;
        if (opts.stringifier) str = opts.stringifier;
        if (str.stringify) str = str.stringify;
        let map = new MapGenerator(str, this.result.root, this.result.opts);
        let data = map.generate();
        this.result.css = data[0];
        this.result.map = data[1];
        return this.result;
      }
      sync() {
        if (this.error) throw this.error;
        if (this.processed) return this.result;
        this.processed = true;
        if (this.processing) {
          throw this.getAsyncError();
        }
        for (let plugin of this.plugins) {
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            this.walkSync(root22);
          }
          if (this.listeners.OnceExit) {
            if (root22.type === "document") {
              for (let subRoot of root22.nodes) {
                this.visitSync(this.listeners.OnceExit, subRoot);
              }
            } else {
              this.visitSync(this.listeners.OnceExit, root22);
            }
          }
        }
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this.opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this.css;
      }
      visitSync(visitors, node22) {
        for (let [plugin, visitor] of visitors) {
          this.result.lastPlugin = plugin;
          let promise;
          try {
            promise = visitor(node22, this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22.proxyOf);
          }
          if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
            return true;
          }
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
      }
      visitTick(stack) {
        let visit2 = stack[stack.length - 1];
        let { node: node22, visitors } = visit2;
        if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
          stack.pop();
          return;
        }
        if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
          let [plugin, visitor] = visitors[visit2.visitorIndex];
          visit2.visitorIndex += 1;
          if (visit2.visitorIndex === visitors.length) {
            visit2.visitors = [];
            visit2.visitorIndex = 0;
          }
          this.result.lastPlugin = plugin;
          try {
            return visitor(node22.toProxy(), this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22);
          }
        }
        if (visit2.iterator !== 0) {
          let iterator = visit2.iterator;
          let child;
          while (child = node22.nodes[node22.indexes[iterator]]) {
            node22.indexes[iterator] += 1;
            if (!child[isClean]) {
              child[isClean] = true;
              stack.push(toStack(child));
              return;
            }
          }
          visit2.iterator = 0;
          delete node22.indexes[iterator];
        }
        let events = visit2.events;
        while (visit2.eventIndex < events.length) {
          let event = events[visit2.eventIndex];
          visit2.eventIndex += 1;
          if (event === CHILDREN) {
            if (node22.nodes && node22.nodes.length) {
              node22[isClean] = true;
              visit2.iterator = node22.getIterator();
            }
            return;
          } else if (this.listeners[event]) {
            visit2.visitors = this.listeners[event];
            return;
          }
        }
        stack.pop();
      }
      walkSync(node22) {
        node22[isClean] = true;
        let events = getEvents(node22);
        for (let event of events) {
          if (event === CHILDREN) {
            if (node22.nodes) {
              node22.each((child) => {
                if (!child[isClean]) this.walkSync(child);
              });
            }
          } else {
            let visitors = this.listeners[event];
            if (visitors) {
              if (this.visitSync(visitors, node22.toProxy())) return;
            }
          }
        }
      }
      warnings() {
        return this.sync().warnings();
      }
      get content() {
        return this.stringify().content;
      }
      get css() {
        return this.stringify().css;
      }
      get map() {
        return this.stringify().map;
      }
      get messages() {
        return this.sync().messages;
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        return this.sync().root;
      }
      get [Symbol.toStringTag]() {
        return "LazyResult";
      }
    }
    LazyResult.registerPostcss = (dependant) => {
      postcss22 = dependant;
    };
    lazyResult$12 = LazyResult;
    LazyResult.default = LazyResult;
    Root.registerLazyResult(LazyResult);
    Document2.registerLazyResult(LazyResult);
    return lazyResult$12;
  }
  function requireNoWorkResult$12() {
    if (hasRequiredNoWorkResult$12) return noWorkResult$12;
    hasRequiredNoWorkResult$12 = 1;
    let MapGenerator = requireMapGenerator$12();
    let stringify2 = requireStringify$12();
    let warnOnce22 = requireWarnOnce$12();
    let parse = requireParse$12();
    const Result = requireResult$12();
    class NoWorkResult {
      constructor(processor22, css, opts) {
        css = css.toString();
        this.stringified = false;
        this._processor = processor22;
        this._css = css;
        this._opts = opts;
        this._map = void 0;
        let root22;
        let str = stringify2;
        this.result = new Result(this._processor, root22, this._opts);
        this.result.css = css;
        let self2 = this;
        Object.defineProperty(this.result, "root", {
          get() {
            return self2.root;
          }
        });
        let map = new MapGenerator(str, root22, this._opts, css);
        if (map.isMap()) {
          let [generatedCSS, generatedMap] = map.generate();
          if (generatedCSS) {
            this.result.css = generatedCSS;
          }
          if (generatedMap) {
            this.result.map = generatedMap;
          }
        } else {
          map.clearAnnotation();
          this.result.css = map.css;
        }
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        return Promise.resolve(this.result);
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      sync() {
        if (this.error) throw this.error;
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this._opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this._css;
      }
      warnings() {
        return [];
      }
      get content() {
        return this.result.css;
      }
      get css() {
        return this.result.css;
      }
      get map() {
        return this.result.map;
      }
      get messages() {
        return [];
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        if (this._root) {
          return this._root;
        }
        let root22;
        let parser22 = parse;
        try {
          root22 = parser22(this._css, this._opts);
        } catch (error) {
          this.error = error;
        }
        if (this.error) {
          throw this.error;
        } else {
          this._root = root22;
          return root22;
        }
      }
      get [Symbol.toStringTag]() {
        return "NoWorkResult";
      }
    }
    noWorkResult$12 = NoWorkResult;
    NoWorkResult.default = NoWorkResult;
    return noWorkResult$12;
  }
  function requireProcessor$12() {
    if (hasRequiredProcessor$12) return processor$12;
    hasRequiredProcessor$12 = 1;
    let NoWorkResult = requireNoWorkResult$12();
    let LazyResult = requireLazyResult$12();
    let Document2 = requireDocument$12();
    let Root = requireRoot$12();
    class Processor {
      constructor(plugins = []) {
        this.version = "8.4.38";
        this.plugins = this.normalize(plugins);
      }
      normalize(plugins) {
        let normalized = [];
        for (let i2 of plugins) {
          if (i2.postcss === true) {
            i2 = i2();
          } else if (i2.postcss) {
            i2 = i2.postcss;
          }
          if (typeof i2 === "object" && Array.isArray(i2.plugins)) {
            normalized = normalized.concat(i2.plugins);
          } else if (typeof i2 === "object" && i2.postcssPlugin) {
            normalized.push(i2);
          } else if (typeof i2 === "function") {
            normalized.push(i2);
          } else if (typeof i2 === "object" && (i2.parse || i2.stringify)) {
            if (true) {
              throw new Error(
                "PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation."
              );
            }
          } else {
            throw new Error(i2 + " is not a PostCSS plugin");
          }
        }
        return normalized;
      }
      process(css, opts = {}) {
        if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
          return new NoWorkResult(this, css, opts);
        } else {
          return new LazyResult(this, css, opts);
        }
      }
      use(plugin) {
        this.plugins = this.plugins.concat(this.normalize([plugin]));
        return this;
      }
    }
    processor$12 = Processor;
    Processor.default = Processor;
    Root.registerProcessor(Processor);
    Document2.registerProcessor(Processor);
    return processor$12;
  }
  function requireFromJSON$12() {
    if (hasRequiredFromJSON$12) return fromJSON_1$12;
    hasRequiredFromJSON$12 = 1;
    let Declaration = requireDeclaration$12();
    let PreviousMap = requirePreviousMap$12();
    let Comment = requireComment$12();
    let AtRule = requireAtRule$12();
    let Input = requireInput$12();
    let Root = requireRoot$12();
    let Rule = requireRule$12();
    function fromJSON(json, inputs) {
      if (Array.isArray(json)) return json.map((n22) => fromJSON(n22));
      let { inputs: ownInputs, ...defaults } = json;
      if (ownInputs) {
        inputs = [];
        for (let input22 of ownInputs) {
          let inputHydrated = { ...input22, __proto__: Input.prototype };
          if (inputHydrated.map) {
            inputHydrated.map = {
              ...inputHydrated.map,
              __proto__: PreviousMap.prototype
            };
          }
          inputs.push(inputHydrated);
        }
      }
      if (defaults.nodes) {
        defaults.nodes = json.nodes.map((n22) => fromJSON(n22, inputs));
      }
      if (defaults.source) {
        let { inputId, ...source } = defaults.source;
        defaults.source = source;
        if (inputId != null) {
          defaults.source.input = inputs[inputId];
        }
      }
      if (defaults.type === "root") {
        return new Root(defaults);
      } else if (defaults.type === "decl") {
        return new Declaration(defaults);
      } else if (defaults.type === "rule") {
        return new Rule(defaults);
      } else if (defaults.type === "comment") {
        return new Comment(defaults);
      } else if (defaults.type === "atrule") {
        return new AtRule(defaults);
      } else {
        throw new Error("Unknown node type: " + json.type);
      }
    }
    fromJSON_1$12 = fromJSON;
    fromJSON.default = fromJSON;
    return fromJSON_1$12;
  }
  function requirePostcss$12() {
    if (hasRequiredPostcss$12) return postcss_1$12;
    hasRequiredPostcss$12 = 1;
    let CssSyntaxError = requireCssSyntaxError$12();
    let Declaration = requireDeclaration$12();
    let LazyResult = requireLazyResult$12();
    let Container = requireContainer$12();
    let Processor = requireProcessor$12();
    let stringify2 = requireStringify$12();
    let fromJSON = requireFromJSON$12();
    let Document2 = requireDocument$12();
    let Warning = requireWarning$12();
    let Comment = requireComment$12();
    let AtRule = requireAtRule$12();
    let Result = requireResult$12();
    let Input = requireInput$12();
    let parse = requireParse$12();
    let list = requireList$12();
    let Rule = requireRule$12();
    let Root = requireRoot$12();
    let Node2 = requireNode$12();
    function postcss22(...plugins) {
      if (plugins.length === 1 && Array.isArray(plugins[0])) {
        plugins = plugins[0];
      }
      return new Processor(plugins);
    }
    postcss22.plugin = function plugin(name, initializer) {
      let warningPrinted = false;
      function creator(...args) {
        if (console && console.warn && !warningPrinted) {
          warningPrinted = true;
          console.warn(
            name + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration"
          );
          if (process.env.LANG && process.env.LANG.startsWith("cn")) {
            console.warn(
              name + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226"
            );
          }
        }
        let transformer = initializer(...args);
        transformer.postcssPlugin = name;
        transformer.postcssVersion = new Processor().version;
        return transformer;
      }
      let cache;
      Object.defineProperty(creator, "postcss", {
        get() {
          if (!cache) cache = creator();
          return cache;
        }
      });
      creator.process = function(css, processOpts, pluginOpts) {
        return postcss22([creator(pluginOpts)]).process(css, processOpts);
      };
      return creator;
    };
    postcss22.stringify = stringify2;
    postcss22.parse = parse;
    postcss22.fromJSON = fromJSON;
    postcss22.list = list;
    postcss22.comment = (defaults) => new Comment(defaults);
    postcss22.atRule = (defaults) => new AtRule(defaults);
    postcss22.decl = (defaults) => new Declaration(defaults);
    postcss22.rule = (defaults) => new Rule(defaults);
    postcss22.root = (defaults) => new Root(defaults);
    postcss22.document = (defaults) => new Document2(defaults);
    postcss22.CssSyntaxError = CssSyntaxError;
    postcss22.Declaration = Declaration;
    postcss22.Container = Container;
    postcss22.Processor = Processor;
    postcss22.Document = Document2;
    postcss22.Comment = Comment;
    postcss22.Warning = Warning;
    postcss22.AtRule = AtRule;
    postcss22.Result = Result;
    postcss22.Input = Input;
    postcss22.Rule = Rule;
    postcss22.Root = Root;
    postcss22.Node = Node2;
    LazyResult.registerPostcss(postcss22);
    postcss_1$12 = postcss22;
    postcss22.default = postcss22;
    return postcss_1$12;
  }
  function getDefaultExportFromCjs2(x2) {
    return x2 && x2.__esModule && Object.prototype.hasOwnProperty.call(x2, "default") ? x2["default"] : x2;
  }
  function getAugmentedNamespace2(n22) {
    if (Object.prototype.hasOwnProperty.call(n22, "__esModule")) return n22;
    var f2 = n22.default;
    if (typeof f2 == "function") {
      var a2 = function a22() {
        if (this instanceof a22) {
          return Reflect.construct(f2, arguments, this.constructor);
        }
        return f2.apply(this, arguments);
      };
      a2.prototype = f2.prototype;
    } else a2 = {};
    Object.defineProperty(a2, "__esModule", { value: true });
    Object.keys(n22).forEach(function(k2) {
      var d2 = Object.getOwnPropertyDescriptor(n22, k2);
      Object.defineProperty(a2, k2, d2.get ? d2 : {
        enumerable: true,
        get: function() {
          return n22[k2];
        }
      });
    });
    return a2;
  }
  function requirePicocolors_browser2() {
    if (hasRequiredPicocolors_browser2) return picocolors_browser2.exports;
    hasRequiredPicocolors_browser2 = 1;
    var x2 = String;
    var create = function() {
      return { isColorSupported: false, reset: x2, bold: x2, dim: x2, italic: x2, underline: x2, inverse: x2, hidden: x2, strikethrough: x2, black: x2, red: x2, green: x2, yellow: x2, blue: x2, magenta: x2, cyan: x2, white: x2, gray: x2, bgBlack: x2, bgRed: x2, bgGreen: x2, bgYellow: x2, bgBlue: x2, bgMagenta: x2, bgCyan: x2, bgWhite: x2 };
    };
    picocolors_browser2.exports = create();
    picocolors_browser2.exports.createColors = create;
    return picocolors_browser2.exports;
  }
  function requireCssSyntaxError2() {
    if (hasRequiredCssSyntaxError2) return cssSyntaxError2;
    hasRequiredCssSyntaxError2 = 1;
    let pico = /* @__PURE__ */ requirePicocolors_browser2();
    let terminalHighlight = require$$22;
    class CssSyntaxError extends Error {
      constructor(message, line2, column, source, file, plugin) {
        super(message);
        this.name = "CssSyntaxError";
        this.reason = message;
        if (file) {
          this.file = file;
        }
        if (source) {
          this.source = source;
        }
        if (plugin) {
          this.plugin = plugin;
        }
        if (typeof line2 !== "undefined" && typeof column !== "undefined") {
          if (typeof line2 === "number") {
            this.line = line2;
            this.column = column;
          } else {
            this.line = line2.line;
            this.column = line2.column;
            this.endLine = column.line;
            this.endColumn = column.column;
          }
        }
        this.setMessage();
        if (Error.captureStackTrace) {
          Error.captureStackTrace(this, CssSyntaxError);
        }
      }
      setMessage() {
        this.message = this.plugin ? this.plugin + ": " : "";
        this.message += this.file ? this.file : "<css input>";
        if (typeof this.line !== "undefined") {
          this.message += ":" + this.line + ":" + this.column;
        }
        this.message += ": " + this.reason;
      }
      showSourceCode(color) {
        if (!this.source) return "";
        let css = this.source;
        if (color == null) color = pico.isColorSupported;
        if (terminalHighlight) {
          if (color) css = terminalHighlight(css);
        }
        let lines = css.split(/\r?\n/);
        let start3 = Math.max(this.line - 3, 0);
        let end = Math.min(this.line + 2, lines.length);
        let maxWidth = String(end).length;
        let mark, aside;
        if (color) {
          let { bold, gray, red } = pico.createColors(true);
          mark = (text) => bold(red(text));
          aside = (text) => gray(text);
        } else {
          mark = aside = (str) => str;
        }
        return lines.slice(start3, end).map((line2, index22) => {
          let number = start3 + 1 + index22;
          let gutter = " " + (" " + number).slice(-maxWidth) + " | ";
          if (number === this.line) {
            let spacing = aside(gutter.replace(/\d/g, " ")) + line2.slice(0, this.column - 1).replace(/[^\t]/g, " ");
            return mark(">") + aside(gutter) + line2 + "\n " + spacing + mark("^");
          }
          return " " + aside(gutter) + line2;
        }).join("\n");
      }
      toString() {
        let code = this.showSourceCode();
        if (code) {
          code = "\n\n" + code + "\n";
        }
        return this.name + ": " + this.message + code;
      }
    }
    cssSyntaxError2 = CssSyntaxError;
    CssSyntaxError.default = CssSyntaxError;
    return cssSyntaxError2;
  }
  function requireSymbols2() {
    if (hasRequiredSymbols2) return symbols2;
    hasRequiredSymbols2 = 1;
    symbols2.isClean = Symbol("isClean");
    symbols2.my = Symbol("my");
    return symbols2;
  }
  function requireStringifier2() {
    if (hasRequiredStringifier2) return stringifier2;
    hasRequiredStringifier2 = 1;
    const DEFAULT_RAW = {
      after: "\n",
      beforeClose: "\n",
      beforeComment: "\n",
      beforeDecl: "\n",
      beforeOpen: " ",
      beforeRule: "\n",
      colon: ": ",
      commentLeft: " ",
      commentRight: " ",
      emptyBody: "",
      indent: "    ",
      semicolon: false
    };
    function capitalize(str) {
      return str[0].toUpperCase() + str.slice(1);
    }
    class Stringifier {
      constructor(builder) {
        this.builder = builder;
      }
      atrule(node22, semicolon) {
        let name = "@" + node22.name;
        let params = node22.params ? this.rawValue(node22, "params") : "";
        if (typeof node22.raws.afterName !== "undefined") {
          name += node22.raws.afterName;
        } else if (params) {
          name += " ";
        }
        if (node22.nodes) {
          this.block(node22, name + params);
        } else {
          let end = (node22.raws.between || "") + (semicolon ? ";" : "");
          this.builder(name + params + end, node22);
        }
      }
      beforeAfter(node22, detect) {
        let value;
        if (node22.type === "decl") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (node22.type === "comment") {
          value = this.raw(node22, null, "beforeComment");
        } else if (detect === "before") {
          value = this.raw(node22, null, "beforeRule");
        } else {
          value = this.raw(node22, null, "beforeClose");
        }
        let buf = node22.parent;
        let depth = 0;
        while (buf && buf.type !== "root") {
          depth += 1;
          buf = buf.parent;
        }
        if (value.includes("\n")) {
          let indent = this.raw(node22, null, "indent");
          if (indent.length) {
            for (let step = 0; step < depth; step++) value += indent;
          }
        }
        return value;
      }
      block(node22, start3) {
        let between = this.raw(node22, "between", "beforeOpen");
        this.builder(start3 + between + "{", node22, "start");
        let after;
        if (node22.nodes && node22.nodes.length) {
          this.body(node22);
          after = this.raw(node22, "after");
        } else {
          after = this.raw(node22, "after", "emptyBody");
        }
        if (after) this.builder(after);
        this.builder("}", node22, "end");
      }
      body(node22) {
        let last = node22.nodes.length - 1;
        while (last > 0) {
          if (node22.nodes[last].type !== "comment") break;
          last -= 1;
        }
        let semicolon = this.raw(node22, "semicolon");
        for (let i2 = 0; i2 < node22.nodes.length; i2++) {
          let child = node22.nodes[i2];
          let before = this.raw(child, "before");
          if (before) this.builder(before);
          this.stringify(child, last !== i2 || semicolon);
        }
      }
      comment(node22) {
        let left = this.raw(node22, "left", "commentLeft");
        let right = this.raw(node22, "right", "commentRight");
        this.builder("/*" + left + node22.text + right + "*/", node22);
      }
      decl(node22, semicolon) {
        let between = this.raw(node22, "between", "colon");
        let string = node22.prop + between + this.rawValue(node22, "value");
        if (node22.important) {
          string += node22.raws.important || " !important";
        }
        if (semicolon) string += ";";
        this.builder(string, node22);
      }
      document(node22) {
        this.body(node22);
      }
      raw(node22, own, detect) {
        let value;
        if (!detect) detect = own;
        if (own) {
          value = node22.raws[own];
          if (typeof value !== "undefined") return value;
        }
        let parent = node22.parent;
        if (detect === "before") {
          if (!parent || parent.type === "root" && parent.first === node22) {
            return "";
          }
          if (parent && parent.type === "document") {
            return "";
          }
        }
        if (!parent) return DEFAULT_RAW[detect];
        let root22 = node22.root();
        if (!root22.rawCache) root22.rawCache = {};
        if (typeof root22.rawCache[detect] !== "undefined") {
          return root22.rawCache[detect];
        }
        if (detect === "before" || detect === "after") {
          return this.beforeAfter(node22, detect);
        } else {
          let method = "raw" + capitalize(detect);
          if (this[method]) {
            value = this[method](root22, node22);
          } else {
            root22.walk((i2) => {
              value = i2.raws[own];
              if (typeof value !== "undefined") return false;
            });
          }
        }
        if (typeof value === "undefined") value = DEFAULT_RAW[detect];
        root22.rawCache[detect] = value;
        return value;
      }
      rawBeforeClose(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length > 0) {
            if (typeof i2.raws.after !== "undefined") {
              value = i2.raws.after;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawBeforeComment(root22, node22) {
        let value;
        root22.walkComments((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeDecl");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeDecl(root22, node22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.before !== "undefined") {
            value = i2.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        });
        if (typeof value === "undefined") {
          value = this.raw(node22, null, "beforeRule");
        } else if (value) {
          value = value.replace(/\S/g, "");
        }
        return value;
      }
      rawBeforeOpen(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.type !== "decl") {
            value = i2.raws.between;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawBeforeRule(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && (i2.parent !== root22 || root22.first !== i2)) {
            if (typeof i2.raws.before !== "undefined") {
              value = i2.raws.before;
              if (value.includes("\n")) {
                value = value.replace(/[^\n]+$/, "");
              }
              return false;
            }
          }
        });
        if (value) value = value.replace(/\S/g, "");
        return value;
      }
      rawColon(root22) {
        let value;
        root22.walkDecls((i2) => {
          if (typeof i2.raws.between !== "undefined") {
            value = i2.raws.between.replace(/[^\s:]/g, "");
            return false;
          }
        });
        return value;
      }
      rawEmptyBody(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length === 0) {
            value = i2.raws.after;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawIndent(root22) {
        if (root22.raws.indent) return root22.raws.indent;
        let value;
        root22.walk((i2) => {
          let p2 = i2.parent;
          if (p2 && p2 !== root22 && p2.parent && p2.parent === root22) {
            if (typeof i2.raws.before !== "undefined") {
              let parts = i2.raws.before.split("\n");
              value = parts[parts.length - 1];
              value = value.replace(/\S/g, "");
              return false;
            }
          }
        });
        return value;
      }
      rawSemicolon(root22) {
        let value;
        root22.walk((i2) => {
          if (i2.nodes && i2.nodes.length && i2.last.type === "decl") {
            value = i2.raws.semicolon;
            if (typeof value !== "undefined") return false;
          }
        });
        return value;
      }
      rawValue(node22, prop) {
        let value = node22[prop];
        let raw = node22.raws[prop];
        if (raw && raw.value === value) {
          return raw.raw;
        }
        return value;
      }
      root(node22) {
        this.body(node22);
        if (node22.raws.after) this.builder(node22.raws.after);
      }
      rule(node22) {
        this.block(node22, this.rawValue(node22, "selector"));
        if (node22.raws.ownSemicolon) {
          this.builder(node22.raws.ownSemicolon, node22, "end");
        }
      }
      stringify(node22, semicolon) {
        if (!this[node22.type]) {
          throw new Error(
            "Unknown AST node type " + node22.type + ". Maybe you need to change PostCSS stringifier."
          );
        }
        this[node22.type](node22, semicolon);
      }
    }
    stringifier2 = Stringifier;
    Stringifier.default = Stringifier;
    return stringifier2;
  }
  function requireStringify2() {
    if (hasRequiredStringify2) return stringify_12;
    hasRequiredStringify2 = 1;
    let Stringifier = requireStringifier2();
    function stringify2(node22, builder) {
      let str = new Stringifier(builder);
      str.stringify(node22);
    }
    stringify_12 = stringify2;
    stringify2.default = stringify2;
    return stringify_12;
  }
  function requireNode2() {
    if (hasRequiredNode2) return node2;
    hasRequiredNode2 = 1;
    let { isClean, my } = requireSymbols2();
    let CssSyntaxError = requireCssSyntaxError2();
    let Stringifier = requireStringifier2();
    let stringify2 = requireStringify2();
    function cloneNode(obj, parent) {
      let cloned = new obj.constructor();
      for (let i2 in obj) {
        if (!Object.prototype.hasOwnProperty.call(obj, i2)) {
          continue;
        }
        if (i2 === "proxyCache") continue;
        let value = obj[i2];
        let type = typeof value;
        if (i2 === "parent" && type === "object") {
          if (parent) cloned[i2] = parent;
        } else if (i2 === "source") {
          cloned[i2] = value;
        } else if (Array.isArray(value)) {
          cloned[i2] = value.map((j) => cloneNode(j, cloned));
        } else {
          if (type === "object" && value !== null) value = cloneNode(value);
          cloned[i2] = value;
        }
      }
      return cloned;
    }
    class Node2 {
      constructor(defaults = {}) {
        this.raws = {};
        this[isClean] = false;
        this[my] = true;
        for (let name in defaults) {
          if (name === "nodes") {
            this.nodes = [];
            for (let node22 of defaults[name]) {
              if (typeof node22.clone === "function") {
                this.append(node22.clone());
              } else {
                this.append(node22);
              }
            }
          } else {
            this[name] = defaults[name];
          }
        }
      }
      addToError(error) {
        error.postcssNode = this;
        if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
          let s2 = this.source;
          error.stack = error.stack.replace(
            /\n\s{4}at /,
            `$&${s2.input.from}:${s2.start.line}:${s2.start.column}$&`
          );
        }
        return error;
      }
      after(add2) {
        this.parent.insertAfter(this, add2);
        return this;
      }
      assign(overrides = {}) {
        for (let name in overrides) {
          this[name] = overrides[name];
        }
        return this;
      }
      before(add2) {
        this.parent.insertBefore(this, add2);
        return this;
      }
      cleanRaws(keepBetween) {
        delete this.raws.before;
        delete this.raws.after;
        if (!keepBetween) delete this.raws.between;
      }
      clone(overrides = {}) {
        let cloned = cloneNode(this);
        for (let name in overrides) {
          cloned[name] = overrides[name];
        }
        return cloned;
      }
      cloneAfter(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertAfter(this, cloned);
        return cloned;
      }
      cloneBefore(overrides = {}) {
        let cloned = this.clone(overrides);
        this.parent.insertBefore(this, cloned);
        return cloned;
      }
      error(message, opts = {}) {
        if (this.source) {
          let { end, start: start3 } = this.rangeBy(opts);
          return this.source.input.error(
            message,
            { column: start3.column, line: start3.line },
            { column: end.column, line: end.line },
            opts
          );
        }
        return new CssSyntaxError(message);
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
            prop === "text") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      markDirty() {
        if (this[isClean]) {
          this[isClean] = false;
          let next = this;
          while (next = next.parent) {
            next[isClean] = false;
          }
        }
      }
      next() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 + 1];
      }
      positionBy(opts, stringRepresentation) {
        let pos = this.source.start;
        if (opts.index) {
          pos = this.positionInside(opts.index, stringRepresentation);
        } else if (opts.word) {
          stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) pos = this.positionInside(index22, stringRepresentation);
        }
        return pos;
      }
      positionInside(index22, stringRepresentation) {
        let string = stringRepresentation || this.toString();
        let column = this.source.start.column;
        let line2 = this.source.start.line;
        for (let i2 = 0; i2 < index22; i2++) {
          if (string[i2] === "\n") {
            column = 1;
            line2 += 1;
          } else {
            column += 1;
          }
        }
        return { column, line: line2 };
      }
      prev() {
        if (!this.parent) return void 0;
        let index22 = this.parent.index(this);
        return this.parent.nodes[index22 - 1];
      }
      rangeBy(opts) {
        let start3 = {
          column: this.source.start.column,
          line: this.source.start.line
        };
        let end = this.source.end ? {
          column: this.source.end.column + 1,
          line: this.source.end.line
        } : {
          column: start3.column + 1,
          line: start3.line
        };
        if (opts.word) {
          let stringRepresentation = this.toString();
          let index22 = stringRepresentation.indexOf(opts.word);
          if (index22 !== -1) {
            start3 = this.positionInside(index22, stringRepresentation);
            end = this.positionInside(index22 + opts.word.length, stringRepresentation);
          }
        } else {
          if (opts.start) {
            start3 = {
              column: opts.start.column,
              line: opts.start.line
            };
          } else if (opts.index) {
            start3 = this.positionInside(opts.index);
          }
          if (opts.end) {
            end = {
              column: opts.end.column,
              line: opts.end.line
            };
          } else if (typeof opts.endIndex === "number") {
            end = this.positionInside(opts.endIndex);
          } else if (opts.index) {
            end = this.positionInside(opts.index + 1);
          }
        }
        if (end.line < start3.line || end.line === start3.line && end.column <= start3.column) {
          end = { column: start3.column + 1, line: start3.line };
        }
        return { end, start: start3 };
      }
      raw(prop, defaultType) {
        let str = new Stringifier();
        return str.raw(this, prop, defaultType);
      }
      remove() {
        if (this.parent) {
          this.parent.removeChild(this);
        }
        this.parent = void 0;
        return this;
      }
      replaceWith(...nodes) {
        if (this.parent) {
          let bookmark = this;
          let foundSelf = false;
          for (let node22 of nodes) {
            if (node22 === this) {
              foundSelf = true;
            } else if (foundSelf) {
              this.parent.insertAfter(bookmark, node22);
              bookmark = node22;
            } else {
              this.parent.insertBefore(bookmark, node22);
            }
          }
          if (!foundSelf) {
            this.remove();
          }
        }
        return this;
      }
      root() {
        let result22 = this;
        while (result22.parent && result22.parent.type !== "document") {
          result22 = result22.parent;
        }
        return result22;
      }
      toJSON(_2, inputs) {
        let fixed = {};
        let emitInputs = inputs == null;
        inputs = inputs || /* @__PURE__ */ new Map();
        let inputsNextIndex = 0;
        for (let name in this) {
          if (!Object.prototype.hasOwnProperty.call(this, name)) {
            continue;
          }
          if (name === "parent" || name === "proxyCache") continue;
          let value = this[name];
          if (Array.isArray(value)) {
            fixed[name] = value.map((i2) => {
              if (typeof i2 === "object" && i2.toJSON) {
                return i2.toJSON(null, inputs);
              } else {
                return i2;
              }
            });
          } else if (typeof value === "object" && value.toJSON) {
            fixed[name] = value.toJSON(null, inputs);
          } else if (name === "source") {
            let inputId = inputs.get(value.input);
            if (inputId == null) {
              inputId = inputsNextIndex;
              inputs.set(value.input, inputsNextIndex);
              inputsNextIndex++;
            }
            fixed[name] = {
              end: value.end,
              inputId,
              start: value.start
            };
          } else {
            fixed[name] = value;
          }
        }
        if (emitInputs) {
          fixed.inputs = [...inputs.keys()].map((input22) => input22.toJSON());
        }
        return fixed;
      }
      toProxy() {
        if (!this.proxyCache) {
          this.proxyCache = new Proxy(this, this.getProxyProcessor());
        }
        return this.proxyCache;
      }
      toString(stringifier22 = stringify2) {
        if (stringifier22.stringify) stringifier22 = stringifier22.stringify;
        let result22 = "";
        stringifier22(this, (i2) => {
          result22 += i2;
        });
        return result22;
      }
      warn(result22, text, opts) {
        let data = { node: this };
        for (let i2 in opts) data[i2] = opts[i2];
        return result22.warn(text, data);
      }
      get proxyOf() {
        return this;
      }
    }
    node2 = Node2;
    Node2.default = Node2;
    return node2;
  }
  function requireDeclaration2() {
    if (hasRequiredDeclaration2) return declaration2;
    hasRequiredDeclaration2 = 1;
    let Node2 = requireNode2();
    class Declaration extends Node2 {
      constructor(defaults) {
        if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
          defaults = { ...defaults, value: String(defaults.value) };
        }
        super(defaults);
        this.type = "decl";
      }
      get variable() {
        return this.prop.startsWith("--") || this.prop[0] === "$";
      }
    }
    declaration2 = Declaration;
    Declaration.default = Declaration;
    return declaration2;
  }
  function requireNonSecure2() {
    if (hasRequiredNonSecure2) return nonSecure2;
    hasRequiredNonSecure2 = 1;
    let urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
    let customAlphabet = (alphabet, defaultSize = 21) => {
      return (size = defaultSize) => {
        let id2 = "";
        let i2 = size;
        while (i2--) {
          id2 += alphabet[Math.random() * alphabet.length | 0];
        }
        return id2;
      };
    };
    let nanoid = (size = 21) => {
      let id2 = "";
      let i2 = size;
      while (i2--) {
        id2 += urlAlphabet[Math.random() * 64 | 0];
      }
      return id2;
    };
    nonSecure2 = { nanoid, customAlphabet };
    return nonSecure2;
  }
  function requirePreviousMap2() {
    if (hasRequiredPreviousMap2) return previousMap2;
    hasRequiredPreviousMap2 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$22;
    let { existsSync, readFileSync } = require$$22;
    let { dirname, join } = require$$22;
    function fromBase64(str) {
      if (Buffer) {
        return Buffer.from(str, "base64").toString();
      } else {
        return window.atob(str);
      }
    }
    class PreviousMap {
      constructor(css, opts) {
        if (opts.map === false) return;
        this.loadAnnotation(css);
        this.inline = this.startWith(this.annotation, "data:");
        let prev = opts.map ? opts.map.prev : void 0;
        let text = this.loadMap(opts.from, prev);
        if (!this.mapFile && opts.from) {
          this.mapFile = opts.from;
        }
        if (this.mapFile) this.root = dirname(this.mapFile);
        if (text) this.text = text;
      }
      consumer() {
        if (!this.consumerCache) {
          this.consumerCache = new SourceMapConsumer(this.text);
        }
        return this.consumerCache;
      }
      decodeInline(text) {
        let baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
        let baseUri = /^data:application\/json;base64,/;
        let charsetUri = /^data:application\/json;charset=utf-?8,/;
        let uri = /^data:application\/json,/;
        if (charsetUri.test(text) || uri.test(text)) {
          return decodeURIComponent(text.substr(RegExp.lastMatch.length));
        }
        if (baseCharsetUri.test(text) || baseUri.test(text)) {
          return fromBase64(text.substr(RegExp.lastMatch.length));
        }
        let encoding = text.match(/data:application\/json;([^,]+),/)[1];
        throw new Error("Unsupported source map encoding " + encoding);
      }
      getAnnotationURL(sourceMapString) {
        return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
      }
      isMap(map) {
        if (typeof map !== "object") return false;
        return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
      }
      loadAnnotation(css) {
        let comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
        if (!comments) return;
        let start3 = css.lastIndexOf(comments.pop());
        let end = css.indexOf("*/", start3);
        if (start3 > -1 && end > -1) {
          this.annotation = this.getAnnotationURL(css.substring(start3, end));
        }
      }
      loadFile(path) {
        this.root = dirname(path);
        if (existsSync(path)) {
          this.mapFile = path;
          return readFileSync(path, "utf-8").toString().trim();
        }
      }
      loadMap(file, prev) {
        if (prev === false) return false;
        if (prev) {
          if (typeof prev === "string") {
            return prev;
          } else if (typeof prev === "function") {
            let prevPath = prev(file);
            if (prevPath) {
              let map = this.loadFile(prevPath);
              if (!map) {
                throw new Error(
                  "Unable to load previous source map: " + prevPath.toString()
                );
              }
              return map;
            }
          } else if (prev instanceof SourceMapConsumer) {
            return SourceMapGenerator.fromSourceMap(prev).toString();
          } else if (prev instanceof SourceMapGenerator) {
            return prev.toString();
          } else if (this.isMap(prev)) {
            return JSON.stringify(prev);
          } else {
            throw new Error(
              "Unsupported previous source map format: " + prev.toString()
            );
          }
        } else if (this.inline) {
          return this.decodeInline(this.annotation);
        } else if (this.annotation) {
          let map = this.annotation;
          if (file) map = join(dirname(file), map);
          return this.loadFile(map);
        }
      }
      startWith(string, start3) {
        if (!string) return false;
        return string.substr(0, start3.length) === start3;
      }
      withContent() {
        return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
      }
    }
    previousMap2 = PreviousMap;
    PreviousMap.default = PreviousMap;
    return previousMap2;
  }
  function requireInput2() {
    if (hasRequiredInput2) return input2;
    hasRequiredInput2 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$22;
    let { fileURLToPath, pathToFileURL } = require$$22;
    let { isAbsolute, resolve } = require$$22;
    let { nanoid } = /* @__PURE__ */ requireNonSecure2();
    let terminalHighlight = require$$22;
    let CssSyntaxError = requireCssSyntaxError2();
    let PreviousMap = requirePreviousMap2();
    let fromOffsetCache = Symbol("fromOffsetCache");
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(resolve && isAbsolute);
    class Input {
      constructor(css, opts = {}) {
        if (css === null || typeof css === "undefined" || typeof css === "object" && !css.toString) {
          throw new Error(`PostCSS received ${css} instead of CSS string`);
        }
        this.css = css.toString();
        if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
          this.hasBOM = true;
          this.css = this.css.slice(1);
        } else {
          this.hasBOM = false;
        }
        if (opts.from) {
          if (!pathAvailable || /^\w+:\/\//.test(opts.from) || isAbsolute(opts.from)) {
            this.file = opts.from;
          } else {
            this.file = resolve(opts.from);
          }
        }
        if (pathAvailable && sourceMapAvailable) {
          let map = new PreviousMap(this.css, opts);
          if (map.text) {
            this.map = map;
            let file = map.consumer().file;
            if (!this.file && file) this.file = this.mapResolve(file);
          }
        }
        if (!this.file) {
          this.id = "<input css " + nanoid(6) + ">";
        }
        if (this.map) this.map.file = this.from;
      }
      error(message, line2, column, opts = {}) {
        let result22, endLine, endColumn;
        if (line2 && typeof line2 === "object") {
          let start3 = line2;
          let end = column;
          if (typeof start3.offset === "number") {
            let pos = this.fromOffset(start3.offset);
            line2 = pos.line;
            column = pos.col;
          } else {
            line2 = start3.line;
            column = start3.column;
          }
          if (typeof end.offset === "number") {
            let pos = this.fromOffset(end.offset);
            endLine = pos.line;
            endColumn = pos.col;
          } else {
            endLine = end.line;
            endColumn = end.column;
          }
        } else if (!column) {
          let pos = this.fromOffset(line2);
          line2 = pos.line;
          column = pos.col;
        }
        let origin = this.origin(line2, column, endLine, endColumn);
        if (origin) {
          result22 = new CssSyntaxError(
            message,
            origin.endLine === void 0 ? origin.line : { column: origin.column, line: origin.line },
            origin.endLine === void 0 ? origin.column : { column: origin.endColumn, line: origin.endLine },
            origin.source,
            origin.file,
            opts.plugin
          );
        } else {
          result22 = new CssSyntaxError(
            message,
            endLine === void 0 ? line2 : { column, line: line2 },
            endLine === void 0 ? column : { column: endColumn, line: endLine },
            this.css,
            this.file,
            opts.plugin
          );
        }
        result22.input = { column, endColumn, endLine, line: line2, source: this.css };
        if (this.file) {
          if (pathToFileURL) {
            result22.input.url = pathToFileURL(this.file).toString();
          }
          result22.input.file = this.file;
        }
        return result22;
      }
      fromOffset(offset) {
        let lastLine, lineToIndex;
        if (!this[fromOffsetCache]) {
          let lines = this.css.split("\n");
          lineToIndex = new Array(lines.length);
          let prevIndex = 0;
          for (let i2 = 0, l2 = lines.length; i2 < l2; i2++) {
            lineToIndex[i2] = prevIndex;
            prevIndex += lines[i2].length + 1;
          }
          this[fromOffsetCache] = lineToIndex;
        } else {
          lineToIndex = this[fromOffsetCache];
        }
        lastLine = lineToIndex[lineToIndex.length - 1];
        let min = 0;
        if (offset >= lastLine) {
          min = lineToIndex.length - 1;
        } else {
          let max = lineToIndex.length - 2;
          let mid;
          while (min < max) {
            mid = min + (max - min >> 1);
            if (offset < lineToIndex[mid]) {
              max = mid - 1;
            } else if (offset >= lineToIndex[mid + 1]) {
              min = mid + 1;
            } else {
              min = mid;
              break;
            }
          }
        }
        return {
          col: offset - lineToIndex[min] + 1,
          line: min + 1
        };
      }
      mapResolve(file) {
        if (/^\w+:\/\//.test(file)) {
          return file;
        }
        return resolve(this.map.consumer().sourceRoot || this.map.root || ".", file);
      }
      origin(line2, column, endLine, endColumn) {
        if (!this.map) return false;
        let consumer = this.map.consumer();
        let from = consumer.originalPositionFor({ column, line: line2 });
        if (!from.source) return false;
        let to;
        if (typeof endLine === "number") {
          to = consumer.originalPositionFor({ column: endColumn, line: endLine });
        }
        let fromUrl;
        if (isAbsolute(from.source)) {
          fromUrl = pathToFileURL(from.source);
        } else {
          fromUrl = new URL(
            from.source,
            this.map.consumer().sourceRoot || pathToFileURL(this.map.mapFile)
          );
        }
        let result22 = {
          column: from.column,
          endColumn: to && to.column,
          endLine: to && to.line,
          line: from.line,
          url: fromUrl.toString()
        };
        if (fromUrl.protocol === "file:") {
          if (fileURLToPath) {
            result22.file = fileURLToPath(fromUrl);
          } else {
            throw new Error(`file: protocol is not available in this PostCSS build`);
          }
        }
        let source = consumer.sourceContentFor(from.source);
        if (source) result22.source = source;
        return result22;
      }
      toJSON() {
        let json = {};
        for (let name of ["hasBOM", "css", "file", "id"]) {
          if (this[name] != null) {
            json[name] = this[name];
          }
        }
        if (this.map) {
          json.map = { ...this.map };
          if (json.map.consumerCache) {
            json.map.consumerCache = void 0;
          }
        }
        return json;
      }
      get from() {
        return this.file || this.id;
      }
    }
    input2 = Input;
    Input.default = Input;
    if (terminalHighlight && terminalHighlight.registerInput) {
      terminalHighlight.registerInput(Input);
    }
    return input2;
  }
  function requireMapGenerator2() {
    if (hasRequiredMapGenerator2) return mapGenerator2;
    hasRequiredMapGenerator2 = 1;
    let { SourceMapConsumer, SourceMapGenerator } = require$$22;
    let { dirname, relative, resolve, sep } = require$$22;
    let { pathToFileURL } = require$$22;
    let Input = requireInput2();
    let sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
    let pathAvailable = Boolean(dirname && resolve && relative && sep);
    class MapGenerator {
      constructor(stringify2, root22, opts, cssString) {
        this.stringify = stringify2;
        this.mapOpts = opts.map || {};
        this.root = root22;
        this.opts = opts;
        this.css = cssString;
        this.originalCSS = cssString;
        this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
        this.memoizedFileURLs = /* @__PURE__ */ new Map();
        this.memoizedPaths = /* @__PURE__ */ new Map();
        this.memoizedURLs = /* @__PURE__ */ new Map();
      }
      addAnnotation() {
        let content;
        if (this.isInline()) {
          content = "data:application/json;base64," + this.toBase64(this.map.toString());
        } else if (typeof this.mapOpts.annotation === "string") {
          content = this.mapOpts.annotation;
        } else if (typeof this.mapOpts.annotation === "function") {
          content = this.mapOpts.annotation(this.opts.to, this.root);
        } else {
          content = this.outputFile() + ".map";
        }
        let eol = "\n";
        if (this.css.includes("\r\n")) eol = "\r\n";
        this.css += eol + "/*# sourceMappingURL=" + content + " */";
      }
      applyPrevMaps() {
        for (let prev of this.previous()) {
          let from = this.toUrl(this.path(prev.file));
          let root22 = prev.root || dirname(prev.file);
          let map;
          if (this.mapOpts.sourcesContent === false) {
            map = new SourceMapConsumer(prev.text);
            if (map.sourcesContent) {
              map.sourcesContent = null;
            }
          } else {
            map = prev.consumer();
          }
          this.map.applySourceMap(map, from, this.toUrl(this.path(root22)));
        }
      }
      clearAnnotation() {
        if (this.mapOpts.annotation === false) return;
        if (this.root) {
          let node22;
          for (let i2 = this.root.nodes.length - 1; i2 >= 0; i2--) {
            node22 = this.root.nodes[i2];
            if (node22.type !== "comment") continue;
            if (node22.text.indexOf("# sourceMappingURL=") === 0) {
              this.root.removeChild(i2);
            }
          }
        } else if (this.css) {
          this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
        }
      }
      generate() {
        this.clearAnnotation();
        if (pathAvailable && sourceMapAvailable && this.isMap()) {
          return this.generateMap();
        } else {
          let result22 = "";
          this.stringify(this.root, (i2) => {
            result22 += i2;
          });
          return [result22];
        }
      }
      generateMap() {
        if (this.root) {
          this.generateString();
        } else if (this.previous().length === 1) {
          let prev = this.previous()[0].consumer();
          prev.file = this.outputFile();
          this.map = SourceMapGenerator.fromSourceMap(prev, {
            ignoreInvalidMapping: true
          });
        } else {
          this.map = new SourceMapGenerator({
            file: this.outputFile(),
            ignoreInvalidMapping: true
          });
          this.map.addMapping({
            generated: { column: 0, line: 1 },
            original: { column: 0, line: 1 },
            source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
          });
        }
        if (this.isSourcesContent()) this.setSourcesContent();
        if (this.root && this.previous().length > 0) this.applyPrevMaps();
        if (this.isAnnotation()) this.addAnnotation();
        if (this.isInline()) {
          return [this.css];
        } else {
          return [this.css, this.map];
        }
      }
      generateString() {
        this.css = "";
        this.map = new SourceMapGenerator({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        let line2 = 1;
        let column = 1;
        let noSource = "<no source>";
        let mapping = {
          generated: { column: 0, line: 0 },
          original: { column: 0, line: 0 },
          source: ""
        };
        let lines, last;
        this.stringify(this.root, (str, node22, type) => {
          this.css += str;
          if (node22 && type !== "end") {
            mapping.generated.line = line2;
            mapping.generated.column = column - 1;
            if (node22.source && node22.source.start) {
              mapping.source = this.sourcePath(node22);
              mapping.original.line = node22.source.start.line;
              mapping.original.column = node22.source.start.column - 1;
              this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              this.map.addMapping(mapping);
            }
          }
          lines = str.match(/\n/g);
          if (lines) {
            line2 += lines.length;
            last = str.lastIndexOf("\n");
            column = str.length - last;
          } else {
            column += str.length;
          }
          if (node22 && type !== "start") {
            let p2 = node22.parent || { raws: {} };
            let childless = node22.type === "decl" || node22.type === "atrule" && !node22.nodes;
            if (!childless || node22 !== p2.last || p2.raws.semicolon) {
              if (node22.source && node22.source.end) {
                mapping.source = this.sourcePath(node22);
                mapping.original.line = node22.source.end.line;
                mapping.original.column = node22.source.end.column - 1;
                mapping.generated.line = line2;
                mapping.generated.column = column - 2;
                this.map.addMapping(mapping);
              } else {
                mapping.source = noSource;
                mapping.original.line = 1;
                mapping.original.column = 0;
                mapping.generated.line = line2;
                mapping.generated.column = column - 1;
                this.map.addMapping(mapping);
              }
            }
          }
        });
      }
      isAnnotation() {
        if (this.isInline()) {
          return true;
        }
        if (typeof this.mapOpts.annotation !== "undefined") {
          return this.mapOpts.annotation;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.annotation);
        }
        return true;
      }
      isInline() {
        if (typeof this.mapOpts.inline !== "undefined") {
          return this.mapOpts.inline;
        }
        let annotation = this.mapOpts.annotation;
        if (typeof annotation !== "undefined" && annotation !== true) {
          return false;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.inline);
        }
        return true;
      }
      isMap() {
        if (typeof this.opts.map !== "undefined") {
          return !!this.opts.map;
        }
        return this.previous().length > 0;
      }
      isSourcesContent() {
        if (typeof this.mapOpts.sourcesContent !== "undefined") {
          return this.mapOpts.sourcesContent;
        }
        if (this.previous().length) {
          return this.previous().some((i2) => i2.withContent());
        }
        return true;
      }
      outputFile() {
        if (this.opts.to) {
          return this.path(this.opts.to);
        } else if (this.opts.from) {
          return this.path(this.opts.from);
        } else {
          return "to.css";
        }
      }
      path(file) {
        if (this.mapOpts.absolute) return file;
        if (file.charCodeAt(0) === 60) return file;
        if (/^\w+:\/\//.test(file)) return file;
        let cached = this.memoizedPaths.get(file);
        if (cached) return cached;
        let from = this.opts.to ? dirname(this.opts.to) : ".";
        if (typeof this.mapOpts.annotation === "string") {
          from = dirname(resolve(from, this.mapOpts.annotation));
        }
        let path = relative(from, file);
        this.memoizedPaths.set(file, path);
        return path;
      }
      previous() {
        if (!this.previousMaps) {
          this.previousMaps = [];
          if (this.root) {
            this.root.walk((node22) => {
              if (node22.source && node22.source.input.map) {
                let map = node22.source.input.map;
                if (!this.previousMaps.includes(map)) {
                  this.previousMaps.push(map);
                }
              }
            });
          } else {
            let input22 = new Input(this.originalCSS, this.opts);
            if (input22.map) this.previousMaps.push(input22.map);
          }
        }
        return this.previousMaps;
      }
      setSourcesContent() {
        let already = {};
        if (this.root) {
          this.root.walk((node22) => {
            if (node22.source) {
              let from = node22.source.input.from;
              if (from && !already[from]) {
                already[from] = true;
                let fromUrl = this.usesFileUrls ? this.toFileUrl(from) : this.toUrl(this.path(from));
                this.map.setSourceContent(fromUrl, node22.source.input.css);
              }
            }
          });
        } else if (this.css) {
          let from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
          this.map.setSourceContent(from, this.css);
        }
      }
      sourcePath(node22) {
        if (this.mapOpts.from) {
          return this.toUrl(this.mapOpts.from);
        } else if (this.usesFileUrls) {
          return this.toFileUrl(node22.source.input.from);
        } else {
          return this.toUrl(this.path(node22.source.input.from));
        }
      }
      toBase64(str) {
        if (Buffer) {
          return Buffer.from(str).toString("base64");
        } else {
          return window.btoa(unescape(encodeURIComponent(str)));
        }
      }
      toFileUrl(path) {
        let cached = this.memoizedFileURLs.get(path);
        if (cached) return cached;
        if (pathToFileURL) {
          let fileURL = pathToFileURL(path).toString();
          this.memoizedFileURLs.set(path, fileURL);
          return fileURL;
        } else {
          throw new Error(
            "`map.absolute` option is not available in this PostCSS build"
          );
        }
      }
      toUrl(path) {
        let cached = this.memoizedURLs.get(path);
        if (cached) return cached;
        if (sep === "\\") {
          path = path.replace(/\\/g, "/");
        }
        let url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
        this.memoizedURLs.set(path, url);
        return url;
      }
    }
    mapGenerator2 = MapGenerator;
    return mapGenerator2;
  }
  function requireComment2() {
    if (hasRequiredComment2) return comment2;
    hasRequiredComment2 = 1;
    let Node2 = requireNode2();
    class Comment extends Node2 {
      constructor(defaults) {
        super(defaults);
        this.type = "comment";
      }
    }
    comment2 = Comment;
    Comment.default = Comment;
    return comment2;
  }
  function requireContainer2() {
    if (hasRequiredContainer2) return container2;
    hasRequiredContainer2 = 1;
    let { isClean, my } = requireSymbols2();
    let Declaration = requireDeclaration2();
    let Comment = requireComment2();
    let Node2 = requireNode2();
    let parse, Rule, AtRule, Root;
    function cleanSource(nodes) {
      return nodes.map((i2) => {
        if (i2.nodes) i2.nodes = cleanSource(i2.nodes);
        delete i2.source;
        return i2;
      });
    }
    function markDirtyUp(node22) {
      node22[isClean] = false;
      if (node22.proxyOf.nodes) {
        for (let i2 of node22.proxyOf.nodes) {
          markDirtyUp(i2);
        }
      }
    }
    class Container extends Node2 {
      append(...children) {
        for (let child of children) {
          let nodes = this.normalize(child, this.last);
          for (let node22 of nodes) this.proxyOf.nodes.push(node22);
        }
        this.markDirty();
        return this;
      }
      cleanRaws(keepBetween) {
        super.cleanRaws(keepBetween);
        if (this.nodes) {
          for (let node22 of this.nodes) node22.cleanRaws(keepBetween);
        }
      }
      each(callback) {
        if (!this.proxyOf.nodes) return void 0;
        let iterator = this.getIterator();
        let index22, result22;
        while (this.indexes[iterator] < this.proxyOf.nodes.length) {
          index22 = this.indexes[iterator];
          result22 = callback(this.proxyOf.nodes[index22], index22);
          if (result22 === false) break;
          this.indexes[iterator] += 1;
        }
        delete this.indexes[iterator];
        return result22;
      }
      every(condition) {
        return this.nodes.every(condition);
      }
      getIterator() {
        if (!this.lastEach) this.lastEach = 0;
        if (!this.indexes) this.indexes = {};
        this.lastEach += 1;
        let iterator = this.lastEach;
        this.indexes[iterator] = 0;
        return iterator;
      }
      getProxyProcessor() {
        return {
          get(node22, prop) {
            if (prop === "proxyOf") {
              return node22;
            } else if (!node22[prop]) {
              return node22[prop];
            } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
              return (...args) => {
                return node22[prop](
                  ...args.map((i2) => {
                    if (typeof i2 === "function") {
                      return (child, index22) => i2(child.toProxy(), index22);
                    } else {
                      return i2;
                    }
                  })
                );
              };
            } else if (prop === "every" || prop === "some") {
              return (cb) => {
                return node22[prop](
                  (child, ...other) => cb(child.toProxy(), ...other)
                );
              };
            } else if (prop === "root") {
              return () => node22.root().toProxy();
            } else if (prop === "nodes") {
              return node22.nodes.map((i2) => i2.toProxy());
            } else if (prop === "first" || prop === "last") {
              return node22[prop].toProxy();
            } else {
              return node22[prop];
            }
          },
          set(node22, prop, value) {
            if (node22[prop] === value) return true;
            node22[prop] = value;
            if (prop === "name" || prop === "params" || prop === "selector") {
              node22.markDirty();
            }
            return true;
          }
        };
      }
      index(child) {
        if (typeof child === "number") return child;
        if (child.proxyOf) child = child.proxyOf;
        return this.proxyOf.nodes.indexOf(child);
      }
      insertAfter(exist, add2) {
        let existIndex = this.index(exist);
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex]).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex + 1, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex < index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      insertBefore(exist, add2) {
        let existIndex = this.index(exist);
        let type = existIndex === 0 ? "prepend" : false;
        let nodes = this.normalize(add2, this.proxyOf.nodes[existIndex], type).reverse();
        existIndex = this.index(exist);
        for (let node22 of nodes) this.proxyOf.nodes.splice(existIndex, 0, node22);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (existIndex <= index22) {
            this.indexes[id2] = index22 + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      normalize(nodes, sample) {
        if (typeof nodes === "string") {
          nodes = cleanSource(parse(nodes).nodes);
        } else if (typeof nodes === "undefined") {
          nodes = [];
        } else if (Array.isArray(nodes)) {
          nodes = nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type === "root" && this.type !== "document") {
          nodes = nodes.nodes.slice(0);
          for (let i2 of nodes) {
            if (i2.parent) i2.parent.removeChild(i2, "ignore");
          }
        } else if (nodes.type) {
          nodes = [nodes];
        } else if (nodes.prop) {
          if (typeof nodes.value === "undefined") {
            throw new Error("Value field is missed in node creation");
          } else if (typeof nodes.value !== "string") {
            nodes.value = String(nodes.value);
          }
          nodes = [new Declaration(nodes)];
        } else if (nodes.selector) {
          nodes = [new Rule(nodes)];
        } else if (nodes.name) {
          nodes = [new AtRule(nodes)];
        } else if (nodes.text) {
          nodes = [new Comment(nodes)];
        } else {
          throw new Error("Unknown node type in node creation");
        }
        let processed = nodes.map((i2) => {
          if (!i2[my]) Container.rebuild(i2);
          i2 = i2.proxyOf;
          if (i2.parent) i2.parent.removeChild(i2);
          if (i2[isClean]) markDirtyUp(i2);
          if (typeof i2.raws.before === "undefined") {
            if (sample && typeof sample.raws.before !== "undefined") {
              i2.raws.before = sample.raws.before.replace(/\S/g, "");
            }
          }
          i2.parent = this.proxyOf;
          return i2;
        });
        return processed;
      }
      prepend(...children) {
        children = children.reverse();
        for (let child of children) {
          let nodes = this.normalize(child, this.first, "prepend").reverse();
          for (let node22 of nodes) this.proxyOf.nodes.unshift(node22);
          for (let id2 in this.indexes) {
            this.indexes[id2] = this.indexes[id2] + nodes.length;
          }
        }
        this.markDirty();
        return this;
      }
      push(child) {
        child.parent = this;
        this.proxyOf.nodes.push(child);
        return this;
      }
      removeAll() {
        for (let node22 of this.proxyOf.nodes) node22.parent = void 0;
        this.proxyOf.nodes = [];
        this.markDirty();
        return this;
      }
      removeChild(child) {
        child = this.index(child);
        this.proxyOf.nodes[child].parent = void 0;
        this.proxyOf.nodes.splice(child, 1);
        let index22;
        for (let id2 in this.indexes) {
          index22 = this.indexes[id2];
          if (index22 >= child) {
            this.indexes[id2] = index22 - 1;
          }
        }
        this.markDirty();
        return this;
      }
      replaceValues(pattern, opts, callback) {
        if (!callback) {
          callback = opts;
          opts = {};
        }
        this.walkDecls((decl) => {
          if (opts.props && !opts.props.includes(decl.prop)) return;
          if (opts.fast && !decl.value.includes(opts.fast)) return;
          decl.value = decl.value.replace(pattern, callback);
        });
        this.markDirty();
        return this;
      }
      some(condition) {
        return this.nodes.some(condition);
      }
      walk(callback) {
        return this.each((child, i2) => {
          let result22;
          try {
            result22 = callback(child, i2);
          } catch (e2) {
            throw child.addToError(e2);
          }
          if (result22 !== false && child.walk) {
            result22 = child.walk(callback);
          }
          return result22;
        });
      }
      walkAtRules(name, callback) {
        if (!callback) {
          callback = name;
          return this.walk((child, i2) => {
            if (child.type === "atrule") {
              return callback(child, i2);
            }
          });
        }
        if (name instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "atrule" && name.test(child.name)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "atrule" && child.name === name) {
            return callback(child, i2);
          }
        });
      }
      walkComments(callback) {
        return this.walk((child, i2) => {
          if (child.type === "comment") {
            return callback(child, i2);
          }
        });
      }
      walkDecls(prop, callback) {
        if (!callback) {
          callback = prop;
          return this.walk((child, i2) => {
            if (child.type === "decl") {
              return callback(child, i2);
            }
          });
        }
        if (prop instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "decl" && prop.test(child.prop)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "decl" && child.prop === prop) {
            return callback(child, i2);
          }
        });
      }
      walkRules(selector2, callback) {
        if (!callback) {
          callback = selector2;
          return this.walk((child, i2) => {
            if (child.type === "rule") {
              return callback(child, i2);
            }
          });
        }
        if (selector2 instanceof RegExp) {
          return this.walk((child, i2) => {
            if (child.type === "rule" && selector2.test(child.selector)) {
              return callback(child, i2);
            }
          });
        }
        return this.walk((child, i2) => {
          if (child.type === "rule" && child.selector === selector2) {
            return callback(child, i2);
          }
        });
      }
      get first() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[0];
      }
      get last() {
        if (!this.proxyOf.nodes) return void 0;
        return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
      }
    }
    Container.registerParse = (dependant) => {
      parse = dependant;
    };
    Container.registerRule = (dependant) => {
      Rule = dependant;
    };
    Container.registerAtRule = (dependant) => {
      AtRule = dependant;
    };
    Container.registerRoot = (dependant) => {
      Root = dependant;
    };
    container2 = Container;
    Container.default = Container;
    Container.rebuild = (node22) => {
      if (node22.type === "atrule") {
        Object.setPrototypeOf(node22, AtRule.prototype);
      } else if (node22.type === "rule") {
        Object.setPrototypeOf(node22, Rule.prototype);
      } else if (node22.type === "decl") {
        Object.setPrototypeOf(node22, Declaration.prototype);
      } else if (node22.type === "comment") {
        Object.setPrototypeOf(node22, Comment.prototype);
      } else if (node22.type === "root") {
        Object.setPrototypeOf(node22, Root.prototype);
      }
      node22[my] = true;
      if (node22.nodes) {
        node22.nodes.forEach((child) => {
          Container.rebuild(child);
        });
      }
    };
    return container2;
  }
  function requireDocument2() {
    if (hasRequiredDocument2) return document$12;
    hasRequiredDocument2 = 1;
    let Container = requireContainer2();
    let LazyResult, Processor;
    class Document2 extends Container {
      constructor(defaults) {
        super({ type: "document", ...defaults });
        if (!this.nodes) {
          this.nodes = [];
        }
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Document2.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Document2.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    document$12 = Document2;
    Document2.default = Document2;
    return document$12;
  }
  function requireWarnOnce2() {
    if (hasRequiredWarnOnce2) return warnOnce2;
    hasRequiredWarnOnce2 = 1;
    let printed = {};
    warnOnce2 = function warnOnce22(message) {
      if (printed[message]) return;
      printed[message] = true;
      if (typeof console !== "undefined" && console.warn) {
        console.warn(message);
      }
    };
    return warnOnce2;
  }
  function requireWarning2() {
    if (hasRequiredWarning2) return warning2;
    hasRequiredWarning2 = 1;
    class Warning {
      constructor(text, opts = {}) {
        this.type = "warning";
        this.text = text;
        if (opts.node && opts.node.source) {
          let range = opts.node.rangeBy(opts);
          this.line = range.start.line;
          this.column = range.start.column;
          this.endLine = range.end.line;
          this.endColumn = range.end.column;
        }
        for (let opt in opts) this[opt] = opts[opt];
      }
      toString() {
        if (this.node) {
          return this.node.error(this.text, {
            index: this.index,
            plugin: this.plugin,
            word: this.word
          }).message;
        }
        if (this.plugin) {
          return this.plugin + ": " + this.text;
        }
        return this.text;
      }
    }
    warning2 = Warning;
    Warning.default = Warning;
    return warning2;
  }
  function requireResult2() {
    if (hasRequiredResult2) return result2;
    hasRequiredResult2 = 1;
    let Warning = requireWarning2();
    class Result {
      constructor(processor22, root22, opts) {
        this.processor = processor22;
        this.messages = [];
        this.root = root22;
        this.opts = opts;
        this.css = void 0;
        this.map = void 0;
      }
      toString() {
        return this.css;
      }
      warn(text, opts = {}) {
        if (!opts.plugin) {
          if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
            opts.plugin = this.lastPlugin.postcssPlugin;
          }
        }
        let warning22 = new Warning(text, opts);
        this.messages.push(warning22);
        return warning22;
      }
      warnings() {
        return this.messages.filter((i2) => i2.type === "warning");
      }
      get content() {
        return this.css;
      }
    }
    result2 = Result;
    Result.default = Result;
    return result2;
  }
  function requireTokenize2() {
    if (hasRequiredTokenize2) return tokenize2;
    hasRequiredTokenize2 = 1;
    const SINGLE_QUOTE = "'".charCodeAt(0);
    const DOUBLE_QUOTE = '"'.charCodeAt(0);
    const BACKSLASH = "\\".charCodeAt(0);
    const SLASH = "/".charCodeAt(0);
    const NEWLINE = "\n".charCodeAt(0);
    const SPACE = " ".charCodeAt(0);
    const FEED = "\f".charCodeAt(0);
    const TAB = "	".charCodeAt(0);
    const CR = "\r".charCodeAt(0);
    const OPEN_SQUARE = "[".charCodeAt(0);
    const CLOSE_SQUARE = "]".charCodeAt(0);
    const OPEN_PARENTHESES = "(".charCodeAt(0);
    const CLOSE_PARENTHESES = ")".charCodeAt(0);
    const OPEN_CURLY = "{".charCodeAt(0);
    const CLOSE_CURLY = "}".charCodeAt(0);
    const SEMICOLON = ";".charCodeAt(0);
    const ASTERISK = "*".charCodeAt(0);
    const COLON = ":".charCodeAt(0);
    const AT = "@".charCodeAt(0);
    const RE_AT_END = /[\t\n\f\r "#'()/;[\\\]{}]/g;
    const RE_WORD_END = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
    const RE_BAD_BRACKET = /.[\r\n"'(/\\]/;
    const RE_HEX_ESCAPE = /[\da-f]/i;
    tokenize2 = function tokenizer(input22, options = {}) {
      let css = input22.css.valueOf();
      let ignore = options.ignoreErrors;
      let code, next, quote, content, escape2;
      let escaped, escapePos, prev, n22, currentToken;
      let length = css.length;
      let pos = 0;
      let buffer = [];
      let returned = [];
      function position() {
        return pos;
      }
      function unclosed(what) {
        throw input22.error("Unclosed " + what, pos);
      }
      function endOfFile() {
        return returned.length === 0 && pos >= length;
      }
      function nextToken(opts) {
        if (returned.length) return returned.pop();
        if (pos >= length) return;
        let ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
        code = css.charCodeAt(pos);
        switch (code) {
          case NEWLINE:
          case SPACE:
          case TAB:
          case CR:
          case FEED: {
            next = pos;
            do {
              next += 1;
              code = css.charCodeAt(next);
            } while (code === SPACE || code === NEWLINE || code === TAB || code === CR || code === FEED);
            currentToken = ["space", css.slice(pos, next)];
            pos = next - 1;
            break;
          }
          case OPEN_SQUARE:
          case CLOSE_SQUARE:
          case OPEN_CURLY:
          case CLOSE_CURLY:
          case COLON:
          case SEMICOLON:
          case CLOSE_PARENTHESES: {
            let controlChar = String.fromCharCode(code);
            currentToken = [controlChar, controlChar, pos];
            break;
          }
          case OPEN_PARENTHESES: {
            prev = buffer.length ? buffer.pop()[1] : "";
            n22 = css.charCodeAt(pos + 1);
            if (prev === "url" && n22 !== SINGLE_QUOTE && n22 !== DOUBLE_QUOTE && n22 !== SPACE && n22 !== NEWLINE && n22 !== TAB && n22 !== FEED && n22 !== CR) {
              next = pos;
              do {
                escaped = false;
                next = css.indexOf(")", next + 1);
                if (next === -1) {
                  if (ignore || ignoreUnclosed) {
                    next = pos;
                    break;
                  } else {
                    unclosed("bracket");
                  }
                }
                escapePos = next;
                while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                  escapePos -= 1;
                  escaped = !escaped;
                }
              } while (escaped);
              currentToken = ["brackets", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              next = css.indexOf(")", pos + 1);
              content = css.slice(pos, next + 1);
              if (next === -1 || RE_BAD_BRACKET.test(content)) {
                currentToken = ["(", "(", pos];
              } else {
                currentToken = ["brackets", content, pos, next];
                pos = next;
              }
            }
            break;
          }
          case SINGLE_QUOTE:
          case DOUBLE_QUOTE: {
            quote = code === SINGLE_QUOTE ? "'" : '"';
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(quote, next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos + 1;
                  break;
                } else {
                  unclosed("string");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = ["string", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case AT: {
            RE_AT_END.lastIndex = pos + 1;
            RE_AT_END.test(css);
            if (RE_AT_END.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_AT_END.lastIndex - 2;
            }
            currentToken = ["at-word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          case BACKSLASH: {
            next = pos;
            escape2 = true;
            while (css.charCodeAt(next + 1) === BACKSLASH) {
              next += 1;
              escape2 = !escape2;
            }
            code = css.charCodeAt(next + 1);
            if (escape2 && code !== SLASH && code !== SPACE && code !== NEWLINE && code !== TAB && code !== CR && code !== FEED) {
              next += 1;
              if (RE_HEX_ESCAPE.test(css.charAt(next))) {
                while (RE_HEX_ESCAPE.test(css.charAt(next + 1))) {
                  next += 1;
                }
                if (css.charCodeAt(next + 1) === SPACE) {
                  next += 1;
                }
              }
            }
            currentToken = ["word", css.slice(pos, next + 1), pos, next];
            pos = next;
            break;
          }
          default: {
            if (code === SLASH && css.charCodeAt(pos + 1) === ASTERISK) {
              next = css.indexOf("*/", pos + 2) + 1;
              if (next === 0) {
                if (ignore || ignoreUnclosed) {
                  next = css.length;
                } else {
                  unclosed("comment");
                }
              }
              currentToken = ["comment", css.slice(pos, next + 1), pos, next];
              pos = next;
            } else {
              RE_WORD_END.lastIndex = pos + 1;
              RE_WORD_END.test(css);
              if (RE_WORD_END.lastIndex === 0) {
                next = css.length - 1;
              } else {
                next = RE_WORD_END.lastIndex - 2;
              }
              currentToken = ["word", css.slice(pos, next + 1), pos, next];
              buffer.push(currentToken);
              pos = next;
            }
            break;
          }
        }
        pos++;
        return currentToken;
      }
      function back(token) {
        returned.push(token);
      }
      return {
        back,
        endOfFile,
        nextToken,
        position
      };
    };
    return tokenize2;
  }
  function requireAtRule2() {
    if (hasRequiredAtRule2) return atRule2;
    hasRequiredAtRule2 = 1;
    let Container = requireContainer2();
    class AtRule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "atrule";
      }
      append(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.append(...children);
      }
      prepend(...children) {
        if (!this.proxyOf.nodes) this.nodes = [];
        return super.prepend(...children);
      }
    }
    atRule2 = AtRule;
    AtRule.default = AtRule;
    Container.registerAtRule(AtRule);
    return atRule2;
  }
  function requireRoot2() {
    if (hasRequiredRoot2) return root2;
    hasRequiredRoot2 = 1;
    let Container = requireContainer2();
    let LazyResult, Processor;
    class Root extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "root";
        if (!this.nodes) this.nodes = [];
      }
      normalize(child, sample, type) {
        let nodes = super.normalize(child);
        if (sample) {
          if (type === "prepend") {
            if (this.nodes.length > 1) {
              sample.raws.before = this.nodes[1].raws.before;
            } else {
              delete sample.raws.before;
            }
          } else if (this.first !== sample) {
            for (let node22 of nodes) {
              node22.raws.before = sample.raws.before;
            }
          }
        }
        return nodes;
      }
      removeChild(child, ignore) {
        let index22 = this.index(child);
        if (!ignore && index22 === 0 && this.nodes.length > 1) {
          this.nodes[1].raws.before = this.nodes[index22].raws.before;
        }
        return super.removeChild(child);
      }
      toResult(opts = {}) {
        let lazy = new LazyResult(new Processor(), this, opts);
        return lazy.stringify();
      }
    }
    Root.registerLazyResult = (dependant) => {
      LazyResult = dependant;
    };
    Root.registerProcessor = (dependant) => {
      Processor = dependant;
    };
    root2 = Root;
    Root.default = Root;
    Container.registerRoot(Root);
    return root2;
  }
  function requireList2() {
    if (hasRequiredList2) return list_12;
    hasRequiredList2 = 1;
    let list = {
      comma(string) {
        return list.split(string, [","], true);
      },
      space(string) {
        let spaces = [" ", "\n", "	"];
        return list.split(string, spaces);
      },
      split(string, separators, last) {
        let array = [];
        let current = "";
        let split = false;
        let func = 0;
        let inQuote = false;
        let prevQuote = "";
        let escape2 = false;
        for (let letter of string) {
          if (escape2) {
            escape2 = false;
          } else if (letter === "\\") {
            escape2 = true;
          } else if (inQuote) {
            if (letter === prevQuote) {
              inQuote = false;
            }
          } else if (letter === '"' || letter === "'") {
            inQuote = true;
            prevQuote = letter;
          } else if (letter === "(") {
            func += 1;
          } else if (letter === ")") {
            if (func > 0) func -= 1;
          } else if (func === 0) {
            if (separators.includes(letter)) split = true;
          }
          if (split) {
            if (current !== "") array.push(current.trim());
            current = "";
            split = false;
          } else {
            current += letter;
          }
        }
        if (last || current !== "") array.push(current.trim());
        return array;
      }
    };
    list_12 = list;
    list.default = list;
    return list_12;
  }
  function requireRule2() {
    if (hasRequiredRule2) return rule2;
    hasRequiredRule2 = 1;
    let Container = requireContainer2();
    let list = requireList2();
    class Rule extends Container {
      constructor(defaults) {
        super(defaults);
        this.type = "rule";
        if (!this.nodes) this.nodes = [];
      }
      get selectors() {
        return list.comma(this.selector);
      }
      set selectors(values) {
        let match = this.selector ? this.selector.match(/,\s*/) : null;
        let sep = match ? match[0] : "," + this.raw("between", "beforeOpen");
        this.selector = values.join(sep);
      }
    }
    rule2 = Rule;
    Rule.default = Rule;
    Container.registerRule(Rule);
    return rule2;
  }
  function requireParser2() {
    if (hasRequiredParser2) return parser2;
    hasRequiredParser2 = 1;
    let Declaration = requireDeclaration2();
    let tokenizer = requireTokenize2();
    let Comment = requireComment2();
    let AtRule = requireAtRule2();
    let Root = requireRoot2();
    let Rule = requireRule2();
    const SAFE_COMMENT_NEIGHBOR = {
      empty: true,
      space: true
    };
    function findLastWithPosition(tokens) {
      for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
        let token = tokens[i2];
        let pos = token[3] || token[2];
        if (pos) return pos;
      }
    }
    class Parser {
      constructor(input22) {
        this.input = input22;
        this.root = new Root();
        this.current = this.root;
        this.spaces = "";
        this.semicolon = false;
        this.createTokenizer();
        this.root.source = { input: input22, start: { column: 1, line: 1, offset: 0 } };
      }
      atrule(token) {
        let node22 = new AtRule();
        node22.name = token[1].slice(1);
        if (node22.name === "") {
          this.unnamedAtrule(node22, token);
        }
        this.init(node22, token[2]);
        let type;
        let prev;
        let shift;
        let last = false;
        let open = false;
        let params = [];
        let brackets = [];
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          type = token[0];
          if (type === "(" || type === "[") {
            brackets.push(type === "(" ? ")" : "]");
          } else if (type === "{" && brackets.length > 0) {
            brackets.push("}");
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
          }
          if (brackets.length === 0) {
            if (type === ";") {
              node22.source.end = this.getPosition(token[2]);
              node22.source.end.offset++;
              this.semicolon = true;
              break;
            } else if (type === "{") {
              open = true;
              break;
            } else if (type === "}") {
              if (params.length > 0) {
                shift = params.length - 1;
                prev = params[shift];
                while (prev && prev[0] === "space") {
                  prev = params[--shift];
                }
                if (prev) {
                  node22.source.end = this.getPosition(prev[3] || prev[2]);
                  node22.source.end.offset++;
                }
              }
              this.end(token);
              break;
            } else {
              params.push(token);
            }
          } else {
            params.push(token);
          }
          if (this.tokenizer.endOfFile()) {
            last = true;
            break;
          }
        }
        node22.raws.between = this.spacesAndCommentsFromEnd(params);
        if (params.length) {
          node22.raws.afterName = this.spacesAndCommentsFromStart(params);
          this.raw(node22, "params", params);
          if (last) {
            token = params[params.length - 1];
            node22.source.end = this.getPosition(token[3] || token[2]);
            node22.source.end.offset++;
            this.spaces = node22.raws.between;
            node22.raws.between = "";
          }
        } else {
          node22.raws.afterName = "";
          node22.params = "";
        }
        if (open) {
          node22.nodes = [];
          this.current = node22;
        }
      }
      checkMissedSemicolon(tokens) {
        let colon = this.colon(tokens);
        if (colon === false) return;
        let founded = 0;
        let token;
        for (let j = colon - 1; j >= 0; j--) {
          token = tokens[j];
          if (token[0] !== "space") {
            founded += 1;
            if (founded === 2) break;
          }
        }
        throw this.input.error(
          "Missed semicolon",
          token[0] === "word" ? token[3] + 1 : token[2]
        );
      }
      colon(tokens) {
        let brackets = 0;
        let token, type, prev;
        for (let [i2, element] of tokens.entries()) {
          token = element;
          type = token[0];
          if (type === "(") {
            brackets += 1;
          }
          if (type === ")") {
            brackets -= 1;
          }
          if (brackets === 0 && type === ":") {
            if (!prev) {
              this.doubleColon(token);
            } else if (prev[0] === "word" && prev[1] === "progid") {
              continue;
            } else {
              return i2;
            }
          }
          prev = token;
        }
        return false;
      }
      comment(token) {
        let node22 = new Comment();
        this.init(node22, token[2]);
        node22.source.end = this.getPosition(token[3] || token[2]);
        node22.source.end.offset++;
        let text = token[1].slice(2, -2);
        if (/^\s*$/.test(text)) {
          node22.text = "";
          node22.raws.left = text;
          node22.raws.right = "";
        } else {
          let match = text.match(/^(\s*)([^]*\S)(\s*)$/);
          node22.text = match[2];
          node22.raws.left = match[1];
          node22.raws.right = match[3];
        }
      }
      createTokenizer() {
        this.tokenizer = tokenizer(this.input);
      }
      decl(tokens, customProperty) {
        let node22 = new Declaration();
        this.init(node22, tokens[0][2]);
        let last = tokens[tokens.length - 1];
        if (last[0] === ";") {
          this.semicolon = true;
          tokens.pop();
        }
        node22.source.end = this.getPosition(
          last[3] || last[2] || findLastWithPosition(tokens)
        );
        node22.source.end.offset++;
        while (tokens[0][0] !== "word") {
          if (tokens.length === 1) this.unknownWord(tokens);
          node22.raws.before += tokens.shift()[1];
        }
        node22.source.start = this.getPosition(tokens[0][2]);
        node22.prop = "";
        while (tokens.length) {
          let type = tokens[0][0];
          if (type === ":" || type === "space" || type === "comment") {
            break;
          }
          node22.prop += tokens.shift()[1];
        }
        node22.raws.between = "";
        let token;
        while (tokens.length) {
          token = tokens.shift();
          if (token[0] === ":") {
            node22.raws.between += token[1];
            break;
          } else {
            if (token[0] === "word" && /\w/.test(token[1])) {
              this.unknownWord([token]);
            }
            node22.raws.between += token[1];
          }
        }
        if (node22.prop[0] === "_" || node22.prop[0] === "*") {
          node22.raws.before += node22.prop[0];
          node22.prop = node22.prop.slice(1);
        }
        let firstSpaces = [];
        let next;
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          firstSpaces.push(tokens.shift());
        }
        this.precheckMissedSemicolon(tokens);
        for (let i2 = tokens.length - 1; i2 >= 0; i2--) {
          token = tokens[i2];
          if (token[1].toLowerCase() === "!important") {
            node22.important = true;
            let string = this.stringFrom(tokens, i2);
            string = this.spacesFromEnd(tokens) + string;
            if (string !== " !important") node22.raws.important = string;
            break;
          } else if (token[1].toLowerCase() === "important") {
            let cache = tokens.slice(0);
            let str = "";
            for (let j = i2; j > 0; j--) {
              let type = cache[j][0];
              if (str.trim().indexOf("!") === 0 && type !== "space") {
                break;
              }
              str = cache.pop()[1] + str;
            }
            if (str.trim().indexOf("!") === 0) {
              node22.important = true;
              node22.raws.important = str;
              tokens = cache;
            }
          }
          if (token[0] !== "space" && token[0] !== "comment") {
            break;
          }
        }
        let hasWord = tokens.some((i2) => i2[0] !== "space" && i2[0] !== "comment");
        if (hasWord) {
          node22.raws.between += firstSpaces.map((i2) => i2[1]).join("");
          firstSpaces = [];
        }
        this.raw(node22, "value", firstSpaces.concat(tokens), customProperty);
        if (node22.value.includes(":") && !customProperty) {
          this.checkMissedSemicolon(tokens);
        }
      }
      doubleColon(token) {
        throw this.input.error(
          "Double colon",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
      emptyRule(token) {
        let node22 = new Rule();
        this.init(node22, token[2]);
        node22.selector = "";
        node22.raws.between = "";
        this.current = node22;
      }
      end(token) {
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.semicolon = false;
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.spaces = "";
        if (this.current.parent) {
          this.current.source.end = this.getPosition(token[2]);
          this.current.source.end.offset++;
          this.current = this.current.parent;
        } else {
          this.unexpectedClose(token);
        }
      }
      endFile() {
        if (this.current.parent) this.unclosedBlock();
        if (this.current.nodes && this.current.nodes.length) {
          this.current.raws.semicolon = this.semicolon;
        }
        this.current.raws.after = (this.current.raws.after || "") + this.spaces;
        this.root.source.end = this.getPosition(this.tokenizer.position());
      }
      freeSemicolon(token) {
        this.spaces += token[1];
        if (this.current.nodes) {
          let prev = this.current.nodes[this.current.nodes.length - 1];
          if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
            prev.raws.ownSemicolon = this.spaces;
            this.spaces = "";
          }
        }
      }
      // Helpers
      getPosition(offset) {
        let pos = this.input.fromOffset(offset);
        return {
          column: pos.col,
          line: pos.line,
          offset
        };
      }
      init(node22, offset) {
        this.current.push(node22);
        node22.source = {
          input: this.input,
          start: this.getPosition(offset)
        };
        node22.raws.before = this.spaces;
        this.spaces = "";
        if (node22.type !== "comment") this.semicolon = false;
      }
      other(start3) {
        let end = false;
        let type = null;
        let colon = false;
        let bracket = null;
        let brackets = [];
        let customProperty = start3[1].startsWith("--");
        let tokens = [];
        let token = start3;
        while (token) {
          type = token[0];
          tokens.push(token);
          if (type === "(" || type === "[") {
            if (!bracket) bracket = token;
            brackets.push(type === "(" ? ")" : "]");
          } else if (customProperty && colon && type === "{") {
            if (!bracket) bracket = token;
            brackets.push("}");
          } else if (brackets.length === 0) {
            if (type === ";") {
              if (colon) {
                this.decl(tokens, customProperty);
                return;
              } else {
                break;
              }
            } else if (type === "{") {
              this.rule(tokens);
              return;
            } else if (type === "}") {
              this.tokenizer.back(tokens.pop());
              end = true;
              break;
            } else if (type === ":") {
              colon = true;
            }
          } else if (type === brackets[brackets.length - 1]) {
            brackets.pop();
            if (brackets.length === 0) bracket = null;
          }
          token = this.tokenizer.nextToken();
        }
        if (this.tokenizer.endOfFile()) end = true;
        if (brackets.length > 0) this.unclosedBracket(bracket);
        if (end && colon) {
          if (!customProperty) {
            while (tokens.length) {
              token = tokens[tokens.length - 1][0];
              if (token !== "space" && token !== "comment") break;
              this.tokenizer.back(tokens.pop());
            }
          }
          this.decl(tokens, customProperty);
        } else {
          this.unknownWord(tokens);
        }
      }
      parse() {
        let token;
        while (!this.tokenizer.endOfFile()) {
          token = this.tokenizer.nextToken();
          switch (token[0]) {
            case "space":
              this.spaces += token[1];
              break;
            case ";":
              this.freeSemicolon(token);
              break;
            case "}":
              this.end(token);
              break;
            case "comment":
              this.comment(token);
              break;
            case "at-word":
              this.atrule(token);
              break;
            case "{":
              this.emptyRule(token);
              break;
            default:
              this.other(token);
              break;
          }
        }
        this.endFile();
      }
      precheckMissedSemicolon() {
      }
      raw(node22, prop, tokens, customProperty) {
        let token, type;
        let length = tokens.length;
        let value = "";
        let clean = true;
        let next, prev;
        for (let i2 = 0; i2 < length; i2 += 1) {
          token = tokens[i2];
          type = token[0];
          if (type === "space" && i2 === length - 1 && !customProperty) {
            clean = false;
          } else if (type === "comment") {
            prev = tokens[i2 - 1] ? tokens[i2 - 1][0] : "empty";
            next = tokens[i2 + 1] ? tokens[i2 + 1][0] : "empty";
            if (!SAFE_COMMENT_NEIGHBOR[prev] && !SAFE_COMMENT_NEIGHBOR[next]) {
              if (value.slice(-1) === ",") {
                clean = false;
              } else {
                value += token[1];
              }
            } else {
              clean = false;
            }
          } else {
            value += token[1];
          }
        }
        if (!clean) {
          let raw = tokens.reduce((all, i2) => all + i2[1], "");
          node22.raws[prop] = { raw, value };
        }
        node22[prop] = value;
      }
      rule(tokens) {
        tokens.pop();
        let node22 = new Rule();
        this.init(node22, tokens[0][2]);
        node22.raws.between = this.spacesAndCommentsFromEnd(tokens);
        this.raw(node22, "selector", tokens);
        this.current = node22;
      }
      spacesAndCommentsFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space" && lastTokenType !== "comment") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      // Errors
      spacesAndCommentsFromStart(tokens) {
        let next;
        let spaces = "";
        while (tokens.length) {
          next = tokens[0][0];
          if (next !== "space" && next !== "comment") break;
          spaces += tokens.shift()[1];
        }
        return spaces;
      }
      spacesFromEnd(tokens) {
        let lastTokenType;
        let spaces = "";
        while (tokens.length) {
          lastTokenType = tokens[tokens.length - 1][0];
          if (lastTokenType !== "space") break;
          spaces = tokens.pop()[1] + spaces;
        }
        return spaces;
      }
      stringFrom(tokens, from) {
        let result22 = "";
        for (let i2 = from; i2 < tokens.length; i2++) {
          result22 += tokens[i2][1];
        }
        tokens.splice(from, tokens.length - from);
        return result22;
      }
      unclosedBlock() {
        let pos = this.current.source.start;
        throw this.input.error("Unclosed block", pos.line, pos.column);
      }
      unclosedBracket(bracket) {
        throw this.input.error(
          "Unclosed bracket",
          { offset: bracket[2] },
          { offset: bracket[2] + 1 }
        );
      }
      unexpectedClose(token) {
        throw this.input.error(
          "Unexpected }",
          { offset: token[2] },
          { offset: token[2] + 1 }
        );
      }
      unknownWord(tokens) {
        throw this.input.error(
          "Unknown word",
          { offset: tokens[0][2] },
          { offset: tokens[0][2] + tokens[0][1].length }
        );
      }
      unnamedAtrule(node22, token) {
        throw this.input.error(
          "At-rule without name",
          { offset: token[2] },
          { offset: token[2] + token[1].length }
        );
      }
    }
    parser2 = Parser;
    return parser2;
  }
  function requireParse2() {
    if (hasRequiredParse2) return parse_12;
    hasRequiredParse2 = 1;
    let Container = requireContainer2();
    let Parser = requireParser2();
    let Input = requireInput2();
    function parse(css, opts) {
      let input22 = new Input(css, opts);
      let parser22 = new Parser(input22);
      try {
        parser22.parse();
      } catch (e2) {
        if (true) {
          if (e2.name === "CssSyntaxError" && opts && opts.from) {
            if (/\.scss$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
            } else if (/\.sass/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
            } else if (/\.less$/i.test(opts.from)) {
              e2.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
            }
          }
        }
        throw e2;
      }
      return parser22.root;
    }
    parse_12 = parse;
    parse.default = parse;
    Container.registerParse(parse);
    return parse_12;
  }
  function requireLazyResult2() {
    if (hasRequiredLazyResult2) return lazyResult2;
    hasRequiredLazyResult2 = 1;
    let { isClean, my } = requireSymbols2();
    let MapGenerator = requireMapGenerator2();
    let stringify2 = requireStringify2();
    let Container = requireContainer2();
    let Document2 = requireDocument2();
    let warnOnce22 = requireWarnOnce2();
    let Result = requireResult2();
    let parse = requireParse2();
    let Root = requireRoot2();
    const TYPE_TO_CLASS_NAME = {
      atrule: "AtRule",
      comment: "Comment",
      decl: "Declaration",
      document: "Document",
      root: "Root",
      rule: "Rule"
    };
    const PLUGIN_PROPS = {
      AtRule: true,
      AtRuleExit: true,
      Comment: true,
      CommentExit: true,
      Declaration: true,
      DeclarationExit: true,
      Document: true,
      DocumentExit: true,
      Once: true,
      OnceExit: true,
      postcssPlugin: true,
      prepare: true,
      Root: true,
      RootExit: true,
      Rule: true,
      RuleExit: true
    };
    const NOT_VISITORS = {
      Once: true,
      postcssPlugin: true,
      prepare: true
    };
    const CHILDREN = 0;
    function isPromise(obj) {
      return typeof obj === "object" && typeof obj.then === "function";
    }
    function getEvents(node22) {
      let key = false;
      let type = TYPE_TO_CLASS_NAME[node22.type];
      if (node22.type === "decl") {
        key = node22.prop.toLowerCase();
      } else if (node22.type === "atrule") {
        key = node22.name.toLowerCase();
      }
      if (key && node22.append) {
        return [
          type,
          type + "-" + key,
          CHILDREN,
          type + "Exit",
          type + "Exit-" + key
        ];
      } else if (key) {
        return [type, type + "-" + key, type + "Exit", type + "Exit-" + key];
      } else if (node22.append) {
        return [type, CHILDREN, type + "Exit"];
      } else {
        return [type, type + "Exit"];
      }
    }
    function toStack(node22) {
      let events;
      if (node22.type === "document") {
        events = ["Document", CHILDREN, "DocumentExit"];
      } else if (node22.type === "root") {
        events = ["Root", CHILDREN, "RootExit"];
      } else {
        events = getEvents(node22);
      }
      return {
        eventIndex: 0,
        events,
        iterator: 0,
        node: node22,
        visitorIndex: 0,
        visitors: []
      };
    }
    function cleanMarks(node22) {
      node22[isClean] = false;
      if (node22.nodes) node22.nodes.forEach((i2) => cleanMarks(i2));
      return node22;
    }
    let postcss22 = {};
    class LazyResult {
      constructor(processor22, css, opts) {
        this.stringified = false;
        this.processed = false;
        let root22;
        if (typeof css === "object" && css !== null && (css.type === "root" || css.type === "document")) {
          root22 = cleanMarks(css);
        } else if (css instanceof LazyResult || css instanceof Result) {
          root22 = cleanMarks(css.root);
          if (css.map) {
            if (typeof opts.map === "undefined") opts.map = {};
            if (!opts.map.inline) opts.map.inline = false;
            opts.map.prev = css.map;
          }
        } else {
          let parser22 = parse;
          if (opts.syntax) parser22 = opts.syntax.parse;
          if (opts.parser) parser22 = opts.parser;
          if (parser22.parse) parser22 = parser22.parse;
          try {
            root22 = parser22(css, opts);
          } catch (error) {
            this.processed = true;
            this.error = error;
          }
          if (root22 && !root22[my]) {
            Container.rebuild(root22);
          }
        }
        this.result = new Result(processor22, root22, opts);
        this.helpers = { ...postcss22, postcss: postcss22, result: this.result };
        this.plugins = this.processor.plugins.map((plugin) => {
          if (typeof plugin === "object" && plugin.prepare) {
            return { ...plugin, ...plugin.prepare(this.result) };
          } else {
            return plugin;
          }
        });
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        if (this.processed) return Promise.resolve(this.result);
        if (!this.processing) {
          this.processing = this.runAsync();
        }
        return this.processing;
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      getAsyncError() {
        throw new Error("Use process(css).then(cb) to work with async plugins");
      }
      handleError(error, node22) {
        let plugin = this.result.lastPlugin;
        try {
          if (node22) node22.addToError(error);
          this.error = error;
          if (error.name === "CssSyntaxError" && !error.plugin) {
            error.plugin = plugin.postcssPlugin;
            error.setMessage();
          } else if (plugin.postcssVersion) {
            if (true) {
              let pluginName = plugin.postcssPlugin;
              let pluginVer = plugin.postcssVersion;
              let runtimeVer = this.result.processor.version;
              let a2 = pluginVer.split(".");
              let b2 = runtimeVer.split(".");
              if (a2[0] !== b2[0] || parseInt(a2[1]) > parseInt(b2[1])) {
                console.error(
                  "Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below."
                );
              }
            }
          }
        } catch (err) {
          if (console && console.error) console.error(err);
        }
        return error;
      }
      prepareVisitors() {
        this.listeners = {};
        let add2 = (plugin, type, cb) => {
          if (!this.listeners[type]) this.listeners[type] = [];
          this.listeners[type].push([plugin, cb]);
        };
        for (let plugin of this.plugins) {
          if (typeof plugin === "object") {
            for (let event in plugin) {
              if (!PLUGIN_PROPS[event] && /^[A-Z]/.test(event)) {
                throw new Error(
                  `Unknown event ${event} in ${plugin.postcssPlugin}. Try to update PostCSS (${this.processor.version} now).`
                );
              }
              if (!NOT_VISITORS[event]) {
                if (typeof plugin[event] === "object") {
                  for (let filter in plugin[event]) {
                    if (filter === "*") {
                      add2(plugin, event, plugin[event][filter]);
                    } else {
                      add2(
                        plugin,
                        event + "-" + filter.toLowerCase(),
                        plugin[event][filter]
                      );
                    }
                  }
                } else if (typeof plugin[event] === "function") {
                  add2(plugin, event, plugin[event]);
                }
              }
            }
          }
        }
        this.hasListener = Object.keys(this.listeners).length > 0;
      }
      async runAsync() {
        this.plugin = 0;
        for (let i2 = 0; i2 < this.plugins.length; i2++) {
          let plugin = this.plugins[i2];
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            try {
              await promise;
            } catch (error) {
              throw this.handleError(error);
            }
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            let stack = [toStack(root22)];
            while (stack.length > 0) {
              let promise = this.visitTick(stack);
              if (isPromise(promise)) {
                try {
                  await promise;
                } catch (e2) {
                  let node22 = stack[stack.length - 1].node;
                  throw this.handleError(e2, node22);
                }
              }
            }
          }
          if (this.listeners.OnceExit) {
            for (let [plugin, visitor] of this.listeners.OnceExit) {
              this.result.lastPlugin = plugin;
              try {
                if (root22.type === "document") {
                  let roots = root22.nodes.map(
                    (subRoot) => visitor(subRoot, this.helpers)
                  );
                  await Promise.all(roots);
                } else {
                  await visitor(root22, this.helpers);
                }
              } catch (e2) {
                throw this.handleError(e2);
              }
            }
          }
        }
        this.processed = true;
        return this.stringify();
      }
      runOnRoot(plugin) {
        this.result.lastPlugin = plugin;
        try {
          if (typeof plugin === "object" && plugin.Once) {
            if (this.result.root.type === "document") {
              let roots = this.result.root.nodes.map(
                (root22) => plugin.Once(root22, this.helpers)
              );
              if (isPromise(roots[0])) {
                return Promise.all(roots);
              }
              return roots;
            }
            return plugin.Once(this.result.root, this.helpers);
          } else if (typeof plugin === "function") {
            return plugin(this.result.root, this.result);
          }
        } catch (error) {
          throw this.handleError(error);
        }
      }
      stringify() {
        if (this.error) throw this.error;
        if (this.stringified) return this.result;
        this.stringified = true;
        this.sync();
        let opts = this.result.opts;
        let str = stringify2;
        if (opts.syntax) str = opts.syntax.stringify;
        if (opts.stringifier) str = opts.stringifier;
        if (str.stringify) str = str.stringify;
        let map = new MapGenerator(str, this.result.root, this.result.opts);
        let data = map.generate();
        this.result.css = data[0];
        this.result.map = data[1];
        return this.result;
      }
      sync() {
        if (this.error) throw this.error;
        if (this.processed) return this.result;
        this.processed = true;
        if (this.processing) {
          throw this.getAsyncError();
        }
        for (let plugin of this.plugins) {
          let promise = this.runOnRoot(plugin);
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
        this.prepareVisitors();
        if (this.hasListener) {
          let root22 = this.result.root;
          while (!root22[isClean]) {
            root22[isClean] = true;
            this.walkSync(root22);
          }
          if (this.listeners.OnceExit) {
            if (root22.type === "document") {
              for (let subRoot of root22.nodes) {
                this.visitSync(this.listeners.OnceExit, subRoot);
              }
            } else {
              this.visitSync(this.listeners.OnceExit, root22);
            }
          }
        }
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this.opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this.css;
      }
      visitSync(visitors, node22) {
        for (let [plugin, visitor] of visitors) {
          this.result.lastPlugin = plugin;
          let promise;
          try {
            promise = visitor(node22, this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22.proxyOf);
          }
          if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
            return true;
          }
          if (isPromise(promise)) {
            throw this.getAsyncError();
          }
        }
      }
      visitTick(stack) {
        let visit2 = stack[stack.length - 1];
        let { node: node22, visitors } = visit2;
        if (node22.type !== "root" && node22.type !== "document" && !node22.parent) {
          stack.pop();
          return;
        }
        if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
          let [plugin, visitor] = visitors[visit2.visitorIndex];
          visit2.visitorIndex += 1;
          if (visit2.visitorIndex === visitors.length) {
            visit2.visitors = [];
            visit2.visitorIndex = 0;
          }
          this.result.lastPlugin = plugin;
          try {
            return visitor(node22.toProxy(), this.helpers);
          } catch (e2) {
            throw this.handleError(e2, node22);
          }
        }
        if (visit2.iterator !== 0) {
          let iterator = visit2.iterator;
          let child;
          while (child = node22.nodes[node22.indexes[iterator]]) {
            node22.indexes[iterator] += 1;
            if (!child[isClean]) {
              child[isClean] = true;
              stack.push(toStack(child));
              return;
            }
          }
          visit2.iterator = 0;
          delete node22.indexes[iterator];
        }
        let events = visit2.events;
        while (visit2.eventIndex < events.length) {
          let event = events[visit2.eventIndex];
          visit2.eventIndex += 1;
          if (event === CHILDREN) {
            if (node22.nodes && node22.nodes.length) {
              node22[isClean] = true;
              visit2.iterator = node22.getIterator();
            }
            return;
          } else if (this.listeners[event]) {
            visit2.visitors = this.listeners[event];
            return;
          }
        }
        stack.pop();
      }
      walkSync(node22) {
        node22[isClean] = true;
        let events = getEvents(node22);
        for (let event of events) {
          if (event === CHILDREN) {
            if (node22.nodes) {
              node22.each((child) => {
                if (!child[isClean]) this.walkSync(child);
              });
            }
          } else {
            let visitors = this.listeners[event];
            if (visitors) {
              if (this.visitSync(visitors, node22.toProxy())) return;
            }
          }
        }
      }
      warnings() {
        return this.sync().warnings();
      }
      get content() {
        return this.stringify().content;
      }
      get css() {
        return this.stringify().css;
      }
      get map() {
        return this.stringify().map;
      }
      get messages() {
        return this.sync().messages;
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        return this.sync().root;
      }
      get [Symbol.toStringTag]() {
        return "LazyResult";
      }
    }
    LazyResult.registerPostcss = (dependant) => {
      postcss22 = dependant;
    };
    lazyResult2 = LazyResult;
    LazyResult.default = LazyResult;
    Root.registerLazyResult(LazyResult);
    Document2.registerLazyResult(LazyResult);
    return lazyResult2;
  }
  function requireNoWorkResult2() {
    if (hasRequiredNoWorkResult2) return noWorkResult2;
    hasRequiredNoWorkResult2 = 1;
    let MapGenerator = requireMapGenerator2();
    let stringify2 = requireStringify2();
    let warnOnce22 = requireWarnOnce2();
    let parse = requireParse2();
    const Result = requireResult2();
    class NoWorkResult {
      constructor(processor22, css, opts) {
        css = css.toString();
        this.stringified = false;
        this._processor = processor22;
        this._css = css;
        this._opts = opts;
        this._map = void 0;
        let root22;
        let str = stringify2;
        this.result = new Result(this._processor, root22, this._opts);
        this.result.css = css;
        let self2 = this;
        Object.defineProperty(this.result, "root", {
          get() {
            return self2.root;
          }
        });
        let map = new MapGenerator(str, root22, this._opts, css);
        if (map.isMap()) {
          let [generatedCSS, generatedMap] = map.generate();
          if (generatedCSS) {
            this.result.css = generatedCSS;
          }
          if (generatedMap) {
            this.result.map = generatedMap;
          }
        } else {
          map.clearAnnotation();
          this.result.css = map.css;
        }
      }
      async() {
        if (this.error) return Promise.reject(this.error);
        return Promise.resolve(this.result);
      }
      catch(onRejected) {
        return this.async().catch(onRejected);
      }
      finally(onFinally) {
        return this.async().then(onFinally, onFinally);
      }
      sync() {
        if (this.error) throw this.error;
        return this.result;
      }
      then(onFulfilled, onRejected) {
        if (true) {
          if (!("from" in this._opts)) {
            warnOnce22(
              "Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning."
            );
          }
        }
        return this.async().then(onFulfilled, onRejected);
      }
      toString() {
        return this._css;
      }
      warnings() {
        return [];
      }
      get content() {
        return this.result.css;
      }
      get css() {
        return this.result.css;
      }
      get map() {
        return this.result.map;
      }
      get messages() {
        return [];
      }
      get opts() {
        return this.result.opts;
      }
      get processor() {
        return this.result.processor;
      }
      get root() {
        if (this._root) {
          return this._root;
        }
        let root22;
        let parser22 = parse;
        try {
          root22 = parser22(this._css, this._opts);
        } catch (error) {
          this.error = error;
        }
        if (this.error) {
          throw this.error;
        } else {
          this._root = root22;
          return root22;
        }
      }
      get [Symbol.toStringTag]() {
        return "NoWorkResult";
      }
    }
    noWorkResult2 = NoWorkResult;
    NoWorkResult.default = NoWorkResult;
    return noWorkResult2;
  }
  function requireProcessor2() {
    if (hasRequiredProcessor2) return processor2;
    hasRequiredProcessor2 = 1;
    let NoWorkResult = requireNoWorkResult2();
    let LazyResult = requireLazyResult2();
    let Document2 = requireDocument2();
    let Root = requireRoot2();
    class Processor {
      constructor(plugins = []) {
        this.version = "8.4.38";
        this.plugins = this.normalize(plugins);
      }
      normalize(plugins) {
        let normalized = [];
        for (let i2 of plugins) {
          if (i2.postcss === true) {
            i2 = i2();
          } else if (i2.postcss) {
            i2 = i2.postcss;
          }
          if (typeof i2 === "object" && Array.isArray(i2.plugins)) {
            normalized = normalized.concat(i2.plugins);
          } else if (typeof i2 === "object" && i2.postcssPlugin) {
            normalized.push(i2);
          } else if (typeof i2 === "function") {
            normalized.push(i2);
          } else if (typeof i2 === "object" && (i2.parse || i2.stringify)) {
            if (true) {
              throw new Error(
                "PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation."
              );
            }
          } else {
            throw new Error(i2 + " is not a PostCSS plugin");
          }
        }
        return normalized;
      }
      process(css, opts = {}) {
        if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
          return new NoWorkResult(this, css, opts);
        } else {
          return new LazyResult(this, css, opts);
        }
      }
      use(plugin) {
        this.plugins = this.plugins.concat(this.normalize([plugin]));
        return this;
      }
    }
    processor2 = Processor;
    Processor.default = Processor;
    Root.registerProcessor(Processor);
    Document2.registerProcessor(Processor);
    return processor2;
  }
  function requireFromJSON2() {
    if (hasRequiredFromJSON2) return fromJSON_12;
    hasRequiredFromJSON2 = 1;
    let Declaration = requireDeclaration2();
    let PreviousMap = requirePreviousMap2();
    let Comment = requireComment2();
    let AtRule = requireAtRule2();
    let Input = requireInput2();
    let Root = requireRoot2();
    let Rule = requireRule2();
    function fromJSON(json, inputs) {
      if (Array.isArray(json)) return json.map((n22) => fromJSON(n22));
      let { inputs: ownInputs, ...defaults } = json;
      if (ownInputs) {
        inputs = [];
        for (let input22 of ownInputs) {
          let inputHydrated = { ...input22, __proto__: Input.prototype };
          if (inputHydrated.map) {
            inputHydrated.map = {
              ...inputHydrated.map,
              __proto__: PreviousMap.prototype
            };
          }
          inputs.push(inputHydrated);
        }
      }
      if (defaults.nodes) {
        defaults.nodes = json.nodes.map((n22) => fromJSON(n22, inputs));
      }
      if (defaults.source) {
        let { inputId, ...source } = defaults.source;
        defaults.source = source;
        if (inputId != null) {
          defaults.source.input = inputs[inputId];
        }
      }
      if (defaults.type === "root") {
        return new Root(defaults);
      } else if (defaults.type === "decl") {
        return new Declaration(defaults);
      } else if (defaults.type === "rule") {
        return new Rule(defaults);
      } else if (defaults.type === "comment") {
        return new Comment(defaults);
      } else if (defaults.type === "atrule") {
        return new AtRule(defaults);
      } else {
        throw new Error("Unknown node type: " + json.type);
      }
    }
    fromJSON_12 = fromJSON;
    fromJSON.default = fromJSON;
    return fromJSON_12;
  }
  function requirePostcss2() {
    if (hasRequiredPostcss2) return postcss_12;
    hasRequiredPostcss2 = 1;
    let CssSyntaxError = requireCssSyntaxError2();
    let Declaration = requireDeclaration2();
    let LazyResult = requireLazyResult2();
    let Container = requireContainer2();
    let Processor = requireProcessor2();
    let stringify2 = requireStringify2();
    let fromJSON = requireFromJSON2();
    let Document2 = requireDocument2();
    let Warning = requireWarning2();
    let Comment = requireComment2();
    let AtRule = requireAtRule2();
    let Result = requireResult2();
    let Input = requireInput2();
    let parse = requireParse2();
    let list = requireList2();
    let Rule = requireRule2();
    let Root = requireRoot2();
    let Node2 = requireNode2();
    function postcss22(...plugins) {
      if (plugins.length === 1 && Array.isArray(plugins[0])) {
        plugins = plugins[0];
      }
      return new Processor(plugins);
    }
    postcss22.plugin = function plugin(name, initializer) {
      let warningPrinted = false;
      function creator(...args) {
        if (console && console.warn && !warningPrinted) {
          warningPrinted = true;
          console.warn(
            name + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration"
          );
          if (process.env.LANG && process.env.LANG.startsWith("cn")) {
            console.warn(
              name + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226"
            );
          }
        }
        let transformer = initializer(...args);
        transformer.postcssPlugin = name;
        transformer.postcssVersion = new Processor().version;
        return transformer;
      }
      let cache;
      Object.defineProperty(creator, "postcss", {
        get() {
          if (!cache) cache = creator();
          return cache;
        }
      });
      creator.process = function(css, processOpts, pluginOpts) {
        return postcss22([creator(pluginOpts)]).process(css, processOpts);
      };
      return creator;
    };
    postcss22.stringify = stringify2;
    postcss22.parse = parse;
    postcss22.fromJSON = fromJSON;
    postcss22.list = list;
    postcss22.comment = (defaults) => new Comment(defaults);
    postcss22.atRule = (defaults) => new AtRule(defaults);
    postcss22.decl = (defaults) => new Declaration(defaults);
    postcss22.rule = (defaults) => new Rule(defaults);
    postcss22.root = (defaults) => new Root(defaults);
    postcss22.document = (defaults) => new Document2(defaults);
    postcss22.CssSyntaxError = CssSyntaxError;
    postcss22.Declaration = Declaration;
    postcss22.Container = Container;
    postcss22.Processor = Processor;
    postcss22.Document = Document2;
    postcss22.Comment = Comment;
    postcss22.Warning = Warning;
    postcss22.AtRule = AtRule;
    postcss22.Result = Result;
    postcss22.Input = Input;
    postcss22.Rule = Rule;
    postcss22.Root = Root;
    postcss22.Node = Node2;
    LazyResult.registerPostcss(postcss22);
    postcss_12 = postcss22;
    postcss22.default = postcss22;
    return postcss_12;
  }
  function angularZoneUnpatchedAlternative2(key) {
    var _a22, _b;
    const angularUnpatchedVersionSymbol = (_b = (_a22 = globalThis == null ? void 0 : globalThis.Zone) == null ? void 0 : _a22.__symbol__) == null ? void 0 : _b.call(_a22, key);
    if (angularUnpatchedVersionSymbol && globalThis[angularUnpatchedVersionSymbol]) {
      return globalThis[angularUnpatchedVersionSymbol];
    } else {
      return void 0;
    }
  }
  function getUntaintedPrototype2(key) {
    if (untaintedBasePrototype2[key])
      return untaintedBasePrototype2[key];
    const candidate = angularZoneUnpatchedAlternative2(key) || globalThis[key];
    const defaultPrototype = candidate.prototype;
    const accessorNames = key in testableAccessors2 ? testableAccessors2[key] : void 0;
    const isUntaintedAccessors = Boolean(
      accessorNames && // @ts-expect-error 2345
      accessorNames.every(
        (accessor) => {
          var _a22, _b;
          return Boolean(
            (_b = (_a22 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a22.get) == null ? void 0 : _b.toString().includes("[native code]")
          );
        }
      )
    );
    const methodNames = key in testableMethods2 ? testableMethods2[key] : void 0;
    const isUntaintedMethods = Boolean(
      methodNames && methodNames.every(
        // @ts-expect-error 2345
        (method) => {
          var _a22;
          return typeof defaultPrototype[method] === "function" && ((_a22 = defaultPrototype[method]) == null ? void 0 : _a22.toString().includes("[native code]"));
        }
      )
    );
    if (isUntaintedAccessors && isUntaintedMethods) {
      untaintedBasePrototype2[key] = candidate.prototype;
      return candidate.prototype;
    }
    try {
      const iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      const win = iframeEl.contentWindow;
      if (!win) return candidate.prototype;
      const untaintedObject = win[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype2[key] = untaintedObject;
    } catch {
      return defaultPrototype;
    }
  }
  function getUntaintedAccessor2(key, instance, accessor) {
    var _a22;
    const cacheKey = `${key}.${String(accessor)}`;
    if (untaintedAccessorCache2[cacheKey])
      return untaintedAccessorCache2[cacheKey].call(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype2(key);
    const untaintedAccessor = (_a22 = Object.getOwnPropertyDescriptor(
      untaintedPrototype,
      accessor
    )) == null ? void 0 : _a22.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache2[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  function getUntaintedMethod2(key, instance, method) {
    const cacheKey = `${key}.${String(method)}`;
    if (untaintedMethodCache2[cacheKey])
      return untaintedMethodCache2[cacheKey].bind(
        instance
      );
    const untaintedPrototype = getUntaintedPrototype2(key);
    const untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache2[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes2(n22) {
    return getUntaintedAccessor2("Node", n22, "childNodes");
  }
  function parentNode2(n22) {
    return getUntaintedAccessor2("Node", n22, "parentNode");
  }
  function parentElement2(n22) {
    return getUntaintedAccessor2("Node", n22, "parentElement");
  }
  function textContent2(n22) {
    return getUntaintedAccessor2("Node", n22, "textContent");
  }
  function contains2(n22, other) {
    return getUntaintedMethod2("Node", n22, "contains")(other);
  }
  function getRootNode2(n22) {
    return getUntaintedMethod2("Node", n22, "getRootNode")();
  }
  function host2(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor2("ShadowRoot", n22, "host");
  }
  function styleSheets2(n22) {
    return n22.styleSheets;
  }
  function shadowRoot2(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor2("Element", n22, "shadowRoot");
  }
  function querySelector2(n22, selectors) {
    return getUntaintedAccessor2("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll2(n22, selectors) {
    return getUntaintedAccessor2("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor2() {
    return getUntaintedPrototype2("MutationObserver").constructor;
  }
  function on2(type, fn, target = document) {
    const options = { capture: true, passive: true };
    target.addEventListener(type, fn, options);
    return () => target.removeEventListener(type, fn, options);
  }
  function throttle2(func, wait, options = {}) {
    let timeout = null;
    let previous = 0;
    return function(...args) {
      const now = Date.now();
      if (!previous && options.leading === false) {
        previous = now;
      }
      const remaining = wait - (now - previous);
      const context = this;
      if (remaining <= 0 || remaining > wait) {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
        previous = now;
        func.apply(context, args);
      } else if (!timeout && options.trailing !== false) {
        timeout = setTimeout(() => {
          previous = options.leading === false ? 0 : Date.now();
          timeout = null;
          func.apply(context, args);
        }, remaining);
      }
    };
  }
  function hookSetter2(target, key, d2, isRevoked, win = window) {
    const original = win.Object.getOwnPropertyDescriptor(target, key);
    win.Object.defineProperty(
      target,
      key,
      isRevoked ? d2 : {
        set(value) {
          setTimeout(() => {
            d2.set.call(this, value);
          }, 0);
          if (original && original.set) {
            original.set.call(this, value);
          }
        }
      }
    );
    return () => hookSetter2(target, key, original || {}, true);
  }
  function patch2(source, name, replacement) {
    try {
      if (!(name in source)) {
        return () => {
        };
      }
      const original = source[name];
      const wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name] = wrapped;
      return () => {
        source[name] = original;
      };
    } catch {
      return () => {
      };
    }
  }
  function getWindowScroll2(win) {
    var _a22, _b, _c, _d;
    const doc = win.document;
    return {
      left: doc.scrollingElement ? doc.scrollingElement.scrollLeft : win.pageXOffset !== void 0 ? win.pageXOffset : doc.documentElement.scrollLeft || (doc == null ? void 0 : doc.body) && ((_a22 = index3.parentElement(doc.body)) == null ? void 0 : _a22.scrollLeft) || ((_b = doc == null ? void 0 : doc.body) == null ? void 0 : _b.scrollLeft) || 0,
      top: doc.scrollingElement ? doc.scrollingElement.scrollTop : win.pageYOffset !== void 0 ? win.pageYOffset : (doc == null ? void 0 : doc.documentElement.scrollTop) || (doc == null ? void 0 : doc.body) && ((_c = index3.parentElement(doc.body)) == null ? void 0 : _c.scrollTop) || ((_d = doc == null ? void 0 : doc.body) == null ? void 0 : _d.scrollTop) || 0
    };
  }
  function getWindowHeight2() {
    return window.innerHeight || document.documentElement && document.documentElement.clientHeight || document.body && document.body.clientHeight;
  }
  function getWindowWidth2() {
    return window.innerWidth || document.documentElement && document.documentElement.clientWidth || document.body && document.body.clientWidth;
  }
  function closestElementOfNode2(node22) {
    if (!node22) {
      return null;
    }
    const el = node22.nodeType === node22.ELEMENT_NODE ? node22 : index3.parentElement(node22);
    return el;
  }
  function isBlocked2(node22, blockClass, blockSelector, checkAncestors) {
    if (!node22) {
      return false;
    }
    const el = closestElementOfNode2(node22);
    if (!el) {
      return false;
    }
    try {
      if (typeof blockClass === "string") {
        if (el.classList.contains(blockClass)) return true;
        if (checkAncestors && el.closest("." + blockClass) !== null) return true;
      } else {
        if (classMatchesRegex2(el, blockClass, checkAncestors)) return true;
      }
    } catch (e2) {
    }
    if (blockSelector) {
      if (el.matches(blockSelector)) return true;
      if (checkAncestors && el.closest(blockSelector) !== null) return true;
    }
    return false;
  }
  function isSerialized2(n22, mirror2) {
    return mirror2.getId(n22) !== -1;
  }
  function isIgnored2(n22, mirror2, slimDOMOptions) {
    if (n22.tagName === "TITLE" && slimDOMOptions.headTitleMutations) {
      return true;
    }
    return mirror2.getId(n22) === IGNORED_NODE2;
  }
  function isAncestorRemoved2(target, mirror2) {
    if (isShadowRoot3(target)) {
      return false;
    }
    const id2 = mirror2.getId(target);
    if (!mirror2.has(id2)) {
      return true;
    }
    const parent = index3.parentNode(target);
    if (parent && parent.nodeType === target.DOCUMENT_NODE) {
      return false;
    }
    if (!parent) {
      return true;
    }
    return isAncestorRemoved2(parent, mirror2);
  }
  function legacy_isTouchEvent2(event) {
    return Boolean(event.changedTouches);
  }
  function polyfill$12(win = window) {
    if ("NodeList" in win && !win.NodeList.prototype.forEach) {
      win.NodeList.prototype.forEach = Array.prototype.forEach;
    }
    if ("DOMTokenList" in win && !win.DOMTokenList.prototype.forEach) {
      win.DOMTokenList.prototype.forEach = Array.prototype.forEach;
    }
  }
  function isSerializedIframe2(n22, mirror2) {
    return Boolean(n22.nodeName === "IFRAME" && mirror2.getMeta(n22));
  }
  function isSerializedStylesheet2(n22, mirror2) {
    return Boolean(
      n22.nodeName === "LINK" && n22.nodeType === n22.ELEMENT_NODE && n22.getAttribute && n22.getAttribute("rel") === "stylesheet" && mirror2.getMeta(n22)
    );
  }
  function hasShadowRoot2(n22) {
    if (!n22) return false;
    if (n22 instanceof BaseRRNode2 && "shadowRoot" in n22) {
      return Boolean(n22.shadowRoot);
    }
    return Boolean(index3.shadowRoot(n22));
  }
  function getShadowHost2(n22) {
    var _a22;
    let shadowHost = null;
    if ("getRootNode" in n22 && ((_a22 = index3.getRootNode(n22)) == null ? void 0 : _a22.nodeType) === Node.DOCUMENT_FRAGMENT_NODE && index3.host(index3.getRootNode(n22)))
      shadowHost = index3.host(index3.getRootNode(n22));
    return shadowHost;
  }
  function getRootShadowHost2(n22) {
    let rootShadowHost = n22;
    let shadowHost;
    while (shadowHost = getShadowHost2(rootShadowHost))
      rootShadowHost = shadowHost;
    return rootShadowHost;
  }
  function shadowHostInDom2(n22) {
    const doc = n22.ownerDocument;
    if (!doc) return false;
    const shadowHost = getRootShadowHost2(n22);
    return index3.contains(doc, shadowHost);
  }
  function inDom2(n22) {
    const doc = n22.ownerDocument;
    if (!doc) return false;
    return index3.contains(doc, n22) || shadowHostInDom2(n22);
  }
  function isNodeInLinkedList(n22) {
    return "__ln" in n22;
  }
  function deepDelete(addsSet, n22) {
    addsSet.delete(n22);
    index3.childNodes(n22).forEach((childN) => deepDelete(addsSet, childN));
  }
  function processRemoves(n22, cache) {
    const queue = [n22];
    while (queue.length) {
      const next = queue.pop();
      if (cache.has(next)) continue;
      cache.add(next);
      index3.childNodes(next).forEach((n222) => queue.push(n222));
    }
    return;
  }
  function isParentRemoved(removes, n22, mirror2) {
    if (removes.size === 0) return false;
    return _isParentRemoved(removes, n22);
  }
  function _isParentRemoved(removes, n22, _mirror22) {
    const node22 = index3.parentNode(n22);
    if (!node22) return false;
    return removes.has(node22);
  }
  function isAncestorInSet(set, n22) {
    if (set.size === 0) return false;
    return _isAncestorInSet(set, n22);
  }
  function _isAncestorInSet(set, n22) {
    const parent = index3.parentNode(n22);
    if (!parent) {
      return false;
    }
    if (set.has(parent)) {
      return true;
    }
    return _isAncestorInSet(set, parent);
  }
  function registerErrorHandler(handler) {
    errorHandler = handler;
  }
  function unregisterErrorHandler() {
    errorHandler = void 0;
  }
  function externalFunctionWrapper(func) {
    return (...args) => {
      try {
        return func(...args);
      } catch (error) {
        try {
          error._external_ = true;
        } catch {
        }
        throw error;
      }
    };
  }
  function getEventTarget(event) {
    try {
      if ("composedPath" in event) {
        const path = event.composedPath();
        if (path.length) {
          return path[0];
        }
      } else if ("path" in event && event.path.length) {
        return event.path[0];
      }
    } catch {
    }
    return event && event.target;
  }
  function initMutationObserver(options, rootEl) {
    const mutationBuffer = new MutationBuffer();
    mutationBuffers.push(mutationBuffer);
    mutationBuffer.init(options);
    const observer = new (mutationObserverCtor2())(
      callbackWrapper(mutationBuffer.processMutations.bind(mutationBuffer))
    );
    observer.observe(rootEl, {
      attributes: true,
      attributeOldValue: true,
      characterData: true,
      characterDataOldValue: true,
      childList: true,
      subtree: true
    });
    return observer;
  }
  function initMoveObserver({
    mousemoveCb,
    sampling,
    doc,
    mirror: mirror2
  }) {
    if (sampling.mousemove === false) {
      return () => {
      };
    }
    const threshold = typeof sampling.mousemove === "number" ? sampling.mousemove : 50;
    const callbackThreshold = typeof sampling.mousemoveCallback === "number" ? sampling.mousemoveCallback : 500;
    let positions = [];
    let timeBaseline;
    const wrappedCb = throttle2(
      callbackWrapper(
        (source) => {
          const totalOffset = Date.now() - timeBaseline;
          mousemoveCb(
            positions.map((p2) => {
              p2.timeOffset -= totalOffset;
              return p2;
            }),
            source
          );
          positions = [];
          timeBaseline = null;
        }
      ),
      callbackThreshold
    );
    const updatePosition = callbackWrapper(
      throttle2(
        callbackWrapper((evt) => {
          const target = getEventTarget(evt);
          const { clientX, clientY } = legacy_isTouchEvent2(evt) ? evt.changedTouches[0] : evt;
          if (!timeBaseline) {
            timeBaseline = nowTimestamp2();
          }
          positions.push({
            x: clientX,
            y: clientY,
            id: mirror2.getId(target),
            timeOffset: nowTimestamp2() - timeBaseline
          });
          wrappedCb(
            typeof DragEvent !== "undefined" && evt instanceof DragEvent ? IncrementalSource2.Drag : evt instanceof MouseEvent ? IncrementalSource2.MouseMove : IncrementalSource2.TouchMove
          );
        }),
        threshold,
        {
          trailing: false
        }
      )
    );
    const handlers2 = [
      on2("mousemove", updatePosition, doc),
      on2("touchmove", updatePosition, doc),
      on2("drag", updatePosition, doc)
    ];
    return callbackWrapper(() => {
      handlers2.forEach((h2) => h2());
    });
  }
  function initMouseInteractionObserver({
    mouseInteractionCb,
    doc,
    mirror: mirror2,
    blockClass,
    blockSelector,
    sampling
  }) {
    if (sampling.mouseInteraction === false) {
      return () => {
      };
    }
    const disableMap = sampling.mouseInteraction === true || sampling.mouseInteraction === void 0 ? {} : sampling.mouseInteraction;
    const handlers2 = [];
    let currentPointerType = null;
    const getHandler = (eventKey) => {
      return (event) => {
        const target = getEventTarget(event);
        if (isBlocked2(target, blockClass, blockSelector, true)) {
          return;
        }
        let pointerType = null;
        let thisEventKey = eventKey;
        if ("pointerType" in event) {
          switch (event.pointerType) {
            case "mouse":
              pointerType = PointerTypes.Mouse;
              break;
            case "touch":
              pointerType = PointerTypes.Touch;
              break;
            case "pen":
              pointerType = PointerTypes.Pen;
              break;
          }
          if (pointerType === PointerTypes.Touch) {
            if (MouseInteractions2[eventKey] === MouseInteractions2.MouseDown) {
              thisEventKey = "TouchStart";
            } else if (MouseInteractions2[eventKey] === MouseInteractions2.MouseUp) {
              thisEventKey = "TouchEnd";
            }
          } else if (pointerType === PointerTypes.Pen) ;
        } else if (legacy_isTouchEvent2(event)) {
          pointerType = PointerTypes.Touch;
        }
        if (pointerType !== null) {
          currentPointerType = pointerType;
          if (thisEventKey.startsWith("Touch") && pointerType === PointerTypes.Touch || thisEventKey.startsWith("Mouse") && pointerType === PointerTypes.Mouse) {
            pointerType = null;
          }
        } else if (MouseInteractions2[eventKey] === MouseInteractions2.Click) {
          pointerType = currentPointerType;
          currentPointerType = null;
        }
        const e2 = legacy_isTouchEvent2(event) ? event.changedTouches[0] : event;
        if (!e2) {
          return;
        }
        const id2 = mirror2.getId(target);
        const { clientX, clientY } = e2;
        callbackWrapper(mouseInteractionCb)({
          type: MouseInteractions2[thisEventKey],
          id: id2,
          x: clientX,
          y: clientY,
          ...pointerType !== null && { pointerType }
        });
      };
    };
    Object.keys(MouseInteractions2).filter(
      (key) => Number.isNaN(Number(key)) && !key.endsWith("_Departed") && disableMap[key] !== false
    ).forEach((eventKey) => {
      let eventName = toLowerCase2(eventKey);
      const handler = getHandler(eventKey);
      if (window.PointerEvent) {
        switch (MouseInteractions2[eventKey]) {
          case MouseInteractions2.MouseDown:
          case MouseInteractions2.MouseUp:
            eventName = eventName.replace(
              "mouse",
              "pointer"
            );
            break;
          case MouseInteractions2.TouchStart:
          case MouseInteractions2.TouchEnd:
            return;
        }
      }
      handlers2.push(on2(eventName, handler, doc));
    });
    return callbackWrapper(() => {
      handlers2.forEach((h2) => h2());
    });
  }
  function initScrollObserver({
    scrollCb,
    doc,
    mirror: mirror2,
    blockClass,
    blockSelector,
    sampling
  }) {
    const updatePosition = callbackWrapper(
      throttle2(
        callbackWrapper((evt) => {
          const target = getEventTarget(evt);
          if (!target || isBlocked2(target, blockClass, blockSelector, true)) {
            return;
          }
          const id2 = mirror2.getId(target);
          if (target === doc && doc.defaultView) {
            const scrollLeftTop = getWindowScroll2(doc.defaultView);
            scrollCb({
              id: id2,
              x: scrollLeftTop.left,
              y: scrollLeftTop.top
            });
          } else {
            scrollCb({
              id: id2,
              x: target.scrollLeft,
              y: target.scrollTop
            });
          }
        }),
        sampling.scroll || 100
      )
    );
    return on2("scroll", updatePosition, doc);
  }
  function initViewportResizeObserver({ viewportResizeCb }, { win }) {
    let lastH = -1;
    let lastW = -1;
    const updateDimension = callbackWrapper(
      throttle2(
        callbackWrapper(() => {
          const height = getWindowHeight2();
          const width = getWindowWidth2();
          if (lastH !== height || lastW !== width) {
            viewportResizeCb({
              width: Number(width),
              height: Number(height)
            });
            lastH = height;
            lastW = width;
          }
        }),
        200
      )
    );
    return on2("resize", updateDimension, win);
  }
  function initInputObserver({
    inputCb,
    doc,
    mirror: mirror2,
    blockClass,
    blockSelector,
    ignoreClass,
    ignoreSelector,
    maskInputOptions,
    maskInputFn,
    sampling,
    userTriggeredOnInput
  }) {
    function eventHandler(event) {
      let target = getEventTarget(event);
      const userTriggered = event.isTrusted;
      const tagName2 = target && target.tagName;
      if (target && tagName2 === "OPTION") {
        target = index3.parentElement(target);
      }
      if (!target || !tagName2 || INPUT_TAGS.indexOf(tagName2) < 0 || isBlocked2(target, blockClass, blockSelector, true)) {
        return;
      }
      if (target.classList.contains(ignoreClass) || ignoreSelector && target.matches(ignoreSelector)) {
        return;
      }
      let text = target.value;
      let isChecked = false;
      const type = getInputType2(target) || "";
      if (type === "radio" || type === "checkbox") {
        isChecked = target.checked;
      } else if (maskInputOptions[tagName2.toLowerCase()] || maskInputOptions[type]) {
        text = maskInputValue({
          element: target,
          maskInputOptions,
          tagName: tagName2,
          type,
          value: text,
          maskInputFn
        });
      }
      cbWithDedup(
        target,
        userTriggeredOnInput ? { text, isChecked, userTriggered } : { text, isChecked }
      );
      const name = target.name;
      if (type === "radio" && name && isChecked) {
        doc.querySelectorAll(`input[type="radio"][name="${name}"]`).forEach((el) => {
          if (el !== target) {
            const text2 = el.value;
            cbWithDedup(
              el,
              userTriggeredOnInput ? { text: text2, isChecked: !isChecked, userTriggered: false } : { text: text2, isChecked: !isChecked }
            );
          }
        });
      }
    }
    function cbWithDedup(target, v2) {
      const lastInputValue = lastInputValueMap.get(target);
      if (!lastInputValue || lastInputValue.text !== v2.text || lastInputValue.isChecked !== v2.isChecked) {
        lastInputValueMap.set(target, v2);
        const id2 = mirror2.getId(target);
        callbackWrapper(inputCb)({
          ...v2,
          id: id2
        });
      }
    }
    const events = sampling.input === "last" ? ["change"] : ["input", "change"];
    const handlers2 = events.map(
      (eventName) => on2(eventName, callbackWrapper(eventHandler), doc)
    );
    const currentWindow = doc.defaultView;
    if (!currentWindow) {
      return () => {
        handlers2.forEach((h2) => h2());
      };
    }
    const propertyDescriptor = currentWindow.Object.getOwnPropertyDescriptor(
      currentWindow.HTMLInputElement.prototype,
      "value"
    );
    const hookProperties = [
      [currentWindow.HTMLInputElement.prototype, "value"],
      [currentWindow.HTMLInputElement.prototype, "checked"],
      [currentWindow.HTMLSelectElement.prototype, "value"],
      [currentWindow.HTMLTextAreaElement.prototype, "value"],
      // Some UI library use selectedIndex to set select value
      [currentWindow.HTMLSelectElement.prototype, "selectedIndex"],
      [currentWindow.HTMLOptionElement.prototype, "selected"]
    ];
    if (propertyDescriptor && propertyDescriptor.set) {
      handlers2.push(
        ...hookProperties.map(
          (p2) => hookSetter2(
            p2[0],
            p2[1],
            {
              set() {
                callbackWrapper(eventHandler)({
                  target: this,
                  isTrusted: false
                  // userTriggered to false as this could well be programmatic
                });
              }
            },
            false,
            currentWindow
          )
        )
      );
    }
    return callbackWrapper(() => {
      handlers2.forEach((h2) => h2());
    });
  }
  function getNestedCSSRulePositions(rule22) {
    const positions = [];
    function recurse(childRule, pos) {
      if (hasNestedCSSRule("CSSGroupingRule") && childRule.parentRule instanceof CSSGroupingRule || hasNestedCSSRule("CSSMediaRule") && childRule.parentRule instanceof CSSMediaRule || hasNestedCSSRule("CSSSupportsRule") && childRule.parentRule instanceof CSSSupportsRule || hasNestedCSSRule("CSSConditionRule") && childRule.parentRule instanceof CSSConditionRule) {
        const rules2 = Array.from(
          childRule.parentRule.cssRules
        );
        const index22 = rules2.indexOf(childRule);
        pos.unshift(index22);
      } else if (childRule.parentStyleSheet) {
        const rules2 = Array.from(childRule.parentStyleSheet.cssRules);
        const index22 = rules2.indexOf(childRule);
        pos.unshift(index22);
      }
      return pos;
    }
    return recurse(rule22, positions);
  }
  function getIdAndStyleId(sheet, mirror2, styleMirror) {
    let id2, styleId;
    if (!sheet) return {};
    if (sheet.ownerNode) id2 = mirror2.getId(sheet.ownerNode);
    else styleId = styleMirror.getId(sheet);
    return {
      styleId,
      id: id2
    };
  }
  function initStyleSheetObserver({ styleSheetRuleCb, mirror: mirror2, stylesheetManager }, { win }) {
    if (!win.CSSStyleSheet || !win.CSSStyleSheet.prototype) {
      return () => {
      };
    }
    const insertRule = win.CSSStyleSheet.prototype.insertRule;
    win.CSSStyleSheet.prototype.insertRule = new Proxy(insertRule, {
      apply: callbackWrapper(
        (target, thisArg, argumentsList) => {
          const [rule22, index22] = argumentsList;
          const { id: id2, styleId } = getIdAndStyleId(
            thisArg,
            mirror2,
            stylesheetManager.styleMirror
          );
          if (id2 && id2 !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id: id2,
              styleId,
              adds: [{ rule: rule22, index: index22 }]
            });
          }
          return externalFunctionWrapper(
            () => target.apply(thisArg, argumentsList)
          )();
        }
      )
    });
    win.CSSStyleSheet.prototype.addRule = function(selector2, styleBlock, index22 = this.cssRules.length) {
      const rule22 = `${selector2} { ${styleBlock} }`;
      return win.CSSStyleSheet.prototype.insertRule.apply(this, [rule22, index22]);
    };
    const deleteRule = win.CSSStyleSheet.prototype.deleteRule;
    win.CSSStyleSheet.prototype.deleteRule = new Proxy(deleteRule, {
      apply: callbackWrapper(
        (target, thisArg, argumentsList) => {
          const [index22] = argumentsList;
          const { id: id2, styleId } = getIdAndStyleId(
            thisArg,
            mirror2,
            stylesheetManager.styleMirror
          );
          if (id2 && id2 !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id: id2,
              styleId,
              removes: [{ index: index22 }]
            });
          }
          return externalFunctionWrapper(
            () => target.apply(thisArg, argumentsList)
          )();
        }
      )
    });
    win.CSSStyleSheet.prototype.removeRule = function(index22) {
      return win.CSSStyleSheet.prototype.deleteRule.apply(this, [index22]);
    };
    let replace;
    if (win.CSSStyleSheet.prototype.replace) {
      replace = win.CSSStyleSheet.prototype.replace;
      win.CSSStyleSheet.prototype.replace = new Proxy(replace, {
        apply: callbackWrapper(
          (target, thisArg, argumentsList) => {
            const [text] = argumentsList;
            const { id: id2, styleId } = getIdAndStyleId(
              thisArg,
              mirror2,
              stylesheetManager.styleMirror
            );
            if (id2 && id2 !== -1 || styleId && styleId !== -1) {
              styleSheetRuleCb({
                id: id2,
                styleId,
                replace: text
              });
            }
            return target.apply(thisArg, argumentsList);
          }
        )
      });
    }
    let replaceSync;
    if (win.CSSStyleSheet.prototype.replaceSync) {
      replaceSync = win.CSSStyleSheet.prototype.replaceSync;
      win.CSSStyleSheet.prototype.replaceSync = new Proxy(replaceSync, {
        apply: callbackWrapper(
          (target, thisArg, argumentsList) => {
            const [text] = argumentsList;
            const { id: id2, styleId } = getIdAndStyleId(
              thisArg,
              mirror2,
              stylesheetManager.styleMirror
            );
            if (id2 && id2 !== -1 || styleId && styleId !== -1) {
              styleSheetRuleCb({
                id: id2,
                styleId,
                replaceSync: text
              });
            }
            return target.apply(thisArg, argumentsList);
          }
        )
      });
    }
    const supportedNestedCSSRuleTypes = {};
    if (canMonkeyPatchNestedCSSRule("CSSGroupingRule")) {
      supportedNestedCSSRuleTypes.CSSGroupingRule = win.CSSGroupingRule;
    } else {
      if (canMonkeyPatchNestedCSSRule("CSSMediaRule")) {
        supportedNestedCSSRuleTypes.CSSMediaRule = win.CSSMediaRule;
      }
      if (canMonkeyPatchNestedCSSRule("CSSConditionRule")) {
        supportedNestedCSSRuleTypes.CSSConditionRule = win.CSSConditionRule;
      }
      if (canMonkeyPatchNestedCSSRule("CSSSupportsRule")) {
        supportedNestedCSSRuleTypes.CSSSupportsRule = win.CSSSupportsRule;
      }
    }
    const unmodifiedFunctions = {};
    Object.entries(supportedNestedCSSRuleTypes).forEach(([typeKey, type]) => {
      unmodifiedFunctions[typeKey] = {
        // eslint-disable-next-line @typescript-eslint/unbound-method
        insertRule: type.prototype.insertRule,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        deleteRule: type.prototype.deleteRule
      };
      type.prototype.insertRule = new Proxy(
        unmodifiedFunctions[typeKey].insertRule,
        {
          apply: callbackWrapper(
            (target, thisArg, argumentsList) => {
              const [rule22, index22] = argumentsList;
              const { id: id2, styleId } = getIdAndStyleId(
                thisArg.parentStyleSheet,
                mirror2,
                stylesheetManager.styleMirror
              );
              if (id2 && id2 !== -1 || styleId && styleId !== -1) {
                styleSheetRuleCb({
                  id: id2,
                  styleId,
                  adds: [
                    {
                      rule: rule22,
                      index: [
                        ...getNestedCSSRulePositions(thisArg),
                        index22 || 0
                        // defaults to 0
                      ]
                    }
                  ]
                });
              }
              return target.apply(thisArg, argumentsList);
            }
          )
        }
      );
      type.prototype.deleteRule = new Proxy(
        unmodifiedFunctions[typeKey].deleteRule,
        {
          apply: callbackWrapper(
            (target, thisArg, argumentsList) => {
              const [index22] = argumentsList;
              const { id: id2, styleId } = getIdAndStyleId(
                thisArg.parentStyleSheet,
                mirror2,
                stylesheetManager.styleMirror
              );
              if (id2 && id2 !== -1 || styleId && styleId !== -1) {
                styleSheetRuleCb({
                  id: id2,
                  styleId,
                  removes: [
                    { index: [...getNestedCSSRulePositions(thisArg), index22] }
                  ]
                });
              }
              return target.apply(thisArg, argumentsList);
            }
          )
        }
      );
    });
    return callbackWrapper(() => {
      win.CSSStyleSheet.prototype.insertRule = insertRule;
      win.CSSStyleSheet.prototype.deleteRule = deleteRule;
      replace && (win.CSSStyleSheet.prototype.replace = replace);
      replaceSync && (win.CSSStyleSheet.prototype.replaceSync = replaceSync);
      Object.entries(supportedNestedCSSRuleTypes).forEach(([typeKey, type]) => {
        type.prototype.insertRule = unmodifiedFunctions[typeKey].insertRule;
        type.prototype.deleteRule = unmodifiedFunctions[typeKey].deleteRule;
      });
    });
  }
  function initAdoptedStyleSheetObserver({
    mirror: mirror2,
    stylesheetManager
  }, host22) {
    var _a22, _b, _c;
    let hostId = null;
    if (host22.nodeName === "#document") hostId = mirror2.getId(host22);
    else hostId = mirror2.getId(index3.host(host22));
    const patchTarget = host22.nodeName === "#document" ? (_a22 = host22.defaultView) == null ? void 0 : _a22.Document : (_c = (_b = host22.ownerDocument) == null ? void 0 : _b.defaultView) == null ? void 0 : _c.ShadowRoot;
    const originalPropertyDescriptor = (patchTarget == null ? void 0 : patchTarget.prototype) ? Object.getOwnPropertyDescriptor(
      patchTarget == null ? void 0 : patchTarget.prototype,
      "adoptedStyleSheets"
    ) : void 0;
    if (hostId === null || hostId === -1 || !patchTarget || !originalPropertyDescriptor)
      return () => {
      };
    Object.defineProperty(host22, "adoptedStyleSheets", {
      configurable: originalPropertyDescriptor.configurable,
      enumerable: originalPropertyDescriptor.enumerable,
      get() {
        var _a32;
        return (_a32 = originalPropertyDescriptor.get) == null ? void 0 : _a32.call(this);
      },
      set(sheets) {
        var _a32;
        const result22 = (_a32 = originalPropertyDescriptor.set) == null ? void 0 : _a32.call(this, sheets);
        if (hostId !== null && hostId !== -1) {
          try {
            stylesheetManager.adoptStyleSheets(sheets, hostId);
          } catch (e2) {
          }
        }
        return result22;
      }
    });
    return callbackWrapper(() => {
      Object.defineProperty(host22, "adoptedStyleSheets", {
        configurable: originalPropertyDescriptor.configurable,
        enumerable: originalPropertyDescriptor.enumerable,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        get: originalPropertyDescriptor.get,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        set: originalPropertyDescriptor.set
      });
    });
  }
  function initStyleDeclarationObserver({
    styleDeclarationCb,
    mirror: mirror2,
    ignoreCSSAttributes,
    stylesheetManager
  }, { win }) {
    const setProperty = win.CSSStyleDeclaration.prototype.setProperty;
    win.CSSStyleDeclaration.prototype.setProperty = new Proxy(setProperty, {
      apply: callbackWrapper(
        (target, thisArg, argumentsList) => {
          var _a22;
          const [property, value, priority] = argumentsList;
          if (ignoreCSSAttributes.has(property)) {
            return setProperty.apply(thisArg, [property, value, priority]);
          }
          const { id: id2, styleId } = getIdAndStyleId(
            (_a22 = thisArg.parentRule) == null ? void 0 : _a22.parentStyleSheet,
            mirror2,
            stylesheetManager.styleMirror
          );
          if (id2 && id2 !== -1 || styleId && styleId !== -1) {
            styleDeclarationCb({
              id: id2,
              styleId,
              set: {
                property,
                value,
                priority
              },
              // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
              index: getNestedCSSRulePositions(thisArg.parentRule)
            });
          }
          return target.apply(thisArg, argumentsList);
        }
      )
    });
    const removeProperty = win.CSSStyleDeclaration.prototype.removeProperty;
    win.CSSStyleDeclaration.prototype.removeProperty = new Proxy(removeProperty, {
      apply: callbackWrapper(
        (target, thisArg, argumentsList) => {
          var _a22;
          const [property] = argumentsList;
          if (ignoreCSSAttributes.has(property)) {
            return removeProperty.apply(thisArg, [property]);
          }
          const { id: id2, styleId } = getIdAndStyleId(
            (_a22 = thisArg.parentRule) == null ? void 0 : _a22.parentStyleSheet,
            mirror2,
            stylesheetManager.styleMirror
          );
          if (id2 && id2 !== -1 || styleId && styleId !== -1) {
            styleDeclarationCb({
              id: id2,
              styleId,
              remove: {
                property
              },
              // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
              index: getNestedCSSRulePositions(thisArg.parentRule)
            });
          }
          return target.apply(thisArg, argumentsList);
        }
      )
    });
    return callbackWrapper(() => {
      win.CSSStyleDeclaration.prototype.setProperty = setProperty;
      win.CSSStyleDeclaration.prototype.removeProperty = removeProperty;
    });
  }
  function initMediaInteractionObserver({
    mediaInteractionCb,
    blockClass,
    blockSelector,
    mirror: mirror2,
    sampling,
    doc
  }) {
    const handler = callbackWrapper(
      (type) => throttle2(
        callbackWrapper((event) => {
          const target = getEventTarget(event);
          if (!target || isBlocked2(target, blockClass, blockSelector, true)) {
            return;
          }
          const { currentTime, volume, muted, playbackRate, loop } = target;
          mediaInteractionCb({
            type,
            id: mirror2.getId(target),
            currentTime,
            volume,
            muted,
            playbackRate,
            loop
          });
        }),
        sampling.media || 500
      )
    );
    const handlers2 = [
      on2("play", handler(MediaInteractions.Play), doc),
      on2("pause", handler(MediaInteractions.Pause), doc),
      on2("seeked", handler(MediaInteractions.Seeked), doc),
      on2("volumechange", handler(MediaInteractions.VolumeChange), doc),
      on2("ratechange", handler(MediaInteractions.RateChange), doc)
    ];
    return callbackWrapper(() => {
      handlers2.forEach((h2) => h2());
    });
  }
  function initFontObserver({ fontCb, doc }) {
    const win = doc.defaultView;
    if (!win) {
      return () => {
      };
    }
    const handlers2 = [];
    const fontMap = /* @__PURE__ */ new WeakMap();
    const originalFontFace = win.FontFace;
    win.FontFace = function FontFace2(family, source, descriptors) {
      const fontFace = new originalFontFace(family, source, descriptors);
      fontMap.set(fontFace, {
        family,
        buffer: typeof source !== "string",
        descriptors,
        fontSource: typeof source === "string" ? source : JSON.stringify(Array.from(new Uint8Array(source)))
      });
      return fontFace;
    };
    const restoreHandler = patch2(
      doc.fonts,
      "add",
      function(original) {
        return function(fontFace) {
          setTimeout(
            callbackWrapper(() => {
              const p2 = fontMap.get(fontFace);
              if (p2) {
                fontCb(p2);
                fontMap.delete(fontFace);
              }
            }),
            0
          );
          return original.apply(this, [fontFace]);
        };
      }
    );
    handlers2.push(() => {
      win.FontFace = originalFontFace;
    });
    handlers2.push(restoreHandler);
    return callbackWrapper(() => {
      handlers2.forEach((h2) => h2());
    });
  }
  function initSelectionObserver(param) {
    const { doc, mirror: mirror2, blockClass, blockSelector, selectionCb } = param;
    let collapsed = true;
    const updateSelection = callbackWrapper(() => {
      const selection = doc.getSelection();
      if (!selection || collapsed && (selection == null ? void 0 : selection.isCollapsed)) return;
      collapsed = selection.isCollapsed || false;
      const ranges = [];
      const count = selection.rangeCount || 0;
      for (let i2 = 0; i2 < count; i2++) {
        const range = selection.getRangeAt(i2);
        const { startContainer, startOffset, endContainer, endOffset } = range;
        const blocked = isBlocked2(startContainer, blockClass, blockSelector, true) || isBlocked2(endContainer, blockClass, blockSelector, true);
        if (blocked) continue;
        ranges.push({
          start: mirror2.getId(startContainer),
          startOffset,
          end: mirror2.getId(endContainer),
          endOffset
        });
      }
      selectionCb({ ranges });
    });
    updateSelection();
    return on2("selectionchange", updateSelection);
  }
  function initCustomElementObserver({
    doc,
    customElementCb
  }) {
    const win = doc.defaultView;
    if (!win || !win.customElements) return () => {
    };
    const restoreHandler = patch2(
      win.customElements,
      "define",
      function(original) {
        return function(name, constructor, options) {
          try {
            customElementCb({
              define: {
                name
              }
            });
          } catch (e2) {
            console.warn(`Custom element callback failed for ${name}`);
          }
          return original.apply(this, [name, constructor, options]);
        };
      }
    );
    return restoreHandler;
  }
  function mergeHooks(o2, hooks) {
    const {
      mutationCb,
      mousemoveCb,
      mouseInteractionCb,
      scrollCb,
      viewportResizeCb,
      inputCb,
      mediaInteractionCb,
      styleSheetRuleCb,
      styleDeclarationCb,
      canvasMutationCb,
      fontCb,
      selectionCb,
      customElementCb
    } = o2;
    o2.mutationCb = (...p2) => {
      if (hooks.mutation) {
        hooks.mutation(...p2);
      }
      mutationCb(...p2);
    };
    o2.mousemoveCb = (...p2) => {
      if (hooks.mousemove) {
        hooks.mousemove(...p2);
      }
      mousemoveCb(...p2);
    };
    o2.mouseInteractionCb = (...p2) => {
      if (hooks.mouseInteraction) {
        hooks.mouseInteraction(...p2);
      }
      mouseInteractionCb(...p2);
    };
    o2.scrollCb = (...p2) => {
      if (hooks.scroll) {
        hooks.scroll(...p2);
      }
      scrollCb(...p2);
    };
    o2.viewportResizeCb = (...p2) => {
      if (hooks.viewportResize) {
        hooks.viewportResize(...p2);
      }
      viewportResizeCb(...p2);
    };
    o2.inputCb = (...p2) => {
      if (hooks.input) {
        hooks.input(...p2);
      }
      inputCb(...p2);
    };
    o2.mediaInteractionCb = (...p2) => {
      if (hooks.mediaInteaction) {
        hooks.mediaInteaction(...p2);
      }
      mediaInteractionCb(...p2);
    };
    o2.styleSheetRuleCb = (...p2) => {
      if (hooks.styleSheetRule) {
        hooks.styleSheetRule(...p2);
      }
      styleSheetRuleCb(...p2);
    };
    o2.styleDeclarationCb = (...p2) => {
      if (hooks.styleDeclaration) {
        hooks.styleDeclaration(...p2);
      }
      styleDeclarationCb(...p2);
    };
    o2.canvasMutationCb = (...p2) => {
      if (hooks.canvasMutation) {
        hooks.canvasMutation(...p2);
      }
      canvasMutationCb(...p2);
    };
    o2.fontCb = (...p2) => {
      if (hooks.font) {
        hooks.font(...p2);
      }
      fontCb(...p2);
    };
    o2.selectionCb = (...p2) => {
      if (hooks.selection) {
        hooks.selection(...p2);
      }
      selectionCb(...p2);
    };
    o2.customElementCb = (...c2) => {
      if (hooks.customElement) {
        hooks.customElement(...c2);
      }
      customElementCb(...c2);
    };
  }
  function initObservers(o2, hooks = {}) {
    const currentWindow = o2.doc.defaultView;
    if (!currentWindow) {
      return () => {
      };
    }
    mergeHooks(o2, hooks);
    let mutationObserver;
    if (o2.recordDOM) {
      mutationObserver = initMutationObserver(o2, o2.doc);
    }
    const mousemoveHandler = initMoveObserver(o2);
    const mouseInteractionHandler = initMouseInteractionObserver(o2);
    const scrollHandler = initScrollObserver(o2);
    const viewportResizeHandler = initViewportResizeObserver(o2, {
      win: currentWindow
    });
    const inputHandler = initInputObserver(o2);
    const mediaInteractionHandler = initMediaInteractionObserver(o2);
    let styleSheetObserver = () => {
    };
    let adoptedStyleSheetObserver = () => {
    };
    let styleDeclarationObserver = () => {
    };
    let fontObserver = () => {
    };
    if (o2.recordDOM) {
      styleSheetObserver = initStyleSheetObserver(o2, { win: currentWindow });
      adoptedStyleSheetObserver = initAdoptedStyleSheetObserver(o2, o2.doc);
      styleDeclarationObserver = initStyleDeclarationObserver(o2, {
        win: currentWindow
      });
      if (o2.collectFonts) {
        fontObserver = initFontObserver(o2);
      }
    }
    const selectionObserver = initSelectionObserver(o2);
    const customElementObserver = initCustomElementObserver(o2);
    const pluginHandlers = [];
    for (const plugin of o2.plugins) {
      pluginHandlers.push(
        plugin.observer(plugin.callback, currentWindow, plugin.options)
      );
    }
    return callbackWrapper(() => {
      mutationBuffers.forEach((b2) => b2.reset());
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
      mousemoveHandler();
      mouseInteractionHandler();
      scrollHandler();
      viewportResizeHandler();
      inputHandler();
      mediaInteractionHandler();
      styleSheetObserver();
      adoptedStyleSheetObserver();
      styleDeclarationObserver();
      fontObserver();
      selectionObserver();
      customElementObserver();
      pluginHandlers.forEach((h2) => h2());
    });
  }
  function hasNestedCSSRule(prop) {
    return typeof window[prop] !== "undefined";
  }
  function canMonkeyPatchNestedCSSRule(prop) {
    return Boolean(
      typeof window[prop] !== "undefined" && // Note: Generally, this check _shouldn't_ be necessary
      // However, in some scenarios (e.g. jsdom) this can sometimes fail, so we check for it here
      window[prop].prototype && "insertRule" in window[prop].prototype && "deleteRule" in window[prop].prototype
    );
  }
  function variableListFor$1(ctx, ctor) {
    let contextMap = canvasVarMap.get(ctx);
    if (!contextMap) {
      contextMap = /* @__PURE__ */ new Map();
      canvasVarMap.set(ctx, contextMap);
    }
    if (!contextMap.has(ctor)) {
      contextMap.set(ctor, []);
    }
    return contextMap.get(ctor);
  }
  function serializeArg(value, win, ctx) {
    if (value instanceof Array) {
      return value.map((arg) => serializeArg(arg, win, ctx));
    } else if (value === null) {
      return value;
    } else if (value instanceof Float32Array || value instanceof Float64Array || value instanceof Int32Array || value instanceof Uint32Array || value instanceof Uint8Array || value instanceof Uint16Array || value instanceof Int16Array || value instanceof Int8Array || value instanceof Uint8ClampedArray) {
      const name = value.constructor.name;
      return {
        rr_type: name,
        args: [Object.values(value)]
      };
    } else if (
      // SharedArrayBuffer disabled on most browsers due to spectre.
      // More info: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/SharedArrayBuffer/SharedArrayBuffer
      // value instanceof SharedArrayBuffer ||
      value instanceof ArrayBuffer
    ) {
      const name = value.constructor.name;
      const base64 = encode2(value);
      return {
        rr_type: name,
        base64
      };
    } else if (value instanceof DataView) {
      const name = value.constructor.name;
      return {
        rr_type: name,
        args: [
          serializeArg(value.buffer, win, ctx),
          value.byteOffset,
          value.byteLength
        ]
      };
    } else if (value instanceof HTMLImageElement) {
      const name = value.constructor.name;
      const { src } = value;
      return {
        rr_type: name,
        src
      };
    } else if (value instanceof HTMLCanvasElement) {
      const name = "HTMLImageElement";
      const src = value.toDataURL();
      return {
        rr_type: name,
        src
      };
    } else if (value instanceof ImageData) {
      const name = value.constructor.name;
      return {
        rr_type: name,
        args: [serializeArg(value.data, win, ctx), value.width, value.height]
      };
    } else if (isInstanceOfWebGLObject(value, win) || typeof value === "object") {
      const name = value.constructor.name;
      const index22 = saveWebGLVar(value, win, ctx);
      return {
        rr_type: name,
        index: index22
      };
    }
    return value;
  }
  function initCanvas2DMutationObserver(cb, win, blockClass, blockSelector) {
    const handlers2 = [];
    const props2D = Object.getOwnPropertyNames(
      win.CanvasRenderingContext2D.prototype
    );
    for (const prop of props2D) {
      try {
        if (typeof win.CanvasRenderingContext2D.prototype[prop] !== "function") {
          continue;
        }
        const restoreHandler = patch2(
          win.CanvasRenderingContext2D.prototype,
          prop,
          function(original) {
            return function(...args) {
              if (!isBlocked2(this.canvas, blockClass, blockSelector, true)) {
                setTimeout(() => {
                  const recordArgs = serializeArgs(args, win, this);
                  cb(this.canvas, {
                    type: CanvasContext["2D"],
                    property: prop,
                    args: recordArgs
                  });
                }, 0);
              }
              return original.apply(this, args);
            };
          }
        );
        handlers2.push(restoreHandler);
      } catch {
        const hookHandler = hookSetter2(
          win.CanvasRenderingContext2D.prototype,
          prop,
          {
            set(v2) {
              cb(this.canvas, {
                type: CanvasContext["2D"],
                property: prop,
                args: [v2],
                setter: true
              });
            }
          }
        );
        handlers2.push(hookHandler);
      }
    }
    return () => {
      handlers2.forEach((h2) => h2());
    };
  }
  function getNormalizedContextName(contextType) {
    return contextType === "experimental-webgl" ? "webgl" : contextType;
  }
  function initCanvasContextObserver(win, blockClass, blockSelector, setPreserveDrawingBufferToTrue) {
    const handlers2 = [];
    try {
      const restoreHandler = patch2(
        win.HTMLCanvasElement.prototype,
        "getContext",
        function(original) {
          return function(contextType, ...args) {
            if (!isBlocked2(this, blockClass, blockSelector, true)) {
              const ctxName = getNormalizedContextName(contextType);
              if (!("__context" in this)) this.__context = ctxName;
              if (setPreserveDrawingBufferToTrue && ["webgl", "webgl2"].includes(ctxName)) {
                if (args[0] && typeof args[0] === "object") {
                  const contextAttributes = args[0];
                  if (!contextAttributes.preserveDrawingBuffer) {
                    contextAttributes.preserveDrawingBuffer = true;
                  }
                } else {
                  args.splice(0, 1, {
                    preserveDrawingBuffer: true
                  });
                }
              }
            }
            return original.apply(this, [contextType, ...args]);
          };
        }
      );
      handlers2.push(restoreHandler);
    } catch {
      console.error("failed to patch HTMLCanvasElement.prototype.getContext");
    }
    return () => {
      handlers2.forEach((h2) => h2());
    };
  }
  function patchGLPrototype(prototype, type, cb, blockClass, blockSelector, win) {
    const handlers2 = [];
    const props = Object.getOwnPropertyNames(prototype);
    for (const prop of props) {
      if (
        //prop.startsWith('get') ||  // e.g. getProgramParameter, but too risky
        [
          "isContextLost",
          "canvas",
          "drawingBufferWidth",
          "drawingBufferHeight"
        ].includes(prop)
      ) {
        continue;
      }
      try {
        if (typeof prototype[prop] !== "function") {
          continue;
        }
        const restoreHandler = patch2(
          prototype,
          prop,
          function(original) {
            return function(...args) {
              const result22 = original.apply(this, args);
              saveWebGLVar(result22, win, this);
              if ("tagName" in this.canvas && !isBlocked2(this.canvas, blockClass, blockSelector, true)) {
                const recordArgs = serializeArgs(args, win, this);
                const mutation = {
                  type,
                  property: prop,
                  args: recordArgs
                };
                cb(this.canvas, mutation);
              }
              return result22;
            };
          }
        );
        handlers2.push(restoreHandler);
      } catch {
        const hookHandler = hookSetter2(prototype, prop, {
          set(v2) {
            cb(this.canvas, {
              type,
              property: prop,
              args: [v2],
              setter: true
            });
          }
        });
        handlers2.push(hookHandler);
      }
    }
    return handlers2;
  }
  function initCanvasWebGLMutationObserver(cb, win, blockClass, blockSelector) {
    const handlers2 = [];
    handlers2.push(
      ...patchGLPrototype(
        win.WebGLRenderingContext.prototype,
        CanvasContext.WebGL,
        cb,
        blockClass,
        blockSelector,
        win
      )
    );
    if (typeof win.WebGL2RenderingContext !== "undefined") {
      handlers2.push(
        ...patchGLPrototype(
          win.WebGL2RenderingContext.prototype,
          CanvasContext.WebGL2,
          cb,
          blockClass,
          blockSelector,
          win
        )
      );
    }
    return () => {
      handlers2.forEach((h2) => h2());
    };
  }
  function WorkerWrapper(options) {
    let objURL;
    try {
      objURL = blob && (self.URL || self.webkitURL).createObjectURL(blob);
      if (!objURL) throw "";
      const worker = new Worker(objURL, {
        name: options == null ? void 0 : options.name
      });
      worker.addEventListener("error", () => {
        (self.URL || self.webkitURL).revokeObjectURL(objURL);
      });
      return worker;
    } catch (e2) {
      return new Worker(
        "data:text/javascript;charset=utf-8," + encodeURIComponent(jsContent2),
        {
          name: options == null ? void 0 : options.name
        }
      );
    } finally {
      objURL && (self.URL || self.webkitURL).revokeObjectURL(objURL);
    }
  }
  function record(options = {}) {
    const {
      emit,
      checkoutEveryNms,
      checkoutEveryNth,
      blockClass = "rr-block",
      blockSelector = null,
      ignoreClass = "rr-ignore",
      ignoreSelector = null,
      maskTextClass = "rr-mask",
      maskTextSelector = null,
      inlineStylesheet = true,
      maskAllInputs,
      maskInputOptions: _maskInputOptions,
      slimDOMOptions: _slimDOMOptions,
      maskInputFn,
      maskTextFn,
      maskAttributeFn: maskAttributeFn2,
      hooks,
      packFn,
      sampling = {},
      dataURLOptions = {},
      mousemoveWait,
      recordDOM = true,
      recordCanvas = false,
      recordCrossOriginIframes = false,
      recordAfter = options.recordAfter === "DOMContentLoaded" ? options.recordAfter : "load",
      userTriggeredOnInput = false,
      collectFonts = false,
      inlineImages = false,
      captureAdoptedStyleSheets = true,
      plugins,
      keepIframeSrcFn = () => false,
      ignoreCSSAttributes = /* @__PURE__ */ new Set([]),
      errorHandler: errorHandler2,
      applyBackgroundColorToBlockedElements = false
    } = options;
    registerErrorHandler(errorHandler2);
    const inEmittingFrame = recordCrossOriginIframes ? window.parent === window : true;
    let passEmitsToParent = false;
    if (!inEmittingFrame) {
      try {
        if (window.parent.document) {
          passEmitsToParent = false;
        }
      } catch (e2) {
        passEmitsToParent = true;
      }
    }
    if (inEmittingFrame && !emit) {
      throw new Error("emit function is required");
    }
    if (!inEmittingFrame && !passEmitsToParent) {
      return () => {
      };
    }
    if (mousemoveWait !== void 0 && sampling.mousemove === void 0) {
      sampling.mousemove = mousemoveWait;
    }
    mirror.reset();
    const maskInputOptions = maskAllInputs === true ? {
      color: true,
      date: true,
      "datetime-local": true,
      email: true,
      month: true,
      number: true,
      range: true,
      search: true,
      tel: true,
      text: true,
      time: true,
      url: true,
      week: true,
      textarea: true,
      select: true,
      password: true
    } : _maskInputOptions !== void 0 ? _maskInputOptions : { password: true };
    const slimDOMOptions = _slimDOMOptions === true || _slimDOMOptions === "all" ? {
      script: true,
      comment: true,
      headFavicon: true,
      headWhitespace: true,
      headMetaSocial: true,
      headMetaRobots: true,
      headMetaHttpEquiv: true,
      headMetaVerification: true,
      // the following are off for slimDOMOptions === true,
      // as they destroy some (hidden) info:
      headMetaAuthorship: _slimDOMOptions === "all",
      headMetaDescKeywords: _slimDOMOptions === "all",
      headTitleMutations: _slimDOMOptions === "all"
    } : _slimDOMOptions ? _slimDOMOptions : {};
    polyfill$12();
    let lastFullSnapshotEvent;
    let incrementalSnapshotCount = 0;
    const eventProcessor = (e2) => {
      for (const plugin of plugins || []) {
        if (plugin.eventProcessor) {
          e2 = plugin.eventProcessor(e2);
        }
      }
      if (packFn && // Disable packing events which will be emitted to parent frames.
      !passEmitsToParent) {
        e2 = packFn(e2);
      }
      return e2;
    };
    wrappedEmit = (r2, isCheckout) => {
      var _a22;
      const e2 = r2;
      e2.timestamp = nowTimestamp2();
      if (((_a22 = mutationBuffers[0]) == null ? void 0 : _a22.isFrozen()) && e2.type !== EventType2.FullSnapshot && !(e2.type === EventType2.IncrementalSnapshot && e2.data.source === IncrementalSource2.Mutation)) {
        mutationBuffers.forEach((buf) => buf.unfreeze());
      }
      if (inEmittingFrame) {
        emit == null ? void 0 : emit(eventProcessor(e2), isCheckout);
      } else if (passEmitsToParent) {
        const message = {
          type: "rrweb",
          event: eventProcessor(e2),
          origin: window.location.origin,
          isCheckout
        };
        window.parent.postMessage(message, "*");
      }
      if (e2.type === EventType2.FullSnapshot) {
        lastFullSnapshotEvent = e2;
        incrementalSnapshotCount = 0;
      } else if (e2.type === EventType2.IncrementalSnapshot) {
        if (e2.data.source === IncrementalSource2.Mutation && e2.data.isAttachIframe) {
          return;
        }
        incrementalSnapshotCount++;
        const exceedCount = checkoutEveryNth && incrementalSnapshotCount >= checkoutEveryNth;
        const exceedTime = checkoutEveryNms && e2.timestamp - lastFullSnapshotEvent.timestamp > checkoutEveryNms;
        if (exceedCount || exceedTime) {
          takeFullSnapshot$1(true);
        }
      }
    };
    const wrappedMutationEmit = (m2) => {
      wrappedEmit({
        type: EventType2.IncrementalSnapshot,
        data: {
          source: IncrementalSource2.Mutation,
          ...m2
        }
      });
    };
    const wrappedScrollEmit = (p2) => wrappedEmit({
      type: EventType2.IncrementalSnapshot,
      data: {
        source: IncrementalSource2.Scroll,
        ...p2
      }
    });
    const wrappedCanvasMutationEmit = (p2) => wrappedEmit({
      type: EventType2.IncrementalSnapshot,
      data: {
        source: IncrementalSource2.CanvasMutation,
        ...p2
      }
    });
    const wrappedAdoptedStyleSheetEmit = (a2) => wrappedEmit({
      type: EventType2.IncrementalSnapshot,
      data: {
        source: IncrementalSource2.AdoptedStyleSheet,
        ...a2
      }
    });
    const stylesheetManager = new StylesheetManager({
      mutationCb: wrappedMutationEmit,
      adoptedStyleSheetCb: wrappedAdoptedStyleSheetEmit
    });
    const iframeManager = new IframeManager({
      mirror,
      mutationCb: wrappedMutationEmit,
      stylesheetManager,
      recordCrossOriginIframes,
      wrappedEmit
    });
    for (const plugin of plugins || []) {
      if (plugin.getMirror)
        plugin.getMirror({
          nodeMirror: mirror,
          crossOriginIframeMirror: iframeManager.crossOriginIframeMirror,
          crossOriginIframeStyleMirror: iframeManager.crossOriginIframeStyleMirror
        });
    }
    const processedNodeManager = new ProcessedNodeManager();
    canvasManager = new CanvasManager({
      recordCanvas,
      mutationCb: wrappedCanvasMutationEmit,
      win: window,
      blockClass,
      blockSelector,
      mirror,
      sampling: sampling.canvas,
      dataURLOptions
    });
    const shadowDomManager = new ShadowDomManager({
      mutationCb: wrappedMutationEmit,
      scrollCb: wrappedScrollEmit,
      bypassOptions: {
        blockClass,
        blockSelector,
        maskTextClass,
        maskTextSelector,
        inlineStylesheet,
        maskInputOptions,
        dataURLOptions,
        maskTextFn,
        maskInputFn,
        maskAttributeFn: maskAttributeFn2,
        recordCanvas,
        inlineImages,
        sampling,
        slimDOMOptions,
        iframeManager,
        stylesheetManager,
        canvasManager,
        keepIframeSrcFn,
        processedNodeManager
      },
      mirror
    });
    takeFullSnapshot$1 = (isCheckout = false) => {
      if (!recordDOM) {
        return;
      }
      wrappedEmit(
        {
          type: EventType2.Meta,
          data: {
            href: window.location.href,
            width: getWindowWidth2(),
            height: getWindowHeight2()
          }
        },
        isCheckout
      );
      stylesheetManager.reset();
      shadowDomManager.init();
      mutationBuffers.forEach((buf) => buf.lock());
      const node22 = snapshot(document, {
        mirror,
        blockClass,
        blockSelector,
        maskTextClass,
        maskTextSelector,
        inlineStylesheet,
        maskAllInputs: maskInputOptions,
        maskTextFn,
        maskInputFn,
        maskAttributeFn: maskAttributeFn2,
        slimDOM: slimDOMOptions,
        dataURLOptions,
        recordCanvas,
        inlineImages,
        applyBackgroundColorToBlockedElements,
        onSerialize: (n22) => {
          if (isSerializedIframe2(n22, mirror)) {
            iframeManager.addIframe(n22);
          }
          if (isSerializedStylesheet2(n22, mirror)) {
            stylesheetManager.trackLinkElement(n22);
          }
          if (hasShadowRoot2(n22)) {
            shadowDomManager.addShadowRoot(index3.shadowRoot(n22), document);
          }
        },
        // stylesheetManager.reset() runs just before snapshot() so styleMirror
        // ids are always fresh and in sync with the snapshot's serialized styleIds.
        onAdoptedStyleSheet: captureAdoptedStyleSheets ? (sheet) => stylesheetManager.styleMirror.add(sheet) : void 0,
        onIframeLoad: (iframe, childSn) => {
          iframeManager.attachIframe(iframe, childSn);
          shadowDomManager.observeAttachShadow(iframe);
        },
        onStylesheetLoad: (linkEl, childSn) => {
          stylesheetManager.attachLinkElement(linkEl, childSn);
        },
        keepIframeSrcFn
      });
      if (!node22) {
        return console.warn("Failed to snapshot the document");
      }
      wrappedEmit(
        {
          type: EventType2.FullSnapshot,
          data: {
            node: node22,
            initialOffset: getWindowScroll2(window)
          }
        },
        isCheckout
      );
      mutationBuffers.forEach((buf) => buf.unlock());
      if (document.adoptedStyleSheets && document.adoptedStyleSheets.length > 0)
        stylesheetManager.adoptStyleSheets(
          document.adoptedStyleSheets,
          mirror.getId(document)
        );
    };
    try {
      const handlers2 = [];
      const observe = (doc) => {
        var _a22;
        return callbackWrapper(initObservers)(
          {
            mutationCb: wrappedMutationEmit,
            mousemoveCb: (positions, source) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source,
                positions
              }
            }),
            mouseInteractionCb: (d2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.MouseInteraction,
                ...d2
              }
            }),
            scrollCb: wrappedScrollEmit,
            viewportResizeCb: (d2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.ViewportResize,
                ...d2
              }
            }),
            inputCb: (v2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.Input,
                ...v2
              }
            }),
            mediaInteractionCb: (p2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.MediaInteraction,
                ...p2
              }
            }),
            styleSheetRuleCb: (r2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.StyleSheetRule,
                ...r2
              }
            }),
            styleDeclarationCb: (r2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.StyleDeclaration,
                ...r2
              }
            }),
            canvasMutationCb: wrappedCanvasMutationEmit,
            fontCb: (p2) => wrappedEmit({
              type: EventType2.IncrementalSnapshot,
              data: {
                source: IncrementalSource2.Font,
                ...p2
              }
            }),
            selectionCb: (p2) => {
              wrappedEmit({
                type: EventType2.IncrementalSnapshot,
                data: {
                  source: IncrementalSource2.Selection,
                  ...p2
                }
              });
            },
            customElementCb: (c2) => {
              wrappedEmit({
                type: EventType2.IncrementalSnapshot,
                data: {
                  source: IncrementalSource2.CustomElement,
                  ...c2
                }
              });
            },
            blockClass,
            ignoreClass,
            ignoreSelector,
            maskTextClass,
            maskTextSelector,
            maskInputOptions,
            inlineStylesheet,
            sampling,
            recordDOM,
            recordCanvas,
            inlineImages,
            userTriggeredOnInput,
            collectFonts,
            doc,
            maskInputFn,
            maskTextFn,
            maskAttributeFn: maskAttributeFn2,
            keepIframeSrcFn,
            blockSelector,
            slimDOMOptions,
            dataURLOptions,
            mirror,
            iframeManager,
            stylesheetManager,
            shadowDomManager,
            processedNodeManager,
            canvasManager,
            ignoreCSSAttributes,
            plugins: ((_a22 = plugins == null ? void 0 : plugins.filter((p2) => p2.observer)) == null ? void 0 : _a22.map((p2) => ({
              observer: p2.observer,
              options: p2.options,
              callback: (payload) => wrappedEmit({
                type: EventType2.Plugin,
                data: {
                  plugin: p2.name,
                  payload
                }
              })
            }))) || []
          },
          hooks
        );
      };
      iframeManager.addLoadListener((iframeEl) => {
        try {
          handlers2.push(observe(iframeEl.contentDocument));
        } catch (error) {
          console.warn(error);
        }
      });
      const init3 = () => {
        takeFullSnapshot$1();
        handlers2.push(observe(document));
        recording = true;
      };
      if (document.readyState === "interactive" || document.readyState === "complete") {
        init3();
      } else {
        handlers2.push(
          on2("DOMContentLoaded", () => {
            wrappedEmit({
              type: EventType2.DomContentLoaded,
              data: {}
            });
            if (recordAfter === "DOMContentLoaded") init3();
          })
        );
        handlers2.push(
          on2(
            "load",
            () => {
              wrappedEmit({
                type: EventType2.Load,
                data: {}
              });
              if (recordAfter === "load") init3();
            },
            window
          )
        );
      }
      return () => {
        handlers2.forEach((h2) => h2());
        processedNodeManager.destroy();
        recording = false;
        unregisterErrorHandler();
      };
    } catch (error) {
      console.warn(error);
    }
  }
  var __defProp3, __defNormalProp3, __publicField3, _a3, __defProp$12, __defNormalProp$12, __publicField$12, NodeType$3, testableAccessors$12, testableMethods$12, untaintedBasePrototype$12, untaintedAccessorCache$12, untaintedMethodCache$12, index$12, Mirror2, ORIGINAL_ATTRIBUTE_NAME, URL_IN_CSS_REF, URL_PROTOCOL_MATCH, URL_WWW_MATCH, DATA_URI, _DEFAULT_BLOCKED_ELEMENT_BACKGROUND_COLOR, _id, tagNameRegex, IGNORED_NODE2, canvasService, canvasCtx, SRCSET_NOT_SPACES, SRCSET_COMMAS_OR_SPACES, cachedDocument, picocolors_browser$12, hasRequiredPicocolors_browser$12, __viteBrowserExternal$22, __viteBrowserExternal$1$12, require$$2$12, cssSyntaxError$12, hasRequiredCssSyntaxError$12, symbols$12, hasRequiredSymbols$12, stringifier$12, hasRequiredStringifier$12, stringify_1$12, hasRequiredStringify$12, node$12, hasRequiredNode$12, declaration$12, hasRequiredDeclaration$12, nonSecure$12, hasRequiredNonSecure$12, previousMap$12, hasRequiredPreviousMap$12, input$12, hasRequiredInput$12, mapGenerator$12, hasRequiredMapGenerator$12, comment$12, hasRequiredComment$12, container$12, hasRequiredContainer$12, document$1$12, hasRequiredDocument$12, warnOnce$12, hasRequiredWarnOnce$12, warning$12, hasRequiredWarning$12, result$12, hasRequiredResult$12, tokenize$12, hasRequiredTokenize$12, atRule$12, hasRequiredAtRule$12, root$12, hasRequiredRoot$12, list_1$12, hasRequiredList$12, rule$12, hasRequiredRule$12, parser$12, hasRequiredParser$12, parse_1$12, hasRequiredParse$12, lazyResult$12, hasRequiredLazyResult$12, noWorkResult$12, hasRequiredNoWorkResult$12, processor$12, hasRequiredProcessor$12, fromJSON_1$12, hasRequiredFromJSON$12, postcss_1$12, hasRequiredPostcss$12, postcssExports$12, postcss$12, __defProp23, __defNormalProp23, __publicField23, picocolors_browser2, hasRequiredPicocolors_browser2, __viteBrowserExternal2, __viteBrowserExternal$12, require$$22, cssSyntaxError2, hasRequiredCssSyntaxError2, symbols2, hasRequiredSymbols2, stringifier2, hasRequiredStringifier2, stringify_12, hasRequiredStringify2, node2, hasRequiredNode2, declaration2, hasRequiredDeclaration2, nonSecure2, hasRequiredNonSecure2, previousMap2, hasRequiredPreviousMap2, input2, hasRequiredInput2, mapGenerator2, hasRequiredMapGenerator2, comment2, hasRequiredComment2, container2, hasRequiredContainer2, document$12, hasRequiredDocument2, warnOnce2, hasRequiredWarnOnce2, warning2, hasRequiredWarning2, result2, hasRequiredResult2, tokenize2, hasRequiredTokenize2, atRule2, hasRequiredAtRule2, root2, hasRequiredRoot2, list_12, hasRequiredList2, rule2, hasRequiredRule2, parser2, hasRequiredParser2, parse_12, hasRequiredParse2, lazyResult2, hasRequiredLazyResult2, noWorkResult2, hasRequiredNoWorkResult2, processor2, hasRequiredProcessor2, fromJSON_12, hasRequiredFromJSON2, postcss_12, hasRequiredPostcss2, postcssExports2, postcss2, BaseRRNode2, testableAccessors2, testableMethods2, untaintedBasePrototype2, untaintedAccessorCache2, untaintedMethodCache2, index3, DEPARTED_MIRROR_ACCESS_WARNING2, _mirror2, nowTimestamp2, StyleSheetMirror2, EventType2, IncrementalSource2, MouseInteractions2, PointerTypes, CanvasContext, MediaInteractions, NodeType, DoubleLinkedList, moveKey, MutationBuffer, errorHandler, callbackWrapper, mutationBuffers, INPUT_TAGS, lastInputValueMap, CrossOriginIframeMirror, IframeManager, ShadowDomManager, chars2, lookup2, i$1, encode2, canvasVarMap, saveWebGLVar, serializeArgs, isInstanceOfWebGLObject, jsContent2, blob, CanvasManager, StylesheetManager, ProcessedNodeManager, wrappedEmit, takeFullSnapshot$1, canvasManager, recording, mirror, n3, addCustomEvent, freezePage, takeFullSnapshot;
  var init_rrweb_record = __esm({
    "node_modules/@amplitude/rrweb-record/dist/rrweb-record.js"() {
      __defProp3 = Object.defineProperty;
      __defNormalProp3 = (obj, key, value) => key in obj ? __defProp3(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField3 = (obj, key, value) => __defNormalProp3(obj, typeof key !== "symbol" ? key + "" : key, value);
      __defProp$12 = Object.defineProperty;
      __defNormalProp$12 = (obj, key, value) => key in obj ? __defProp$12(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField$12 = (obj, key, value) => __defNormalProp$12(obj, typeof key !== "symbol" ? key + "" : key, value);
      NodeType$3 = /* @__PURE__ */ ((NodeType2) => {
        NodeType2[NodeType2["Document"] = 0] = "Document";
        NodeType2[NodeType2["DocumentType"] = 1] = "DocumentType";
        NodeType2[NodeType2["Element"] = 2] = "Element";
        NodeType2[NodeType2["Text"] = 3] = "Text";
        NodeType2[NodeType2["CDATA"] = 4] = "CDATA";
        NodeType2[NodeType2["Comment"] = 5] = "Comment";
        return NodeType2;
      })(NodeType$3 || {});
      testableAccessors$12 = {
        Node: ["childNodes", "parentNode", "parentElement", "textContent"],
        ShadowRoot: ["host", "styleSheets"],
        Element: ["shadowRoot", "querySelector", "querySelectorAll"],
        MutationObserver: []
      };
      testableMethods$12 = {
        Node: ["contains", "getRootNode"],
        ShadowRoot: ["getSelection"],
        Element: [],
        MutationObserver: ["constructor"]
      };
      untaintedBasePrototype$12 = {};
      untaintedAccessorCache$12 = {};
      untaintedMethodCache$12 = {};
      index$12 = {
        childNodes: childNodes$12,
        parentNode: parentNode$12,
        parentElement: parentElement$12,
        textContent: textContent$12,
        contains: contains$12,
        getRootNode: getRootNode$12,
        host: host$12,
        styleSheets: styleSheets$12,
        shadowRoot: shadowRoot$12,
        querySelector: querySelector$12,
        querySelectorAll: querySelectorAll$12,
        mutationObserver: mutationObserverCtor$12
      };
      Mirror2 = class {
        constructor() {
          __publicField$12(this, "idNodeMap", /* @__PURE__ */ new Map());
          __publicField$12(this, "nodeMetaMap", /* @__PURE__ */ new WeakMap());
        }
        getId(n22) {
          var _a22;
          if (!n22) return -1;
          const id2 = (_a22 = this.getMeta(n22)) == null ? void 0 : _a22.id;
          return id2 != null ? id2 : -1;
        }
        getNode(id2) {
          return this.idNodeMap.get(id2) || null;
        }
        getIds() {
          return Array.from(this.idNodeMap.keys());
        }
        getMeta(n22) {
          return this.nodeMetaMap.get(n22) || null;
        }
        // removes the node from idNodeMap
        // doesn't remove the node from nodeMetaMap
        removeNodeFromMap(n22) {
          const id2 = this.getId(n22);
          this.idNodeMap.delete(id2);
          if (n22.childNodes) {
            n22.childNodes.forEach(
              (childNode) => this.removeNodeFromMap(childNode)
            );
          }
        }
        has(id2) {
          return this.idNodeMap.has(id2);
        }
        hasNode(node22) {
          return this.nodeMetaMap.has(node22);
        }
        add(n22, meta) {
          const id2 = meta.id;
          this.idNodeMap.set(id2, n22);
          this.nodeMetaMap.set(n22, meta);
        }
        replace(id2, n22) {
          const oldNode = this.getNode(id2);
          if (oldNode) {
            const meta = this.nodeMetaMap.get(oldNode);
            if (meta) this.nodeMetaMap.set(n22, meta);
          }
          this.idNodeMap.set(id2, n22);
        }
        reset() {
          this.idNodeMap = /* @__PURE__ */ new Map();
          this.nodeMetaMap = /* @__PURE__ */ new WeakMap();
        }
      };
      ORIGINAL_ATTRIBUTE_NAME = "__rrweb_original__";
      URL_IN_CSS_REF = /url\((?:(')([^']*)'|(")(.*?)"|([^)]*))\)/gm;
      URL_PROTOCOL_MATCH = /^(?:[a-z+]+:)?\/\//i;
      URL_WWW_MATCH = /^www\..*/i;
      DATA_URI = /^(data:)([^,]*),(.*)/i;
      _DEFAULT_BLOCKED_ELEMENT_BACKGROUND_COLOR = "lightgrey";
      _id = 1;
      tagNameRegex = new RegExp("[^a-z0-9-_:]");
      IGNORED_NODE2 = -2;
      SRCSET_NOT_SPACES = /^[^ \t\n\r\u000c]+/;
      SRCSET_COMMAS_OR_SPACES = /^[, \t\n\r\u000c]+/;
      cachedDocument = /* @__PURE__ */ new WeakMap();
      picocolors_browser$12 = { exports: {} };
      __viteBrowserExternal$22 = {};
      __viteBrowserExternal$1$12 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
        __proto__: null,
        default: __viteBrowserExternal$22
      }, Symbol.toStringTag, { value: "Module" }));
      require$$2$12 = /* @__PURE__ */ getAugmentedNamespace$12(__viteBrowserExternal$1$12);
      symbols$12 = {};
      postcssExports$12 = requirePostcss$12();
      postcss$12 = /* @__PURE__ */ getDefaultExportFromCjs$12(postcssExports$12);
      postcss$12.stringify;
      postcss$12.fromJSON;
      postcss$12.plugin;
      postcss$12.parse;
      postcss$12.list;
      postcss$12.document;
      postcss$12.comment;
      postcss$12.atRule;
      postcss$12.rule;
      postcss$12.decl;
      postcss$12.root;
      postcss$12.CssSyntaxError;
      postcss$12.Declaration;
      postcss$12.Container;
      postcss$12.Processor;
      postcss$12.Document;
      postcss$12.Comment;
      postcss$12.Warning;
      postcss$12.AtRule;
      postcss$12.Result;
      postcss$12.Input;
      postcss$12.Rule;
      postcss$12.Root;
      postcss$12.Node;
      __defProp23 = Object.defineProperty;
      __defNormalProp23 = (obj, key, value) => key in obj ? __defProp23(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
      __publicField23 = (obj, key, value) => __defNormalProp23(obj, typeof key !== "symbol" ? key + "" : key, value);
      picocolors_browser2 = { exports: {} };
      __viteBrowserExternal2 = {};
      __viteBrowserExternal$12 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
        __proto__: null,
        default: __viteBrowserExternal2
      }, Symbol.toStringTag, { value: "Module" }));
      require$$22 = /* @__PURE__ */ getAugmentedNamespace2(__viteBrowserExternal$12);
      symbols2 = {};
      postcssExports2 = requirePostcss2();
      postcss2 = /* @__PURE__ */ getDefaultExportFromCjs2(postcssExports2);
      postcss2.stringify;
      postcss2.fromJSON;
      postcss2.plugin;
      postcss2.parse;
      postcss2.list;
      postcss2.document;
      postcss2.comment;
      postcss2.atRule;
      postcss2.rule;
      postcss2.decl;
      postcss2.root;
      postcss2.CssSyntaxError;
      postcss2.Declaration;
      postcss2.Container;
      postcss2.Processor;
      postcss2.Document;
      postcss2.Comment;
      postcss2.Warning;
      postcss2.AtRule;
      postcss2.Result;
      postcss2.Input;
      postcss2.Rule;
      postcss2.Root;
      postcss2.Node;
      BaseRRNode2 = class _BaseRRNode {
        // eslint-disable-next-line @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any
        constructor(..._args) {
          __publicField23(this, "parentElement", null);
          __publicField23(this, "parentNode", null);
          __publicField23(this, "ownerDocument");
          __publicField23(this, "firstChild", null);
          __publicField23(this, "lastChild", null);
          __publicField23(this, "previousSibling", null);
          __publicField23(this, "nextSibling", null);
          __publicField23(this, "ELEMENT_NODE", 1);
          __publicField23(this, "TEXT_NODE", 3);
          __publicField23(this, "nodeType");
          __publicField23(this, "nodeName");
          __publicField23(this, "RRNodeType");
        }
        get childNodes() {
          const childNodes22 = [];
          let childIterator = this.firstChild;
          while (childIterator) {
            childNodes22.push(childIterator);
            childIterator = childIterator.nextSibling;
          }
          return childNodes22;
        }
        contains(node22) {
          if (!(node22 instanceof _BaseRRNode)) return false;
          else if (node22.ownerDocument !== this.ownerDocument) return false;
          else if (node22 === this) return true;
          while (node22.parentNode) {
            if (node22.parentNode === this) return true;
            node22 = node22.parentNode;
          }
          return false;
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        appendChild(_newChild) {
          throw new Error(
            `RRDomException: Failed to execute 'appendChild' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        insertBefore(_newChild, _refChild) {
          throw new Error(
            `RRDomException: Failed to execute 'insertBefore' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        removeChild(_node) {
          throw new Error(
            `RRDomException: Failed to execute 'removeChild' on 'RRNode': This RRNode type does not support this method.`
          );
        }
        toString() {
          return "RRNode";
        }
      };
      testableAccessors2 = {
        Node: ["childNodes", "parentNode", "parentElement", "textContent"],
        ShadowRoot: ["host", "styleSheets"],
        Element: ["shadowRoot", "querySelector", "querySelectorAll"],
        MutationObserver: []
      };
      testableMethods2 = {
        Node: ["contains", "getRootNode"],
        ShadowRoot: ["getSelection"],
        Element: [],
        MutationObserver: ["constructor"]
      };
      untaintedBasePrototype2 = {};
      untaintedAccessorCache2 = {};
      untaintedMethodCache2 = {};
      index3 = {
        childNodes: childNodes2,
        parentNode: parentNode2,
        parentElement: parentElement2,
        textContent: textContent2,
        contains: contains2,
        getRootNode: getRootNode2,
        host: host2,
        styleSheets: styleSheets2,
        shadowRoot: shadowRoot2,
        querySelector: querySelector2,
        querySelectorAll: querySelectorAll2,
        mutationObserver: mutationObserverCtor2
      };
      DEPARTED_MIRROR_ACCESS_WARNING2 = "Please stop import mirror directly. Instead of that,\r\nnow you can use replayer.getMirror() to access the mirror instance of a replayer,\r\nor you can use record.mirror to access the mirror instance during recording.";
      _mirror2 = {
        map: {},
        getId() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
          return -1;
        },
        getNode() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
          return null;
        },
        removeNodeFromMap() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
        },
        has() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
          return false;
        },
        reset() {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
        }
      };
      if (typeof window !== "undefined" && window.Proxy && window.Reflect) {
        _mirror2 = new Proxy(_mirror2, {
          get(target, prop, receiver) {
            if (prop === "map") {
              console.error(DEPARTED_MIRROR_ACCESS_WARNING2);
            }
            return Reflect.get(target, prop, receiver);
          }
        });
      }
      nowTimestamp2 = Date.now;
      if (!/* @__PURE__ */ /[1-9][0-9]{12}/.test(Date.now().toString())) {
        nowTimestamp2 = () => (/* @__PURE__ */ new Date()).getTime();
      }
      StyleSheetMirror2 = class {
        constructor() {
          __publicField3(this, "id", 1);
          __publicField3(this, "styleIDMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "idStyleMap", /* @__PURE__ */ new Map());
        }
        getId(stylesheet) {
          var _a4;
          return (_a4 = this.styleIDMap.get(stylesheet)) != null ? _a4 : -1;
        }
        has(stylesheet) {
          return this.styleIDMap.has(stylesheet);
        }
        /**
         * @returns If the stylesheet is in the mirror, returns the id of the stylesheet. If not, return the new assigned id.
         */
        add(stylesheet, id2) {
          if (this.has(stylesheet)) return this.getId(stylesheet);
          let newId;
          if (id2 === void 0) {
            newId = this.id++;
          } else newId = id2;
          this.styleIDMap.set(stylesheet, newId);
          this.idStyleMap.set(newId, stylesheet);
          return newId;
        }
        getStyle(id2) {
          return this.idStyleMap.get(id2) || null;
        }
        reset() {
          this.styleIDMap = /* @__PURE__ */ new WeakMap();
          this.idStyleMap = /* @__PURE__ */ new Map();
          this.id = 1;
        }
        generateId() {
          return this.id++;
        }
      };
      EventType2 = /* @__PURE__ */ ((EventType22) => {
        EventType22[EventType22["DomContentLoaded"] = 0] = "DomContentLoaded";
        EventType22[EventType22["Load"] = 1] = "Load";
        EventType22[EventType22["FullSnapshot"] = 2] = "FullSnapshot";
        EventType22[EventType22["IncrementalSnapshot"] = 3] = "IncrementalSnapshot";
        EventType22[EventType22["Meta"] = 4] = "Meta";
        EventType22[EventType22["Custom"] = 5] = "Custom";
        EventType22[EventType22["Plugin"] = 6] = "Plugin";
        return EventType22;
      })(EventType2 || {});
      IncrementalSource2 = /* @__PURE__ */ ((IncrementalSource22) => {
        IncrementalSource22[IncrementalSource22["Mutation"] = 0] = "Mutation";
        IncrementalSource22[IncrementalSource22["MouseMove"] = 1] = "MouseMove";
        IncrementalSource22[IncrementalSource22["MouseInteraction"] = 2] = "MouseInteraction";
        IncrementalSource22[IncrementalSource22["Scroll"] = 3] = "Scroll";
        IncrementalSource22[IncrementalSource22["ViewportResize"] = 4] = "ViewportResize";
        IncrementalSource22[IncrementalSource22["Input"] = 5] = "Input";
        IncrementalSource22[IncrementalSource22["TouchMove"] = 6] = "TouchMove";
        IncrementalSource22[IncrementalSource22["MediaInteraction"] = 7] = "MediaInteraction";
        IncrementalSource22[IncrementalSource22["StyleSheetRule"] = 8] = "StyleSheetRule";
        IncrementalSource22[IncrementalSource22["CanvasMutation"] = 9] = "CanvasMutation";
        IncrementalSource22[IncrementalSource22["Font"] = 10] = "Font";
        IncrementalSource22[IncrementalSource22["Log"] = 11] = "Log";
        IncrementalSource22[IncrementalSource22["Drag"] = 12] = "Drag";
        IncrementalSource22[IncrementalSource22["StyleDeclaration"] = 13] = "StyleDeclaration";
        IncrementalSource22[IncrementalSource22["Selection"] = 14] = "Selection";
        IncrementalSource22[IncrementalSource22["AdoptedStyleSheet"] = 15] = "AdoptedStyleSheet";
        IncrementalSource22[IncrementalSource22["CustomElement"] = 16] = "CustomElement";
        return IncrementalSource22;
      })(IncrementalSource2 || {});
      MouseInteractions2 = /* @__PURE__ */ ((MouseInteractions22) => {
        MouseInteractions22[MouseInteractions22["MouseUp"] = 0] = "MouseUp";
        MouseInteractions22[MouseInteractions22["MouseDown"] = 1] = "MouseDown";
        MouseInteractions22[MouseInteractions22["Click"] = 2] = "Click";
        MouseInteractions22[MouseInteractions22["ContextMenu"] = 3] = "ContextMenu";
        MouseInteractions22[MouseInteractions22["DblClick"] = 4] = "DblClick";
        MouseInteractions22[MouseInteractions22["Focus"] = 5] = "Focus";
        MouseInteractions22[MouseInteractions22["Blur"] = 6] = "Blur";
        MouseInteractions22[MouseInteractions22["TouchStart"] = 7] = "TouchStart";
        MouseInteractions22[MouseInteractions22["TouchMove_Departed"] = 8] = "TouchMove_Departed";
        MouseInteractions22[MouseInteractions22["TouchEnd"] = 9] = "TouchEnd";
        MouseInteractions22[MouseInteractions22["TouchCancel"] = 10] = "TouchCancel";
        return MouseInteractions22;
      })(MouseInteractions2 || {});
      PointerTypes = /* @__PURE__ */ ((PointerTypes2) => {
        PointerTypes2[PointerTypes2["Mouse"] = 0] = "Mouse";
        PointerTypes2[PointerTypes2["Pen"] = 1] = "Pen";
        PointerTypes2[PointerTypes2["Touch"] = 2] = "Touch";
        return PointerTypes2;
      })(PointerTypes || {});
      CanvasContext = /* @__PURE__ */ ((CanvasContext2) => {
        CanvasContext2[CanvasContext2["2D"] = 0] = "2D";
        CanvasContext2[CanvasContext2["WebGL"] = 1] = "WebGL";
        CanvasContext2[CanvasContext2["WebGL2"] = 2] = "WebGL2";
        return CanvasContext2;
      })(CanvasContext || {});
      MediaInteractions = /* @__PURE__ */ ((MediaInteractions2) => {
        MediaInteractions2[MediaInteractions2["Play"] = 0] = "Play";
        MediaInteractions2[MediaInteractions2["Pause"] = 1] = "Pause";
        MediaInteractions2[MediaInteractions2["Seeked"] = 2] = "Seeked";
        MediaInteractions2[MediaInteractions2["VolumeChange"] = 3] = "VolumeChange";
        MediaInteractions2[MediaInteractions2["RateChange"] = 4] = "RateChange";
        return MediaInteractions2;
      })(MediaInteractions || {});
      NodeType = /* @__PURE__ */ ((NodeType2) => {
        NodeType2[NodeType2["Document"] = 0] = "Document";
        NodeType2[NodeType2["DocumentType"] = 1] = "DocumentType";
        NodeType2[NodeType2["Element"] = 2] = "Element";
        NodeType2[NodeType2["Text"] = 3] = "Text";
        NodeType2[NodeType2["CDATA"] = 4] = "CDATA";
        NodeType2[NodeType2["Comment"] = 5] = "Comment";
        return NodeType2;
      })(NodeType || {});
      DoubleLinkedList = class {
        constructor() {
          __publicField3(this, "length", 0);
          __publicField3(this, "head", null);
          __publicField3(this, "tail", null);
        }
        get(position) {
          if (position >= this.length) {
            throw new Error("Position outside of list range");
          }
          let current = this.head;
          for (let index22 = 0; index22 < position; index22++) {
            current = (current == null ? void 0 : current.next) || null;
          }
          return current;
        }
        addNode(n22) {
          const node22 = {
            value: n22,
            previous: null,
            next: null
          };
          n22.__ln = node22;
          if (n22.previousSibling && isNodeInLinkedList(n22.previousSibling)) {
            const current = n22.previousSibling.__ln.next;
            node22.next = current;
            node22.previous = n22.previousSibling.__ln;
            n22.previousSibling.__ln.next = node22;
            if (current) {
              current.previous = node22;
            }
          } else if (n22.nextSibling && isNodeInLinkedList(n22.nextSibling) && n22.nextSibling.__ln.previous) {
            const current = n22.nextSibling.__ln.previous;
            node22.previous = current;
            node22.next = n22.nextSibling.__ln;
            n22.nextSibling.__ln.previous = node22;
            if (current) {
              current.next = node22;
            }
          } else {
            if (this.head) {
              this.head.previous = node22;
            }
            node22.next = this.head;
            this.head = node22;
          }
          if (node22.next === null) {
            this.tail = node22;
          }
          this.length++;
        }
        removeNode(n22) {
          const current = n22.__ln;
          if (!this.head) {
            return;
          }
          if (!current.previous) {
            this.head = current.next;
            if (this.head) {
              this.head.previous = null;
            } else {
              this.tail = null;
            }
          } else {
            current.previous.next = current.next;
            if (current.next) {
              current.next.previous = current.previous;
            } else {
              this.tail = current.previous;
            }
          }
          if (n22.__ln) {
            delete n22.__ln;
          }
          this.length--;
        }
      };
      moveKey = (id2, parentId) => `${id2}@${parentId}`;
      MutationBuffer = class {
        constructor() {
          __publicField3(this, "frozen", false);
          __publicField3(this, "locked", false);
          __publicField3(this, "texts", []);
          __publicField3(this, "attributes", []);
          __publicField3(this, "attributeMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "removes", []);
          __publicField3(this, "mapRemoves", []);
          __publicField3(this, "movedMap", {});
          __publicField3(this, "addedSet", /* @__PURE__ */ new Set());
          __publicField3(this, "movedSet", /* @__PURE__ */ new Set());
          __publicField3(this, "droppedSet", /* @__PURE__ */ new Set());
          __publicField3(this, "removesSubTreeCache", /* @__PURE__ */ new Set());
          __publicField3(this, "mutationCb");
          __publicField3(this, "blockClass");
          __publicField3(this, "blockSelector");
          __publicField3(this, "maskTextClass");
          __publicField3(this, "maskTextSelector");
          __publicField3(this, "inlineStylesheet");
          __publicField3(this, "maskInputOptions");
          __publicField3(this, "maskTextFn");
          __publicField3(this, "maskInputFn");
          __publicField3(this, "maskAttributeFn");
          __publicField3(this, "keepIframeSrcFn");
          __publicField3(this, "recordCanvas");
          __publicField3(this, "inlineImages");
          __publicField3(this, "slimDOMOptions");
          __publicField3(this, "dataURLOptions");
          __publicField3(this, "doc");
          __publicField3(this, "mirror");
          __publicField3(this, "iframeManager");
          __publicField3(this, "stylesheetManager");
          __publicField3(this, "shadowDomManager");
          __publicField3(this, "canvasManager");
          __publicField3(this, "processedNodeManager");
          __publicField3(this, "unattachedDoc");
          __publicField3(this, "processMutations", (mutations) => {
            mutations.forEach(this.processMutation);
            this.emit();
          });
          __publicField3(this, "emit", () => {
            if (this.frozen || this.locked) {
              return;
            }
            const adds = [];
            const addedIds = /* @__PURE__ */ new Set();
            const addList = new DoubleLinkedList();
            const getNextId = (n22) => {
              let ns = n22;
              let nextId = IGNORED_NODE2;
              while (nextId === IGNORED_NODE2) {
                ns = ns && ns.nextSibling;
                nextId = ns && this.mirror.getId(ns);
              }
              return nextId;
            };
            const pushAdd = (n22) => {
              const parent = index3.parentNode(n22);
              if (!parent || !inDom2(n22)) {
                return;
              }
              let cssCaptured = false;
              if (n22.nodeType === Node.TEXT_NODE) {
                const parentTag = parent.tagName;
                if (parentTag === "TEXTAREA") {
                  return;
                } else if (parentTag === "STYLE" && this.addedSet.has(parent)) {
                  cssCaptured = true;
                }
              }
              const parentId = isShadowRoot3(parent) ? this.mirror.getId(getShadowHost2(n22)) : this.mirror.getId(parent);
              const nextId = getNextId(n22);
              if (parentId === -1 || nextId === -1) {
                return addList.addNode(n22);
              }
              const sn = serializeNodeWithId(n22, {
                doc: this.doc,
                mirror: this.mirror,
                blockClass: this.blockClass,
                blockSelector: this.blockSelector,
                maskTextClass: this.maskTextClass,
                maskTextSelector: this.maskTextSelector,
                skipChild: true,
                newlyAddedElement: true,
                inlineStylesheet: this.inlineStylesheet,
                maskInputOptions: this.maskInputOptions,
                maskTextFn: this.maskTextFn,
                maskInputFn: this.maskInputFn,
                slimDOMOptions: this.slimDOMOptions,
                dataURLOptions: this.dataURLOptions,
                recordCanvas: this.recordCanvas,
                inlineImages: this.inlineImages,
                onSerialize: (currentN) => {
                  if (isSerializedIframe2(currentN, this.mirror)) {
                    this.iframeManager.addIframe(currentN);
                  }
                  if (isSerializedStylesheet2(currentN, this.mirror)) {
                    this.stylesheetManager.trackLinkElement(
                      currentN
                    );
                  }
                  if (hasShadowRoot2(n22)) {
                    this.shadowDomManager.addShadowRoot(index3.shadowRoot(n22), this.doc);
                  }
                },
                onIframeLoad: (iframe, childSn) => {
                  this.iframeManager.attachIframe(iframe, childSn);
                  this.shadowDomManager.observeAttachShadow(iframe);
                },
                onStylesheetLoad: (link, childSn) => {
                  this.stylesheetManager.attachLinkElement(link, childSn);
                },
                cssCaptured
              });
              if (sn) {
                adds.push({
                  parentId,
                  nextId,
                  node: sn
                });
                addedIds.add(sn.id);
              }
            };
            while (this.mapRemoves.length) {
              this.mirror.removeNodeFromMap(this.mapRemoves.shift());
            }
            for (const n22 of this.movedSet) {
              if (isParentRemoved(this.removesSubTreeCache, n22, this.mirror) && !this.movedSet.has(index3.parentNode(n22))) {
                continue;
              }
              pushAdd(n22);
            }
            for (const n22 of this.addedSet) {
              if (!isAncestorInSet(this.droppedSet, n22) && !isParentRemoved(this.removesSubTreeCache, n22, this.mirror)) {
                pushAdd(n22);
              } else if (isAncestorInSet(this.movedSet, n22)) {
                pushAdd(n22);
              } else {
                this.droppedSet.add(n22);
              }
            }
            let candidate = null;
            while (addList.length) {
              let node22 = null;
              if (candidate) {
                const parentId = this.mirror.getId(index3.parentNode(candidate.value));
                const nextId = getNextId(candidate.value);
                if (parentId !== -1 && nextId !== -1) {
                  node22 = candidate;
                }
              }
              if (!node22) {
                let tailNode = addList.tail;
                while (tailNode) {
                  const _node = tailNode;
                  tailNode = tailNode.previous;
                  if (_node) {
                    const parentId = this.mirror.getId(index3.parentNode(_node.value));
                    const nextId = getNextId(_node.value);
                    if (nextId === -1) continue;
                    else if (parentId !== -1) {
                      node22 = _node;
                      break;
                    } else {
                      const unhandledNode = _node.value;
                      const parent = index3.parentNode(unhandledNode);
                      if (parent && parent.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
                        const shadowHost = index3.host(parent);
                        const parentId2 = this.mirror.getId(shadowHost);
                        if (parentId2 !== -1) {
                          node22 = _node;
                          break;
                        }
                      }
                    }
                  }
                }
              }
              if (!node22) {
                while (addList.head) {
                  addList.removeNode(addList.head.value);
                }
                break;
              }
              candidate = node22.previous;
              addList.removeNode(node22.value);
              pushAdd(node22.value);
            }
            const payload = {
              texts: this.texts.map((text) => {
                const n22 = text.node;
                const parent = index3.parentNode(n22);
                if (parent && parent.tagName === "TEXTAREA") {
                  this.genTextAreaValueMutation(parent);
                }
                return {
                  id: this.mirror.getId(n22),
                  value: text.value
                };
              }).filter((text) => !addedIds.has(text.id)).filter((text) => this.mirror.has(text.id)),
              attributes: this.attributes.map((attribute) => {
                const { attributes } = attribute;
                if (typeof attributes.style === "string") {
                  const diffAsStr = JSON.stringify(attribute.styleDiff);
                  const unchangedAsStr = JSON.stringify(attribute._unchangedStyles);
                  if (diffAsStr.length < attributes.style.length) {
                    if ((diffAsStr + unchangedAsStr).split("var(").length === attributes.style.split("var(").length) {
                      attributes.style = attribute.styleDiff;
                    }
                  }
                }
                return {
                  id: this.mirror.getId(attribute.node),
                  attributes
                };
              }).filter((attribute) => !addedIds.has(attribute.id)).filter((attribute) => this.mirror.has(attribute.id)),
              removes: this.removes,
              adds
            };
            if (!payload.texts.length && !payload.attributes.length && !payload.removes.length && !payload.adds.length) {
              return;
            }
            this.texts = [];
            this.attributes = [];
            this.attributeMap = /* @__PURE__ */ new WeakMap();
            this.removes = [];
            this.addedSet = /* @__PURE__ */ new Set();
            this.movedSet = /* @__PURE__ */ new Set();
            this.droppedSet = /* @__PURE__ */ new Set();
            this.removesSubTreeCache = /* @__PURE__ */ new Set();
            this.movedMap = {};
            this.mutationCb(payload);
          });
          __publicField3(this, "genTextAreaValueMutation", (textarea) => {
            let item = this.attributeMap.get(textarea);
            if (!item) {
              item = {
                node: textarea,
                attributes: {},
                styleDiff: {},
                _unchangedStyles: {}
              };
              this.attributes.push(item);
              this.attributeMap.set(textarea, item);
            }
            const value = Array.from(
              index3.childNodes(textarea),
              (cn) => index3.textContent(cn) || ""
            ).join("");
            item.attributes.value = maskInputValue({
              element: textarea,
              maskInputOptions: this.maskInputOptions,
              tagName: textarea.tagName,
              type: getInputType2(textarea),
              value,
              maskInputFn: this.maskInputFn
            });
          });
          __publicField3(this, "processMutation", (m2) => {
            if (isIgnored2(m2.target, this.mirror, this.slimDOMOptions)) {
              return;
            }
            switch (m2.type) {
              case "characterData": {
                const value = index3.textContent(m2.target);
                if (!isBlocked2(m2.target, this.blockClass, this.blockSelector, false) && value !== m2.oldValue) {
                  this.texts.push({
                    value: needMaskingText(
                      m2.target,
                      this.maskTextClass,
                      this.maskTextSelector,
                      true
                      // checkAncestors
                    ) && value ? this.maskTextFn ? this.maskTextFn(value, closestElementOfNode2(m2.target)) : value.replace(/[\S]/g, "*") : value,
                    node: m2.target
                  });
                }
                break;
              }
              case "attributes": {
                const target = m2.target;
                let attributeName = m2.attributeName;
                let value = m2.target.getAttribute(attributeName);
                if (attributeName === "value") {
                  const type = getInputType2(target);
                  value = maskInputValue({
                    element: target,
                    maskInputOptions: this.maskInputOptions,
                    tagName: target.tagName,
                    type,
                    value,
                    maskInputFn: this.maskInputFn
                  });
                }
                if (isBlocked2(m2.target, this.blockClass, this.blockSelector, false) || value === m2.oldValue) {
                  return;
                }
                let item = this.attributeMap.get(m2.target);
                if (target.tagName === "IFRAME" && attributeName === "src" && !this.keepIframeSrcFn(value)) {
                  if (!target.contentDocument) {
                    attributeName = "rr_src";
                  } else {
                    return;
                  }
                }
                if (!item) {
                  item = {
                    node: m2.target,
                    attributes: {},
                    styleDiff: {},
                    _unchangedStyles: {}
                  };
                  this.attributes.push(item);
                  this.attributeMap.set(m2.target, item);
                }
                if (attributeName === "type" && target.tagName === "INPUT" && (m2.oldValue || "").toLowerCase() === "password") {
                  target.setAttribute("data-rr-is-password", "true");
                }
                if (!ignoreAttribute(target.tagName, attributeName)) {
                  item.attributes[attributeName] = transformAttribute(
                    this.doc,
                    toLowerCase2(target.tagName),
                    toLowerCase2(attributeName),
                    value
                  );
                  if (this.maskAttributeFn && typeof item.attributes[attributeName] === "string") {
                    const maskedValue = this.maskAttributeFn(
                      attributeName,
                      item.attributes[attributeName],
                      target
                    );
                    if (maskedValue !== null) {
                      item.attributes[attributeName] = maskedValue;
                    } else {
                      delete item.attributes[attributeName];
                    }
                  }
                  if (attributeName === "style") {
                    if (!this.unattachedDoc) {
                      try {
                        this.unattachedDoc = document.implementation.createHTMLDocument();
                      } catch (e2) {
                        this.unattachedDoc = this.doc;
                      }
                    }
                    const old = this.unattachedDoc.createElement("span");
                    if (m2.oldValue) {
                      old.setAttribute("style", m2.oldValue);
                    }
                    for (const pname of Array.from(target.style)) {
                      const newValue = target.style.getPropertyValue(pname);
                      const newPriority = target.style.getPropertyPriority(pname);
                      if (newValue !== old.style.getPropertyValue(pname) || newPriority !== old.style.getPropertyPriority(pname)) {
                        if (newPriority === "") {
                          item.styleDiff[pname] = newValue;
                        } else {
                          item.styleDiff[pname] = [newValue, newPriority];
                        }
                      } else {
                        item._unchangedStyles[pname] = [newValue, newPriority];
                      }
                    }
                    for (const pname of Array.from(old.style)) {
                      if (target.style.getPropertyValue(pname) === "") {
                        item.styleDiff[pname] = false;
                      }
                    }
                  } else if (attributeName === "open" && target.tagName === "DIALOG") {
                    if (target.matches("dialog:modal")) {
                      item.attributes["rr_open_mode"] = "modal";
                    } else {
                      item.attributes["rr_open_mode"] = "non-modal";
                    }
                  }
                }
                break;
              }
              case "childList": {
                if (isBlocked2(m2.target, this.blockClass, this.blockSelector, true))
                  return;
                if (m2.target.tagName === "TEXTAREA") {
                  this.genTextAreaValueMutation(m2.target);
                  return;
                }
                m2.addedNodes.forEach((n22) => this.genAdds(n22, m2.target));
                m2.removedNodes.forEach((n22) => {
                  const nodeId = this.mirror.getId(n22);
                  const parentId = isShadowRoot3(m2.target) ? this.mirror.getId(index3.host(m2.target)) : this.mirror.getId(m2.target);
                  if (isBlocked2(m2.target, this.blockClass, this.blockSelector, false) || isIgnored2(n22, this.mirror, this.slimDOMOptions) || !isSerialized2(n22, this.mirror)) {
                    return;
                  }
                  if (this.addedSet.has(n22)) {
                    deepDelete(this.addedSet, n22);
                    this.droppedSet.add(n22);
                  } else if (this.addedSet.has(m2.target) && nodeId === -1) ;
                  else if (isAncestorRemoved2(m2.target, this.mirror)) ;
                  else if (this.movedSet.has(n22) && this.movedMap[moveKey(nodeId, parentId)]) {
                    deepDelete(this.movedSet, n22);
                  } else {
                    this.removes.push({
                      parentId,
                      id: nodeId,
                      isShadow: isShadowRoot3(m2.target) && isNativeShadowDom(m2.target) ? true : void 0
                    });
                    processRemoves(n22, this.removesSubTreeCache);
                  }
                  this.mapRemoves.push(n22);
                });
                break;
              }
            }
          });
          __publicField3(this, "genAdds", (n22, target) => {
            if (this.processedNodeManager.inOtherBuffer(n22, this)) return;
            if (this.addedSet.has(n22) || this.movedSet.has(n22)) return;
            if (this.mirror.hasNode(n22)) {
              if (isIgnored2(n22, this.mirror, this.slimDOMOptions)) {
                return;
              }
              this.movedSet.add(n22);
              let targetId = null;
              if (target && this.mirror.hasNode(target)) {
                targetId = this.mirror.getId(target);
              }
              if (targetId && targetId !== -1) {
                this.movedMap[moveKey(this.mirror.getId(n22), targetId)] = true;
              }
            } else {
              this.addedSet.add(n22);
              this.droppedSet.delete(n22);
            }
            if (!isBlocked2(n22, this.blockClass, this.blockSelector, false)) {
              index3.childNodes(n22).forEach((childN) => this.genAdds(childN));
              if (hasShadowRoot2(n22)) {
                index3.childNodes(index3.shadowRoot(n22)).forEach((childN) => {
                  this.processedNodeManager.add(childN, this);
                  this.genAdds(childN, n22);
                });
              }
            }
          });
        }
        init(options) {
          [
            "mutationCb",
            "blockClass",
            "blockSelector",
            "maskTextClass",
            "maskTextSelector",
            "inlineStylesheet",
            "maskInputOptions",
            "maskTextFn",
            "maskInputFn",
            "maskAttributeFn",
            "keepIframeSrcFn",
            "recordCanvas",
            "inlineImages",
            "slimDOMOptions",
            "dataURLOptions",
            "doc",
            "mirror",
            "iframeManager",
            "stylesheetManager",
            "shadowDomManager",
            "canvasManager",
            "processedNodeManager"
          ].forEach((key) => {
            this[key] = options[key];
          });
        }
        freeze() {
          this.frozen = true;
          this.canvasManager.freeze();
        }
        unfreeze() {
          this.frozen = false;
          this.canvasManager.unfreeze();
          this.emit();
        }
        isFrozen() {
          return this.frozen;
        }
        lock() {
          this.locked = true;
          this.canvasManager.lock();
        }
        unlock() {
          this.locked = false;
          this.canvasManager.unlock();
          this.emit();
        }
        reset() {
          this.shadowDomManager.reset();
          this.canvasManager.reset();
        }
      };
      callbackWrapper = (cb) => {
        if (!errorHandler) {
          return cb;
        }
        const rrwebWrapped = ((...rest) => {
          try {
            return cb(...rest);
          } catch (error) {
            if (errorHandler && errorHandler(error) === true) {
              return;
            }
            throw error;
          }
        });
        return rrwebWrapped;
      };
      mutationBuffers = [];
      INPUT_TAGS = ["INPUT", "TEXTAREA", "SELECT"];
      lastInputValueMap = /* @__PURE__ */ new WeakMap();
      CrossOriginIframeMirror = class {
        constructor(generateIdFn) {
          __publicField3(this, "iframeIdToRemoteIdMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "iframeRemoteIdToIdMap", /* @__PURE__ */ new WeakMap());
          this.generateIdFn = generateIdFn;
        }
        getId(iframe, remoteId, idToRemoteMap, remoteToIdMap) {
          const idToRemoteIdMap = idToRemoteMap || this.getIdToRemoteIdMap(iframe);
          const remoteIdToIdMap = remoteToIdMap || this.getRemoteIdToIdMap(iframe);
          let id2 = idToRemoteIdMap.get(remoteId);
          if (!id2) {
            id2 = this.generateIdFn();
            idToRemoteIdMap.set(remoteId, id2);
            remoteIdToIdMap.set(id2, remoteId);
          }
          return id2;
        }
        getIds(iframe, remoteId) {
          const idToRemoteIdMap = this.getIdToRemoteIdMap(iframe);
          const remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
          return remoteId.map(
            (id2) => this.getId(iframe, id2, idToRemoteIdMap, remoteIdToIdMap)
          );
        }
        getRemoteId(iframe, id2, map) {
          const remoteIdToIdMap = map || this.getRemoteIdToIdMap(iframe);
          if (typeof id2 !== "number") return id2;
          const remoteId = remoteIdToIdMap.get(id2);
          if (!remoteId) return -1;
          return remoteId;
        }
        getRemoteIds(iframe, ids) {
          const remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
          return ids.map((id2) => this.getRemoteId(iframe, id2, remoteIdToIdMap));
        }
        reset(iframe) {
          if (!iframe) {
            this.iframeIdToRemoteIdMap = /* @__PURE__ */ new WeakMap();
            this.iframeRemoteIdToIdMap = /* @__PURE__ */ new WeakMap();
            return;
          }
          this.iframeIdToRemoteIdMap.delete(iframe);
          this.iframeRemoteIdToIdMap.delete(iframe);
        }
        getIdToRemoteIdMap(iframe) {
          let idToRemoteIdMap = this.iframeIdToRemoteIdMap.get(iframe);
          if (!idToRemoteIdMap) {
            idToRemoteIdMap = /* @__PURE__ */ new Map();
            this.iframeIdToRemoteIdMap.set(iframe, idToRemoteIdMap);
          }
          return idToRemoteIdMap;
        }
        getRemoteIdToIdMap(iframe) {
          let remoteIdToIdMap = this.iframeRemoteIdToIdMap.get(iframe);
          if (!remoteIdToIdMap) {
            remoteIdToIdMap = /* @__PURE__ */ new Map();
            this.iframeRemoteIdToIdMap.set(iframe, remoteIdToIdMap);
          }
          return remoteIdToIdMap;
        }
      };
      IframeManager = class {
        constructor(options) {
          __publicField3(this, "iframes", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "crossOriginIframeMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "crossOriginIframeMirror", new CrossOriginIframeMirror(genId));
          __publicField3(this, "crossOriginIframeStyleMirror");
          __publicField3(this, "crossOriginIframeRootIdMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "mirror");
          __publicField3(this, "mutationCb");
          __publicField3(this, "wrappedEmit");
          __publicField3(this, "loadListener");
          __publicField3(this, "stylesheetManager");
          __publicField3(this, "recordCrossOriginIframes");
          this.mutationCb = options.mutationCb;
          this.wrappedEmit = options.wrappedEmit;
          this.stylesheetManager = options.stylesheetManager;
          this.recordCrossOriginIframes = options.recordCrossOriginIframes;
          this.crossOriginIframeStyleMirror = new CrossOriginIframeMirror(
            this.stylesheetManager.styleMirror.generateId.bind(
              this.stylesheetManager.styleMirror
            )
          );
          this.mirror = options.mirror;
          if (this.recordCrossOriginIframes) {
            window.addEventListener("message", this.handleMessage.bind(this));
          }
        }
        addIframe(iframeEl) {
          this.iframes.set(iframeEl, true);
          if (iframeEl.contentWindow)
            this.crossOriginIframeMap.set(iframeEl.contentWindow, iframeEl);
        }
        addLoadListener(cb) {
          this.loadListener = cb;
        }
        attachIframe(iframeEl, childSn) {
          var _a22, _b;
          this.mutationCb({
            adds: [
              {
                parentId: this.mirror.getId(iframeEl),
                nextId: null,
                node: childSn
              }
            ],
            removes: [],
            texts: [],
            attributes: [],
            isAttachIframe: true
          });
          if (this.recordCrossOriginIframes)
            (_a22 = iframeEl.contentWindow) == null ? void 0 : _a22.addEventListener(
              "message",
              this.handleMessage.bind(this)
            );
          (_b = this.loadListener) == null ? void 0 : _b.call(this, iframeEl);
          if (iframeEl.contentDocument && iframeEl.contentDocument.adoptedStyleSheets && iframeEl.contentDocument.adoptedStyleSheets.length > 0)
            this.stylesheetManager.adoptStyleSheets(
              iframeEl.contentDocument.adoptedStyleSheets,
              this.mirror.getId(iframeEl.contentDocument)
            );
        }
        handleMessage(message) {
          const crossOriginMessageEvent = message;
          if (crossOriginMessageEvent.data.type !== "rrweb" || // To filter out the rrweb messages which are forwarded by some sites.
          crossOriginMessageEvent.origin !== crossOriginMessageEvent.data.origin)
            return;
          const iframeSourceWindow = message.source;
          if (!iframeSourceWindow) return;
          const iframeEl = this.crossOriginIframeMap.get(message.source);
          if (!iframeEl) return;
          const transformedEvent = this.transformCrossOriginEvent(
            iframeEl,
            crossOriginMessageEvent.data.event
          );
          if (transformedEvent)
            this.wrappedEmit(
              transformedEvent,
              crossOriginMessageEvent.data.isCheckout
            );
        }
        transformCrossOriginEvent(iframeEl, e2) {
          var _a22;
          switch (e2.type) {
            case EventType2.FullSnapshot: {
              this.crossOriginIframeMirror.reset(iframeEl);
              this.crossOriginIframeStyleMirror.reset(iframeEl);
              this.replaceIdOnNode(e2.data.node, iframeEl);
              const rootId = e2.data.node.id;
              this.crossOriginIframeRootIdMap.set(iframeEl, rootId);
              this.patchRootIdOnNode(e2.data.node, rootId);
              return {
                timestamp: e2.timestamp,
                type: EventType2.IncrementalSnapshot,
                data: {
                  source: IncrementalSource2.Mutation,
                  adds: [
                    {
                      parentId: this.mirror.getId(iframeEl),
                      nextId: null,
                      node: e2.data.node
                    }
                  ],
                  removes: [],
                  texts: [],
                  attributes: [],
                  isAttachIframe: true
                }
              };
            }
            case EventType2.Meta:
            case EventType2.Load:
            case EventType2.DomContentLoaded: {
              return false;
            }
            case EventType2.Plugin: {
              return e2;
            }
            case EventType2.Custom: {
              this.replaceIds(
                e2.data.payload,
                iframeEl,
                ["id", "parentId", "previousId", "nextId"]
              );
              return e2;
            }
            case EventType2.IncrementalSnapshot: {
              switch (e2.data.source) {
                case IncrementalSource2.Mutation: {
                  e2.data.adds.forEach((n22) => {
                    this.replaceIds(n22, iframeEl, [
                      "parentId",
                      "nextId",
                      "previousId"
                    ]);
                    this.replaceIdOnNode(n22.node, iframeEl);
                    const rootId = this.crossOriginIframeRootIdMap.get(iframeEl);
                    rootId && this.patchRootIdOnNode(n22.node, rootId);
                  });
                  e2.data.removes.forEach((n22) => {
                    this.replaceIds(n22, iframeEl, ["parentId", "id"]);
                  });
                  e2.data.attributes.forEach((n22) => {
                    this.replaceIds(n22, iframeEl, ["id"]);
                  });
                  e2.data.texts.forEach((n22) => {
                    this.replaceIds(n22, iframeEl, ["id"]);
                  });
                  return e2;
                }
                case IncrementalSource2.Drag:
                case IncrementalSource2.TouchMove:
                case IncrementalSource2.MouseMove: {
                  e2.data.positions.forEach((p2) => {
                    this.replaceIds(p2, iframeEl, ["id"]);
                  });
                  return e2;
                }
                case IncrementalSource2.ViewportResize: {
                  return false;
                }
                case IncrementalSource2.MediaInteraction:
                case IncrementalSource2.MouseInteraction:
                case IncrementalSource2.Scroll:
                case IncrementalSource2.CanvasMutation:
                case IncrementalSource2.Input: {
                  this.replaceIds(e2.data, iframeEl, ["id"]);
                  return e2;
                }
                case IncrementalSource2.StyleSheetRule:
                case IncrementalSource2.StyleDeclaration: {
                  this.replaceIds(e2.data, iframeEl, ["id"]);
                  this.replaceStyleIds(e2.data, iframeEl, ["styleId"]);
                  return e2;
                }
                case IncrementalSource2.Font: {
                  return e2;
                }
                case IncrementalSource2.Selection: {
                  e2.data.ranges.forEach((range) => {
                    this.replaceIds(range, iframeEl, ["start", "end"]);
                  });
                  return e2;
                }
                case IncrementalSource2.AdoptedStyleSheet: {
                  this.replaceIds(e2.data, iframeEl, ["id"]);
                  this.replaceStyleIds(e2.data, iframeEl, ["styleIds"]);
                  (_a22 = e2.data.styles) == null ? void 0 : _a22.forEach((style) => {
                    this.replaceStyleIds(style, iframeEl, ["styleId"]);
                  });
                  return e2;
                }
              }
            }
          }
          return false;
        }
        replace(iframeMirror, obj, iframeEl, keys) {
          for (const key of keys) {
            if (!Array.isArray(obj[key]) && typeof obj[key] !== "number") continue;
            if (Array.isArray(obj[key])) {
              obj[key] = iframeMirror.getIds(
                iframeEl,
                obj[key]
              );
            } else {
              obj[key] = iframeMirror.getId(iframeEl, obj[key]);
            }
          }
          return obj;
        }
        replaceIds(obj, iframeEl, keys) {
          return this.replace(this.crossOriginIframeMirror, obj, iframeEl, keys);
        }
        replaceStyleIds(obj, iframeEl, keys) {
          return this.replace(this.crossOriginIframeStyleMirror, obj, iframeEl, keys);
        }
        replaceIdOnNode(node22, iframeEl) {
          this.replaceIds(node22, iframeEl, ["id", "rootId"]);
          if ("childNodes" in node22) {
            node22.childNodes.forEach((child) => {
              this.replaceIdOnNode(child, iframeEl);
            });
          }
        }
        patchRootIdOnNode(node22, rootId) {
          if (node22.type !== NodeType.Document && !node22.rootId) node22.rootId = rootId;
          if ("childNodes" in node22) {
            node22.childNodes.forEach((child) => {
              this.patchRootIdOnNode(child, rootId);
            });
          }
        }
      };
      ShadowDomManager = class {
        constructor(options) {
          __publicField3(this, "shadowDoms", /* @__PURE__ */ new WeakSet());
          __publicField3(this, "mutationCb");
          __publicField3(this, "scrollCb");
          __publicField3(this, "bypassOptions");
          __publicField3(this, "mirror");
          __publicField3(this, "restoreHandlers", []);
          this.mutationCb = options.mutationCb;
          this.scrollCb = options.scrollCb;
          this.bypassOptions = options.bypassOptions;
          this.mirror = options.mirror;
          this.init();
        }
        init() {
          this.reset();
          this.patchAttachShadow(Element, document);
        }
        addShadowRoot(shadowRoot22, doc) {
          if (!isNativeShadowDom(shadowRoot22)) return;
          if (this.shadowDoms.has(shadowRoot22)) return;
          this.shadowDoms.add(shadowRoot22);
          const observer = initMutationObserver(
            {
              ...this.bypassOptions,
              doc,
              mutationCb: this.mutationCb,
              mirror: this.mirror,
              shadowDomManager: this
            },
            shadowRoot22
          );
          this.restoreHandlers.push(() => observer.disconnect());
          this.restoreHandlers.push(
            initScrollObserver({
              ...this.bypassOptions,
              scrollCb: this.scrollCb,
              // https://gist.github.com/praveenpuglia/0832da687ed5a5d7a0907046c9ef1813
              // scroll is not allowed to pass the boundary, so we need to listen the shadow document
              doc: shadowRoot22,
              mirror: this.mirror
            })
          );
          setTimeout(() => {
            if (shadowRoot22.adoptedStyleSheets && shadowRoot22.adoptedStyleSheets.length > 0)
              this.bypassOptions.stylesheetManager.adoptStyleSheets(
                shadowRoot22.adoptedStyleSheets,
                this.mirror.getId(index3.host(shadowRoot22))
              );
            this.restoreHandlers.push(
              initAdoptedStyleSheetObserver(
                {
                  mirror: this.mirror,
                  stylesheetManager: this.bypassOptions.stylesheetManager
                },
                shadowRoot22
              )
            );
          }, 0);
        }
        /**
         * Monkey patch 'attachShadow' of an IFrameElement to observe newly added shadow doms.
         */
        observeAttachShadow(iframeElement) {
          if (!iframeElement.contentWindow || !iframeElement.contentDocument) return;
          this.patchAttachShadow(
            iframeElement.contentWindow.Element,
            iframeElement.contentDocument
          );
        }
        /**
         * Patch 'attachShadow' to observe newly added shadow doms.
         */
        patchAttachShadow(element, doc) {
          const manager = this;
          this.restoreHandlers.push(
            patch2(
              element.prototype,
              "attachShadow",
              function(original) {
                return function(option) {
                  const sRoot = original.call(this, option);
                  const shadowRootEl = index3.shadowRoot(this);
                  if (shadowRootEl && inDom2(this))
                    manager.addShadowRoot(shadowRootEl, doc);
                  return sRoot;
                };
              }
            )
          );
        }
        reset() {
          this.restoreHandlers.forEach((handler) => {
            try {
              handler();
            } catch (e2) {
            }
          });
          this.restoreHandlers = [];
          this.shadowDoms = /* @__PURE__ */ new WeakSet();
        }
      };
      chars2 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
      lookup2 = typeof Uint8Array === "undefined" ? [] : new Uint8Array(256);
      for (i$1 = 0; i$1 < chars2.length; i$1++) {
        lookup2[chars2.charCodeAt(i$1)] = i$1;
      }
      encode2 = function(arraybuffer) {
        var bytes = new Uint8Array(arraybuffer), i2, len = bytes.length, base64 = "";
        for (i2 = 0; i2 < len; i2 += 3) {
          base64 += chars2[bytes[i2] >> 2];
          base64 += chars2[(bytes[i2] & 3) << 4 | bytes[i2 + 1] >> 4];
          base64 += chars2[(bytes[i2 + 1] & 15) << 2 | bytes[i2 + 2] >> 6];
          base64 += chars2[bytes[i2 + 2] & 63];
        }
        if (len % 3 === 2) {
          base64 = base64.substring(0, base64.length - 1) + "=";
        } else if (len % 3 === 1) {
          base64 = base64.substring(0, base64.length - 2) + "==";
        }
        return base64;
      };
      canvasVarMap = /* @__PURE__ */ new Map();
      saveWebGLVar = (value, win, ctx) => {
        if (!value || !(isInstanceOfWebGLObject(value, win) || typeof value === "object"))
          return;
        const name = value.constructor.name;
        const list = variableListFor$1(ctx, name);
        let index22 = list.indexOf(value);
        if (index22 === -1) {
          index22 = list.length;
          list.push(value);
        }
        return index22;
      };
      serializeArgs = (args, win, ctx) => {
        return args.map((arg) => serializeArg(arg, win, ctx));
      };
      isInstanceOfWebGLObject = (value, win) => {
        const webGLConstructorNames = [
          "WebGLActiveInfo",
          "WebGLBuffer",
          "WebGLFramebuffer",
          "WebGLProgram",
          "WebGLRenderbuffer",
          "WebGLShader",
          "WebGLShaderPrecisionFormat",
          "WebGLTexture",
          "WebGLUniformLocation",
          "WebGLVertexArrayObject",
          // In old Chrome versions, value won't be an instanceof WebGLVertexArrayObject.
          "WebGLVertexArrayObjectOES"
        ];
        const supportedWebGLConstructorNames = webGLConstructorNames.filter(
          (name) => typeof win[name] === "function"
        );
        return Boolean(
          supportedWebGLConstructorNames.find(
            (name) => value instanceof win[name]
          )
        );
      };
      jsContent2 = '(function() {\n  "use strict";\n  var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";\n  var lookup = typeof Uint8Array === "undefined" ? [] : new Uint8Array(256);\n  for (var i = 0; i < chars.length; i++) {\n    lookup[chars.charCodeAt(i)] = i;\n  }\n  var encode = function(arraybuffer) {\n    var bytes = new Uint8Array(arraybuffer), i2, len = bytes.length, base64 = "";\n    for (i2 = 0; i2 < len; i2 += 3) {\n      base64 += chars[bytes[i2] >> 2];\n      base64 += chars[(bytes[i2] & 3) << 4 | bytes[i2 + 1] >> 4];\n      base64 += chars[(bytes[i2 + 1] & 15) << 2 | bytes[i2 + 2] >> 6];\n      base64 += chars[bytes[i2 + 2] & 63];\n    }\n    if (len % 3 === 2) {\n      base64 = base64.substring(0, base64.length - 1) + "=";\n    } else if (len % 3 === 1) {\n      base64 = base64.substring(0, base64.length - 2) + "==";\n    }\n    return base64;\n  };\n  const lastBlobMap = /* @__PURE__ */ new Map();\n  const transparentBlobMap = /* @__PURE__ */ new Map();\n  async function getTransparentBlobFor(width, height, dataURLOptions) {\n    const id = `${width}-${height}`;\n    if ("OffscreenCanvas" in globalThis) {\n      if (transparentBlobMap.has(id)) return transparentBlobMap.get(id);\n      const offscreen = new OffscreenCanvas(width, height);\n      offscreen.getContext("2d");\n      const blob = await offscreen.convertToBlob(dataURLOptions);\n      const arrayBuffer = await blob.arrayBuffer();\n      const base64 = encode(arrayBuffer);\n      transparentBlobMap.set(id, base64);\n      return base64;\n    } else {\n      return "";\n    }\n  }\n  const worker = self;\n  worker.onmessage = async function(e) {\n    if ("OffscreenCanvas" in globalThis) {\n      const { id, bitmap, width, height, dataURLOptions } = e.data;\n      const transparentBase64 = getTransparentBlobFor(\n        width,\n        height,\n        dataURLOptions\n      );\n      const offscreen = new OffscreenCanvas(width, height);\n      const ctx = offscreen.getContext("2d");\n      ctx.drawImage(bitmap, 0, 0);\n      bitmap.close();\n      const blob = await offscreen.convertToBlob(dataURLOptions);\n      const type = blob.type;\n      const arrayBuffer = await blob.arrayBuffer();\n      const base64 = encode(arrayBuffer);\n      if (!lastBlobMap.has(id) && await transparentBase64 === base64) {\n        lastBlobMap.set(id, base64);\n        return worker.postMessage({ id });\n      }\n      if (lastBlobMap.get(id) === base64) return worker.postMessage({ id });\n      worker.postMessage({\n        id,\n        type,\n        base64,\n        width,\n        height\n      });\n      lastBlobMap.set(id, base64);\n    } else {\n      return worker.postMessage({ id: e.data.id });\n    }\n  };\n})();\n//# sourceMappingURL=image-bitmap-data-url-worker-IJpC7g_b.js.map\n';
      blob = typeof self !== "undefined" && self.Blob && new Blob([jsContent2], { type: "text/javascript;charset=utf-8" });
      CanvasManager = class {
        constructor(options) {
          __publicField3(this, "pendingCanvasMutations", /* @__PURE__ */ new Map());
          __publicField3(this, "rafStamps", { latestId: 0, invokeId: null });
          __publicField3(this, "mirror");
          __publicField3(this, "mutationCb");
          __publicField3(this, "resetObservers");
          __publicField3(this, "frozen", false);
          __publicField3(this, "locked", false);
          __publicField3(this, "processMutation", (target, mutation) => {
            const newFrame = this.rafStamps.invokeId && this.rafStamps.latestId !== this.rafStamps.invokeId;
            if (newFrame || !this.rafStamps.invokeId)
              this.rafStamps.invokeId = this.rafStamps.latestId;
            if (!this.pendingCanvasMutations.has(target)) {
              this.pendingCanvasMutations.set(target, []);
            }
            this.pendingCanvasMutations.get(target).push(mutation);
          });
          const {
            sampling = "all",
            win,
            blockClass,
            blockSelector,
            recordCanvas,
            dataURLOptions
          } = options;
          this.mutationCb = options.mutationCb;
          this.mirror = options.mirror;
          if (recordCanvas && sampling === "all")
            this.initCanvasMutationObserver(win, blockClass, blockSelector);
          if (recordCanvas && typeof sampling === "number")
            this.initCanvasFPSObserver(sampling, win, blockClass, blockSelector, {
              dataURLOptions
            });
        }
        reset() {
          this.pendingCanvasMutations.clear();
          this.resetObservers && this.resetObservers();
        }
        freeze() {
          this.frozen = true;
        }
        unfreeze() {
          this.frozen = false;
        }
        lock() {
          this.locked = true;
        }
        unlock() {
          this.locked = false;
        }
        initCanvasFPSObserver(fps, win, blockClass, blockSelector, options) {
          const canvasContextReset = initCanvasContextObserver(
            win,
            blockClass,
            blockSelector,
            true
          );
          const snapshotInProgressMap = /* @__PURE__ */ new Map();
          const worker = new WorkerWrapper();
          worker.onmessage = (e2) => {
            const { id: id2 } = e2.data;
            snapshotInProgressMap.set(id2, false);
            if (!("base64" in e2.data)) return;
            const { base64, type, width, height } = e2.data;
            this.mutationCb({
              id: id2,
              type: CanvasContext["2D"],
              commands: [
                {
                  property: "clearRect",
                  // wipe canvas
                  args: [0, 0, width, height]
                },
                {
                  property: "drawImage",
                  // draws (semi-transparent) image
                  args: [
                    {
                      rr_type: "ImageBitmap",
                      args: [
                        {
                          rr_type: "Blob",
                          data: [{ rr_type: "ArrayBuffer", base64 }],
                          type
                        }
                      ]
                    },
                    0,
                    0
                  ]
                }
              ]
            });
          };
          const timeBetweenSnapshots = 1e3 / fps;
          let lastSnapshotTime = 0;
          let rafId;
          const getCanvas = () => {
            const matchedCanvas = [];
            win.document.querySelectorAll("canvas").forEach((canvas) => {
              if (!isBlocked2(canvas, blockClass, blockSelector, true)) {
                matchedCanvas.push(canvas);
              }
            });
            return matchedCanvas;
          };
          const takeCanvasSnapshots = (timestamp) => {
            if (lastSnapshotTime && timestamp - lastSnapshotTime < timeBetweenSnapshots) {
              rafId = requestAnimationFrame(takeCanvasSnapshots);
              return;
            }
            lastSnapshotTime = timestamp;
            getCanvas().forEach(async (canvas) => {
              var _a22;
              const id2 = this.mirror.getId(canvas);
              if (snapshotInProgressMap.get(id2)) return;
              if (canvas.width === 0 || canvas.height === 0) return;
              snapshotInProgressMap.set(id2, true);
              if (["webgl", "webgl2"].includes(canvas.__context)) {
                const context = canvas.getContext(canvas.__context);
                if (((_a22 = context == null ? void 0 : context.getContextAttributes()) == null ? void 0 : _a22.preserveDrawingBuffer) === false) {
                  context.clear(context.COLOR_BUFFER_BIT);
                }
              }
              const bitmap = await createImageBitmap(canvas);
              worker.postMessage(
                {
                  id: id2,
                  bitmap,
                  width: canvas.width,
                  height: canvas.height,
                  dataURLOptions: options.dataURLOptions
                },
                [bitmap]
              );
            });
            rafId = requestAnimationFrame(takeCanvasSnapshots);
          };
          rafId = requestAnimationFrame(takeCanvasSnapshots);
          this.resetObservers = () => {
            canvasContextReset();
            cancelAnimationFrame(rafId);
          };
        }
        initCanvasMutationObserver(win, blockClass, blockSelector) {
          this.startRAFTimestamping();
          this.startPendingCanvasMutationFlusher();
          const canvasContextReset = initCanvasContextObserver(
            win,
            blockClass,
            blockSelector,
            false
          );
          const canvas2DReset = initCanvas2DMutationObserver(
            this.processMutation.bind(this),
            win,
            blockClass,
            blockSelector
          );
          const canvasWebGL1and2Reset = initCanvasWebGLMutationObserver(
            this.processMutation.bind(this),
            win,
            blockClass,
            blockSelector
          );
          this.resetObservers = () => {
            canvasContextReset();
            canvas2DReset();
            canvasWebGL1and2Reset();
          };
        }
        startPendingCanvasMutationFlusher() {
          requestAnimationFrame(() => this.flushPendingCanvasMutations());
        }
        startRAFTimestamping() {
          const setLatestRAFTimestamp = (timestamp) => {
            this.rafStamps.latestId = timestamp;
            requestAnimationFrame(setLatestRAFTimestamp);
          };
          requestAnimationFrame(setLatestRAFTimestamp);
        }
        flushPendingCanvasMutations() {
          this.pendingCanvasMutations.forEach(
            (_values, canvas) => {
              const id2 = this.mirror.getId(canvas);
              this.flushPendingCanvasMutationFor(canvas, id2);
            }
          );
          requestAnimationFrame(() => this.flushPendingCanvasMutations());
        }
        flushPendingCanvasMutationFor(canvas, id2) {
          if (this.frozen || this.locked) {
            return;
          }
          const valuesWithType = this.pendingCanvasMutations.get(canvas);
          if (!valuesWithType || id2 === -1) return;
          const values = valuesWithType.map((value) => {
            const { type: type2, ...rest } = value;
            return rest;
          });
          const { type } = valuesWithType[0];
          this.mutationCb({ id: id2, type, commands: values });
          this.pendingCanvasMutations.delete(canvas);
        }
      };
      StylesheetManager = class {
        constructor(options) {
          __publicField3(this, "trackedLinkElements", /* @__PURE__ */ new WeakSet());
          __publicField3(this, "mutationCb");
          __publicField3(this, "adoptedStyleSheetCb");
          __publicField3(this, "styleMirror", new StyleSheetMirror2());
          this.mutationCb = options.mutationCb;
          this.adoptedStyleSheetCb = options.adoptedStyleSheetCb;
        }
        attachLinkElement(linkEl, childSn) {
          if ("_cssText" in childSn.attributes)
            this.mutationCb({
              adds: [],
              removes: [],
              texts: [],
              attributes: [
                {
                  id: childSn.id,
                  attributes: childSn.attributes
                }
              ]
            });
          this.trackLinkElement(linkEl);
        }
        trackLinkElement(linkEl) {
          if (this.trackedLinkElements.has(linkEl)) return;
          this.trackedLinkElements.add(linkEl);
          this.trackStylesheetInLinkElement(linkEl);
        }
        adoptStyleSheets(sheets, hostId) {
          if (sheets.length === 0) return;
          const adoptedStyleSheetData = {
            id: hostId,
            styleIds: []
          };
          const styles = [];
          for (const sheet of sheets) {
            let styleId;
            if (!this.styleMirror.has(sheet)) {
              styleId = this.styleMirror.add(sheet);
              let rules2 = [];
              try {
                rules2 = Array.from(sheet.cssRules, (r2, index22) => ({
                  rule: stringifyRule(r2, sheet.href),
                  index: index22
                }));
              } catch (e2) {
                if (e2 instanceof DOMException && e2.name === "SecurityError") ;
                else {
                  throw e2;
                }
              }
              styles.push({ styleId, rules: rules2 });
            } else styleId = this.styleMirror.getId(sheet);
            adoptedStyleSheetData.styleIds.push(styleId);
          }
          if (styles.length > 0) adoptedStyleSheetData.styles = styles;
          this.adoptedStyleSheetCb(adoptedStyleSheetData);
        }
        reset() {
          this.styleMirror.reset();
          this.trackedLinkElements = /* @__PURE__ */ new WeakSet();
        }
        // TODO: take snapshot on stylesheet reload by applying event listener
        trackStylesheetInLinkElement(_linkEl) {
        }
      };
      ProcessedNodeManager = class {
        constructor() {
          __publicField3(this, "nodeMap", /* @__PURE__ */ new WeakMap());
          __publicField3(this, "active", false);
        }
        inOtherBuffer(node22, thisBuffer) {
          const buffers = this.nodeMap.get(node22);
          return buffers && Array.from(buffers).some((buffer) => buffer !== thisBuffer);
        }
        add(node22, buffer) {
          if (!this.active) {
            this.active = true;
            requestAnimationFrame(() => {
              this.nodeMap = /* @__PURE__ */ new WeakMap();
              this.active = false;
            });
          }
          this.nodeMap.set(node22, (this.nodeMap.get(node22) || /* @__PURE__ */ new Set()).add(buffer));
        }
        destroy() {
        }
      };
      recording = false;
      try {
        if (Array.from([1], (x2) => x2 * 2)[0] !== 2) {
          const cleanFrame = document.createElement("iframe");
          document.body.appendChild(cleanFrame);
          Array.from = ((_a3 = cleanFrame.contentWindow) == null ? void 0 : _a3.Array.from) || Array.from;
          document.body.removeChild(cleanFrame);
        }
      } catch (err) {
        console.debug("Unable to override Array.from", err);
      }
      mirror = createMirror$22();
      record.addCustomEvent = (tag, payload) => {
        if (!recording) {
          throw new Error("please add custom event after start recording");
        }
        wrappedEmit({
          type: EventType2.Custom,
          data: {
            tag,
            payload
          }
        });
      };
      record.freezePage = () => {
        mutationBuffers.forEach((buf) => buf.freeze());
      };
      record.takeFullSnapshot = (isCheckout) => {
        if (!recording) {
          throw new Error("please take full snapshot after start recording");
        }
        takeFullSnapshot$1(isCheckout);
      };
      record.mirror = mirror;
      !(function(t2) {
        t2[t2.NotStarted = 0] = "NotStarted", t2[t2.Running = 1] = "Running", t2[t2.Stopped = 2] = "Stopped";
      })(n3 || (n3 = {}));
      ({ addCustomEvent } = record);
      ({ freezePage } = record);
      ({ takeFullSnapshot } = record);
    }
  });

  // node_modules/@amplitude/session-replay-browser/lib/esm/network-body-capture.js
  var isBodyCaptureEnabled, getBodyMaskingLists, applyBodyMasking, captureSerializedBody;
  var init_network_body_capture = __esm({
    "node_modules/@amplitude/session-replay-browser/lib/esm/network-body-capture.js"() {
      init_esm();
      isBodyCaptureEnabled = function(config2) {
        if (config2 === void 0) {
          return false;
        }
        if (typeof config2 === "boolean") {
          return config2;
        }
        return config2.enabled !== false;
      };
      getBodyMaskingLists = function(config2) {
        var _a4;
        if (typeof config2 === "object" && config2 !== null) {
          return {
            allowlist: (_a4 = config2.allowlist) !== null && _a4 !== void 0 ? _a4 : [],
            excludelist: config2.excludelist || config2.blocklist || []
          };
        }
        return { allowlist: [], excludelist: [] };
      };
      applyBodyMasking = function(serialized, allowlist, excludelist) {
        if (allowlist.length === 0 && excludelist.length === 0) {
          return serialized;
        }
        var effectiveAllowlist = allowlist.length > 0 ? allowlist : ["/**"];
        try {
          var json = JSON.parse(serialized);
          pruneJson(json, effectiveAllowlist, excludelist);
          return JSON.stringify(json);
        } catch (_a4) {
          return serialized;
        }
      };
      captureSerializedBody = function(serialized, config2, maxBytes, truncate) {
        var _a4 = getBodyMaskingLists(config2), allowlist = _a4.allowlist, excludelist = _a4.excludelist;
        var masked = applyBodyMasking(serialized, allowlist, excludelist);
        return truncate(masked, maxBytes);
      };
    }
  });

  // node_modules/@amplitude/session-replay-browser/lib/esm/observers.js
  var observers_exports = {};
  __export(observers_exports, {
    NetworkObservers: () => NetworkObservers
  });
  function isBinaryContentType(contentType) {
    if (!contentType)
      return false;
    return BINARY_CONTENT_TYPE_PREFIXES.some(function(prefix) {
      return contentType.toLowerCase().startsWith(prefix);
    });
  }
  function serializeRequestBody(body) {
    if (body === null || body === void 0)
      return void 0;
    if (typeof body === "string")
      return body;
    if (body instanceof URLSearchParams)
      return body.toString();
    if (body instanceof FormData) {
      var parts_1 = [];
      body.forEach(function(value, key) {
        parts_1.push("".concat(key, "=").concat(typeof value === "string" ? value : "[File]"));
      });
      return parts_1.join("&");
    }
    return void 0;
  }
  function truncateToByteLimit(str, maxBytes) {
    if (new Blob([str]).size <= maxBytes) {
      return { value: str, truncated: false };
    }
    var lo = 0;
    var hi = Math.min(str.length, maxBytes);
    while (lo < hi) {
      var mid = Math.ceil((lo + hi) / 2);
      if (new Blob([str.slice(0, mid)]).size <= maxBytes) {
        lo = mid;
      } else {
        hi = mid - 1;
      }
    }
    if (lo > 0 && str.charCodeAt(lo - 1) >= 55296 && str.charCodeAt(lo - 1) <= 56319) {
      lo -= 1;
    }
    return { value: str.slice(0, lo), truncated: true };
  }
  var DEFAULT_MAX_BODY_SIZE_BYTES, BINARY_CONTENT_TYPE_PREFIXES, NetworkObservers;
  var init_observers = __esm({
    "node_modules/@amplitude/session-replay-browser/lib/esm/observers.js"() {
      init_tslib_es6();
      init_esm();
      init_network_body_capture();
      DEFAULT_MAX_BODY_SIZE_BYTES = 10240;
      BINARY_CONTENT_TYPE_PREFIXES = ["image/", "audio/", "video/", "application/octet-stream", "font/"];
      NetworkObservers = /** @class */
      (function() {
        function NetworkObservers2() {
          this.fetchObserver = null;
        }
        NetworkObservers2.prototype.start = function(eventCallback, networkConfig) {
          this.eventCallback = eventCallback;
          this.networkConfig = networkConfig;
          this.observeFetch();
        };
        NetworkObservers2.prototype.stop = function() {
          var _a4;
          (_a4 = this.fetchObserver) === null || _a4 === void 0 ? void 0 : _a4.call(this);
          this.fetchObserver = null;
          this.eventCallback = void 0;
          this.networkConfig = void 0;
        };
        NetworkObservers2.prototype.notifyEvent = function(event) {
          var _a4;
          (_a4 = this.eventCallback) === null || _a4 === void 0 ? void 0 : _a4.call(this, event);
        };
        NetworkObservers2.prototype.observeFetch = function() {
          var _this = this;
          var globalScope3 = getGlobalScope();
          if (!globalScope3)
            return;
          var originalFetch = globalScope3.fetch;
          if (!originalFetch)
            return;
          globalScope3.fetch = function(input3, init3) {
            return __awaiter(_this, void 0, void 0, function() {
              var startTime, requestEvent, bodyConfig, serialized, maxBytes, response, endTime, headers_1, contentType, cloned, error_1, endTime, typedError;
              var _this2 = this;
              var _a4, _b;
              return __generator(this, function(_c) {
                switch (_c.label) {
                  case 0:
                    startTime = Date.now();
                    requestEvent = {
                      timestamp: startTime,
                      type: "fetch",
                      method: (init3 === null || init3 === void 0 ? void 0 : init3.method) || "GET",
                      url: input3.toString(),
                      requestHeaders: init3 === null || init3 === void 0 ? void 0 : init3.headers
                    };
                    bodyConfig = (_a4 = this.networkConfig) === null || _a4 === void 0 ? void 0 : _a4.body;
                    if (bodyConfig && isBodyCaptureEnabled(bodyConfig.request)) {
                      serialized = serializeRequestBody(init3 === null || init3 === void 0 ? void 0 : init3.body);
                      if (serialized !== void 0) {
                        maxBytes = (_b = bodyConfig.maxBodySizeBytes) !== null && _b !== void 0 ? _b : DEFAULT_MAX_BODY_SIZE_BYTES;
                        requestEvent.requestBody = captureSerializedBody(serialized, bodyConfig.request, maxBytes, truncateToByteLimit).value;
                      }
                    }
                    _c.label = 1;
                  case 1:
                    _c.trys.push([1, 3, , 4]);
                    return [4, originalFetch(input3, init3)];
                  case 2:
                    response = _c.sent();
                    endTime = Date.now();
                    requestEvent.status = response.status;
                    requestEvent.duration = endTime - startTime;
                    headers_1 = {};
                    response.headers.forEach(function(value, key) {
                      headers_1[key] = value;
                    });
                    requestEvent.responseHeaders = headers_1;
                    if (bodyConfig && isBodyCaptureEnabled(bodyConfig.response)) {
                      contentType = headers_1["content-type"] || null;
                      if (isBinaryContentType(contentType)) {
                        requestEvent.responseBodyStatus = "skipped_binary";
                        this.notifyEvent(requestEvent);
                      } else {
                        cloned = response.clone();
                        cloned.text().then(function(text) {
                          var _a5;
                          var maxBytes2 = (_a5 = bodyConfig.maxBodySizeBytes) !== null && _a5 !== void 0 ? _a5 : DEFAULT_MAX_BODY_SIZE_BYTES;
                          var _b2 = captureSerializedBody(text, bodyConfig.response, maxBytes2, truncateToByteLimit), value = _b2.value, truncated = _b2.truncated;
                          requestEvent.responseBody = value;
                          requestEvent.responseBodyStatus = truncated ? "truncated" : "captured";
                          _this2.notifyEvent(requestEvent);
                        }, function() {
                          requestEvent.responseBodyStatus = "error";
                          _this2.notifyEvent(requestEvent);
                        });
                      }
                    } else {
                      this.notifyEvent(requestEvent);
                    }
                    return [2, response];
                  case 3:
                    error_1 = _c.sent();
                    endTime = Date.now();
                    requestEvent.duration = endTime - startTime;
                    typedError = error_1;
                    requestEvent.error = {
                      name: typedError.name || "UnknownError",
                      message: typedError.message || "An unknown error occurred"
                    };
                    this.notifyEvent(requestEvent);
                    throw error_1;
                  case 4:
                    return [
                      2
                      /*return*/
                    ];
                }
              });
            });
          };
          this.fetchObserver = function() {
            globalScope3.fetch = originalFetch;
          };
        };
        return NetworkObservers2;
      })();
    }
  });

  // node_modules/@amplitude/analytics-browser/lib/esm/browser-client-factory.js
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/browser-client.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/default-tracking.js
  init_tslib_es6();
  init_esm();
  var isTrackingEnabled = function(autocapture, event) {
    if (typeof autocapture === "boolean") {
      return autocapture;
    }
    if ((autocapture === null || autocapture === void 0 ? void 0 : autocapture[event]) === false) {
      return false;
    }
    if (isChromeExtension()) {
      return !!(autocapture === null || autocapture === void 0 ? void 0 : autocapture[event]);
    }
    return true;
  };
  var isAttributionTrackingEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "attribution");
  };
  var isFileDownloadTrackingEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "fileDownloads");
  };
  var isFormInteractionTrackingEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "formInteractions");
  };
  var isPageViewTrackingEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "pageViews");
  };
  var isSessionTrackingEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "sessions");
  };
  var isPageUrlEnrichmentEnabled = function(autocapture) {
    return isTrackingEnabled(autocapture, "pageUrlEnrichment");
  };
  var isNetworkTrackingEnabled = function(autocapture) {
    if (typeof autocapture === "boolean") {
      return autocapture;
    }
    if (typeof autocapture === "object" && (autocapture.networkTracking === true || typeof autocapture.networkTracking === "object")) {
      return true;
    }
    return false;
  };
  var isElementInteractionsEnabled = function(autocapture) {
    if (typeof autocapture === "boolean") {
      return autocapture;
    }
    if (typeof autocapture === "object" && (autocapture.elementInteractions === true || typeof autocapture.elementInteractions === "object")) {
      return true;
    }
    return false;
  };
  var isWebVitalsEnabled = function(autocapture) {
    if (typeof autocapture === "boolean") {
      return autocapture;
    }
    if (typeof autocapture === "object" && autocapture.webVitals === true) {
      return true;
    }
    return false;
  };
  var isFrustrationInteractionsEnabled = function(autocapture) {
    if (typeof autocapture === "boolean") {
      return autocapture;
    }
    if (typeof autocapture === "object" && (autocapture.frustrationInteractions === true || typeof autocapture.frustrationInteractions === "object")) {
      return true;
    }
    return false;
  };
  var isPerformanceTrackingEnabled = function(autocapture) {
    if (typeof autocapture === "object" && (autocapture.performanceTracking === true || typeof autocapture.performanceTracking === "object")) {
      return true;
    }
    return false;
  };
  var getPerformanceTrackingConfig = function(config2) {
    if (typeof config2.autocapture !== "object") {
      return void 0;
    }
    var performanceTracking = config2.autocapture.performanceTracking;
    if (performanceTracking === true) {
      return { mainThreadBlock: true };
    }
    if (typeof performanceTracking === "object" && performanceTracking !== null) {
      return performanceTracking;
    }
    return void 0;
  };
  var isCustomEnrichmentEnabled = function(customEnrichment) {
    if (typeof customEnrichment === "boolean") {
      return customEnrichment;
    }
    if (typeof customEnrichment === "object" && customEnrichment !== null && customEnrichment.enabled !== false) {
      return true;
    }
    return false;
  };
  var getElementInteractionsConfig = function(config2) {
    if (isElementInteractionsEnabled(config2.autocapture) && typeof config2.autocapture === "object" && typeof config2.autocapture.elementInteractions === "object") {
      return config2.autocapture.elementInteractions;
    }
    return void 0;
  };
  var getFrustrationInteractionsConfig = function(config2) {
    if (isFrustrationInteractionsEnabled(config2.autocapture) && typeof config2.autocapture === "object" && typeof config2.autocapture.frustrationInteractions === "object") {
      return config2.autocapture.frustrationInteractions;
    }
    return void 0;
  };
  var getNetworkTrackingConfig = function(config2) {
    if (isNetworkTrackingEnabled(config2.autocapture)) {
      var networkTrackingConfig = void 0;
      if (typeof config2.autocapture === "object" && typeof config2.autocapture.networkTracking === "object") {
        networkTrackingConfig = config2.autocapture.networkTracking;
      } else if (config2.networkTrackingOptions) {
        networkTrackingConfig = config2.networkTrackingOptions;
      }
      return __assign(__assign({}, networkTrackingConfig), { captureRules: normalizeNetworkCaptureRules(networkTrackingConfig === null || networkTrackingConfig === void 0 ? void 0 : networkTrackingConfig.captureRules, config2.loggerProvider) });
    }
    return;
  };
  var getPageViewTrackingConfig = function(config2) {
    var trackOn = function() {
      return false;
    };
    var trackHistoryChanges = void 0;
    var eventType;
    var pageCounter = config2.pageCounter;
    var isDefaultPageViewTrackingEnabled = isPageViewTrackingEnabled(config2.defaultTracking);
    if (isDefaultPageViewTrackingEnabled) {
      trackOn = void 0;
      eventType = void 0;
      if (config2.defaultTracking && typeof config2.defaultTracking === "object" && config2.defaultTracking.pageViews && typeof config2.defaultTracking.pageViews === "object") {
        if ("trackOn" in config2.defaultTracking.pageViews) {
          trackOn = config2.defaultTracking.pageViews.trackOn;
        }
        if ("trackHistoryChanges" in config2.defaultTracking.pageViews) {
          trackHistoryChanges = config2.defaultTracking.pageViews.trackHistoryChanges;
        }
        if ("eventType" in config2.defaultTracking.pageViews && config2.defaultTracking.pageViews.eventType) {
          eventType = config2.defaultTracking.pageViews.eventType;
        }
      }
    }
    return {
      trackOn,
      trackHistoryChanges,
      eventType,
      pageCounter
    };
  };
  var getAttributionTrackingConfig = function(config2) {
    if (isAttributionTrackingEnabled(config2.defaultTracking) && config2.defaultTracking && typeof config2.defaultTracking === "object" && config2.defaultTracking.attribution && typeof config2.defaultTracking.attribution === "object") {
      return __assign({}, config2.defaultTracking.attribution);
    }
    return {};
  };
  var getFormInteractionsConfig = function(config2) {
    if (isFormInteractionTrackingEnabled(config2.defaultTracking) && config2.defaultTracking && typeof config2.defaultTracking === "object" && typeof config2.defaultTracking.formInteractions === "object") {
      return config2.defaultTracking.formInteractions;
    }
    return void 0;
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/utils/snippet-helper.js
  var convertProxyObjectToRealObject = function(instance, queue) {
    for (var i2 = 0; i2 < queue.length; i2++) {
      var _a4 = queue[i2], name_1 = _a4.name, args = _a4.args, resolve = _a4.resolve;
      var fn = instance && instance[name_1];
      if (typeof fn === "function") {
        var result3 = fn.apply(instance, args);
        if (typeof resolve === "function") {
          resolve(result3 === null || result3 === void 0 ? void 0 : result3.promise);
        }
      }
    }
    return instance;
  };
  var isInstanceProxy = function(instance) {
    var instanceProxy = instance;
    return instanceProxy && instanceProxy._q !== void 0;
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/utils/environment.js
  init_esm();
  var isChromeExtension2 = function(scope) {
    var _a4, _b;
    return typeof ((_b = (_a4 = scope.chrome) === null || _a4 === void 0 ? void 0 : _a4.runtime) === null || _b === void 0 ? void 0 : _b.id) === "string";
  };
  var isServiceWorker = function(scope) {
    var ctor = scope.ServiceWorkerGlobalScope;
    return typeof ctor === "function" && scope instanceof ctor;
  };
  var isWebWorker = function(scope) {
    var ctor = scope.WorkerGlobalScope;
    return typeof ctor === "function" && scope instanceof ctor;
  };
  var isBrowser = function(scope) {
    return typeof scope.document !== "undefined";
  };
  var isNode = function(scope) {
    var _a4, _b;
    return typeof ((_b = (_a4 = scope.process) === null || _a4 === void 0 ? void 0 : _a4.versions) === null || _b === void 0 ? void 0 : _b.node) === "string";
  };
  function getRuntimeEnvironment() {
    var scope = getGlobalScope();
    if (!scope) {
      return "unknown";
    }
    var isExtension = isChromeExtension2(scope);
    if (isServiceWorker(scope)) {
      return isExtension ? "chrome_extension_service_worker" : "service_worker";
    }
    if (isWebWorker(scope)) {
      return "web_worker";
    }
    if (isBrowser(scope)) {
      return isExtension ? "chrome_extension" : "browser";
    }
    if (isNode(scope)) {
      return "node";
    }
    return "unknown";
  }

  // node_modules/@amplitude/analytics-browser/lib/esm/plugins/context.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/version.js
  var VERSION = "2.45.10";

  // node_modules/@amplitude/analytics-browser/lib/esm/lib-prefix.js
  var LIBPREFIX = "amplitude-ts";

  // node_modules/@amplitude/analytics-browser/lib/esm/plugins/context.js
  var BROWSER_PLATFORM = "Web";
  var IP_ADDRESS = "$remote";
  var Context = (
    /** @class */
    (function() {
      function Context2() {
        this.name = "@amplitude/plugin-context-browser";
        this.type = "before";
        this.library = "".concat(LIBPREFIX, "/").concat(VERSION);
        if (typeof navigator !== "undefined") {
          this.userAgent = navigator.userAgent;
        }
      }
      Context2.prototype.setup = function(config2) {
        this.config = config2;
        return Promise.resolve(void 0);
      };
      Context2.prototype.execute = function(context) {
        var _a4, _b;
        return __awaiter(this, void 0, void 0, function() {
          var time, lastEventId, nextEventId, event;
          return __generator(this, function(_c) {
            time = (/* @__PURE__ */ new Date()).getTime();
            lastEventId = (_a4 = this.config.lastEventId) !== null && _a4 !== void 0 ? _a4 : -1;
            nextEventId = (_b = context.event_id) !== null && _b !== void 0 ? _b : lastEventId + 1;
            this.config.lastEventId = nextEventId;
            if (!context.time) {
              this.config.lastEventTime = time;
            }
            event = __assign(__assign(__assign(__assign(__assign(__assign(__assign(__assign({ user_id: this.config.userId, device_id: this.config.deviceId, session_id: this.config.sessionId, time }, this.config.appVersion && { app_version: this.config.appVersion }), this.config.trackingOptions.platform && { platform: BROWSER_PLATFORM }), this.config.trackingOptions.language && { language: getLanguage2() }), this.config.trackingOptions.ipAddress && { ip: IP_ADDRESS }), { insert_id: UUID(), partner_id: this.config.partnerId, plan: this.config.plan }), this.config.ingestionMetadata && {
              ingestion_metadata: {
                source_name: this.config.ingestionMetadata.sourceName,
                source_version: this.config.ingestionMetadata.sourceVersion
              }
            }), context), { event_id: nextEventId, library: this.library, user_agent: this.userAgent });
            return [2, event];
          });
        });
      };
      return Context2;
    })()
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/config.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/storage/local-storage.js
  init_tslib_es6();
  init_esm();
  var MAX_ARRAY_LENGTH = 1e3;
  var LocalStorage = (
    /** @class */
    (function(_super) {
      __extends(LocalStorage2, _super);
      function LocalStorage2(config2) {
        var _this = this;
        var _a4, _b;
        var localStorage2;
        try {
          localStorage2 = (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.localStorage;
        } catch (e2) {
          (_b = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider) === null || _b === void 0 ? void 0 : _b.debug("Failed to access localStorage. error=".concat(JSON.stringify(e2)));
          localStorage2 = void 0;
        }
        _this = _super.call(this, localStorage2) || this;
        _this.loggerProvider = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider;
        return _this;
      }
      LocalStorage2.prototype.set = function(key, value) {
        var _a4;
        return __awaiter(this, void 0, void 0, function() {
          var droppedEventsCount;
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                if (!(Array.isArray(value) && value.length > MAX_ARRAY_LENGTH)) return [3, 2];
                droppedEventsCount = value.length - MAX_ARRAY_LENGTH;
                return [4, _super.prototype.set.call(this, key, value.slice(0, MAX_ARRAY_LENGTH))];
              case 1:
                _b.sent();
                (_a4 = this.loggerProvider) === null || _a4 === void 0 ? void 0 : _a4.error("Failed to save ".concat(droppedEventsCount, " events because the queue length exceeded ").concat(MAX_ARRAY_LENGTH, "."));
                return [3, 4];
              case 2:
                return [4, _super.prototype.set.call(this, key, value)];
              case 3:
                _b.sent();
                _b.label = 4;
              case 4:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      return LocalStorage2;
    })(BrowserStorage)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/storage/session-storage.js
  init_tslib_es6();
  init_esm();
  var SessionStorage = (
    /** @class */
    (function(_super) {
      __extends(SessionStorage2, _super);
      function SessionStorage2() {
        var _a4;
        return _super.call(this, (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.sessionStorage) || this;
      }
      return SessionStorage2;
    })(BrowserStorage)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/transports/xhr.js
  init_tslib_es6();
  init_esm();
  var XHRTransport = (
    /** @class */
    (function(_super) {
      __extends(XHRTransport2, _super);
      function XHRTransport2(customHeaders) {
        if (customHeaders === void 0) {
          customHeaders = {};
        }
        var _this = _super.call(this) || this;
        _this.state = {
          done: 4
        };
        _this.customHeaders = customHeaders;
        return _this;
      }
      XHRTransport2.prototype.send = function(serverUrl, payload, shouldCompressUploadBody) {
        if (shouldCompressUploadBody === void 0) {
          shouldCompressUploadBody = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          var _this = this;
          return __generator(this, function(_a4) {
            return [2, new Promise(function(resolve, reject) {
              if (typeof XMLHttpRequest === "undefined") {
                reject(new Error("XHRTransport is not supported."));
              }
              var xhr = new XMLHttpRequest();
              xhr.open("POST", serverUrl, true);
              xhr.onreadystatechange = function() {
                if (xhr.readyState === _this.state.done) {
                  var responseText = xhr.responseText;
                  try {
                    resolve(_this.buildResponse(JSON.parse(responseText)));
                  } catch (_a5) {
                    resolve(_this.buildResponse({ code: xhr.status }));
                  }
                }
              };
              var headers = {
                "Content-Type": "application/json",
                Accept: "*/*"
              };
              var bodyString = JSON.stringify(payload);
              var shouldCompressBody = shouldCompressUploadBody && bodyString.length >= MIN_GZIP_UPLOAD_BODY_SIZE_BYTES && isCompressionStreamAvailable();
              var sendBody = function(body) {
                var e_1, _a5;
                headers = __assign(__assign({}, _this.customHeaders), headers);
                try {
                  for (var _b = __values(Object.entries(headers)), _c = _b.next(); !_c.done; _c = _b.next()) {
                    var _d = __read(_c.value, 2), key = _d[0], value = _d[1];
                    xhr.setRequestHeader(key, value);
                  }
                } catch (e_1_1) {
                  e_1 = { error: e_1_1 };
                } finally {
                  try {
                    if (_c && !_c.done && (_a5 = _b.return)) _a5.call(_b);
                  } finally {
                    if (e_1) throw e_1.error;
                  }
                }
                xhr.send(body);
              };
              var doSend = function() {
                return __awaiter(_this, void 0, void 0, function() {
                  var compressed;
                  return __generator(this, function(_a5) {
                    switch (_a5.label) {
                      case 0:
                        if (!shouldCompressBody) return [3, 2];
                        return [4, compressToGzipArrayBuffer(bodyString)];
                      case 1:
                        compressed = _a5.sent();
                        if (compressed) {
                          headers["Content-Encoding"] = "gzip";
                          sendBody(compressed);
                        } else {
                          sendBody(bodyString);
                        }
                        return [3, 3];
                      case 2:
                        sendBody(bodyString);
                        _a5.label = 3;
                      case 3:
                        return [
                          2
                          /*return*/
                        ];
                    }
                  });
                });
              };
              doSend().catch(reject);
            })];
          });
        });
      };
      return XHRTransport2;
    })(BaseTransport)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/transports/fetch.js
  init_tslib_es6();
  init_esm();
  var KEEPALIVE_MAX_BODY_SIZE_BYTES = 16 * 1024;
  var FetchTransport2 = (
    /** @class */
    (function(_super) {
      __extends(FetchTransport3, _super);
      function FetchTransport3(customHeaders, enableKeepalive, referrerPolicy) {
        if (customHeaders === void 0) {
          customHeaders = {};
        }
        var _this = _super.call(this) || this;
        _this.customHeaders = customHeaders;
        _this.enableKeepalive = enableKeepalive !== false;
        _this.referrerPolicy = referrerPolicy;
        return _this;
      }
      FetchTransport3.prototype.send = function(serverUrl, payload, shouldCompressUploadBody) {
        if (shouldCompressUploadBody === void 0) {
          shouldCompressUploadBody = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          var bodyString, shouldCompressBody, body, headers, compressed, bodySize, options, response, responseText;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                if (typeof fetch === "undefined") {
                  throw new Error("FetchTransport is not supported");
                }
                bodyString = JSON.stringify(payload);
                shouldCompressBody = shouldCompressUploadBody && bodyString.length >= MIN_GZIP_UPLOAD_BODY_SIZE_BYTES && isCompressionStreamAvailable();
                body = bodyString;
                headers = {
                  "Content-Type": "application/json",
                  Accept: "*/*"
                };
                if (!shouldCompressBody) return [3, 2];
                return [4, compressToGzipArrayBuffer(bodyString)];
              case 1:
                compressed = _a4.sent();
                if (compressed) {
                  headers["Content-Encoding"] = "gzip";
                  body = compressed;
                }
                _a4.label = 2;
              case 2:
                headers = __assign(__assign({}, this.customHeaders), headers);
                bodySize = typeof body === "string" ? body.length : body.byteLength;
                options = {
                  headers,
                  body,
                  method: "POST",
                  keepalive: this.enableKeepalive && bodySize <= KEEPALIVE_MAX_BODY_SIZE_BYTES
                };
                if (this.referrerPolicy) {
                  options.referrerPolicy = this.referrerPolicy;
                }
                return [4, fetch(serverUrl, options)];
              case 3:
                response = _a4.sent();
                return [4, response.text()];
              case 4:
                responseText = _a4.sent();
                try {
                  return [2, this.buildResponse(JSON.parse(responseText))];
                } catch (_b) {
                  return [2, this.buildResponse({ code: response.status })];
                }
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      return FetchTransport3;
    })(BaseTransport)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/transports/send-beacon.js
  init_tslib_es6();
  init_esm();
  var SendBeaconTransport = (
    /** @class */
    (function(_super) {
      __extends(SendBeaconTransport2, _super);
      function SendBeaconTransport2() {
        return _super.call(this) || this;
      }
      SendBeaconTransport2.prototype.send = function(serverUrl, payload, _enableRequestBodyCompression) {
        if (_enableRequestBodyCompression === void 0) {
          _enableRequestBodyCompression = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          var _this = this;
          return __generator(this, function(_a4) {
            return [2, new Promise(function(resolve, reject) {
              var globalScope3 = getGlobalScope();
              if (!(globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.navigator.sendBeacon)) {
                throw new Error("SendBeaconTransport is not supported");
              }
              try {
                var data = JSON.stringify(payload);
                var success = globalScope3.navigator.sendBeacon(serverUrl, data);
                if (success) {
                  return resolve(_this.buildResponse({
                    code: 200,
                    events_ingested: payload.events.length,
                    payload_size_bytes: data.length,
                    server_upload_time: Date.now()
                  }));
                }
                return resolve(_this.buildResponse({ code: 500 }));
              } catch (e2) {
                reject(e2);
              }
            })];
          });
        });
      };
      return SendBeaconTransport2;
    })(BaseTransport)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/cookie-migration/index.js
  init_tslib_es6();
  init_esm();
  var parseLegacyCookies = function(apiKey2, cookieStorage, deleteLegacyCookies) {
    if (deleteLegacyCookies === void 0) {
      deleteLegacyCookies = true;
    }
    return __awaiter(void 0, void 0, void 0, function() {
      var cookieName, cookies, _a4, deviceId, userId, optOut, sessionId, lastEventTime, lastEventId;
      return __generator(this, function(_b) {
        switch (_b.label) {
          case 0:
            cookieName = getOldCookieName(apiKey2);
            return [4, cookieStorage.getRaw(cookieName)];
          case 1:
            cookies = _b.sent();
            if (!cookies) {
              return [2, {
                optOut: false
              }];
            }
            if (!deleteLegacyCookies) return [3, 3];
            return [4, cookieStorage.remove(cookieName)];
          case 2:
            _b.sent();
            _b.label = 3;
          case 3:
            _a4 = __read(cookies.split("."), 6), deviceId = _a4[0], userId = _a4[1], optOut = _a4[2], sessionId = _a4[3], lastEventTime = _a4[4], lastEventId = _a4[5];
            return [2, {
              deviceId,
              userId: decode(userId),
              sessionId: parseTime(sessionId),
              lastEventId: parseTime(lastEventId),
              lastEventTime: parseTime(lastEventTime),
              optOut: Boolean(optOut)
            }];
        }
      });
    });
  };
  var parseTime = function(num) {
    var integer = parseInt(num, 32);
    if (isNaN(integer)) {
      return void 0;
    }
    return integer;
  };
  var decode = function(value) {
    if (!atob || !escape || !value) {
      return void 0;
    }
    try {
      return decodeURIComponent(escape(atob(value)));
    } catch (_a4) {
      return void 0;
    }
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/constants.js
  var DEFAULT_EVENT_PREFIX = "[Amplitude]";
  var DEFAULT_PAGE_VIEW_EVENT = "".concat(DEFAULT_EVENT_PREFIX, " Page Viewed");
  var DEFAULT_FORM_START_EVENT = "".concat(DEFAULT_EVENT_PREFIX, " Form Started");
  var DEFAULT_FORM_SUBMIT_EVENT = "".concat(DEFAULT_EVENT_PREFIX, " Form Submitted");
  var DEFAULT_FILE_DOWNLOAD_EVENT = "".concat(DEFAULT_EVENT_PREFIX, " File Downloaded");
  var DEFAULT_SESSION_START_EVENT = "session_start";
  var DEFAULT_SESSION_END_EVENT = "session_end";
  var FILE_EXTENSION = "".concat(DEFAULT_EVENT_PREFIX, " File Extension");
  var FILE_NAME = "".concat(DEFAULT_EVENT_PREFIX, " File Name");
  var LINK_ID = "".concat(DEFAULT_EVENT_PREFIX, " Link ID");
  var LINK_TEXT = "".concat(DEFAULT_EVENT_PREFIX, " Link Text");
  var LINK_URL = "".concat(DEFAULT_EVENT_PREFIX, " Link URL");
  var FORM_ID = "".concat(DEFAULT_EVENT_PREFIX, " Form ID");
  var FORM_NAME = "".concat(DEFAULT_EVENT_PREFIX, " Form Name");
  var FORM_DESTINATION = "".concat(DEFAULT_EVENT_PREFIX, " Form Destination");
  var DEFAULT_IDENTITY_STORAGE = "cookie";
  var DEFAULT_SERVER_ZONE = "US";

  // node_modules/@amplitude/analytics-browser/lib/esm/attribution/helpers.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/types.js
  init_esm();

  // node_modules/@amplitude/analytics-browser/lib/esm/attribution/helpers.js
  var domainWithoutSubdomain = function(domain) {
    var parts = domain.split(".");
    if (parts.length <= 2) {
      return domain;
    }
    return parts.slice(parts.length - 2, parts.length).join(".");
  };
  var isDirectTraffic = function(current) {
    return Object.values(current).every(function(value) {
      return !value;
    });
  };
  var isEmptyCampaign = function(campaign) {
    var campaignWithoutReferrer = __assign(__assign({}, campaign), { referring_domain: void 0, referrer: void 0 });
    return Object.values(campaignWithoutReferrer).every(function(value) {
      return !value;
    });
  };
  var isNewCampaign = function(current, previous, options, logger, isNewSession2, topLevelDomain) {
    if (isNewSession2 === void 0) {
      isNewSession2 = true;
    }
    var referrer = current.referrer, referring_domain = current.referring_domain, currentCampaign = __rest(current, ["referrer", "referring_domain"]);
    var _a4 = previous || {}, _previous_referrer = _a4.referrer, prevReferringDomain = _a4.referring_domain, previousCampaign = __rest(_a4, ["referrer", "referring_domain"]);
    var excludeInternalReferrers = options.excludeInternalReferrers;
    if (excludeInternalReferrers) {
      var condition = getExcludeInternalReferrersCondition(excludeInternalReferrers, logger);
      if (!(condition instanceof TypeError) && current.referring_domain && isInternalReferrer(current.referring_domain, topLevelDomain)) {
        if (condition === "always") {
          debugLogInternalReferrerExclude(condition, current.referring_domain, logger);
          return false;
        } else if (condition === "ifEmptyCampaign" && isEmptyCampaign(current)) {
          debugLogInternalReferrerExclude(condition, current.referring_domain, logger);
          return false;
        }
      }
    }
    if (isExcludedReferrer(options.excludeReferrers, current.referring_domain)) {
      logger.debug("This is not a new campaign because ".concat(current.referring_domain, " is in the exclude referrer list."));
      return false;
    }
    if (!isNewSession2 && isDirectTraffic(current) && previous) {
      logger.debug("This is not a new campaign because this is a direct traffic in the same session.");
      return false;
    }
    var hasNewCampaign = JSON.stringify(currentCampaign) !== JSON.stringify(previousCampaign);
    var hasNewDomain = domainWithoutSubdomain(referring_domain || "") !== domainWithoutSubdomain(prevReferringDomain || "");
    var result3 = !previous || hasNewCampaign || hasNewDomain;
    if (!result3) {
      logger.debug("This is not a new campaign because it's the same as the previous one.");
    } else {
      logger.debug("This is a new campaign. An $identify event will be sent.");
    }
    return result3;
  };
  var isExcludedReferrer = function(excludeReferrers, referringDomain) {
    if (excludeReferrers === void 0) {
      excludeReferrers = [];
    }
    if (referringDomain === void 0) {
      referringDomain = "";
    }
    return excludeReferrers.some(function(value) {
      return value instanceof RegExp ? value.test(referringDomain) : value === referringDomain;
    });
  };
  var isSubdomainOf = function(subDomain, domain) {
    var cookieDomainWithLeadingDot = domain.startsWith(".") ? domain : ".".concat(domain);
    var subDomainWithLeadingDot = subDomain.startsWith(".") ? subDomain : ".".concat(subDomain);
    if (subDomainWithLeadingDot.endsWith(cookieDomainWithLeadingDot))
      return true;
    return false;
  };
  var createCampaignEvent = function(campaign, options) {
    var campaignParameters = __assign(__assign({}, BASE_CAMPAIGN), campaign);
    var identifyEvent = Object.entries(campaignParameters).reduce(function(identify2, _a4) {
      var _b;
      var _c = __read(_a4, 2), key = _c[0], value = _c[1];
      identify2.setOnce("initial_".concat(key), (_b = value !== null && value !== void 0 ? value : options.initialEmptyValue) !== null && _b !== void 0 ? _b : "EMPTY");
      if (value) {
        return identify2.set(key, value);
      }
      return identify2.unset(key);
    }, new Identify());
    return createIdentifyEvent(identifyEvent);
  };
  var getDefaultExcludedReferrers = function(cookieDomain) {
    var domain = cookieDomain;
    if (domain) {
      if (domain.startsWith(".")) {
        domain = domain.substring(1);
      }
      return [new RegExp("".concat(domain.replace(".", "\\."), "$"))];
    }
    return [];
  };
  var getExcludeInternalReferrersCondition = function(excludeInternalReferrers, logger) {
    if (excludeInternalReferrers === true) {
      return EXCLUDE_INTERNAL_REFERRERS_CONDITIONS.always;
    }
    if (typeof excludeInternalReferrers === "object") {
      var condition = excludeInternalReferrers.condition;
      if (typeof condition === "string" && Object.keys(EXCLUDE_INTERNAL_REFERRERS_CONDITIONS).includes(condition)) {
        return condition;
      } else if (typeof condition === "undefined") {
        return EXCLUDE_INTERNAL_REFERRERS_CONDITIONS.always;
      }
    }
    var errorMessage = "Invalid configuration provided for attribution.excludeInternalReferrers: ".concat(JSON.stringify(excludeInternalReferrers));
    logger.error(errorMessage);
    return new TypeError(errorMessage);
  };
  function debugLogInternalReferrerExclude(condition, referringDomain, logger) {
    var baseMessage = "This is not a new campaign because referring_domain=".concat(referringDomain, " is on the same domain as the current page and it is configured to exclude internal referrers");
    if (condition === "always") {
      logger.debug(baseMessage);
    } else if (condition === "ifEmptyCampaign") {
      logger.debug("".concat(baseMessage, " with empty campaign parameters"));
    }
  }
  var KNOWN_2LDS = [
    "ac.in",
    "ac.jp",
    "ac.kr",
    "ac.th",
    "ac.uk",
    "ac.za",
    "appspot.com",
    "asn.au",
    "azurewebsites.net",
    "cloudfront.net",
    "myshopify.com",
    "blogspot.com",
    "co.ca",
    "co.in",
    "co.jp",
    "co.kr",
    "co.nz",
    "co.th",
    "co.uk",
    "co.za",
    "com.ar",
    "com.au",
    "com.br",
    "com.cn",
    "com.hk",
    "com.in",
    "com.jp",
    "com.kr",
    "com.mx",
    "com.pl",
    "com.sg",
    "com.tr",
    "com.tw",
    "ed.jp",
    "edu.au",
    "edu.br",
    "edu.cn",
    "edu.hk",
    "edu.sg",
    "edu.th",
    "edu.tr",
    "edu.tw",
    "firebaseapp.com",
    "fly.dev",
    "gc.ca",
    "geek.nz",
    "github.io",
    "gitlab.io",
    "go.jp",
    "go.kr",
    "go.th",
    "gob.ar",
    "gob.mx",
    "gov.au",
    "gov.br",
    "gov.cn",
    "gov.hk",
    "gov.in",
    "gov.pl",
    "gov.sg",
    "gov.tr",
    "gov.tw",
    "gov.uk",
    "gov.za",
    "govt.nz",
    "gr.jp",
    "herokuapp.com",
    "id.au",
    "idv.hk",
    "iwi.nz",
    "lg.jp",
    "ltd.uk",
    "maori.nz",
    "me.uk",
    "mil.kr",
    "ne.jp",
    "ne.kr",
    "net.au",
    "net.br",
    "net.cn",
    "net.hk",
    "net.in",
    "net.nz",
    "net.pl",
    "net.sg",
    "net.tr",
    "net.tw",
    "net.za",
    "onrender.com",
    "or.jp",
    "or.kr",
    "or.th",
    "org.ar",
    "org.au",
    "org.br",
    "org.cn",
    "org.hk",
    "org.in",
    "org.mx",
    "org.nz",
    "org.pl",
    "org.sg",
    "org.tw",
    "org.uk",
    "org.za",
    "pages.dev",
    "pe.kr",
    "plc.uk",
    "re.kr",
    "res.in",
    "sch.uk",
    "vercel.app",
    "netlify.app",
    "workers.dev"
  ];
  var getDomain = function(hostnameParam) {
    var _a4, _b;
    var hostname = hostnameParam || ((_b = (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.location) === null || _b === void 0 ? void 0 : _b.hostname);
    if (!hostname) {
      return "";
    }
    var parts = hostname.split(".");
    var tld = parts[parts.length - 1];
    var name = parts[parts.length - 2];
    if (KNOWN_2LDS.find(function(tld2) {
      return hostname.endsWith(".".concat(tld2));
    })) {
      tld = parts[parts.length - 2] + "." + parts[parts.length - 1];
      name = parts[parts.length - 3];
    }
    if (!name)
      return tld;
    return "".concat(name, ".").concat(tld);
  };
  var isInternalReferrer = function(referringDomain, topLevelDomain) {
    var globalScope3 = getGlobalScope();
    if (!globalScope3)
      return false;
    var internalDomain = (topLevelDomain || "").trim() || getDomain(globalScope3.location.hostname);
    return isSubdomainOf(referringDomain, internalDomain);
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/config.js
  var BrowserConfig = (
    /** @class */
    (function(_super) {
      __extends(BrowserConfig2, _super);
      function BrowserConfig2(apiKey2, appVersion, cookieStorage, cookieOptions, defaultTracking, autocapture, deviceId, flushIntervalMillis, flushMaxRetries, flushQueueSize, identityStorage, ingestionMetadata, instanceName, lastEventId, lastEventTime, loggerProvider, logLevel, minIdLength, offline, optOut, partnerId, plan, serverUrl, serverZone, sessionId, deferredSessionId, sessionTimeout, storageProvider, trackingOptions, transport, useBatch, fetchRemoteConfig, userId, pageCounter, debugLogsEnabled, networkTrackingOptions, identify2, enableDiagnostics, diagnosticsSampleRate, diagnosticsClient, remoteConfig, topLevelDomain, enableRequestBodyCompression, customEnrichment) {
        if (cookieStorage === void 0) {
          cookieStorage = new MemoryStorage();
        }
        if (cookieOptions === void 0) {
          cookieOptions = {
            domain: "",
            expiration: 365,
            sameSite: "Lax",
            secure: false,
            upgrade: true
          };
        }
        if (flushIntervalMillis === void 0) {
          flushIntervalMillis = 1e3;
        }
        if (flushMaxRetries === void 0) {
          flushMaxRetries = 5;
        }
        if (flushQueueSize === void 0) {
          flushQueueSize = 30;
        }
        if (identityStorage === void 0) {
          identityStorage = DEFAULT_IDENTITY_STORAGE;
        }
        if (loggerProvider === void 0) {
          loggerProvider = new Logger();
        }
        if (logLevel === void 0) {
          logLevel = LogLevel.Warn;
        }
        if (offline === void 0) {
          offline = false;
        }
        if (optOut === void 0) {
          optOut = false;
        }
        if (serverUrl === void 0) {
          serverUrl = "";
        }
        if (serverZone === void 0) {
          serverZone = DEFAULT_SERVER_ZONE;
        }
        if (sessionTimeout === void 0) {
          sessionTimeout = 30 * 60 * 1e3;
        }
        if (storageProvider === void 0) {
          storageProvider = new LocalStorage({ loggerProvider });
        }
        if (trackingOptions === void 0) {
          trackingOptions = {
            ipAddress: true,
            language: true,
            platform: true
          };
        }
        if (transport === void 0) {
          transport = "fetch";
        }
        if (useBatch === void 0) {
          useBatch = false;
        }
        if (fetchRemoteConfig === void 0) {
          fetchRemoteConfig = true;
        }
        if (enableDiagnostics === void 0) {
          enableDiagnostics = true;
        }
        if (diagnosticsSampleRate === void 0) {
          diagnosticsSampleRate = 0;
        }
        if (enableRequestBodyCompression === void 0) {
          enableRequestBodyCompression = false;
        }
        var _this = this;
        var _a4;
        _this = _super.call(this, { apiKey: apiKey2, instanceName, storageProvider, transportProvider: createTransport(transport) }) || this;
        _this.apiKey = apiKey2;
        _this.appVersion = appVersion;
        _this.cookieOptions = cookieOptions;
        _this.defaultTracking = defaultTracking;
        _this.autocapture = autocapture;
        _this.flushIntervalMillis = flushIntervalMillis;
        _this.flushMaxRetries = flushMaxRetries;
        _this.flushQueueSize = flushQueueSize;
        _this.identityStorage = identityStorage;
        _this.ingestionMetadata = ingestionMetadata;
        _this.loggerProvider = loggerProvider;
        _this.logLevel = logLevel;
        _this.minIdLength = minIdLength;
        _this.offline = offline;
        _this.partnerId = partnerId;
        _this.plan = plan;
        _this.serverUrl = serverUrl;
        _this.serverZone = serverZone;
        _this.sessionTimeout = sessionTimeout;
        _this.storageProvider = storageProvider;
        _this.trackingOptions = trackingOptions;
        _this.transport = transport;
        _this.useBatch = useBatch;
        _this.fetchRemoteConfig = fetchRemoteConfig;
        _this.networkTrackingOptions = networkTrackingOptions;
        _this.identify = identify2;
        _this.enableDiagnostics = enableDiagnostics;
        _this.diagnosticsSampleRate = diagnosticsSampleRate;
        _this.diagnosticsClient = diagnosticsClient;
        _this.remoteConfig = remoteConfig;
        _this.topLevelDomain = topLevelDomain;
        _this.enableRequestBodyCompression = enableRequestBodyCompression;
        _this.customEnrichment = customEnrichment;
        _this.version = VERSION;
        _this._optOut = false;
        _this._cookieStorage = cookieStorage;
        _this.deviceId = deviceId;
        _this.lastEventId = lastEventId;
        _this.lastEventTime = lastEventTime;
        _this.optOut = optOut;
        _this.deferredSessionId = deferredSessionId;
        _this.sessionId = sessionId;
        _this.pageCounter = pageCounter;
        _this.userId = userId;
        _this.debugLogsEnabled = debugLogsEnabled;
        _this.loggerProvider.enable(debugLogsEnabled ? LogLevel.Debug : _this.logLevel);
        _this.networkTrackingOptions = networkTrackingOptions;
        _this.identify = identify2;
        _this.enableDiagnostics = enableDiagnostics;
        _this.diagnosticsSampleRate = diagnosticsSampleRate;
        _this.diagnosticsClient = diagnosticsClient;
        var _fetchRemoteConfig = (_a4 = remoteConfig === null || remoteConfig === void 0 ? void 0 : remoteConfig.fetchRemoteConfig) !== null && _a4 !== void 0 ? _a4 : fetchRemoteConfig;
        _this.remoteConfig = _this.remoteConfig || {};
        _this.remoteConfig.fetchRemoteConfig = _fetchRemoteConfig;
        _this.fetchRemoteConfig = _fetchRemoteConfig;
        _this.topLevelDomain = topLevelDomain || getDomain();
        return _this;
      }
      Object.defineProperty(BrowserConfig2.prototype, "cookieStorage", {
        get: function() {
          return this._cookieStorage;
        },
        set: function(cookieStorage) {
          if (this._cookieStorage !== cookieStorage) {
            this._cookieStorage = cookieStorage;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "deviceId", {
        get: function() {
          return this._deviceId;
        },
        set: function(deviceId) {
          if (this._deviceId !== deviceId) {
            this._deviceId = deviceId;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "userId", {
        get: function() {
          return this._userId;
        },
        set: function(userId) {
          if (this._userId !== userId) {
            this._userId = userId;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "sessionId", {
        get: function() {
          return this._sessionId;
        },
        set: function(sessionId) {
          if (this._sessionId !== sessionId) {
            this._sessionId = sessionId;
            if (sessionId !== void 0 && this._deferredSessionId !== void 0) {
              this._deferredSessionId = void 0;
            }
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "deferredSessionId", {
        get: function() {
          return this._deferredSessionId;
        },
        set: function(deferredSessionId) {
          if (this._deferredSessionId !== deferredSessionId && deferredSessionId !== this.sessionId) {
            this._deferredSessionId = deferredSessionId;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "optOut", {
        get: function() {
          return this._optOut;
        },
        set: function(optOut) {
          if (this._optOut !== optOut) {
            this._optOut = optOut;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "lastEventTime", {
        get: function() {
          return this._lastEventTime;
        },
        set: function(lastEventTime) {
          if (this._lastEventTime !== lastEventTime) {
            this._lastEventTime = lastEventTime;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "lastEventId", {
        get: function() {
          return this._lastEventId;
        },
        set: function(lastEventId) {
          if (this._lastEventId !== lastEventId) {
            this._lastEventId = lastEventId;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "pageCounter", {
        get: function() {
          return this._pageCounter;
        },
        set: function(pageCounter) {
          if (this._pageCounter !== pageCounter) {
            this._pageCounter = pageCounter;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(BrowserConfig2.prototype, "debugLogsEnabled", {
        set: function(debugLogsEnabled) {
          if (this._debugLogsEnabled !== debugLogsEnabled) {
            this._debugLogsEnabled = debugLogsEnabled;
            this.updateStorage();
          }
        },
        enumerable: false,
        configurable: true
      });
      BrowserConfig2.prototype.updateStorage = function() {
        var cache = {
          deviceId: this._deviceId,
          userId: this._userId,
          sessionId: this._sessionId,
          deferredSessionId: this._deferredSessionId,
          optOut: this._optOut,
          lastEventTime: this._lastEventTime,
          lastEventId: this._lastEventId,
          pageCounter: this._pageCounter,
          debugLogsEnabled: this._debugLogsEnabled,
          cookieDomain: void 0
        };
        if (this.cookieStorage instanceof CookieStorage) {
          cache.cookieDomain = this.cookieStorage.options.domain;
        }
        void this.cookieStorage.set(getCookieName(this.apiKey), cache);
      };
      return BrowserConfig2;
    })(Config)
  );
  var useBrowserConfig = function(apiKey2, options, amplitudeInstance, diagnosticsClient, earlyConfig) {
    if (options === void 0) {
      options = {};
    }
    return __awaiter(void 0, void 0, void 0, function() {
      var identityStorage, defaultCookieDomain, cookieOptions, cookieConfig, cookieStorage, legacyCookies, previousCookies, queryParams, ampTimestamp, isWithinTimeLimit, deviceId, lastEventId, lastEventTime, optOut, sessionId, deferredSessionId, userId, trackingOptions, pageCounter, debugLogsEnabled, browserConfig;
      var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _0, _1, _2, _3, _4, _5;
      return __generator(this, function(_6) {
        switch (_6.label) {
          case 0:
            identityStorage = options.identityStorage || DEFAULT_IDENTITY_STORAGE;
            defaultCookieDomain = "";
            if (!(identityStorage === DEFAULT_IDENTITY_STORAGE && !((_a4 = options.cookieOptions) === null || _a4 === void 0 ? void 0 : _a4.domain) && ((_b = options.cookieOptions) === null || _b === void 0 ? void 0 : _b.domain) !== "")) return [3, 2];
            return [4, getTopLevelDomain(void 0, diagnosticsClient)];
          case 1:
            defaultCookieDomain = _6.sent();
            _6.label = 2;
          case 2:
            cookieOptions = __assign({ domain: (_d = (_c = options.cookieOptions) === null || _c === void 0 ? void 0 : _c.domain) !== null && _d !== void 0 ? _d : defaultCookieDomain, expiration: 365, sameSite: "Lax", secure: false, upgrade: true }, options.cookieOptions);
            cookieConfig = {
              // if more than one cookie with the same key exists,
              // look for the cookie that has the domain attribute set to cookieOptions.domain
              duplicateResolverFn: function(value) {
                var decodedValue = decodeCookieValue(value);
                if (!decodedValue) {
                  return false;
                }
                var parsed = JSON.parse(decodedValue);
                return isDomainEqual(parsed.cookieDomain, cookieOptions.domain);
              },
              diagnosticsClient
            };
            cookieStorage = createCookieStorage(options.identityStorage, cookieOptions, cookieConfig);
            return [4, parseLegacyCookies(apiKey2, cookieStorage, (_f = (_e = options.cookieOptions) === null || _e === void 0 ? void 0 : _e.upgrade) !== null && _f !== void 0 ? _f : true)];
          case 3:
            legacyCookies = _6.sent();
            return [4, cookieStorage.get(getCookieName(apiKey2))];
          case 4:
            previousCookies = _6.sent();
            queryParams = getQueryParams();
            ampTimestamp = queryParams.ampTimestamp ? Number(queryParams.ampTimestamp) : void 0;
            isWithinTimeLimit = ampTimestamp ? Date.now() < ampTimestamp : true;
            deviceId = (_l = (_k = (_j = (_g = options.deviceId) !== null && _g !== void 0 ? _g : isWithinTimeLimit ? (_h = queryParams.ampDeviceId) !== null && _h !== void 0 ? _h : queryParams.deviceId : void 0) !== null && _j !== void 0 ? _j : previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.deviceId) !== null && _k !== void 0 ? _k : legacyCookies.deviceId) !== null && _l !== void 0 ? _l : UUID();
            lastEventId = (_m = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.lastEventId) !== null && _m !== void 0 ? _m : legacyCookies.lastEventId;
            lastEventTime = (_o = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.lastEventTime) !== null && _o !== void 0 ? _o : legacyCookies.lastEventTime;
            optOut = (_q = (_p = options.optOut) !== null && _p !== void 0 ? _p : previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.optOut) !== null && _q !== void 0 ? _q : legacyCookies.optOut;
            sessionId = (_r = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.sessionId) !== null && _r !== void 0 ? _r : legacyCookies.sessionId;
            deferredSessionId = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.deferredSessionId;
            userId = (_t = (_s = options.userId) !== null && _s !== void 0 ? _s : previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.userId) !== null && _t !== void 0 ? _t : legacyCookies.userId;
            amplitudeInstance.previousSessionDeviceId = (_u = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.deviceId) !== null && _u !== void 0 ? _u : legacyCookies.deviceId;
            amplitudeInstance.previousSessionUserId = (_v = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.userId) !== null && _v !== void 0 ? _v : legacyCookies.userId;
            trackingOptions = {
              ipAddress: (_x = (_w = options.trackingOptions) === null || _w === void 0 ? void 0 : _w.ipAddress) !== null && _x !== void 0 ? _x : true,
              language: (_z = (_y = options.trackingOptions) === null || _y === void 0 ? void 0 : _y.language) !== null && _z !== void 0 ? _z : true,
              platform: (_1 = (_0 = options.trackingOptions) === null || _0 === void 0 ? void 0 : _0.platform) !== null && _1 !== void 0 ? _1 : true
            };
            pageCounter = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.pageCounter;
            debugLogsEnabled = previousCookies === null || previousCookies === void 0 ? void 0 : previousCookies.debugLogsEnabled;
            if (options.autocapture !== void 0) {
              options.defaultTracking = options.autocapture;
            }
            browserConfig = new BrowserConfig(
              apiKey2,
              options.appVersion,
              cookieStorage,
              cookieOptions,
              options.defaultTracking,
              options.autocapture,
              deviceId,
              options.flushIntervalMillis,
              options.flushMaxRetries,
              options.flushQueueSize,
              identityStorage,
              options.ingestionMetadata,
              options.instanceName,
              lastEventId,
              lastEventTime,
              // Use earlyConfig.loggerProvider to ensure consistent logger across DiagnosticsClient/RemoteConfigClient/BrowserConfig
              (_2 = earlyConfig === null || earlyConfig === void 0 ? void 0 : earlyConfig.loggerProvider) !== null && _2 !== void 0 ? _2 : options.loggerProvider,
              options.logLevel,
              options.minIdLength,
              options.offline,
              optOut,
              options.partnerId,
              options.plan,
              options.serverUrl,
              // Use earlyConfig.serverZone to ensure consistent serverZone
              (_3 = earlyConfig === null || earlyConfig === void 0 ? void 0 : earlyConfig.serverZone) !== null && _3 !== void 0 ? _3 : options.serverZone,
              sessionId,
              deferredSessionId,
              options.sessionTimeout,
              options.storageProvider,
              trackingOptions,
              options.transport,
              options.useBatch,
              options.fetchRemoteConfig,
              userId,
              pageCounter,
              debugLogsEnabled,
              options.networkTrackingOptions,
              options.identify,
              // Use earlyConfig values (already has remote config applied), otherwise fall back to options
              (_4 = earlyConfig === null || earlyConfig === void 0 ? void 0 : earlyConfig.enableDiagnostics) !== null && _4 !== void 0 ? _4 : options.enableDiagnostics,
              (_5 = earlyConfig === null || earlyConfig === void 0 ? void 0 : earlyConfig.diagnosticsSampleRate) !== null && _5 !== void 0 ? _5 : amplitudeInstance._diagnosticsSampleRate,
              diagnosticsClient,
              options.remoteConfig,
              defaultCookieDomain,
              options.enableRequestBodyCompression,
              options.customEnrichment
            );
            return [4, browserConfig.storageProvider.isEnabled()];
          case 5:
            if (!_6.sent()) {
              browserConfig.loggerProvider.warn("Storage provider ".concat(browserConfig.storageProvider.constructor.name, " is not enabled. Falling back to MemoryStorage."));
              browserConfig.storageProvider = new MemoryStorage();
            }
            return [2, browserConfig];
        }
      });
    });
  };
  var createCookieStorage = function(identityStorage, cookieOptions, cookieConfig) {
    if (identityStorage === void 0) {
      identityStorage = DEFAULT_IDENTITY_STORAGE;
    }
    if (cookieOptions === void 0) {
      cookieOptions = {};
    }
    switch (identityStorage) {
      case "localStorage":
        return new LocalStorage();
      case "sessionStorage":
        return new SessionStorage();
      case "none":
        return new MemoryStorage();
      case "cookie":
      default:
        return new CookieStorage(__assign(__assign({}, cookieOptions), { expirationDays: cookieOptions.expiration }), cookieConfig);
    }
  };
  var shouldFetchRemoteConfig = function(options) {
    var _a4, _b;
    if (options === void 0) {
      options = {};
    }
    if (((_a4 = options.remoteConfig) === null || _a4 === void 0 ? void 0 : _a4.fetchRemoteConfig) === true) {
      return true;
    } else if (((_b = options.remoteConfig) === null || _b === void 0 ? void 0 : _b.fetchRemoteConfig) === false || options.fetchRemoteConfig === false) {
      return false;
    } else {
      return true;
    }
  };
  var createTransport = function(transport) {
    var type = typeof transport === "object" ? transport.type : transport;
    var headers = typeof transport === "object" ? transport.headers : void 0;
    var enableKeepalive = typeof transport === "object" ? transport.enableKeepalive : void 0;
    var referrerPolicy = typeof transport === "object" ? transport.referrerPolicy : void 0;
    if (type === "xhr") {
      return new XHRTransport(headers);
    }
    if (type === "beacon") {
      return new SendBeaconTransport();
    }
    return new FetchTransport2(headers, enableKeepalive, referrerPolicy);
  };
  var getTopLevelDomain = function(url, diagnosticsClient) {
    return __awaiter(void 0, void 0, void 0, function() {
      var host3, parts, levels, skipLevel, i2, i2, domain, result3, e_1;
      return __generator(this, function(_a4) {
        switch (_a4.label) {
          case 0:
            return [4, new CookieStorage(void 0, { diagnosticsClient }).isEnabled()];
          case 1:
            if (!_a4.sent() || !url && (typeof location === "undefined" || !location.hostname)) {
              return [2, ""];
            }
            host3 = url !== null && url !== void 0 ? url : location.hostname;
            parts = host3.split(".");
            if (parts.length === 1) {
              return [2, ""];
            }
            levels = [];
            skipLevel = 1;
            if (KNOWN_2LDS.find(function(tld) {
              return host3.endsWith(".".concat(tld));
            })) {
              skipLevel = 2;
            }
            for (i2 = parts.length - skipLevel - 1; i2 >= 0; --i2) {
              levels.push(parts.slice(i2).join("."));
            }
            i2 = 0;
            _a4.label = 2;
          case 2:
            if (!(i2 < levels.length)) return [3, 7];
            domain = levels[i2];
            _a4.label = 3;
          case 3:
            _a4.trys.push([3, 5, , 6]);
            return [4, CookieStorage.isDomainWritable(domain)];
          case 4:
            result3 = _a4.sent();
            if (result3) {
              return [2, "." + domain];
            }
            return [3, 6];
          case 5:
            e_1 = _a4.sent();
            if (diagnosticsClient) {
              diagnosticsClient.recordEvent("cookies.tld.failure", {
                reason: "Unexpected exception checking domain is writable: ".concat(domain),
                error: e_1 instanceof Error ? e_1.message : String(e_1)
              });
            }
            return [3, 6];
          case 6:
            i2++;
            return [3, 2];
          case 7:
            if (diagnosticsClient) {
              diagnosticsClient.recordEvent("cookies.tld.failure", {
                reason: "Could not determine TLD for host ".concat(host3)
              });
            }
            return [2, ""];
        }
      });
    });
  };

  // node_modules/@amplitude/plugin-page-view-tracking-browser/lib/esm/page-view-tracking.js
  init_tslib_es6();
  init_esm();
  var defaultPageViewEvent = "[Amplitude] Page Viewed";
  var PAGE_VIEW_SESSION_STORAGE_KEY = "AMP_PAGE_VIEW";
  var pageViewTrackingPlugin = function(options) {
    if (options === void 0) {
      options = {};
    }
    var amplitude;
    var globalScope3 = getGlobalScope();
    var loggerProvider = void 0;
    var isTracking = false;
    var localConfig;
    var sessionStorage;
    var trackOn = options.trackOn, trackHistoryChanges = options.trackHistoryChanges, _a4 = options.eventType, eventType = _a4 === void 0 ? defaultPageViewEvent : _a4;
    var getDecodeURI2 = function(locationStr) {
      var decodedLocationStr = locationStr;
      try {
        decodedLocationStr = decodeURI(locationStr);
      } catch (e2) {
        loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Malformed URI sequence: ", e2);
      }
      return decodedLocationStr;
    };
    var createPageViewEvent = function(pageViewId) {
      return __awaiter(void 0, void 0, void 0, function() {
        var locationHREF, _a5;
        var _b;
        return __generator(this, function(_c) {
          switch (_c.label) {
            case 0:
              locationHREF = getDecodeURI2(typeof location !== "undefined" && location.href || "");
              _b = {
                event_type: eventType
              };
              _a5 = [{}];
              return [4, getCampaignParams()];
            case 1:
              return [2, (_b.event_properties = __assign.apply(void 0, [__assign.apply(void 0, _a5.concat([_c.sent()])), { "[Amplitude] Page Domain": (
                /* istanbul ignore next */
                typeof location !== "undefined" && location.hostname || ""
              ), "[Amplitude] Page Location": locationHREF, "[Amplitude] Page Path": (
                /* istanbul ignore next */
                typeof location !== "undefined" && getDecodeURI2(location.pathname) || ""
              ), "[Amplitude] Page Title": (
                /* istanbul ignore next */
                getPageTitle(replaceSensitiveString)
              ), "[Amplitude] Page URL": locationHREF.split("?")[0], "[Amplitude] Page View ID": pageViewId }]), _b)];
          }
        });
      });
    };
    var shouldTrackOnPageLoad = function() {
      return typeof trackOn === "undefined" || typeof trackOn === "function" && trackOn();
    };
    var previousURL = typeof location !== "undefined" ? location.href : null;
    var trackHistoryPageView = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var newURL, shouldTrackPageView, pageViewId, _a5, _b, _c;
        return __generator(this, function(_d) {
          switch (_d.label) {
            case 0:
              newURL = location.href;
              shouldTrackPageView = shouldTrackHistoryPageView(trackHistoryChanges, newURL, previousURL || "") && shouldTrackOnPageLoad();
              previousURL = newURL;
              if (!shouldTrackPageView) return [3, 4];
              pageViewId = void 0;
              if (sessionStorage) {
                pageViewId = UUID();
                void sessionStorage.set(PAGE_VIEW_SESSION_STORAGE_KEY, { pageViewId });
              }
              loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.log("Tracking page view event");
              if (!(amplitude === null || amplitude === void 0)) return [3, 1];
              _a5 = void 0;
              return [3, 3];
            case 1:
              _c = (_b = amplitude).track;
              return [4, createPageViewEvent(pageViewId)];
            case 2:
              _a5 = _c.apply(_b, [_d.sent()]);
              _d.label = 3;
            case 3:
              _a5;
              _d.label = 4;
            case 4:
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    var handlePageChange = function() {
      void trackHistoryPageView();
    };
    var plugin = {
      name: "@amplitude/plugin-page-view-tracking-browser",
      type: "enrichment",
      setup: function(config2, client) {
        return __awaiter(void 0, void 0, void 0, function() {
          var pageViewId, _a5, _b;
          return __generator(this, function(_c) {
            switch (_c.label) {
              case 0:
                amplitude = client;
                localConfig = config2;
                loggerProvider = config2.loggerProvider;
                loggerProvider.log("Installing @amplitude/plugin-page-view-tracking-browser");
                isTracking = true;
                if (globalScope3) {
                  try {
                    sessionStorage = new BrowserStorage(globalScope3.sessionStorage);
                  } catch (error) {
                    loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.debug("sessionStorage is not available in this environment.");
                  }
                  globalScope3.addEventListener("popstate", handlePageChange);
                  globalScope3.history.pushState = new Proxy(globalScope3.history.pushState, {
                    apply: function(target, thisArg, _a6) {
                      var _b2 = __read(_a6, 3), state = _b2[0], unused = _b2[1], url = _b2[2];
                      target.apply(thisArg, [state, unused, url]);
                      if (isTracking) {
                        handlePageChange();
                      }
                    }
                  });
                }
                if (!shouldTrackOnPageLoad()) return [3, 2];
                loggerProvider.log("Tracking page view event");
                pageViewId = void 0;
                if (sessionStorage) {
                  pageViewId = UUID();
                  void sessionStorage.set(PAGE_VIEW_SESSION_STORAGE_KEY, { pageViewId });
                }
                _b = (_a5 = amplitude).track;
                return [4, createPageViewEvent(pageViewId)];
              case 1:
                _b.apply(_a5, [_c.sent()]);
                _c.label = 2;
              case 2:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      },
      execute: function(event) {
        return __awaiter(void 0, void 0, void 0, function() {
          var pageViewId, pageViewSession, pageViewEvent;
          return __generator(this, function(_a5) {
            switch (_a5.label) {
              case 0:
                if (!(trackOn === "attribution" && isCampaignEvent(event))) return [3, 4];
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.log("Enriching campaign event to page view event with campaign parameters");
                pageViewId = void 0;
                if (!sessionStorage) return [3, 2];
                return [4, sessionStorage.get(PAGE_VIEW_SESSION_STORAGE_KEY)];
              case 1:
                pageViewSession = _a5.sent();
                pageViewId = pageViewSession === null || pageViewSession === void 0 ? void 0 : pageViewSession.pageViewId;
                _a5.label = 2;
              case 2:
                return [4, createPageViewEvent(pageViewId)];
              case 3:
                pageViewEvent = _a5.sent();
                event.event_type = pageViewEvent.event_type;
                event.event_properties = __assign(__assign({}, event.event_properties), pageViewEvent.event_properties);
                _a5.label = 4;
              case 4:
                if (localConfig && event.event_type === eventType) {
                  localConfig.pageCounter = !localConfig.pageCounter ? 1 : localConfig.pageCounter + 1;
                  event.event_properties = __assign(__assign({}, event.event_properties), { "[Amplitude] Page Counter": localConfig.pageCounter });
                }
                return [2, event];
            }
          });
        });
      },
      teardown: function() {
        return __awaiter(void 0, void 0, void 0, function() {
          return __generator(this, function(_a5) {
            if (globalScope3) {
              globalScope3.removeEventListener("popstate", handlePageChange);
              isTracking = false;
            }
            return [
              2
              /*return*/
            ];
          });
        });
      }
    };
    return plugin;
  };
  var getCampaignParams = function() {
    return __awaiter(void 0, void 0, void 0, function() {
      var _a4;
      return __generator(this, function(_b) {
        switch (_b.label) {
          case 0:
            _a4 = omitUndefined;
            return [4, new CampaignParser().parse()];
          case 1:
            return [2, _a4.apply(void 0, [_b.sent()])];
        }
      });
    });
  };
  var isCampaignEvent = function(event) {
    if (event.event_type === "$identify" && event.user_properties) {
      var properties = event.user_properties;
      var $set = properties[IdentifyOperation.SET] || {};
      var $unset = properties[IdentifyOperation.UNSET] || {};
      var userProperties_1 = __spreadArray(__spreadArray([], __read(Object.keys($set)), false), __read(Object.keys($unset)), false);
      return Object.keys(BASE_CAMPAIGN).every(function(value) {
        return userProperties_1.includes(value);
      });
    }
    return false;
  };
  var shouldTrackHistoryPageView = function(trackingOption, newURLStr, oldURLStr) {
    switch (trackingOption) {
      case "pathOnly": {
        if (oldURLStr == "")
          return true;
        var newURL = new URL(newURLStr);
        var oldURL = new URL(oldURLStr);
        var newBaseStr = newURL.origin + newURL.pathname;
        var oldBaseStr = oldURL.origin + oldURL.pathname;
        return newBaseStr !== oldBaseStr;
      }
      default:
        return newURLStr !== oldURLStr;
    }
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/plugins/form-interaction-tracking.js
  init_tslib_es6();
  init_esm();
  var formInteractionTracking = function() {
    var observer;
    var eventListeners = [];
    var addEventListener2 = function(element, type2, handler) {
      element.addEventListener(type2, handler);
      eventListeners.push({
        element,
        type: type2,
        handler
      });
    };
    var removeClickListeners = function() {
      eventListeners.forEach(function(_a4) {
        var element = _a4.element, type2 = _a4.type, handler = _a4.handler;
        element === null || element === void 0 ? void 0 : element.removeEventListener(type2, handler);
      });
      eventListeners = [];
    };
    var formInteractionsConfig;
    var name = "@amplitude/plugin-form-interaction-tracking-browser";
    var type = "enrichment";
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var initializeFormTracking, window_1;
        return __generator(this, function(_a4) {
          formInteractionsConfig = getFormInteractionsConfig(config2);
          initializeFormTracking = function() {
            if (!amplitude) {
              config2.loggerProvider.warn("Form interaction tracking requires a later version of @amplitude/analytics-browser. Form interaction events are not tracked.");
              return;
            }
            if (typeof document === "undefined") {
              return;
            }
            var addedFormNodes = /* @__PURE__ */ new WeakSet();
            var addFormInteractionListener = function(form) {
              if (addedFormNodes.has(form)) {
                return;
              }
              addedFormNodes.add(form);
              var hasFormChanged = false;
              addEventListener2(form, "change", function() {
                var _a5;
                var formDestination = extractFormAction(form);
                if (!hasFormChanged) {
                  amplitude.track(DEFAULT_FORM_START_EVENT, (_a5 = {}, _a5[FORM_ID] = stringOrUndefined(form.id), _a5[FORM_NAME] = stringOrUndefined(form.name), _a5[FORM_DESTINATION] = formDestination, _a5));
                }
                hasFormChanged = true;
              });
              addEventListener2(form, "submit", function(event) {
                var _a5, _b;
                var formDestination = extractFormAction(form);
                if (!hasFormChanged) {
                  amplitude.track(DEFAULT_FORM_START_EVENT, (_a5 = {}, _a5[FORM_ID] = stringOrUndefined(form.id), _a5[FORM_NAME] = stringOrUndefined(form.name), _a5[FORM_DESTINATION] = formDestination, _a5));
                }
                hasFormChanged = true;
                if ((formInteractionsConfig === null || formInteractionsConfig === void 0 ? void 0 : formInteractionsConfig.shouldTrackSubmit) !== void 0) {
                  if (typeof formInteractionsConfig.shouldTrackSubmit === "function" && typeof SubmitEvent !== "undefined" && event instanceof SubmitEvent) {
                    try {
                      var shouldTrack = formInteractionsConfig.shouldTrackSubmit(event);
                      if (!shouldTrack) {
                        return;
                      }
                    } catch (e2) {
                      config2.loggerProvider.warn("shouldTrackSubmit callback threw an error, proceeding with tracking.");
                    }
                  } else {
                    config2.loggerProvider.warn("shouldTrackSubmit is ignored because it is not a function or event is not a SubmitEvent.");
                  }
                }
                amplitude.track(DEFAULT_FORM_SUBMIT_EVENT, (_b = {}, _b[FORM_ID] = stringOrUndefined(form.id), _b[FORM_NAME] = stringOrUndefined(form.name), _b[FORM_DESTINATION] = formDestination, _b));
                hasFormChanged = false;
              });
            };
            var forms = Array.from(document.getElementsByTagName("form"));
            forms.forEach(addFormInteractionListener);
            if (typeof MutationObserver !== "undefined") {
              observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                  mutation.addedNodes.forEach(function(node3) {
                    try {
                      if (node3.nodeName === "FORM") {
                        addFormInteractionListener(node3);
                      }
                      if ("querySelectorAll" in node3 && typeof node3.querySelectorAll === "function") {
                        var queryableNode = node3;
                        var forms_1 = queryableNode.querySelectorAll("form");
                        forms_1 === null || forms_1 === void 0 ? void 0 : forms_1.forEach(addFormInteractionListener);
                      }
                    } catch (error) {
                      config2.loggerProvider.warn("Failed to track form interactions for an added node: ".concat(String(error)));
                    }
                  });
                });
              });
              observer.observe(document.body, {
                subtree: true,
                childList: true
              });
            }
          };
          if (document.readyState === "complete") {
            initializeFormTracking();
          } else {
            window_1 = getGlobalScope();
            if (window_1) {
              window_1.addEventListener("load", initializeFormTracking);
            } else {
              config2.loggerProvider.debug("Form interaction tracking is not installed because global is undefined.");
            }
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          observer === null || observer === void 0 ? void 0 : observer.disconnect();
          removeClickListeners();
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };
  var stringOrUndefined = function(name) {
    if (typeof name !== "string") {
      return void 0;
    }
    return name;
  };
  var extractFormAction = function(form) {
    var formDestination = form.getAttribute("action");
    try {
      formDestination = new URL(encodeURI(formDestination !== null && formDestination !== void 0 ? formDestination : ""), window.location.href).href;
    } catch (_a4) {
    }
    return formDestination;
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/plugins/file-download-tracking.js
  init_tslib_es6();
  init_esm();
  var fileDownloadTracking = function() {
    var observer;
    var eventListeners = [];
    var addEventListener2 = function(element, type2, handler) {
      element.addEventListener(type2, handler);
      eventListeners.push({
        element,
        type: type2,
        handler
      });
    };
    var removeClickListeners = function() {
      eventListeners.forEach(function(_a4) {
        var element = _a4.element, type2 = _a4.type, handler = _a4.handler;
        element === null || element === void 0 ? void 0 : element.removeEventListener(type2, handler);
      });
      eventListeners = [];
    };
    var name = "@amplitude/plugin-file-download-tracking-browser";
    var type = "enrichment";
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var initializeFileDownloadTracking, window_1;
        return __generator(this, function(_a4) {
          initializeFileDownloadTracking = function() {
            if (!amplitude) {
              config2.loggerProvider.warn("File download tracking requires a later version of @amplitude/analytics-browser. File download events are not tracked.");
              return;
            }
            if (typeof document === "undefined") {
              return;
            }
            var addFileDownloadListener = function(a2) {
              var url;
              try {
                url = new URL(a2.href, window.location.href);
              } catch (_a5) {
                return;
              }
              var result3 = ext.exec(url.href);
              var fileExtension = result3 === null || result3 === void 0 ? void 0 : result3[1];
              if (fileExtension) {
                addEventListener2(a2, "click", function() {
                  var _a5;
                  if (fileExtension) {
                    amplitude.track(DEFAULT_FILE_DOWNLOAD_EVENT, (_a5 = {}, _a5[FILE_EXTENSION] = fileExtension, _a5[FILE_NAME] = url.pathname, _a5[LINK_ID] = a2.id, _a5[LINK_TEXT] = a2.text, _a5[LINK_URL] = a2.href, _a5));
                  }
                });
              }
            };
            var ext = /\.(pdf|xlsx?|docx?|txt|rtf|csv|exe|key|pp(s|t|tx)|7z|pkg|rar|gz|zip|avi|mov|mp4|mpe?g|wmv|midi?|mp3|wav|wma)(\?.+)?$/;
            var links = Array.from(document.getElementsByTagName("a"));
            links.forEach(addFileDownloadListener);
            if (typeof MutationObserver !== "undefined") {
              observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                  mutation.addedNodes.forEach(function(node3) {
                    try {
                      if (node3.nodeName === "A") {
                        addFileDownloadListener(node3);
                      }
                      if ("querySelectorAll" in node3 && typeof node3.querySelectorAll === "function") {
                        var queryableNode = node3;
                        var links_1 = queryableNode.querySelectorAll("a");
                        links_1 === null || links_1 === void 0 ? void 0 : links_1.forEach(addFileDownloadListener);
                      }
                    } catch (error) {
                      config2.loggerProvider.warn("Failed to track file downloads for an added node: ".concat(String(error)));
                    }
                  });
                });
              });
              observer.observe(document.body, {
                subtree: true,
                childList: true
              });
            }
          };
          if (document.readyState === "complete") {
            initializeFileDownloadTracking();
          } else {
            window_1 = getGlobalScope();
            if (window_1) {
              window_1.addEventListener("load", initializeFileDownloadTracking);
            } else {
              config2.loggerProvider.debug("File download tracking is not installed because global is undefined.");
            }
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          observer === null || observer === void 0 ? void 0 : observer.disconnect();
          removeClickListeners();
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/det-notification.js
  var notified = false;
  var detNotify = function(config2) {
    if (notified || config2.defaultTracking !== void 0) {
      return;
    }
    var message = "`options.defaultTracking` is set to undefined. This implicitly configures your Amplitude instance to track Page Views, Sessions, File Downloads, and Form Interactions. You can suppress this warning by explicitly setting a value to `options.defaultTracking`. The value must either be a boolean, to enable and disable all default events, or an object, for advanced configuration. For example:\n\namplitude.init(<YOUR_API_KEY>, {\n  defaultTracking: true,\n});\n\nVisit https://www.docs.developers.amplitude.com/data/sdks/browser-2/#tracking-default-events for more details.";
    config2.loggerProvider.warn(message);
    notified = true;
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/plugins/network-connectivity-checker.js
  init_tslib_es6();
  init_esm();
  var networkConnectivityCheckerPlugin = function() {
    var name = "@amplitude/plugin-network-checker-browser";
    var type = "before";
    var globalScope3 = getGlobalScope();
    var eventListeners = [];
    var addNetworkListener = function(type2, handler) {
      if (globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.addEventListener) {
        globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.addEventListener(type2, handler);
        eventListeners.push({
          type: type2,
          handler
        });
      }
    };
    var removeNetworkListeners = function() {
      eventListeners.forEach(function(_a4) {
        var type2 = _a4.type, handler = _a4.handler;
        globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.removeEventListener(type2, handler);
      });
      eventListeners = [];
    };
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          if (typeof navigator === "undefined") {
            config2.loggerProvider.debug("Network connectivity checker plugin is disabled because navigator is not available.");
            config2.offline = false;
            return [
              2
              /*return*/
            ];
          }
          config2.offline = !navigator.onLine;
          addNetworkListener("online", function() {
            if (config2.offline === false) {
              return;
            }
            config2.loggerProvider.debug("Network connectivity changed to online.");
            config2.offline = false;
            setTimeout(function() {
              amplitude.flush();
            }, config2.flushIntervalMillis);
          });
          addNetworkListener("offline", function() {
            config2.loggerProvider.debug("Network connectivity changed to offline.");
            config2.offline = true;
          });
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          removeNetworkListeners();
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      teardown
    };
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/config/joined-config.js
  init_tslib_es6();
  init_esm();
  function updateBrowserConfigWithRemoteConfig(remoteConfig, browserConfig) {
    var _a4, _b;
    if (!remoteConfig) {
      return;
    }
    translateRemoteConfigToLocal(remoteConfig);
    try {
      browserConfig.loggerProvider.debug("Update browser config with remote configuration:", safeJsonStringify(remoteConfig));
      var typedRemoteConfig = remoteConfig;
      if (typedRemoteConfig && "autocapture" in typedRemoteConfig) {
        if (typeof typedRemoteConfig.autocapture === "boolean") {
          browserConfig.autocapture = typedRemoteConfig.autocapture;
        }
        if (typeof typedRemoteConfig.autocapture === "object" && typedRemoteConfig.autocapture !== null) {
          var transformedAutocaptureRemoteConfig = __assign({}, typedRemoteConfig.autocapture);
          if (browserConfig.autocapture === void 0) {
            browserConfig.autocapture = typedRemoteConfig.autocapture;
          }
          if (typeof typedRemoteConfig.autocapture.elementInteractions === "object" && typedRemoteConfig.autocapture.elementInteractions !== null && ((_a4 = typedRemoteConfig.autocapture.elementInteractions.pageUrlAllowlistRegex) === null || _a4 === void 0 ? void 0 : _a4.length)) {
            transformedAutocaptureRemoteConfig.elementInteractions = __assign({}, typedRemoteConfig.autocapture.elementInteractions);
            var transformedRcElementInteractions = transformedAutocaptureRemoteConfig.elementInteractions;
            var exactAllowList = (_b = transformedRcElementInteractions.pageUrlAllowlist) !== null && _b !== void 0 ? _b : [];
            var urlsRegex = typedRemoteConfig.autocapture.elementInteractions.pageUrlAllowlistRegex;
            transformedRcElementInteractions.pageUrlAllowlist = mergeUrls(exactAllowList, urlsRegex, browserConfig.loggerProvider);
            delete transformedRcElementInteractions.pageUrlAllowlistRegex;
          }
          var transformedNetworkTracking = transformNetworkTrackingRemoteConfig(typedRemoteConfig.autocapture.networkTracking, browserConfig.loggerProvider);
          if (transformedNetworkTracking) {
            transformedAutocaptureRemoteConfig.networkTracking = transformedNetworkTracking;
          }
          if (typeof browserConfig.autocapture === "boolean") {
            browserConfig.autocapture = __assign({ attribution: browserConfig.autocapture, fileDownloads: browserConfig.autocapture, formInteractions: browserConfig.autocapture, pageViews: browserConfig.autocapture, sessions: browserConfig.autocapture, elementInteractions: browserConfig.autocapture, webVitals: browserConfig.autocapture, frustrationInteractions: browserConfig.autocapture }, transformedAutocaptureRemoteConfig);
          }
          if (typeof browserConfig.autocapture === "object") {
            browserConfig.autocapture = __assign(__assign({}, browserConfig.autocapture), transformedAutocaptureRemoteConfig);
          }
        }
        browserConfig.defaultTracking = browserConfig.autocapture;
      }
      if ("customEnrichment" in typedRemoteConfig && typedRemoteConfig.customEnrichment !== null) {
        if (browserConfig.customEnrichment !== false) {
          browserConfig.customEnrichment = typedRemoteConfig.customEnrichment;
        }
      }
      browserConfig.loggerProvider.debug("Browser config after remote config update:", safeJsonStringify(browserConfig));
    } catch (e2) {
      browserConfig.loggerProvider.error("Failed to apply remote configuration because of error: ", e2);
    }
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture-plugin.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/version.js
  var VERSION2 = "1.29.1";

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/constants.js
  init_esm();
  var PLUGIN_NAME = "@amplitude/plugin-autocapture-browser";
  var FRUSTRATION_PLUGIN_NAME = "@amplitude/plugin-frustration-browser";
  var PERFORMANCE_PLUGIN_NAME = "@amplitude/plugin-performance-browser";
  var AMPLITUDE_ELEMENT_CLICKED_EVENT = "[Amplitude] Element Clicked";
  var AMPLITUDE_ELEMENT_DEAD_CLICKED_EVENT = "[Amplitude] Dead Click";
  var AMPLITUDE_ELEMENT_RAGE_CLICKED_EVENT = "[Amplitude] Rage Click";
  var AMPLITUDE_ELEMENT_ERROR_CLICKED_EVENT = "[Amplitude] Error Click";
  var AMPLITUDE_ELEMENT_CHANGED_EVENT = "[Amplitude] Element Changed";
  var AMPLITUDE_THRASHED_CURSOR_EVENT = "[Amplitude] Thrashed Cursor";
  var AMPLITUDE_MAIN_THREAD_BLOCK_EVENT = "[Amplitude] Main Thread Block";
  var AMPLITUDE_EVENT_PROP_ELEMENT_ID = "[Amplitude] Element ID";
  var AMPLITUDE_EVENT_PROP_ELEMENT_CLASS = "[Amplitude] Element Class";
  var AMPLITUDE_EVENT_PROP_ELEMENT_TAG = "[Amplitude] Element Tag";
  var AMPLITUDE_EVENT_PROP_ELEMENT_TEXT = "[Amplitude] Element Text";
  var AMPLITUDE_EVENT_PROP_ELEMENT_HIERARCHY = "[Amplitude] Element Hierarchy";
  var AMPLITUDE_EVENT_PROP_ELEMENT_HREF = "[Amplitude] Element Href";
  var AMPLITUDE_EVENT_PROP_ELEMENT_POSITION_LEFT = "[Amplitude] Element Position Left";
  var AMPLITUDE_EVENT_PROP_ELEMENT_POSITION_TOP = "[Amplitude] Element Position Top";
  var AMPLITUDE_EVENT_PROP_ELEMENT_ARIA_LABEL = "[Amplitude] Element Aria Label";
  var AMPLITUDE_EVENT_PROP_ELEMENT_ATTRIBUTES = "[Amplitude] Element Attributes";
  var AMPLITUDE_EVENT_PROP_ELEMENT_PATH = "[Amplitude] Element Path";
  var AMPLITUDE_EVENT_PROP_ELEMENT_PARENT_LABEL = "[Amplitude] Element Parent Label";
  var AMPLITUDE_EVENT_PROP_PAGE_URL = "[Amplitude] Page URL";
  var AMPLITUDE_EVENT_PROP_PAGE_TITLE = "[Amplitude] Page Title";
  var AMPLITUDE_EVENT_PROP_VIEWPORT_HEIGHT = "[Amplitude] Viewport Height";
  var AMPLITUDE_EVENT_PROP_VIEWPORT_WIDTH = "[Amplitude] Viewport Width";
  var AMPLITUDE_EVENT_PROP_MAX_PAGE_X = "[Amplitude] Max Page X";
  var AMPLITUDE_EVENT_PROP_MAX_PAGE_Y = "[Amplitude] Max Page Y";
  var AMPLITUDE_EVENT_PROP_PAGE_VIEW_ID = "[Amplitude] Page View ID";
  var AMPLITUDE_VISUAL_TAGGING_SELECTOR_SCRIPT_URL = "https://cdn.amplitude.com/libs/visual-tagging-selector-1.0.0-alpha.js.gz";
  var AMPLITUDE_VISUAL_TAGGING_HIGHLIGHT_CLASS = "amp-visual-tagging-selector-highlight";
  var DATA_AMP_MASK_ATTRIBUTES = "data-amp-mask-attributes";
  var DATA_AMP_INTERNAL_SHADOW = "data-amp-internal-shadow";
  var MAX_MASK_TEXT_PATTERNS = 25;
  var MAX_ATTRIBUTE_LENGTH = 128;
  var PAGE_VIEW_SESSION_STORAGE_KEY2 = "AMP_PAGE_VIEW";
  var MAX_ELEMENT_EXPOSED_STR_LENGTH = 18e3;
  var EXPOSURE_VIEWED_THRESHOLD = 0.5;
  var EXPOSURE_SNAPSHOT_QUIET_MS = 150;
  var EXPOSURE_SNAPSHOT_MAX_WAIT_MS = 4e3;
  var EXPOSURE_SNAPSHOT_FLUSH_BUFFER_MS = 50;

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/helpers.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/element-selector/lib/esm/patterns/autogenerated-ids.js
  init_tslib_es6();
  var DEFAULT_AUTOGENERATED_ID_PATTERNS = [
    // React useId() — ":r0:", ":r1:", ":rk:". Stable for one render; new id on next mount.
    /^:r[0-9a-z]+:$/,
    // Radix UI primitives — "radix-:r3:", "radix-A1B2C3". Prefix match (the
    // shape of what comes after `radix-` varies by Radix version and React
    // major; only the prefix is reliable).
    /^radix-/,
    // Headless UI (Tailwind Labs) — "headlessui-menu-button-:r5:". Prefix match.
    /^headlessui-/,
    // MUI internal id prefix — "mui-12345". Don't confuse with Mui-* CSS class state markers.
    /^mui-/,
    // Hex-only ids >=16 chars — UUIDs without hyphens, build hashes. Whole-string match.
    /^[a-f0-9]{16,}$/i,
    // Trailing long digit run — "session-1700000000", "row-20250515". Timestamps, counters.
    /-\d{8,}$/,
    // Any 4+ consecutive digits anywhere — "product-2134", "swiper-wrapper-…41039".
    // ContentSquare's heuristic; catches the digit-heavy portions of library-generated ids.
    /\d{4,}/
  ];
  function compile(patterns, logger) {
    var e_1, _a4;
    var compiled = [];
    try {
      for (var patterns_1 = __values(patterns), patterns_1_1 = patterns_1.next(); !patterns_1_1.done; patterns_1_1 = patterns_1.next()) {
        var pattern = patterns_1_1.value;
        try {
          compiled.push(new RegExp(pattern));
        } catch (e2) {
          var message = e2 instanceof Error ? e2.message : String(e2);
          logger === null || logger === void 0 ? void 0 : logger.warn('@amplitude/element-selector: ignoring invalid autogenerated-id pattern "'.concat(pattern, '" (').concat(message, ")"));
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (patterns_1_1 && !patterns_1_1.done && (_a4 = patterns_1.return)) _a4.call(patterns_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return compiled;
  }
  function isStableId(id2, patterns) {
    var e_2, _a4;
    if (id2 === null || id2 === void 0 || id2 === "")
      return false;
    try {
      for (var patterns_2 = __values(patterns), patterns_2_1 = patterns_2.next(); !patterns_2_1.done; patterns_2_1 = patterns_2.next()) {
        var pattern = patterns_2_1.value;
        if (pattern.test(id2))
          return false;
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (patterns_2_1 && !patterns_2_1.done && (_a4 = patterns_2.return)) _a4.call(patterns_2);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    return true;
  }

  // node_modules/@amplitude/element-selector/lib/esm/patterns/unstable-classes.js
  init_tslib_es6();
  var DEFAULT_UNSTABLE_CLASS_PATTERNS = [
    // ===== Tailwind / build-tool utilities =====
    // Tailwind spacing: p-4, px-2, py-8, pt-1, mt-4, etc.
    /^(p|m|px|py|mx|my|pt|pb|pl|pr|mt|mb|ml|mr)-\d+$/,
    // Tailwind sizing: w-full, h-screen, max-w-[1440px], etc.
    /^(w|h|min-w|max-w|min-h|max-h)-/,
    // Tailwind color / visual: bg-blue-500, text-white, border-gray-200, ring-2, etc.
    /^(text|bg|border|ring|fill|stroke)-/,
    // Tailwind state variants: hover:underline, focus:ring-2, active:bg-transparent.
    /^(hover|focus|active|disabled|group-hover):/,
    // Tailwind breakpoint variants: md:flex, lg:grid-cols-3.
    /^(sm|md|lg|xl|2xl):/,
    // Tailwind z-index utility: z-10, z-50.
    /^z-\d+$/,
    // Tailwind arbitrary data-attribute variants: data-[state=open]:bg-white.
    /^data-\[/,
    // Tailwind arbitrary selector variants: [&_.swiper-slide]:h-auto, [&>div]:p-4.
    /^\[/,
    // ===== CSS-in-JS / build hashes =====
    // Emotion: css-1abcd23, css-9xyzkw0.
    /^css-[a-z0-9]{6,}$/,
    // CSS modules: Button_root__abc123, Card_container__xyz789. The middle
    // segment can be as short as `root` (4 chars) in practice; the trailing
    // hash is the load-bearing part.
    /^[a-zA-Z]+_[a-zA-Z0-9]{3,}__[a-zA-Z0-9]{5,}$/,
    // styled-components: sc-bdVaJa, sc-1jjuPXC0.
    /^sc-[a-zA-Z0-9]{6,}$/,
    // styled-jsx (Next.js): jsx-1234567.
    /^jsx-\d+$/,
    // ===== Library runtime state classes =====
    // Swiper carousel slide states. Move between elements as carousel advances.
    /^swiper-slide-(visible|fully-visible|active|prev|next|duplicate)$/,
    // BEM-style interaction state: is-active, is-open, is-selected, etc.
    /^is-(active|open|selected|hovered|focused|expanded)$/,
    // MUI per-component state: MuiButton-focusVisible, MuiSwitch-checked, etc.
    /^Mui[A-Z][a-zA-Z]+-(focused|selected|disabled|expanded|focusVisible|active|checked)$/,
    // MUI bare state classes: Mui-selected, Mui-disabled.
    /^Mui-(selected|focused|disabled|expanded|focusVisible|active|checked)$/,
    // Radix-style state class mirrors: data-state-open, data-state-checked.
    /^data-state-/
  ];
  function compile2(patterns, logger) {
    var e_1, _a4;
    var compiled = [];
    try {
      for (var patterns_1 = __values(patterns), patterns_1_1 = patterns_1.next(); !patterns_1_1.done; patterns_1_1 = patterns_1.next()) {
        var pattern = patterns_1_1.value;
        try {
          compiled.push(new RegExp(pattern));
        } catch (e2) {
          var message = e2 instanceof Error ? e2.message : String(e2);
          logger === null || logger === void 0 ? void 0 : logger.warn('@amplitude/element-selector: ignoring invalid unstable-class pattern "'.concat(pattern, '" (').concat(message, ")"));
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (patterns_1_1 && !patterns_1_1.done && (_a4 = patterns_1.return)) _a4.call(patterns_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return compiled;
  }

  // node_modules/@amplitude/element-selector/lib/esm/helpers/get-stable-id.js
  function getStableId(el, cfg2) {
    var trackAttr = el.getAttribute(cfg2.explicitTrackingAttribute);
    if (trackAttr !== null && trackAttr === "") {
      return null;
    }
    var id2 = el.getAttribute("id");
    if (id2 === null || id2 === "") {
      return null;
    }
    if (!isStableId(id2, cfg2.autogeneratedIdPatterns)) {
      return null;
    }
    return id2;
  }

  // node_modules/@amplitude/element-selector/lib/esm/helpers/describe-relative.js
  function describeRelative(_anchor, trail) {
    return trail.map(stepFor).join(" > ");
  }
  function stepFor(el) {
    var tag = el.tagName.toLowerCase();
    var parent = el.parentElement;
    if (parent === null) {
      return tag;
    }
    var index4 = sameTypeIndex(el, parent);
    return "".concat(tag, ":nth-of-type(").concat(index4, ")");
  }
  function sameTypeIndex(el, parent) {
    var count = 0;
    for (var i2 = 0; i2 < parent.children.length; i2++) {
      var sibling = parent.children[i2];
      if (sibling.tagName === el.tagName) {
        count += 1;
        if (sibling === el)
          return count;
      }
    }
    return 1;
  }

  // node_modules/@amplitude/element-selector/lib/esm/helpers/shadow.js
  init_tslib_es6();
  var SHADOW_BOUNDARY_DELIMITER = " >>> ";
  var SHADOW_CHILD_CHAIN_PREFIX = ":scope > ";
  function isShadowRoot(node3) {
    return typeof ShadowRoot !== "undefined" && node3 instanceof ShadowRoot;
  }
  function anchorSegmentToShadowScope(segment, scope, isRootAnchored) {
    if (segment && isRootAnchored && isShadowRoot(scope)) {
      return "".concat(SHADOW_CHILD_CHAIN_PREFIX).concat(segment);
    }
    return segment;
  }
  function legacyPathIsRootAnchored(el) {
    for (var node3 = el; node3 !== null; node3 = node3.parentElement) {
      if (node3.getAttribute("id")) {
        return false;
      }
    }
    return true;
  }
  function rootOf(el) {
    var _a4;
    var root3 = el.getRootNode();
    if (isShadowRoot(root3)) {
      return root3;
    }
    return (_a4 = el.ownerDocument) !== null && _a4 !== void 0 ? _a4 : document;
  }
  function composedParent(el) {
    if (el.parentElement) {
      return el.parentElement;
    }
    var root3 = el.getRootNode();
    if (isShadowRoot(root3)) {
      return root3.host;
    }
    return null;
  }
  var MAX_SHADOW_COMPOSED_WALK_ITERATIONS = 1024;
  var MAX_SHADOW_DOM_TRAVERSAL_NODES = 5e4;
  function walkComposedAncestors(start3, maxShadowDepth) {
    var node3, crossings, iterations, host3;
    return __generator(this, function(_a4) {
      switch (_a4.label) {
        case 0:
          node3 = start3;
          crossings = 0;
          iterations = 0;
          _a4.label = 1;
        case 1:
          if (!(node3 !== null && iterations < MAX_SHADOW_COMPOSED_WALK_ITERATIONS)) return [3, 3];
          iterations += 1;
          return [4, node3];
        case 2:
          _a4.sent();
          if (node3.parentElement) {
            node3 = node3.parentElement;
            return [3, 1];
          }
          if (crossings < maxShadowDepth && typeof node3.getRootNode === "function") {
            host3 = composedParent(node3);
            if (host3 && host3 !== node3) {
              crossings += 1;
              node3 = host3;
              return [3, 1];
            }
          }
          return [3, 3];
        case 3:
          return [
            2
            /*return*/
          ];
      }
    });
  }
  function collectOpenShadowRoots(root3, maxShadowDepth) {
    var found = [];
    if (maxShadowDepth <= 0) {
      return found;
    }
    var stack = [[root3, 0]];
    var visited = 0;
    while (stack.length && visited < MAX_SHADOW_DOM_TRAVERSAL_NODES) {
      visited += 1;
      var _a4 = __read(stack.pop(), 2), el = _a4[0], depth = _a4[1];
      var shadowRoot3 = el.shadowRoot;
      if (shadowRoot3 && depth + 1 <= maxShadowDepth) {
        found.push({ root: shadowRoot3, depth: depth + 1 });
        var shadowChildren = shadowRoot3.children;
        for (var i2 = 0; i2 < shadowChildren.length; i2++) {
          stack.push([shadowChildren[i2], depth + 1]);
        }
      }
      var children = el.children;
      for (var i2 = 0; i2 < children.length; i2++) {
        stack.push([children[i2], depth]);
      }
    }
    return found;
  }
  function segmentWalk(el, maxShadowDepth) {
    var innermostFirst = [];
    var node3 = el;
    var iterations = 0;
    while (node3 !== null && iterations < MAX_SHADOW_COMPOSED_WALK_ITERATIONS) {
      iterations += 1;
      var root3 = rootOf(node3);
      innermostFirst.push({ root: root3, target: node3 });
      var actualRoot = node3.getRootNode();
      if (!isShadowRoot(actualRoot)) {
        break;
      }
      node3 = actualRoot.host;
    }
    var totalCrossings = innermostFirst.length - 1;
    var truncated = false;
    var kept = innermostFirst;
    if (totalCrossings > maxShadowDepth) {
      kept = innermostFirst.slice(totalCrossings - maxShadowDepth);
      truncated = true;
    }
    return { segments: kept.reverse(), truncated };
  }
  function positionalStep(el, shadowAware) {
    var tag = el.tagName.toLowerCase();
    var parent = el.parentElement;
    if (parent === null) {
      var root3 = el.getRootNode();
      if (shadowAware && isShadowRoot(root3)) {
        return "".concat(tag, ":nth-of-type(").concat(sameTypeIndexAmong(el, root3.children), ")");
      }
      return tag;
    }
    return "".concat(tag, ":nth-of-type(").concat(sameTypeIndexAmong(el, parent.children), ")");
  }
  function sameTypeIndexAmong(el, siblings) {
    var count = 0;
    for (var i2 = 0; i2 < siblings.length; i2++) {
      var sibling = siblings[i2];
      if (sibling.tagName === el.tagName) {
        count += 1;
        if (sibling === el) {
          return count;
        }
      }
    }
    return 1;
  }

  // node_modules/@amplitude/element-selector/lib/esm/strategies/explicit-tracking-attribute.js
  var explicitTrackingAttribute = {
    name: "explicitTrackingAttribute",
    try: function(el, ctx) {
      var attrName = ctx.config.explicitTrackingAttribute;
      var value = el.getAttribute(attrName);
      if (value === null || value === "") {
        return null;
      }
      return "[".concat(attrName, "=").concat(JSON.stringify(value), "]");
    }
  };

  // node_modules/@amplitude/element-selector/lib/esm/helpers/escape-css-identifier.js
  function escapeCssIdentifier(value) {
    var css = globalThis.CSS;
    if (css && typeof css.escape === "function") {
      return css.escape(value);
    }
    var string = String(value);
    var length = string.length;
    var result3 = "";
    for (var index4 = 0; index4 < length; index4++) {
      var codeUnit = string.charCodeAt(index4);
      if (codeUnit === 0) {
        result3 += "\uFFFD";
        continue;
      }
      if (codeUnit >= 1 && codeUnit <= 31 || codeUnit === 127 || index4 === 0 && codeUnit >= 48 && codeUnit <= 57 || index4 === 1 && codeUnit >= 48 && codeUnit <= 57 && string.charCodeAt(0) === 45) {
        result3 += "\\".concat(codeUnit.toString(16), " ");
        continue;
      }
      if (index4 === 0 && length === 1 && codeUnit === 45) {
        result3 += "\\-";
        continue;
      }
      if (codeUnit >= 128 || codeUnit === 45 || codeUnit === 95 || codeUnit >= 48 && codeUnit <= 57 || codeUnit >= 65 && codeUnit <= 90 || codeUnit >= 97 && codeUnit <= 122) {
        result3 += string.charAt(index4);
        continue;
      }
      result3 += "\\".concat(string.charAt(index4));
    }
    return result3;
  }

  // node_modules/@amplitude/element-selector/lib/esm/helpers/escape-id.js
  function escapeIdForCss(id2) {
    return escapeCssIdentifier(id2);
  }

  // node_modules/@amplitude/element-selector/lib/esm/strategies/stable-id.js
  var stableId = {
    name: "stableId",
    try: function(el, ctx) {
      var id2 = getStableId(el, ctx.config);
      if (id2 === null) {
        return null;
      }
      return "".concat(el.tagName.toLowerCase(), "#").concat(escapeIdForCss(id2));
    }
  };

  // node_modules/@amplitude/element-selector/lib/esm/orchestrator.js
  init_tslib_es6();

  // node_modules/@amplitude/element-selector/lib/esm/config/resolve-config.js
  init_tslib_es6();
  var DEFAULT_SHADOW_DOM_ENABLED = false;
  var DEFAULT_MAX_SHADOW_DOM_DEPTH = 1;
  var MAX_SHADOW_DOM_DEPTH = 10;
  var DEFAULT_RESOLVED_CONFIG = Object.freeze({
    // v1 ships disabled — autocapture only flips to the new engine when the
    // remote config explicitly enables it for an org.
    enabled: false,
    explicitTrackingAttribute: "data-amp-track-id",
    autogeneratedIdPatterns: __spreadArray([], __read(DEFAULT_AUTOGENERATED_ID_PATTERNS), false),
    unstableClassPatterns: __spreadArray([], __read(DEFAULT_UNSTABLE_CLASS_PATTERNS), false),
    maxAncestorWalkDepth: void 0,
    shadowDomEnabled: DEFAULT_SHADOW_DOM_ENABLED,
    maxShadowDomDepth: DEFAULT_MAX_SHADOW_DOM_DEPTH
  });
  function normalizeResolvedConfig(config2) {
    var _a4;
    return __assign(__assign({}, config2), { shadowDomEnabled: (_a4 = config2.shadowDomEnabled) !== null && _a4 !== void 0 ? _a4 : DEFAULT_SHADOW_DOM_ENABLED, maxShadowDomDepth: clampShadowDepth(config2.maxShadowDomDepth) });
  }
  function resolveSelectorConfig(remote, logger) {
    if (!remote) {
      return cloneDefaults();
    }
    var resolved = cloneDefaults();
    if (typeof remote.enabled === "boolean") {
      resolved.enabled = remote.enabled;
    }
    if (typeof remote.explicitTrackingAttribute === "string" && remote.explicitTrackingAttribute !== "") {
      resolved.explicitTrackingAttribute = remote.explicitTrackingAttribute;
    }
    if (Array.isArray(remote.autogeneratedIdPatterns)) {
      resolved.autogeneratedIdPatterns = compile(remote.autogeneratedIdPatterns, logger);
    }
    if (Array.isArray(remote.unstableClassPatterns)) {
      resolved.unstableClassPatterns = compile2(remote.unstableClassPatterns, logger);
    }
    resolved.maxAncestorWalkDepth = clampDepth(remote.maxAncestorWalkDepth);
    if (typeof remote.shadowDomEnabled === "boolean") {
      resolved.shadowDomEnabled = remote.shadowDomEnabled;
    }
    resolved.maxShadowDomDepth = clampShadowDepth(remote.maxShadowDomDepth);
    return normalizeResolvedConfig(resolved);
  }
  function clampDepth(depth) {
    if (typeof depth !== "number")
      return void 0;
    if (!Number.isFinite(depth))
      return void 0;
    if (depth <= 0)
      return void 0;
    return Math.floor(depth);
  }
  function clampShadowDepth(depth) {
    if (typeof depth !== "number" || !Number.isFinite(depth)) {
      return DEFAULT_MAX_SHADOW_DOM_DEPTH;
    }
    return Math.min(Math.max(Math.floor(depth), 1), MAX_SHADOW_DOM_DEPTH);
  }
  function cloneDefaults() {
    return {
      enabled: DEFAULT_RESOLVED_CONFIG.enabled,
      explicitTrackingAttribute: DEFAULT_RESOLVED_CONFIG.explicitTrackingAttribute,
      autogeneratedIdPatterns: __spreadArray([], __read(DEFAULT_RESOLVED_CONFIG.autogeneratedIdPatterns), false),
      unstableClassPatterns: __spreadArray([], __read(DEFAULT_RESOLVED_CONFIG.unstableClassPatterns), false),
      maxAncestorWalkDepth: DEFAULT_RESOLVED_CONFIG.maxAncestorWalkDepth,
      shadowDomEnabled: DEFAULT_RESOLVED_CONFIG.shadowDomEnabled,
      maxShadowDomDepth: DEFAULT_RESOLVED_CONFIG.maxShadowDomDepth
    };
  }

  // node_modules/@amplitude/element-selector/lib/esm/orchestrator.js
  var DEFAULT_STRATEGIES = [explicitTrackingAttribute, stableId];
  function runOrchestrator(el, config2, options) {
    var e_1, _a4;
    var _b, _c, _d;
    if (options === void 0) {
      options = {};
    }
    var resolved = normalizeResolvedConfig(config2);
    var strategies = (_b = options.strategies) !== null && _b !== void 0 ? _b : DEFAULT_STRATEGIES;
    var scope = (_d = (_c = options.scope) !== null && _c !== void 0 ? _c : el.ownerDocument) !== null && _d !== void 0 ? _d : document;
    var ctx = { scope, config: resolved };
    var walk = collectWalk(el, resolved.maxAncestorWalkDepth);
    try {
      for (var strategies_1 = __values(strategies), strategies_1_1 = strategies_1.next(); !strategies_1_1.done; strategies_1_1 = strategies_1.next()) {
        var strategy = strategies_1_1.value;
        for (var i2 = 0; i2 < walk.length; i2++) {
          var anchor = walk[i2];
          var anchorSelector = strategy.try(anchor, ctx);
          if (anchorSelector === null)
            continue;
          if (i2 === 0) {
            if (isUniqueMatch(scope, anchorSelector, el, strategy.name, options.logger)) {
              return anchorSelector;
            }
            continue;
          }
          var trail = walk.slice(0, i2).reverse();
          var descent = describeRelative(anchor, trail);
          var composed = "".concat(anchorSelector, " > ").concat(descent);
          if (isUniqueMatch(scope, composed, el, strategy.name, options.logger)) {
            return composed;
          }
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (strategies_1_1 && !strategies_1_1.done && (_a4 = strategies_1.return)) _a4.call(strategies_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return null;
  }
  function collectWalk(el, maxDepth) {
    var walk = [];
    var cursor = el;
    while (cursor !== null) {
      walk.push(cursor);
      if (maxDepth !== void 0 && walk.length > maxDepth)
        break;
      cursor = cursor.parentElement;
    }
    return walk;
  }
  function isUniqueMatch(scope, selector2, el, strategyName, logger) {
    var matches;
    try {
      matches = scope.querySelectorAll(selector2);
    } catch (e2) {
      var message = e2 instanceof Error ? e2.message : String(e2);
      logger === null || logger === void 0 ? void 0 : logger.debug('@amplitude/element-selector: strategy "'.concat(strategyName, '" emitted a malformed selector "').concat(selector2, '" (').concat(message, ")"));
      return false;
    }
    return matches.length === 1 && matches[0] === el;
  }

  // node_modules/@amplitude/element-selector/lib/esm/fallback-css-path.js
  init_tslib_es6();
  function fallbackCssPath(el, config2, options) {
    var _a4, _b;
    if (options === void 0) {
      options = {};
    }
    var resolved = normalizeResolvedConfig(config2);
    var scope = (_b = (_a4 = options.scope) !== null && _a4 !== void 0 ? _a4 : el.ownerDocument) !== null && _b !== void 0 ? _b : document;
    var scopeIsShadowRoot = isShadowRoot(scope);
    var segments = [];
    var cursor = el;
    var depth = 0;
    var honorDepthCap = !(resolved.shadowDomEnabled && scopeIsShadowRoot);
    while (cursor !== null) {
      if (honorDepthCap && resolved.maxAncestorWalkDepth !== void 0 && depth > resolved.maxAncestorWalkDepth) {
        break;
      }
      var id2 = getStableId(cursor, resolved);
      if (id2 !== null) {
        var anchorSegment = "".concat(cursor.tagName.toLowerCase(), "#").concat(escapeIdForCss(id2));
        var candidate = __spreadArray([anchorSegment], __read(segments), false).join(" > ");
        if (isUniqueMatch2(scope, candidate, el)) {
          return candidate;
        }
      }
      segments.unshift(positionalStep(cursor, resolved.shadowDomEnabled));
      cursor = cursor.parentElement;
      depth += 1;
    }
    var path = segments.join(" > ");
    if (resolved.shadowDomEnabled && scopeIsShadowRoot && cursor === null) {
      return "".concat(SHADOW_CHILD_CHAIN_PREFIX).concat(path);
    }
    return path;
  }
  function isUniqueMatch2(scope, selector2, el) {
    var matches;
    try {
      matches = scope.querySelectorAll(selector2);
    } catch (_e) {
      return false;
    }
    return matches.length === 1 && matches[0] === el;
  }

  // node_modules/@amplitude/element-selector/lib/esm/legacy-css-path.js
  init_tslib_es6();
  var Step = (
    /** @class */
    (function() {
      function Step2(value, optimized) {
        this.value = value;
        this.optimized = optimized;
      }
      Step2.prototype.toString = function() {
        return this.value;
      };
      return Step2;
    })()
  );
  var legacyCssPath = function(node3, optimized) {
    if (node3.nodeType !== Node.ELEMENT_NODE) {
      return "";
    }
    var steps = [];
    var contextNode = node3;
    while (contextNode) {
      var step = cssPathStep(contextNode, Boolean(optimized), contextNode === node3);
      if (!step) {
        break;
      }
      steps.push(step);
      if (step.optimized) {
        break;
      }
      contextNode = contextNode.parentElement;
    }
    steps.reverse();
    return steps.join(" > ");
  };
  var cssPathStep = function(node3, optimized, isTargetNode) {
    var e_1, _a4;
    if (node3.nodeType !== Node.ELEMENT_NODE) {
      return null;
    }
    var id2 = node3.getAttribute("id");
    if (optimized) {
      if (id2) {
        return new Step(idSelector(id2), true);
      }
      var nodeNameLower = node3.tagName.toLowerCase();
      if (nodeNameLower === "body" || nodeNameLower === "head" || nodeNameLower === "html") {
        return new Step(nodeNameLower, true);
      }
    }
    var nodeName = node3.tagName.toLowerCase();
    if (id2) {
      return new Step(nodeName + idSelector(id2), true);
    }
    var parent = node3.parentNode;
    if (!parent || parent.nodeType === Node.DOCUMENT_NODE) {
      return new Step(nodeName, true);
    }
    var prefixedOwnClassNamesArray = prefixedElementClassNames(node3);
    var needsClassNames = false;
    var needsNthChild = false;
    var ownIndex = -1;
    var elementIndex = -1;
    var siblings = parent.children;
    for (var i2 = 0; siblings && (ownIndex === -1 || !needsNthChild) && i2 < siblings.length; ++i2) {
      var sibling = siblings[i2];
      if (sibling.nodeType !== Node.ELEMENT_NODE) {
        continue;
      }
      elementIndex += 1;
      if (sibling === node3) {
        ownIndex = elementIndex;
        continue;
      }
      if (needsNthChild) {
        continue;
      }
      if (sibling.tagName.toLowerCase() !== nodeName) {
        continue;
      }
      needsClassNames = true;
      var ownClassNames = new Set(prefixedOwnClassNamesArray);
      if (!ownClassNames.size) {
        needsNthChild = true;
        continue;
      }
      var siblingClassNamesArray = prefixedElementClassNames(sibling);
      for (var j = 0; j < siblingClassNamesArray.length; ++j) {
        var siblingClass = siblingClassNamesArray[j];
        if (!ownClassNames.has(siblingClass)) {
          continue;
        }
        ownClassNames.delete(siblingClass);
        if (!ownClassNames.size) {
          needsNthChild = true;
          break;
        }
      }
    }
    var result3 = nodeName;
    if (isTargetNode && nodeName.toLowerCase() === "input" && node3.getAttribute("type") && !node3.getAttribute("id") && !node3.getAttribute("class")) {
      result3 += "[type=" + escapeCssIdentifier(node3.getAttribute("type") || "") + "]";
    }
    if (needsNthChild) {
      result3 += ":nth-child(" + String(ownIndex + 1) + ")";
    } else if (needsClassNames) {
      try {
        for (var prefixedOwnClassNamesArray_1 = __values(prefixedOwnClassNamesArray), prefixedOwnClassNamesArray_1_1 = prefixedOwnClassNamesArray_1.next(); !prefixedOwnClassNamesArray_1_1.done; prefixedOwnClassNamesArray_1_1 = prefixedOwnClassNamesArray_1.next()) {
          var prefixedName = prefixedOwnClassNamesArray_1_1.value;
          result3 += "." + escapeCssIdentifier(prefixedName.slice(1));
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (prefixedOwnClassNamesArray_1_1 && !prefixedOwnClassNamesArray_1_1.done && (_a4 = prefixedOwnClassNamesArray_1.return)) _a4.call(prefixedOwnClassNamesArray_1);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    }
    return new Step(result3, false);
  };
  var prefixedElementClassNames = function(el) {
    var classAttribute = el.getAttribute("class");
    if (!classAttribute) {
      return [];
    }
    return classAttribute.split(/\s+/g).filter(Boolean).map(function(name) {
      return "$" + name;
    });
  };
  var idSelector = function(id2) {
    return "#" + escapeCssIdentifier(id2);
  };
  function safeLegacyCssPath(el, logger) {
    try {
      return legacyCssPath(el);
    } catch (e2) {
      var message = e2 instanceof Error ? e2.message : String(e2);
      logger === null || logger === void 0 ? void 0 : logger.warn("@amplitude/element-selector: legacyCssPath threw \u2014 emitting empty selector: ".concat(message));
      return "";
    }
  }

  // node_modules/@amplitude/element-selector/lib/esm/engine.js
  init_tslib_es6();
  function createSelectorEngine(initialConfig, options) {
    if (options === void 0) {
      options = {};
    }
    var config2 = normalizeResolvedConfig(initialConfig);
    var subscribers = /* @__PURE__ */ new Set();
    var logger = options.logger;
    var legacyTree = function(target) {
      return safeLegacyCssPath(target, logger);
    };
    var engineTree = function(target, scope) {
      var _a4;
      return (_a4 = runOrchestrator(target, config2, { strategies: options.strategies, scope, logger })) !== null && _a4 !== void 0 ? _a4 : fallbackCssPath(target, config2, { scope });
    };
    var legacyTreeInShadow = function(target, scope) {
      return anchorSegmentToShadowScope(legacyTree(target, scope), scope, legacyPathIsRootAnchored(target));
    };
    function pierce(el, perTree) {
      var _a4 = segmentWalk(el, config2.maxShadowDomDepth), segments = _a4.segments, truncated = _a4.truncated;
      if (truncated) {
        logger === null || logger === void 0 ? void 0 : logger.debug("@amplitude/element-selector: target is nested deeper than maxShadowDomDepth (".concat(config2.maxShadowDomDepth, ") \u2014 emitting a best-effort selector for the outermost in-budget shadow host"));
      }
      return segments.map(function(_a5) {
        var target = _a5.target, root3 = _a5.root;
        return perTree(target, root3);
      }).join(SHADOW_BOUNDARY_DELIMITER);
    }
    return {
      generate: function(el) {
        var _a4, _b;
        try {
          if (!config2.shadowDomEnabled) {
            var perTree = config2.enabled ? engineTree : legacyTree;
            return perTree(el, (_b = (_a4 = options.scope) !== null && _a4 !== void 0 ? _a4 : el.ownerDocument) !== null && _b !== void 0 ? _b : document);
          }
          return pierce(el, config2.enabled ? engineTree : legacyTreeInShadow);
        } catch (e2) {
          var message = e2 instanceof Error ? e2.message : String(e2);
          logger === null || logger === void 0 ? void 0 : logger.warn("@amplitude/element-selector: selector generation threw \u2014 falling back to legacy cssPath: ".concat(message));
          return safeLegacyCssPath(el, logger);
        }
      },
      getConfig: function() {
        return config2;
      },
      updateConfig: function(next) {
        var e_1, _a4;
        config2 = normalizeResolvedConfig(next);
        try {
          for (var subscribers_1 = __values(subscribers), subscribers_1_1 = subscribers_1.next(); !subscribers_1_1.done; subscribers_1_1 = subscribers_1.next()) {
            var cb = subscribers_1_1.value;
            try {
              cb(config2);
            } catch (e2) {
              var message = e2 instanceof Error ? e2.message : String(e2);
              logger === null || logger === void 0 ? void 0 : logger.warn("@amplitude/element-selector: onConfigChange subscriber threw \u2014 ".concat(message));
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (subscribers_1_1 && !subscribers_1_1.done && (_a4 = subscribers_1.return)) _a4.call(subscribers_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
      },
      onConfigChange: function(cb) {
        subscribers.add(cb);
        return function() {
          subscribers.delete(cb);
        };
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/shadow-mode.js
  init_tslib_es6();
  var DEFAULT_MAX_SHADOW_DOM_DEPTH2 = 1;
  var SHADOW_OFF = Object.freeze({ enabled: false, maxDepth: 0 });
  var shadowModeFromConfig = function(config2) {
    var _a4;
    var maxDepth = (_a4 = config2.maxShadowDomDepth) !== null && _a4 !== void 0 ? _a4 : DEFAULT_MAX_SHADOW_DOM_DEPTH2;
    if (!config2.shadowDomEnabled || maxDepth <= 0) {
      return SHADOW_OFF;
    }
    return Object.freeze({ enabled: true, maxDepth });
  };
  var createShadowGate = function() {
    var mode = SHADOW_OFF;
    var listeners = /* @__PURE__ */ new Set();
    var notify = function(armedMode) {
      var e_1, _a4;
      try {
        for (var _b = __values(__spreadArray([], __read(listeners), false)), _c = _b.next(); !_c.done; _c = _b.next()) {
          var cb = _c.value;
          try {
            cb(armedMode);
          } catch (_d) {
          }
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
      listeners.clear();
    };
    return {
      get: function() {
        return mode;
      },
      arm: function(next) {
        if (mode.enabled || !next.enabled) {
          return mode;
        }
        mode = next;
        notify(next);
        return mode;
      },
      onArm: function(cb) {
        if (mode.enabled) {
          var armedMode = mode;
          try {
            cb(armedMode);
          } catch (_a4) {
          }
          return function() {
            return void 0;
          };
        }
        listeners.add(cb);
        return function() {
          listeners.delete(cb);
        };
      }
    };
  };
  var sharedShadowGate;
  var getSharedShadowGate = function() {
    if (!sharedShadowGate) {
      sharedShadowGate = createShadowGate();
    }
    return sharedShadowGate;
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/helpers.js
  var SENSITIVE_TAGS = ["input", "select", "textarea"];
  var isElementPointerCursor = function(element, actionType) {
    var _a4;
    var computedStyle = (_a4 = window === null || window === void 0 ? void 0 : window.getComputedStyle) === null || _a4 === void 0 ? void 0 : _a4.call(window, element);
    return (computedStyle === null || computedStyle === void 0 ? void 0 : computedStyle.getPropertyValue("cursor")) === "pointer" && actionType === "click";
  };
  var isUrlAllowed = function(autocaptureOptions) {
    var pageUrlAllowlist = autocaptureOptions.pageUrlAllowlist, pageUrlExcludelist = autocaptureOptions.pageUrlExcludelist;
    if (pageUrlExcludelist && pageUrlExcludelist.length > 0 && isUrlMatchAllowlist(window.location.href, pageUrlExcludelist)) {
      return false;
    }
    if (!isUrlMatchAllowlist(window.location.href, pageUrlAllowlist)) {
      return false;
    }
    return true;
  };
  var createShouldTrackEvent = function(autocaptureOptions, allowlist) {
    return function(actionType, element) {
      var _a4, _b;
      var shouldTrackEventResolver = autocaptureOptions.shouldTrackEventResolver;
      var tag = (_b = (_a4 = element === null || element === void 0 ? void 0 : element.tagName) === null || _a4 === void 0 ? void 0 : _a4.toLowerCase) === null || _b === void 0 ? void 0 : _b.call(_a4);
      if (!tag) {
        return false;
      }
      if (shouldTrackEventResolver) {
        return shouldTrackEventResolver(actionType, element);
      }
      if (!isUrlAllowed(autocaptureOptions)) {
        return false;
      }
      var elementType = String(element === null || element === void 0 ? void 0 : element.getAttribute("type")) || "";
      if (typeof elementType === "string") {
        switch (elementType.toLowerCase()) {
          case "hidden":
            return false;
          case "password":
            return false;
        }
      }
      if (allowlist) {
        var hasMatchAnyAllowedSelector = allowlist.some(function(selector2) {
          var _a5;
          return !!((_a5 = element === null || element === void 0 ? void 0 : element.matches) === null || _a5 === void 0 ? void 0 : _a5.call(element, selector2));
        });
        if (!hasMatchAnyAllowedSelector) {
          return false;
        }
      }
      switch (tag) {
        case "input":
        case "select":
        case "textarea":
          return actionType === "change" || actionType === "click";
        default:
          return actionType === "click";
      }
    };
  };
  var isNonSensitiveElement = function(element) {
    var _a4, _b, _c;
    var tag = (_b = (_a4 = element === null || element === void 0 ? void 0 : element.tagName) === null || _a4 === void 0 ? void 0 : _a4.toLowerCase) === null || _b === void 0 ? void 0 : _b.call(_a4);
    var isContentEditable = element instanceof HTMLElement ? ((_c = element.getAttribute("contenteditable")) === null || _c === void 0 ? void 0 : _c.toLowerCase()) === "true" : false;
    return !SENSITIVE_TAGS.includes(tag) && !isContentEditable;
  };
  var parseAttributesToMask = function(attributeString) {
    return attributeString ? attributeString.split(",").map(function(attr2) {
      return attr2.trim();
    }).filter(function(attr2) {
      return attr2.length > 0 && attr2 !== "id" && attr2 !== "class";
    }) : [];
  };
  var extractPrefixedAttributes = function(attrs, prefix) {
    return Object.entries(attrs).reduce(function(attributes, _a4) {
      var _b = __read(_a4, 2), attributeName = _b[0], attributeValue = _b[1];
      if (attributeName.startsWith(prefix)) {
        var attributeKey = attributeName.replace(prefix, "");
        if (attributeKey) {
          attributes[attributeKey] = attributeValue || "";
        }
      }
      return attributes;
    }, {});
  };
  var isEmpty = function(value) {
    return value === void 0 || value === null || typeof value === "object" && Object.keys(value).length === 0 || typeof value === "string" && value.trim().length === 0;
  };
  var removeEmptyProperties = function(properties) {
    return Object.keys(properties).reduce(function(filteredProperties, key) {
      var value = properties[key];
      if (!isEmpty(value)) {
        filteredProperties[key] = value;
      }
      return filteredProperties;
    }, {});
  };
  var getCurrentPageViewId = function() {
    var _a4;
    try {
      var globalScope3 = getGlobalScope();
      var raw = (_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.sessionStorage) === null || _a4 === void 0 ? void 0 : _a4.getItem(PAGE_VIEW_SESSION_STORAGE_KEY2);
      if (!raw) {
        return void 0;
      }
      var parsed = JSON.parse(raw);
      if (typeof parsed.pageViewId === "string") {
        return parsed.pageViewId;
      }
    } catch (_b) {
    }
    return void 0;
  };
  var getClosestElement = function(element, selectors, shadow) {
    if (shadow === void 0) {
      shadow = SHADOW_OFF;
    }
    if (shadow.enabled) {
      return getClosestElementInShadow(element, selectors, shadow.maxDepth);
    }
    return getClosestElementLight(element, selectors);
  };
  var getClosestElementLight = function(element, selectors) {
    if (!element) {
      return null;
    }
    if (selectors.some(function(selector2) {
      var _a4;
      return (_a4 = element === null || element === void 0 ? void 0 : element.matches) === null || _a4 === void 0 ? void 0 : _a4.call(element, selector2);
    })) {
      return element;
    }
    return getClosestElementLight(element === null || element === void 0 ? void 0 : element.parentElement, selectors);
  };
  var getClosestElementInShadow = function(element, selectors, maxDepth) {
    var e_1, _a4;
    if (!element) {
      return null;
    }
    var _loop_1 = function(current2) {
      if (selectors.some(function(selector2) {
        var _a5;
        return (_a5 = current2 === null || current2 === void 0 ? void 0 : current2.matches) === null || _a5 === void 0 ? void 0 : _a5.call(current2, selector2);
      })) {
        return { value: current2 };
      }
    };
    try {
      for (var _b = __values(walkComposedAncestors(element, maxDepth)), _c = _b.next(); !_c.done; _c = _b.next()) {
        var current = _c.value;
        var state_1 = _loop_1(current);
        if (typeof state_1 === "object")
          return state_1.value;
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return null;
  };
  var querySelectorAllDeep = function(root3, selectorString, shadow) {
    if (shadow === void 0) {
      shadow = SHADOW_OFF;
    }
    return shadow.enabled ? querySelectorAllDeepInShadow(root3, selectorString, shadow.maxDepth) : querySelectorAllDeepLight(root3, selectorString);
  };
  var querySelectorAllDeepLight = function(root3, selectorString) {
    var out = [];
    if (!selectorString) {
      return out;
    }
    root3.querySelectorAll(selectorString).forEach(function(el) {
      return out.push(el);
    });
    return out;
  };
  var querySelectorAllDeepInShadow = function(root3, selectorString, maxDepth) {
    var out = querySelectorAllDeepLight(root3, selectorString);
    var start3 = root3 instanceof Document ? root3.documentElement : root3;
    if (!start3) {
      return out;
    }
    collectOpenShadowRoots(start3, maxDepth).forEach(function(_a4) {
      var shadowRoot3 = _a4.root;
      shadowRoot3.querySelectorAll(selectorString).forEach(function(el) {
        return out.push(el);
      });
    });
    return out;
  };
  var resolveEventTarget = function(event, shadow) {
    var _a4, _b;
    if (shadow === void 0) {
      shadow = SHADOW_OFF;
    }
    if (!shadow.enabled) {
      return event.target;
    }
    var path = (_a4 = event.composedPath) === null || _a4 === void 0 ? void 0 : _a4.call(event);
    var fallback = (_b = event.target) !== null && _b !== void 0 ? _b : null;
    if (!path || path.length === 0) {
      return fallback;
    }
    var innermost = path[0];
    if (!(innermost instanceof Element)) {
      return innermost !== null && innermost !== void 0 ? innermost : fallback;
    }
    var _c = segmentWalk(innermost, shadow.maxDepth), segments = _c.segments, truncated = _c.truncated;
    if (truncated && segments.length > 0) {
      return segments[segments.length - 1].target;
    }
    return innermost;
  };
  var filterOutNonTrackableEvents = function(event) {
    if (event.event.target === null || !event.closestTrackedAncestor) {
      return false;
    }
    return true;
  };
  function isElementBasedEvent(event) {
    return event.type === "click" || event.type === "change";
  }
  function normalizePageUrl(href) {
    if (!href) {
      return "";
    }
    try {
      var parsed = new URL(href);
      parsed.search = "";
      return getDecodeURI(parsed.href);
    } catch (_a4) {
      return getDecodeURI(href);
    }
  }
  function getNormalizedPageUrl(globalScope3) {
    var _a4, _b;
    if (globalScope3 === void 0) {
      globalScope3 = getGlobalScope();
    }
    return normalizePageUrl((_b = (_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location) === null || _a4 === void 0 ? void 0 : _a4.href) !== null && _b !== void 0 ? _b : "");
  }
  function resolveHistoryNavigationUrl(url, globalScope3) {
    var _a4;
    if (globalScope3 === void 0) {
      globalScope3 = getGlobalScope();
    }
    if (url == null || url === "") {
      return getNormalizedPageUrl(globalScope3);
    }
    var baseHref = ((_a4 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location) === null || _a4 === void 0 ? void 0 : _a4.href) || "http://localhost/";
    try {
      return normalizePageUrl(new URL(url, baseHref).href);
    } catch (_b) {
      return getNormalizedPageUrl(globalScope3);
    }
  }
  var MouseButton;
  (function(MouseButton2) {
    MouseButton2[MouseButton2["LEFT_OR_TOUCH_CONTACT"] = 0] = "LEFT_OR_TOUCH_CONTACT";
    MouseButton2[MouseButton2["MIDDLE"] = 1] = "MIDDLE";
    MouseButton2[MouseButton2["RIGHT"] = 2] = "RIGHT";
  })(MouseButton || (MouseButton = {}));

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/libs/messenger.js
  var VISUAL_TAGGING_BRAND = "__AMPLITUDE_VISUAL_TAGGING__";
  function enableVisualTagging(messenger, options) {
    var branded = messenger;
    if (branded[VISUAL_TAGGING_BRAND] === true) {
      return;
    }
    branded[VISUAL_TAGGING_BRAND] = true;
    var dataExtractor = options.dataExtractor, isElementSelectable = options.isElementSelectable, cssSelectorAllowlist = options.cssSelectorAllowlist, actionClickAllowlist = options.actionClickAllowlist;
    var amplitudeVisualTaggingSelectorInstance = null;
    var onSelect = function(data) {
      messenger.notify({ action: "element-selected", data });
    };
    var onTrack = function(type, properties) {
      if (type === "selector-mode-changed") {
        messenger.notify({ action: "track-selector-mode-changed", data: properties });
      } else if (type === "selector-moved") {
        messenger.notify({ action: "track-selector-moved", data: properties });
      }
    };
    messenger.registerActionHandler("initialize-visual-tagging-selector", function(actionData) {
      messenger.loadScriptOnce(AMPLITUDE_VISUAL_TAGGING_SELECTOR_SCRIPT_URL).then(function() {
        var _a4;
        amplitudeVisualTaggingSelectorInstance = (_a4 = window === null || window === void 0 ? void 0 : window.amplitudeVisualTaggingSelector) === null || _a4 === void 0 ? void 0 : _a4.call(window, {
          getEventTagProps: dataExtractor.getEventTagProps,
          isElementSelectable: function(element) {
            if (isElementSelectable) {
              return isElementSelectable((actionData === null || actionData === void 0 ? void 0 : actionData.actionType) || "click", element);
            }
            return true;
          },
          onTrack,
          onSelect,
          visualHighlightClass: AMPLITUDE_VISUAL_TAGGING_HIGHLIGHT_CLASS,
          messenger,
          cssSelectorAllowlist,
          actionClickAllowlist,
          extractDataFromDataSource: dataExtractor.extractDataFromDataSource,
          dataExtractor,
          diagnostics: {
            autocapture: {
              version: VERSION2
            }
          }
        });
        messenger.notify({ action: "selector-loaded" });
      }).catch(function() {
        var _a4;
        (_a4 = messenger.logger) === null || _a4 === void 0 ? void 0 : _a4.warn("Failed to initialize visual tagging selector");
      });
    });
    messenger.registerActionHandler("close-visual-tagging-selector", function() {
      var _a4;
      (_a4 = amplitudeVisualTaggingSelectorInstance === null || amplitudeVisualTaggingSelectorInstance === void 0 ? void 0 : amplitudeVisualTaggingSelectorInstance.close) === null || _a4 === void 0 ? void 0 : _a4.call(amplitudeVisualTaggingSelectorInstance);
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-click.js
  function trackClicks(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, shouldTrackEvent = _a4.shouldTrackEvent, evaluateTriggers = _a4.evaluateTriggers;
    var clickObservable = allObservables.clickObservable;
    var clickObservableFiltered = clickObservable.filter(filterOutNonTrackableEvents).filter(function(click) {
      return shouldTrackEvent("click", click.closestTrackedAncestor);
    }).map(function(click) {
      return evaluateTriggers(click);
    });
    var clicks = clickObservableFiltered;
    return clicks.subscribe(function(click) {
      amplitude === null || amplitude === void 0 ? void 0 : amplitude.track(AMPLITUDE_ELEMENT_CLICKED_EVENT, click.targetElementProperties);
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-change.js
  function trackChange(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, getEventProperties = _a4.getEventProperties, shouldTrackEvent = _a4.shouldTrackEvent, evaluateTriggers = _a4.evaluateTriggers;
    var changeObservable = allObservables.changeObservable;
    var filteredChangeObservable = changeObservable.filter(filterOutNonTrackableEvents).filter(function(changeEvent) {
      return shouldTrackEvent("change", changeEvent.closestTrackedAncestor);
    }).map(function(changeEvent) {
      return evaluateTriggers(changeEvent);
    });
    return filteredChangeObservable.subscribe(function(changeEvent) {
      amplitude === null || amplitude === void 0 ? void 0 : amplitude.track(AMPLITUDE_ELEMENT_CHANGED_EVENT, getEventProperties("change", changeEvent.closestTrackedAncestor));
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-action-click.js
  init_esm();
  function trackActionClick(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, options = _a4.options, getEventProperties = _a4.getEventProperties, shouldTrackEvent = _a4.shouldTrackEvent, shouldTrackActionClick = _a4.shouldTrackActionClick;
    var clickObservable = allObservables.clickObservable, mutationObservable = allObservables.mutationObservable, navigateObservable = allObservables.navigateObservable;
    var filteredClickObservable = clickObservable.filter(function(click) {
      return !shouldTrackEvent("click", click.closestTrackedAncestor);
    }).map(function(click) {
      var closestActionClickEl = getClosestElement(click.event.target, options.actionClickAllowlist);
      click.closestTrackedAncestor = closestActionClickEl;
      if (click.closestTrackedAncestor !== null) {
        click.targetElementProperties = getEventProperties(click.type, click.closestTrackedAncestor);
      }
      return click;
    }).filter(filterOutNonTrackableEvents).filter(function(clickEvent) {
      return shouldTrackActionClick("click", clickEvent.closestTrackedAncestor);
    });
    var mutationOrNavigate = navigateObservable ? merge(mutationObservable, navigateObservable) : mutationObservable;
    var clickMutationNavigateObservable = merge(filteredClickObservable, mutationOrNavigate);
    var actionClickTimer = null;
    var lastClickEvent = null;
    var actionClickObservable = asyncMap(clickMutationNavigateObservable, function(event) {
      if (actionClickTimer) {
        clearTimeout(actionClickTimer);
        actionClickTimer = null;
      }
      if (event.type === "click") {
        lastClickEvent = event;
        actionClickTimer = setTimeout(function() {
          actionClickTimer = null;
          lastClickEvent = null;
        }, 500);
        return Promise.resolve(null);
      } else {
        if (lastClickEvent) {
          var event_1 = lastClickEvent;
          lastClickEvent = null;
          return Promise.resolve(event_1);
        }
      }
      return Promise.resolve(null);
    });
    return actionClickObservable.subscribe(function(actionClick) {
      if (!actionClick)
        return;
      amplitude === null || amplitude === void 0 ? void 0 : amplitude.track(AMPLITUDE_ELEMENT_CLICKED_EVENT, getEventProperties("click", actionClick.closestTrackedAncestor));
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-scroll.js
  init_esm();
  function trackScroll(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables;
    void amplitude;
    var scrollObservable = allObservables.scrollObservable;
    var state = { maxX: 0, maxY: 0 };
    var scrollSubscription = scrollObservable.subscribe(function() {
      var _a5, _b, _c, _d;
      var globalScope3 = getGlobalScope();
      var currentX = Math.floor((_b = (_a5 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.scrollX) !== null && _a5 !== void 0 ? _a5 : globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.pageXOffset) !== null && _b !== void 0 ? _b : 0);
      var currentY = Math.floor((_d = (_c = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.scrollY) !== null && _c !== void 0 ? _c : globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.pageYOffset) !== null && _d !== void 0 ? _d : 0);
      state.maxX = Math.max(state.maxX, currentX);
      state.maxY = Math.max(state.maxY, currentY);
    });
    return {
      unsubscribe: function() {
        scrollSubscription.unsubscribe();
      },
      getState: function() {
        return state;
      },
      reset: function() {
        state.maxX = 0;
        state.maxY = 0;
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/observables.js
  init_tslib_es6();
  init_esm();
  var globalScope2 = getGlobalScope();
  var readGate = function(gate) {
    return gate.get();
  };
  var MUTATION_OBSERVER_INIT = {
    childList: true,
    attributes: true,
    characterData: true,
    subtree: true
  };
  var createMutationObservable = function(shadowGate) {
    if (!shadowGate) {
      return new import_zen_observable.default(function(observer) {
        var mutationObserver = new MutationObserver(function(mutations) {
          observer.next(mutations);
        });
        if (document.body) {
          mutationObserver.observe(document.body, {
            childList: true,
            attributes: true,
            characterData: true,
            subtree: true
          });
        }
        return function() {
          return mutationObserver.disconnect();
        };
      });
    }
    return new import_zen_observable.default(function(observer) {
      var observed = /* @__PURE__ */ new WeakSet();
      var rootDepth = /* @__PURE__ */ new WeakMap();
      var observeRoot = function(root3, depth) {
        if (observed.has(root3)) {
          return;
        }
        observed.add(root3);
        if (isShadowRoot(root3)) {
          rootDepth.set(root3, depth);
        }
        mutationObserver.observe(root3, MUTATION_OBSERVER_INIT);
      };
      var observeShadowRootsWithin = function(host3, baseDepth, maxDepth) {
        var remaining = maxDepth - baseDepth;
        if (remaining <= 0) {
          return;
        }
        collectOpenShadowRoots(host3, remaining).forEach(function(_a4) {
          var root3 = _a4.root, depth = _a4.depth;
          return observeRoot(root3, baseDepth + depth);
        });
      };
      var depthOfTree = function(node3) {
        var treeRoot = node3.getRootNode();
        if (!isShadowRoot(treeRoot)) {
          return 0;
        }
        var recorded = rootDepth.get(treeRoot);
        return recorded !== null && recorded !== void 0 ? recorded : 0;
      };
      var mutationObserver = new MutationObserver(function(mutations) {
        var e_1, _a4;
        observer.next(mutations);
        try {
          var shadow_1 = readGate(shadowGate);
          if (!shadow_1.enabled) {
            return;
          }
          var _loop_1 = function(mutation2) {
            var baseDepth = depthOfTree(mutation2.target);
            mutation2.addedNodes.forEach(function(node3) {
              if (node3 instanceof Element) {
                observeShadowRootsWithin(node3, baseDepth, shadow_1.maxDepth);
              }
            });
          };
          try {
            for (var mutations_1 = __values(mutations), mutations_1_1 = mutations_1.next(); !mutations_1_1.done; mutations_1_1 = mutations_1.next()) {
              var mutation = mutations_1_1.value;
              _loop_1(mutation);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (mutations_1_1 && !mutations_1_1.done && (_a4 = mutations_1.return)) _a4.call(mutations_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
        } catch (_b) {
        }
      });
      var cancelArmListener;
      if (document.body) {
        var body_1 = document.body;
        observeRoot(body_1, 0);
        cancelArmListener = shadowGate.onArm(function(shadow) {
          observeShadowRootsWithin(body_1, 0, shadow.maxDepth);
        });
      }
      return function() {
        cancelArmListener === null || cancelArmListener === void 0 ? void 0 : cancelArmListener();
        mutationObserver.disconnect();
      };
    });
  };
  var createClickObservable = function(clickType) {
    if (clickType === void 0) {
      clickType = "click";
    }
    return new import_zen_observable.default(function(observer) {
      var _a4;
      var handler = function(event) {
        observer.next(event);
      };
      (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.document.addEventListener(clickType, handler, { capture: true });
      return function() {
        var _a5;
        (_a5 = getGlobalScope()) === null || _a5 === void 0 ? void 0 : _a5.document.removeEventListener(clickType, handler, { capture: true });
      };
    });
  };
  var createScrollObservable = function() {
    return new import_zen_observable.default(function(observer) {
      var _a4, _b;
      var handler = function(event) {
        observer.next(event);
      };
      (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.addEventListener("scroll", handler, { capture: true, passive: true });
      (_b = getGlobalScope()) === null || _b === void 0 ? void 0 : _b.addEventListener("resize", handler, { passive: true });
      return function() {
        var _a5, _b2;
        (_a5 = getGlobalScope()) === null || _a5 === void 0 ? void 0 : _a5.removeEventListener("scroll", handler, { capture: true });
        (_b2 = getGlobalScope()) === null || _b2 === void 0 ? void 0 : _b2.removeEventListener("resize", handler);
      };
    });
  };
  var createConsoleErrorObservable = function() {
    return new import_zen_observable.default(function(observer) {
      var handler = function(_2) {
        var args = [];
        for (var _i = 1; _i < arguments.length; _i++) {
          args[_i - 1] = arguments[_i];
        }
        var message = void 0;
        if (Array.isArray(args[0]) && typeof args[0][0] === "string") {
          message = args[0][0];
        }
        observer.next({ kind: "console", message });
      };
      consoleObserver.addListener("error", handler);
      return function() {
        consoleObserver.removeListener(handler);
      };
    });
  };
  var createExposureObservable = function(mutationObservable, selectorAllowlist, registerRescanOrShadowGate, maybeShadowGate) {
    var registerRescan = typeof registerRescanOrShadowGate === "function" ? registerRescanOrShadowGate : void 0;
    var shadowGate = typeof registerRescanOrShadowGate === "function" ? maybeShadowGate : registerRescanOrShadowGate;
    return new import_zen_observable.default(function(observer) {
      var globalScope3 = getGlobalScope();
      if (!(globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.IntersectionObserver)) {
        return function() {
          return;
        };
      }
      var intersectionObserver = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
          observer.next(entry);
        });
      }, {
        root: null,
        rootMargin: "0px",
        // Keep intersecting elements as a small candidate set. Their mid-height
        // line is evaluated on scroll by trackExposure.
        threshold: 0
      });
      var selectorString = selectorAllowlist.join(",");
      var observeMatchingElementsLight = function(root3) {
        if (root3 instanceof Element && root3.matches(selectorString)) {
          intersectionObserver.observe(root3);
        }
        root3.querySelectorAll(selectorString).forEach(function(element) {
          intersectionObserver.observe(element);
        });
      };
      var observeMatchesInShadow = function(root3) {
        if (!shadowGate) {
          return;
        }
        try {
          querySelectorAllDeep(root3, selectorString, readGate(shadowGate)).forEach(function(element) {
            intersectionObserver.observe(element);
          });
        } catch (_a4) {
        }
      };
      var rescan = function() {
        var doc = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.document;
        if (!doc) {
          return;
        }
        var elements = [];
        try {
          if (shadowGate && readGate(shadowGate).enabled) {
            elements = querySelectorAllDeep(doc, selectorString, readGate(shadowGate));
          } else {
            elements = doc.querySelectorAll(selectorString);
          }
        } catch (_a4) {
          return;
        }
        Array.from(elements).forEach(function(element) {
          intersectionObserver.unobserve(element);
          intersectionObserver.observe(element);
        });
      };
      registerRescan === null || registerRescan === void 0 ? void 0 : registerRescan(rescan);
      rescan();
      var cancelArmListener = shadowGate === null || shadowGate === void 0 ? void 0 : shadowGate.onArm(function() {
        if (globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.document) {
          observeMatchesInShadow(globalScope3.document);
        }
      });
      var mutationSubscription = mutationObservable.subscribe(function(_a4) {
        var event = _a4.event;
        return event.forEach(function(_a5) {
          var addedNodes = _a5.addedNodes;
          return addedNodes.forEach(function(node3) {
            if (node3 instanceof DocumentFragment) {
              observeMatchingElementsLight(node3);
              return;
            }
            if (!(node3 instanceof Element)) {
              return;
            }
            if (!shadowGate || !readGate(shadowGate).enabled) {
              observeMatchingElementsLight(node3);
              return;
            }
            try {
              if (node3.matches(selectorString)) {
                intersectionObserver.observe(node3);
              }
            } catch (_a6) {
            }
            observeMatchesInShadow(node3);
          });
        });
      });
      return function() {
        registerRescan === null || registerRescan === void 0 ? void 0 : registerRescan(void 0);
        cancelArmListener === null || cancelArmListener === void 0 ? void 0 : cancelArmListener();
        mutationSubscription.unsubscribe();
        intersectionObserver.disconnect();
      };
    });
  };
  var createUnhandledErrorObservable = function() {
    return new import_zen_observable.default(function(observer) {
      var handler = function(event) {
        if (!(event instanceof ErrorEvent)) {
          return;
        }
        var output = {
          kind: "error"
        };
        if (event.error instanceof Error || event.error instanceof DOMException) {
          output = __assign(__assign({}, output), { message: event.error.message, stack: event.error.stack, filename: event.filename, lineNumber: event.lineno, columnNumber: event.colno });
        } else if (typeof event.error === "string") {
          output.message = event.error;
        }
        observer.next(output);
      };
      globalScope2.addEventListener("error", handler);
      return function() {
        globalScope2.removeEventListener("error", handler);
      };
    });
  };
  var createUnhandledRejectionObservable = function() {
    return new import_zen_observable.default(function(observer) {
      var handler = function(event) {
        var output = {
          kind: "unhandledrejection"
        };
        if (event.reason instanceof Error || event.reason instanceof DOMException) {
          output.message = event.reason.message;
          output.stack = event.reason.stack;
        } else if (typeof event.reason === "string") {
          output.message = event.reason;
        }
        observer.next(output);
      };
      globalScope2.addEventListener("unhandledrejection", handler);
      return function() {
        globalScope2.removeEventListener("unhandledrejection", handler);
      };
    });
  };
  var createErrorObservable = function() {
    var unhandledErrorObservable = merge(createUnhandledErrorObservable(), createUnhandledRejectionObservable());
    return merge(unhandledErrorObservable, createConsoleErrorObservable());
  };
  var createMouseMoveObservable = function() {
    return new import_zen_observable.default(function(observer) {
      var handler = function(event) {
        observer.next(event);
      };
      var args = { capture: true };
      globalScope2.document.addEventListener("mousemove", handler, args);
      return function() {
        globalScope2.document.removeEventListener("mousemove", handler, args);
      };
    });
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/pageActions/triggers.js
  init_tslib_es6();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/pageActions/matchEventToFilter.js
  var matchEventToFilter = function(event, filter) {
    try {
      if (filter.subprop_key === "[Amplitude] Element Text") {
        return filter.subprop_op === "is" && filter.subprop_value.includes(event.targetElementProperties["[Amplitude] Element Text"]);
      } else if (filter.subprop_key === "[Amplitude] Element Hierarchy") {
        return filter.subprop_op === "autotrack css match" && !!event.closestTrackedAncestor.closest(filter.subprop_value.toString());
      }
    } catch (error) {
      console.error("Error matching event to filter", error);
      return false;
    }
    return false;
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/pageActions/actions.js
  var getDataSource = function(dataSource, contextElement) {
    try {
      if (dataSource.sourceType === "DOM_ELEMENT") {
        var scopingElement = document.documentElement;
        if (dataSource.scope && contextElement) {
          scopingElement = contextElement.closest(dataSource.scope);
        }
        if (scopingElement && dataSource.selector) {
          return scopingElement.querySelector(dataSource.selector);
        }
        return scopingElement;
      }
    } catch (error) {
      return void 0;
    }
    return void 0;
  };
  var executeActions = function(actions, ev, dataExtractor) {
    actions.forEach(function(action) {
      if (typeof action === "string") {
        return;
      }
      if (action.actionType === "ATTACH_EVENT_PROPERTY") {
        var data = dataExtractor.extractDataFromDataSource(action.dataSource, ev.closestTrackedAncestor);
        ev.targetElementProperties[action.destinationKey] = data;
      }
    });
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/pageActions/triggers.js
  var eventTypeToBrowserEventMap = {
    "[Amplitude] Element Clicked": "click",
    "[Amplitude] Element Changed": "change"
  };
  var groupLabeledEventIdsByEventType = function(labeledEvents) {
    var e_1, _a4, e_2, _b;
    var groupedLabeledEvents = Object.values(eventTypeToBrowserEventMap).reduce(function(acc, browserEvent2) {
      acc[browserEvent2] = /* @__PURE__ */ new Set();
      return acc;
    }, {});
    if (!labeledEvents) {
      return groupedLabeledEvents;
    }
    try {
      for (var labeledEvents_1 = __values(labeledEvents), labeledEvents_1_1 = labeledEvents_1.next(); !labeledEvents_1_1.done; labeledEvents_1_1 = labeledEvents_1.next()) {
        var le = labeledEvents_1_1.value;
        try {
          try {
            for (var _c = (e_2 = void 0, __values(le.definition)), _d = _c.next(); !_d.done; _d = _c.next()) {
              var def = _d.value;
              var browserEvent = eventTypeToBrowserEventMap[def.event_type];
              if (browserEvent) {
                groupedLabeledEvents[browserEvent].add(le.id);
              }
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
            } finally {
              if (e_2) throw e_2.error;
            }
          }
        } catch (e2) {
          console.warn("Skipping Labeled Event due to malformed definition", le === null || le === void 0 ? void 0 : le.id, e2);
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (labeledEvents_1_1 && !labeledEvents_1_1.done && (_a4 = labeledEvents_1.return)) _a4.call(labeledEvents_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return groupedLabeledEvents;
  };
  var createLabeledEventToTriggerMap = function(triggers) {
    var e_3, _a4, e_4, _b;
    var labeledEventToTriggerMap = /* @__PURE__ */ new Map();
    try {
      for (var triggers_1 = __values(triggers), triggers_1_1 = triggers_1.next(); !triggers_1_1.done; triggers_1_1 = triggers_1.next()) {
        var trigger = triggers_1_1.value;
        try {
          for (var _c = (e_4 = void 0, __values(trigger.conditions)), _d = _c.next(); !_d.done; _d = _c.next()) {
            var condition = _d.value;
            if (condition.type === "LABELED_EVENT") {
              var eventId = condition.match.eventId;
              var existingTriggers = labeledEventToTriggerMap.get(eventId);
              if (!existingTriggers) {
                existingTriggers = [];
                labeledEventToTriggerMap.set(eventId, existingTriggers);
              }
              existingTriggers.push(trigger);
            }
          }
        } catch (e_4_1) {
          e_4 = { error: e_4_1 };
        } finally {
          try {
            if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
          } finally {
            if (e_4) throw e_4.error;
          }
        }
      }
    } catch (e_3_1) {
      e_3 = { error: e_3_1 };
    } finally {
      try {
        if (triggers_1_1 && !triggers_1_1.done && (_a4 = triggers_1.return)) _a4.call(triggers_1);
      } finally {
        if (e_3) throw e_3.error;
      }
    }
    return labeledEventToTriggerMap;
  };
  var matchEventToLabeledEvents = function(event, labeledEvents) {
    return labeledEvents.filter(function(le) {
      return le.definition.some(function(def) {
        return eventTypeToBrowserEventMap[def.event_type] === event.type && def.filters.every(function(filter) {
          return matchEventToFilter(event, filter);
        });
      });
    });
  };
  var matchLabeledEventsToTriggers = function(labeledEvents, leToTriggerMap) {
    var e_5, _a4, e_6, _b;
    var matchingTriggers = /* @__PURE__ */ new Set();
    try {
      for (var labeledEvents_2 = __values(labeledEvents), labeledEvents_2_1 = labeledEvents_2.next(); !labeledEvents_2_1.done; labeledEvents_2_1 = labeledEvents_2.next()) {
        var le = labeledEvents_2_1.value;
        var triggers = leToTriggerMap.get(le.id);
        if (triggers) {
          try {
            for (var triggers_2 = (e_6 = void 0, __values(triggers)), triggers_2_1 = triggers_2.next(); !triggers_2_1.done; triggers_2_1 = triggers_2.next()) {
              var trigger = triggers_2_1.value;
              matchingTriggers.add(trigger);
            }
          } catch (e_6_1) {
            e_6 = { error: e_6_1 };
          } finally {
            try {
              if (triggers_2_1 && !triggers_2_1.done && (_b = triggers_2.return)) _b.call(triggers_2);
            } finally {
              if (e_6) throw e_6.error;
            }
          }
        }
      }
    } catch (e_5_1) {
      e_5 = { error: e_5_1 };
    } finally {
      try {
        if (labeledEvents_2_1 && !labeledEvents_2_1.done && (_a4 = labeledEvents_2.return)) _a4.call(labeledEvents_2);
      } finally {
        if (e_5) throw e_5.error;
      }
    }
    return Array.from(matchingTriggers);
  };
  var TriggerEvaluator = (
    /** @class */
    (function() {
      function TriggerEvaluator2(groupedLabeledEvents, labeledEventToTriggerMap, dataExtractor, options) {
        this.groupedLabeledEvents = groupedLabeledEvents;
        this.labeledEventToTriggerMap = labeledEventToTriggerMap;
        this.dataExtractor = dataExtractor;
        this.options = options;
      }
      TriggerEvaluator2.prototype.evaluate = function(event) {
        var e_7, _a4;
        var pageActions = this.options.pageActions;
        if (!pageActions) {
          return event;
        }
        var matchingLabeledEvents = matchEventToLabeledEvents(event, Array.from(this.groupedLabeledEvents[event.type]).map(function(id2) {
          return pageActions.labeledEvents[id2];
        }));
        var matchingTriggers = matchLabeledEventsToTriggers(matchingLabeledEvents, this.labeledEventToTriggerMap);
        try {
          for (var matchingTriggers_1 = __values(matchingTriggers), matchingTriggers_1_1 = matchingTriggers_1.next(); !matchingTriggers_1_1.done; matchingTriggers_1_1 = matchingTriggers_1.next()) {
            var trigger = matchingTriggers_1_1.value;
            executeActions(trigger.actions, event, this.dataExtractor);
          }
        } catch (e_7_1) {
          e_7 = { error: e_7_1 };
        } finally {
          try {
            if (matchingTriggers_1_1 && !matchingTriggers_1_1.done && (_a4 = matchingTriggers_1.return)) _a4.call(matchingTriggers_1);
          } finally {
            if (e_7) throw e_7.error;
          }
        }
        return event;
      };
      TriggerEvaluator2.prototype.update = function(groupedLabeledEvents, labeledEventToTriggerMap, options) {
        this.groupedLabeledEvents = groupedLabeledEvents;
        this.labeledEventToTriggerMap = labeledEventToTriggerMap;
        this.options = options;
      };
      return TriggerEvaluator2;
    })()
  );
  var createTriggerEvaluator = function(groupedLabeledEvents, labeledEventToTriggerMap, dataExtractor, options) {
    return new TriggerEvaluator(groupedLabeledEvents, labeledEventToTriggerMap, dataExtractor, options);
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/data-extractor.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/hierarchy.js
  init_tslib_es6();
  init_esm();
  var BLOCKED_ATTRIBUTES = /* @__PURE__ */ new Set([
    // Already captured elsewhere in the hierarchy object
    "id",
    "class",
    // non-useful and potentially large attribute
    "style",
    // sensitive as prefilled form data may populate this attribute
    "value",
    // DOM events
    "onclick",
    "onchange",
    "oninput",
    "onblur",
    "onsubmit",
    "onfocus",
    "onkeydown",
    "onkeyup",
    "onkeypress",
    // React specific
    "data-reactid",
    "data-react-checksum",
    "data-reactroot",
    // Amplitude specific - used for redaction but should not be included in getElementProperties
    DATA_AMP_MASK_ATTRIBUTES,
    TEXT_MASK_ATTRIBUTE,
    // Amplitude specific - synthesized below, so a page-authored copy must not leak through
    DATA_AMP_INTERNAL_SHADOW
  ]);
  var SENSITIVE_ELEMENT_ATTRIBUTE_ALLOWLIST = ["type"];
  var SVG_TAGS = ["svg", "path", "g"];
  var HIGHLY_SENSITIVE_INPUT_TYPES = ["password", "hidden"];
  function siblingCollection(element, shadowAware) {
    if (element.parentElement) {
      return element.parentElement.children;
    }
    var root3 = element.getRootNode();
    if (shadowAware && isShadowRoot(root3)) {
      return root3.children;
    }
    return [];
  }
  function getElementProperties(element, userMaskedAttributeNames, shadow) {
    var e_1, _a4, _b;
    var _c, _d;
    if (shadow === void 0) {
      shadow = SHADOW_OFF;
    }
    if (element === null) {
      return null;
    }
    var tagName2 = String(element.tagName).toLowerCase();
    var properties = {
      tag: tagName2
    };
    var siblings = Array.from(siblingCollection(element, shadow.enabled));
    if (siblings.length) {
      properties.index = siblings.indexOf(element);
      properties.indexOfType = siblings.filter(function(el) {
        return el.tagName === element.tagName;
      }).indexOf(element);
    }
    var prevSiblingTag = (_d = (_c = element.previousElementSibling) === null || _c === void 0 ? void 0 : _c.tagName) === null || _d === void 0 ? void 0 : _d.toLowerCase();
    if (prevSiblingTag) {
      properties.prevSib = String(prevSiblingTag);
    }
    var id2 = element.getAttribute("id");
    if (id2) {
      properties.id = String(id2);
    }
    var classes = Array.from(element.classList);
    if (classes.length) {
      properties.classes = classes;
    }
    var attributes = {};
    var attributesArray = Array.from(element.attributes);
    var filteredAttributes = attributesArray.filter(function(attr3) {
      return !BLOCKED_ATTRIBUTES.has(attr3.name);
    });
    var isSensitiveElement = !isNonSensitiveElement(element);
    if (!HIGHLY_SENSITIVE_INPUT_TYPES.includes(String(element.getAttribute("type"))) && !SVG_TAGS.includes(tagName2)) {
      try {
        for (var filteredAttributes_1 = __values(filteredAttributes), filteredAttributes_1_1 = filteredAttributes_1.next(); !filteredAttributes_1_1.done; filteredAttributes_1_1 = filteredAttributes_1.next()) {
          var attr2 = filteredAttributes_1_1.value;
          if (isSensitiveElement && !SENSITIVE_ELEMENT_ATTRIBUTE_ALLOWLIST.includes(attr2.name)) {
            continue;
          }
          if (userMaskedAttributeNames.has(attr2.name)) {
            attributes[attr2.name] = MASKED_TEXT_VALUE;
            continue;
          }
          attributes[attr2.name] = String(attr2.value).substring(0, MAX_ATTRIBUTE_LENGTH);
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (filteredAttributes_1_1 && !filteredAttributes_1_1.done && (_a4 = filteredAttributes_1.return)) _a4.call(filteredAttributes_1);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    }
    if (Object.keys(attributes).length) {
      properties.attrs = attributes;
    }
    if (element.shadowRoot) {
      properties.attrs = __assign(__assign({}, properties.attrs || {}), (_b = {}, _b[DATA_AMP_INTERNAL_SHADOW] = "true", _b));
      properties.shadow = true;
    }
    return properties;
  }
  function getAncestors(targetEl, shadow) {
    if (shadow === void 0) {
      shadow = SHADOW_OFF;
    }
    if (!targetEl) {
      return [];
    }
    return shadow.enabled ? getAncestorsInShadow(targetEl, shadow.maxDepth) : getAncestorsLight(targetEl);
  }
  function getAncestorsLight(targetEl) {
    var ancestors = [];
    ancestors.push(targetEl);
    var current = targetEl.parentElement;
    while (current && current.tagName !== "HTML") {
      ancestors.push(current);
      current = current.parentElement;
    }
    return ancestors;
  }
  function getAncestorsInShadow(targetEl, maxDepth) {
    var e_2, _a4;
    var ancestors = [];
    try {
      for (var _b = __values(walkComposedAncestors(targetEl, maxDepth)), _c = _b.next(); !_c.done; _c = _b.next()) {
        var el = _c.value;
        if (el.tagName === "HTML") {
          break;
        }
        ancestors.push(el);
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    return ancestors;
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/data-extractor.js
  var hasMaskedAncestorLight = function(element) {
    return element.closest("[".concat(TEXT_MASK_ATTRIBUTE, "]")) !== null;
  };
  var hasMaskedAncestorInShadow = function(element, shadow) {
    var _a4, _b;
    if (getAncestors(element, shadow).some(function(ancestor) {
      return ancestor.hasAttribute(TEXT_MASK_ATTRIBUTE);
    })) {
      return true;
    }
    return (_b = (_a4 = document.documentElement) === null || _a4 === void 0 ? void 0 : _a4.hasAttribute(TEXT_MASK_ATTRIBUTE)) !== null && _b !== void 0 ? _b : false;
  };
  var sharedSelectorEngine;
  function getSharedSelectorEngine() {
    if (!sharedSelectorEngine) {
      sharedSelectorEngine = createSelectorEngine(resolveSelectorConfig());
    }
    return sharedSelectorEngine;
  }
  var DataExtractor = (
    /** @class */
    /* @__PURE__ */ (function() {
      function DataExtractor2(options, context) {
        var e_1, _a4;
        var _this = this;
        var _b;
        this.replaceSensitiveString = function(text) {
          return replaceSensitiveString(text, _this.additionalMaskTextPatterns);
        };
        this.getHierarchy = function(element) {
          var e_2, _a5;
          var _b2, _c;
          var startTime = performance.now();
          var hierarchy = [];
          if (!element) {
            return [];
          }
          var shadow = _this.getShadowMode();
          var ancestors = getAncestors(element, shadow);
          var elementToAttributesToMaskMap = /* @__PURE__ */ new Map();
          for (var i2 = ancestors.length - 1; i2 >= 0; i2--) {
            var node3 = ancestors[i2];
            if (node3) {
              var attributesToMask = parseAttributesToMask(node3.getAttribute(DATA_AMP_MASK_ATTRIBUTES));
              var ancestorAttributesToMask = i2 === ancestors.length - 1 ? [] : (_b2 = elementToAttributesToMaskMap.get(ancestors[i2 + 1])) !== null && _b2 !== void 0 ? _b2 : /* @__PURE__ */ new Set();
              var combinedAttributesToMask = new Set(__spreadArray(__spreadArray([], __read(ancestorAttributesToMask), false), __read(attributesToMask), false));
              elementToAttributesToMaskMap.set(node3, combinedAttributesToMask);
            }
          }
          hierarchy = ancestors.map(function(el) {
            var _a6;
            return getElementProperties(el, (_a6 = elementToAttributesToMaskMap.get(el)) !== null && _a6 !== void 0 ? _a6 : /* @__PURE__ */ new Set(), shadow);
          });
          var _loop_1 = function(hierarchyNode2) {
            if (hierarchyNode2 === null || hierarchyNode2 === void 0 ? void 0 : hierarchyNode2.attrs) {
              Object.entries(hierarchyNode2.attrs).forEach(function(_a6) {
                var _b3 = __read(_a6, 2), key = _b3[0], value = _b3[1];
                if (hierarchyNode2.attrs) {
                  hierarchyNode2.attrs[key] = _this.replaceSensitiveString(value);
                }
              });
            }
          };
          try {
            for (var hierarchy_1 = __values(hierarchy), hierarchy_1_1 = hierarchy_1.next(); !hierarchy_1_1.done; hierarchy_1_1 = hierarchy_1.next()) {
              var hierarchyNode = hierarchy_1_1.value;
              _loop_1(hierarchyNode);
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (hierarchy_1_1 && !hierarchy_1_1.done && (_a5 = hierarchy_1.return)) _a5.call(hierarchy_1);
            } finally {
              if (e_2) throw e_2.error;
            }
          }
          var endTime = performance.now();
          (_c = _this.diagnosticsClient) === null || _c === void 0 ? void 0 : _c.recordHistogram("autocapturePlugin.getHierarchy", endTime - startTime);
          return hierarchy;
        };
        this.getNearestLabel = function(element) {
          var parent = element.parentElement;
          if (!parent) {
            return "";
          }
          var labelElement;
          try {
            labelElement = parent.querySelector(":scope>span,h1,h2,h3,h4,h5,h6");
          } catch (_a5) {
            labelElement = null;
          }
          if (labelElement) {
            return _this.getText(labelElement);
          }
          return _this.getNearestLabel(parent);
        };
        this.getElementPath = function(element) {
          var _a5;
          if (!element) {
            return "";
          }
          var startTime = performance.now();
          var elementPath = _this.selectorEngine.generate(element);
          var endTime = performance.now();
          (_a5 = _this.diagnosticsClient) === null || _a5 === void 0 ? void 0 : _a5.recordHistogram("autocapturePlugin.getElementPath", endTime - startTime);
          return elementPath;
        };
        this.updateSelectorConfig = function(remote, logger) {
          var _a5;
          if (remote === null || remote === void 0) {
            return;
          }
          var hasEnabled = typeof remote.enabled === "boolean";
          var hasShadow = typeof remote.shadowDomEnabled === "boolean";
          if (!hasEnabled && !hasShadow) {
            logger === null || logger === void 0 ? void 0 : logger.debug("@amplitude/element-selector: ignoring remote-config delivery without an explicit `enabled` or `shadowDomEnabled` flag \u2014 keeping current engine state.");
            return;
          }
          var resolved = resolveSelectorConfig(remote, logger);
          var prevEnabled = _this.selectorEngine.getConfig().enabled;
          var prevShadow = _this.shadowGate.get().enabled;
          var mode = _this.shadowGate.arm(shadowModeFromConfig(resolved));
          _this.selectorEngine.updateConfig(__assign(__assign(__assign({}, hasEnabled ? resolved : _this.selectorEngine.getConfig()), { shadowDomEnabled: mode.enabled }), mode.enabled && { maxShadowDomDepth: mode.maxDepth }));
          if (hasEnabled && prevEnabled !== resolved.enabled) {
            logger === null || logger === void 0 ? void 0 : logger.debug(resolved.enabled ? "@amplitude/element-selector: engine enabled \u2014 now emitting new element-selector element paths for autocapture events." : "@amplitude/element-selector: engine disabled \u2014 reverting to legacy cssPath for element paths.");
          }
          if (!prevShadow && mode.enabled) {
            (_a5 = _this.diagnosticsClient) === null || _a5 === void 0 ? void 0 : _a5.setTag("plugin.autocapture.shadowDom", "enabled:".concat(mode.maxDepth));
          }
        };
        this.getShadowMode = function() {
          return _this.shadowGate.get();
        };
        this.getEventProperties = function(actionType, element, dataAttributePrefix) {
          var _a5;
          var _b2, _c, _d;
          var tag = (_c = (_b2 = element === null || element === void 0 ? void 0 : element.tagName) === null || _b2 === void 0 ? void 0 : _b2.toLowerCase) === null || _c === void 0 ? void 0 : _c.call(_b2);
          var rect = typeof element.getBoundingClientRect === "function" ? element.getBoundingClientRect() : { left: null, top: null };
          var hierarchy = _this.getHierarchy(element);
          var currentElementAttributes = (_d = hierarchy[0]) === null || _d === void 0 ? void 0 : _d.attrs;
          var nearestLabel = _this.getNearestLabel(element);
          var attributes = extractPrefixedAttributes(currentElementAttributes !== null && currentElementAttributes !== void 0 ? currentElementAttributes : {}, dataAttributePrefix);
          var properties = (_a5 = {}, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_HIERARCHY] = hierarchy, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_TAG] = tag, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_TEXT] = _this.getText(element), _a5[AMPLITUDE_EVENT_PROP_ELEMENT_POSITION_LEFT] = rect.left == null ? null : Math.round(rect.left), _a5[AMPLITUDE_EVENT_PROP_ELEMENT_POSITION_TOP] = rect.top == null ? null : Math.round(rect.top), _a5[AMPLITUDE_EVENT_PROP_ELEMENT_ATTRIBUTES] = attributes, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_PATH] = _this.getElementPath(element), _a5[AMPLITUDE_EVENT_PROP_ELEMENT_PARENT_LABEL] = nearestLabel, _a5[AMPLITUDE_EVENT_PROP_PAGE_URL] = getDecodeURI(window.location.href.split("?")[0]), _a5[AMPLITUDE_EVENT_PROP_PAGE_TITLE] = getPageTitle(_this.replaceSensitiveString), _a5[AMPLITUDE_EVENT_PROP_VIEWPORT_HEIGHT] = window.innerHeight, _a5[AMPLITUDE_EVENT_PROP_VIEWPORT_WIDTH] = window.innerWidth, _a5);
          var pageViewId = getCurrentPageViewId();
          if (pageViewId) {
            properties[AMPLITUDE_EVENT_PROP_PAGE_VIEW_ID] = pageViewId;
          }
          properties[AMPLITUDE_EVENT_PROP_ELEMENT_ID] = element.getAttribute("id") || "";
          properties[AMPLITUDE_EVENT_PROP_ELEMENT_CLASS] = element.getAttribute("class");
          properties[AMPLITUDE_EVENT_PROP_ELEMENT_ARIA_LABEL] = currentElementAttributes === null || currentElementAttributes === void 0 ? void 0 : currentElementAttributes["aria-label"];
          if (tag === "a" && actionType === "click" && element instanceof HTMLAnchorElement) {
            var href = element.href.substring(0, MAX_ATTRIBUTE_LENGTH);
            properties[AMPLITUDE_EVENT_PROP_ELEMENT_HREF] = _this.replaceSensitiveString(href);
          }
          return removeEmptyProperties(properties);
        };
        this.addTypeAndTimestamp = function(event, type) {
          return {
            event,
            timestamp: Date.now(),
            type
          };
        };
        this.addAdditionalEventProperties = function(event, type, selectorAllowlist, dataAttributePrefix, isCapturingCursorPointer) {
          if (isCapturingCursorPointer === void 0) {
            isCapturingCursorPointer = false;
          }
          var baseEvent = _this.addTypeAndTimestamp(event, type);
          try {
            if (isElementBasedEvent(baseEvent) && baseEvent.event.target !== null) {
              var shadow = _this.getShadowMode();
              var eventTarget = resolveEventTarget(baseEvent.event, shadow);
              if (isCapturingCursorPointer) {
                var isCursorPointer = isElementPointerCursor(eventTarget, baseEvent.type);
                if (isCursorPointer) {
                  baseEvent.closestTrackedAncestor = eventTarget;
                  baseEvent.targetElementProperties = _this.getEventProperties(baseEvent.type, baseEvent.closestTrackedAncestor, dataAttributePrefix);
                  return baseEvent;
                }
              }
              var closestTrackedAncestor = getClosestElement(eventTarget, selectorAllowlist, shadow);
              if (closestTrackedAncestor) {
                baseEvent.closestTrackedAncestor = closestTrackedAncestor;
                baseEvent.targetElementProperties = _this.getEventProperties(baseEvent.type, closestTrackedAncestor, dataAttributePrefix);
              }
            }
          } catch (_a5) {
          }
          return baseEvent;
        };
        this.extractDataFromDataSource = function(dataSource, contextElement) {
          if (dataSource.sourceType === "DOM_ELEMENT") {
            var sourceElement = getDataSource(dataSource, contextElement);
            if (!sourceElement) {
              return void 0;
            }
            if (dataSource.elementExtractType === "TEXT") {
              return _this.getText(sourceElement);
            } else if (dataSource.elementExtractType === "ATTRIBUTE" && dataSource.attribute) {
              return sourceElement.getAttribute(dataSource.attribute);
            }
            return void 0;
          }
          return void 0;
        };
        this.getTextWithMaskedDescendants = function(element) {
          var e_3, _a5;
          var maskedSelector = "[".concat(TEXT_MASK_ATTRIBUTE, "], [contenteditable]");
          if (!element.querySelector(maskedSelector)) {
            return element.innerText;
          }
          var output = "";
          var childNodes3 = Array.from(element.childNodes);
          try {
            for (var childNodes_1 = __values(childNodes3), childNodes_1_1 = childNodes_1.next(); !childNodes_1_1.done; childNodes_1_1 = childNodes_1.next()) {
              var childNode = childNodes_1_1.value;
              if (childNode.nodeType === Node.TEXT_NODE) {
                output += childNode.textContent || "";
                continue;
              }
              if (!(childNode instanceof Element)) {
                continue;
              }
              if (childNode.hasAttribute(TEXT_MASK_ATTRIBUTE) || childNode.hasAttribute("contenteditable")) {
                output += MASKED_TEXT_VALUE;
                continue;
              }
              output += _this.getTextWithMaskedDescendants(childNode);
            }
          } catch (e_3_1) {
            e_3 = { error: e_3_1 };
          } finally {
            try {
              if (childNodes_1_1 && !childNodes_1_1.done && (_a5 = childNodes_1.return)) _a5.call(childNodes_1);
            } finally {
              if (e_3) throw e_3.error;
            }
          }
          return output;
        };
        this.hasMaskedAncestor = function(element) {
          var shadow = _this.getShadowMode();
          return shadow.enabled ? hasMaskedAncestorInShadow(element, shadow) : hasMaskedAncestorLight(element);
        };
        this.getText = function(element) {
          var hasMaskAttribute = _this.hasMaskedAncestor(element);
          if (hasMaskAttribute) {
            return MASKED_TEXT_VALUE;
          }
          var output = "";
          if (!element.querySelector("[".concat(TEXT_MASK_ATTRIBUTE, "], [contenteditable]"))) {
            output = element.innerText || "";
          } else {
            output = _this.getTextWithMaskedDescendants(element);
          }
          return _this.replaceSensitiveString(output.substring(0, 255)).replace(/\s+/g, " ").trim();
        };
        this.getEventTagProps = function(element) {
          var _a5;
          var _b2, _c;
          if (!element) {
            return {};
          }
          var tag = (_c = (_b2 = element === null || element === void 0 ? void 0 : element.tagName) === null || _b2 === void 0 ? void 0 : _b2.toLowerCase) === null || _c === void 0 ? void 0 : _c.call(_b2);
          var properties = (_a5 = {}, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_TAG] = tag, _a5[AMPLITUDE_EVENT_PROP_ELEMENT_TEXT] = _this.getText(element), _a5[AMPLITUDE_EVENT_PROP_PAGE_URL] = window.location.href.split("?")[0], _a5);
          return removeEmptyProperties(properties);
        };
        this.diagnosticsClient = context === null || context === void 0 ? void 0 : context.diagnosticsClient;
        this.selectorEngine = getSharedSelectorEngine();
        this.shadowGate = getSharedShadowGate();
        var rawPatterns = (_b = options.maskTextRegex) !== null && _b !== void 0 ? _b : [];
        var compiled = [];
        try {
          for (var rawPatterns_1 = __values(rawPatterns), rawPatterns_1_1 = rawPatterns_1.next(); !rawPatterns_1_1.done; rawPatterns_1_1 = rawPatterns_1.next()) {
            var entry = rawPatterns_1_1.value;
            if (compiled.length >= MAX_MASK_TEXT_PATTERNS) {
              break;
            }
            if (entry instanceof RegExp) {
              compiled.push(entry);
            } else if ("pattern" in entry && typeof entry.pattern === "string") {
              try {
                compiled.push(new RegExp(entry.pattern, "i"));
              } catch (_c) {
              }
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (rawPatterns_1_1 && !rawPatterns_1_1.done && (_a4 = rawPatterns_1.return)) _a4.call(rawPatterns_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        this.additionalMaskTextPatterns = compiled;
      }
      return DataExtractor2;
    })()
  );

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/element-selector-config.js
  var ELEMENT_SELECTOR_REMOTE_CONFIG_KEY = "configs.analyticsSDK.browserSDK.autocapture.elementSelector";
  function subscribeToElementSelectorConfig(config2, dataExtractor) {
    var remoteConfigClient = config2.remoteConfigClient;
    if (!config2.fetchRemoteConfig || !remoteConfigClient) {
      return void 0;
    }
    var subscriptionId = remoteConfigClient.subscribe(ELEMENT_SELECTOR_REMOTE_CONFIG_KEY, "all", function(remoteConfig, source) {
      config2.loggerProvider.debug("@amplitude/element-selector: remote-config delivery (source=".concat(String(source), ") ").concat(JSON.stringify(remoteConfig)));
      dataExtractor.updateSelectorConfig(remoteConfig, config2.loggerProvider);
    });
    return function() {
      remoteConfigClient.unsubscribe(subscriptionId);
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture-plugin.js
  init_esm();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-exposure.js
  init_esm();
  var MID_HEIGHT_LINE_TOLERANCE_PX = 1;
  var getVisualParent = function(element) {
    var _a4;
    return element.assignedSlot || element.parentElement || ((_a4 = element.parentNode) === null || _a4 === void 0 ? void 0 : _a4.host) || null;
  };
  var getClippingParent = function(element, position) {
    return (position === "fixed" || position === "absolute") && element instanceof HTMLElement ? element.offsetParent : getVisualParent(element);
  };
  var isMidHeightLineVisible = function(element) {
    var globalScope3 = getGlobalScope();
    var viewportHeight = globalScope3.document.documentElement.clientHeight || globalScope3.innerHeight;
    var viewportWidth = globalScope3.document.documentElement.clientWidth || globalScope3.innerWidth;
    var rect = element.getBoundingClientRect();
    var midHeightLine = rect.top + rect.height * EXPOSURE_VIEWED_THRESHOLD;
    var visibleLeft = Math.max(rect.left, 0);
    var visibleRight = Math.min(rect.right, viewportWidth);
    if (midHeightLine < -MID_HEIGHT_LINE_TOLERANCE_PX || midHeightLine > viewportHeight + MID_HEIGHT_LINE_TOLERANCE_PX || visibleLeft >= visibleRight || rect.width <= 0) {
      return false;
    }
    var ancestor = getClippingParent(element, globalScope3.getComputedStyle(element).position);
    while (ancestor) {
      var style = globalScope3.getComputedStyle(ancestor);
      var clipsY = style.overflowY && style.overflowY !== "visible";
      var clipsX = style.overflowX && style.overflowX !== "visible";
      if (clipsY || clipsX) {
        var ancestorRect = ancestor.getBoundingClientRect();
        if (clipsY && (midHeightLine < ancestorRect.top - MID_HEIGHT_LINE_TOLERANCE_PX || midHeightLine > ancestorRect.bottom + MID_HEIGHT_LINE_TOLERANCE_PX)) {
          return false;
        }
        if (clipsX) {
          visibleLeft = Math.max(visibleLeft, ancestorRect.left);
          visibleRight = Math.min(visibleRight, ancestorRect.right);
          if (visibleLeft >= visibleRight) {
            return false;
          }
        }
      }
      ancestor = getClippingParent(ancestor, style.position);
    }
    return true;
  };
  function trackExposure(_a4) {
    var allObservables = _a4.allObservables, onExposure2 = _a4.onExposure, dataExtractor = _a4.dataExtractor, _b = _a4.exposureDuration, exposureDuration = _b === void 0 ? DEFAULT_EXPOSURE_DURATION : _b;
    var exposureMap = /* @__PURE__ */ new Map();
    var exposureTimerMap = /* @__PURE__ */ new Map();
    var intersectingElements = /* @__PURE__ */ new Set();
    var cancelExposure = function(element) {
      var timer = exposureTimerMap.get(element);
      if (timer) {
        clearTimeout(timer);
        exposureTimerMap.set(element, null);
      }
    };
    var updateExposure = function(element) {
      if (!isMidHeightLineVisible(element)) {
        cancelExposure(element);
        return;
      }
      if (exposureMap.get(element) || exposureTimerMap.get(element)) {
        return;
      }
      var timer = setTimeout(function() {
        exposureMap.set(element, true);
        var elementPath = dataExtractor.getElementPath(element);
        onExposure2(elementPath);
        exposureTimerMap.set(element, null);
      }, exposureDuration);
      exposureTimerMap.set(element, timer);
    };
    var exposureObservable = allObservables.exposureObservable, scrollObservable = allObservables.scrollObservable;
    var exposureSubscription = exposureObservable.subscribe(function(event) {
      var entry = event;
      var element = entry.target;
      if (entry.isIntersecting) {
        intersectingElements.add(element);
        updateExposure(element);
      } else {
        intersectingElements.delete(element);
        cancelExposure(element);
      }
    });
    var scrollSubscription = scrollObservable.subscribe(function() {
      intersectingElements.forEach(updateExposure);
    });
    var clearPageViewState = function() {
      exposureTimerMap.forEach(function(timer) {
        if (timer) {
          clearTimeout(timer);
        }
      });
      exposureTimerMap.clear();
      exposureMap.clear();
    };
    return {
      unsubscribe: function() {
        exposureSubscription.unsubscribe();
        scrollSubscription.unsubscribe();
        clearPageViewState();
        intersectingElements.clear();
      },
      reset: function() {
        clearPageViewState();
        intersectingElements.forEach(updateExposure);
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-viewport-content-updated.js
  init_esm();
  function fireViewportContentUpdated(_a4) {
    var _b;
    var _c, _d;
    var amplitude = _a4.amplitude, scrollTracker = _a4.scrollTracker, currentElementExposed = _a4.currentElementExposed, elementExposedForPage = _a4.elementExposedForPage, elementExposedInSentEvents = _a4.elementExposedInSentEvents, exposureTracker = _a4.exposureTracker, isPageEnd = _a4.isPageEnd, lastScroll = _a4.lastScroll;
    var pageScrollMaxState = scrollTracker.getState();
    var globalScope3 = getGlobalScope();
    var viewportWidth = (_c = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.innerWidth) !== null && _c !== void 0 ? _c : 0;
    var viewportHeight = (_d = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.innerHeight) !== null && _d !== void 0 ? _d : 0;
    var newExposures = Array.from(currentElementExposed).filter(function(elementPath) {
      return !elementExposedInSentEvents.has(elementPath);
    });
    var eventProperties = (_b = {}, _b[AMPLITUDE_EVENT_PROP_PAGE_URL] = getNormalizedPageUrl(globalScope3), _b[AMPLITUDE_EVENT_PROP_MAX_PAGE_X] = pageScrollMaxState.maxX + viewportWidth, _b[AMPLITUDE_EVENT_PROP_MAX_PAGE_Y] = pageScrollMaxState.maxY + viewportHeight, _b[AMPLITUDE_EVENT_PROP_VIEWPORT_HEIGHT] = viewportHeight, _b[AMPLITUDE_EVENT_PROP_VIEWPORT_WIDTH] = viewportWidth, _b["[Amplitude] Element Exposed"] = newExposures, _b);
    var pageViewId = getCurrentPageViewId();
    if (pageViewId) {
      eventProperties[AMPLITUDE_EVENT_PROP_PAGE_VIEW_ID] = pageViewId;
    }
    var resetPageViewState = function() {
      scrollTracker.reset();
      var resetScroll = scrollTracker.getState();
      lastScroll.maxX = resetScroll.maxX;
      lastScroll.maxY = resetScroll.maxY;
      elementExposedForPage.clear();
      elementExposedInSentEvents.clear();
      exposureTracker === null || exposureTracker === void 0 ? void 0 : exposureTracker.reset();
    };
    if (newExposures.length === 0 && pageScrollMaxState.maxX === lastScroll.maxX && pageScrollMaxState.maxY === lastScroll.maxY) {
      if (isPageEnd) {
        resetPageViewState();
      }
      return;
    }
    amplitude === null || amplitude === void 0 ? void 0 : amplitude.track("[Amplitude] Viewport Content Updated", eventProperties);
    lastScroll.maxX = pageScrollMaxState.maxX;
    lastScroll.maxY = pageScrollMaxState.maxY;
    newExposures.forEach(function(elementPath) {
      elementExposedInSentEvents.add(elementPath);
    });
    currentElementExposed.clear();
    if (isPageEnd) {
      resetPageViewState();
    }
  }
  function onExposure(elementPath, elementExposedForPage, elementExposedInSentEvents, currentElementExposed, fireViewportContentUpdatedCallback) {
    if (elementExposedForPage.has(elementPath) || elementExposedInSentEvents.has(elementPath)) {
      return;
    }
    elementExposedForPage.add(elementPath);
    currentElementExposed.add(elementPath);
    var exposedArray = Array.from(currentElementExposed);
    var exposedString = JSON.stringify(exposedArray);
    if (exposedString.length >= MAX_ELEMENT_EXPOSED_STR_LENGTH) {
      fireViewportContentUpdatedCallback(false);
    }
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/schedule-initial-exposure-snapshot.js
  function createInitialExposureSnapshotScheduler(_a4) {
    var mutationObservable = _a4.mutationObservable, onRescan = _a4.onRescan, onFlush = _a4.onFlush, exposureDuration = _a4.exposureDuration, _b = _a4.quietMs, quietMs = _b === void 0 ? EXPOSURE_SNAPSHOT_QUIET_MS : _b, _c = _a4.maxWaitMs, maxWaitMs = _c === void 0 ? EXPOSURE_SNAPSHOT_MAX_WAIT_MS : _c;
    var snapshotTaken = false;
    var quietTimer;
    var maxWaitTimer;
    var exposureWaitTimer;
    var mutationSubscription;
    var domContentLoadedListener;
    var clearTimers = function() {
      if (quietTimer) {
        clearTimeout(quietTimer);
        quietTimer = void 0;
      }
      if (maxWaitTimer) {
        clearTimeout(maxWaitTimer);
        maxWaitTimer = void 0;
      }
      if (exposureWaitTimer) {
        clearTimeout(exposureWaitTimer);
        exposureWaitTimer = void 0;
      }
    };
    var removeDomContentLoadedListener = function() {
      if (domContentLoadedListener) {
        document.removeEventListener("DOMContentLoaded", domContentLoadedListener);
        domContentLoadedListener = void 0;
      }
    };
    var canSnapshot = function() {
      return typeof document !== "undefined" && document.readyState !== "loading";
    };
    var takeSnapshot = function() {
      if (snapshotTaken) {
        return;
      }
      if (!canSnapshot()) {
        if (!domContentLoadedListener) {
          domContentLoadedListener = function() {
            domContentLoadedListener = void 0;
            scheduleQuietTimer();
          };
          document.addEventListener("DOMContentLoaded", domContentLoadedListener);
        }
        return;
      }
      snapshotTaken = true;
      clearTimers();
      removeDomContentLoadedListener();
      onRescan();
      exposureWaitTimer = setTimeout(function() {
        exposureWaitTimer = void 0;
        onFlush();
      }, exposureDuration + EXPOSURE_SNAPSHOT_FLUSH_BUFFER_MS);
    };
    var scheduleQuietTimer = function() {
      if (snapshotTaken) {
        return;
      }
      if (quietTimer) {
        clearTimeout(quietTimer);
      }
      quietTimer = setTimeout(takeSnapshot, quietMs);
    };
    var arm = function() {
      clearTimers();
      removeDomContentLoadedListener();
      snapshotTaken = false;
      scheduleQuietTimer();
      maxWaitTimer = setTimeout(takeSnapshot, maxWaitMs);
    };
    return {
      start: function() {
        if (mutationSubscription) {
          return;
        }
        mutationSubscription = mutationObservable.subscribe(function() {
          scheduleQuietTimer();
        });
        arm();
      },
      reset: function() {
        arm();
      },
      stop: function() {
        mutationSubscription === null || mutationSubscription === void 0 ? void 0 : mutationSubscription.unsubscribe();
        mutationSubscription = void 0;
        clearTimers();
        removeDomContentLoadedListener();
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture-plugin.js
  var ObservablesEnum;
  (function(ObservablesEnum3) {
    ObservablesEnum3["ClickObservable"] = "clickObservable";
    ObservablesEnum3["PointerDownObservable"] = "pointerDownObservable";
    ObservablesEnum3["ChangeObservable"] = "changeObservable";
    ObservablesEnum3["NavigateObservable"] = "navigateObservable";
    ObservablesEnum3["MutationObservable"] = "mutationObservable";
    ObservablesEnum3["ScrollObservable"] = "scrollObservable";
    ObservablesEnum3["ExposureObservable"] = "exposureObservable";
    ObservablesEnum3["BrowserErrorObservable"] = "browserErrorObservable";
    ObservablesEnum3["SelectionObservable"] = "selectionObservable";
    ObservablesEnum3["MouseMoveObservable"] = "mouseMoveObservable";
  })(ObservablesEnum || (ObservablesEnum = {}));
  var autocapturePlugin = function(options, context) {
    var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m;
    if (options === void 0) {
      options = {};
    }
    context === null || context === void 0 ? void 0 : context.diagnosticsClient.setTag("plugin.autocapture.version", VERSION2);
    var _o = options.dataAttributePrefix, dataAttributePrefix = _o === void 0 ? DEFAULT_DATA_ATTRIBUTE_PREFIX : _o, _p = options.visualTaggingOptions, visualTaggingOptions = _p === void 0 ? {
      enabled: true
    } : _p;
    options.cssSelectorAllowlist = (_a4 = options.cssSelectorAllowlist) !== null && _a4 !== void 0 ? _a4 : DEFAULT_CSS_SELECTOR_ALLOWLIST;
    options.actionClickAllowlist = (_b = options.actionClickAllowlist) !== null && _b !== void 0 ? _b : DEFAULT_ACTION_CLICK_ALLOWLIST;
    options.debounceTime = (_c = options.debounceTime) !== null && _c !== void 0 ? _c : 0;
    var isViewportContentUpdatedEnabled = ((_d = options.viewportContentUpdated) === null || _d === void 0 ? void 0 : _d.enabled) !== false;
    var resolvedExposureDuration = (_g = (_f = (_e = options.viewportContentUpdated) === null || _e === void 0 ? void 0 : _e.exposureDuration) !== null && _f !== void 0 ? _f : options.exposureDuration) !== null && _g !== void 0 ? _g : DEFAULT_EXPOSURE_DURATION;
    options.viewportContentUpdated = __assign(__assign({}, options.viewportContentUpdated), { exposureDuration: resolvedExposureDuration });
    options.pageUrlExcludelist = (_h = options.pageUrlExcludelist) === null || _h === void 0 ? void 0 : _h.reduce(function(acc, excludePattern) {
      if (typeof excludePattern === "string") {
        acc.push(excludePattern);
      }
      if (excludePattern instanceof RegExp) {
        acc.push(excludePattern);
      }
      if (typeof excludePattern === "object" && excludePattern !== null && "pattern" in excludePattern) {
        try {
          acc.push(new RegExp(excludePattern.pattern));
        } catch (regexError) {
          console.warn("Invalid regex pattern: ".concat(excludePattern.pattern), regexError);
          return acc;
        }
      }
      return acc;
    }, []);
    var name = PLUGIN_NAME;
    var type = "enrichment";
    var subscriptions = [];
    var dataExtractor = new DataExtractor(options, context);
    var elementExposedForPage = /* @__PURE__ */ new Set();
    var elementExposedInSentEvents = /* @__PURE__ */ new Set();
    var currentElementExposed = /* @__PURE__ */ new Set();
    var beforeUnloadCleanup;
    var rescanElementsForExposure;
    var createObservables = function() {
      var _a5;
      var clickObservable = multicast(createClickObservable().map(function(click) {
        return dataExtractor.addAdditionalEventProperties(click, "click", options.cssSelectorAllowlist, dataAttributePrefix);
      }));
      var changeObservable = multicast(new import_zen_observable.default(function(observer) {
        var _a6;
        var handler = function(changeEvent) {
          var enrichedChangeEvent = dataExtractor.addAdditionalEventProperties(changeEvent, "change", options.cssSelectorAllowlist, dataAttributePrefix);
          observer.next(enrichedChangeEvent);
        };
        (_a6 = getGlobalScope()) === null || _a6 === void 0 ? void 0 : _a6.document.addEventListener("change", handler, { capture: true });
        return function() {
          var _a7;
          return (_a7 = getGlobalScope()) === null || _a7 === void 0 ? void 0 : _a7.document.removeEventListener("change", handler);
        };
      }));
      var navigateObservable;
      if (window.navigation) {
        navigateObservable = multicast(new import_zen_observable.default(function(observer) {
          var handler = function(navigateEvent) {
            var enrichedNavigateEvent = dataExtractor.addAdditionalEventProperties(navigateEvent, "navigate", options.cssSelectorAllowlist, dataAttributePrefix);
            observer.next(enrichedNavigateEvent);
          };
          window.navigation.addEventListener("navigate", handler);
          return function() {
            window.navigation.removeEventListener("navigate", handler);
          };
        }));
      }
      var shadowGate = dataExtractor.shadowGate;
      var mutationObservable = multicast(createMutationObservable(shadowGate).map(function(mutation) {
        return dataExtractor.addAdditionalEventProperties(mutation, "mutation", options.cssSelectorAllowlist, dataAttributePrefix);
      }));
      var scrollObservable = createScrollObservable();
      var exposureObservable = createExposureObservable(mutationObservable, options.cssSelectorAllowlist, function(rescan) {
        rescanElementsForExposure = rescan;
      }, shadowGate);
      return _a5 = {}, _a5[ObservablesEnum.ChangeObservable] = changeObservable, // [ObservablesEnum.ErrorObservable]: errorObservable,
      _a5[ObservablesEnum.ClickObservable] = clickObservable, _a5[ObservablesEnum.MutationObservable] = mutationObservable, _a5[ObservablesEnum.NavigateObservable] = navigateObservable, _a5[ObservablesEnum.ScrollObservable] = scrollObservable, _a5[ObservablesEnum.ExposureObservable] = exposureObservable, _a5;
    };
    var groupedLabeledEvents = groupLabeledEventIdsByEventType(Object.values((_k = (_j = options.pageActions) === null || _j === void 0 ? void 0 : _j.labeledEvents) !== null && _k !== void 0 ? _k : {}));
    var labeledEventToTriggerMap = createLabeledEventToTriggerMap((_m = (_l = options.pageActions) === null || _l === void 0 ? void 0 : _l.triggers) !== null && _m !== void 0 ? _m : []);
    var evaluateTriggers = createTriggerEvaluator(groupedLabeledEvents, labeledEventToTriggerMap, dataExtractor, options);
    var recomputePageActionsData = function(remotePageActions) {
      var _a5, _b2;
      if (remotePageActions) {
        options.pageActions = __assign(__assign({}, options.pageActions), remotePageActions);
        groupedLabeledEvents = groupLabeledEventIdsByEventType(Object.values((_a5 = options.pageActions.labeledEvents) !== null && _a5 !== void 0 ? _a5 : {}));
        labeledEventToTriggerMap = createLabeledEventToTriggerMap((_b2 = options.pageActions.triggers) !== null && _b2 !== void 0 ? _b2 : []);
        evaluateTriggers.update(groupedLabeledEvents, labeledEventToTriggerMap, options);
      }
    };
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var pageViewEndFired, elementSelectorConfigCleanup, shouldTrackEvent, shouldTrackActionClick, allObservables, clickTrackingSubscription, changeSubscription, actionClickSubscription, scrollTracker, lastScroll, trackers, globalScope3, initialExposureSnapshotScheduler, handleViewportContentUpdated, trackedPageUrl, handleSpaNavigation, handleHistoryStateChange, handleExposure, beforeUnloadHandler_1, navigateObservable, popstateHandler_1, originalPushState_1, originalReplaceState_1, allowlist, actionClickAllowlist, messenger;
        var _a5;
        return __generator(this, function(_b2) {
          if (typeof document === "undefined") {
            return [
              2
              /*return*/
            ];
          }
          pageViewEndFired = false;
          if (config2.fetchRemoteConfig) {
            if (!config2.remoteConfigClient) {
              config2.loggerProvider.debug("Remote config client is not provided, skipping remote config fetch");
            } else {
              config2.remoteConfigClient.subscribe("configs.analyticsSDK.pageActions", "all", function(remoteConfig) {
                recomputePageActionsData(remoteConfig);
              });
            }
          }
          elementSelectorConfigCleanup = subscribeToElementSelectorConfig(config2, dataExtractor);
          if (elementSelectorConfigCleanup) {
            subscriptions.push({ unsubscribe: elementSelectorConfigCleanup });
          }
          shouldTrackEvent = createShouldTrackEvent(options, options.cssSelectorAllowlist);
          shouldTrackActionClick = createShouldTrackEvent(options, options.actionClickAllowlist);
          allObservables = createObservables();
          clickTrackingSubscription = trackClicks({
            allObservables,
            amplitude,
            shouldTrackEvent,
            evaluateTriggers: evaluateTriggers.evaluate.bind(evaluateTriggers)
          });
          subscriptions.push(clickTrackingSubscription);
          changeSubscription = trackChange({
            allObservables,
            getEventProperties: function() {
              var args = [];
              for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
              }
              return dataExtractor.getEventProperties.apply(dataExtractor, __spreadArray(__spreadArray([], __read(args), false), [dataAttributePrefix], false));
            },
            amplitude,
            shouldTrackEvent,
            evaluateTriggers: evaluateTriggers.evaluate.bind(evaluateTriggers)
          });
          subscriptions.push(changeSubscription);
          actionClickSubscription = trackActionClick({
            allObservables,
            options,
            getEventProperties: function() {
              var args = [];
              for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
              }
              return dataExtractor.getEventProperties.apply(dataExtractor, __spreadArray(__spreadArray([], __read(args), false), [dataAttributePrefix], false));
            },
            amplitude,
            shouldTrackEvent,
            shouldTrackActionClick
          });
          if (actionClickSubscription) {
            subscriptions.push(actionClickSubscription);
          }
          scrollTracker = trackScroll({
            allObservables,
            amplitude
          });
          subscriptions.push(scrollTracker);
          lastScroll = __assign({}, scrollTracker.getState());
          trackers = {};
          globalScope3 = getGlobalScope();
          handleViewportContentUpdated = function(isPageEnd) {
            if (isPageEnd) {
              initialExposureSnapshotScheduler === null || initialExposureSnapshotScheduler === void 0 ? void 0 : initialExposureSnapshotScheduler.reset();
            }
            if (isPageEnd && pageViewEndFired) {
              return;
            }
            if (isPageEnd) {
              pageViewEndFired = true;
              setTimeout(function() {
                pageViewEndFired = false;
              }, 100);
            }
            fireViewportContentUpdated({
              amplitude,
              scrollTracker,
              currentElementExposed,
              elementExposedForPage,
              elementExposedInSentEvents,
              exposureTracker: trackers.exposure,
              isPageEnd,
              lastScroll
            });
          };
          trackedPageUrl = getNormalizedPageUrl(globalScope3);
          handleSpaNavigation = function() {
            var currentPageUrl = getNormalizedPageUrl(globalScope3);
            if (currentPageUrl === trackedPageUrl) {
              return;
            }
            trackedPageUrl = currentPageUrl;
            handleViewportContentUpdated(true);
          };
          handleHistoryStateChange = function(applyHistoryChange, nextUrl) {
            var previousPageUrl = getNormalizedPageUrl(globalScope3);
            var nextPageUrl = resolveHistoryNavigationUrl(nextUrl, globalScope3);
            if (previousPageUrl === nextPageUrl) {
              applyHistoryChange();
              return;
            }
            trackedPageUrl = nextPageUrl;
            handleViewportContentUpdated(true);
            applyHistoryChange();
          };
          handleExposure = function(elementPath) {
            onExposure(elementPath, elementExposedForPage, elementExposedInSentEvents, currentElementExposed, handleViewportContentUpdated);
          };
          if (isViewportContentUpdatedEnabled) {
            trackers.exposure = trackExposure({
              allObservables,
              onExposure: handleExposure,
              dataExtractor,
              exposureDuration: resolvedExposureDuration
            });
            if (trackers.exposure) {
              subscriptions.push(trackers.exposure);
            }
            initialExposureSnapshotScheduler = createInitialExposureSnapshotScheduler({
              mutationObservable: allObservables[ObservablesEnum.MutationObservable],
              onRescan: function() {
                var _a6, _b3;
                (_b3 = (_a6 = config2.loggerProvider) === null || _a6 === void 0 ? void 0 : _a6.debug) === null || _b3 === void 0 ? void 0 : _b3.call(_a6, "@amplitude/plugin-autocapture-browser: initial exposure snapshot \u2014 rescanning DOM");
                rescanElementsForExposure === null || rescanElementsForExposure === void 0 ? void 0 : rescanElementsForExposure();
              },
              onFlush: function() {
                var _a6, _b3;
                (_b3 = (_a6 = config2.loggerProvider) === null || _a6 === void 0 ? void 0 : _a6.debug) === null || _b3 === void 0 ? void 0 : _b3.call(_a6, "@amplitude/plugin-autocapture-browser: initial exposure snapshot \u2014 flushing viewport content updated");
                handleViewportContentUpdated(false);
              },
              exposureDuration: resolvedExposureDuration
            });
            initialExposureSnapshotScheduler.start();
            subscriptions.push({
              unsubscribe: function() {
                initialExposureSnapshotScheduler === null || initialExposureSnapshotScheduler === void 0 ? void 0 : initialExposureSnapshotScheduler.stop();
              }
            });
            beforeUnloadHandler_1 = function() {
              handleViewportContentUpdated(true);
            };
            globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.addEventListener("beforeunload", beforeUnloadHandler_1);
            beforeUnloadCleanup = function() {
              globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.removeEventListener("beforeunload", beforeUnloadHandler_1);
            };
            subscriptions.push({ unsubscribe: function() {
              return beforeUnloadCleanup();
            } });
            navigateObservable = allObservables[ObservablesEnum.NavigateObservable];
            if (navigateObservable) {
              subscriptions.push(navigateObservable.subscribe(function(timestampedEvent) {
                var _a6, _b3;
                var destinationUrl = (_b3 = (_a6 = timestampedEvent.event) === null || _a6 === void 0 ? void 0 : _a6.destination) === null || _b3 === void 0 ? void 0 : _b3.url;
                if (destinationUrl) {
                  var nextPageUrl = normalizePageUrl(destinationUrl);
                  if (nextPageUrl === trackedPageUrl) {
                    return;
                  }
                  trackedPageUrl = nextPageUrl;
                  handleViewportContentUpdated(true);
                  return;
                }
                handleSpaNavigation();
              }));
            } else if (globalScope3) {
              popstateHandler_1 = function() {
                handleSpaNavigation();
              };
              globalScope3.addEventListener("popstate", popstateHandler_1);
              originalPushState_1 = globalScope3.history.pushState;
              originalReplaceState_1 = globalScope3.history.replaceState;
              if (globalScope3.history && originalPushState_1) {
                globalScope3.history.pushState = new Proxy(originalPushState_1, {
                  apply: function(target, thisArg, args) {
                    handleHistoryStateChange(function() {
                      target.apply(thisArg, args);
                    }, args[2]);
                  }
                });
              }
              if (globalScope3.history && originalReplaceState_1) {
                globalScope3.history.replaceState = new Proxy(originalReplaceState_1, {
                  apply: function(target, thisArg, args) {
                    handleHistoryStateChange(function() {
                      target.apply(thisArg, args);
                    }, args[2]);
                  }
                });
              }
              subscriptions.push({
                unsubscribe: function() {
                  globalScope3.removeEventListener("popstate", popstateHandler_1);
                  if (globalScope3.history && originalPushState_1) {
                    globalScope3.history.pushState = originalPushState_1;
                  }
                  if (globalScope3.history && originalReplaceState_1) {
                    globalScope3.history.replaceState = originalReplaceState_1;
                  }
                }
              });
            }
          }
          (_a5 = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider) === null || _a5 === void 0 ? void 0 : _a5.log("".concat(name, " has been successfully added."));
          if (window.opener && visualTaggingOptions.enabled) {
            allowlist = options.cssSelectorAllowlist;
            actionClickAllowlist = options.actionClickAllowlist;
            messenger = getOrCreateWindowMessenger();
            enableVisualTagging(messenger, {
              dataExtractor,
              isElementSelectable: createShouldTrackEvent(options, __spreadArray(__spreadArray([], __read(allowlist), false), __read(actionClickAllowlist), false)),
              cssSelectorAllowlist: allowlist,
              actionClickAllowlist
            });
            enableBackgroundCapture(messenger);
            messenger.setup(__assign({ logger: config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider }, (config2 === null || config2 === void 0 ? void 0 : config2.serverZone) && { endpoint: AMPLITUDE_ORIGINS_MAP[config2.serverZone] }));
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a5) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var subscriptions_1, subscriptions_1_1, subscription2;
        var e_1, _a5;
        return __generator(this, function(_b2) {
          try {
            for (subscriptions_1 = __values(subscriptions), subscriptions_1_1 = subscriptions_1.next(); !subscriptions_1_1.done; subscriptions_1_1 = subscriptions_1.next()) {
              subscription2 = subscriptions_1_1.value;
              subscription2.unsubscribe();
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (subscriptions_1_1 && !subscriptions_1_1.done && (_a5 = subscriptions_1.return)) _a5.call(subscriptions_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/frustration-plugin.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-dead-click.js
  init_tslib_es6();
  init_esm();
  var DEAD_CLICK_TIMEOUT = 3e3;
  var CHANGE_EVENTS = ["mutation", "navigate"];
  function trackDeadClick(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, getEventProperties = _a4.getEventProperties, shouldTrackDeadClick = _a4.shouldTrackDeadClick;
    var clickObservable = allObservables.clickObservable, mutationObservable = allObservables.mutationObservable, navigateObservable = allObservables.navigateObservable;
    var filteredClickObservable = clickObservable.filter(function(click) {
      return filterOutNonTrackableEvents(click) && shouldTrackDeadClick("click", click.closestTrackedAncestor) && click.event.target instanceof Element && click.event.target.closest('a[target="_blank"]') === null && click.event.button === MouseButton.LEFT_OR_TOUCH_CONTACT;
    });
    var changeObservables = navigateObservable ? merge(mutationObservable, navigateObservable) : mutationObservable;
    var clicksAndChangeObservable = merge(filteredClickObservable, changeObservables);
    var deadClickTimer = null;
    var deadClickObservable = asyncMap(clicksAndChangeObservable, function(event) {
      if (deadClickTimer && CHANGE_EVENTS.includes(event.type)) {
        clearTimeout(deadClickTimer);
        deadClickTimer = null;
        return Promise.resolve(null);
      } else if (event.type === "click") {
        if (deadClickTimer) {
          return Promise.resolve(null);
        }
        return new Promise(function(resolve) {
          deadClickTimer = setTimeout(function() {
            resolve(event);
            deadClickTimer = null;
          }, DEAD_CLICK_TIMEOUT);
        });
      }
      return Promise.resolve(null);
    });
    return deadClickObservable.subscribe(function(actionClick) {
      if (!actionClick)
        return;
      var deadClickEvent = {
        "[Amplitude] X": actionClick.event.clientX,
        "[Amplitude] Y": actionClick.event.clientY
      };
      amplitude.track(AMPLITUDE_ELEMENT_DEAD_CLICKED_EVENT, __assign(__assign({}, getEventProperties("click", actionClick.closestTrackedAncestor)), deadClickEvent), { time: actionClick.timestamp });
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-rage-click.js
  init_tslib_es6();
  init_esm();
  var RAGE_CLICK_THRESHOLD = DEFAULT_RAGE_CLICK_THRESHOLD;
  var RAGE_CLICK_WINDOW_MS = DEFAULT_RAGE_CLICK_WINDOW_MS;
  var RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD = DEFAULT_RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD;
  function addCoordinates(regionBox, click) {
    var _a4, _b, _c, _d;
    var _e = click.event, pageX = _e.pageX, pageY = _e.pageY;
    regionBox.yMin = Math.min((_a4 = regionBox.yMin) !== null && _a4 !== void 0 ? _a4 : pageY, pageY);
    regionBox.yMax = Math.max((_b = regionBox.yMax) !== null && _b !== void 0 ? _b : pageY, pageY);
    regionBox.xMin = Math.min((_c = regionBox.xMin) !== null && _c !== void 0 ? _c : pageX, pageX);
    regionBox.xMax = Math.max((_d = regionBox.xMax) !== null && _d !== void 0 ? _d : pageX, pageX);
    regionBox.isOutOfBounds = regionBox.yMax - regionBox.yMin > RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD || regionBox.xMax - regionBox.xMin > RAGE_CLICK_OUT_OF_BOUNDS_THRESHOLD;
  }
  function getRageClickAnalyticsEvent(clickWindow) {
    if (clickWindow.length === 0) {
      return null;
    }
    var firstClick = clickWindow[0];
    var lastClick = clickWindow[clickWindow.length - 1];
    var rageClickEvent = __assign({ "[Amplitude] Begin Time": new Date(firstClick.timestamp).toISOString(), "[Amplitude] End Time": new Date(lastClick.timestamp).toISOString(), "[Amplitude] Duration": lastClick.timestamp - firstClick.timestamp, "[Amplitude] Clicks": clickWindow.map(function(click) {
      return {
        X: click.event.pageX,
        Y: click.event.pageY,
        Time: click.timestamp
      };
    }), "[Amplitude] Click Count": clickWindow.length }, firstClick.targetElementProperties);
    return { rageClickEvent, time: firstClick.timestamp };
  }
  function isClickOutsideRageClickWindow(clickWindow, click) {
    var firstIndex = Math.max(0, clickWindow.length - RAGE_CLICK_THRESHOLD + 1);
    var firstClick = clickWindow[firstIndex];
    return click.timestamp - firstClick.timestamp >= RAGE_CLICK_WINDOW_MS;
  }
  function isNewElement(clickWindow, click) {
    return clickWindow.length > 0 && clickWindow[clickWindow.length - 1].closestTrackedAncestor !== click.closestTrackedAncestor;
  }
  function trackRageClicks(_a4) {
    var _this = this;
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, shouldTrackRageClick = _a4.shouldTrackRageClick;
    var pointerDownObservable = allObservables.pointerDownObservable, selectionObservable = allObservables.selectionObservable;
    var clickWindow = [];
    var clickBoundingBox = {};
    var pendingRageClick = null;
    function resetClickWindow(click) {
      clickWindow = [];
      clickBoundingBox = {};
      if (click) {
        addCoordinates(clickBoundingBox, click);
        clickWindow.push(click);
      }
    }
    var rageClickObservable = asyncMap(pointerDownObservable.filter(function(click) {
      return shouldTrackRageClick("click", click.closestTrackedAncestor);
    }), function(click) {
      return __awaiter(_this, void 0, void 0, function() {
        var resolutionValue;
        return __generator(this, function(_a5) {
          addCoordinates(clickBoundingBox, click);
          resolutionValue = null;
          if (clickWindow.length === 0 || isNewElement(clickWindow, click) || isClickOutsideRageClickWindow(clickWindow, click) || clickBoundingBox.isOutOfBounds) {
            if (pendingRageClick) {
              resolutionValue = getRageClickAnalyticsEvent(clickWindow);
            }
            resetClickWindow(click);
          } else {
            clickWindow.push(click);
          }
          if (pendingRageClick) {
            clearTimeout(pendingRageClick.timerId);
            pendingRageClick.resolve(resolutionValue);
            pendingRageClick = null;
          }
          if (clickWindow.length >= RAGE_CLICK_THRESHOLD) {
            return [2, new Promise(function(resolve) {
              pendingRageClick = {
                resolve,
                timerId: setTimeout(function() {
                  resolve(getRageClickAnalyticsEvent(clickWindow));
                }, RAGE_CLICK_WINDOW_MS)
              };
            })];
          }
          return [2, null];
        });
      });
    });
    var selectionSubscription = selectionObservable === null || selectionObservable === void 0 ? void 0 : selectionObservable.subscribe(function() {
      resetClickWindow();
    });
    var rageClickSubscription = rageClickObservable.subscribe(function(data) {
      if (data === null) {
        return;
      }
      amplitude.track(AMPLITUDE_ELEMENT_RAGE_CLICKED_EVENT, data.rageClickEvent, { time: data.time });
    });
    return {
      unsubscribe: function() {
        rageClickSubscription.unsubscribe();
        selectionSubscription === null || selectionSubscription === void 0 ? void 0 : selectionSubscription.unsubscribe();
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-error-click.js
  init_tslib_es6();
  init_esm();
  var ERROR_CLICK_TIMEOUT = 2e3;
  function trackErrorClicks(_a4) {
    var amplitude = _a4.amplitude, allObservables = _a4.allObservables, shouldTrackErrorClick = _a4.shouldTrackErrorClick;
    var pointerDownObservable = allObservables.pointerDownObservable, browserErrorObservable = allObservables.browserErrorObservable;
    var filteredClickObservable = pointerDownObservable.filter(function(click) {
      return filterOutNonTrackableEvents(click) && shouldTrackErrorClick("click", click.closestTrackedAncestor) && click.event.target instanceof Element && click.event.target.closest('a[target="_blank"]') === null && click.event.button === MouseButton.LEFT_OR_TOUCH_CONTACT;
    });
    var errorClickTimer = null;
    var latestClickEvent = null;
    var clearClickTimer = function() {
      if (errorClickTimer !== null) {
        clearTimeout(errorClickTimer);
        errorClickTimer = null;
      }
      latestClickEvent = null;
    };
    return merge(filteredClickObservable, browserErrorObservable).subscribe(function(event) {
      var _a5;
      if (event.type === "click") {
        clearClickTimer();
        latestClickEvent = event;
        errorClickTimer = setTimeout(clearClickTimer, ERROR_CLICK_TIMEOUT);
        return;
      }
      if (event.type === "error" && latestClickEvent) {
        amplitude.track(AMPLITUDE_ELEMENT_ERROR_CLICKED_EVENT, __assign((_a5 = {}, _a5["[Amplitude] Kind"] = event.event.kind, _a5["[Amplitude] Message"] = event.event.message, _a5["[Amplitude] Stack"] = event.event.stack, _a5["[Amplitude] Filename"] = event.event.filename, _a5["[Amplitude] Line Number"] = event.event.lineNumber, _a5["[Amplitude] Column Number"] = event.event.columnNumber, _a5), latestClickEvent.targetElementProperties));
        clearClickTimer();
      }
    });
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-thrashed-cursor.js
  init_esm();
  var Direction;
  (function(Direction2) {
    Direction2["INCREASING"] = "increasing";
    Direction2["DECREASING"] = "decreasing";
  })(Direction || (Direction = {}));
  var Axis;
  (function(Axis2) {
    Axis2["X"] = "x";
    Axis2["Y"] = "y";
  })(Axis || (Axis = {}));
  var createMouseDirectionChangeObservable = function(_a4) {
    var allWindowObservables = _a4.allWindowObservables;
    var mouseMoveObservable = allWindowObservables.mouseMoveObservable;
    return new import_zen_observable.default(function(observer) {
      var lastPosition = null;
      var xDirection = null;
      var yDirection = null;
      return mouseMoveObservable.subscribe(function(event) {
        var currentPosition = { x: event.clientX, y: event.clientY };
        if (lastPosition === null) {
          lastPosition = currentPosition;
          return;
        }
        if (currentPosition.x > lastPosition.x) {
          if (xDirection === Direction.DECREASING) {
            observer.next(Axis.X);
          }
          xDirection = Direction.INCREASING;
        } else if (currentPosition.x < lastPosition.x) {
          if (xDirection === Direction.INCREASING) {
            observer.next(Axis.X);
          }
          xDirection = Direction.DECREASING;
        }
        if (currentPosition.y > lastPosition.y) {
          if (yDirection === Direction.DECREASING) {
            observer.next(Axis.Y);
          }
          yDirection = Direction.INCREASING;
        } else if (currentPosition.y < lastPosition.y) {
          if (yDirection === Direction.INCREASING) {
            observer.next(Axis.Y);
          }
          yDirection = Direction.DECREASING;
        }
        lastPosition = currentPosition;
      });
    });
  };
  function addDirectionChange(directionChangeSeries) {
    var now = +Date.now();
    directionChangeSeries.startTime = directionChangeSeries.startTime || now;
    var changes = directionChangeSeries.changes, changesThreshold = directionChangeSeries.changesThreshold;
    changes.push(now);
    if (changes.length > changesThreshold)
      changes.shift();
  }
  function isThrashedCursor(directionChanges) {
    var changes = directionChanges.changes, changesThreshold = directionChanges.changesThreshold, thresholdMs = directionChanges.thresholdMs;
    if (changes.length < changesThreshold)
      return false;
    var delta = changes[changes.length - 1] - changes[0];
    return delta < thresholdMs;
  }
  function resetDirectionChangeSeries(directionChangeSeries) {
    directionChangeSeries.changes = [];
    directionChangeSeries.startTime = void 0;
  }
  function adjustWindow(directionChanges) {
    var changes = directionChanges.changes, thresholdMs = directionChanges.thresholdMs;
    var leftPtr = 0;
    var lastChange = changes[changes.length - 1];
    for (; leftPtr < changes.length; leftPtr++) {
      var delta = lastChange - changes[leftPtr];
      if (delta < thresholdMs) {
        break;
      }
    }
    if (leftPtr === 0)
      return;
    directionChanges.startTime = changes[leftPtr];
    directionChanges.changes.splice(0, leftPtr);
  }
  function getPendingThrashedCursor(directionChangesX, directionChangesY) {
    var startTime = void 0;
    if (isThrashedCursor(directionChangesX)) {
      startTime = directionChangesX.startTime;
    }
    if (isThrashedCursor(directionChangesY)) {
      var startTimeY = directionChangesY.startTime;
      if (startTimeY && (!startTime || startTimeY < startTime)) {
        startTime = startTimeY;
      }
    }
    return startTime;
  }
  var DEFAULT_THRESHOLD = 20;
  var DEFAULT_WINDOW_MS = 2e3;
  var createThrashedCursorObservable = function(_a4) {
    var mouseDirectionChangeObservable = _a4.mouseDirectionChangeObservable, _b = _a4.directionChanges, directionChanges = _b === void 0 ? DEFAULT_THRESHOLD : _b, _c = _a4.thresholdMs, thresholdMs = _c === void 0 ? DEFAULT_WINDOW_MS : _c;
    return new import_zen_observable.default(function(observer) {
      var xDirectionChanges = { changes: [], changesThreshold: directionChanges, thresholdMs };
      var yDirectionChanges = { changes: [], changesThreshold: directionChanges, thresholdMs };
      var pendingThrashedCursor = void 0;
      var timer = null;
      function emitPendingThrashedCursor() {
        if (pendingThrashedCursor !== void 0) {
          observer.next(pendingThrashedCursor);
          pendingThrashedCursor = void 0;
          if (timer !== null)
            clearTimeout(timer);
          resetDirectionChangeSeries(xDirectionChanges);
          resetDirectionChangeSeries(yDirectionChanges);
        }
      }
      return mouseDirectionChangeObservable.subscribe(function(axis) {
        if (timer !== null)
          clearTimeout(timer);
        addDirectionChange(axis === Axis.X ? xDirectionChanges : yDirectionChanges);
        var nextPendingThrashedCursor = getPendingThrashedCursor(xDirectionChanges, yDirectionChanges);
        if (nextPendingThrashedCursor) {
          pendingThrashedCursor = pendingThrashedCursor || nextPendingThrashedCursor;
          timer = setTimeout(function() {
            emitPendingThrashedCursor();
            timer = null;
          }, thresholdMs);
        } else {
          emitPendingThrashedCursor();
        }
        adjustWindow(xDirectionChanges);
        adjustWindow(yDirectionChanges);
        return function() {
          if (timer !== null) {
            clearTimeout(timer);
            timer = null;
          }
        };
      });
    });
  };
  var trackThrashedCursor = function(_a4) {
    var amplitude = _a4.amplitude, options = _a4.options, allObservables = _a4.allObservables, _b = _a4.directionChanges, directionChanges = _b === void 0 ? DEFAULT_THRESHOLD : _b, _c = _a4.thresholdMs, thresholdMs = _c === void 0 ? DEFAULT_WINDOW_MS : _c;
    var mouseDirectionChangeObservable = createMouseDirectionChangeObservable({ allWindowObservables: allObservables });
    var thrashedCursorObservable = createThrashedCursorObservable({
      mouseDirectionChangeObservable,
      directionChanges,
      thresholdMs
    });
    return thrashedCursorObservable.subscribe(function(time) {
      if (!isUrlAllowed(options)) {
        return;
      }
      amplitude.track(AMPLITUDE_THRASHED_CURSOR_EVENT, void 0, { time });
    });
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/frustration-plugin.js
  function getCssSelectorAllowlist(options, attribute, defaultAllowlist, enabled) {
    if (!enabled) {
      return [];
    }
    var config2 = options[attribute];
    if (typeof config2 === "object" && config2 !== null && "cssSelectorAllowlist" in config2 && Array.isArray(config2.cssSelectorAllowlist)) {
      return config2.cssSelectorAllowlist;
    }
    return defaultAllowlist;
  }
  var MINIMUM_THRASHED_CURSOR_DIRECTION_CHANGES = 5;
  var MAXIMUM_THRASHED_CURSOR_THRESHOLD = 4e3;
  var frustrationPlugin = function(options) {
    var _a4;
    if (options === void 0) {
      options = {};
    }
    var name = FRUSTRATION_PLUGIN_NAME;
    var type = "enrichment";
    var subscriptions = [];
    var isErrorClicksEnabled = options.errorClicks !== false;
    if (!options.errorClicks) {
      isErrorClicksEnabled = false;
    }
    var deadClicksEnabled = options.deadClicks !== false && options.deadClicks !== null;
    var rageClicksEnabled = options.rageClicks !== false && options.rageClicks !== null;
    var thrashedCursorEnabled = options.thrashedCursor !== false && options.thrashedCursor !== null;
    if (!options.thrashedCursor) {
      thrashedCursorEnabled = false;
    }
    var rageCssSelectors = getCssSelectorAllowlist(options, "rageClicks", DEFAULT_RAGE_CLICK_ALLOWLIST, rageClicksEnabled);
    var deadCssSelectors = getCssSelectorAllowlist(options, "deadClicks", DEFAULT_DEAD_CLICK_ALLOWLIST, deadClicksEnabled);
    var errorCssSelectors = getCssSelectorAllowlist(options, "errorClicks", DEFAULT_ERROR_CLICK_ALLOWLIST, isErrorClicksEnabled);
    var dataAttributePrefix = (_a4 = options.dataAttributePrefix) !== null && _a4 !== void 0 ? _a4 : DEFAULT_DATA_ATTRIBUTE_PREFIX;
    var dataExtractor = new DataExtractor(options);
    var combinedCssSelectors = __spreadArray([], __read(new Set(__spreadArray(__spreadArray(__spreadArray([], __read(rageCssSelectors), false), __read(deadCssSelectors), false), __read(errorCssSelectors), false))), false);
    var createObservables = function() {
      var _a5;
      var createEnrichedClickObservable = function(clickType) {
        return multicast(createClickObservable(clickType).map(function(click) {
          return dataExtractor.addAdditionalEventProperties(click, "click", combinedCssSelectors, dataAttributePrefix, true);
        }));
      };
      var clickObservable = createEnrichedClickObservable("click");
      var pointerDownObservable = createEnrichedClickObservable("pointerdown");
      var browserErrorObservables = multicast(createErrorObservable().map(function(error) {
        return dataExtractor.addTypeAndTimestamp(error, "error");
      }));
      var enrichedMutationObservable = multicast(createMutationObservable(dataExtractor.shadowGate).map(function(mutation) {
        return dataExtractor.addAdditionalEventProperties(mutation, "mutation", combinedCssSelectors, dataAttributePrefix);
      }));
      var enrichedNavigateObservable;
      if (window.navigation) {
        var navigateObservable = new import_zen_observable.default(function(observer) {
          var handler = function(event) {
            observer.next(__assign(__assign({}, event), { type: "navigate" }));
          };
          window.navigation.addEventListener("navigate", handler);
          return function() {
            window.navigation.removeEventListener("navigate", handler);
          };
        });
        enrichedNavigateObservable = multicast(navigateObservable.map(function(navigate) {
          return dataExtractor.addAdditionalEventProperties(navigate, "navigate", combinedCssSelectors, dataAttributePrefix);
        }));
      }
      var selectionObservable = multicast(new import_zen_observable.default(function(observer) {
        var handler = function() {
          var el = document.activeElement;
          if (el && (el.tagName === "TEXTAREA" || el.tagName === "INPUT")) {
            var start3 = void 0;
            var end = void 0;
            try {
              start3 = el.selectionStart;
              end = el.selectionEnd;
              if (start3 === end)
                return;
            } catch (error) {
              return;
            }
            return observer.next();
          }
          var selection = window.getSelection();
          if (!selection || selection.isCollapsed)
            return;
          return observer.next();
        };
        window.document.addEventListener("selectionchange", handler);
        return function() {
          window.document.removeEventListener("selectionchange", handler);
        };
      }));
      var mouseMoveObservable = multicast(createMouseMoveObservable());
      return _a5 = {}, _a5[ObservablesEnum.ClickObservable] = clickObservable, _a5[ObservablesEnum.PointerDownObservable] = pointerDownObservable, _a5[ObservablesEnum.MutationObservable] = enrichedMutationObservable, _a5[ObservablesEnum.NavigateObservable] = enrichedNavigateObservable, _a5[ObservablesEnum.BrowserErrorObservable] = browserErrorObservables, _a5[ObservablesEnum.SelectionObservable] = selectionObservable, _a5[ObservablesEnum.MouseMoveObservable] = mouseMoveObservable, _a5;
    };
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var allObservables, elementSelectorConfigCleanup, shouldTrackRageClick, rageClickSubscription, shouldTrackDeadClick, deadClickSubscription, shouldTrackErrorClick, errorClickSubscription, directionChanges, thresholdMs, thrashedCursorSubscription;
        var _a5;
        return __generator(this, function(_b) {
          if (typeof document === "undefined") {
            return [
              2
              /*return*/
            ];
          }
          allObservables = createObservables();
          elementSelectorConfigCleanup = subscribeToElementSelectorConfig(config2, dataExtractor);
          if (elementSelectorConfigCleanup) {
            subscriptions.push({ unsubscribe: elementSelectorConfigCleanup });
          }
          if (rageClicksEnabled) {
            shouldTrackRageClick = createShouldTrackEvent(options, rageCssSelectors);
            rageClickSubscription = trackRageClicks({
              allObservables,
              amplitude,
              shouldTrackRageClick
            });
            subscriptions.push(rageClickSubscription);
          }
          if (deadClicksEnabled) {
            shouldTrackDeadClick = createShouldTrackEvent(options, deadCssSelectors);
            deadClickSubscription = trackDeadClick({
              amplitude,
              allObservables,
              getEventProperties: function(actionType, element) {
                return dataExtractor.getEventProperties(actionType, element, dataAttributePrefix);
              },
              shouldTrackDeadClick
            });
            subscriptions.push(deadClickSubscription);
          }
          if (isErrorClicksEnabled) {
            shouldTrackErrorClick = createShouldTrackEvent(options, errorCssSelectors);
            errorClickSubscription = trackErrorClicks({
              amplitude,
              allObservables,
              shouldTrackErrorClick
            });
            subscriptions.push(errorClickSubscription);
          }
          if (thrashedCursorEnabled) {
            directionChanges = void 0, thresholdMs = void 0;
            if (typeof options.thrashedCursor === "object") {
              directionChanges = options.thrashedCursor.directionChanges;
              thresholdMs = options.thrashedCursor.threshold;
              if (directionChanges && directionChanges < MINIMUM_THRASHED_CURSOR_DIRECTION_CHANGES) {
                config2.loggerProvider.warn("'thrashedCursor.directionChanges' of ".concat(directionChanges, " is below the minimum of ").concat(MINIMUM_THRASHED_CURSOR_DIRECTION_CHANGES, ", setting to ").concat(MINIMUM_THRASHED_CURSOR_DIRECTION_CHANGES));
                directionChanges = MINIMUM_THRASHED_CURSOR_DIRECTION_CHANGES;
              }
              if (thresholdMs && thresholdMs > MAXIMUM_THRASHED_CURSOR_THRESHOLD) {
                config2.loggerProvider.warn("'thrashedCursor.threshold' of ".concat(thresholdMs, " is above the maximum of ").concat(MAXIMUM_THRASHED_CURSOR_THRESHOLD, ", setting to ").concat(MAXIMUM_THRASHED_CURSOR_THRESHOLD));
                thresholdMs = MAXIMUM_THRASHED_CURSOR_THRESHOLD;
              }
            }
            thrashedCursorSubscription = trackThrashedCursor({
              amplitude,
              options,
              allObservables,
              directionChanges,
              thresholdMs
            });
            subscriptions.push(thrashedCursorSubscription);
          }
          (_a5 = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider) === null || _a5 === void 0 ? void 0 : _a5.log("".concat(name, " has been successfully added."));
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a5) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var subscriptions_1, subscriptions_1_1, subscription2;
        var e_1, _a5;
        return __generator(this, function(_b) {
          try {
            for (subscriptions_1 = __values(subscriptions), subscriptions_1_1 = subscriptions_1.next(); !subscriptions_1_1.done; subscriptions_1_1 = subscriptions_1.next()) {
              subscription2 = subscriptions_1_1.value;
              subscription2.unsubscribe();
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (subscriptions_1_1 && !subscriptions_1_1.done && (_a5 = subscriptions_1.return)) _a5.call(subscriptions_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/performance-plugin.js
  init_tslib_es6();

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/autocapture/track-long-task.js
  init_tslib_es6();
  var DEFAULT_DURATION_THRESHOLD = 100;
  var MEASURE_BUFFER_WINDOW_MS = 1e4;
  function getOverlappingMeasures(entry, measures) {
    var taskEnd = entry.startTime + entry.duration;
    return measures.filter(function(measure) {
      return measure.startTime < taskEnd && measure.startTime + measure.duration > entry.startTime;
    }).map(function(measure) {
      return measure.name;
    });
  }
  function buildLoAFProperties(entry, measures) {
    var _a4;
    var overlappingMeasures = getOverlappingMeasures(entry, measures);
    var scripts = (_a4 = entry.scripts) !== null && _a4 !== void 0 ? _a4 : [];
    var scriptURLs = scripts.map(function(s2) {
      return s2.sourceURL;
    }).filter(Boolean);
    var scriptFunctions = scripts.map(function(s2) {
      return s2.sourceFunctionName;
    }).filter(Boolean);
    var scriptPositions = scripts.map(function(s2) {
      return s2.sourceCharPosition;
    }).filter(function(p2) {
      return typeof p2 === "number" && p2 >= 0;
    });
    var invokerTypes = scripts.map(function(s2) {
      return s2.invokerType;
    }).filter(Boolean);
    var invokers = scripts.map(function(s2) {
      return s2.invoker;
    }).filter(Boolean);
    return __assign(__assign(__assign(__assign(__assign(__assign(__assign({ "[Amplitude] Main Thread Block Source": "long-animation-frame", "[Amplitude] Main Thread Block Duration": entry.duration, "[Amplitude] Main Thread Block Blocking Duration": entry.blockingDuration, "[Amplitude] Main Thread Block Start Time": entry.startTime }, overlappingMeasures.length > 0 && { "[Amplitude] Main Thread Block Measures": overlappingMeasures }), { "[Amplitude] Main Thread Block Render Start": entry.renderStart, "[Amplitude] Main Thread Block Style And Layout Start": entry.styleAndLayoutStart, "[Amplitude] Main Thread Block Script Count": scripts.length }), scriptURLs.length > 0 && { "[Amplitude] Main Thread Block Script URLs": scriptURLs }), scriptFunctions.length > 0 && { "[Amplitude] Main Thread Block Script Functions": scriptFunctions }), scriptPositions.length > 0 && { "[Amplitude] Main Thread Block Script Positions": scriptPositions }), invokerTypes.length > 0 && { "[Amplitude] Main Thread Block Invoker Types": invokerTypes }), invokers.length > 0 && { "[Amplitude] Main Thread Block Invokers": invokers });
  }
  function buildLongTaskProperties(entry, measures) {
    var _a4;
    var overlappingMeasures = getOverlappingMeasures(entry, measures);
    var attribution = (_a4 = entry.attribution) !== null && _a4 !== void 0 ? _a4 : [];
    return __assign(__assign({ "[Amplitude] Main Thread Block Source": "long-task", "[Amplitude] Main Thread Block Duration": entry.duration, "[Amplitude] Main Thread Block Blocking Duration": entry.duration, "[Amplitude] Main Thread Block Start Time": entry.startTime }, overlappingMeasures.length > 0 && { "[Amplitude] Main Thread Block Measures": overlappingMeasures }), attribution.length > 0 && {
      "[Amplitude] Main Thread Block Attribution": attribution.map(function(a2) {
        return a2.name;
      })
    });
  }
  function getSupportedEntryType() {
    if (typeof PerformanceObserver === "undefined")
      return null;
    try {
      var supported = PerformanceObserver.supportedEntryTypes;
      if (supported.includes("long-animation-frame"))
        return "long-animation-frame";
      if (supported.includes("longtask"))
        return "longtask";
    } catch (_a4) {
    }
    return null;
  }
  function trackMainThreadBlock(_a4) {
    var amplitude = _a4.amplitude, options = _a4.options, _b = _a4.durationThreshold, durationThreshold = _b === void 0 ? DEFAULT_DURATION_THRESHOLD : _b;
    var entryType = getSupportedEntryType();
    if (!entryType) {
      return { unsubscribe: function() {
        return void 0;
      } };
    }
    var measures = [];
    var measureObserver = new PerformanceObserver(function(list) {
      var e_1, _a5;
      var now = performance.now();
      try {
        for (var _b2 = __values(list.getEntries()), _c = _b2.next(); !_c.done; _c = _b2.next()) {
          var entry = _c.value;
          measures.push(entry);
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (_c && !_c.done && (_a5 = _b2.return)) _a5.call(_b2);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
      var cutoff = now - MEASURE_BUFFER_WINDOW_MS;
      while (measures.length > 0 && measures[0].startTime < cutoff) {
        measures.shift();
      }
    });
    try {
      measureObserver.observe({ entryTypes: ["measure"] });
    } catch (_c) {
    }
    var blockObserver = new PerformanceObserver(function(list) {
      var e_2, _a5;
      try {
        for (var _b2 = __values(list.getEntries()), _c = _b2.next(); !_c.done; _c = _b2.next()) {
          var entry = _c.value;
          if (!isUrlAllowed(options)) {
            return;
          }
          if (entry.duration < durationThreshold) {
            continue;
          }
          var properties = entryType === "long-animation-frame" ? buildLoAFProperties(entry, measures) : buildLongTaskProperties(entry, measures);
          amplitude.track(AMPLITUDE_MAIN_THREAD_BLOCK_EVENT, properties);
        }
      } catch (e_2_1) {
        e_2 = { error: e_2_1 };
      } finally {
        try {
          if (_c && !_c.done && (_a5 = _b2.return)) _a5.call(_b2);
        } finally {
          if (e_2) throw e_2.error;
        }
      }
    });
    try {
      blockObserver.observe({ entryTypes: [entryType] });
    } catch (_d) {
      measureObserver.disconnect();
      return { unsubscribe: function() {
        return void 0;
      } };
    }
    return {
      unsubscribe: function() {
        blockObserver.disconnect();
        measureObserver.disconnect();
      }
    };
  }

  // node_modules/@amplitude/plugin-autocapture-browser/lib/esm/performance-plugin.js
  var DEFAULT_DURATION_THRESHOLD2 = 100;
  var performancePlugin = function(options) {
    if (options === void 0) {
      options = {};
    }
    var name = PERFORMANCE_PLUGIN_NAME;
    var type = "enrichment";
    var subscriptions = [];
    var mainThreadBlockEnabled = options.mainThreadBlock === true || typeof options.mainThreadBlock === "object" && options.mainThreadBlock !== null;
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var durationThreshold, subscription2;
        var _a4;
        return __generator(this, function(_b) {
          if (typeof document === "undefined") {
            return [
              2
              /*return*/
            ];
          }
          if (mainThreadBlockEnabled) {
            durationThreshold = DEFAULT_DURATION_THRESHOLD2;
            if (typeof options.mainThreadBlock === "object" && options.mainThreadBlock.durationThreshold !== void 0) {
              durationThreshold = options.mainThreadBlock.durationThreshold;
            }
            subscription2 = trackMainThreadBlock({
              amplitude,
              options,
              durationThreshold
            });
            subscriptions.push(subscription2);
          }
          (_a4 = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider) === null || _a4 === void 0 ? void 0 : _a4.log("".concat(name, " performance tracking has been successfully added."));
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var subscriptions_1, subscriptions_1_1, subscription2;
        var e_1, _a4;
        return __generator(this, function(_b) {
          try {
            for (subscriptions_1 = __values(subscriptions), subscriptions_1_1 = subscriptions_1.next(); !subscriptions_1_1.done; subscriptions_1_1 = subscriptions_1.next()) {
              subscription2 = subscriptions_1_1.value;
              subscription2.unsubscribe();
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (subscriptions_1_1 && !subscriptions_1_1.done && (_a4 = subscriptions_1.return)) _a4.call(subscriptions_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/plugin-network-capture-browser/lib/esm/network-capture-plugin.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/plugin-network-capture-browser/lib/esm/constants.js
  var PLUGIN_NAME2 = "@amplitude/plugin-network-capture-browser";
  var AMPLITUDE_NETWORK_REQUEST_EVENT = "[Amplitude] Network Request";
  var IS_HEADER_CAPTURE_EXPERIMENTAL = true;

  // node_modules/@amplitude/plugin-network-capture-browser/lib/esm/network-capture-plugin.js
  init_esm();

  // node_modules/@amplitude/plugin-network-capture-browser/lib/esm/track-network-event.js
  init_tslib_es6();
  init_esm();
  var DEFAULT_STATUS_CODE_RANGE = "500-599";
  function wildcardMatch(str, pattern) {
    var escapedPattern = pattern.replace(/[-[\]{}()+?.,\\^$|#\s]/g, "\\$&");
    var regexPattern = "^" + escapedPattern.replace(/\*/g, ".*") + "$";
    var regex = new RegExp(regexPattern);
    return regex.test(str);
  }
  function isStatusCodeInRange(statusCode, range) {
    var e_1, _a4;
    var ranges = range.split(",");
    try {
      for (var ranges_1 = __values(ranges), ranges_1_1 = ranges_1.next(); !ranges_1_1.done; ranges_1_1 = ranges_1.next()) {
        var r2 = ranges_1_1.value;
        var _b = __read(r2.split("-").map(Number), 2), start3 = _b[0], end = _b[1];
        if (statusCode === start3 && end === void 0) {
          return true;
        }
        if (statusCode >= start3 && statusCode <= end) {
          return true;
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (ranges_1_1 && !ranges_1_1.done && (_a4 = ranges_1.return)) _a4.call(ranges_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return false;
  }
  function isCaptureRuleMatch(rule3, hostname, status, url, method) {
    if (rule3.hosts && !rule3.hosts.find(function(host3) {
      return wildcardMatch(hostname, host3);
    })) {
      return;
    }
    if (url && rule3.urls && !isUrlMatchAllowlist(url, rule3.urls)) {
      return;
    }
    if (method && rule3.methods && !rule3.methods.find(function(allowedMethod) {
      return method.toLowerCase() === allowedMethod.toLowerCase() || allowedMethod === "*";
    })) {
      return;
    }
    if (status || status === 0) {
      var statusCodeRange = rule3.statusCodeRange || DEFAULT_STATUS_CODE_RANGE;
      if (!isStatusCodeInRange(status, statusCodeRange)) {
        return false;
      }
    }
    return true;
  }
  function parseUrlFromString(href) {
    var _a4;
    var hashIndex = href.indexOf("#");
    var fragment = hashIndex >= 0 ? href.slice(hashIndex + 1) : "";
    var urlWithoutHash = hashIndex >= 0 ? href.slice(0, hashIndex) : href;
    var queryIndex = urlWithoutHash.indexOf("?");
    var query = queryIndex >= 0 ? urlWithoutHash.slice(queryIndex + 1) : "";
    var hrefWithoutQueryOrHash = queryIndex >= 0 ? urlWithoutHash.slice(0, queryIndex) : urlWithoutHash;
    var authorityMatch = hrefWithoutQueryOrHash.match(/^[a-z][a-z0-9+.-]*:\/\/([^/?#]+)/i);
    var host3 = (_a4 = authorityMatch === null || authorityMatch === void 0 ? void 0 : authorityMatch[1]) !== null && _a4 !== void 0 ? _a4 : "";
    return { query, fragment, href, hrefWithoutQueryOrHash, host: host3 };
  }
  function parseUrl(url) {
    var _a4;
    if (!url) {
      return;
    }
    try {
      var currentHref = (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.location.href;
      var urlObj = new URL(url, currentHref);
      var query = urlObj.searchParams.toString();
      var fragment = urlObj.hash.replace("#", "");
      var href = urlObj.href;
      var host3 = urlObj.host;
      urlObj.hash = "";
      urlObj.search = "";
      var hrefWithoutQueryOrHash = urlObj.href;
      return { query, fragment, href, hrefWithoutQueryOrHash, host: host3 };
    } catch (_b) {
      try {
        return parseUrlFromString(url);
      } catch (_c) {
        return;
      }
    }
  }
  function isAmplitudeNetworkRequestEvent(host3, requestWrapper) {
    if (host3.includes("amplitude.com")) {
      try {
        var body = requestWrapper.body;
        if (typeof body !== "string") {
          return false;
        }
        var bodyObj = JSON.parse(body);
        var events = bodyObj.events;
        if (events.find(function(event) {
          return event.event_type === AMPLITUDE_NETWORK_REQUEST_EVENT;
        })) {
          return true;
        }
      } catch (e2) {
      }
    }
    return false;
  }
  function parseHeaderCaptureRule(rule3) {
    if (typeof rule3 !== "object" || rule3 === null) {
      if (rule3) {
        return __spreadArray([], __read(SAFE_HEADERS), false);
      } else if (rule3 === void 0) {
        var res = IS_HEADER_CAPTURE_EXPERIMENTAL ? void 0 : __spreadArray([], __read(SAFE_HEADERS), false);
        return res;
      }
      return;
    }
    if (rule3.length === 0) {
      return;
    }
    return rule3;
  }
  function isBodyCaptureRuleEmpty(rule3) {
    var _a4, _b, _c;
    return !((_a4 = rule3 === null || rule3 === void 0 ? void 0 : rule3.allowlist) === null || _a4 === void 0 ? void 0 : _a4.length) && !((_b = rule3 === null || rule3 === void 0 ? void 0 : rule3.blocklist) === null || _b === void 0 ? void 0 : _b.length) && !((_c = rule3 === null || rule3 === void 0 ? void 0 : rule3.excludelist) === null || _c === void 0 ? void 0 : _c.length);
  }
  function shouldTrackNetworkEvent(networkEvent, options) {
    var _a4;
    if (options === void 0) {
      options = {};
    }
    var urlObj = parseUrl(networkEvent.url);
    if (!urlObj) {
      return false;
    }
    var host3 = urlObj.host;
    if (options.ignoreAmplitudeRequests !== false && (wildcardMatch(host3, "*.amplitude.com") || wildcardMatch(host3, "amplitude.com"))) {
      return false;
    }
    if ((_a4 = options.ignoreHosts) === null || _a4 === void 0 ? void 0 : _a4.find(function(ignoreHost) {
      return wildcardMatch(host3, ignoreHost);
    })) {
      return false;
    }
    if (!options.captureRules && networkEvent.status !== void 0 && !isStatusCodeInRange(networkEvent.status, DEFAULT_STATUS_CODE_RANGE)) {
      return false;
    }
    if (options.captureRules) {
      var isMatch_1;
      __spreadArray([], __read(options.captureRules), false).reverse().find(function(rule3) {
        isMatch_1 = isCaptureRuleMatch(rule3, host3, networkEvent.status, networkEvent.url, networkEvent.method);
        if (isMatch_1) {
          var responseHeadersRule = parseHeaderCaptureRule(rule3.responseHeaders);
          if (networkEvent.responseWrapper && responseHeadersRule) {
            var responseHeaders = networkEvent.responseWrapper.headers(responseHeadersRule);
            if (responseHeaders) {
              networkEvent.responseHeaders = responseHeaders;
            }
          }
          var requestHeadersRule = parseHeaderCaptureRule(rule3.requestHeaders);
          if (networkEvent.requestWrapper && requestHeadersRule) {
            var requestHeaders = networkEvent.requestWrapper.headers(requestHeadersRule);
            if (requestHeaders) {
              networkEvent.requestHeaders = requestHeaders;
            }
          }
          if (networkEvent.responseWrapper && rule3.responseBody && !isBodyCaptureRuleEmpty(rule3.responseBody)) {
            var excludelist = rule3.responseBody.excludelist || rule3.responseBody.blocklist;
            networkEvent.responseBodyJson = networkEvent.responseWrapper.json(rule3.responseBody.allowlist, excludelist);
          }
          if (networkEvent.requestWrapper && rule3.requestBody && !isBodyCaptureRuleEmpty(rule3.requestBody)) {
            var excludelist = rule3.requestBody.excludelist || rule3.requestBody.blocklist;
            networkEvent.requestBodyJson = networkEvent.requestWrapper.json(rule3.requestBody.allowlist, excludelist);
          }
        }
        return isMatch_1 !== void 0;
      });
      if (!isMatch_1) {
        return false;
      }
    }
    if (networkEvent.requestWrapper && isAmplitudeNetworkRequestEvent(host3, networkEvent.requestWrapper)) {
      return false;
    }
    return true;
  }
  function logNetworkAnalyticsEvent(networkAnalyticsEvent, request, amplitude, loggerProvider) {
    return __awaiter(this, void 0, void 0, function() {
      var _a4, requestBody, responseBody;
      return __generator(this, function(_b) {
        switch (_b.label) {
          case 0:
            if (!(request.requestBodyJson || request.responseBodyJson)) return [3, 2];
            return [4, Promise.all([request.requestBodyJson, request.responseBodyJson])];
          case 1:
            _a4 = __read.apply(void 0, [_b.sent(), 2]), requestBody = _a4[0], responseBody = _a4[1];
            if (requestBody) {
              try {
                networkAnalyticsEvent["[Amplitude] Request Body"] = JSON.stringify(requestBody);
              } catch (e2) {
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.debug("Failed to stringify request body", e2);
              }
            }
            if (responseBody) {
              try {
                networkAnalyticsEvent["[Amplitude] Response Body"] = JSON.stringify(responseBody);
              } catch (e2) {
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.debug("Failed to stringify response body");
              }
            }
            _b.label = 2;
          case 2:
            amplitude === null || amplitude === void 0 ? void 0 : amplitude.track(AMPLITUDE_NETWORK_REQUEST_EVENT, networkAnalyticsEvent);
            return [
              2
              /*return*/
            ];
        }
      });
    });
  }
  function trackNetworkEvents(_a4) {
    var allObservables = _a4.allObservables, networkTrackingOptions = _a4.networkTrackingOptions, amplitude = _a4.amplitude, loggerProvider = _a4.loggerProvider;
    var networkObservable = allObservables.networkObservable;
    var filteredNetworkObservable = networkObservable.filter(function(event) {
      return shouldTrackNetworkEvent(event.event, networkTrackingOptions);
    });
    return filteredNetworkObservable.subscribe(function(networkEvent) {
      var _a5;
      var _b, _c;
      var request = networkEvent.event;
      var urlObj = parseUrl(request.url);
      if (!urlObj) {
        return;
      }
      var responseBodySize = (_b = request.responseWrapper) === null || _b === void 0 ? void 0 : _b.bodySize;
      var requestBodySize = (_c = request.requestWrapper) === null || _c === void 0 ? void 0 : _c.bodySize;
      var networkAnalyticsEvent = (_a5 = {}, _a5["[Amplitude] URL"] = urlObj.hrefWithoutQueryOrHash, _a5["[Amplitude] URL Query"] = urlObj.query, _a5["[Amplitude] URL Fragment"] = urlObj.fragment, _a5["[Amplitude] Request Method"] = request.method, _a5["[Amplitude] Status Code"] = request.status, _a5["[Amplitude] Start Time"] = request.startTime, _a5["[Amplitude] Completion Time"] = request.endTime, _a5["[Amplitude] Duration"] = request.duration, _a5["[Amplitude] Request Body Size"] = requestBodySize, _a5["[Amplitude] Response Body Size"] = responseBodySize, _a5["[Amplitude] Request Type"] = request.type, _a5["[Amplitude] Request Headers"] = request.requestHeaders, _a5["[Amplitude] Response Headers"] = request.responseHeaders, _a5);
      void logNetworkAnalyticsEvent(networkAnalyticsEvent, request, amplitude, loggerProvider);
    });
  }

  // node_modules/@amplitude/plugin-network-capture-browser/lib/esm/network-capture-plugin.js
  var ObservablesEnum2;
  (function(ObservablesEnum3) {
    ObservablesEnum3["NetworkObservable"] = "networkObservable";
  })(ObservablesEnum2 || (ObservablesEnum2 = {}));
  var subscription;
  var networkCapturePlugin = function(options) {
    if (options === void 0) {
      options = {};
    }
    var name = PLUGIN_NAME2;
    var type = "enrichment";
    var logger;
    var addAdditionalEventProperties = function(event, type2) {
      var baseEvent = {
        event,
        timestamp: Date.now(),
        type: type2
      };
      return baseEvent;
    };
    var createObservables = function() {
      var _a4;
      var networkObservable = new import_zen_observable.default(function(observer) {
        var callback = new NetworkEventCallback(function(event) {
          var eventWithProperties = addAdditionalEventProperties(event, "network");
          observer.next(eventWithProperties);
        });
        networkObserver.subscribe(callback, logger);
        return function() {
          networkObserver.unsubscribe(callback);
        };
      });
      return _a4 = {}, _a4[ObservablesEnum2.NetworkObservable] = networkObservable, _a4;
    };
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var allObservables;
        return __generator(this, function(_a4) {
          if (typeof document === "undefined" && !isReactNative()) {
            return [
              2
              /*return*/
            ];
          }
          allObservables = createObservables();
          logger = config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider;
          subscription = trackNetworkEvents({
            allObservables,
            networkTrackingOptions: options,
            amplitude,
            loggerProvider: logger
          });
          logger === null || logger === void 0 ? void 0 : logger.log("".concat(name, " has been successfully added."));
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          subscription.unsubscribe();
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name,
      type,
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/plugin-web-vitals-browser/lib/esm/web-vitals-plugin.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/plugin-web-vitals-browser/lib/esm/constants.js
  var PLUGIN_NAME3 = "web-vitals-browser";
  var WEB_VITALS_EVENT_NAME = "[Amplitude] Web Vitals";

  // node_modules/web-vitals/dist/web-vitals.js
  var e = -1;
  var t = (t2) => {
    addEventListener("pageshow", ((n4) => {
      n4.persisted && (e = n4.timeStamp, t2(n4));
    }), true);
  };
  var n = (e2, t2, n4, i2) => {
    let s2, o2;
    return (r2) => {
      t2.value >= 0 && (r2 || i2) && (o2 = t2.value - (s2 != null ? s2 : 0), (o2 || void 0 === s2) && (s2 = t2.value, t2.delta = o2, t2.rating = ((e3, t3) => e3 > t3[1] ? "poor" : e3 > t3[0] ? "needs-improvement" : "good")(t2.value, n4), e2(t2)));
    };
  };
  var i = (e2) => {
    requestAnimationFrame((() => requestAnimationFrame((() => e2()))));
  };
  var s = () => {
    const e2 = performance.getEntriesByType("navigation")[0];
    if (e2 && e2.responseStart > 0 && e2.responseStart < performance.now()) return e2;
  };
  var o = () => {
    var _a4;
    const e2 = s();
    return (_a4 = e2 == null ? void 0 : e2.activationStart) != null ? _a4 : 0;
  };
  var r = (t2, n4 = -1) => {
    const i2 = s();
    let r2 = "navigate";
    e >= 0 ? r2 = "back-forward-cache" : i2 && (document.prerendering || o() > 0 ? r2 = "prerender" : document.wasDiscarded ? r2 = "restore" : i2.type && (r2 = i2.type.replace(/_/g, "-")));
    return { name: t2, value: n4, rating: "good", delta: 0, entries: [], id: `v5-${Date.now()}-${Math.floor(8999999999999 * Math.random()) + 1e12}`, navigationType: r2 };
  };
  var c = /* @__PURE__ */ new WeakMap();
  function a(e2, t2) {
    return c.get(e2) || c.set(e2, new t2()), c.get(e2);
  }
  var d = class {
    constructor() {
      __publicField(this, "t");
      __publicField(this, "i", 0);
      __publicField(this, "o", []);
    }
    h(e2) {
      var _a4;
      if (e2.hadRecentInput) return;
      const t2 = this.o[0], n4 = this.o.at(-1);
      this.i && t2 && n4 && e2.startTime - n4.startTime < 1e3 && e2.startTime - t2.startTime < 5e3 ? (this.i += e2.value, this.o.push(e2)) : (this.i = e2.value, this.o = [e2]), (_a4 = this.t) == null ? void 0 : _a4.call(this, e2);
    }
  };
  var h = (e2, t2, n4 = {}) => {
    try {
      if (PerformanceObserver.supportedEntryTypes.includes(e2)) {
        const i2 = new PerformanceObserver(((e3) => {
          Promise.resolve().then((() => {
            t2(e3.getEntries());
          }));
        }));
        return i2.observe({ type: e2, buffered: true, ...n4 }), i2;
      }
    } catch {
    }
  };
  var f = (e2) => {
    let t2 = false;
    return () => {
      t2 || (e2(), t2 = true);
    };
  };
  var u = -1;
  var l = /* @__PURE__ */ new Set();
  var m = () => "hidden" !== document.visibilityState || document.prerendering ? 1 / 0 : 0;
  var p = (e2) => {
    if ("hidden" === document.visibilityState) {
      if ("visibilitychange" === e2.type) for (const e3 of l) e3();
      isFinite(u) || (u = "visibilitychange" === e2.type ? e2.timeStamp : 0, removeEventListener("prerenderingchange", p, true));
    }
  };
  var v = () => {
    var _a4;
    if (u < 0) {
      const e2 = o(), n4 = document.prerendering ? void 0 : (_a4 = globalThis.performance.getEntriesByType("visibility-state").filter(((t2) => "hidden" === t2.name && t2.startTime > e2))[0]) == null ? void 0 : _a4.startTime;
      u = n4 != null ? n4 : m(), addEventListener("visibilitychange", p, true), addEventListener("prerenderingchange", p, true), t((() => {
        setTimeout((() => {
          u = m();
        }));
      }));
    }
    return { get firstHiddenTime() {
      return u;
    }, onHidden(e2) {
      l.add(e2);
    } };
  };
  var g = (e2) => {
    document.prerendering ? addEventListener("prerenderingchange", (() => e2()), true) : e2();
  };
  var y = [1800, 3e3];
  var E = (e2, s2 = {}) => {
    g((() => {
      const c2 = v();
      let a2, d2 = r("FCP");
      const f2 = h("paint", ((e3) => {
        for (const t2 of e3) "first-contentful-paint" === t2.name && (f2.disconnect(), t2.startTime < c2.firstHiddenTime && (d2.value = Math.max(t2.startTime - o(), 0), d2.entries.push(t2), a2(true)));
      }));
      f2 && (a2 = n(e2, d2, y, s2.reportAllChanges), t(((t2) => {
        d2 = r("FCP"), a2 = n(e2, d2, y, s2.reportAllChanges), i((() => {
          d2.value = performance.now() - t2.timeStamp, a2(true);
        }));
      })));
    }));
  };
  var b = [0.1, 0.25];
  var L = (e2, s2 = {}) => {
    const o2 = v();
    E(f((() => {
      let c2, f2 = r("CLS", 0);
      const u2 = a(s2, d), l2 = (e3) => {
        for (const t2 of e3) u2.h(t2);
        u2.i > f2.value && (f2.value = u2.i, f2.entries = u2.o, c2());
      }, m2 = h("layout-shift", l2);
      m2 && (c2 = n(e2, f2, b, s2.reportAllChanges), o2.onHidden((() => {
        l2(m2.takeRecords()), c2(true);
      })), t((() => {
        u2.i = 0, f2 = r("CLS", 0), c2 = n(e2, f2, b, s2.reportAllChanges), i((() => c2()));
      })), setTimeout(c2));
    })));
  };
  var P = 0;
  var T = 1 / 0;
  var _ = 0;
  var M = (e2) => {
    for (const t2 of e2) t2.interactionId && (T = Math.min(T, t2.interactionId), _ = Math.max(_, t2.interactionId), P = _ ? (_ - T) / 7 + 1 : 0);
  };
  var w;
  var C = () => {
    var _a4;
    return w ? P : (_a4 = performance.interactionCount) != null ? _a4 : 0;
  };
  var I = () => {
    "interactionCount" in performance || w || (w = h("event", M, { type: "event", buffered: true, durationThreshold: 0 }));
  };
  var F = 0;
  var k = class {
    constructor() {
      __publicField(this, "u", []);
      __publicField(this, "l", /* @__PURE__ */ new Map());
      __publicField(this, "m");
      __publicField(this, "p");
    }
    v() {
      F = C(), this.u.length = 0, this.l.clear();
    }
    L() {
      const e2 = Math.min(this.u.length - 1, Math.floor((C() - F) / 50));
      return this.u[e2];
    }
    h(e2) {
      var _a4, _b;
      if ((_a4 = this.m) == null ? void 0 : _a4.call(this, e2), !e2.interactionId && "first-input" !== e2.entryType) return;
      const t2 = this.u.at(-1);
      let n4 = this.l.get(e2.interactionId);
      if (n4 || this.u.length < 10 || e2.duration > t2.P) {
        if (n4 ? e2.duration > n4.P ? (n4.entries = [e2], n4.P = e2.duration) : e2.duration === n4.P && e2.startTime === n4.entries[0].startTime && n4.entries.push(e2) : (n4 = { id: e2.interactionId, entries: [e2], P: e2.duration }, this.l.set(n4.id, n4), this.u.push(n4)), this.u.sort(((e3, t3) => t3.P - e3.P)), this.u.length > 10) {
          const e3 = this.u.splice(10);
          for (const t3 of e3) this.l.delete(t3.id);
        }
        (_b = this.p) == null ? void 0 : _b.call(this, n4);
      }
    }
  };
  var A = (e2) => {
    const t2 = globalThis.requestIdleCallback || setTimeout;
    "hidden" === document.visibilityState ? e2() : (e2 = f(e2), addEventListener("visibilitychange", e2, { once: true, capture: true }), t2((() => {
      e2(), removeEventListener("visibilitychange", e2, { capture: true });
    })));
  };
  var B = [200, 500];
  var S = (e2, i2 = {}) => {
    if (!globalThis.PerformanceEventTiming || !("interactionId" in PerformanceEventTiming.prototype)) return;
    const s2 = v();
    g((() => {
      var _a4;
      I();
      let o2, c2 = r("INP");
      const d2 = a(i2, k), f2 = (e3) => {
        A((() => {
          for (const t3 of e3) d2.h(t3);
          const t2 = d2.L();
          t2 && t2.P !== c2.value && (c2.value = t2.P, c2.entries = t2.entries, o2());
        }));
      }, u2 = h("event", f2, { durationThreshold: (_a4 = i2.durationThreshold) != null ? _a4 : 40 });
      o2 = n(e2, c2, B, i2.reportAllChanges), u2 && (u2.observe({ type: "first-input", buffered: true }), s2.onHidden((() => {
        f2(u2.takeRecords()), o2(true);
      })), t((() => {
        d2.v(), c2 = r("INP"), o2 = n(e2, c2, B, i2.reportAllChanges);
      })));
    }));
  };
  var N = class {
    constructor() {
      __publicField(this, "m");
    }
    h(e2) {
      var _a4;
      (_a4 = this.m) == null ? void 0 : _a4.call(this, e2);
    }
  };
  var q = [2500, 4e3];
  var x = (e2, s2 = {}) => {
    g((() => {
      const c2 = v();
      let d2, u2 = r("LCP");
      const l2 = a(s2, N), m2 = (e3) => {
        s2.reportAllChanges || (e3 = e3.slice(-1));
        for (const t2 of e3) l2.h(t2), t2.startTime < c2.firstHiddenTime && (u2.value = Math.max(t2.startTime - o(), 0), u2.entries = [t2], d2());
      }, p2 = h("largest-contentful-paint", m2);
      if (p2) {
        d2 = n(e2, u2, q, s2.reportAllChanges);
        const o2 = f((() => {
          m2(p2.takeRecords()), p2.disconnect(), d2(true);
        })), c3 = (e3) => {
          e3.isTrusted && (A(o2), removeEventListener(e3.type, c3, { capture: true }));
        };
        for (const e3 of ["keydown", "click", "visibilitychange"]) addEventListener(e3, c3, { capture: true });
        t(((t2) => {
          u2 = r("LCP"), d2 = n(e2, u2, q, s2.reportAllChanges), i((() => {
            u2.value = performance.now() - t2.timeStamp, d2(true);
          }));
        }));
      }
    }));
  };
  var H = [800, 1800];
  var O = (e2) => {
    document.prerendering ? g((() => O(e2))) : "complete" !== document.readyState ? addEventListener("load", (() => O(e2)), true) : setTimeout(e2);
  };
  var $ = (e2, i2 = {}) => {
    let c2 = r("TTFB"), a2 = n(e2, c2, H, i2.reportAllChanges);
    O((() => {
      const d2 = s();
      d2 && (c2.value = Math.max(d2.responseStart - o(), 0), c2.entries = [d2], a2(true), t((() => {
        c2 = r("TTFB", 0), a2 = n(e2, c2, H, i2.reportAllChanges), a2(true);
      })));
    }));
  };

  // node_modules/@amplitude/plugin-web-vitals-browser/lib/esm/web-vitals-plugin.js
  function getMetricStartTime(metric) {
    var _a4;
    var startTime = ((_a4 = metric.entries[0]) === null || _a4 === void 0 ? void 0 : _a4.startTime) || 0;
    return performance.timeOrigin + startTime;
  }
  function processMetric(metric) {
    return {
      value: metric.value,
      rating: metric.rating,
      delta: metric.delta,
      navigationType: metric.navigationType,
      id: metric.id,
      timestamp: Math.floor(getMetricStartTime(metric)),
      navigationStart: Math.floor(performance.timeOrigin)
    };
  }
  var webVitalsPlugin = function() {
    var visibilityListener = null;
    var globalScope3 = getGlobalScope();
    var doc = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.document;
    var location2 = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location;
    var setup = function(config2, amplitude) {
      return __awaiter(void 0, void 0, void 0, function() {
        var locationHref, webVitalsPayload;
        return __generator(this, function(_a4) {
          if (doc === void 0) {
            return [
              2
              /*return*/
            ];
          }
          locationHref = getDecodeURI(
            /* istanbul ignore next */
            (location2 === null || location2 === void 0 ? void 0 : location2.href) || "",
            config2.loggerProvider
          );
          webVitalsPayload = {
            "[Amplitude] Page Domain": (
              /* istanbul ignore next */
              (location2 === null || location2 === void 0 ? void 0 : location2.hostname) || ""
            ),
            "[Amplitude] Page Location": locationHref,
            "[Amplitude] Page Path": getDecodeURI(
              /* istanbul ignore next */
              (location2 === null || location2 === void 0 ? void 0 : location2.pathname) || "",
              config2.loggerProvider
            ),
            "[Amplitude] Page Title": (
              /* istanbul ignore next */
              typeof document !== "undefined" && document.title || ""
            ),
            "[Amplitude] Page URL": getDecodeURI(locationHref.split("?")[0], config2.loggerProvider)
          };
          x(function(metric) {
            webVitalsPayload["[Amplitude] LCP"] = processMetric(metric);
          });
          E(function(metric) {
            webVitalsPayload["[Amplitude] FCP"] = processMetric(metric);
          });
          S(function(metric) {
            webVitalsPayload["[Amplitude] INP"] = processMetric(metric);
          });
          L(function(metric) {
            webVitalsPayload["[Amplitude] CLS"] = processMetric(metric);
          });
          $(function(metric) {
            webVitalsPayload["[Amplitude] TTFB"] = processMetric(metric);
          });
          visibilityListener = function() {
            if (doc.visibilityState === "hidden" && visibilityListener) {
              amplitude.track(WEB_VITALS_EVENT_NAME, webVitalsPayload);
              doc.removeEventListener("visibilitychange", visibilityListener);
              visibilityListener = null;
            }
          };
          doc.addEventListener("visibilitychange", visibilityListener);
          return [
            2
            /*return*/
          ];
        });
      });
    };
    var execute = function(event) {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          return [2, event];
        });
      });
    };
    var teardown = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        return __generator(this, function(_a4) {
          if (visibilityListener) {
            doc === null || doc === void 0 ? void 0 : doc.removeEventListener("visibilitychange", visibilityListener);
          }
          return [
            2
            /*return*/
          ];
        });
      });
    };
    return {
      name: PLUGIN_NAME3,
      type: "enrichment",
      setup,
      execute,
      teardown
    };
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/attribution/web-attribution.js
  init_tslib_es6();
  init_esm();
  var WebAttribution = (
    /** @class */
    (function() {
      function WebAttribution2(options, config2) {
        var _a4;
        this.shouldTrackNewCampaign = false;
        this.options = __assign({ initialEmptyValue: "EMPTY", resetSessionOnNewCampaign: false, excludeReferrers: getDefaultExcludedReferrers(((_a4 = config2.cookieOptions) === null || _a4 === void 0 ? void 0 : _a4.domain) || config2.topLevelDomain), optOut: config2.optOut }, options);
        this.storage = config2.cookieStorage;
        this.storageKey = getStorageKey(config2.apiKey, "MKTG");
        this.webExpStorageKey = getStorageKey(config2.apiKey, "MKTG_ORIGINAL");
        this.currentCampaign = BASE_CAMPAIGN;
        this.sessionTimeout = config2.sessionTimeout;
        this.lastEventTime = config2.lastEventTime;
        this.logger = config2.loggerProvider;
        this.topLevelDomain = config2.topLevelDomain;
        config2.loggerProvider.log("Installing web attribution tracking.");
      }
      WebAttribution2.prototype.init = function() {
        return __awaiter(this, void 0, void 0, function() {
          var isEventInNewSession;
          var _a4;
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                if (this.options.optOut) {
                  return [
                    2
                    /*return*/
                  ];
                }
                return [4, this.fetchCampaign()];
              case 1:
                _a4 = __read.apply(void 0, [_b.sent(), 2]), this.currentCampaign = _a4[0], this.previousCampaign = _a4[1];
                isEventInNewSession = !this.lastEventTime ? true : isNewSession(this.sessionTimeout, this.lastEventTime);
                if (!isNewCampaign(this.currentCampaign, this.previousCampaign, this.options, this.logger, isEventInNewSession, this.topLevelDomain)) return [3, 3];
                this.shouldTrackNewCampaign = true;
                return [4, this.storage.set(this.storageKey, this.currentCampaign)];
              case 2:
                _b.sent();
                _b.label = 3;
              case 3:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      WebAttribution2.prototype.fetchCampaign = function() {
        return __awaiter(this, void 0, void 0, function() {
          var originalCampaign;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                return [4, this.storage.get(this.webExpStorageKey)];
              case 1:
                originalCampaign = _a4.sent();
                if (!originalCampaign) return [3, 3];
                return [4, this.storage.remove(this.webExpStorageKey)];
              case 2:
                _a4.sent();
                _a4.label = 3;
              case 3:
                return [4, Promise.all([originalCampaign || new CampaignParser().parse(), this.storage.get(this.storageKey)])];
              case 4:
                return [2, _a4.sent()];
            }
          });
        });
      };
      WebAttribution2.prototype.generateCampaignEvent = function(event_id) {
        this.shouldTrackNewCampaign = false;
        var campaignEvent = createCampaignEvent(this.currentCampaign, this.options);
        if (event_id) {
          campaignEvent.event_id = event_id;
        }
        return campaignEvent;
      };
      WebAttribution2.prototype.shouldSetSessionIdOnNewCampaign = function() {
        return this.shouldTrackNewCampaign && !!this.options.resetSessionOnNewCampaign;
      };
      return WebAttribution2;
    })()
  );

  // node_modules/@amplitude/plugin-event-property-attribution-browser/lib/esm/event-property-tracking.js
  init_tslib_es6();
  init_esm();
  var ATTRIBUTION_EVENT_TYPE = "[Amplitude] Attribution";
  var EVENT_PROPERTY_EXCLUDED_EVENT_TYPES = /* @__PURE__ */ new Set([
    SpecialEventType.IDENTIFY,
    SpecialEventType.GROUP_IDENTIFY
  ]);
  var toEventPropertyCampaign = function(campaign) {
    return omitUndefined(campaign);
  };
  var eventPropertyTrackingPlugin = function(options) {
    var _a4;
    if (options === void 0) {
      options = {};
    }
    var fallbackAttributionEvent = (_a4 = options.fallbackAttributionEvent) !== null && _a4 !== void 0 ? _a4 : false;
    var globalScope3 = getGlobalScope();
    var amplitude;
    var loggerProvider;
    var eventPropertyCampaign = {};
    var isTracking = false;
    var isProxied = false;
    var originalPushState;
    var originalReplaceState;
    var installedPushState;
    var installedReplaceState;
    var updateCampaignState = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var currentCampaign;
        return __generator(this, function(_a5) {
          switch (_a5.label) {
            case 0:
              return [4, new CampaignParser().parse()];
            case 1:
              currentCampaign = _a5.sent();
              eventPropertyCampaign = toEventPropertyCampaign(currentCampaign);
              if (fallbackAttributionEvent) {
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.log("Tracking attribution fallback event.");
                amplitude === null || amplitude === void 0 ? void 0 : amplitude.track(ATTRIBUTION_EVENT_TYPE, eventPropertyCampaign);
              }
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    var onHistoryChange = function() {
      void updateCampaignState();
    };
    var createHistoryStateProxy = function(method) {
      return new Proxy(method, {
        apply: function(target, thisArg, args) {
          Reflect.apply(target, thisArg, args);
          if (isTracking) {
            onHistoryChange();
          }
        }
      });
    };
    return {
      name: "@amplitude/plugin-event-property-attribution-browser",
      type: "enrichment",
      setup: function(config2, client) {
        return __awaiter(void 0, void 0, void 0, function() {
          return __generator(this, function(_a5) {
            switch (_a5.label) {
              case 0:
                amplitude = client;
                loggerProvider = config2.loggerProvider;
                isTracking = true;
                loggerProvider.log("Installing event property attribution tracking.");
                return [4, updateCampaignState()];
              case 1:
                _a5.sent();
                if (!globalScope3) {
                  return [
                    2
                    /*return*/
                  ];
                }
                globalScope3.addEventListener("popstate", onHistoryChange);
                if (!isProxied) {
                  originalPushState = Reflect.get(globalScope3.history, "pushState");
                  originalReplaceState = Reflect.get(globalScope3.history, "replaceState");
                  if (!originalPushState || !originalReplaceState) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  installedPushState = createHistoryStateProxy(originalPushState);
                  globalScope3.history.pushState = installedPushState;
                  installedReplaceState = createHistoryStateProxy(originalReplaceState);
                  globalScope3.history.replaceState = installedReplaceState;
                  isProxied = true;
                }
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      },
      execute: function(event) {
        return __awaiter(void 0, void 0, void 0, function() {
          return __generator(this, function(_a5) {
            if (EVENT_PROPERTY_EXCLUDED_EVENT_TYPES.has(event.event_type)) {
              return [2, event];
            }
            event.event_properties = __assign(__assign({}, eventPropertyCampaign), event.event_properties);
            return [2, event];
          });
        });
      },
      teardown: function() {
        return __awaiter(void 0, void 0, void 0, function() {
          var currentPushState, currentReplaceState;
          return __generator(this, function(_a5) {
            if (globalScope3) {
              globalScope3.removeEventListener("popstate", onHistoryChange);
              currentPushState = Reflect.get(globalScope3.history, "pushState");
              currentReplaceState = Reflect.get(globalScope3.history, "replaceState");
              if (isProxied && currentPushState === installedPushState && originalPushState) {
                globalScope3.history.pushState = originalPushState;
              }
              if (isProxied && currentReplaceState === installedReplaceState && originalReplaceState) {
                globalScope3.history.replaceState = originalReplaceState;
              }
            }
            isTracking = false;
            isProxied = false;
            originalPushState = void 0;
            originalReplaceState = void 0;
            installedPushState = void 0;
            installedReplaceState = void 0;
            eventPropertyCampaign = {};
            return [
              2
              /*return*/
            ];
          });
        });
      }
    };
  };

  // node_modules/@amplitude/plugin-page-url-enrichment-browser/lib/esm/page-url-enrichment.js
  init_tslib_es6();
  init_esm();
  var CURRENT_PAGE_STORAGE_KEY = "AMP_CURRENT_PAGE";
  var PREVIOUS_PAGE_STORAGE_KEY = "AMP_PREVIOUS_PAGE";
  var URL_INFO_STORAGE_KEY = "AMP_URL_INFO";
  var PreviousPageType;
  (function(PreviousPageType2) {
    PreviousPageType2["Direct"] = "direct";
    PreviousPageType2["Internal"] = "internal";
    PreviousPageType2["External"] = "external";
  })(PreviousPageType || (PreviousPageType = {}));
  var EXCLUDED_DEFAULT_EVENT_TYPES = /* @__PURE__ */ new Set([
    SpecialEventType.IDENTIFY,
    SpecialEventType.GROUP_IDENTIFY,
    SpecialEventType.REVENUE
  ]);
  var pageUrlEnrichmentPlugin = function(_a4) {
    var _b = _a4 === void 0 ? {} : _a4, _c = _b.internalDomains, internalDomains = _c === void 0 ? [] : _c;
    var globalScope3 = getGlobalScope();
    var sessionStorage = void 0;
    var isStorageEnabled = false;
    var loggerProvider = void 0;
    var isProxied = false;
    var isTracking = false;
    var getHostname = function(url) {
      var hostname;
      try {
        var decodedUrl = getDecodeURI(url, loggerProvider);
        hostname = new URL(decodedUrl).hostname;
      } catch (e2) {
        loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Could not parse URL: ", e2);
      }
      return hostname;
    };
    var getPrevPageType = function(previousPage) {
      var currentDomain = typeof location !== "undefined" && location.hostname || "";
      var previousPageDomain = previousPage ? getHostname(previousPage) : void 0;
      if (!previousPageDomain) {
        return PreviousPageType.Direct;
      }
      var isCurrentInternal = internalDomains.some(function(domain) {
        return currentDomain.indexOf(domain) !== -1;
      });
      var isPrevInternal = internalDomains.some(function(domain) {
        return previousPageDomain.indexOf(domain) !== -1;
      });
      if (currentDomain === previousPageDomain || isPrevInternal && isCurrentInternal) {
        return PreviousPageType.Internal;
      }
      return PreviousPageType.External;
    };
    var saveURLInfo = function() {
      return __awaiter(void 0, void 0, void 0, function() {
        var URLInfo, currentURL, storedCurrentURL, previousURL;
        var _a5;
        return __generator(this, function(_b2) {
          switch (_b2.label) {
            case 0:
              if (!(sessionStorage && isStorageEnabled)) return [3, 3];
              return [4, sessionStorage.get(URL_INFO_STORAGE_KEY)];
            case 1:
              URLInfo = _b2.sent();
              currentURL = getDecodeURI(typeof location !== "undefined" && location.href || "");
              storedCurrentURL = (URLInfo === null || URLInfo === void 0 ? void 0 : URLInfo[CURRENT_PAGE_STORAGE_KEY]) || "";
              previousURL = void 0;
              if (currentURL === storedCurrentURL) {
                previousURL = (URLInfo === null || URLInfo === void 0 ? void 0 : URLInfo[PREVIOUS_PAGE_STORAGE_KEY]) || "";
              } else if (storedCurrentURL) {
                previousURL = storedCurrentURL;
              } else {
                previousURL = document.referrer || "";
              }
              return [4, sessionStorage.set(URL_INFO_STORAGE_KEY, (_a5 = {}, _a5[CURRENT_PAGE_STORAGE_KEY] = currentURL, _a5[PREVIOUS_PAGE_STORAGE_KEY] = previousURL, _a5))];
            case 2:
              _b2.sent();
              _b2.label = 3;
            case 3:
              return [
                2
                /*return*/
              ];
          }
        });
      });
    };
    var saveUrlInfoWrapper = function() {
      void saveURLInfo();
    };
    var plugin = {
      name: "@amplitude/plugin-page-url-enrichment-browser",
      type: "enrichment",
      setup: function(config2, _2) {
        return __awaiter(void 0, void 0, void 0, function() {
          var referrer, referrerHostname, currentHostname, currentURL, existingURLInfo, storedCurrentURL, arrivedFromDifferentOrigin;
          var _a5;
          var _b2;
          return __generator(this, function(_c2) {
            switch (_c2.label) {
              case 0:
                loggerProvider = config2.loggerProvider;
                loggerProvider.log("Installing @amplitude/plugin-page-url-enrichment-browser");
                isTracking = true;
                if (!globalScope3) return [3, 7];
                try {
                  sessionStorage = new BrowserStorage(globalScope3.sessionStorage);
                } catch (error) {
                  loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.debug("sessionStorage is not available in this environment.");
                }
                return [4, sessionStorage === null || sessionStorage === void 0 ? void 0 : sessionStorage.isEnabled()];
              case 1:
                isStorageEnabled = (_b2 = _c2.sent()) !== null && _b2 !== void 0 ? _b2 : false;
                if (!(sessionStorage && isStorageEnabled)) return [3, 6];
                referrer = typeof document !== "undefined" && document.referrer || "";
                referrerHostname = referrer ? getHostname(referrer) : void 0;
                currentHostname = typeof location !== "undefined" && location.hostname || "";
                currentURL = getDecodeURI(typeof location !== "undefined" && location.href || "");
                return [4, sessionStorage.get(URL_INFO_STORAGE_KEY)];
              case 2:
                existingURLInfo = _c2.sent();
                storedCurrentURL = (existingURLInfo === null || existingURLInfo === void 0 ? void 0 : existingURLInfo[CURRENT_PAGE_STORAGE_KEY]) || "";
                arrivedFromDifferentOrigin = !!referrerHostname && referrerHostname !== currentHostname && storedCurrentURL !== currentURL;
                if (!arrivedFromDifferentOrigin) return [3, 4];
                return [4, sessionStorage.set(URL_INFO_STORAGE_KEY, (_a5 = {}, _a5[CURRENT_PAGE_STORAGE_KEY] = currentURL, _a5[PREVIOUS_PAGE_STORAGE_KEY] = referrer, _a5))];
              case 3:
                _c2.sent();
                return [3, 6];
              case 4:
                return [4, saveURLInfo()];
              case 5:
                _c2.sent();
                _c2.label = 6;
              case 6:
                globalScope3.addEventListener("popstate", saveUrlInfoWrapper);
                if (!isProxied) {
                  globalScope3.history.pushState = new Proxy(globalScope3.history.pushState, {
                    apply: function(target, thisArg, _a6) {
                      var _b3 = __read(_a6, 3), state = _b3[0], unused = _b3[1], url = _b3[2];
                      target.apply(thisArg, [state, unused, url]);
                      if (isTracking) {
                        saveUrlInfoWrapper();
                      }
                    }
                  });
                  globalScope3.history.replaceState = new Proxy(globalScope3.history.replaceState, {
                    apply: function(target, thisArg, _a6) {
                      var _b3 = __read(_a6, 3), state = _b3[0], unused = _b3[1], url = _b3[2];
                      target.apply(thisArg, [state, unused, url]);
                      if (isTracking) {
                        saveUrlInfoWrapper();
                      }
                    }
                  });
                  isProxied = true;
                }
                _c2.label = 7;
              case 7:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      },
      execute: function(event) {
        return __awaiter(void 0, void 0, void 0, function() {
          var locationHREF, URLInfo, previousPage;
          return __generator(this, function(_a5) {
            switch (_a5.label) {
              case 0:
                locationHREF = getDecodeURI(typeof location !== "undefined" && location.href || "");
                if (!(sessionStorage && isStorageEnabled)) return [3, 2];
                return [4, sessionStorage.get(URL_INFO_STORAGE_KEY)];
              case 1:
                URLInfo = _a5.sent();
                previousPage = (URLInfo === null || URLInfo === void 0 ? void 0 : URLInfo[PREVIOUS_PAGE_STORAGE_KEY]) || "";
                if (EXCLUDED_DEFAULT_EVENT_TYPES.has(event.event_type)) {
                  return [2, event];
                }
                event.event_properties = __assign(__assign({}, event.event_properties || {}), { "[Amplitude] Page Domain": addIfNotExist(event, "[Amplitude] Page Domain", typeof location !== "undefined" && location.hostname || ""), "[Amplitude] Page Location": addIfNotExist(event, "[Amplitude] Page Location", locationHREF), "[Amplitude] Page Path": addIfNotExist(event, "[Amplitude] Page Path", typeof location !== "undefined" && getDecodeURI(location.pathname) || ""), "[Amplitude] Page Title": addIfNotExist(event, "[Amplitude] Page Title", getPageTitle(replaceSensitiveString)), "[Amplitude] Page URL": addIfNotExist(event, "[Amplitude] Page URL", locationHREF.split("?")[0]), "[Amplitude] Previous Page Location": previousPage, "[Amplitude] Previous Page Type": getPrevPageType(previousPage) });
                _a5.label = 2;
              case 2:
                return [2, event];
            }
          });
        });
      },
      teardown: function() {
        return __awaiter(void 0, void 0, void 0, function() {
          return __generator(this, function(_a5) {
            switch (_a5.label) {
              case 0:
                if (globalScope3) {
                  globalScope3.removeEventListener("popstate", saveUrlInfoWrapper);
                  isTracking = false;
                }
                if (!(sessionStorage && isStorageEnabled)) return [3, 2];
                return [4, sessionStorage.set(URL_INFO_STORAGE_KEY, {})];
              case 1:
                _a5.sent();
                _a5.label = 2;
              case 2:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      }
    };
    return plugin;
  };
  function addIfNotExist(event, key, value) {
    if (!event.event_properties) {
      event.event_properties = {};
    }
    if (event.event_properties[key] === void 0) {
      return value;
    }
    return event.event_properties[key];
  }

  // node_modules/@amplitude/plugin-custom-enrichment-browser/lib/esm/custom-enrichment.js
  init_tslib_es6();
  var customEnrichmentPlugin = function() {
    var loggerProvider;
    var unsubscribe;
    var enrichEvent;
    function isCustomEnrichmentConfig(config2) {
      if (typeof config2 !== "object" || config2 === null) {
        return false;
      }
      return "body" in config2 && typeof config2.body === "string";
    }
    function createEnrichEvent(body) {
      if (body) {
        try {
          var fn = new Function("return " + body)();
          if (typeof fn === "function") {
            return fn;
          }
          loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Custom enrichment body did not evaluate to a function");
        } catch (error) {
          loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Could not create custom enrichment function", error);
        }
      }
      return function(event) {
        return event;
      };
    }
    var plugin = {
      name: "@amplitude/plugin-custom-enrichment-browser",
      type: "enrichment",
      setup: function(config2, _2) {
        return __awaiter(void 0, void 0, void 0, function() {
          var subscriptionId_1;
          var _a4;
          return __generator(this, function(_b) {
            loggerProvider = config2.loggerProvider;
            loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.log("Installing @amplitude/plugin-custom-enrichment-browser");
            if ((_a4 = config2.remoteConfig) === null || _a4 === void 0 ? void 0 : _a4.fetchRemoteConfig) {
              if (!config2.remoteConfigClient) {
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.debug("Remote config client is not provided, skipping remote config fetch");
              } else {
                subscriptionId_1 = config2.remoteConfigClient.subscribe("configs.analyticsSDK.browserSDK.customEnrichment", "all", function(remoteConfig) {
                  if (remoteConfig && isCustomEnrichmentConfig(remoteConfig)) {
                    enrichEvent = createEnrichEvent(remoteConfig.body || "");
                  } else {
                    enrichEvent = createEnrichEvent("");
                  }
                });
                unsubscribe = function() {
                  var _a5;
                  return (_a5 = config2.remoteConfigClient) === null || _a5 === void 0 ? void 0 : _a5.unsubscribe(subscriptionId_1);
                };
              }
            }
            return [
              2
              /*return*/
            ];
          });
        });
      },
      execute: function(event) {
        return __awaiter(void 0, void 0, void 0, function() {
          var _a4;
          return __generator(this, function(_b) {
            if (enrichEvent) {
              try {
                return [2, (_a4 = enrichEvent(event)) !== null && _a4 !== void 0 ? _a4 : null];
              } catch (error) {
                loggerProvider === null || loggerProvider === void 0 ? void 0 : loggerProvider.error("Could not execute custom enrichment function", error);
                return [2, event];
              }
            }
            return [2, event];
          });
        });
      },
      teardown: function() {
        return __awaiter(void 0, void 0, void 0, function() {
          return __generator(this, function(_a4) {
            if (unsubscribe) {
              unsubscribe();
            }
            return [
              2
              /*return*/
            ];
          });
        });
      }
    };
    return plugin;
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/attribution/tracking-methods.js
  init_tslib_es6();
  var USER_PROPERTY_TRACKING_METHOD = "userProperty";
  var EVENT_PROPERTY_TRACKING_METHOD = "eventProperty";
  var isTrackingMethod = function(value) {
    return value === USER_PROPERTY_TRACKING_METHOD || value === EVENT_PROPERTY_TRACKING_METHOD;
  };
  var DEFAULT_TRACKING_METHODS = [
    USER_PROPERTY_TRACKING_METHOD,
    EVENT_PROPERTY_TRACKING_METHOD
  ];
  var normalizeTrackingMethod = function(trackingMethod) {
    var normalized = __spreadArray([], __read(new Set((Array.isArray(trackingMethod) ? trackingMethod : [trackingMethod]).filter(isTrackingMethod))), false);
    return normalized.length > 0 ? normalized : __spreadArray([], __read(DEFAULT_TRACKING_METHODS), false);
  };
  var hasTrackingMethod = function(options, trackingMethod) {
    return normalizeTrackingMethod(options.trackingMethod).includes(trackingMethod);
  };
  var isUserPropertyAttributionEnabled = function(options) {
    return hasTrackingMethod(options, USER_PROPERTY_TRACKING_METHOD);
  };
  var isEventPropertyAttributionEnabled = function(options) {
    return hasTrackingMethod(options, EVENT_PROPERTY_TRACKING_METHOD);
  };

  // node_modules/@amplitude/analytics-browser/lib/esm/browser-client.js
  var UNSPECIFIED_SESSION_ID = -1;
  var AmplitudeBrowser = (
    /** @class */
    (function(_super) {
      __extends(AmplitudeBrowser2, _super);
      function AmplitudeBrowser2() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        _this._diagnosticsSampleRate = 0;
        return _this;
      }
      AmplitudeBrowser2.prototype.init = function(apiKey2, userIdOrOptions, maybeOptions) {
        if (apiKey2 === void 0) {
          apiKey2 = "";
        }
        var userId;
        var options;
        if (arguments.length > 2) {
          userId = userIdOrOptions;
          options = maybeOptions;
        } else {
          if (typeof userIdOrOptions === "string") {
            userId = userIdOrOptions;
            options = void 0;
          } else {
            userId = userIdOrOptions === null || userIdOrOptions === void 0 ? void 0 : userIdOrOptions.userId;
            options = userIdOrOptions;
          }
        }
        return returnWrapper(this._init(__assign(__assign({}, options), { userId, apiKey: apiKey2 })));
      };
      AmplitudeBrowser2.prototype._init = function(options) {
        var _a4, _b, _c, _d, _e, _f, _g, _h;
        return __awaiter(this, void 0, void 0, function() {
          var fetchRemoteConfig, loggerProvider, serverZone, remoteConfigClient, diagnosticsSampleRate, enableDiagnostics, diagnosticsClient, browserOptions, attributionTrackingOptions, queryParams, ampTimestamp, isWithinTimeLimit, querySessionId, deferredSessionId, connector;
          var _this = this;
          return __generator(this, function(_j) {
            switch (_j.label) {
              case 0:
                if (this.initializing) {
                  return [
                    2
                    /*return*/
                  ];
                }
                this.initializing = true;
                fetchRemoteConfig = shouldFetchRemoteConfig(options);
                loggerProvider = (_a4 = options.loggerProvider) !== null && _a4 !== void 0 ? _a4 : new Logger();
                if (!options.loggerProvider) {
                  loggerProvider.enable((_b = options.logLevel) !== null && _b !== void 0 ? _b : LogLevel.Warn);
                }
                serverZone = (_c = options.serverZone) !== null && _c !== void 0 ? _c : DEFAULT_SERVER_ZONE;
                diagnosticsSampleRate = this._diagnosticsSampleRate;
                enableDiagnostics = (_d = options.enableDiagnostics) !== null && _d !== void 0 ? _d : true;
                if (!fetchRemoteConfig) return [3, 2];
                remoteConfigClient = new RemoteConfigClient(
                  options.apiKey,
                  loggerProvider,
                  serverZone,
                  /* istanbul ignore next */
                  (_e = options.remoteConfig) === null || _e === void 0 ? void 0 : _e.serverUrl
                );
                return [4, new Promise(function(resolve) {
                  remoteConfigClient === null || remoteConfigClient === void 0 ? void 0 : remoteConfigClient.subscribe("configs.diagnostics.browserSDK", "all", function(remoteConfig, source, lastFetch) {
                    loggerProvider.debug("Diagnostics remote configuration received:", JSON.stringify({
                      remoteConfig,
                      source,
                      lastFetch
                    }, null, 2));
                    if (remoteConfig) {
                      var sampleRate = remoteConfig.sampleRate;
                      if (typeof sampleRate === "number" && !isNaN(sampleRate)) {
                        diagnosticsSampleRate = sampleRate;
                      }
                      var enabled = remoteConfig.enabled;
                      if (typeof enabled === "boolean") {
                        enableDiagnostics = enabled;
                      }
                    }
                    resolve();
                  });
                })];
              case 1:
                _j.sent();
                _j.label = 2;
              case 2:
                diagnosticsClient = new DiagnosticsClient(options.apiKey, loggerProvider, serverZone, {
                  enabled: enableDiagnostics,
                  sampleRate: diagnosticsSampleRate
                });
                diagnosticsClient.setTag("library", "".concat(LIBPREFIX, "/").concat(VERSION));
                diagnosticsClient.setTag("platform", BROWSER_PLATFORM);
                diagnosticsClient.setTag("web_environment", getRuntimeEnvironment());
                if (typeof navigator !== "undefined") {
                  diagnosticsClient.setTag("user_agent", navigator.userAgent);
                }
                return [4, useBrowserConfig(options.apiKey, options, this, diagnosticsClient, {
                  loggerProvider,
                  serverZone,
                  enableDiagnostics,
                  diagnosticsSampleRate
                })];
              case 3:
                browserOptions = _j.sent();
                if (!(fetchRemoteConfig && remoteConfigClient)) return [3, 5];
                return [4, new Promise(function(resolve) {
                  remoteConfigClient === null || remoteConfigClient === void 0 ? void 0 : remoteConfigClient.subscribe("configs.analyticsSDK.browserSDK", "all", function(remoteConfig, source, lastFetch) {
                    browserOptions.loggerProvider.debug("Remote configuration received:", JSON.stringify({
                      remoteConfig,
                      source,
                      lastFetch
                    }, null, 2));
                    if (remoteConfig) {
                      updateBrowserConfigWithRemoteConfig(remoteConfig, browserOptions);
                    }
                    resolve();
                  });
                })];
              case 4:
                _j.sent();
                _j.label = 5;
              case 5:
                return [4, _super.prototype._init.call(this, browserOptions)];
              case 6:
                _j.sent();
                this.logBrowserOptions(browserOptions);
                this.config.remoteConfigClient = remoteConfigClient;
                attributionTrackingOptions = getAttributionTrackingConfig(this.config);
                if (!(isAttributionTrackingEnabled(this.config.defaultTracking) && isUserPropertyAttributionEnabled(attributionTrackingOptions))) return [3, 8];
                if (this.config.optOut) {
                  this.timeline.addOptOutListener(function(optOut) {
                    return __awaiter(_this, void 0, void 0, function() {
                      return __generator(this, function(_a5) {
                        switch (_a5.label) {
                          case 0:
                            if (!!optOut) return [3, 2];
                            this.webAttribution = new WebAttribution(attributionTrackingOptions, this.config);
                            return [4, this.webAttribution.init()];
                          case 1:
                            _a5.sent();
                            _a5.label = 2;
                          case 2:
                            return [
                              2
                              /*return*/
                            ];
                        }
                      });
                    });
                  });
                }
                this.webAttribution = new WebAttribution(attributionTrackingOptions, this.config);
                return [4, this.webAttribution.init()];
              case 7:
                _j.sent();
                _j.label = 8;
              case 8:
                queryParams = getQueryParams();
                ampTimestamp = queryParams.ampTimestamp ? Number(queryParams.ampTimestamp) : void 0;
                isWithinTimeLimit = ampTimestamp ? Date.now() < ampTimestamp : true;
                querySessionId = isWithinTimeLimit && !Number.isNaN(Number(queryParams.ampSessionId)) ? Number(queryParams.ampSessionId) : void 0;
                deferredSessionId = this.config.deferredSessionId;
                if (deferredSessionId === UNSPECIFIED_SESSION_ID && !this.config.optOut) {
                  deferredSessionId = Date.now();
                }
                this.setSessionId((_h = (_g = (_f = options.sessionId) !== null && _f !== void 0 ? _f : querySessionId) !== null && _g !== void 0 ? _g : deferredSessionId) !== null && _h !== void 0 ? _h : this.config.sessionId);
                if (this.config.optOut) {
                  this.timeline.addOptOutListener(function(optOut) {
                    return __awaiter(_this, void 0, void 0, function() {
                      return __generator(this, function(_a5) {
                        if (!optOut && this.config.deferredSessionId) {
                          if (this.config.deferredSessionId === UNSPECIFIED_SESSION_ID) {
                            this.setSessionId(void 0);
                          } else {
                            this.setSessionId(this.config.deferredSessionId);
                          }
                        }
                        return [
                          2
                          /*return*/
                        ];
                      });
                    });
                  });
                }
                connector = getAnalyticsConnector(this.config.instanceName);
                connector.identityStore.setIdentity({
                  userId: this.config.userId,
                  deviceId: this.config.deviceId
                });
                if (!(this.config.offline !== OfflineDisabled)) return [3, 10];
                return [4, this.add(networkConnectivityCheckerPlugin()).promise];
              case 9:
                _j.sent();
                _j.label = 10;
              case 10:
                return [4, this.add(new Destination({ diagnosticsClient })).promise];
              case 11:
                _j.sent();
                return [4, this.add(new Context()).promise];
              case 12:
                _j.sent();
                return [4, this.add(new IdentityEventSender()).promise];
              case 13:
                _j.sent();
                detNotify(this.config);
                if (!isFileDownloadTrackingEnabled(this.config.defaultTracking)) return [3, 15];
                this.config.loggerProvider.debug("Adding file download tracking plugin");
                return [4, this.add(fileDownloadTracking()).promise];
              case 14:
                _j.sent();
                _j.label = 15;
              case 15:
                if (!isFormInteractionTrackingEnabled(this.config.defaultTracking)) return [3, 17];
                this.config.loggerProvider.debug("Adding form interaction plugin");
                return [4, this.add(formInteractionTracking()).promise];
              case 16:
                _j.sent();
                _j.label = 17;
              case 17:
                if (!isPageViewTrackingEnabled(this.config.defaultTracking)) return [3, 20];
                if (!!this.config.optOut) return [3, 19];
                this.config.loggerProvider.debug("Adding page view tracking plugin");
                return [4, this.add(pageViewTrackingPlugin(getPageViewTrackingConfig(this.config))).promise];
              case 18:
                _j.sent();
                return [3, 20];
              case 19:
                this.timeline.addOptOutListener(function(optOut) {
                  return __awaiter(_this, void 0, void 0, function() {
                    return __generator(this, function(_a5) {
                      switch (_a5.label) {
                        case 0:
                          if (optOut) {
                            return [
                              2
                              /*return*/
                            ];
                          }
                          this.config.loggerProvider.debug("Adding page view tracking plugin");
                          return [4, this.add(pageViewTrackingPlugin(getPageViewTrackingConfig(this.config))).promise];
                        case 1:
                          _a5.sent();
                          return [
                            2
                            /*return*/
                          ];
                      }
                    });
                  });
                });
                _j.label = 20;
              case 20:
                if (!(isAttributionTrackingEnabled(this.config.defaultTracking) && isEventPropertyAttributionEnabled(attributionTrackingOptions))) return [3, 22];
                this.config.loggerProvider.debug("Adding event property attribution plugin");
                return [4, this.add(eventPropertyTrackingPlugin(attributionTrackingOptions)).promise];
              case 21:
                _j.sent();
                _j.label = 22;
              case 22:
                if (!isElementInteractionsEnabled(this.config.autocapture)) return [3, 24];
                this.config.loggerProvider.debug("Adding user interactions plugin (autocapture plugin)");
                return [4, this.add(autocapturePlugin(getElementInteractionsConfig(this.config), { diagnosticsClient })).promise];
              case 23:
                _j.sent();
                _j.label = 24;
              case 24:
                if (!isFrustrationInteractionsEnabled(this.config.autocapture)) return [3, 26];
                this.config.loggerProvider.debug("Adding frustration interactions plugin");
                return [4, this.add(frustrationPlugin(getFrustrationInteractionsConfig(this.config))).promise];
              case 25:
                _j.sent();
                _j.label = 26;
              case 26:
                if (!isNetworkTrackingEnabled(this.config.autocapture)) return [3, 28];
                this.config.loggerProvider.debug("Adding network tracking plugin");
                return [4, this.add(networkCapturePlugin(getNetworkTrackingConfig(this.config))).promise];
              case 27:
                _j.sent();
                _j.label = 28;
              case 28:
                if (!isWebVitalsEnabled(this.config.autocapture)) return [3, 30];
                this.config.loggerProvider.debug("Adding web vitals plugin");
                return [4, this.add(webVitalsPlugin()).promise];
              case 29:
                _j.sent();
                _j.label = 30;
              case 30:
                if (!isPerformanceTrackingEnabled(this.config.autocapture)) return [3, 32];
                this.config.loggerProvider.debug("Adding performance tracking plugin");
                return [4, this.add(performancePlugin(getPerformanceTrackingConfig(this.config))).promise];
              case 31:
                _j.sent();
                _j.label = 32;
              case 32:
                if (!isPageUrlEnrichmentEnabled(this.config.autocapture)) return [3, 34];
                this.config.loggerProvider.debug("Adding referrer page url plugin");
                return [4, this.add(pageUrlEnrichmentPlugin()).promise];
              case 33:
                _j.sent();
                _j.label = 34;
              case 34:
                if (!isCustomEnrichmentEnabled(this.config.customEnrichment)) return [3, 36];
                this.config.loggerProvider.debug("Adding custom enrichment plugin");
                return [4, this.add(customEnrichmentPlugin()).promise];
              case 35:
                _j.sent();
                _j.label = 36;
              case 36:
                this.initializing = false;
                return [4, this.runQueuedFunctions("dispatchQ")];
              case 37:
                _j.sent();
                connector.eventBridge.setEventReceiver(function(event) {
                  var _a5 = event.eventProperties || {}, time = _a5.time, cleanEventProperties = __rest(_a5, ["time"]);
                  var eventOptions = typeof time === "number" ? { time } : void 0;
                  void _this.track(event.eventType, cleanEventProperties, eventOptions);
                });
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      AmplitudeBrowser2.prototype.getUserId = function() {
        var _a4;
        return (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.userId;
      };
      AmplitudeBrowser2.prototype.setUserId = function(userId) {
        if (!this.config) {
          this.q.push(this.setUserId.bind(this, userId));
          return;
        }
        this.config.loggerProvider.debug("function setUserId: ", userId);
        if (userId !== this.config.userId || userId === void 0) {
          this.config.userId = userId;
          this.timeline.onIdentityChanged({ userId });
          setConnectorUserId(userId, this.config.instanceName);
        }
      };
      AmplitudeBrowser2.prototype.getDeviceId = function() {
        var _a4;
        return (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.deviceId;
      };
      AmplitudeBrowser2.prototype.setDeviceId = function(deviceId) {
        if (!this.config) {
          this.q.push(this.setDeviceId.bind(this, deviceId));
          return;
        }
        this.config.loggerProvider.debug("function setDeviceId: ", deviceId);
        if (deviceId !== this.config.deviceId) {
          this.config.deviceId = deviceId;
          this.timeline.onIdentityChanged({ deviceId });
          setConnectorDeviceId(deviceId, this.config.instanceName);
        }
      };
      AmplitudeBrowser2.prototype.reset = function() {
        this.setDeviceId(UUID());
        this.setUserId(void 0);
        this.timeline.onReset();
      };
      AmplitudeBrowser2.prototype.getIdentity = function() {
        var _a4, _b;
        return {
          deviceId: (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.deviceId,
          userId: (_b = this.config) === null || _b === void 0 ? void 0 : _b.userId,
          userProperties: this.userProperties
        };
      };
      AmplitudeBrowser2.prototype.setIdentity = function(identity) {
        var e_1, _a4;
        var _b;
        if ("userId" in identity) {
          this.setUserId(identity.userId);
        }
        if ("deviceId" in identity && identity.deviceId) {
          this.setDeviceId(identity.deviceId);
        }
        if ("userProperties" in identity) {
          this.userProperties = identity.userProperties;
          var identifyObj = new Identify();
          var userProperties = (_b = identity.userProperties) !== null && _b !== void 0 ? _b : {};
          try {
            for (var _c = __values(Object.entries(userProperties)), _d = _c.next(); !_d.done; _d = _c.next()) {
              var _e = __read(_d.value, 2), key = _e[0], value = _e[1];
              identifyObj.set(key, value);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (_d && !_d.done && (_a4 = _c.return)) _a4.call(_c);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          this.identify(identifyObj);
        }
      };
      AmplitudeBrowser2.prototype.getOptOut = function() {
        var _a4;
        return (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.optOut;
      };
      AmplitudeBrowser2.prototype.getSessionId = function() {
        var _a4;
        return (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.sessionId;
      };
      AmplitudeBrowser2.prototype.setSessionId = function(sessionId) {
        var _a4;
        var promises = [];
        if (!this.config) {
          this.q.push(this.setSessionId.bind(this, sessionId));
          return returnWrapper(Promise.resolve());
        }
        if (this.config.optOut) {
          this.config.deferredSessionId = sessionId !== null && sessionId !== void 0 ? sessionId : UNSPECIFIED_SESSION_ID;
          return returnWrapper(Promise.resolve());
        }
        if (sessionId === void 0) {
          sessionId = Date.now();
        }
        if (sessionId === this.config.sessionId) {
          return returnWrapper(Promise.resolve());
        }
        this.config.loggerProvider.debug("function setSessionId: ", sessionId);
        var previousSessionId = this.getSessionId();
        if (previousSessionId !== sessionId) {
          this.timeline.onSessionIdChanged(sessionId);
        }
        var lastEventTime = this.config.lastEventTime;
        var lastEventId = (_a4 = this.config.lastEventId) !== null && _a4 !== void 0 ? _a4 : -1;
        this.config.sessionId = sessionId;
        this.config.lastEventTime = void 0;
        this.config.pageCounter = 0;
        if (isSessionTrackingEnabled(this.config.defaultTracking)) {
          if (previousSessionId && lastEventTime) {
            promises.push(this.track(DEFAULT_SESSION_END_EVENT, void 0, {
              device_id: this.previousSessionDeviceId,
              event_id: ++lastEventId,
              session_id: previousSessionId,
              time: lastEventTime + 1,
              user_id: this.previousSessionUserId
            }).promise);
          }
          this.config.lastEventTime = this.config.sessionId;
        }
        var isCampaignEventTracked = this.trackCampaignEventIfNeeded(++lastEventId, promises);
        if (this.config.identify) {
          promises.push(this.track(createIdentifyEvent(this.config.identify)).promise);
        }
        if (isSessionTrackingEnabled(this.config.defaultTracking)) {
          promises.push(this.track(DEFAULT_SESSION_START_EVENT, void 0, {
            event_id: isCampaignEventTracked ? ++lastEventId : lastEventId,
            session_id: this.config.sessionId,
            time: this.config.lastEventTime
          }).promise);
        }
        this.previousSessionDeviceId = this.config.deviceId;
        this.previousSessionUserId = this.config.userId;
        return returnWrapper(Promise.all(promises));
      };
      AmplitudeBrowser2.prototype.extendSession = function() {
        if (!this.config) {
          this.q.push(this.extendSession.bind(this));
          return;
        }
        this.config.lastEventTime = Date.now();
      };
      AmplitudeBrowser2.prototype.setTransport = function(transport) {
        if (!this.config) {
          this.q.push(this.setTransport.bind(this, transport));
          return;
        }
        this.config.transportProvider = createTransport(transport);
      };
      AmplitudeBrowser2.prototype.identify = function(identify2, eventOptions) {
        if (isInstanceProxy(identify2)) {
          var queue = identify2._q;
          identify2._q = [];
          identify2 = convertProxyObjectToRealObject(new Identify(), queue);
        }
        if (eventOptions === null || eventOptions === void 0 ? void 0 : eventOptions.user_id) {
          this.setUserId(eventOptions.user_id);
        }
        if (eventOptions === null || eventOptions === void 0 ? void 0 : eventOptions.device_id) {
          this.setDeviceId(eventOptions.device_id);
        }
        return _super.prototype.identify.call(this, identify2, eventOptions);
      };
      AmplitudeBrowser2.prototype.groupIdentify = function(groupType, groupName, identify2, eventOptions) {
        if (isInstanceProxy(identify2)) {
          var queue = identify2._q;
          identify2._q = [];
          identify2 = convertProxyObjectToRealObject(new Identify(), queue);
        }
        return _super.prototype.groupIdentify.call(this, groupType, groupName, identify2, eventOptions);
      };
      AmplitudeBrowser2.prototype.revenue = function(revenue2, eventOptions) {
        if (isInstanceProxy(revenue2)) {
          var queue = revenue2._q;
          revenue2._q = [];
          revenue2 = convertProxyObjectToRealObject(new Revenue(), queue);
        }
        return _super.prototype.revenue.call(this, revenue2, eventOptions);
      };
      AmplitudeBrowser2.prototype.trackCampaignEventIfNeeded = function(lastEventId, promises) {
        if (!this.webAttribution || !this.webAttribution.shouldTrackNewCampaign || !isUserPropertyAttributionEnabled(this.webAttribution.options)) {
          return false;
        }
        var campaignEvent = this.webAttribution.generateCampaignEvent(lastEventId);
        if (promises) {
          promises.push(this.track(campaignEvent).promise);
        } else {
          this.track(campaignEvent);
        }
        this.config.loggerProvider.log("Tracking attribution.");
        return true;
      };
      AmplitudeBrowser2.prototype.process = function(event) {
        return __awaiter(this, void 0, void 0, function() {
          var currentTime, isEventInNewSession, shouldSetSessionIdOnNewCampaign;
          return __generator(this, function(_a4) {
            currentTime = Date.now();
            isEventInNewSession = isNewSession(this.config.sessionTimeout, this.config.lastEventTime);
            shouldSetSessionIdOnNewCampaign = this.webAttribution && this.webAttribution.shouldSetSessionIdOnNewCampaign();
            if (event.event_type !== DEFAULT_SESSION_START_EVENT && event.event_type !== DEFAULT_SESSION_END_EVENT && (!event.session_id || event.session_id === this.getSessionId())) {
              if (isEventInNewSession || shouldSetSessionIdOnNewCampaign) {
                this.setSessionId(currentTime);
                if (shouldSetSessionIdOnNewCampaign) {
                  this.config.loggerProvider.log("Created a new session for new campaign.");
                }
              } else if (!isEventInNewSession) {
                this.trackCampaignEventIfNeeded();
              }
            }
            return [2, _super.prototype.process.call(this, event)];
          });
        });
      };
      AmplitudeBrowser2.prototype.logBrowserOptions = function(browserConfig) {
        try {
          var browserConfigCopy = __assign(__assign({}, browserConfig), { apiKey: browserConfig.apiKey.substring(0, 10) + "********" });
          this.config.loggerProvider.debug("Initialized Amplitude with BrowserConfig:", safeJsonStringify(browserConfigCopy));
        } catch (e2) {
          this.config.loggerProvider.error("Error logging browser config", e2);
        }
      };
      AmplitudeBrowser2.prototype._setDiagnosticsSampleRate = function(sampleRate) {
        if (sampleRate > 1 || sampleRate < 0) {
          return;
        }
        if (!this.config) {
          this._diagnosticsSampleRate = sampleRate;
          return;
        }
      };
      return AmplitudeBrowser2;
    })(AmplitudeCore)
  );

  // node_modules/@amplitude/analytics-browser/lib/esm/browser-client-factory.js
  var createInstance = function() {
    var client = new AmplitudeBrowser();
    return {
      init: debugWrapper(client.init.bind(client), "init", getClientLogConfig(client), getClientStates(client, ["config"])),
      add: debugWrapper(client.add.bind(client), "add", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.plugins"])),
      remove: debugWrapper(client.remove.bind(client), "remove", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.plugins"])),
      track: debugWrapper(client.track.bind(client), "track", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      logEvent: debugWrapper(client.logEvent.bind(client), "logEvent", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      identify: debugWrapper(client.identify.bind(client), "identify", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      groupIdentify: debugWrapper(client.groupIdentify.bind(client), "groupIdentify", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      setGroup: debugWrapper(client.setGroup.bind(client), "setGroup", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      revenue: debugWrapper(client.revenue.bind(client), "revenue", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      flush: debugWrapper(client.flush.bind(client), "flush", getClientLogConfig(client), getClientStates(client, ["config.apiKey", "timeline.queue.length"])),
      getUserId: debugWrapper(client.getUserId.bind(client), "getUserId", getClientLogConfig(client), getClientStates(client, ["config", "config.userId"])),
      setUserId: debugWrapper(client.setUserId.bind(client), "setUserId", getClientLogConfig(client), getClientStates(client, ["config", "config.userId"])),
      getDeviceId: debugWrapper(client.getDeviceId.bind(client), "getDeviceId", getClientLogConfig(client), getClientStates(client, ["config", "config.deviceId"])),
      setDeviceId: debugWrapper(client.setDeviceId.bind(client), "setDeviceId", getClientLogConfig(client), getClientStates(client, ["config", "config.deviceId"])),
      reset: debugWrapper(client.reset.bind(client), "reset", getClientLogConfig(client), getClientStates(client, ["config", "config.userId", "config.deviceId"])),
      getSessionId: debugWrapper(client.getSessionId.bind(client), "getSessionId", getClientLogConfig(client), getClientStates(client, ["config"])),
      setSessionId: debugWrapper(client.setSessionId.bind(client), "setSessionId", getClientLogConfig(client), getClientStates(client, ["config"])),
      extendSession: debugWrapper(client.extendSession.bind(client), "extendSession", getClientLogConfig(client), getClientStates(client, ["config"])),
      setOptOut: debugWrapper(client.setOptOut.bind(client), "setOptOut", getClientLogConfig(client), getClientStates(client, ["config"])),
      setTransport: debugWrapper(client.setTransport.bind(client), "setTransport", getClientLogConfig(client), getClientStates(client, ["config"])),
      getIdentity: debugWrapper(client.getIdentity.bind(client), "getIdentity", getClientLogConfig(client), getClientStates(client, ["config"])),
      setIdentity: debugWrapper(client.setIdentity.bind(client), "setIdentity", getClientLogConfig(client), getClientStates(client, ["config", "config.userId", "config.deviceId"])),
      getOptOut: debugWrapper(client.getOptOut.bind(client), "getOptOut", getClientLogConfig(client), getClientStates(client, ["config"])),
      _setDiagnosticsSampleRate: debugWrapper(client._setDiagnosticsSampleRate.bind(client), "_setDiagnosticsSampleRate", getClientLogConfig(client), getClientStates(client, ["config"]))
    };
  };
  var browser_client_factory_default = createInstance();

  // node_modules/@amplitude/analytics-browser/lib/esm/index.js
  var add = browser_client_factory_default.add;
  var extendSession = browser_client_factory_default.extendSession;
  var flush = browser_client_factory_default.flush;
  var getDeviceId = browser_client_factory_default.getDeviceId;
  var getIdentity = browser_client_factory_default.getIdentity;
  var getOptOut = browser_client_factory_default.getOptOut;
  var getSessionId = browser_client_factory_default.getSessionId;
  var getUserId = browser_client_factory_default.getUserId;
  var groupIdentify = browser_client_factory_default.groupIdentify;
  var identify = browser_client_factory_default.identify;
  var init = browser_client_factory_default.init;
  var logEvent = browser_client_factory_default.logEvent;
  var remove = browser_client_factory_default.remove;
  var reset = browser_client_factory_default.reset;
  var revenue = browser_client_factory_default.revenue;
  var setDeviceId = browser_client_factory_default.setDeviceId;
  var setGroup = browser_client_factory_default.setGroup;
  var setIdentity = browser_client_factory_default.setIdentity;
  var setOptOut = browser_client_factory_default.setOptOut;
  var setSessionId = browser_client_factory_default.setSessionId;
  var setTransport = browser_client_factory_default.setTransport;
  var setUserId = browser_client_factory_default.setUserId;
  var track = browser_client_factory_default.track;
  var _setDiagnosticsSampleRate = browser_client_factory_default._setDiagnosticsSampleRate;

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/session-replay.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/session-replay-factory.js
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/config/local-config.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/constants.js
  init_esm();
  var DEFAULT_EVENT_PROPERTY_PREFIX = "[Amplitude]";
  var DEFAULT_SESSION_REPLAY_PROPERTY = "".concat(DEFAULT_EVENT_PROPERTY_PREFIX, " Session Replay ID");
  var DEFAULT_SAMPLE_RATE = 0;
  var DEFAULT_SERVER_ZONE2 = ServerZone.US;
  var DEFAULT_PERFORMANCE_CONFIG = { enabled: true };
  var DEFAULT_URL_CHANGE_POLLING_INTERVAL = 1e3;
  var SESSION_REPLAY_DEBUG_PROPERTY = "".concat(DEFAULT_EVENT_PROPERTY_PREFIX, " Session Replay Debug");
  var BLOCK_CLASS = "amp-block";
  var MASK_TEXT_CLASS = "amp-mask";
  var UNMASK_TEXT_CLASS = "amp-unmask";
  var SESSION_REPLAY_SERVER_URL = "https://api-sr.amplitude.com/sessions/v2/track";
  var SESSION_REPLAY_EU_URL = "https://api-sr.eu.amplitude.com/sessions/v2/track";
  var SESSION_REPLAY_STAGING_URL = "https://api-sr.stag2.amplitude.com/sessions/v2/track";
  var STORAGE_PREFIX2 = "".concat(AMPLITUDE_PREFIX, "_replay_unsent");
  var MAX_EVENT_LIST_SIZE = 2e6;
  var DEFAULT_MAX_PERSISTED_EVENTS_SIZE_BYTES = 6e6;
  var MAX_SINGLE_EVENT_SIZE = 9 * 1e6;
  var WAF_PAYLOAD_TOO_LARGE_PATTERN = /payload.*exceed/i;
  var INTERACTION_MIN_INTERVAL = 3e4;
  var INTERACTION_MAX_INTERVAL = 6e4;
  var MIN_INTERVAL = 500;
  var MAX_INTERVAL = 10 * 1e3;
  var DEFAULT_FLUSH_MIN_INTERVAL_MS = 1e3;
  var DEFAULT_FLUSH_MAX_INTERVAL_MS = 10 * 1e3;
  var MAX_IDB_STORAGE_LENGTH = 1e3 * 60 * 60 * 24 * 3;
  var KB_SIZE = 1024;
  var MAX_URL_LENGTH = 1e3;
  var MAX_KEEPALIVE_BYTES = 64 * 1024;
  var SEND_TIMEOUT_MS = 1e4;
  var EVENT_SKIPPED_HEADER = "X-Session-Replay-Event-Skipped";
  var EVENT_SKIP_CODE_THROTTLED = "429";
  var EVENT_SKIP_CODE_INVALID_RANGE = "4004";
  var EVENT_SKIP_CODE_CAPTURE_DISABLED = "4005";
  var THROTTLED_FLUSH_PAUSE_MS = 6e4;
  var MERGE_AFTER_THROTTLE_SOFT_CAP = 2 * MAX_EVENT_LIST_SIZE;
  var CROSS_ORIGIN_IFRAME_MESSAGE_TYPE = "amplitude-sr-iframe";
  var CustomRRwebEvent;
  (function(CustomRRwebEvent2) {
    CustomRRwebEvent2["GET_SR_PROPS"] = "get-sr-props";
    CustomRRwebEvent2["DEBUG_INFO"] = "debug-info";
    CustomRRwebEvent2["FETCH_REQUEST"] = "fetch-request";
    CustomRRwebEvent2["METADATA"] = "metadata";
    CustomRRwebEvent2["TARGETING_DECISION"] = "targeting-decision";
    CustomRRwebEvent2["REPLAY_GATE_DECISION"] = "replay-gate-decision";
  })(CustomRRwebEvent || (CustomRRwebEvent = {}));

  // node_modules/@amplitude/session-replay-browser/lib/esm/logger.js
  var SafeLoggerProvider = (
    /** @class */
    (function() {
      function SafeLoggerProvider2(loggerProvider) {
        this.logger = loggerProvider;
        this.log = this.getSafeMethod("log");
        this.warn = this.getSafeMethod("warn");
        this.error = this.getSafeMethod("error");
        this.debug = this.getSafeMethod("debug");
      }
      SafeLoggerProvider2.prototype.getSafeMethod = function(method) {
        var _a4;
        if (!this.logger) {
          return (function() {
          });
        }
        var fn = this.logger[method];
        if (typeof fn === "function") {
          var originalFn2 = (_a4 = fn.__rrweb_original__) !== null && _a4 !== void 0 ? _a4 : fn;
          return originalFn2.bind(this.logger);
        }
        return (function() {
        });
      };
      SafeLoggerProvider2.prototype.enable = function(logLevel) {
        this.logger.enable(logLevel);
      };
      SafeLoggerProvider2.prototype.disable = function() {
        this.logger.disable();
      };
      return SafeLoggerProvider2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/helpers.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/config/types.js
  var DEFAULT_MASK_LEVEL = "medium";

  // node_modules/@amplitude/session-replay-browser/lib/esm/utils/get-input-type.js
  function toLowerCase(str) {
    return str.toLowerCase();
  }
  function getInputType(element) {
    var type = element.type;
    return element.hasAttribute("data-rr-is-password") ? "password" : type ? (
      // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion
      toLowerCase(type)
    ) : null;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/utils/server-url.js
  function getServerUrl2(serverZone, trackServerUrl) {
    if (trackServerUrl)
      return trackServerUrl;
    if (serverZone === "STAGING")
      return SESSION_REPLAY_STAGING_URL;
    if (serverZone === "EU")
      return SESSION_REPLAY_EU_URL;
    return SESSION_REPLAY_SERVER_URL;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/helpers.js
  var isMaskedForLevel = function(elementType, level, element) {
    switch (level) {
      case "light": {
        if (elementType !== "input") {
          return false;
        }
        var inputType = element ? getInputType(element) : "";
        if (!inputType) {
          return false;
        }
        if (["password", "hidden", "email", "tel"].includes(inputType)) {
          return true;
        }
        if (element.autocomplete.startsWith("cc-")) {
          return true;
        }
        return false;
      }
      case "medium":
        return elementType === "input";
      case "conservative":
        return true;
      default:
        return isMaskedForLevel(elementType, DEFAULT_MASK_LEVEL, element);
    }
  };
  var getEffectiveMaskLevel = function(url, config2) {
    var e_1, _a4;
    var _b;
    if (url && config2.urlMaskLevels) {
      try {
        for (var _c = __values(config2.urlMaskLevels), _d = _c.next(); !_d.done; _d = _c.next()) {
          var rule3 = _d.value;
          if (globToRegex(rule3.match).test(url)) {
            return rule3.maskLevel;
          }
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (_d && !_d.done && (_a4 = _c.return)) _a4.call(_c);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    }
    return (_b = config2.defaultMaskLevel) !== null && _b !== void 0 ? _b : DEFAULT_MASK_LEVEL;
  };
  var isMasked = function(elementType, config2, element, currentUrl) {
    var _a4, _b;
    if (config2 === void 0) {
      config2 = { defaultMaskLevel: DEFAULT_MASK_LEVEL };
    }
    if (element) {
      if (element.closest("." + MASK_TEXT_CLASS)) {
        return true;
      }
      var shouldMask = ((_a4 = config2.maskSelector) !== null && _a4 !== void 0 ? _a4 : []).some(function(selector2) {
        return element.closest(selector2);
      });
      if (shouldMask) {
        return true;
      }
      if (element.closest("." + UNMASK_TEXT_CLASS)) {
        return false;
      }
      var shouldUnmask = ((_b = config2.unmaskSelector) !== null && _b !== void 0 ? _b : []).some(function(selector2) {
        return element.closest(selector2);
      });
      if (shouldUnmask) {
        return false;
      }
    }
    return isMaskedForLevel(elementType, getEffectiveMaskLevel(currentUrl, config2), element);
  };
  var maskFn = function(elementType, config2, getCurrentUrl2) {
    return function(text, element) {
      return isMasked(elementType, config2, element, getCurrentUrl2 === null || getCurrentUrl2 === void 0 ? void 0 : getCurrentUrl2()) ? text.replace(/[^\s]/g, "*") : text;
    };
  };
  var maskAttributeFn = function(config2, getCurrentUrl2) {
    return function(key, value, element) {
      var _a4;
      if (key === "style")
        return value;
      if (!((_a4 = config2 === null || config2 === void 0 ? void 0 : config2.maskAttributes) !== null && _a4 !== void 0 ? _a4 : []).includes(key))
        return value;
      var elementType = ["INPUT", "SELECT", "TEXTAREA"].includes(element.tagName) ? "input" : "text";
      return isMasked(elementType, config2, element, getCurrentUrl2 === null || getCurrentUrl2 === void 0 ? void 0 : getCurrentUrl2()) ? value.replace(/[^\s]/g, "*") : value;
    };
  };
  var getCurrentUrl = function() {
    var globalScope3 = getGlobalScope();
    return (globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location) ? globalScope3.location.href : "";
  };
  var generateSessionReplayId = function(sessionId, deviceId) {
    return "".concat(deviceId, "/").concat(sessionId);
  };
  var isValidGlobUrl = function(globUrl) {
    if (typeof globUrl !== "string" || globUrl.trim() === "")
      return false;
    var urlPattern = /^\/|^https?:\/\/[^\s]+$/;
    if (!urlPattern.test(globUrl))
      return false;
    return true;
  };
  var globRegexCache = /* @__PURE__ */ new Map();
  var globToRegex = function(glob) {
    var cached = globRegexCache.get(glob);
    if (cached)
      return cached;
    var T_TRAILING = "\0TRAIL\0";
    var T_MIDDLE = "\0MID\0";
    var T_DSTAR = "\0DS\0";
    var T_STAR = "\0ST\0";
    var T_QUEST = "\0QU\0";
    var s2 = glob;
    s2 = s2.replace(/\/\*\*$/, T_TRAILING);
    s2 = s2.replace(/\/\*\*\//g, T_MIDDLE);
    s2 = s2.replace(/\*\*/g, T_DSTAR);
    s2 = s2.replace(/\*/g, T_STAR);
    s2 = s2.replace(/\?/g, T_QUEST);
    s2 = s2.replace(/[.+^${}()|[\]\\]/g, "\\$&");
    s2 = s2.split(T_TRAILING).join("(/.*)?");
    s2 = s2.split(T_MIDDLE).join("/(.*\\/)?");
    s2 = s2.split(T_DSTAR).join(".*");
    s2 = s2.split(T_STAR).join(".*");
    s2 = s2.split(T_QUEST).join(".");
    var regex = new RegExp("^".concat(s2, "$"));
    globRegexCache.set(glob, regex);
    return regex;
  };
  var validateUGCFilterRules = function(ugcFilterRules) {
    if (!ugcFilterRules.every(function(rule3) {
      return typeof rule3.selector === "string" && typeof rule3.replacement === "string";
    })) {
      throw new Error("ugcFilterRules must be an array of objects with selector and replacement properties");
    }
    if (!ugcFilterRules.every(function(rule3) {
      return isValidGlobUrl(rule3.selector);
    })) {
      throw new Error("ugcFilterRules must be an array of objects with valid globs");
    }
  };
  var getPageUrl = function(pageUrl, ugcFilterRules) {
    var e_2, _a4;
    try {
      for (var ugcFilterRules_1 = __values(ugcFilterRules), ugcFilterRules_1_1 = ugcFilterRules_1.next(); !ugcFilterRules_1_1.done; ugcFilterRules_1_1 = ugcFilterRules_1.next()) {
        var rule3 = ugcFilterRules_1_1.value;
        var regex = globToRegex(rule3.selector);
        if (regex.test(pageUrl)) {
          return pageUrl.replace(regex, rule3.replacement);
        }
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (ugcFilterRules_1_1 && !ugcFilterRules_1_1.done && (_a4 = ugcFilterRules_1.return)) _a4.call(ugcFilterRules_1);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    return pageUrl;
  };
  var getStorageSize = function() {
    return __awaiter(void 0, void 0, void 0, function() {
      var globalScope3, _a4, usage, quota, usageDetails, totalStorageSize, percentOfQuota, e_3;
      return __generator(this, function(_b) {
        switch (_b.label) {
          case 0:
            _b.trys.push([0, 3, , 4]);
            globalScope3 = getGlobalScope();
            if (!globalScope3) return [3, 2];
            return [4, globalScope3.navigator.storage.estimate()];
          case 1:
            _a4 = _b.sent(), usage = _a4.usage, quota = _a4.quota, usageDetails = _a4.usageDetails;
            totalStorageSize = usage ? Math.round(usage / KB_SIZE) : 0;
            percentOfQuota = usage && quota ? Math.round((usage / quota + Number.EPSILON) * 1e3) / 1e3 : 0;
            return [2, { totalStorageSize, percentOfQuota, usageDetails: JSON.stringify(usageDetails) }];
          case 2:
            return [3, 4];
          case 3:
            e_3 = _b.sent();
            return [3, 4];
          case 4:
            return [2, { totalStorageSize: 0, percentOfQuota: 0, usageDetails: "" }];
        }
      });
    });
  };
  var getDebugConfig = function(config2) {
    var debugConfig = __assign({}, config2);
    var apiKey2 = debugConfig.apiKey;
    debugConfig.apiKey = "****".concat(apiKey2.substring(apiKey2.length - 4));
    return debugConfig;
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/config/local-config.js
  var getDefaultConfig2 = function() {
    return {
      flushMaxRetries: 2,
      logLevel: LogLevel.Warn,
      loggerProvider: new Logger(),
      transportProvider: new FetchTransport()
    };
  };
  var SessionReplayLocalConfig = (
    /** @class */
    (function(_super) {
      __extends(SessionReplayLocalConfig2, _super);
      function SessionReplayLocalConfig2(apiKey2, options) {
        var _this = this;
        var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p;
        var defaultConfig = getDefaultConfig2();
        _this = _super.call(this, __assign(__assign({ transportProvider: defaultConfig.transportProvider, loggerProvider: new SafeLoggerProvider(options.loggerProvider || defaultConfig.loggerProvider) }, options), { apiKey: apiKey2 })) || this;
        _this.flushMaxRetries = options.flushMaxRetries !== void 0 && options.flushMaxRetries <= defaultConfig.flushMaxRetries ? options.flushMaxRetries : defaultConfig.flushMaxRetries;
        _this.apiKey = apiKey2;
        _this.sampleRate = options.sampleRate || DEFAULT_SAMPLE_RATE;
        _this.serverZone = options.serverZone || DEFAULT_SERVER_ZONE2;
        _this.configServerUrl = options.configServerUrl;
        _this.trackServerUrl = options.trackServerUrl;
        _this.shouldInlineStylesheet = options.shouldInlineStylesheet;
        _this.inlineImages = options.inlineImages;
        _this.version = options.version;
        _this.performanceConfig = options.performanceConfig || DEFAULT_PERFORMANCE_CONFIG;
        _this.storeType = (_a4 = options.storeType) !== null && _a4 !== void 0 ? _a4 : "memory";
        _this.applyBackgroundColorToBlockedElements = (_b = options.applyBackgroundColorToBlockedElements) !== null && _b !== void 0 ? _b : false;
        _this.enableUrlChangePolling = (_c = options.enableUrlChangePolling) !== null && _c !== void 0 ? _c : false;
        _this.urlChangePollingInterval = (_d = options.urlChangePollingInterval) !== null && _d !== void 0 ? _d : DEFAULT_URL_CHANGE_POLLING_INTERVAL;
        _this.captureDocumentTitle = (_e = options.captureDocumentTitle) !== null && _e !== void 0 ? _e : false;
        if (options.fullSnapshotIntervalMs !== void 0) {
          _this.fullSnapshotIntervalMs = options.fullSnapshotIntervalMs;
        }
        if (options.eagerFullSnapshotSend !== void 0) {
          _this.eagerFullSnapshotSend = options.eagerFullSnapshotSend;
        }
        _this.captureFullSnapshotOnFocus = (_f = options.captureFullSnapshotOnFocus) !== null && _f !== void 0 ? _f : false;
        if (options.maxPersistedEventsSizeBytes !== void 0) {
          _this.maxPersistedEventsSizeBytes = sanitizeByteSize(options.maxPersistedEventsSizeBytes, MIN_EVENT_BYTE_SIZE, MAX_PERSISTED_EVENTS_SIZE_CEILING, "maxPersistedEventsSizeBytes", _this.loggerProvider);
        }
        if (options.maxSingleEventSizeBytes !== void 0) {
          _this.maxSingleEventSizeBytes = sanitizeByteSize(options.maxSingleEventSizeBytes, MIN_EVENT_BYTE_SIZE, MAX_SINGLE_EVENT_SIZE_CEILING, "maxSingleEventSizeBytes", _this.loggerProvider);
        }
        _this.privacyConfig = __assign(__assign({}, (_g = options.privacyConfig) !== null && _g !== void 0 ? _g : {}), { unmaskSelector: Array.from(new Set(__spreadArray([".".concat(UNMASK_TEXT_CLASS)], __read((_j = (_h = options.privacyConfig) === null || _h === void 0 ? void 0 : _h.unmaskSelector) !== null && _j !== void 0 ? _j : []), false))) });
        if (options.interactionConfig) {
          _this.interactionConfig = options.interactionConfig;
          if (_this.interactionConfig.ugcFilterRules) {
            validateUGCFilterRules(_this.interactionConfig.ugcFilterRules);
          }
        }
        if (options.debugMode) {
          _this.debugMode = options.debugMode;
        }
        var legacyOptions = options;
        _this.useWebWorker = (_m = (_k = options.useWebWorker) !== null && _k !== void 0 ? _k : (_l = legacyOptions.experimental) === null || _l === void 0 ? void 0 : _l.useWebWorker) !== null && _m !== void 0 ? _m : true;
        _this.enableTransportCompression = (_o = options.enableTransportCompression) !== null && _o !== void 0 ? _o : true;
        _this.sendTimeoutMs = options.sendTimeoutMs;
        _this.captureAdoptedStyleSheets = (_p = options.captureAdoptedStyleSheets) !== null && _p !== void 0 ? _p : true;
        if (options.crossOriginIframes) {
          _this.crossOriginIframes = options.crossOriginIframes;
        }
        if (options.flushIntervalConfig) {
          _this.flushIntervalConfig = sanitizeFlushIntervalConfig(options.flushIntervalConfig, _this.loggerProvider);
        } else {
          _this.flushIntervalConfig = {
            minIntervalMs: DEFAULT_FLUSH_MIN_INTERVAL_MS,
            maxIntervalMs: DEFAULT_FLUSH_MAX_INTERVAL_MS
          };
        }
        _this.handleSendEvents = options.handleSendEvents;
        _this.handleFetchConfig = options.handleFetchConfig;
        return _this;
      }
      return SessionReplayLocalConfig2;
    })(Config)
  );
  var MIN_FLUSH_INTERVAL_FLOOR_MS = 100;
  var MIN_EVENT_BYTE_SIZE = 1e3;
  var MAX_PERSISTED_EVENTS_SIZE_CEILING = 8e6;
  var MAX_SINGLE_EVENT_SIZE_CEILING = 1e7;
  function sanitizeByteSize(raw, min, max, name, loggerProvider) {
    if (!Number.isFinite(raw)) {
      loggerProvider.warn("".concat(name, " value is not a finite number (got ").concat(String(raw), "); ignoring."));
      return void 0;
    }
    if (raw < min) {
      loggerProvider.warn("".concat(name, " ").concat(raw, " is below floor ").concat(min, "; clamping."));
      return min;
    }
    if (raw > max) {
      loggerProvider.warn("".concat(name, " ").concat(raw, " exceeds ceiling ").concat(max, "; clamping."));
      return max;
    }
    return raw;
  }
  function sanitizeFlushIntervalConfig(raw, loggerProvider) {
    var _a4, _b;
    var sanitized = {};
    if (raw.minIntervalMs !== void 0) {
      if (!Number.isFinite(raw.minIntervalMs) || raw.minIntervalMs < MIN_FLUSH_INTERVAL_FLOOR_MS) {
        loggerProvider.warn("flushIntervalConfig.minIntervalMs ".concat(raw.minIntervalMs, " is below floor ").concat(MIN_FLUSH_INTERVAL_FLOOR_MS, "ms; clamping."));
        sanitized.minIntervalMs = MIN_FLUSH_INTERVAL_FLOOR_MS;
      } else {
        sanitized.minIntervalMs = raw.minIntervalMs;
      }
    }
    if (raw.maxIntervalMs !== void 0) {
      if (Number.isNaN(raw.maxIntervalMs) || raw.maxIntervalMs < MIN_FLUSH_INTERVAL_FLOOR_MS) {
        loggerProvider.warn("flushIntervalConfig.maxIntervalMs ".concat(raw.maxIntervalMs, " is below floor ").concat(MIN_FLUSH_INTERVAL_FLOOR_MS, "ms; clamping."));
        sanitized.maxIntervalMs = MIN_FLUSH_INTERVAL_FLOOR_MS;
      } else {
        sanitized.maxIntervalMs = raw.maxIntervalMs;
      }
    }
    if (sanitized.minIntervalMs !== void 0 || sanitized.maxIntervalMs !== void 0) {
      var effectiveMin = (_a4 = sanitized.minIntervalMs) !== null && _a4 !== void 0 ? _a4 : MIN_INTERVAL;
      var effectiveMax = (_b = sanitized.maxIntervalMs) !== null && _b !== void 0 ? _b : MAX_INTERVAL;
      if (effectiveMax < effectiveMin) {
        if (sanitized.maxIntervalMs === void 0) {
          loggerProvider.warn("flushIntervalConfig.minIntervalMs (".concat(effectiveMin, ") exceeds the default maxIntervalMs (").concat(MAX_INTERVAL, "); raising max to match min."));
          sanitized.maxIntervalMs = effectiveMin;
        } else if (sanitized.minIntervalMs === void 0) {
          loggerProvider.warn("flushIntervalConfig.maxIntervalMs (".concat(effectiveMax, ") is below the default minIntervalMs (").concat(MIN_INTERVAL, "); lowering min to match max."));
          sanitized.minIntervalMs = effectiveMax;
        } else {
          loggerProvider.warn("flushIntervalConfig.maxIntervalMs (".concat(sanitized.maxIntervalMs, ") is less than minIntervalMs (").concat(sanitized.minIntervalMs, "); raising max to match min."));
          sanitized.maxIntervalMs = sanitized.minIntervalMs;
        }
      }
    }
    return sanitized;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/session-replay.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/node_modules/@amplitude/rrweb-types/dist/rrweb-types.js
  var EventType = /* @__PURE__ */ ((EventType22) => {
    EventType22[EventType22["DomContentLoaded"] = 0] = "DomContentLoaded";
    EventType22[EventType22["Load"] = 1] = "Load";
    EventType22[EventType22["FullSnapshot"] = 2] = "FullSnapshot";
    EventType22[EventType22["IncrementalSnapshot"] = 3] = "IncrementalSnapshot";
    EventType22[EventType22["Meta"] = 4] = "Meta";
    EventType22[EventType22["Custom"] = 5] = "Custom";
    EventType22[EventType22["Plugin"] = 6] = "Plugin";
    return EventType22;
  })(EventType || {});
  var IncrementalSource = /* @__PURE__ */ ((IncrementalSource22) => {
    IncrementalSource22[IncrementalSource22["Mutation"] = 0] = "Mutation";
    IncrementalSource22[IncrementalSource22["MouseMove"] = 1] = "MouseMove";
    IncrementalSource22[IncrementalSource22["MouseInteraction"] = 2] = "MouseInteraction";
    IncrementalSource22[IncrementalSource22["Scroll"] = 3] = "Scroll";
    IncrementalSource22[IncrementalSource22["ViewportResize"] = 4] = "ViewportResize";
    IncrementalSource22[IncrementalSource22["Input"] = 5] = "Input";
    IncrementalSource22[IncrementalSource22["TouchMove"] = 6] = "TouchMove";
    IncrementalSource22[IncrementalSource22["MediaInteraction"] = 7] = "MediaInteraction";
    IncrementalSource22[IncrementalSource22["StyleSheetRule"] = 8] = "StyleSheetRule";
    IncrementalSource22[IncrementalSource22["CanvasMutation"] = 9] = "CanvasMutation";
    IncrementalSource22[IncrementalSource22["Font"] = 10] = "Font";
    IncrementalSource22[IncrementalSource22["Log"] = 11] = "Log";
    IncrementalSource22[IncrementalSource22["Drag"] = 12] = "Drag";
    IncrementalSource22[IncrementalSource22["StyleDeclaration"] = 13] = "StyleDeclaration";
    IncrementalSource22[IncrementalSource22["Selection"] = 14] = "Selection";
    IncrementalSource22[IncrementalSource22["AdoptedStyleSheet"] = 15] = "AdoptedStyleSheet";
    IncrementalSource22[IncrementalSource22["CustomElement"] = 16] = "CustomElement";
    return IncrementalSource22;
  })(IncrementalSource || {});
  var MouseInteractions = /* @__PURE__ */ ((MouseInteractions22) => {
    MouseInteractions22[MouseInteractions22["MouseUp"] = 0] = "MouseUp";
    MouseInteractions22[MouseInteractions22["MouseDown"] = 1] = "MouseDown";
    MouseInteractions22[MouseInteractions22["Click"] = 2] = "Click";
    MouseInteractions22[MouseInteractions22["ContextMenu"] = 3] = "ContextMenu";
    MouseInteractions22[MouseInteractions22["DblClick"] = 4] = "DblClick";
    MouseInteractions22[MouseInteractions22["Focus"] = 5] = "Focus";
    MouseInteractions22[MouseInteractions22["Blur"] = 6] = "Blur";
    MouseInteractions22[MouseInteractions22["TouchStart"] = 7] = "TouchStart";
    MouseInteractions22[MouseInteractions22["TouchMove_Departed"] = 8] = "TouchMove_Departed";
    MouseInteractions22[MouseInteractions22["TouchEnd"] = 9] = "TouchEnd";
    MouseInteractions22[MouseInteractions22["TouchCancel"] = 10] = "TouchCancel";
    return MouseInteractions22;
  })(MouseInteractions || {});

  // node_modules/@amplitude/session-replay-browser/lib/esm/config/joined-config.js
  init_tslib_es6();
  init_esm();
  var REMOTE_CONFIG_TIMEOUT_MS = 1500;
  var removeInvalidSelectorsFromPrivacyConfig = function(privacyConfig, loggerProvider) {
    var fragment = document.createDocumentFragment();
    var dropInvalidSelectors = function(selectors) {
      if (selectors === void 0) {
        selectors = [];
      }
      if (typeof selectors === "string") {
        selectors = [selectors];
      }
      selectors = selectors.filter(function(selector2) {
        try {
          fragment.querySelector(selector2);
        } catch (_a4) {
          loggerProvider.warn('[session-replay-browser] omitting selector "'.concat(selector2, '" because it is invalid'));
          return false;
        }
        return true;
      });
      if (selectors.length === 0) {
        return void 0;
      }
      return selectors;
    };
    privacyConfig.blockSelector = dropInvalidSelectors(privacyConfig.blockSelector);
    privacyConfig.maskSelector = dropInvalidSelectors(privacyConfig.maskSelector);
    privacyConfig.unmaskSelector = dropInvalidSelectors(privacyConfig.unmaskSelector);
    return privacyConfig;
  };
  var SessionReplayJoinedConfigGenerator = (
    /** @class */
    (function() {
      function SessionReplayJoinedConfigGenerator2(remoteConfigClient, localConfig) {
        this.localConfig = localConfig;
        this.remoteConfigClient = remoteConfigClient;
      }
      SessionReplayJoinedConfigGenerator2.prototype.generateJoinedConfig = function() {
        var _a4, _b, _c, _d, _e, _f, _g;
        return __awaiter(this, void 0, void 0, function() {
          var config2, sessionReplayRemoteConfig, error_1, samplingConfig, remotePrivacyConfig, targetingConfig, localPrivacyConfig, joinedPrivacyConfig, privacyConfigSelectorMap, selectorMap, _h, _j, _k, selector2, selectorType;
          var e_1, _l;
          var _this = this;
          return __generator(this, function(_m) {
            switch (_m.label) {
              case 0:
                config2 = __assign({}, this.localConfig);
                config2.optOut = this.localConfig.optOut;
                config2.captureEnabled = true;
                _m.label = 1;
              case 1:
                _m.trys.push([1, 3, , 4]);
                return [4, new Promise(function(resolve, reject) {
                  _this.remoteConfigClient.subscribe("configs.sessionReplay", { timeout: REMOTE_CONFIG_TIMEOUT_MS }, function(remoteConfig, source) {
                    var _a5;
                    _this.localConfig.loggerProvider.debug("Session Replay remote configuration received from ".concat(source, ":"), JSON.stringify(remoteConfig, null, 2));
                    if (!remoteConfig) {
                      reject(new Error("No remote config received"));
                      return;
                    }
                    var namespaceConfig = remoteConfig;
                    var samplingConfig2 = namespaceConfig.sr_sampling_config;
                    var privacyConfig = namespaceConfig.sr_privacy_config;
                    var targetingConfig2 = namespaceConfig.sr_targeting_config;
                    var ugcFilterRules = (_a5 = config2.interactionConfig) === null || _a5 === void 0 ? void 0 : _a5.ugcFilterRules;
                    config2.interactionConfig = namespaceConfig.sr_interaction_config;
                    if (config2.interactionConfig && ugcFilterRules) {
                      config2.interactionConfig.ugcFilterRules = ugcFilterRules;
                    }
                    config2.loggingConfig = namespaceConfig.sr_logging_config;
                    if (samplingConfig2 || privacyConfig || targetingConfig2) {
                      sessionReplayRemoteConfig = {};
                      if (samplingConfig2) {
                        sessionReplayRemoteConfig.sr_sampling_config = samplingConfig2;
                      }
                      if (privacyConfig) {
                        sessionReplayRemoteConfig.sr_privacy_config = privacyConfig;
                      }
                      if (targetingConfig2) {
                        sessionReplayRemoteConfig.sr_targeting_config = targetingConfig2;
                      }
                    }
                    resolve();
                  });
                })];
              case 2:
                _m.sent();
                return [3, 4];
              case 3:
                error_1 = _m.sent();
                this.localConfig.loggerProvider.error("Failed to generate joined config: ", error_1);
                config2.captureEnabled = false;
                return [2, {
                  localConfig: this.localConfig,
                  joinedConfig: config2,
                  remoteConfig: void 0
                }];
              case 4:
                if (!sessionReplayRemoteConfig) {
                  return [2, {
                    localConfig: this.localConfig,
                    joinedConfig: config2,
                    remoteConfig: sessionReplayRemoteConfig
                  }];
                }
                samplingConfig = sessionReplayRemoteConfig.sr_sampling_config, remotePrivacyConfig = sessionReplayRemoteConfig.sr_privacy_config, targetingConfig = sessionReplayRemoteConfig.sr_targeting_config;
                if (samplingConfig && Object.keys(samplingConfig).length > 0) {
                  if (Object.prototype.hasOwnProperty.call(samplingConfig, "capture_enabled")) {
                    config2.captureEnabled = samplingConfig.capture_enabled;
                  } else {
                    config2.captureEnabled = false;
                  }
                  if (Object.prototype.hasOwnProperty.call(samplingConfig, "sample_rate")) {
                    config2.sampleRate = samplingConfig.sample_rate;
                  }
                  if (Object.prototype.hasOwnProperty.call(samplingConfig, "min_session_duration_ms")) {
                    config2.minSessionDurationMs = this.sanitizeMinSessionDurationMs(samplingConfig.min_session_duration_ms);
                  }
                } else {
                  config2.captureEnabled = true;
                  this.localConfig.loggerProvider.debug("Remote config successfully fetched, but no values set for project, Session Replay capture enabled.");
                }
                if (remotePrivacyConfig) {
                  localPrivacyConfig = (_a4 = config2.privacyConfig) !== null && _a4 !== void 0 ? _a4 : {};
                  joinedPrivacyConfig = {
                    defaultMaskLevel: (_c = (_b = remotePrivacyConfig.defaultMaskLevel) !== null && _b !== void 0 ? _b : localPrivacyConfig.defaultMaskLevel) !== null && _c !== void 0 ? _c : "medium",
                    blockSelector: [],
                    maskSelector: [],
                    unmaskSelector: [],
                    maskAttributes: __spreadArray([], __read(new Set(__spreadArray(__spreadArray([], __read((_d = localPrivacyConfig.maskAttributes) !== null && _d !== void 0 ? _d : []), false), __read((_e = remotePrivacyConfig.maskAttributes) !== null && _e !== void 0 ? _e : []), false))), false),
                    urlMaskLevels: __spreadArray(__spreadArray([], __read((_f = remotePrivacyConfig.urlMaskLevels) !== null && _f !== void 0 ? _f : []), false), __read((_g = localPrivacyConfig.urlMaskLevels) !== null && _g !== void 0 ? _g : []), false)
                  };
                  privacyConfigSelectorMap = function(privacyConfig) {
                    var e_2, _a5, e_3, _b2, e_4, _c2;
                    var _d2, _e2, _f2;
                    var selectorMap2 = {};
                    if (typeof privacyConfig.blockSelector === "string") {
                      privacyConfig.blockSelector = [privacyConfig.blockSelector];
                    }
                    try {
                      for (var _g2 = __values((_d2 = privacyConfig.blockSelector) !== null && _d2 !== void 0 ? _d2 : []), _h2 = _g2.next(); !_h2.done; _h2 = _g2.next()) {
                        var selector3 = _h2.value;
                        selectorMap2[selector3] = "block";
                      }
                    } catch (e_2_1) {
                      e_2 = { error: e_2_1 };
                    } finally {
                      try {
                        if (_h2 && !_h2.done && (_a5 = _g2.return)) _a5.call(_g2);
                      } finally {
                        if (e_2) throw e_2.error;
                      }
                    }
                    try {
                      for (var _j2 = __values((_e2 = privacyConfig.maskSelector) !== null && _e2 !== void 0 ? _e2 : []), _k2 = _j2.next(); !_k2.done; _k2 = _j2.next()) {
                        var selector3 = _k2.value;
                        selectorMap2[selector3] = "mask";
                      }
                    } catch (e_3_1) {
                      e_3 = { error: e_3_1 };
                    } finally {
                      try {
                        if (_k2 && !_k2.done && (_b2 = _j2.return)) _b2.call(_j2);
                      } finally {
                        if (e_3) throw e_3.error;
                      }
                    }
                    try {
                      for (var _l2 = __values((_f2 = privacyConfig.unmaskSelector) !== null && _f2 !== void 0 ? _f2 : []), _m2 = _l2.next(); !_m2.done; _m2 = _l2.next()) {
                        var selector3 = _m2.value;
                        selectorMap2[selector3] = "unmask";
                      }
                    } catch (e_4_1) {
                      e_4 = { error: e_4_1 };
                    } finally {
                      try {
                        if (_m2 && !_m2.done && (_c2 = _l2.return)) _c2.call(_l2);
                      } finally {
                        if (e_4) throw e_4.error;
                      }
                    }
                    return selectorMap2;
                  };
                  selectorMap = __assign(__assign({}, privacyConfigSelectorMap(localPrivacyConfig)), privacyConfigSelectorMap(remotePrivacyConfig));
                  try {
                    for (_h = __values(Object.entries(selectorMap)), _j = _h.next(); !_j.done; _j = _h.next()) {
                      _k = __read(_j.value, 2), selector2 = _k[0], selectorType = _k[1];
                      if (selectorType === "mask") {
                        joinedPrivacyConfig.maskSelector.push(selector2);
                      } else if (selectorType === "block") {
                        joinedPrivacyConfig.blockSelector.push(selector2);
                      } else if (selectorType === "unmask") {
                        joinedPrivacyConfig.unmaskSelector.push(selector2);
                      }
                    }
                  } catch (e_1_1) {
                    e_1 = { error: e_1_1 };
                  } finally {
                    try {
                      if (_j && !_j.done && (_l = _h.return)) _l.call(_h);
                    } finally {
                      if (e_1) throw e_1.error;
                    }
                  }
                  config2.privacyConfig = removeInvalidSelectorsFromPrivacyConfig(joinedPrivacyConfig, this.localConfig.loggerProvider);
                }
                if (targetingConfig && Object.keys(targetingConfig).length > 0) {
                  config2.targetingConfig = targetingConfig;
                }
                this.localConfig.loggerProvider.debug(JSON.stringify({ name: "session replay joined config", config: getDebugConfig(config2) }, null, 2));
                return [2, {
                  localConfig: this.localConfig,
                  joinedConfig: config2,
                  remoteConfig: sessionReplayRemoteConfig
                }];
            }
          });
        });
      };
      SessionReplayJoinedConfigGenerator2.prototype.sanitizeMinSessionDurationMs = function(raw) {
        if (typeof raw !== "number" || !Number.isFinite(raw)) {
          this.localConfig.loggerProvider.warn("min_session_duration_ms remote value is not a finite number (got ".concat(String(raw), "); ignoring."));
          return void 0;
        }
        if (raw < 0) {
          this.localConfig.loggerProvider.warn("min_session_duration_ms remote value is negative (".concat(raw, "); ignoring."));
          return void 0;
        }
        if (raw > MAX_MIN_SESSION_DURATION_MS) {
          this.localConfig.loggerProvider.warn("min_session_duration_ms remote value ".concat(raw, " exceeds ").concat(MAX_MIN_SESSION_DURATION_MS, "ms ceiling; clamping."));
          return MAX_MIN_SESSION_DURATION_MS;
        }
        return raw;
      };
      return SessionReplayJoinedConfigGenerator2;
    })()
  );
  var MAX_MIN_SESSION_DURATION_MS = 6e4;
  var createSessionReplayJoinedConfigGenerator = function(apiKey2, options) {
    return __awaiter(void 0, void 0, void 0, function() {
      var localConfig, remoteConfigClient;
      return __generator(this, function(_a4) {
        localConfig = new SessionReplayLocalConfig(apiKey2, options);
        remoteConfigClient = new RemoteConfigClient(
          apiKey2,
          localConfig.loggerProvider,
          localConfig.serverZone,
          options.configServerUrl,
          // Custom transport for the config GET. SR's FetchConfigRequest is structurally compatible
          // with RemoteConfigFetchRequest, so the customer's handleFetchConfig drops straight in.
          options.handleFetchConfig
        );
        return [2, new SessionReplayJoinedConfigGenerator(remoteConfigClient, localConfig)];
      });
    });
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/event-compressor.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/merge-mutation-events.js
  init_tslib_es6();
  function isMergeableMutation(event) {
    if (event.type !== EventType.IncrementalSnapshot)
      return false;
    var data = event.data;
    return data.source === IncrementalSource.Mutation && !data.isAttachIframe;
  }
  function isStyleObject(value) {
    return typeof value === "object" && value !== null;
  }
  function mergeStyleDiffs(diffs) {
    var e_1, _a4, e_2, _b;
    var merged = {};
    try {
      for (var diffs_1 = __values(diffs), diffs_1_1 = diffs_1.next(); !diffs_1_1.done; diffs_1_1 = diffs_1.next()) {
        var diff = diffs_1_1.value;
        try {
          for (var _c = (e_2 = void 0, __values(Object.keys(diff))), _d = _c.next(); !_d.done; _d = _c.next()) {
            var prop = _d.value;
            merged[prop] = diff[prop];
          }
        } catch (e_2_1) {
          e_2 = { error: e_2_1 };
        } finally {
          try {
            if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
          } finally {
            if (e_2) throw e_2.error;
          }
        }
      }
    } catch (e_1_1) {
      e_1 = { error: e_1_1 };
    } finally {
      try {
        if (diffs_1_1 && !diffs_1_1.done && (_a4 = diffs_1.return)) _a4.call(diffs_1);
      } finally {
        if (e_1) throw e_1.error;
      }
    }
    return merged;
  }
  function coalesceStyleAttributes(attributes) {
    var e_3, _a4;
    var styleIndicesById = /* @__PURE__ */ new Map();
    attributes.forEach(function(attr2, i2) {
      if (attr2.attributes && "style" in attr2.attributes) {
        var list = styleIndicesById.get(attr2.id);
        if (list)
          list.push(i2);
        else
          styleIndicesById.set(attr2.id, [i2]);
      }
    });
    if (styleIndicesById.size === 0)
      return attributes;
    var styleValueAt = function(i2) {
      return attributes[i2].attributes.style;
    };
    var dropStyleAt = /* @__PURE__ */ new Set();
    var mergedStyleByIndex = /* @__PURE__ */ new Map();
    var _loop_1 = function(indices2) {
      if (indices2.length === 1)
        return "continue";
      var lastResetPos = -1;
      indices2.forEach(function(idx, pos) {
        if (!isStyleObject(styleValueAt(idx)))
          lastResetPos = pos;
      });
      var survivingObjectIndices = [];
      indices2.forEach(function(idx, pos) {
        if (pos === lastResetPos)
          return;
        dropStyleAt.add(idx);
        if (pos > lastResetPos)
          survivingObjectIndices.push(idx);
      });
      if (survivingObjectIndices.length > 0) {
        var lastObjectIdx = survivingObjectIndices[survivingObjectIndices.length - 1];
        var merged = mergeStyleDiffs(survivingObjectIndices.map(function(idx) {
          return styleValueAt(idx);
        }));
        dropStyleAt.delete(lastObjectIdx);
        mergedStyleByIndex.set(lastObjectIdx, merged);
      }
    };
    try {
      for (var _b = __values(styleIndicesById.values()), _c = _b.next(); !_c.done; _c = _b.next()) {
        var indices = _c.value;
        _loop_1(indices);
      }
    } catch (e_3_1) {
      e_3 = { error: e_3_1 };
    } finally {
      try {
        if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
      } finally {
        if (e_3) throw e_3.error;
      }
    }
    if (dropStyleAt.size === 0 && mergedStyleByIndex.size === 0)
      return attributes;
    var result3 = [];
    attributes.forEach(function(attr2, i2) {
      var mergedStyle = mergedStyleByIndex.get(i2);
      if (mergedStyle !== void 0) {
        result3.push(__assign(__assign({}, attr2), { attributes: __assign(__assign({}, attr2.attributes), { style: mergedStyle }) }));
        return;
      }
      if (!dropStyleAt.has(i2)) {
        result3.push(attr2);
        return;
      }
      var rest = __assign({}, attr2.attributes);
      delete rest.style;
      if (Object.keys(rest).length > 0) {
        result3.push(__assign(__assign({}, attr2), { attributes: rest }));
      }
    });
    return result3;
  }
  function mergeGroup(events) {
    var e_4, _a4, e_5, _b;
    var first = events[0];
    var firstAddEventIndex = /* @__PURE__ */ new Map();
    var lastAddEventIndex = /* @__PURE__ */ new Map();
    var firstRemoveEventIndex = /* @__PURE__ */ new Map();
    var lastRemoveEventIndex = /* @__PURE__ */ new Map();
    var lastParentById = /* @__PURE__ */ new Map();
    events.forEach(function(e2, i2) {
      var e_6, _a5, e_7, _b2;
      var data = e2.data;
      try {
        for (var _c2 = __values(data.adds), _d2 = _c2.next(); !_d2.done; _d2 = _c2.next()) {
          var add2 = _d2.value;
          if (!firstAddEventIndex.has(add2.node.id))
            firstAddEventIndex.set(add2.node.id, i2);
          lastAddEventIndex.set(add2.node.id, i2);
          lastParentById.set(add2.node.id, add2.parentId);
        }
      } catch (e_6_1) {
        e_6 = { error: e_6_1 };
      } finally {
        try {
          if (_d2 && !_d2.done && (_a5 = _c2.return)) _a5.call(_c2);
        } finally {
          if (e_6) throw e_6.error;
        }
      }
      try {
        for (var _e = __values(data.removes), _f = _e.next(); !_f.done; _f = _e.next()) {
          var remove2 = _f.value;
          if (!firstRemoveEventIndex.has(remove2.id))
            firstRemoveEventIndex.set(remove2.id, i2);
          lastRemoveEventIndex.set(remove2.id, i2);
        }
      } catch (e_7_1) {
        e_7 = { error: e_7_1 };
      } finally {
        try {
          if (_f && !_f.done && (_b2 = _e.return)) _b2.call(_e);
        } finally {
          if (e_7) throw e_7.error;
        }
      }
    });
    var transientIds = /* @__PURE__ */ new Set();
    var preExistingTransientIds = /* @__PURE__ */ new Set();
    try {
      for (var firstAddEventIndex_1 = __values(firstAddEventIndex), firstAddEventIndex_1_1 = firstAddEventIndex_1.next(); !firstAddEventIndex_1_1.done; firstAddEventIndex_1_1 = firstAddEventIndex_1.next()) {
        var _c = __read(firstAddEventIndex_1_1.value, 2), id2 = _c[0], firstAddIdx = _c[1];
        var firstRemoveIdx = firstRemoveEventIndex.get(id2);
        if (firstRemoveIdx === void 0)
          continue;
        var lastAddIdx = lastAddEventIndex.get(id2);
        var lastRemoveIdx = lastRemoveEventIndex.get(id2);
        if (lastAddIdx >= lastRemoveIdx)
          continue;
        if (firstAddIdx < firstRemoveIdx) {
          transientIds.add(id2);
        } else if (firstRemoveIdx < firstAddIdx) {
          preExistingTransientIds.add(id2);
        }
      }
    } catch (e_4_1) {
      e_4 = { error: e_4_1 };
    } finally {
      try {
        if (firstAddEventIndex_1_1 && !firstAddEventIndex_1_1.done && (_a4 = firstAddEventIndex_1.return)) _a4.call(firstAddEventIndex_1);
      } finally {
        if (e_4) throw e_4.error;
      }
    }
    var cascadeDropAddsOnlyIds = /* @__PURE__ */ new Set();
    if (transientIds.size > 0 || preExistingTransientIds.size > 0) {
      var changed = true;
      while (changed) {
        changed = false;
        try {
          for (var lastParentById_1 = (e_5 = void 0, __values(lastParentById)), lastParentById_1_1 = lastParentById_1.next(); !lastParentById_1_1.done; lastParentById_1_1 = lastParentById_1.next()) {
            var _d = __read(lastParentById_1_1.value, 2), nodeId = _d[0], parentId = _d[1];
            if (!transientIds.has(nodeId) && !preExistingTransientIds.has(nodeId) && !cascadeDropAddsOnlyIds.has(nodeId) && (transientIds.has(parentId) || preExistingTransientIds.has(parentId) || cascadeDropAddsOnlyIds.has(parentId))) {
              var nodeFirstRemoveIdx = firstRemoveEventIndex.get(nodeId);
              var nodeFirstAddIdx = firstAddEventIndex.get(nodeId);
              if (nodeFirstRemoveIdx !== void 0 && nodeFirstAddIdx !== void 0 && nodeFirstRemoveIdx < nodeFirstAddIdx) {
                preExistingTransientIds.add(nodeId);
              } else if (nodeFirstRemoveIdx !== void 0 && nodeFirstAddIdx !== void 0 && nodeFirstRemoveIdx === nodeFirstAddIdx) {
                cascadeDropAddsOnlyIds.add(nodeId);
              } else {
                transientIds.add(nodeId);
              }
              changed = true;
            }
          }
        } catch (e_5_1) {
          e_5 = { error: e_5_1 };
        } finally {
          try {
            if (lastParentById_1_1 && !lastParentById_1_1.done && (_b = lastParentById_1.return)) _b.call(lastParentById_1);
          } finally {
            if (e_5) throw e_5.error;
          }
        }
      }
    }
    var needsFilter = transientIds.size > 0 || preExistingTransientIds.size > 0 || cascadeDropAddsOnlyIds.size > 0;
    var filteredRemoves = [];
    events.forEach(function(e2, eventIdx) {
      var e_8, _a5;
      try {
        for (var _b2 = __values(e2.data.removes), _c2 = _b2.next(); !_c2.done; _c2 = _b2.next()) {
          var r2 = _c2.value;
          if (transientIds.has(r2.id))
            continue;
          if (preExistingTransientIds.has(r2.id) && eventIdx >= firstAddEventIndex.get(r2.id))
            continue;
          if (cascadeDropAddsOnlyIds.has(r2.id) && (transientIds.has(r2.parentId) || preExistingTransientIds.has(r2.parentId) || cascadeDropAddsOnlyIds.has(r2.parentId)))
            continue;
          filteredRemoves.push(r2);
        }
      } catch (e_8_1) {
        e_8 = { error: e_8_1 };
      } finally {
        try {
          if (_c2 && !_c2.done && (_a5 = _b2.return)) _a5.call(_b2);
        } finally {
          if (e_8) throw e_8.error;
        }
      }
    });
    var allAdds = events.flatMap(function(e2) {
      return e2.data.adds;
    });
    var allTexts = events.flatMap(function(e2) {
      return e2.data.texts;
    });
    var allAttributes = events.flatMap(function(e2) {
      return e2.data.attributes;
    });
    var mergedAttributes = needsFilter ? allAttributes.filter(function(a2) {
      return !transientIds.has(a2.id) && !preExistingTransientIds.has(a2.id) && !cascadeDropAddsOnlyIds.has(a2.id);
    }) : allAttributes;
    var merged = {
      source: IncrementalSource.Mutation,
      removes: filteredRemoves,
      adds: needsFilter ? allAdds.filter(function(a2) {
        return !transientIds.has(a2.node.id) && !preExistingTransientIds.has(a2.node.id) && !cascadeDropAddsOnlyIds.has(a2.node.id);
      }) : allAdds,
      texts: needsFilter ? allTexts.filter(function(t2) {
        return !transientIds.has(t2.id) && !preExistingTransientIds.has(t2.id) && !cascadeDropAddsOnlyIds.has(t2.id);
      }) : allTexts,
      attributes: coalesceStyleAttributes(mergedAttributes)
    };
    return __assign(__assign({}, first), { data: merged });
  }
  function mergeMutationEvents(events) {
    if (events.length <= 1)
      return events;
    var result3 = [];
    var i2 = 0;
    while (i2 < events.length) {
      if (!isMergeableMutation(events[i2])) {
        result3.push(events[i2]);
        i2++;
        continue;
      }
      var j = i2 + 1;
      while (j < events.length && isMergeableMutation(events[j])) {
        j++;
      }
      result3.push(j > i2 + 1 ? mergeGroup(events.slice(i2, j)) : events[i2]);
      i2 = j;
    }
    return result3;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/event-compressor.js
  var DEFAULT_TIMEOUT2 = 2e3;
  var EventCompressor = (
    /** @class */
    (function() {
      function EventCompressor2(eventsManager, config2, deviceId, workerScript, onFullSnapshotProcessed) {
        var _this = this;
        var _a4;
        this.taskQueue = [];
        this.pendingQueue = [];
        this.isProcessing = false;
        this.compressEvent = function(event) {
          var _a5 = event, type = _a5.type, timestamp = _a5.timestamp, delay = _a5.delay, data = _a5.data;
          return delay != null ? JSON.stringify({ type, timestamp, delay, data }) : JSON.stringify({ type, timestamp, data });
        };
        this.addCompressedEventToManager = function(compressedEvent, sessionId) {
          var _a5;
          var eventSizeBytes = new Blob([compressedEvent]).size;
          if (eventSizeBytes > ((_a5 = _this.config.maxSingleEventSizeBytes) !== null && _a5 !== void 0 ? _a5 : MAX_SINGLE_EVENT_SIZE)) {
            _this.config.loggerProvider.warn("Session replay event dropped: serialized size ".concat(Math.round(eventSizeBytes / 1024), " KB exceeds maximum allowed event size. If this recurs, please open a GitHub issue at https://github.com/amplitude/Amplitude-TypeScript/issues or contact Amplitude support."));
            return;
          }
          if (_this.eventsManager && _this.deviceId) {
            _this.eventsManager.addEvent({
              event: { type: "replay", data: compressedEvent },
              sessionId,
              deviceId: _this.deviceId
            });
          }
        };
        this.addCompressedEvent = function(event, sessionId) {
          if (_this.worker) {
            try {
              _this.worker.postMessage({ event, sessionId });
            } catch (err) {
              if (err.name === "DataCloneError") {
                _this.worker.postMessage(JSON.stringify({ event, sessionId }));
              } else {
                _this.config.loggerProvider.warn("Unexpected error while posting message to worker:", err);
              }
            }
          } else {
            var compressedEvent = _this.compressEvent(event);
            _this.addCompressedEventToManager(compressedEvent, sessionId);
          }
        };
        this.flushQueue = function() {
          var _a5;
          if (_this.pendingQueue.length > 0) {
            (_a5 = _this.taskQueue).push.apply(_a5, __spreadArray([], __read(_this.mergeMutationTasks(_this.pendingQueue.splice(0))), false));
          }
          while (_this.taskQueue.length > 0) {
            var task = _this.taskQueue.shift();
            if (task) {
              var event_1 = task.event, sessionId = task.sessionId;
              var compressed = _this.compressEvent(event_1);
              _this.addCompressedEventToManager(compressed, sessionId);
            }
          }
          _this.isProcessing = false;
        };
        this.terminate = function() {
          var _a5;
          (_a5 = _this.worker) === null || _a5 === void 0 ? void 0 : _a5.terminate();
        };
        var globalScope3 = getGlobalScope();
        this.canUseIdleCallback = globalScope3 && "requestIdleCallback" in globalScope3;
        this.eventsManager = eventsManager;
        this.config = config2;
        this.deviceId = deviceId;
        this.timeout = ((_a4 = config2.performanceConfig) === null || _a4 === void 0 ? void 0 : _a4.timeout) || DEFAULT_TIMEOUT2;
        this.onFullSnapshotProcessed = onFullSnapshotProcessed;
        if (workerScript) {
          config2.loggerProvider.log("Enabling web worker for compression");
          try {
            var blob2 = new Blob([workerScript], { type: "application/javascript" });
            var blobUrl = URL.createObjectURL(blob2);
            var worker_1 = new Worker(blobUrl);
            worker_1.onerror = function(e2) {
              e2.preventDefault();
              config2.loggerProvider.error("Worker failed, falling back to non-worker compression: ".concat(e2.message, " (").concat(e2.filename, ":").concat(e2.lineno, ")"));
              worker_1.terminate();
              _this.worker = void 0;
            };
            worker_1.onmessage = function(e2) {
              var _a5 = e2.data, compressedEvent = _a5.compressedEvent, sessionId = _a5.sessionId;
              _this.addCompressedEventToManager(compressedEvent, sessionId);
            };
            this.worker = worker_1;
          } catch (error) {
            config2.loggerProvider.error("Failed to create worker, falling back to non-worker compression:", error);
          }
        }
      }
      EventCompressor2.prototype.scheduleIdleProcessing = function() {
        var _this = this;
        if (!this.isProcessing) {
          this.isProcessing = true;
          requestIdleCallback(function(idleDeadline) {
            _this.processQueue(idleDeadline);
          }, { timeout: this.timeout });
        }
      };
      EventCompressor2.prototype.enqueueEvent = function(event, sessionId) {
        var e_1, _a4;
        var _b, _c;
        if (event.type === EventType.FullSnapshot) {
          this.config.loggerProvider.debug("Processing full snapshot immediately.");
          if (this.taskQueue.length > 0 || this.pendingQueue.length > 0) {
            var allTasks = __spreadArray(__spreadArray([], __read(this.taskQueue.splice(0)), false), __read(this.mergeMutationTasks(this.pendingQueue.splice(0))), false);
            try {
              for (var allTasks_1 = __values(allTasks), allTasks_1_1 = allTasks_1.next(); !allTasks_1_1.done; allTasks_1_1 = allTasks_1.next()) {
                var task = allTasks_1_1.value;
                var compressed = this.compressEvent(task.event);
                this.addCompressedEventToManager(compressed, task.sessionId);
              }
            } catch (e_1_1) {
              e_1 = { error: e_1_1 };
            } finally {
              try {
                if (allTasks_1_1 && !allTasks_1_1.done && (_a4 = allTasks_1.return)) _a4.call(allTasks_1);
              } finally {
                if (e_1) throw e_1.error;
              }
            }
            this.isProcessing = false;
          }
          var compressedEvent = this.compressEvent(event);
          this.addCompressedEventToManager(compressedEvent, sessionId);
          (_b = this.onFullSnapshotProcessed) === null || _b === void 0 ? void 0 : _b.call(this);
          return;
        }
        if (this.canUseIdleCallback && ((_c = this.config.performanceConfig) === null || _c === void 0 ? void 0 : _c.enabled)) {
          this.config.loggerProvider.debug("Enqueuing event for processing during idle time.");
          this.pendingQueue.push({ event, sessionId });
          this.scheduleIdleProcessing();
        } else {
          this.config.loggerProvider.debug("Processing event without idle callback.");
          this.addCompressedEvent(event, sessionId);
        }
      };
      EventCompressor2.prototype.processQueue = function(idleDeadline) {
        var _a4;
        var _this = this;
        if (this.pendingQueue.length > 0) {
          (_a4 = this.taskQueue).push.apply(_a4, __spreadArray([], __read(this.mergeMutationTasks(this.pendingQueue.splice(0))), false));
        }
        while (this.taskQueue.length > 0 && (idleDeadline.timeRemaining() > 0 || idleDeadline.didTimeout)) {
          var task = this.taskQueue.shift();
          if (task) {
            var event_2 = task.event, sessionId = task.sessionId;
            this.addCompressedEvent(event_2, sessionId);
          }
        }
        if (this.taskQueue.length > 0 || this.pendingQueue.length > 0) {
          requestIdleCallback(function(idleDeadline2) {
            _this.processQueue(idleDeadline2);
          }, { timeout: this.timeout });
        } else {
          this.isProcessing = false;
        }
      };
      EventCompressor2.prototype.mergeMutationTasks = function(tasks) {
        var e_2, _a4;
        var _b;
        if (((_b = this.config.performanceConfig) === null || _b === void 0 ? void 0 : _b.mergeMutations) === false)
          return tasks;
        if (tasks.length <= 1)
          return tasks;
        var result3 = [];
        var i2 = 0;
        while (i2 < tasks.length) {
          var sessionId = tasks[i2].sessionId;
          var j = i2 + 1;
          while (j < tasks.length && tasks[j].sessionId === sessionId) {
            j++;
          }
          var merged = mergeMutationEvents(tasks.slice(i2, j).map(function(t2) {
            return t2.event;
          }));
          try {
            for (var merged_1 = (e_2 = void 0, __values(merged)), merged_1_1 = merged_1.next(); !merged_1_1.done; merged_1_1 = merged_1.next()) {
              var event_3 = merged_1_1.value;
              result3.push({ event: event_3, sessionId });
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (merged_1_1 && !merged_1_1.done && (_a4 = merged_1.return)) _a4.call(merged_1);
            } finally {
              if (e_2) throw e_2.error;
            }
          }
          i2 = j;
        }
        return result3;
      };
      return EventCompressor2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/events-manager.js
  init_tslib_es6();

  // node_modules/@amplitude/session-replay-browser/lib/esm/track-destination.js
  init_tslib_es6();
  init_esm();

  // node_modules/@amplitude/session-replay-browser/lib/esm/messages.js
  var UNEXPECTED_ERROR_MESSAGE2 = "Unexpected error occurred";
  var UNEXPECTED_NETWORK_ERROR_MESSAGE = "Network error occurred, event batch rejected";
  var MAX_RETRIES_EXCEEDED_MESSAGE2 = "Session replay event batch rejected due to exceeded retry count";
  var STORAGE_FAILURE = "Failed to store session replay events in IndexedDB";
  var MISSING_DEVICE_ID_MESSAGE = "Session replay event batch not sent due to missing device ID";
  var MISSING_API_KEY_MESSAGE2 = "Session replay event batch not sent due to missing api key";
  var SESSION_KILLED_MESSAGE = "Session replay event batch dropped: server signalled capture disabled or session out of valid range for this session";

  // node_modules/@amplitude/session-replay-browser/lib/esm/version.js
  var VERSION3 = "1.50.1";

  // node_modules/@amplitude/session-replay-browser/lib/esm/utils/gzip.js
  init_tslib_es6();
  function gzipJson(jsonStr, scope) {
    return __awaiter(this, void 0, void 0, function() {
      var CS, stream, writer, reader_1, chunks_2, readPromise, totalLength, result3, offset, chunks_1, chunks_1_1, chunk2, _a4;
      var e_1, _b;
      var _this = this;
      return __generator(this, function(_c) {
        switch (_c.label) {
          case 0:
            _c.trys.push([0, 4, , 5]);
            CS = scope.CompressionStream;
            stream = new CS("gzip");
            writer = stream.writable.getWriter();
            reader_1 = stream.readable.getReader();
            chunks_2 = [];
            readPromise = (function() {
              return __awaiter(_this, void 0, void 0, function() {
                var _a5, done, value;
                return __generator(this, function(_b2) {
                  switch (_b2.label) {
                    case 0:
                      return [4, reader_1.read()];
                    case 1:
                      _a5 = _b2.sent(), done = _a5.done, value = _a5.value;
                      if (done)
                        return [3, 3];
                      chunks_2.push(value);
                      _b2.label = 2;
                    case 2:
                      return [3, 0];
                    case 3:
                      return [
                        2
                        /*return*/
                      ];
                  }
                });
              });
            })();
            return [4, writer.write(new TextEncoder().encode(jsonStr))];
          case 1:
            _c.sent();
            return [4, writer.close()];
          case 2:
            _c.sent();
            return [4, readPromise];
          case 3:
            _c.sent();
            totalLength = chunks_2.reduce(function(sum, c2) {
              return sum + c2.length;
            }, 0);
            result3 = new Uint8Array(totalLength);
            offset = 0;
            try {
              for (chunks_1 = __values(chunks_2), chunks_1_1 = chunks_1.next(); !chunks_1_1.done; chunks_1_1 = chunks_1.next()) {
                chunk2 = chunks_1_1.value;
                result3.set(chunk2, offset);
                offset += chunk2.length;
              }
            } catch (e_1_1) {
              e_1 = { error: e_1_1 };
            } finally {
              try {
                if (chunks_1_1 && !chunks_1_1.done && (_b = chunks_1.return)) _b.call(chunks_1);
              } finally {
                if (e_1) throw e_1.error;
              }
            }
            return [2, result3];
          case 4:
            _a4 = _c.sent();
            return [2, null];
          case 5:
            return [
              2
              /*return*/
            ];
        }
      });
    });
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/track-destination.js
  var MAX_KILLED_SESSIONS = 256;
  var MAX_TIMED_OUT_WORKER_REQUESTS = 256;
  function abortable(promise, signal) {
    return new Promise(function(resolve, reject) {
      var onAbort = function() {
        var err = new Error("Custom session replay transport aborted by send timeout");
        err.name = "AbortError";
        reject(err);
      };
      signal.addEventListener("abort", onAbort, { once: true });
      promise.then(function(value) {
        signal.removeEventListener("abort", onAbort);
        resolve(value);
      }, function(err) {
        signal.removeEventListener("abort", onAbort);
        reject(err);
      });
    });
  }
  var SessionReplayTrackDestination = (
    /** @class */
    (function() {
      function SessionReplayTrackDestination2(_a4) {
        var trackServerUrl = _a4.trackServerUrl, loggerProvider = _a4.loggerProvider, payloadBatcher = _a4.payloadBatcher, workerScript = _a4.workerScript, enableTransportCompression = _a4.enableTransportCompression, sendTimeoutMs = _a4.sendTimeoutMs, handleSendEvents = _a4.handleSendEvents;
        var _this = this;
        this.storageKey = "";
        this.retryTimeout = 1e3;
        this.scheduled = null;
        this.queue = [];
        this.sendIdCounter = 0;
        this.pendingWorkerRequests = /* @__PURE__ */ new Map();
        this.timedOutWorkerRequests = /* @__PURE__ */ new Map();
        this.flushPauseUntilMs = 0;
        this.mergeOnNextFlush = false;
        this.coalesceNextFlush = false;
        this.mergeLogFiredThisPause = false;
        this.killedSessions = /* @__PURE__ */ new Set();
        this.loggerProvider = loggerProvider;
        this.payloadBatcher = payloadBatcher ? payloadBatcher : function(payload) {
          return payload;
        };
        this.trackServerUrl = trackServerUrl;
        this.enableTransportCompression = enableTransportCompression !== null && enableTransportCompression !== void 0 ? enableTransportCompression : true;
        this.sendTimeoutMs = sendTimeoutMs !== null && sendTimeoutMs !== void 0 ? sendTimeoutMs : SEND_TIMEOUT_MS;
        this.handleSendEvents = handleSendEvents;
        if (workerScript) {
          try {
            var blob2 = new Blob([workerScript], { type: "application/javascript" });
            var blobUrl = URL.createObjectURL(blob2);
            var worker_1 = new Worker(blobUrl);
            worker_1.onerror = function(e2) {
              var e_1, _a5;
              e2.preventDefault();
              loggerProvider.error("Track destination worker failed, falling back to main-thread sending: ".concat(e2.message, " (").concat(e2.filename, ":").concat(e2.lineno, ")"));
              worker_1.terminate();
              _this.worker = void 0;
              try {
                for (var _b = __values(_this.pendingWorkerRequests), _c = _b.next(); !_c.done; _c = _b.next()) {
                  var _d = __read(_c.value, 2), pending_1 = _d[1];
                  if (pending_1.timeout)
                    clearTimeout(pending_1.timeout);
                  loggerProvider.warn("Session replay event send failed due to worker crash: ".concat(e2.message));
                  pending_1.resolve();
                }
              } catch (e_1_1) {
                e_1 = { error: e_1_1 };
              } finally {
                try {
                  if (_c && !_c.done && (_a5 = _b.return)) _a5.call(_b);
                } finally {
                  if (e_1) throw e_1.error;
                }
              }
              _this.pendingWorkerRequests.clear();
              _this.timedOutWorkerRequests.clear();
            };
            worker_1.onmessage = function(e2) {
              var msg = e2.data;
              if (msg.type === "log") {
                loggerProvider.log(msg.message);
              } else if (msg.type === "warn") {
                loggerProvider.warn(msg.message);
              } else if (msg.type === "payload_too_large") {
                var pending_2 = _this.pendingWorkerRequests.get(msg.id);
                if (pending_2) {
                  if (pending_2.timeout)
                    clearTimeout(pending_2.timeout);
                  _this.handlePayloadTooLargeResponse(pending_2.context, msg.isWaf);
                  pending_2.resolve();
                  _this.pendingWorkerRequests.delete(msg.id);
                } else {
                  var timedOut = _this.timedOutWorkerRequests.get(msg.id);
                  if (timedOut) {
                    _this.timedOutWorkerRequests.delete(msg.id);
                    _this.handlePayloadTooLargeResponse(timedOut, msg.isWaf);
                  }
                }
              } else if (msg.type === "complete") {
                var pending_3 = _this.pendingWorkerRequests.get(msg.id);
                if (pending_3) {
                  if (pending_3.timeout)
                    clearTimeout(pending_3.timeout);
                  if (msg.skipCode !== void 0) {
                    _this.applyServerDirective(pending_3.context.sessionId, msg.skipCode);
                  }
                  _this.completeRequest({ context: pending_3.context });
                  pending_3.resolve();
                  _this.pendingWorkerRequests.delete(msg.id);
                } else {
                  var timedOut = _this.timedOutWorkerRequests.get(msg.id);
                  if (timedOut) {
                    _this.timedOutWorkerRequests.delete(msg.id);
                    if (msg.skipCode !== void 0) {
                      _this.applyServerDirective(timedOut.sessionId, msg.skipCode);
                    }
                    _this.completeRequest({ context: timedOut });
                  }
                }
              } else if (msg.type === "fetch-request") {
                void _this.handleDelegatedFetch(worker_1, msg).catch(function(e3) {
                  loggerProvider.warn("Failed to handle delegated session replay fetch:", e3);
                });
              }
            };
            this.worker = worker_1;
          } catch (error) {
            loggerProvider.error("Failed to create track destination worker, falling back to main-thread sending:", error);
          }
        }
      }
      SessionReplayTrackDestination2.prototype.sendEventsList = function(destinationData) {
        this.addToQueue(__assign(__assign({}, destinationData), { attempts: 0, timeout: 0 }));
      };
      SessionReplayTrackDestination2.prototype.markCoalesceNextFlush = function() {
        this.coalesceNextFlush = true;
        this.schedule(0);
      };
      SessionReplayTrackDestination2.prototype.sendBeacon = function(_a4) {
        var _this = this;
        var _b, _c;
        var events = _a4.events, sessionId = _a4.sessionId, deviceId = _a4.deviceId, apiKey2 = _a4.apiKey, serverZone = _a4.serverZone;
        var MAX_BEACON_BYTES = 64 * 1024;
        var byteLength = function(s2) {
          return new Blob([s2]).size;
        };
        var trimmedEvents = events;
        var payload = JSON.stringify({ version: 2, events: trimmedEvents });
        if (byteLength(payload) > MAX_BEACON_BYTES) {
          var lo = 0;
          var hi = trimmedEvents.length;
          while (lo < hi) {
            var mid = Math.floor((lo + hi + 1) / 2);
            if (byteLength(JSON.stringify({ version: 2, events: trimmedEvents.slice(0, mid) })) <= MAX_BEACON_BYTES) {
              lo = mid;
            } else {
              hi = mid - 1;
            }
          }
          trimmedEvents = trimmedEvents.slice(0, lo);
          payload = JSON.stringify({ version: 2, events: trimmedEvents });
          this.loggerProvider.warn("sendBeacon payload exceeded 64 KB limit, trimmed from ".concat(events.length, " to ").concat(trimmedEvents.length, " events"));
        }
        if (trimmedEvents.length === 0) {
          return;
        }
        if (this.handleSendEvents) {
          var exitParams = new URLSearchParams({
            device_id: deviceId,
            session_id: String(sessionId),
            type: "replay"
          });
          var exitUrl = "".concat(getServerUrl2(serverZone, this.trackServerUrl), "?").concat(exitParams.toString());
          var headers = {
            "Content-Type": "application/json",
            Accept: "*/*",
            Authorization: "Bearer ".concat(apiKey2)
          };
          this.loggerProvider.debug("Routing session replay exit batch (".concat(trimmedEvents.length, " events) through custom transport for session id ").concat(sessionId, "."));
          try {
            void this.handleSendEvents({ url: exitUrl, method: "POST", headers, body: payload, keepalive: true }).catch(function(e2) {
              _this.loggerProvider.warn("Custom transport failed to send session replay exit batch:", e2);
            });
          } catch (e2) {
            this.loggerProvider.warn("Custom transport threw while sending session replay exit batch:", e2);
          }
          return;
        }
        var urlParams = new URLSearchParams({
          device_id: deviceId,
          session_id: String(sessionId),
          type: "replay",
          api_key: apiKey2
        });
        var serverUrl = "".concat(getServerUrl2(serverZone, this.trackServerUrl), "?").concat(urlParams.toString());
        var globalScope3 = getGlobalScope();
        try {
          var payloadBlob = new Blob([payload], { type: "application/json" });
          var sent = (_c = (_b = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.navigator) === null || _b === void 0 ? void 0 : _b.sendBeacon) === null || _c === void 0 ? void 0 : _c.call(_b, serverUrl, payloadBlob);
          if (sent === false) {
            this.loggerProvider.warn("sendBeacon failed to queue session replay payload");
          }
        } catch (_d) {
        }
      };
      SessionReplayTrackDestination2.prototype.addToQueue = function() {
        var _this = this;
        var list = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          list[_i] = arguments[_i];
        }
        var tryable = list.filter(function(context) {
          if (_this.killedSessions.has(context.sessionId)) {
            _this.completeRequest({
              context,
              err: SESSION_KILLED_MESSAGE
            });
            return false;
          }
          if (context.attempts < (context.flushMaxRetries || 0)) {
            context.attempts += 1;
            return true;
          }
          _this.completeRequest({
            context,
            err: MAX_RETRIES_EXCEEDED_MESSAGE2
          });
          return false;
        });
        tryable.forEach(function(context) {
          _this.queue = _this.queue.concat(context);
          _this.schedule(0);
        });
      };
      SessionReplayTrackDestination2.prototype.schedule = function(timeout) {
        var _this = this;
        if (this.scheduled)
          return;
        var pauseRemaining = this.flushPauseUntilMs - Date.now();
        var isPaused = pauseRemaining > 0;
        var effectiveTimeout = pauseRemaining > timeout ? pauseRemaining : timeout;
        if (isPaused) {
          this.mergeOnNextFlush = true;
        }
        this.scheduled = setTimeout(function() {
          void _this.flush(true).then(function() {
            if (_this.queue.length > 0) {
              _this.schedule(timeout);
            }
          });
        }, effectiveTimeout);
      };
      SessionReplayTrackDestination2.prototype.flush = function(useRetry) {
        if (useRetry === void 0) {
          useRetry = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          var list, list_13, list_1_1, context, e_2_1;
          var e_2, _a4;
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                list = this.queue;
                this.queue = [];
                if (this.scheduled) {
                  clearTimeout(this.scheduled);
                  this.scheduled = null;
                }
                if (this.mergeOnNextFlush) {
                  this.mergeOnNextFlush = false;
                  this.coalesceNextFlush = false;
                  list = this.mergeQueueAfterThrottle(list);
                } else if (this.coalesceNextFlush) {
                  this.coalesceNextFlush = false;
                  list = this.mergeDrainBacklog(list);
                }
                _b.label = 1;
              case 1:
                _b.trys.push([1, 6, 7, 8]);
                list_13 = __values(list), list_1_1 = list_13.next();
                _b.label = 2;
              case 2:
                if (!!list_1_1.done) return [3, 5];
                context = list_1_1.value;
                return [4, this.send(context, useRetry)];
              case 3:
                _b.sent();
                _b.label = 4;
              case 4:
                list_1_1 = list_13.next();
                return [3, 2];
              case 5:
                return [3, 8];
              case 6:
                e_2_1 = _b.sent();
                e_2 = { error: e_2_1 };
                return [3, 8];
              case 7:
                try {
                  if (list_1_1 && !list_1_1.done && (_a4 = list_13.return)) _a4.call(list_13);
                } finally {
                  if (e_2) throw e_2.error;
                }
                return [
                  7
                  /*endfinally*/
                ];
              case 8:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayTrackDestination2.prototype.mergeQueueAfterThrottle = function(list) {
        var merged = this.coalesceByIdentity(list);
        if (merged.length < list.length && !this.mergeLogFiredThisPause) {
          this.mergeLogFiredThisPause = true;
          this.loggerProvider.log("Session replay throttle pause ended; merged ".concat(list.length, " queued batches into ").concat(merged.length, " request(s)"));
        }
        return merged;
      };
      SessionReplayTrackDestination2.prototype.mergeDrainBacklog = function(list) {
        var merged = this.coalesceByIdentity(list);
        if (merged.length < list.length) {
          this.loggerProvider.log("Session replay coalesced ".concat(list.length, " persisted page-load backlog batches into ").concat(merged.length, " request(s)"));
        }
        return merged;
      };
      SessionReplayTrackDestination2.prototype.coalesceByIdentity = function(list) {
        var e_3, _a4, e_4, _b;
        var _this = this;
        var _c, _d, _e, _f, _g, _h, _j;
        if (list.length <= 1)
          return list;
        var groups = /* @__PURE__ */ new Map();
        try {
          for (var list_2 = __values(list), list_2_1 = list_2.next(); !list_2_1.done; list_2_1 = list_2.next()) {
            var ctx = list_2_1.value;
            var key = [
              ctx.sessionId,
              (_c = ctx.deviceId) !== null && _c !== void 0 ? _c : "",
              (_d = ctx.apiKey) !== null && _d !== void 0 ? _d : "",
              ctx.type,
              (_e = ctx.serverZone) !== null && _e !== void 0 ? _e : "",
              ctx.sampleRate,
              (_g = (_f = ctx.version) === null || _f === void 0 ? void 0 : _f.type) !== null && _g !== void 0 ? _g : "",
              (_j = (_h = ctx.version) === null || _h === void 0 ? void 0 : _h.version) !== null && _j !== void 0 ? _j : ""
            ].join("|");
            var arr = groups.get(key);
            if (arr)
              arr.push(ctx);
            else
              groups.set(key, [ctx]);
          }
        } catch (e_3_1) {
          e_3 = { error: e_3_1 };
        } finally {
          try {
            if (list_2_1 && !list_2_1.done && (_a4 = list_2.return)) _a4.call(list_2);
          } finally {
            if (e_3) throw e_3.error;
          }
        }
        var merged = [];
        var _loop_1 = function(group2) {
          var e_5, _m;
          if (group2.length === 1) {
            merged.push(group2[0]);
            return "continue";
          }
          var current = null;
          var currentBytes = 0;
          var flushCurrent = function() {
            if (current)
              merged.push(current);
            current = null;
            currentBytes = 0;
          };
          var _loop_2 = function(ctx3) {
            var ctxBytes = ctx3.events.reduce(function(sum, e2) {
              return sum + new Blob([e2]).size;
            }, 0);
            if (current === null) {
              current = __assign(__assign({}, ctx3), { events: __spreadArray([], __read(ctx3.events), false), attempts: 0 });
              currentBytes = ctxBytes;
              return "continue";
            }
            if (currentBytes + ctxBytes > MERGE_AFTER_THROTTLE_SOFT_CAP) {
              flushCurrent();
              current = __assign(__assign({}, ctx3), { events: __spreadArray([], __read(ctx3.events), false), attempts: 0 });
              currentBytes = ctxBytes;
              return "continue";
            }
            var prevOnComplete = current.onComplete;
            var ctxOnComplete = ctx3.onComplete;
            current.events = current.events.concat(ctx3.events);
            currentBytes += ctxBytes;
            current.onComplete = function() {
              return __awaiter(_this, void 0, void 0, function() {
                return __generator(this, function(_a5) {
                  switch (_a5.label) {
                    case 0:
                      return [4, Promise.allSettled([prevOnComplete(), ctxOnComplete()])];
                    case 1:
                      _a5.sent();
                      return [
                        2
                        /*return*/
                      ];
                  }
                });
              });
            };
          };
          try {
            for (var group_1 = (e_5 = void 0, __values(group2)), group_1_1 = group_1.next(); !group_1_1.done; group_1_1 = group_1.next()) {
              var ctx2 = group_1_1.value;
              _loop_2(ctx2);
            }
          } catch (e_5_1) {
            e_5 = { error: e_5_1 };
          } finally {
            try {
              if (group_1_1 && !group_1_1.done && (_m = group_1.return)) _m.call(group_1);
            } finally {
              if (e_5) throw e_5.error;
            }
          }
          flushCurrent();
        };
        try {
          for (var _k = __values(groups.values()), _l = _k.next(); !_l.done; _l = _k.next()) {
            var group = _l.value;
            _loop_1(group);
          }
        } catch (e_4_1) {
          e_4 = { error: e_4_1 };
        } finally {
          try {
            if (_l && !_l.done && (_b = _k.return)) _b.call(_k);
          } finally {
            if (e_4) throw e_4.error;
          }
        }
        return merged;
      };
      SessionReplayTrackDestination2.prototype.send = function(context, useRetry) {
        if (useRetry === void 0) {
          useRetry = true;
        }
        return __awaiter(this, void 0, void 0, function() {
          var apiKey2, deviceId, payload, worker;
          return __generator(this, function(_a4) {
            if (this.killedSessions.has(context.sessionId)) {
              return [2, this.completeRequest({ context, err: SESSION_KILLED_MESSAGE })];
            }
            apiKey2 = context.apiKey;
            if (!apiKey2) {
              return [2, this.completeRequest({ context, err: MISSING_API_KEY_MESSAGE2 })];
            }
            deviceId = context.deviceId;
            if (!deviceId) {
              return [2, this.completeRequest({ context, err: MISSING_DEVICE_ID_MESSAGE })];
            }
            payload = this.payloadBatcher({
              version: 1,
              events: context.events
            });
            if (payload.events.length === 0) {
              this.completeRequest({ context });
              return [
                2
                /*return*/
              ];
            }
            worker = this.worker;
            if (worker) {
              return [2, this.sendViaWorker(worker, context, payload, useRetry)];
            }
            return [2, this.sendOnMainThread(apiKey2, deviceId, context, payload, useRetry)];
          });
        });
      };
      SessionReplayTrackDestination2.prototype.sendViaWorker = function(worker, context, payload, useRetry) {
        return __awaiter(this, void 0, void 0, function() {
          var id2;
          var _this = this;
          return __generator(this, function(_a4) {
            id2 = "".concat(++this.sendIdCounter);
            return [2, new Promise(function(resolve) {
              var _a5;
              var timeout = _this.sendTimeoutMs > 0 ? setTimeout(function() {
                var pending = _this.pendingWorkerRequests.get(id2);
                if (!pending)
                  return;
                _this.pendingWorkerRequests.delete(id2);
                _this.rememberTimedOutRequest(id2, pending.context);
                _this.loggerProvider.warn("Session replay worker send timed out after ".concat(_this.sendTimeoutMs, "ms; leaving events for retry"));
                pending.resolve();
              }, _this.sendTimeoutMs) : void 0;
              _this.pendingWorkerRequests.set(id2, { context, resolve, timeout });
              worker.postMessage({
                type: "send",
                id: id2,
                payload,
                useRetry,
                // Tell the worker to delegate each network attempt back to the main thread, where the
                // custom transport callback lives (functions can't cross postMessage). Absent/false
                // keeps the worker's own internal fetch — the unchanged path for existing integrations.
                useCustomTransport: !!_this.handleSendEvents,
                context: {
                  apiKey: context.apiKey,
                  deviceId: context.deviceId,
                  sessionId: context.sessionId,
                  events: context.events,
                  eventType: context.type,
                  flushMaxRetries: (_a5 = context.flushMaxRetries) !== null && _a5 !== void 0 ? _a5 : 0,
                  sampleRate: context.sampleRate,
                  serverZone: context.serverZone,
                  trackServerUrl: _this.trackServerUrl,
                  version: context.version,
                  currentUrl: getCurrentUrl(),
                  sdkVersion: VERSION3,
                  enableTransportCompression: _this.enableTransportCompression,
                  sendTimeoutMs: _this.sendTimeoutMs
                }
              });
            })];
          });
        });
      };
      SessionReplayTrackDestination2.prototype.rememberTimedOutRequest = function(id2, context) {
        var e_6, _a4;
        this.timedOutWorkerRequests.set(id2, context);
        if (this.timedOutWorkerRequests.size > MAX_TIMED_OUT_WORKER_REQUESTS) {
          try {
            for (var _b = __values(this.timedOutWorkerRequests.keys()), _c = _b.next(); !_c.done; _c = _b.next()) {
              var oldest = _c.value;
              this.timedOutWorkerRequests.delete(oldest);
              break;
            }
          } catch (e_6_1) {
            e_6 = { error: e_6_1 };
          } finally {
            try {
              if (_c && !_c.done && (_a4 = _b.return)) _a4.call(_b);
            } finally {
              if (e_6) throw e_6.error;
            }
          }
        }
      };
      SessionReplayTrackDestination2.prototype.handleDelegatedFetch = function(worker, msg) {
        var _a4, _b, _c;
        return __awaiter(this, void 0, void 0, function() {
          var requestId, url, method, headers, body, keepalive, res, status_1, skipHeader, responseBody, _d, e_7;
          return __generator(this, function(_e) {
            switch (_e.label) {
              case 0:
                requestId = msg.requestId, url = msg.url, method = msg.method, headers = msg.headers, body = msg.body, keepalive = msg.keepalive;
                _e.label = 1;
              case 1:
                _e.trys.push([1, 10, , 11]);
                res = void 0;
                if (!this.handleSendEvents) return [3, 3];
                return [4, this.handleSendEvents({ url, method, headers, body, keepalive })];
              case 2:
                res = _e.sent();
                return [3, 5];
              case 3:
                this.loggerProvider.warn("Delegated session replay fetch received without a custom transport configured; falling back to built-in fetch.");
                return [4, fetch(url, { method, headers, body, keepalive })];
              case 4:
                res = _e.sent();
                _e.label = 5;
              case 5:
                status_1 = res.status;
                skipHeader = null;
                responseBody = "";
                if (status_1 >= 200 && status_1 < 300) {
                  skipHeader = (_c = (_b = (_a4 = res.headers) === null || _a4 === void 0 ? void 0 : _a4.get) === null || _b === void 0 ? void 0 : _b.call(_a4, EVENT_SKIPPED_HEADER)) !== null && _c !== void 0 ? _c : null;
                }
                if (!(status_1 === 413)) return [3, 9];
                _e.label = 6;
              case 6:
                _e.trys.push([6, 8, , 9]);
                return [4, res.text()];
              case 7:
                responseBody = _e.sent();
                return [3, 9];
              case 8:
                _d = _e.sent();
                return [3, 9];
              case 9:
                this.loggerProvider.debug("Delegated session replay fetch (request ".concat(requestId, ") returned status ").concat(status_1, "."));
                worker.postMessage({ type: "fetch-response", requestId, status: status_1, skipHeader, body: responseBody });
                return [3, 11];
              case 10:
                e_7 = _e.sent();
                this.loggerProvider.debug("Delegated session replay fetch (request ".concat(requestId, ") failed:"), e_7);
                worker.postMessage({
                  type: "fetch-response",
                  requestId,
                  status: 0,
                  skipHeader: null,
                  body: String(e_7),
                  error: true
                });
                return [3, 11];
              case 11:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayTrackDestination2.prototype.sendOnMainThread = function(apiKey2, deviceId, context, payload, useRetry) {
        var _a4, _b, _c, _d, _e, _f, _g;
        return __awaiter(this, void 0, void 0, function() {
          var url, version2, sampleRate, urlParams, sessionReplayLibrary, payloadJson, globalScope3, gzipped, _h, payloadSize, controller_1, headers, body, keepalive, options, serverUrl, sendTimeout, res, _j, skipCode, responseBody, responseBody, _k, e_8, isAbort;
          return __generator(this, function(_l) {
            switch (_l.label) {
              case 0:
                url = getCurrentUrl();
                version2 = VERSION3;
                sampleRate = context.sampleRate;
                urlParams = new URLSearchParams({
                  device_id: deviceId,
                  session_id: "".concat(context.sessionId),
                  type: "".concat(context.type)
                });
                sessionReplayLibrary = "".concat((_b = (_a4 = context.version) === null || _a4 === void 0 ? void 0 : _a4.type) !== null && _b !== void 0 ? _b : "standalone", "/").concat((_d = (_c = context.version) === null || _c === void 0 ? void 0 : _c.version) !== null && _d !== void 0 ? _d : version2);
                _l.label = 1;
              case 1:
                _l.trys.push([1, 19, , 23]);
                payloadJson = JSON.stringify(payload);
                globalScope3 = getGlobalScope();
                if (!(this.enableTransportCompression && globalScope3 && "CompressionStream" in globalScope3)) return [3, 3];
                return [4, gzipJson(payloadJson, globalScope3)];
              case 2:
                _h = _l.sent();
                return [3, 4];
              case 3:
                _h = null;
                _l.label = 4;
              case 4:
                gzipped = _h;
                payloadSize = gzipped ? gzipped.byteLength : new Blob([payloadJson]).size;
                controller_1 = new AbortController();
                headers = __assign({ "Content-Type": "application/json", Accept: "*/*", Authorization: "Bearer ".concat(apiKey2), "X-Client-Version": version2, "X-Client-Library": sessionReplayLibrary, "X-Client-Url": url.substring(0, MAX_URL_LENGTH), "X-Client-Sample-Rate": "".concat(sampleRate), "X-Sampling-Hash-Alg": "xxhash32" }, gzipped ? { "Content-Encoding": "gzip" } : {});
                body = gzipped !== null && gzipped !== void 0 ? gzipped : payloadJson;
                keepalive = payloadSize <= MAX_KEEPALIVE_BYTES;
                options = {
                  headers,
                  body,
                  method: "POST",
                  keepalive,
                  signal: controller_1.signal
                };
                serverUrl = "".concat(getServerUrl2(context.serverZone, this.trackServerUrl), "?").concat(urlParams.toString());
                if (payload.events.length === 0) {
                  this.completeRequest({ context });
                  return [
                    2
                    /*return*/
                  ];
                }
                sendTimeout = this.sendTimeoutMs > 0 ? setTimeout(function() {
                  controller_1.abort();
                }, this.sendTimeoutMs) : void 0;
                res = void 0;
                _l.label = 5;
              case 5:
                _l.trys.push([5, , 10, 11]);
                if (!this.handleSendEvents) return [3, 7];
                return [4, abortable(this.handleSendEvents({ url: serverUrl, method: "POST", headers, body, keepalive }), controller_1.signal)];
              case 6:
                _j = _l.sent();
                return [3, 9];
              case 7:
                return [4, fetch(serverUrl, options)];
              case 8:
                _j = _l.sent();
                _l.label = 9;
              case 9:
                res = _j;
                return [3, 11];
              case 10:
                if (sendTimeout)
                  clearTimeout(sendTimeout);
                return [
                  7
                  /*endfinally*/
                ];
              case 11:
                if (res === null) {
                  this.completeRequest({ context, err: UNEXPECTED_ERROR_MESSAGE2 });
                  return [
                    2
                    /*return*/
                  ];
                }
                if (res.status >= 200 && res.status < 300) {
                  skipCode = (_g = (_f = (_e = res.headers) === null || _e === void 0 ? void 0 : _e.get) === null || _f === void 0 ? void 0 : _f.call(_e, EVENT_SKIPPED_HEADER)) !== null && _g !== void 0 ? _g : null;
                  this.applyServerDirective(context.sessionId, skipCode);
                }
                if (!!useRetry) return [3, 12];
                responseBody = "";
                try {
                  responseBody = JSON.stringify(res.body, null, 2);
                } catch (_m) {
                }
                this.completeRequest({ context, success: "".concat(res.status, ": ").concat(responseBody) });
                return [3, 18];
              case 12:
                responseBody = "";
                if (!(res.status === 413)) return [3, 16];
                _l.label = 13;
              case 13:
                _l.trys.push([13, 15, , 16]);
                return [4, res.text()];
              case 14:
                responseBody = _l.sent();
                return [3, 16];
              case 15:
                _k = _l.sent();
                return [3, 16];
              case 16:
                return [4, this.handleReponse(res.status, context, responseBody)];
              case 17:
                _l.sent();
                _l.label = 18;
              case 18:
                return [3, 23];
              case 19:
                e_8 = _l.sent();
                isAbort = !!e_8 && typeof e_8 === "object" && e_8.name === "AbortError";
                if (!(isAbort && useRetry)) return [3, 21];
                return [4, this.handleOtherResponse(context)];
              case 20:
                _l.sent();
                return [3, 22];
              case 21:
                this.completeRequest({ context, err: e_8 });
                _l.label = 22;
              case 22:
                return [3, 23];
              case 23:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayTrackDestination2.prototype.handleReponse = function(status, context, responseBody) {
        if (responseBody === void 0) {
          responseBody = "";
        }
        return __awaiter(this, void 0, void 0, function() {
          var parsedStatus, _a4;
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                parsedStatus = new BaseTransport().buildStatus(status);
                _a4 = parsedStatus;
                switch (_a4) {
                  case Status.Success:
                    return [3, 1];
                  case Status.Failed:
                    return [3, 2];
                  case Status.Timeout:
                    return [3, 2];
                  case Status.RateLimit:
                    return [3, 2];
                  case Status.PayloadTooLarge:
                    return [3, 4];
                }
                return [3, 5];
              case 1:
                this.handleSuccessResponse(context);
                return [3, 8];
              case 2:
                return [4, this.handleOtherResponse(context)];
              case 3:
                _b.sent();
                return [3, 8];
              case 4:
                this.handlePayloadTooLargeResponse(context, WAF_PAYLOAD_TOO_LARGE_PATTERN.test(responseBody));
                return [3, 8];
              case 5:
                if (!(status === 499)) return [3, 7];
                return [4, this.handleOtherResponse(context)];
              case 6:
                _b.sent();
                return [3, 8];
              case 7:
                this.completeRequest({ context, err: UNEXPECTED_NETWORK_ERROR_MESSAGE });
                _b.label = 8;
              case 8:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayTrackDestination2.prototype.handlePayloadTooLargeResponse = function(context, isWaf) {
        var source = isWaf ? "WAF (compressed payload too large)" : "server (event too large)";
        var totalSizeKB = Math.round(context.events.reduce(function(sum, e2) {
          return sum + e2.length;
        }, 0) / KB_SIZE);
        if (!isWaf) {
          this.completeRequest({
            context,
            err: "Session replay event batch dropped: ".concat(source, " rejected payload (").concat(context.events.length, " events, ").concat(totalSizeKB, " KB) \u2014 not retrying non-WAF 413")
          });
          return;
        }
        if (context.events.length === 1) {
          this.completeRequest({
            context,
            err: "Session replay event dropped: single event (".concat(totalSizeKB, " KB, 1 event) rejected by ").concat(source, " \u2014 cannot split further")
          });
          return;
        }
        this.loggerProvider.warn("Session replay event batch rejected by ".concat(source, " (").concat(context.events.length, " events, ").concat(totalSizeKB, " KB total) \u2014 splitting and retrying"));
        void context.onComplete();
        var noop = function() {
          return Promise.resolve();
        };
        var mid = Math.floor(context.events.length / 2);
        this.sendEventsList(__assign(__assign({}, context), { events: context.events.slice(0, mid), onComplete: noop }));
        this.sendEventsList(__assign(__assign({}, context), { events: context.events.slice(mid), onComplete: noop }));
      };
      SessionReplayTrackDestination2.prototype.handleSuccessResponse = function(context) {
        var sizeOfEventsList = Math.round(new Blob(context.events).size / KB_SIZE);
        this.completeRequest({
          context,
          success: "Session replay event batch tracked successfully for session id ".concat(context.sessionId, ", size of events: ").concat(sizeOfEventsList, " KB")
        });
      };
      SessionReplayTrackDestination2.prototype.handleOtherResponse = function(context) {
        return __awaiter(this, void 0, void 0, function() {
          var delay;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                delay = Math.random() * context.attempts * this.retryTimeout;
                context.attempts++;
                if (context.attempts > (context.flushMaxRetries || 0)) {
                  this.completeRequest({ context, err: MAX_RETRIES_EXCEEDED_MESSAGE2 });
                  return [
                    2
                    /*return*/
                  ];
                }
                return [4, new Promise(function(resolve) {
                  return setTimeout(resolve, delay);
                })];
              case 1:
                _a4.sent();
                return [4, this.send(context, true)];
              case 2:
                _a4.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayTrackDestination2.prototype.completeRequest = function(_a4) {
        var context = _a4.context, err = _a4.err, success = _a4.success;
        void context.onComplete();
        if (err) {
          this.loggerProvider.warn(err);
        } else if (success) {
          this.loggerProvider.log(success);
        }
      };
      SessionReplayTrackDestination2.prototype.applyServerDirective = function(sessionId, skipCode) {
        if (skipCode === null) {
          this.flushPauseUntilMs = 0;
          this.mergeLogFiredThisPause = false;
          return;
        }
        if (skipCode === EVENT_SKIP_CODE_THROTTLED) {
          var wasInPause = this.flushPauseUntilMs > Date.now();
          this.flushPauseUntilMs = Date.now() + THROTTLED_FLUSH_PAUSE_MS;
          if (!wasInPause) {
            this.loggerProvider.log("Session replay throttled by server; pausing flush schedule for ".concat(THROTTLED_FLUSH_PAUSE_MS / 1e3, "s"));
          }
          return;
        }
        if (skipCode === EVENT_SKIP_CODE_CAPTURE_DISABLED || skipCode === EVENT_SKIP_CODE_INVALID_RANGE) {
          this.killSession(sessionId, skipCode);
        }
      };
      SessionReplayTrackDestination2.prototype.killSession = function(sessionId, skipCode) {
        var e_9, _a4, e_10, _b;
        if (this.killedSessions.has(sessionId))
          return;
        this.killedSessions.add(sessionId);
        if (this.killedSessions.size > MAX_KILLED_SESSIONS) {
          try {
            for (var _c = __values(this.killedSessions), _d = _c.next(); !_d.done; _d = _c.next()) {
              var oldest = _d.value;
              this.killedSessions.delete(oldest);
              break;
            }
          } catch (e_9_1) {
            e_9 = { error: e_9_1 };
          } finally {
            try {
              if (_d && !_d.done && (_a4 = _c.return)) _a4.call(_c);
            } finally {
              if (e_9) throw e_9.error;
            }
          }
        }
        this.loggerProvider.log("Session replay capture stopped for session ".concat(sessionId, " by server directive ").concat(skipCode, "; remaining events will be dropped"));
        var remaining = [];
        try {
          for (var _e = __values(this.queue), _f = _e.next(); !_f.done; _f = _e.next()) {
            var queued = _f.value;
            if (queued.sessionId === sessionId) {
              this.completeRequest({ context: queued, err: SESSION_KILLED_MESSAGE });
            } else {
              remaining.push(queued);
            }
          }
        } catch (e_10_1) {
          e_10 = { error: e_10_1 };
        } finally {
          try {
            if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
          } finally {
            if (e_10) throw e_10.error;
          }
        }
        this.queue = remaining;
      };
      return SessionReplayTrackDestination2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/events-idb-store.js
  init_tslib_es6();
  init_build();

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/base-events-store.js
  init_tslib_es6();
  var BaseEventsStore = (
    /** @class */
    (function() {
      function BaseEventsStore2(args) {
        var _this = this;
        var _a4, _b, _c;
        this.minInterval = MIN_INTERVAL;
        this.maxInterval = MAX_INTERVAL;
        this.maxPersistedEventsSize = DEFAULT_MAX_PERSISTED_EVENTS_SIZE_BYTES;
        this._timeAtLastSplit = Date.now();
        this.shouldSplitEventsList = function(events, nextEventString) {
          var sizeOfNextEvent = _this.getStringSize(nextEventString);
          var sizeOfEventsList = _this.getEventsArraySize(events);
          if (sizeOfEventsList + sizeOfNextEvent >= _this.maxPersistedEventsSize) {
            return true;
          }
          if (Date.now() - _this.timeAtLastSplit > _this.interval && events.length) {
            _this.interval = Math.min(_this.maxInterval, _this.interval + _this.minInterval);
            _this._timeAtLastSplit = Date.now();
            return true;
          }
          return false;
        };
        this.loggerProvider = args.loggerProvider;
        this.minInterval = (_a4 = args.minInterval) !== null && _a4 !== void 0 ? _a4 : this.minInterval;
        this.maxInterval = (_b = args.maxInterval) !== null && _b !== void 0 ? _b : this.maxInterval;
        this.maxPersistedEventsSize = (_c = args.maxPersistedEventsSize) !== null && _c !== void 0 ? _c : this.maxPersistedEventsSize;
        this.interval = this.minInterval;
      }
      Object.defineProperty(BaseEventsStore2.prototype, "timeAtLastSplit", {
        get: function() {
          return this._timeAtLastSplit;
        },
        enumerable: false,
        configurable: true
      });
      BaseEventsStore2.prototype.getStringSize = function(str) {
        var bytes = 0;
        for (var i2 = 0; i2 < str.length; i2++) {
          var code = str.charCodeAt(i2);
          if (code <= 127) {
            bytes++;
          } else if (code <= 2047) {
            bytes += 2;
          } else if (code >= 55296 && code <= 56319) {
            var next = i2 + 1 < str.length ? str.charCodeAt(i2 + 1) : NaN;
            if (next >= 56320 && next <= 57343) {
              bytes += 4;
              i2++;
            } else {
              bytes += 3;
            }
          } else if (code >= 56320 && code <= 57343) {
            bytes += 3;
          } else {
            bytes += 3;
          }
        }
        return bytes;
      };
      BaseEventsStore2.prototype.getEventsArraySize = function(events) {
        var e_1, _a4;
        var totalSize = 0;
        try {
          for (var events_1 = __values(events), events_1_1 = events_1.next(); !events_1_1.done; events_1_1 = events_1.next()) {
            var event_1 = events_1_1.value;
            totalSize += this.getStringSize(event_1);
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (events_1_1 && !events_1_1.done && (_a4 = events_1.return)) _a4.call(events_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        var overhead = 2 + Math.max(0, events.length - 1) + events.length * 2;
        return totalSize + overhead;
      };
      return BaseEventsStore2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/utils/is-abort-error.js
  var isAbortError = function(e2) {
    return typeof e2 === "object" && e2 !== null && e2.name === "AbortError";
  };
  var logIdbError = function(logger, message, error) {
    if (isAbortError(error)) {
      logger.debug(message);
    } else {
      logger.warn(message);
    }
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/events-idb-store.js
  function generateUUID() {
    try {
      return crypto.randomUUID();
    } catch (_a4) {
      return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, function(c2) {
        var r2 = Math.random() * 16 | 0;
        return (c2 === "x" ? r2 : r2 & 3 | 8).toString(16);
      });
    }
  }
  var currentSequenceKey = "sessionCurrentSequence";
  var sequencesToSendKey = "sequencesToSend";
  var OPEN_DB_TIMEOUT_MS = 2e3;
  var TX_DONE_TIMEOUT_MS = 5e3;
  function withTimeout(promise, ms, message) {
    if (message === void 0) {
      message = "IDB operation timed out";
    }
    return new Promise(function(resolve, reject) {
      var timer = setTimeout(function() {
        return reject(new Error("".concat(message, " after ").concat(ms, "ms")));
      }, ms);
      promise.then(function(v2) {
        clearTimeout(timer);
        resolve(v2);
      }, function(e2) {
        clearTimeout(timer);
        reject(e2);
      });
    });
  }
  function armTxDoneTimeout(txDone, ms, onTimeout) {
    var timer = setTimeout(onTimeout, ms);
    txDone.then(function() {
      return clearTimeout(timer);
    }, function() {
      return clearTimeout(timer);
    });
    return function() {
      return clearTimeout(timer);
    };
  }
  var defineObjectStores = function(db) {
    var sequencesStore;
    var currentSequenceStore;
    if (!db.objectStoreNames.contains(currentSequenceKey)) {
      currentSequenceStore = db.createObjectStore(currentSequenceKey, {
        keyPath: "sessionId"
      });
    }
    if (!db.objectStoreNames.contains(sequencesToSendKey)) {
      sequencesStore = db.createObjectStore(sequencesToSendKey, {
        keyPath: "sequenceId",
        autoIncrement: true
      });
      sequencesStore.createIndex("sessionId", "sessionId");
    }
    return {
      sequencesStore,
      currentSequenceStore
    };
  };
  var createStore = function(dbName) {
    return __awaiter(void 0, void 0, void 0, function() {
      return __generator(this, function(_a4) {
        switch (_a4.label) {
          case 0:
            return [4, withTimeout(openDB(dbName, 1, {
              upgrade: defineObjectStores
            }), OPEN_DB_TIMEOUT_MS, "IDB openDB timed out")];
          case 1:
            return [2, _a4.sent()];
        }
      });
    });
  };
  var SessionReplayEventsIDBStore = (
    /** @class */
    (function(_super) {
      __extends(SessionReplayEventsIDBStore2, _super);
      function SessionReplayEventsIDBStore2(args) {
        var _this = this;
        var _a4;
        _this = _super.call(this, args) || this;
        _this.consecutiveFailures = 0;
        _this.hasTriggeredFallback = false;
        _this.emptyFilteredCount = 0;
        _this.getSequencesToSend = function() {
          return __awaiter(_this, void 0, void 0, function() {
            var errorLogged, timedOut, sequences, tx, cancelTimeout, cursor, _a5, sessionId, events, e_1;
            var _this2 = this;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  errorLogged = false;
                  timedOut = false;
                  _b.label = 1;
                case 1:
                  _b.trys.push([1, 9, , 10]);
                  sequences = [];
                  tx = this.db.transaction("sequencesToSend", "readwrite");
                  tx.done.catch(function(e2) {
                    if (!errorLogged && !timedOut) {
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e2), e2);
                      _this2.recordFailure();
                    }
                  });
                  cancelTimeout = armTxDoneTimeout(tx.done, TX_DONE_TIMEOUT_MS, function() {
                    if (!errorLogged && !timedOut) {
                      timedOut = true;
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": transaction timed out"));
                      _this2.recordFailure();
                    }
                  });
                  return [4, tx.store.openCursor()];
                case 2:
                  cursor = _b.sent();
                  _b.label = 3;
                case 3:
                  if (!cursor) return [3, 8];
                  _a5 = cursor.value, sessionId = _a5.sessionId, events = _a5.events;
                  if (!(events.length === 0)) return [3, 5];
                  this.maybeLogEmptyFiltered("getSequencesToSend");
                  return [4, cursor.delete()];
                case 4:
                  _b.sent();
                  return [3, 6];
                case 5:
                  sequences.push({
                    events,
                    sequenceId: cursor.key,
                    sessionId
                  });
                  _b.label = 6;
                case 6:
                  return [4, cursor.continue()];
                case 7:
                  cursor = _b.sent();
                  return [3, 3];
                case 8:
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, sequences];
                case 9:
                  e_1 = _b.sent();
                  if (!timedOut) {
                    errorLogged = true;
                    logIdbError(this.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_1), e_1);
                    this.recordFailure();
                  }
                  return [3, 10];
                case 10:
                  return [2, void 0];
              }
            });
          });
        };
        _this.storeCurrentSequence = function(sessionId) {
          return __awaiter(_this, void 0, void 0, function() {
            var errorLogged, timedOut, tx, cancelTimeout, currentSequenceData, sequenceId, _tabId, rest, e_2;
            var _this2 = this;
            return __generator(this, function(_a5) {
              switch (_a5.label) {
                case 0:
                  errorLogged = false;
                  timedOut = false;
                  _a5.label = 1;
                case 1:
                  _a5.trys.push([1, 5, , 6]);
                  tx = this.db.transaction([currentSequenceKey, sequencesToSendKey], "readwrite");
                  tx.done.catch(function(e2) {
                    if (!errorLogged && !timedOut) {
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e2), e2);
                      _this2.recordFailure();
                    }
                  });
                  cancelTimeout = armTxDoneTimeout(tx.done, TX_DONE_TIMEOUT_MS, function() {
                    if (!errorLogged && !timedOut) {
                      timedOut = true;
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": transaction timed out"));
                      _this2.recordFailure();
                    }
                  });
                  return [4, tx.objectStore(currentSequenceKey).get(sessionId)];
                case 2:
                  currentSequenceData = _a5.sent();
                  if (!currentSequenceData || currentSequenceData.tabId && currentSequenceData.tabId !== this.tabId) {
                    cancelTimeout();
                    return [2, void 0];
                  }
                  if (currentSequenceData.events.length === 0) {
                    this.maybeLogEmptyFiltered("storeCurrentSequence");
                    cancelTimeout();
                    return [2, void 0];
                  }
                  return [4, tx.objectStore(sequencesToSendKey).put({
                    sessionId,
                    events: currentSequenceData.events,
                    tabId: this.tabId
                  })];
                case 3:
                  sequenceId = _a5.sent();
                  return [4, tx.objectStore(currentSequenceKey).put({
                    sessionId,
                    events: [],
                    tabId: this.tabId
                  })];
                case 4:
                  _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  _tabId = currentSequenceData.tabId, rest = __rest(currentSequenceData, ["tabId"]);
                  return [2, __assign(__assign({}, rest), { sessionId, sequenceId })];
                case 5:
                  e_2 = _a5.sent();
                  if (!timedOut) {
                    errorLogged = true;
                    logIdbError(this.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_2), e_2);
                    this.recordFailure();
                  }
                  return [3, 6];
                case 6:
                  return [2, void 0];
              }
            });
          });
        };
        _this.addEventToCurrentSequence = function(sessionId, event) {
          return __awaiter(_this, void 0, void 0, function() {
            var errorLogged, timedOut, tx, cancelTimeout, sequenceEvents, ownedSequence, eventsToSend, sequenceId, e_3;
            var _this2 = this;
            return __generator(this, function(_a5) {
              switch (_a5.label) {
                case 0:
                  errorLogged = false;
                  timedOut = false;
                  _a5.label = 1;
                case 1:
                  _a5.trys.push([1, 15, , 16]);
                  tx = this.db.transaction([currentSequenceKey, sequencesToSendKey], "readwrite");
                  tx.done.catch(function(e2) {
                    if (!errorLogged && !timedOut) {
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e2), e2);
                      _this2.recordFailure();
                    }
                  });
                  cancelTimeout = armTxDoneTimeout(tx.done, TX_DONE_TIMEOUT_MS, function() {
                    if (!errorLogged && !timedOut) {
                      timedOut = true;
                      logIdbError(_this2.loggerProvider, "".concat(STORAGE_FAILURE, ": transaction timed out"));
                      _this2.recordFailure();
                    }
                  });
                  return [4, tx.objectStore(currentSequenceKey).get(sessionId)];
                case 2:
                  sequenceEvents = _a5.sent();
                  if (!((sequenceEvents === null || sequenceEvents === void 0 ? void 0 : sequenceEvents.tabId) && sequenceEvents.tabId !== this.tabId)) return [3, 6];
                  if (!(sequenceEvents.events.length > 0)) return [3, 4];
                  return [4, tx.objectStore(sequencesToSendKey).put({
                    sessionId,
                    events: sequenceEvents.events,
                    tabId: sequenceEvents.tabId
                  })];
                case 3:
                  _a5.sent();
                  _a5.label = 4;
                case 4:
                  return [4, tx.objectStore(currentSequenceKey).put({ sessionId, events: [event], tabId: this.tabId })];
                case 5:
                  _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, void 0];
                case 6:
                  ownedSequence = sequenceEvents;
                  if (!!ownedSequence) return [3, 8];
                  return [4, tx.objectStore(currentSequenceKey).put({ sessionId, events: [event], tabId: this.tabId })];
                case 7:
                  _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, void 0];
                case 8:
                  if (!!this.shouldSplitEventsList(ownedSequence.events, event)) return [3, 10];
                  return [4, tx.objectStore(currentSequenceKey).put({ sessionId, events: ownedSequence.events.concat(event), tabId: this.tabId })];
                case 9:
                  _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, void 0];
                case 10:
                  eventsToSend = ownedSequence.events;
                  if (!(eventsToSend.length === 0)) return [3, 12];
                  this.maybeLogEmptyFiltered("addEventToCurrentSequence");
                  return [4, tx.objectStore(currentSequenceKey).put({ sessionId, events: [event], tabId: this.tabId })];
                case 11:
                  _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, void 0];
                case 12:
                  return [4, tx.objectStore(currentSequenceKey).put({ sessionId, events: [event], tabId: this.tabId })];
                case 13:
                  _a5.sent();
                  return [4, tx.objectStore(sequencesToSendKey).put({
                    sessionId,
                    events: eventsToSend,
                    tabId: this.tabId
                  })];
                case 14:
                  sequenceId = _a5.sent();
                  this.recordSuccess();
                  cancelTimeout();
                  return [2, {
                    events: eventsToSend,
                    sessionId,
                    sequenceId
                  }];
                case 15:
                  e_3 = _a5.sent();
                  if (!timedOut) {
                    errorLogged = true;
                    logIdbError(this.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_3), e_3);
                    this.recordFailure();
                  }
                  return [3, 16];
                case 16:
                  return [2, void 0];
              }
            });
          });
        };
        _this.storeSendingEvents = function(sessionId, events) {
          return __awaiter(_this, void 0, void 0, function() {
            var sequenceId, e_4;
            return __generator(this, function(_a5) {
              switch (_a5.label) {
                case 0:
                  _a5.trys.push([0, 2, , 3]);
                  return [4, this.db.put(sequencesToSendKey, {
                    sessionId,
                    events,
                    tabId: this.tabId
                  })];
                case 1:
                  sequenceId = _a5.sent();
                  this.recordSuccess();
                  return [2, sequenceId];
                case 2:
                  e_4 = _a5.sent();
                  logIdbError(this.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_4), e_4);
                  this.recordFailure();
                  return [3, 3];
                case 3:
                  return [2, void 0];
              }
            });
          });
        };
        _this.cleanUpSessionEventsStore = function(_sessionId, sequenceId) {
          return __awaiter(_this, void 0, void 0, function() {
            var e_5;
            return __generator(this, function(_a5) {
              switch (_a5.label) {
                case 0:
                  if (!sequenceId) {
                    return [
                      2
                      /*return*/
                    ];
                  }
                  _a5.label = 1;
                case 1:
                  _a5.trys.push([1, 3, , 4]);
                  return [4, this.db.delete(sequencesToSendKey, sequenceId)];
                case 2:
                  _a5.sent();
                  this.recordSuccess();
                  return [3, 4];
                case 3:
                  e_5 = _a5.sent();
                  logIdbError(this.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_5), e_5);
                  this.recordFailure();
                  return [3, 4];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        _this.db = args.db;
        _this.tabId = args.tabId;
        _this.onPersistentFailure = args.onPersistentFailure;
        _this.consecutiveFailureThreshold = (_a4 = args.consecutiveFailureThreshold) !== null && _a4 !== void 0 ? _a4 : 1;
        return _this;
      }
      SessionReplayEventsIDBStore2.prototype.maybeLogEmptyFiltered = function(source) {
        if (this.emptyFilteredCount++ % 100 === 0) {
          this.loggerProvider.debug("Filtered empty session replay sequence at ".concat(source, " (idb store)"));
        }
      };
      SessionReplayEventsIDBStore2.prototype.recordFailure = function() {
        var _a4;
        this.consecutiveFailures++;
        if (!this.hasTriggeredFallback && this.consecutiveFailures >= this.consecutiveFailureThreshold) {
          this.hasTriggeredFallback = true;
          (_a4 = this.onPersistentFailure) === null || _a4 === void 0 ? void 0 : _a4.call(this);
        }
      };
      SessionReplayEventsIDBStore2.prototype.recordSuccess = function() {
        this.consecutiveFailures = 0;
      };
      SessionReplayEventsIDBStore2.new = function(type, args) {
        var _a4;
        return __awaiter(this, void 0, void 0, function() {
          var dbSuffix, dbName, db, tabId, e_6;
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                _b.trys.push([0, 2, , 3]);
                dbSuffix = type === "replay" ? "" : "_".concat(type);
                dbName = "".concat(args.apiKey.substring(0, 10), "_amp_session_replay_events").concat(dbSuffix);
                return [4, createStore(dbName)];
              case 1:
                db = _b.sent();
                tabId = (_a4 = args.tabId) !== null && _a4 !== void 0 ? _a4 : generateUUID();
                return [2, new SessionReplayEventsIDBStore2(__assign(__assign({}, args), { db, tabId }))];
              case 2:
                e_6 = _b.sent();
                logIdbError(args.loggerProvider, "".concat(STORAGE_FAILURE, ": ").concat(e_6), e_6);
                return [3, 3];
              case 3:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayEventsIDBStore2.prototype.getCurrentSequenceEvents = function(sessionId) {
        return __awaiter(this, void 0, void 0, function() {
          var record2, _tabId, rest, allEvents, _a4, _b, record2, _tabId, rest, e_7_1;
          var e_7, _c;
          return __generator(this, function(_d) {
            switch (_d.label) {
              case 0:
                if (!sessionId) return [3, 2];
                return [4, this.db.get("sessionCurrentSequence", sessionId)];
              case 1:
                record2 = _d.sent();
                if (!record2) {
                  return [2, void 0];
                }
                if (record2.tabId && record2.tabId !== this.tabId) {
                  return [2, void 0];
                }
                _tabId = record2.tabId, rest = __rest(record2, ["tabId"]);
                return [2, [rest]];
              case 2:
                allEvents = [];
                _d.label = 3;
              case 3:
                _d.trys.push([3, 8, 9, 10]);
                return [4, this.db.getAll("sessionCurrentSequence")];
              case 4:
                _a4 = __values.apply(void 0, [_d.sent()]), _b = _a4.next();
                _d.label = 5;
              case 5:
                if (!!_b.done) return [3, 7];
                record2 = _b.value;
                if (record2.tabId && record2.tabId !== this.tabId) {
                  return [3, 6];
                }
                _tabId = record2.tabId, rest = __rest(record2, ["tabId"]);
                allEvents.push(rest);
                _d.label = 6;
              case 6:
                _b = _a4.next();
                return [3, 5];
              case 7:
                return [3, 10];
              case 8:
                e_7_1 = _d.sent();
                e_7 = { error: e_7_1 };
                return [3, 10];
              case 9:
                try {
                  if (_b && !_b.done && (_c = _a4.return)) _c.call(_a4);
                } finally {
                  if (e_7) throw e_7.error;
                }
                return [
                  7
                  /*endfinally*/
                ];
              case 10:
                return [2, allEvents];
            }
          });
        });
      };
      return SessionReplayEventsIDBStore2;
    })(BaseEventsStore)
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/events-memory-store.js
  init_tslib_es6();
  var InMemoryEventsStore = (
    /** @class */
    (function(_super) {
      __extends(InMemoryEventsStore2, _super);
      function InMemoryEventsStore2() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        _this.finalizedSequences = {};
        _this.sequences = {};
        _this.sequenceId = 0;
        _this.emptyFilteredCount = 0;
        return _this;
      }
      InMemoryEventsStore2.prototype.resetCurrentSequence = function(sessionId) {
        this.sequences[sessionId] = [];
      };
      InMemoryEventsStore2.prototype.addSequence = function(sessionId) {
        var sequenceId = this.sequenceId++;
        var events = __spreadArray([], __read(this.sequences[sessionId]), false);
        this.finalizedSequences[sequenceId] = { sessionId, events };
        this.resetCurrentSequence(sessionId);
        return { sequenceId, events, sessionId };
      };
      InMemoryEventsStore2.prototype.maybeLogEmptyFiltered = function(source) {
        if (this.emptyFilteredCount++ % 100 === 0) {
          this.loggerProvider.debug("Filtered empty session replay sequence at ".concat(source, " (in-memory store)"));
        }
      };
      InMemoryEventsStore2.prototype.getSequencesToSend = function() {
        return __awaiter(this, void 0, void 0, function() {
          var result3, _a4, _b, _c, sequenceId, _d, sessionId, events;
          var e_1, _e;
          return __generator(this, function(_f) {
            result3 = [];
            try {
              for (_a4 = __values(Object.entries(this.finalizedSequences)), _b = _a4.next(); !_b.done; _b = _a4.next()) {
                _c = __read(_b.value, 2), sequenceId = _c[0], _d = _c[1], sessionId = _d.sessionId, events = _d.events;
                if (events.length === 0) {
                  this.maybeLogEmptyFiltered("getSequencesToSend");
                  delete this.finalizedSequences[Number(sequenceId)];
                  continue;
                }
                result3.push({ sequenceId: Number(sequenceId), sessionId, events });
              }
            } catch (e_1_1) {
              e_1 = { error: e_1_1 };
            } finally {
              try {
                if (_b && !_b.done && (_e = _a4.return)) _e.call(_a4);
              } finally {
                if (e_1) throw e_1.error;
              }
            }
            return [2, result3];
          });
        });
      };
      InMemoryEventsStore2.prototype.storeCurrentSequence = function(sessionId) {
        return __awaiter(this, void 0, void 0, function() {
          var buffered;
          return __generator(this, function(_a4) {
            buffered = this.sequences[sessionId];
            if (!buffered) {
              return [2, void 0];
            }
            if (buffered.length === 0) {
              this.maybeLogEmptyFiltered("storeCurrentSequence");
              return [2, void 0];
            }
            return [2, this.addSequence(sessionId)];
          });
        });
      };
      InMemoryEventsStore2.prototype.addEventToCurrentSequence = function(sessionId, event) {
        return __awaiter(this, void 0, void 0, function() {
          var sequenceReturn;
          return __generator(this, function(_a4) {
            if (!this.sequences[sessionId]) {
              this.resetCurrentSequence(sessionId);
            }
            if (this.shouldSplitEventsList(this.sequences[sessionId], event)) {
              if (this.sequences[sessionId].length === 0) {
                this.maybeLogEmptyFiltered("addEventToCurrentSequence");
              } else {
                sequenceReturn = this.addSequence(sessionId);
              }
            }
            this.sequences[sessionId].push(event);
            return [2, sequenceReturn];
          });
        });
      };
      InMemoryEventsStore2.prototype.storeSendingEvents = function(sessionId, events) {
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_a4) {
            this.finalizedSequences[this.sequenceId] = { sessionId, events };
            return [2, this.sequenceId++];
          });
        });
      };
      InMemoryEventsStore2.prototype.cleanUpSessionEventsStore = function(_sessionId, sequenceId) {
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_a4) {
            if (sequenceId !== void 0) {
              delete this.finalizedSequences[sequenceId];
            }
            return [
              2
              /*return*/
            ];
          });
        });
      };
      return InMemoryEventsStore2;
    })(BaseEventsStore)
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/events-manager.js
  var createEventsManager = function(_a4) {
    var config2 = _a4.config, minInterval = _a4.minInterval, maxInterval = _a4.maxInterval, maxPersistedEventsSize = _a4.maxPersistedEventsSize, type = _a4.type, payloadBatcher = _a4.payloadBatcher, storeType = _a4.storeType, trackDestinationWorkerScript = _a4.trackDestinationWorkerScript, shouldSend = _a4.shouldSend;
    return __awaiter(void 0, void 0, void 0, function() {
      function flush3(useRetry) {
        if (useRetry === void 0) {
          useRetry = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_a5) {
            return [2, trackDestination.flush(useRetry)];
          });
        });
      }
      var maxSingleEventSize, trackDestination, getMemoryStore, lastKnownDeviceId, usingIdbStore, store, switchToMemoryStore, getIdbStoreOrFallback, _b, beaconBuffer, beaconWindowStart, advanceBeaconWindow, sendEventsList, sendCurrentSequenceEvents, sendStoredEvents, addEvent, getBeaconEvents, dropPendingBeaconEvents;
      var _c;
      return __generator(this, function(_d) {
        switch (_d.label) {
          case 0:
            maxSingleEventSize = (_c = config2.maxSingleEventSizeBytes) !== null && _c !== void 0 ? _c : MAX_SINGLE_EVENT_SIZE;
            trackDestination = new SessionReplayTrackDestination(__assign(__assign({}, config2), { loggerProvider: config2.loggerProvider, payloadBatcher, workerScript: trackDestinationWorkerScript }));
            getMemoryStore = function() {
              return new InMemoryEventsStore({
                loggerProvider: config2.loggerProvider,
                maxInterval,
                minInterval,
                maxPersistedEventsSize
              });
            };
            usingIdbStore = false;
            switchToMemoryStore = function() {
              return __awaiter(void 0, void 0, void 0, function() {
                var sequences, _a5, deviceId_1;
                return __generator(this, function(_b2) {
                  switch (_b2.label) {
                    case 0:
                      if (!usingIdbStore)
                        return [
                          2
                          /*return*/
                        ];
                      usingIdbStore = false;
                      config2.loggerProvider.warn("IDB store is experiencing repeated failures; falling back to in-memory event store.");
                      if (!lastKnownDeviceId) return [3, 2];
                      return [4, store.getSequencesToSend()];
                    case 1:
                      _a5 = _b2.sent();
                      return [3, 3];
                    case 2:
                      _a5 = void 0;
                      _b2.label = 3;
                    case 3:
                      sequences = _a5;
                      store = getMemoryStore();
                      if (sequences && lastKnownDeviceId) {
                        deviceId_1 = lastKnownDeviceId;
                        sequences.forEach(function(seq) {
                          sendEventsList({ sequenceId: seq.sequenceId, events: seq.events, sessionId: seq.sessionId, deviceId: deviceId_1 });
                        });
                      }
                      return [
                        2
                        /*return*/
                      ];
                  }
                });
              });
            };
            getIdbStoreOrFallback = function() {
              return __awaiter(void 0, void 0, void 0, function() {
                var idb;
                return __generator(this, function(_a5) {
                  switch (_a5.label) {
                    case 0:
                      return [4, SessionReplayEventsIDBStore.new(type, {
                        loggerProvider: config2.loggerProvider,
                        minInterval,
                        maxInterval,
                        maxPersistedEventsSize,
                        apiKey: config2.apiKey,
                        onPersistentFailure: function() {
                          void switchToMemoryStore();
                        }
                      })];
                    case 1:
                      idb = _a5.sent();
                      if (!idb) {
                        config2.loggerProvider.log("Failed to initialize idb store, falling back to memory store.");
                        return [2, getMemoryStore()];
                      }
                      usingIdbStore = true;
                      return [2, idb];
                  }
                });
              });
            };
            if (!(storeType === "idb")) return [3, 2];
            return [4, getIdbStoreOrFallback()];
          case 1:
            _b = _d.sent();
            return [3, 3];
          case 2:
            _b = getMemoryStore();
            _d.label = 3;
          case 3:
            store = _b;
            beaconBuffer = [];
            beaconWindowStart = 0;
            advanceBeaconWindow = function(upToAbsoluteIdx) {
              if (upToAbsoluteIdx <= beaconWindowStart)
                return;
              var trimCount = Math.min(upToAbsoluteIdx - beaconWindowStart, beaconBuffer.length);
              if (trimCount > 0) {
                beaconBuffer.splice(0, trimCount);
                beaconWindowStart = upToAbsoluteIdx;
              }
            };
            sendEventsList = function(_a5) {
              var rawEvents = _a5.events, sessionId = _a5.sessionId, deviceId = _a5.deviceId, sequenceId = _a5.sequenceId;
              var sizedEvents = rawEvents.map(function(e2) {
                return { event: e2, bytes: new Blob([e2]).size };
              });
              var oversized = sizedEvents.filter(function(s2) {
                return s2.bytes > maxSingleEventSize;
              });
              if (oversized.length > 0) {
                config2.loggerProvider.warn("Dropping ".concat(oversized.length, " oversized event(s) from session replay sequence before send. Sizes: ").concat(oversized.map(function(s2) {
                  return "".concat(Math.round(s2.bytes / 1024), " KB");
                }).join(", "), ". If this recurs, please open a GitHub issue at https://github.com/amplitude/Amplitude-TypeScript/issues or contact Amplitude support."));
              }
              var events = oversized.length > 0 ? sizedEvents.filter(function(s2) {
                return s2.bytes <= maxSingleEventSize;
              }).map(function(s2) {
                return s2.event;
              }) : rawEvents;
              if (events.length === 0) {
                store.cleanUpSessionEventsStore(sessionId, sequenceId).catch(function(e2) {
                  config2.loggerProvider.warn("Failed to clean up session replay events store:", e2);
                });
                return;
              }
              if (config2.debugMode) {
                getStorageSize().then(function(_a6) {
                  var totalStorageSize = _a6.totalStorageSize, percentOfQuota = _a6.percentOfQuota, usageDetails = _a6.usageDetails;
                  config2.loggerProvider.debug("Total storage size: ".concat(totalStorageSize, " KB, percentage of quota: ").concat(percentOfQuota, "%, usage details: ").concat(usageDetails));
                }).catch(function() {
                });
              }
              trackDestination.sendEventsList({
                events,
                sessionId,
                flushMaxRetries: config2.flushMaxRetries,
                apiKey: config2.apiKey,
                deviceId,
                sampleRate: config2.sampleRate,
                serverZone: config2.serverZone,
                version: config2.version,
                type,
                onComplete: function() {
                  return __awaiter(void 0, void 0, void 0, function() {
                    return __generator(this, function(_a6) {
                      switch (_a6.label) {
                        case 0:
                          return [4, store.cleanUpSessionEventsStore(sessionId, sequenceId)];
                        case 1:
                          _a6.sent();
                          return [
                            2
                            /*return*/
                          ];
                      }
                    });
                  });
                }
              });
            };
            sendCurrentSequenceEvents = function(_a5) {
              var sessionId = _a5.sessionId, deviceId = _a5.deviceId;
              lastKnownDeviceId = deviceId;
              if (shouldSend && !shouldSend())
                return;
              var snapshotAbsIdx = beaconWindowStart + beaconBuffer.length;
              store.storeCurrentSequence(sessionId).then(function(currentSequence) {
                if (currentSequence) {
                  advanceBeaconWindow(snapshotAbsIdx);
                  sendEventsList({
                    sequenceId: currentSequence.sequenceId,
                    events: currentSequence.events,
                    sessionId: currentSequence.sessionId,
                    deviceId
                  });
                }
              }).catch(function(e2) {
                config2.loggerProvider.warn("Failed to get current sequence of session replay events for session:", e2);
              });
            };
            sendStoredEvents = function(_a5) {
              var deviceId = _a5.deviceId;
              return __awaiter(void 0, void 0, void 0, function() {
                var sequencesToSend;
                return __generator(this, function(_b2) {
                  switch (_b2.label) {
                    case 0:
                      lastKnownDeviceId = deviceId;
                      return [4, store.getSequencesToSend()];
                    case 1:
                      sequencesToSend = _b2.sent();
                      if (!(sequencesToSend === null || sequencesToSend === void 0 ? void 0 : sequencesToSend.length)) {
                        return [
                          2
                          /*return*/
                        ];
                      }
                      config2.loggerProvider.log("Draining ".concat(sequencesToSend.length, " stored sequence(s) from previous session."));
                      if (sequencesToSend.length > 1) {
                        trackDestination.markCoalesceNextFlush();
                      }
                      sequencesToSend.forEach(function(sequence) {
                        sendEventsList({
                          sequenceId: sequence.sequenceId,
                          events: sequence.events,
                          sessionId: sequence.sessionId,
                          deviceId
                        });
                      });
                      return [
                        2
                        /*return*/
                      ];
                  }
                });
              });
            };
            addEvent = function(_a5) {
              var event = _a5.event, sessionId = _a5.sessionId, deviceId = _a5.deviceId;
              lastKnownDeviceId = deviceId;
              var canSend = !shouldSend || shouldSend();
              var absIdx = beaconWindowStart + beaconBuffer.length;
              beaconBuffer.push(event.data);
              store.addEventToCurrentSequence(sessionId, event.data).then(function(sequenceToSend) {
                if (sequenceToSend) {
                  if (!canSend) {
                    store.cleanUpSessionEventsStore(sequenceToSend.sessionId, sequenceToSend.sequenceId).catch(function(e2) {
                      config2.loggerProvider.warn("Failed to clean up dropped session replay sequence:", e2);
                    });
                    advanceBeaconWindow(absIdx);
                    return;
                  }
                  advanceBeaconWindow(absIdx);
                  sendEventsList({
                    sequenceId: sequenceToSend.sequenceId,
                    events: sequenceToSend.events,
                    sessionId: sequenceToSend.sessionId,
                    deviceId
                  });
                }
              }).catch(function(e2) {
                config2.loggerProvider.warn("Failed to add event to session replay capture:", e2);
              });
            };
            getBeaconEvents = function() {
              return __spreadArray([], __read(beaconBuffer), false);
            };
            dropPendingBeaconEvents = function() {
              advanceBeaconWindow(beaconWindowStart + beaconBuffer.length);
            };
            return [2, {
              sendCurrentSequenceEvents,
              addEvent,
              sendStoredEvents,
              flush: flush3,
              getBeaconEvents,
              dropPendingBeaconEvents,
              trackDestination
            }];
        }
      });
    });
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/events/multi-manager.js
  init_tslib_es6();
  var MultiEventManager = (
    /** @class */
    (function() {
      function MultiEventManager2() {
        var managers = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          managers[_i] = arguments[_i];
        }
        var managersMap = /* @__PURE__ */ new Map();
        managers.forEach(function(t2) {
          managersMap.set(t2.name, t2.manager);
        });
        this.managers = managersMap;
      }
      MultiEventManager2.prototype.sendStoredEvents = function(opts) {
        return __awaiter(this, void 0, void 0, function() {
          var promises;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                promises = [];
                this.managers.forEach(function(manager) {
                  promises.push(manager.sendStoredEvents(opts));
                });
                return [4, Promise.all(promises)];
              case 1:
                _a4.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      MultiEventManager2.prototype.addEvent = function(_a4) {
        var _b;
        var sessionId = _a4.sessionId, event = _a4.event, deviceId = _a4.deviceId;
        (_b = this.managers.get(event.type)) === null || _b === void 0 ? void 0 : _b.addEvent({ sessionId, event, deviceId });
      };
      MultiEventManager2.prototype.sendCurrentSequenceEvents = function(_a4) {
        var sessionId = _a4.sessionId, deviceId = _a4.deviceId;
        this.managers.forEach(function(manager) {
          manager.sendCurrentSequenceEvents({ sessionId, deviceId });
        });
      };
      MultiEventManager2.prototype.flush = function(useRetry) {
        return __awaiter(this, void 0, void 0, function() {
          var promises;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                promises = [];
                this.managers.forEach(function(manager) {
                  promises.push(manager.flush(useRetry));
                });
                return [4, Promise.all(promises)];
              case 1:
                _a4.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      return MultiEventManager2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/hooks/click.js
  init_tslib_es6();

  // node_modules/@amplitude/session-replay-browser/lib/esm/libs/finder.js
  init_tslib_es6();
  var config;
  var rootDocument;
  var start;
  function finder(input3, options) {
    start = /* @__PURE__ */ new Date();
    if (input3.nodeType !== Node.ELEMENT_NODE) {
      throw new Error("Can't generate CSS selector for non-element node type.");
    }
    if ("html" === input3.tagName.toLowerCase()) {
      return "html";
    }
    var defaults = {
      root: document.body,
      idName: function(_name) {
        return true;
      },
      className: function(_name) {
        return true;
      },
      tagName: function(_name) {
        return true;
      },
      attr: function(_name, _value) {
        return false;
      },
      seedMinLength: 1,
      optimizedMinLength: 2,
      threshold: 1e3,
      maxNumberOfTries: 1e4,
      timeoutMs: void 0
    };
    config = __assign(__assign({}, defaults), options);
    rootDocument = findRootDocument(config.root, defaults);
    var path = bottomUpSearch(input3, "all", function() {
      return bottomUpSearch(input3, "two", function() {
        return bottomUpSearch(input3, "one", function() {
          return bottomUpSearch(input3, "none");
        });
      });
    });
    if (path) {
      var optimized = sort(optimize(path, input3));
      if (optimized.length > 0) {
        path = optimized[0];
      }
      return selector(path);
    } else {
      throw new Error("Selector was not found.");
    }
  }
  function findRootDocument(rootNode, defaults) {
    if (rootNode.nodeType === Node.DOCUMENT_NODE) {
      return rootNode;
    }
    if (rootNode === defaults.root) {
      return rootNode.ownerDocument;
    }
    return rootNode;
  }
  function bottomUpSearch(input3, limit, fallback) {
    var path = null;
    var stack = [];
    var current = input3;
    var i2 = 0;
    var _loop_1 = function() {
      var e_1, _a4;
      var elapsedTime = (/* @__PURE__ */ new Date()).getTime() - start.getTime();
      if (config.timeoutMs !== void 0 && elapsedTime > config.timeoutMs) {
        throw new Error("Timeout: Can't find a unique selector after ".concat(elapsedTime, "ms"));
      }
      var level = maybe(id(current)) || maybe.apply(void 0, __spreadArray([], __read(attr(current)), false)) || maybe.apply(void 0, __spreadArray([], __read(classNames(current)), false)) || maybe(tagName(current)) || [any()];
      var nth = index(current);
      if (limit == "all") {
        if (nth) {
          level = level.concat(level.filter(dispensableNth).map(function(node4) {
            return nthChild(node4, nth);
          }));
        }
      } else if (limit == "two") {
        level = level.slice(0, 1);
        if (nth) {
          level = level.concat(level.filter(dispensableNth).map(function(node4) {
            return nthChild(node4, nth);
          }));
        }
      } else if (limit == "one") {
        var _b = __read(level = level.slice(0, 1), 1), node3 = _b[0];
        if (nth && dispensableNth(node3)) {
          level = [nthChild(node3, nth)];
        }
      } else if (limit == "none") {
        level = [any()];
        if (nth) {
          level = [nthChild(level[0], nth)];
        }
      }
      try {
        for (var level_1 = (e_1 = void 0, __values(level)), level_1_1 = level_1.next(); !level_1_1.done; level_1_1 = level_1.next()) {
          var node3 = level_1_1.value;
          node3.level = i2;
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (level_1_1 && !level_1_1.done && (_a4 = level_1.return)) _a4.call(level_1);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
      stack.push(level);
      if (stack.length >= config.seedMinLength) {
        path = findUniquePath(stack, fallback);
        if (path) {
          return "break";
        }
      }
      current = current.parentElement;
      i2++;
    };
    while (current) {
      var state_1 = _loop_1();
      if (state_1 === "break")
        break;
    }
    if (!path) {
      path = findUniquePath(stack, fallback);
    }
    if (!path && fallback) {
      return fallback();
    }
    return path;
  }
  function findUniquePath(stack, fallback) {
    var e_2, _a4;
    var paths = sort(combinations(stack));
    if (paths.length > config.threshold) {
      return fallback ? fallback() : null;
    }
    try {
      for (var paths_1 = __values(paths), paths_1_1 = paths_1.next(); !paths_1_1.done; paths_1_1 = paths_1.next()) {
        var candidate = paths_1_1.value;
        if (unique(candidate)) {
          return candidate;
        }
      }
    } catch (e_2_1) {
      e_2 = { error: e_2_1 };
    } finally {
      try {
        if (paths_1_1 && !paths_1_1.done && (_a4 = paths_1.return)) _a4.call(paths_1);
      } finally {
        if (e_2) throw e_2.error;
      }
    }
    return null;
  }
  function selector(path) {
    var node3 = path[0];
    var query = node3.name;
    for (var i2 = 1; i2 < path.length; i2++) {
      var level = path[i2].level || 0;
      if (node3.level === level - 1) {
        query = "".concat(path[i2].name, " > ").concat(query);
      } else {
        query = "".concat(path[i2].name, " ").concat(query);
      }
      node3 = path[i2];
    }
    return query;
  }
  function penalty(path) {
    return path.map(function(node3) {
      return node3.penalty;
    }).reduce(function(acc, i2) {
      return acc + i2;
    }, 0);
  }
  function unique(path) {
    var css = selector(path);
    switch (rootDocument.querySelectorAll(css).length) {
      case 0:
        throw new Error("Can't select any node with this selector: ".concat(css));
      case 1:
        return true;
      default:
        return false;
    }
  }
  function id(input3) {
    var elementId = input3.getAttribute("id");
    if (elementId && config.idName(elementId)) {
      return {
        name: "#" + CSS.escape(elementId),
        penalty: 0
      };
    }
    return null;
  }
  function attr(input3) {
    var attrs = Array.from(input3.attributes).filter(function(attr2) {
      return config.attr(attr2.name, attr2.value);
    });
    return attrs.map(function(attr2) {
      return {
        name: "[".concat(CSS.escape(attr2.name), '="').concat(CSS.escape(attr2.value), '"]'),
        penalty: 0.5
      };
    });
  }
  function classNames(input3) {
    var names = Array.from(input3.classList).filter(config.className);
    return names.map(function(name) {
      return {
        name: "." + CSS.escape(name),
        penalty: 1
      };
    });
  }
  function tagName(input3) {
    var name = input3.tagName.toLowerCase();
    if (config.tagName(name)) {
      return {
        name,
        penalty: 2
      };
    }
    return null;
  }
  function any() {
    return {
      name: "*",
      penalty: 3
    };
  }
  function index(input3) {
    var parent = input3.parentNode;
    if (!parent) {
      return null;
    }
    var child = parent.firstChild;
    if (!child) {
      return null;
    }
    var i2 = 0;
    while (child) {
      if (child.nodeType === Node.ELEMENT_NODE) {
        i2++;
      }
      if (child === input3) {
        break;
      }
      child = child.nextSibling;
    }
    return i2;
  }
  function nthChild(node3, i2) {
    return {
      name: node3.name + ":nth-child(".concat(i2, ")"),
      penalty: node3.penalty + 1
    };
  }
  function dispensableNth(node3) {
    return node3.name !== "html" && !node3.name.startsWith("#");
  }
  function maybe() {
    var level = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      level[_i] = arguments[_i];
    }
    var list = level.filter(notEmpty);
    if (list.length > 0) {
      return list;
    }
    return null;
  }
  function notEmpty(value) {
    return value !== null && value !== void 0;
  }
  function combinations(stack, path) {
    var _a4, _b, node3, e_3_1;
    var e_3, _c;
    if (path === void 0) {
      path = [];
    }
    return __generator(this, function(_d) {
      switch (_d.label) {
        case 0:
          if (!(stack.length > 0)) return [3, 9];
          _d.label = 1;
        case 1:
          _d.trys.push([1, 6, 7, 8]);
          _a4 = __values(stack[0]), _b = _a4.next();
          _d.label = 2;
        case 2:
          if (!!_b.done) return [3, 5];
          node3 = _b.value;
          return [5, __values(combinations(stack.slice(1, stack.length), path.concat(node3)))];
        case 3:
          _d.sent();
          _d.label = 4;
        case 4:
          _b = _a4.next();
          return [3, 2];
        case 5:
          return [3, 8];
        case 6:
          e_3_1 = _d.sent();
          e_3 = { error: e_3_1 };
          return [3, 8];
        case 7:
          try {
            if (_b && !_b.done && (_c = _a4.return)) _c.call(_a4);
          } finally {
            if (e_3) throw e_3.error;
          }
          return [
            7
            /*endfinally*/
          ];
        case 8:
          return [3, 11];
        case 9:
          return [4, path];
        case 10:
          _d.sent();
          _d.label = 11;
        case 11:
          return [
            2
            /*return*/
          ];
      }
    });
  }
  function sort(paths) {
    return __spreadArray([], __read(paths), false).sort(function(a2, b2) {
      return penalty(a2) - penalty(b2);
    });
  }
  function optimize(path, input3, scope) {
    var i2, newPath, newPathKey;
    if (scope === void 0) {
      scope = {
        counter: 0,
        visited: /* @__PURE__ */ new Map()
      };
    }
    return __generator(this, function(_a4) {
      switch (_a4.label) {
        case 0:
          if (!(path.length > 2 && path.length > config.optimizedMinLength)) return [3, 5];
          i2 = 1;
          _a4.label = 1;
        case 1:
          if (!(i2 < path.length - 1)) return [3, 5];
          if (scope.counter > config.maxNumberOfTries) {
            return [
              2
              /*return*/
            ];
          }
          scope.counter += 1;
          newPath = __spreadArray([], __read(path), false);
          newPath.splice(i2, 1);
          newPathKey = selector(newPath);
          if (scope.visited.has(newPathKey)) {
            return [
              2
              /*return*/
            ];
          }
          if (!(unique(newPath) && same(newPath, input3))) return [3, 4];
          return [4, newPath];
        case 2:
          _a4.sent();
          scope.visited.set(newPathKey, true);
          return [5, __values(optimize(newPath, input3, scope))];
        case 3:
          _a4.sent();
          _a4.label = 4;
        case 4:
          i2++;
          return [3, 1];
        case 5:
          return [
            2
            /*return*/
          ];
      }
    });
  }
  function same(path, input3) {
    return rootDocument.querySelector(selector(path)) === input3;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/hooks/click.js
  init_esm();
  var HOUR_IN_MILLISECONDS = 36e5;
  var clickNonBatcher = function(_a4) {
    var version2 = _a4.version, events = _a4.events;
    var clickEvents = [];
    events.forEach(function(evt) {
      var record2 = JSON.parse(evt);
      record2.count = 1;
      if (record2.type === "click") {
        clickEvents.push(record2);
      }
    });
    return { version: version2, events: clickEvents };
  };
  var clickBatcher = function(_a4) {
    var version2 = _a4.version, events = _a4.events;
    var clickEvents = [];
    events.forEach(function(evt) {
      var record2 = JSON.parse(evt);
      if (record2.type === "click") {
        clickEvents.push(record2);
      }
    });
    var reduced = clickEvents.reduce(function(prev, curr) {
      var x2 = curr.x, y2 = curr.y, selector2 = curr.selector, timestamp = curr.timestamp;
      var hour = timestamp - timestamp % HOUR_IN_MILLISECONDS;
      var k2 = "".concat(x2, ":").concat(y2, ":").concat(selector2 !== null && selector2 !== void 0 ? selector2 : "", ":").concat(hour);
      if (!prev[k2]) {
        prev[k2] = __assign(__assign({}, curr), { timestamp: hour, count: 1 });
      } else {
        prev[k2].count += 1;
      }
      return prev;
    }, {});
    return { version: version2, events: Object.values(reduced) };
  };
  var ClickHandler = (
    /** @class */
    /* @__PURE__ */ (function() {
      function ClickHandler2(logger, scrollWatcher) {
        var _this = this;
        this.createHook = function(_a4) {
          var eventsManager = _a4.eventsManager, sessionId = _a4.sessionId, deviceIdFn = _a4.deviceIdFn, mirror2 = _a4.mirror, ugcFilterRules = _a4.ugcFilterRules, performanceOptions = _a4.performanceOptions;
          return function(e2) {
            if (e2.type !== MouseInteractions.Click) {
              return;
            }
            var globalScope3 = getGlobalScope();
            if (!globalScope3) {
              return;
            }
            var location2 = globalScope3.location, innerHeight = globalScope3.innerHeight, innerWidth = globalScope3.innerWidth;
            if (!location2) {
              return;
            }
            var x2 = e2.x, y2 = e2.y;
            if (x2 === void 0 || y2 === void 0) {
              return;
            }
            var node3 = mirror2.getNode(e2.id);
            var selector2;
            if (node3) {
              try {
                selector2 = finder(node3, performanceOptions);
              } catch (err) {
                _this.logger.debug("error resolving selector from finder");
              }
            }
            var pageUrl = getPageUrl(location2.href, ugcFilterRules);
            var event = {
              x: x2 + _this.scrollWatcher.currentScrollX,
              y: y2 + _this.scrollWatcher.currentScrollY,
              selector: selector2,
              viewportHeight: innerHeight,
              viewportWidth: innerWidth,
              pageUrl,
              timestamp: Date.now(),
              type: "click"
            };
            var deviceId = deviceIdFn();
            if (deviceId) {
              eventsManager.addEvent({ sessionId, event: { type: "interaction", data: JSON.stringify(event) }, deviceId });
            }
          };
        };
        this.logger = logger;
        this.scrollWatcher = scrollWatcher;
      }
      return ClickHandler2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/utils/rrweb.js
  init_esm();
  function getViewportHeight() {
    var globalScope3 = getGlobalScope();
    return (globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.innerHeight) || document.documentElement && document.documentElement.clientHeight || 0;
  }
  function getViewportWidth() {
    var globalScope3 = getGlobalScope();
    return (globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.innerWidth) || document.documentElement && document.documentElement.clientWidth || 0;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/beacon-transport.js
  init_esm();
  var BeaconTransport = (
    /** @class */
    (function() {
      function BeaconTransport2(context, config2) {
        var globalScope3 = getGlobalScope();
        if (globalScope3 && globalScope3.navigator && typeof globalScope3.navigator.sendBeacon === "function") {
          this.sendBeacon = function(pageUrl, payload) {
            try {
              if (globalScope3.navigator.sendBeacon(pageUrl, JSON.stringify(payload))) {
                return true;
              }
            } catch (e2) {
            }
            return false;
          };
        } else {
          this.sendBeacon = function() {
            return false;
          };
        }
        this.sendXhr = function(pageUrl, payload) {
          var xhr = new XMLHttpRequest();
          xhr.open("POST", pageUrl, true);
          xhr.setRequestHeader("Accept", "*/*");
          xhr.send(JSON.stringify(payload));
          return true;
        };
        this.basePageUrl = getServerUrl2(config2.serverZone, config2.trackServerUrl);
        this.apiKey = config2.apiKey;
        this.context = context;
        this.handleSendEvents = config2.handleSendEvents;
        this.loggerProvider = config2.loggerProvider;
      }
      BeaconTransport2.prototype.send = function(deviceId, payload) {
        var _this = this;
        var _a4;
        var _b = this.context, sessionId = _b.sessionId, type = _b.type;
        if (this.handleSendEvents) {
          var params = new URLSearchParams({
            device_id: deviceId,
            session_id: String(sessionId),
            type: String(type)
          });
          var url = "".concat(this.basePageUrl, "?").concat(params.toString());
          var headers = {
            "Content-Type": "application/json",
            Accept: "*/*",
            Authorization: "Bearer ".concat(this.apiKey)
          };
          try {
            void this.handleSendEvents({
              url,
              method: "POST",
              headers,
              body: JSON.stringify(payload),
              keepalive: true
            }).catch(function(e2) {
              var _a5;
              (_a5 = _this.loggerProvider) === null || _a5 === void 0 ? void 0 : _a5.warn("Custom transport failed to send session replay interaction beacon:", e2);
            });
          } catch (e2) {
            (_a4 = this.loggerProvider) === null || _a4 === void 0 ? void 0 : _a4.warn("Custom transport threw while sending session replay interaction beacon:", e2);
          }
          return;
        }
        var urlParams = new URLSearchParams({
          device_id: deviceId,
          session_id: String(sessionId),
          type: String(type),
          api_key: this.apiKey
        });
        var pageUrl = "".concat(this.basePageUrl, "?").concat(urlParams.toString());
        this.sendBeacon(pageUrl, payload) || this.sendXhr(pageUrl, payload);
      };
      return BeaconTransport2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/hooks/scroll.js
  init_esm();
  var ScrollWatcher = (
    /** @class */
    (function() {
      function ScrollWatcher2(transport, config2) {
        var _this = this;
        this.timestamp = Date.now();
        this.hook = function(e2) {
          _this.update(e2);
        };
        this.send = function(deviceIdFn) {
          return function(_2) {
            var _a4, _b, _c, _d;
            var deviceId = deviceIdFn();
            var globalScope3 = getGlobalScope();
            if (globalScope3 && deviceId) {
              var scrollX_1 = (_a4 = globalScope3.scrollX) !== null && _a4 !== void 0 ? _a4 : 0;
              var scrollY_1 = (_b = globalScope3.scrollY) !== null && _b !== void 0 ? _b : 0;
              if (scrollX_1 > 0 || scrollY_1 > 0) {
                _this.update({ id: 1, x: scrollX_1, y: scrollY_1 });
              }
              _this.transport.send(deviceId, {
                version: 1,
                events: [
                  {
                    maxScrollX: _this._maxScrollX,
                    maxScrollY: _this._maxScrollY,
                    maxScrollWidth: _this._maxScrollWidth,
                    maxScrollHeight: _this._maxScrollHeight,
                    viewportHeight: getViewportHeight(),
                    viewportWidth: getViewportWidth(),
                    pageUrl: getPageUrl(globalScope3.location.href, (_d = (_c = _this.config.interactionConfig) === null || _c === void 0 ? void 0 : _c.ugcFilterRules) !== null && _d !== void 0 ? _d : []),
                    timestamp: _this.timestamp,
                    type: "scroll"
                  }
                ]
              });
            }
          };
        };
        this._maxScrollX = 0;
        this._maxScrollY = 0;
        this._currentScrollX = 0;
        this._currentScrollY = 0;
        this._maxScrollWidth = getViewportWidth();
        this._maxScrollHeight = getViewportHeight();
        this.config = config2;
        this.transport = transport;
      }
      ScrollWatcher2.default = function(context, config2) {
        return new ScrollWatcher2(new BeaconTransport(context, config2), config2);
      };
      Object.defineProperty(ScrollWatcher2.prototype, "maxScrollX", {
        get: function() {
          return this._maxScrollX;
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(ScrollWatcher2.prototype, "maxScrollY", {
        get: function() {
          return this._maxScrollY;
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(ScrollWatcher2.prototype, "maxScrollWidth", {
        get: function() {
          return this._maxScrollWidth;
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(ScrollWatcher2.prototype, "maxScrollHeight", {
        get: function() {
          return this._maxScrollHeight;
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(ScrollWatcher2.prototype, "currentScrollX", {
        get: function() {
          return this._currentScrollX;
        },
        enumerable: false,
        configurable: true
      });
      Object.defineProperty(ScrollWatcher2.prototype, "currentScrollY", {
        get: function() {
          return this._currentScrollY;
        },
        enumerable: false,
        configurable: true
      });
      ScrollWatcher2.prototype.update = function(e2) {
        var now = Date.now();
        this._currentScrollX = e2.x;
        this._currentScrollY = e2.y;
        if (e2.x > this._maxScrollX) {
          var width = getViewportWidth();
          this._maxScrollX = e2.x;
          var maxScrollWidth = e2.x + width;
          if (maxScrollWidth > this._maxScrollWidth) {
            this._maxScrollWidth = maxScrollWidth;
          }
          this.timestamp = now;
        }
        if (e2.y > this._maxScrollY) {
          var height = getViewportHeight();
          this._maxScrollY = e2.y;
          var maxScrollHeight = e2.y + height;
          if (maxScrollHeight > this._maxScrollHeight) {
            this._maxScrollHeight = maxScrollHeight;
          }
          this.timestamp = now;
        }
      };
      return ScrollWatcher2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/identifiers.js
  var SessionIdentifiers = (
    /** @class */
    /* @__PURE__ */ (function() {
      function SessionIdentifiers2(_a4) {
        var sessionId = _a4.sessionId, deviceId = _a4.deviceId;
        this.deviceId = deviceId;
        this.sessionId = sessionId;
        if (sessionId && deviceId) {
          this.sessionReplayId = generateSessionReplayId(sessionId, deviceId);
        }
      }
      return SessionIdentifiers2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/replay-start-time-store.js
  init_tslib_es6();
  init_esm();
  var KEY_PREFIX = "AMP_SR_START_";
  var APIKEY_FINGERPRINT_LEN = 10;
  var REPLAY_START_TIME_TTL_MS = 24 * 60 * 60 * 1e3;
  var buildKeyPrefix = function(apiKey2) {
    return "".concat(KEY_PREFIX).concat(apiKey2.substring(0, APIKEY_FINGERPRINT_LEN), "_");
  };
  var buildKey = function(apiKey2, sessionId) {
    return "".concat(buildKeyPrefix(apiKey2)).concat(sessionId);
  };
  var getLocalStorage = function() {
    try {
      var scope = getGlobalScope();
      return scope === null || scope === void 0 ? void 0 : scope.localStorage;
    } catch (_a4) {
      return void 0;
    }
  };
  var getOrInitReplayStartTime = function(apiKey2, sessionId, now, logger) {
    var storage = getLocalStorage();
    if (!storage)
      return void 0;
    var key = buildKey(apiKey2, sessionId);
    try {
      var raw = storage.getItem(key);
      if (raw !== null) {
        var parsed = Number(raw);
        if (Number.isFinite(parsed) && parsed > 0 && parsed <= now && now - parsed < REPLAY_START_TIME_TTL_MS) {
          return parsed;
        }
      }
      storage.setItem(key, String(now));
      return now;
    } catch (e2) {
      logger === null || logger === void 0 ? void 0 : logger.debug("Failed to read/write replay start time from storage: ".concat(String(e2)));
      return void 0;
    }
  };
  var setReplayStartTime = function(apiKey2, sessionId, startTime, logger) {
    var storage = getLocalStorage();
    if (!storage)
      return;
    try {
      storage.setItem(buildKey(apiKey2, sessionId), String(startTime));
    } catch (e2) {
      logger === null || logger === void 0 ? void 0 : logger.debug("Failed to write replay start time to storage: ".concat(String(e2)));
    }
  };
  var removeReplayStartTime = function(apiKey2, sessionId, logger) {
    var storage = getLocalStorage();
    if (!storage)
      return;
    try {
      storage.removeItem(buildKey(apiKey2, sessionId));
    } catch (e2) {
      logger === null || logger === void 0 ? void 0 : logger.debug("Failed to remove replay start time from storage: ".concat(String(e2)));
    }
  };
  var pruneStaleReplayStartTimes = function(apiKey2, now, logger) {
    var e_1, _a4;
    var storage = getLocalStorage();
    if (!storage)
      return;
    var prefix = buildKeyPrefix(apiKey2);
    try {
      var stale = [];
      for (var i2 = 0; i2 < storage.length; i2++) {
        var key = storage.key(i2);
        if (!key || !key.startsWith(prefix))
          continue;
        var raw = storage.getItem(key);
        if (raw === null)
          continue;
        var parsed = Number(raw);
        if (!Number.isFinite(parsed) || parsed <= 0 || now - parsed >= REPLAY_START_TIME_TTL_MS) {
          stale.push(key);
        }
      }
      try {
        for (var stale_1 = __values(stale), stale_1_1 = stale_1.next(); !stale_1_1.done; stale_1_1 = stale_1.next()) {
          var key = stale_1_1.value;
          storage.removeItem(key);
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (stale_1_1 && !stale_1_1.done && (_a4 = stale_1.return)) _a4.call(stale_1);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    } catch (e2) {
      logger === null || logger === void 0 ? void 0 : logger.debug("Failed to prune stale replay start times: ".concat(String(e2)));
    }
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/targeting/targeting-manager.js
  init_tslib_es6();

  // node_modules/@amplitude/session-replay-browser/lib/esm/targeting/targeting-idb-store.js
  init_tslib_es6();
  init_build();
  var MAX_IDB_STORAGE_LENGTH2 = 1e3 * 60 * 60 * 24 * 2;
  var TargetingIDBStore = (
    /** @class */
    /* @__PURE__ */ (function() {
      function TargetingIDBStore3() {
        var _this = this;
        this.dbs = {};
        this.createStore = function(dbName) {
          return __awaiter(_this, void 0, void 0, function() {
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  return [4, openDB(dbName, 1, {
                    upgrade: function(db) {
                      if (!db.objectStoreNames.contains("sessionTargetingMatch")) {
                        db.createObjectStore("sessionTargetingMatch", {
                          keyPath: "sessionId"
                        });
                      }
                    }
                  })];
                case 1:
                  return [2, _a4.sent()];
              }
            });
          });
        };
        this.openOrCreateDB = function(apiKey2) {
          return __awaiter(_this, void 0, void 0, function() {
            var dbName, db;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  if (this.dbs && this.dbs[apiKey2]) {
                    return [2, this.dbs[apiKey2]];
                  }
                  dbName = "".concat(apiKey2.substring(0, 10), "_amp_session_replay_targeting");
                  return [4, this.createStore(dbName)];
                case 1:
                  db = _a4.sent();
                  this.dbs[apiKey2] = db;
                  return [2, db];
              }
            });
          });
        };
        this.getTargetingMatchForSession = function(_a4) {
          var loggerProvider = _a4.loggerProvider, apiKey2 = _a4.apiKey, sessionId = _a4.sessionId;
          return __awaiter(_this, void 0, void 0, function() {
            var db, sessionIdStr, targetingMatchForSession, e_1;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  _b.trys.push([0, 3, , 4]);
                  return [4, this.openOrCreateDB(apiKey2)];
                case 1:
                  db = _b.sent();
                  sessionIdStr = String(sessionId);
                  return [4, db.get("sessionTargetingMatch", sessionIdStr)];
                case 2:
                  targetingMatchForSession = _b.sent();
                  return [2, targetingMatchForSession === null || targetingMatchForSession === void 0 ? void 0 : targetingMatchForSession.targetingMatch];
                case 3:
                  e_1 = _b.sent();
                  logIdbError(loggerProvider, "Failed to get targeting match for session id ".concat(sessionId, ": ").concat(e_1), e_1);
                  return [3, 4];
                case 4:
                  return [2, void 0];
              }
            });
          });
        };
        this.storeTargetingMatchForSession = function(_a4) {
          var loggerProvider = _a4.loggerProvider, apiKey2 = _a4.apiKey, sessionId = _a4.sessionId, targetingMatch = _a4.targetingMatch;
          return __awaiter(_this, void 0, void 0, function() {
            var db, sessionIdStr, targetingMatchForSession, e_2;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  _b.trys.push([0, 3, , 4]);
                  return [4, this.openOrCreateDB(apiKey2)];
                case 1:
                  db = _b.sent();
                  sessionIdStr = String(sessionId);
                  return [4, db.put("sessionTargetingMatch", {
                    targetingMatch,
                    sessionId: sessionIdStr,
                    lastUpdated: Date.now()
                  })];
                case 2:
                  targetingMatchForSession = _b.sent();
                  return [2, targetingMatchForSession];
                case 3:
                  e_2 = _b.sent();
                  logIdbError(loggerProvider, "Failed to store targeting match for session id ".concat(sessionId, ": ").concat(e_2), e_2);
                  return [3, 4];
                case 4:
                  return [2, void 0];
              }
            });
          });
        };
        this.clearStoreOfOldSessions = function(_a4) {
          var loggerProvider = _a4.loggerProvider, apiKey2 = _a4.apiKey, currentSessionId = _a4.currentSessionId;
          return __awaiter(_this, void 0, void 0, function() {
            var db, currentSessionIdStr, tx, allTargetingMatchObjs, i2, targetingMatchObj, amountOfTimeSinceSession, e_3;
            return __generator(this, function(_b) {
              switch (_b.label) {
                case 0:
                  _b.trys.push([0, 8, , 9]);
                  return [4, this.openOrCreateDB(apiKey2)];
                case 1:
                  db = _b.sent();
                  currentSessionIdStr = String(currentSessionId);
                  tx = db.transaction("sessionTargetingMatch", "readwrite");
                  return [4, tx.store.getAll()];
                case 2:
                  allTargetingMatchObjs = _b.sent();
                  i2 = 0;
                  _b.label = 3;
                case 3:
                  if (!(i2 < allTargetingMatchObjs.length)) return [3, 6];
                  targetingMatchObj = allTargetingMatchObjs[i2];
                  amountOfTimeSinceSession = Date.now() - targetingMatchObj.lastUpdated;
                  if (!(targetingMatchObj.sessionId !== currentSessionIdStr && amountOfTimeSinceSession > MAX_IDB_STORAGE_LENGTH2)) return [3, 5];
                  return [4, tx.store.delete(targetingMatchObj.sessionId)];
                case 4:
                  _b.sent();
                  _b.label = 5;
                case 5:
                  i2++;
                  return [3, 3];
                case 6:
                  return [4, tx.done];
                case 7:
                  _b.sent();
                  return [3, 9];
                case 8:
                  e_3 = _b.sent();
                  logIdbError(loggerProvider, "Failed to clear old targeting matches for sessions: ".concat(e_3), e_3);
                  return [3, 9];
                case 9:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
      }
      return TargetingIDBStore3;
    })()
  );
  var targetingIDBStore = new TargetingIDBStore();

  // node_modules/@amplitude/session-replay-browser/lib/esm/targeting/targeting-manager.js
  var evaluateTargetingAndStore = function(_a4) {
    var sessionId = _a4.sessionId, targetingConfig = _a4.targetingConfig, loggerProvider = _a4.loggerProvider, apiKey2 = _a4.apiKey, targetingParams = _a4.targetingParams, _b = _a4.urlChange, urlChange = _b === void 0 ? false : _b;
    return __awaiter(void 0, void 0, void 0, function() {
      var idbTargetingMatch, sessionTargetingMatch, evaluateTargetingPackage, params, targetingResult, err_1, knownError;
      return __generator(this, function(_c) {
        switch (_c.label) {
          case 0:
            return [4, targetingIDBStore.clearStoreOfOldSessions({
              loggerProvider,
              apiKey: apiKey2,
              currentSessionId: sessionId
            })];
          case 1:
            _c.sent();
            return [4, targetingIDBStore.getTargetingMatchForSession({
              loggerProvider,
              apiKey: apiKey2,
              sessionId
            })];
          case 2:
            idbTargetingMatch = _c.sent();
            if (idbTargetingMatch === true && !urlChange) {
              return [2, true];
            }
            sessionTargetingMatch = true;
            _c.label = 3;
          case 3:
            _c.trys.push([3, 6, , 7]);
            return [4, Promise.resolve().then(() => (init_esm2(), esm_exports))];
          case 4:
            evaluateTargetingPackage = _c.sent().evaluateTargeting;
            params = __assign(__assign({}, targetingParams), { flag: targetingConfig, sessionId: typeof sessionId === "string" ? parseInt(sessionId, 10) : sessionId, apiKey: apiKey2, loggerProvider });
            return [4, evaluateTargetingPackage(params)];
          case 5:
            targetingResult = _c.sent();
            if (targetingResult && targetingResult.sr_targeting_config) {
              sessionTargetingMatch = targetingResult.sr_targeting_config.key === "on";
            }
            void targetingIDBStore.storeTargetingMatchForSession({
              loggerProvider,
              apiKey: apiKey2,
              sessionId,
              targetingMatch: sessionTargetingMatch
            });
            return [3, 7];
          case 6:
            err_1 = _c.sent();
            knownError = err_1;
            loggerProvider.warn(knownError.message);
            return [3, 7];
          case 7:
            return [2, sessionTargetingMatch];
        }
      });
    });
  };

  // node_modules/@amplitude/session-replay-browser/lib/esm/sampling.js
  var PRIME32_1 = 2654435761;
  var PRIME32_2 = 2246822519;
  var PRIME32_3 = 3266489917;
  var PRIME32_4 = 668265263;
  var PRIME32_5 = 374761393;
  function rotl32(x2, r2) {
    return (x2 << r2 | x2 >>> 32 - r2) >>> 0;
  }
  function round(acc, input3) {
    acc = acc + Math.imul(input3, PRIME32_2) >>> 0;
    acc = rotl32(acc, 13);
    acc = Math.imul(acc, PRIME32_1) >>> 0;
    return acc;
  }
  function readU32(bytes, offset) {
    return (bytes[offset] | bytes[offset + 1] << 8 | bytes[offset + 2] << 16 | bytes[offset + 3] << 24) >>> 0;
  }
  function toUTF8Bytes(str) {
    var bytes = [];
    for (var i2 = 0; i2 < str.length; i2++) {
      var c2 = str.charCodeAt(i2);
      if (c2 >= 55296 && c2 <= 56319 && i2 + 1 < str.length) {
        var next = str.charCodeAt(i2 + 1);
        if (next >= 56320 && next <= 57343) {
          c2 = (c2 - 55296 << 10) + (next - 56320) + 65536;
          i2++;
        }
      }
      if (c2 < 128) {
        bytes.push(c2);
      } else if (c2 < 2048) {
        bytes.push(192 | c2 >> 6, 128 | c2 & 63);
      } else if (c2 < 65536) {
        bytes.push(224 | c2 >> 12, 128 | c2 >> 6 & 63, 128 | c2 & 63);
      } else {
        bytes.push(240 | c2 >> 18, 128 | c2 >> 12 & 63, 128 | c2 >> 6 & 63, 128 | c2 & 63);
      }
    }
    return new Uint8Array(bytes);
  }
  function xxHash32(input3, seed) {
    if (seed === void 0) {
      seed = 0;
    }
    var bytes = toUTF8Bytes(input3);
    var len = bytes.length;
    var h32;
    var offset = 0;
    if (len >= 16) {
      var v1 = seed + PRIME32_1 + PRIME32_2 >>> 0;
      var v2 = seed + PRIME32_2 >>> 0;
      var v3 = seed >>> 0;
      var v4 = seed - PRIME32_1 >>> 0;
      while (offset <= len - 16) {
        v1 = round(v1, readU32(bytes, offset));
        offset += 4;
        v2 = round(v2, readU32(bytes, offset));
        offset += 4;
        v3 = round(v3, readU32(bytes, offset));
        offset += 4;
        v4 = round(v4, readU32(bytes, offset));
        offset += 4;
      }
      h32 = rotl32(v1, 1) + rotl32(v2, 7) + rotl32(v3, 12) + rotl32(v4, 18) >>> 0;
    } else {
      h32 = seed + PRIME32_5 >>> 0;
    }
    h32 = h32 + len >>> 0;
    while (offset <= len - 4) {
      h32 = h32 + Math.imul(readU32(bytes, offset), PRIME32_3) >>> 0;
      h32 = Math.imul(rotl32(h32, 17), PRIME32_4) >>> 0;
      offset += 4;
    }
    while (offset < len) {
      h32 = h32 + Math.imul(bytes[offset], PRIME32_5) >>> 0;
      h32 = Math.imul(rotl32(h32, 11), PRIME32_1) >>> 0;
      offset++;
    }
    h32 ^= h32 >>> 15;
    h32 = Math.imul(h32, PRIME32_2) >>> 0;
    h32 ^= h32 >>> 13;
    h32 = Math.imul(h32, PRIME32_3) >>> 0;
    h32 ^= h32 >>> 16;
    return h32 >>> 0;
  }
  function isSessionInSample(sessionId, sampleRate) {
    var hash = xxHash32(sessionId.toString());
    var mod = hash % 1e6;
    return mod / 1e6 < sampleRate;
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/plugins/url-tracking-plugin.js
  init_tslib_es6();
  var PATCH_MARKER = "__amplitude_url_tracking_patched__";
  var urlChangeSubscriptionsByScope = /* @__PURE__ */ new WeakMap();
  function subscribeToUrlChanges(globalScope3, onUrlChange, options) {
    if (options === void 0) {
      options = {};
    }
    if (!(globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location)) {
      return function() {
        return;
      };
    }
    var _a4 = options.enablePolling, enablePolling = _a4 === void 0 ? false : _a4, _b = options.pollingInterval, pollingInterval = _b === void 0 ? DEFAULT_URL_CHANGE_POLLING_INTERVAL : _b;
    if (enablePolling) {
      var getHref_1 = function() {
        var _a5;
        return (_a5 = globalScope3.location.href) !== null && _a5 !== void 0 ? _a5 : "";
      };
      var lastHref_1 = getHref_1();
      var id_1 = globalScope3.setInterval(function() {
        var href = getHref_1();
        if (href === lastHref_1) {
          return;
        }
        lastHref_1 = href;
        onUrlChange(href);
      }, pollingInterval);
      return function() {
        if (id_1 != null) {
          globalScope3.clearInterval(id_1);
        }
      };
    }
    var subscriptionState = urlChangeSubscriptionsByScope.get(globalScope3);
    if (!subscriptionState) {
      var lastHref_2 = void 0;
      var callbacks_1 = /* @__PURE__ */ new Set();
      var getHref_2 = function() {
        var _a5;
        return (_a5 = globalScope3.location.href) !== null && _a5 !== void 0 ? _a5 : "";
      };
      var notify_1 = function() {
        var href = getHref_2();
        if (lastHref_2 !== void 0 && href === lastHref_2)
          return;
        lastHref_2 = href;
        callbacks_1.forEach(function(c2) {
          return c2(href);
        });
      };
      var createHistoryMethodPatch = function(originalMethod) {
        var patchedMethod = function() {
          var args = [];
          for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
          }
          var result3 = originalMethod.apply(this, args);
          notify_1();
          return result3;
        };
        patchedMethod[PATCH_MARKER] = true;
        return patchedMethod;
      };
      var history_1 = globalScope3.history;
      if (history_1 === null || history_1 === void 0 ? void 0 : history_1.pushState) {
        var pushState = Reflect.get(history_1, "pushState");
        if (!pushState[PATCH_MARKER]) {
          history_1.pushState = createHistoryMethodPatch(pushState);
        }
      }
      if (history_1 === null || history_1 === void 0 ? void 0 : history_1.replaceState) {
        var replaceState = Reflect.get(history_1, "replaceState");
        if (!replaceState[PATCH_MARKER]) {
          history_1.replaceState = createHistoryMethodPatch(replaceState);
        }
      }
      subscriptionState = {
        callbacks: callbacks_1,
        notify: notify_1,
        onPopStateOrHashChange: function() {
          return notify_1();
        },
        listenersAttached: false
      };
      urlChangeSubscriptionsByScope.set(globalScope3, subscriptionState);
    }
    var state = subscriptionState;
    if (!state.listenersAttached) {
      globalScope3.addEventListener("popstate", state.onPopStateOrHashChange);
      globalScope3.addEventListener("hashchange", state.onPopStateOrHashChange);
      state.listenersAttached = true;
    }
    state.callbacks.add(onUrlChange);
    return function() {
      state.callbacks.delete(onUrlChange);
      if (state.callbacks.size === 0) {
        if (state.listenersAttached) {
          globalScope3.removeEventListener("popstate", state.onPopStateOrHashChange);
          globalScope3.removeEventListener("hashchange", state.onPopStateOrHashChange);
          state.listenersAttached = false;
        }
      }
    };
  }
  function createUrlTrackingPlugin(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "amplitude/url-tracking@1",
      observer: function(cb, globalScope3, pluginOptions) {
        var _a4, _b, _c;
        var config2 = __assign(__assign({}, options), pluginOptions);
        var ugcFilterRules = config2.ugcFilterRules || [];
        var enablePolling = (_a4 = config2.enablePolling) !== null && _a4 !== void 0 ? _a4 : false;
        var pollingInterval = (_b = config2.pollingInterval) !== null && _b !== void 0 ? _b : DEFAULT_URL_CHANGE_POLLING_INTERVAL;
        var captureDocumentTitle = (_c = config2.captureDocumentTitle) !== null && _c !== void 0 ? _c : false;
        if (!globalScope3) {
          return function() {
          };
        }
        var lastTrackedUrl = void 0;
        var getCurrentUrl2 = function() {
          if (!globalScope3.location)
            return "";
          return globalScope3.location.href || "";
        };
        var createUrlChangeEvent = function() {
          var innerHeight = globalScope3.innerHeight, innerWidth = globalScope3.innerWidth, document2 = globalScope3.document;
          var currentUrl = getCurrentUrl2();
          var currentTitle = "";
          if (captureDocumentTitle) {
            currentTitle = (document2 === null || document2 === void 0 ? void 0 : document2.title) || "";
          }
          var filteredUrl = ugcFilterRules.length > 0 ? getPageUrl(currentUrl, ugcFilterRules) : currentUrl;
          return {
            href: filteredUrl,
            title: currentTitle,
            viewportHeight: innerHeight,
            viewportWidth: innerWidth,
            type: "url-change-event"
          };
        };
        var emitUrlChange = function() {
          var currentUrl = getCurrentUrl2();
          if (lastTrackedUrl === void 0 || currentUrl !== lastTrackedUrl) {
            lastTrackedUrl = currentUrl;
            var event_1 = createUrlChangeEvent();
            cb(event_1);
          }
        };
        var unsubscribe = subscribeToUrlChanges(globalScope3, function() {
          return emitUrlChange();
        }, enablePolling ? { enablePolling: true, pollingInterval } : {});
        emitUrlChange();
        return function() {
          return unsubscribe();
        };
      },
      options
    };
  }
  var urlTrackingPlugin = createUrlTrackingPlugin();

  // node_modules/@amplitude/session-replay-browser/lib/esm/cross-origin-iframes.js
  init_tslib_es6();
  init_esm();
  function isInIframe() {
    try {
      var globalScope3 = getGlobalScope();
      if (!globalScope3) {
        return false;
      }
      return globalScope3.parent !== globalScope3;
    } catch (_a4) {
      return true;
    }
  }
  var CrossOriginIframeCoordinator = (
    /** @class */
    (function() {
      function CrossOriginIframeCoordinator2() {
        this.pendingLoadListeners = /* @__PURE__ */ new Map();
      }
      CrossOriginIframeCoordinator2.prototype.start = function() {
        var _this = this;
        var _a4;
        (_a4 = this.mutationObserver) === null || _a4 === void 0 ? void 0 : _a4.disconnect();
        var globalScope3 = getGlobalScope();
        if (!globalScope3) {
          return;
        }
        this.sendToAllIframes({ type: CROSS_ORIGIN_IFRAME_MESSAGE_TYPE, action: "start" });
        this.mutationObserver = new MutationObserver(function(mutations) {
          var e_1, _a5, e_2, _b, e_3, _c;
          try {
            for (var mutations_1 = __values(mutations), mutations_1_1 = mutations_1.next(); !mutations_1_1.done; mutations_1_1 = mutations_1.next()) {
              var mutation = mutations_1_1.value;
              try {
                for (var _d = (e_2 = void 0, __values(Array.from(mutation.addedNodes))), _e = _d.next(); !_e.done; _e = _d.next()) {
                  var node3 = _e.value;
                  if (node3 instanceof HTMLIFrameElement) {
                    _this.sendToIframeAfterLoad(node3);
                  } else if (node3 instanceof Element) {
                    node3.querySelectorAll("iframe").forEach(function(iframe) {
                      _this.sendToIframeAfterLoad(iframe);
                    });
                  }
                }
              } catch (e_2_1) {
                e_2 = { error: e_2_1 };
              } finally {
                try {
                  if (_e && !_e.done && (_b = _d.return)) _b.call(_d);
                } finally {
                  if (e_2) throw e_2.error;
                }
              }
              try {
                for (var _f = (e_3 = void 0, __values(Array.from(mutation.removedNodes))), _g = _f.next(); !_g.done; _g = _f.next()) {
                  var node3 = _g.value;
                  if (node3 instanceof HTMLIFrameElement) {
                    var listener = _this.pendingLoadListeners.get(node3);
                    if (listener) {
                      node3.removeEventListener("load", listener);
                      _this.pendingLoadListeners.delete(node3);
                    }
                  }
                }
              } catch (e_3_1) {
                e_3 = { error: e_3_1 };
              } finally {
                try {
                  if (_g && !_g.done && (_c = _f.return)) _c.call(_f);
                } finally {
                  if (e_3) throw e_3.error;
                }
              }
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (mutations_1_1 && !mutations_1_1.done && (_a5 = mutations_1.return)) _a5.call(mutations_1);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
        });
        this.mutationObserver.observe(document.documentElement, { childList: true, subtree: true });
      };
      CrossOriginIframeCoordinator2.prototype.stop = function() {
        var _a4;
        (_a4 = this.mutationObserver) === null || _a4 === void 0 ? void 0 : _a4.disconnect();
        this.mutationObserver = void 0;
        this.pendingLoadListeners.forEach(function(listener, iframe) {
          iframe.removeEventListener("load", listener);
        });
        this.pendingLoadListeners.clear();
        this.sendToAllIframes({ type: CROSS_ORIGIN_IFRAME_MESSAGE_TYPE, action: "stop" });
      };
      CrossOriginIframeCoordinator2.prototype.sendToIframeAfterLoad = function(iframe) {
        var _this = this;
        var sendStart = function() {
          _this.pendingLoadListeners.delete(iframe);
          _this.sendToIframe(iframe, { type: CROSS_ORIGIN_IFRAME_MESSAGE_TYPE, action: "start" });
        };
        this.pendingLoadListeners.set(iframe, sendStart);
        iframe.addEventListener("load", sendStart, { once: true });
      };
      CrossOriginIframeCoordinator2.prototype.sendToAllIframes = function(message) {
        var _this = this;
        var iframes = document.querySelectorAll("iframe");
        iframes.forEach(function(iframe) {
          return _this.sendToIframe(iframe, message);
        });
      };
      CrossOriginIframeCoordinator2.prototype.sendToIframe = function(iframe, message) {
        var _a4;
        try {
          (_a4 = iframe.contentWindow) === null || _a4 === void 0 ? void 0 : _a4.postMessage(message, "*");
        } catch (_b) {
        }
      };
      return CrossOriginIframeCoordinator2;
    })()
  );
  function listenForParentSignals(callbacks) {
    var globalScope3 = getGlobalScope();
    if (!globalScope3) {
      return function() {
        return void 0;
      };
    }
    var parentFrame = globalScope3.parent;
    function handler(event) {
      if (event.source !== parentFrame) {
        return;
      }
      var data = event.data;
      if ((data === null || data === void 0 ? void 0 : data.type) !== CROSS_ORIGIN_IFRAME_MESSAGE_TYPE) {
        return;
      }
      if (data.action === "start") {
        callbacks.onStart();
      } else if (data.action === "stop") {
        callbacks.onStop();
      }
    }
    globalScope3.addEventListener("message", handler);
    return function() {
      return globalScope3.removeEventListener("message", handler);
    };
  }

  // node_modules/@amplitude/session-replay-browser/lib/esm/session-replay.js
  var SessionReplay = (
    /** @class */
    (function() {
      function SessionReplay2() {
        var _this = this;
        this.name = "@amplitude/session-replay-browser";
        this.recordCancelCallback = null;
        this.eventCount = 0;
        this.sessionTargetingMatch = false;
        this.pageLeaveFns = [];
        this.suppressedSendCount = 0;
        this.hasEmittedGateDecision = false;
        this.recordFunction = null;
        this.recordEventsInFlight = false;
        this.recordingEnabled = true;
        this.recordingGeneration = 0;
        this.startChildRecordingOnSetup = false;
        this.pendingEmitEvents = [];
        this.currentPageUrl = "";
        this.recordEventsPendingShouldLogMetadata = null;
        this.urlChangeCleanup = null;
        this.crossOriginIframeCoordinator = null;
        this.crossOriginParentSignalCleanup = null;
        this.latestUrlChangeTargetingEvaluationId = 0;
        this.teardownEventListeners = function(teardown) {
          var globalScope3 = getGlobalScope();
          if (globalScope3) {
            globalScope3.removeEventListener("blur", _this.blurListener);
            globalScope3.removeEventListener("focus", _this.focusListener);
            !teardown && globalScope3.addEventListener("blur", _this.blurListener);
            !teardown && globalScope3.addEventListener("focus", _this.focusListener);
            if (globalScope3.self && "onpagehide" in globalScope3.self) {
              globalScope3.removeEventListener("pagehide", _this.pageLeaveListener);
              !teardown && globalScope3.addEventListener("pagehide", _this.pageLeaveListener);
            } else {
              globalScope3.removeEventListener("beforeunload", _this.pageLeaveListener);
              !teardown && globalScope3.addEventListener("beforeunload", _this.pageLeaveListener);
            }
          }
        };
        this.blurListener = function() {
          _this.sendEvents();
        };
        this.focusListener = function() {
          var _a4;
          if (!_this.recordingEnabled) {
            return;
          }
          if (_this.recordCancelCallback && _this.recordFunction) {
            if (((_a4 = _this.config) === null || _a4 === void 0 ? void 0 : _a4.captureFullSnapshotOnFocus) === false) {
              return;
            }
            try {
              _this.recordFunction.takeFullSnapshot(true);
            } catch (error) {
              _this.loggerProvider.warn("Failed to take full snapshot on focus:", error);
            }
          } else if (!_this.recordEventsInFlight) {
            void _this.recordEvents(false);
          }
        };
        this.pageLeaveListener = function(e2) {
          var _a4;
          (_a4 = _this.eventCompressor) === null || _a4 === void 0 ? void 0 : _a4.flushQueue();
          _this.sendEvents();
          _this.pageLeaveFns.forEach(function(fn) {
            fn(e2);
          });
        };
        this.evaluateTargetingAndCapture = function(targetingParams, isInit, forceRestart, forceTargetingReevaluation) {
          if (isInit === void 0) {
            isInit = false;
          }
          if (forceRestart === void 0) {
            forceRestart = false;
          }
          if (forceTargetingReevaluation === void 0) {
            forceTargetingReevaluation = false;
          }
          return __awaiter(_this, void 0, void 0, function() {
            var targetingConfig, shouldReEvaluate, urlChangeEvaluationId, eventForTargeting, pageUrl, pageForTargeting, targetingMatch;
            var _a4, _b, _c, _d, _e, _f;
            return __generator(this, function(_g) {
              switch (_g.label) {
                case 0:
                  if (!this.identifiers || !this.identifiers.sessionId || !this.config) {
                    if (this.identifiers && !this.identifiers.sessionId) {
                      this.loggerProvider.log("Session ID has not been set yet, cannot evaluate targeting for Session Replay.");
                    } else {
                      this.loggerProvider.warn("Session replay init has not been called, cannot evaluate targeting.");
                    }
                    return [
                      2
                      /*return*/
                    ];
                  }
                  if (!this.config.targetingConfig) {
                    if (isInit) {
                      this.loggerProvider.log("Targeting config has not been set yet, cannot evaluate targeting.");
                    } else {
                      this.loggerProvider.log("No targeting config set, skipping initialization/recording for event.");
                      return [
                        2
                        /*return*/
                      ];
                    }
                  }
                  this.lastTargetingParams = targetingParams;
                  targetingConfig = this.config.targetingConfig;
                  shouldReEvaluate = targetingConfig && !this.sessionTargetingMatch;
                  if (!shouldReEvaluate) return [3, 2];
                  urlChangeEvaluationId = forceTargetingReevaluation ? this.latestUrlChangeTargetingEvaluationId : void 0;
                  eventForTargeting = targetingParams.event;
                  if (eventForTargeting && Object.values(SpecialEventType).includes(eventForTargeting.event_type)) {
                    eventForTargeting = void 0;
                  }
                  pageUrl = (_e = (_b = (_a4 = targetingParams.page) === null || _a4 === void 0 ? void 0 : _a4.url) !== null && _b !== void 0 ? _b : (_d = (_c = getGlobalScope()) === null || _c === void 0 ? void 0 : _c.location) === null || _d === void 0 ? void 0 : _d.href) !== null && _e !== void 0 ? _e : "";
                  pageForTargeting = (_f = targetingParams.page) !== null && _f !== void 0 ? _f : pageUrl !== "" ? { url: pageUrl } : void 0;
                  return [4, evaluateTargetingAndStore({
                    sessionId: this.identifiers.sessionId,
                    targetingConfig,
                    loggerProvider: this.loggerProvider,
                    apiKey: this.config.apiKey,
                    targetingParams: {
                      userProperties: targetingParams.userProperties,
                      event: eventForTargeting,
                      page: pageForTargeting
                    },
                    urlChange: forceTargetingReevaluation
                  })];
                case 1:
                  targetingMatch = _g.sent();
                  if (forceTargetingReevaluation && urlChangeEvaluationId !== void 0 && urlChangeEvaluationId !== this.latestUrlChangeTargetingEvaluationId) {
                    this.loggerProvider.debug("Ignoring stale URL-change targeting result #".concat(urlChangeEvaluationId, "; latest is #").concat(this.latestUrlChangeTargetingEvaluationId, "."));
                    return [
                      2
                      /*return*/
                    ];
                  }
                  this.sessionTargetingMatch = this.sessionTargetingMatch || targetingMatch;
                  this.loggerProvider.debug(JSON.stringify({
                    name: "targeted replay capture config",
                    sessionTargetingMatch: this.sessionTargetingMatch,
                    event: eventForTargeting,
                    targetingParams
                  }, null, 2));
                  _g.label = 2;
                case 2:
                  if (!isInit) return [3, 3];
                  void this.initialize(true);
                  return [3, 5];
                case 3:
                  if (!(forceRestart || !this.recordCancelCallback)) return [3, 5];
                  this.loggerProvider.log("Recording events for session due to forceRestart or no ongoing recording.");
                  return [4, this.recordEvents()];
                case 4:
                  _g.sent();
                  _g.label = 5;
                case 5:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        this.addCustomRRWebEvent = function(eventName, eventData, addStorageInfo) {
          if (eventData === void 0) {
            eventData = {};
          }
          if (addStorageInfo === void 0) {
            addStorageInfo = true;
          }
          return __awaiter(_this, void 0, void 0, function() {
            var debugInfo, config2, storageSizeData, e_1;
            return __generator(this, function(_a4) {
              switch (_a4.label) {
                case 0:
                  _a4.trys.push([0, 3, , 4]);
                  debugInfo = void 0;
                  config2 = this.config;
                  if (!(config2 && eventName !== CustomRRwebEvent.METADATA)) return [3, 2];
                  debugInfo = {
                    config: getDebugConfig(config2),
                    version: VERSION3
                  };
                  if (!addStorageInfo) return [3, 2];
                  return [4, getStorageSize()];
                case 1:
                  storageSizeData = _a4.sent();
                  debugInfo = __assign(__assign({}, storageSizeData), debugInfo);
                  _a4.label = 2;
                case 2:
                  if (this.recordCancelCallback && this.recordFunction) {
                    this.recordFunction.addCustomEvent(eventName, __assign(__assign({}, eventData), debugInfo));
                  } else {
                    this.loggerProvider.debug("Not able to add custom replay capture event ".concat(eventName, " due to no ongoing recording."));
                  }
                  return [3, 4];
                case 3:
                  e_1 = _a4.sent();
                  this.loggerProvider.debug("Error while adding custom replay capture event: ", e_1);
                  return [3, 4];
                case 4:
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        this.stopRecordingEvents = function() {
          var _a4, _b, _c;
          try {
            _this.loggerProvider.log("Session Replay capture stopping.");
            _this.recordCancelCallback && _this.recordCancelCallback();
            _this.recordCancelCallback = null;
            (_a4 = _this.networkObservers) === null || _a4 === void 0 ? void 0 : _a4.stop();
            (_b = _this.crossOriginIframeCoordinator) === null || _b === void 0 ? void 0 : _b.stop();
            _this.crossOriginIframeCoordinator = null;
            (_c = _this.crossOriginParentSignalCleanup) === null || _c === void 0 ? void 0 : _c.call(_this);
            _this.crossOriginParentSignalCleanup = null;
          } catch (error) {
            var typedError = error;
            _this.loggerProvider.warn("Error occurred while stopping replay capture: ".concat(typedError.toString()));
          }
        };
        this.loggerProvider = new SafeLoggerProvider(new Logger());
      }
      SessionReplay2.prototype.init = function(apiKey2, options) {
        return returnWrapper(this._init(apiKey2, options));
      };
      SessionReplay2.prototype.setupUrlChangeListener = function() {
        var _this = this;
        var _a4, _b;
        (_a4 = this.urlChangeCleanup) === null || _a4 === void 0 ? void 0 : _a4.call(this);
        var globalScope3 = getGlobalScope();
        if (!(globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location)) {
          return;
        }
        var hasTargeting = !!((_b = this.config) === null || _b === void 0 ? void 0 : _b.targetingConfig);
        var onUrlChange = function(href) {
          _this.currentPageUrl = href;
          if (hasTargeting) {
            var evaluationId = ++_this.latestUrlChangeTargetingEvaluationId;
            void _this.evaluateTargetingAndCapture({
              userProperties: {},
              event: void 0,
              page: { url: href }
            }, false, false, true);
            _this.loggerProvider.debug("Queued URL-change targeting re-evaluation #".concat(evaluationId, " for ").concat(href, "."));
          }
        };
        var unsubscribe = subscribeToUrlChanges(globalScope3, onUrlChange);
        this.urlChangeCleanup = function() {
          unsubscribe();
          _this.urlChangeCleanup = null;
        };
      };
      SessionReplay2.prototype.isBelowMinSessionDuration = function() {
        var _a4;
        var minSessionDurationMs = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.minSessionDurationMs;
        if (minSessionDurationMs === void 0 || this.sessionStartTime === void 0) {
          return false;
        }
        return Date.now() - this.sessionStartTime < minSessionDurationMs;
      };
      SessionReplay2.prototype.getCurrentPageForTargeting = function() {
        var _a4, _b;
        var currentUrl = (_b = (_a4 = getGlobalScope()) === null || _a4 === void 0 ? void 0 : _a4.location) === null || _b === void 0 ? void 0 : _b.href;
        return currentUrl != null ? { url: currentUrl } : void 0;
      };
      SessionReplay2.prototype._init = function(apiKey2, options) {
        var _a4, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l, _m, _o, _p;
        return __awaiter(this, void 0, void 0, function() {
          var now, _q, _r, joinedConfig, localConfig, remoteConfig, scrollWatcher, managers, storeType, compressionWorkerScript, trackDestinationWorkerScript, globalScope3, _s, compressionScript2, trackDestinationScript2, rrwebEventManager, error_1, typedError, payloadBatcher, interactionEventManager, error_2, typedError, onFullSnapshotProcessed, pending_2, pending_1, pending_1_1, _t, event_1, sessionId, messenger, needsUrlTracking;
          var e_2, _u;
          var _this = this;
          return __generator(this, function(_v) {
            switch (_v.label) {
              case 0:
                (_a4 = this.urlChangeCleanup) === null || _a4 === void 0 ? void 0 : _a4.call(this);
                this.recordingEnabled = true;
                this.startChildRecordingOnSetup = false;
                this.loggerProvider = new SafeLoggerProvider(options.loggerProvider || new Logger());
                Object.prototype.hasOwnProperty.call(options, "logLevel") && this.loggerProvider.enable(options.logLevel);
                this.currentPageUrl = getCurrentUrl();
                this.identifiers = new SessionIdentifiers({ sessionId: options.sessionId, deviceId: options.deviceId });
                now = Date.now();
                pruneStaleReplayStartTimes(apiKey2, now, this.loggerProvider);
                this.sessionStartTime = options.sessionId !== void 0 ? (_b = getOrInitReplayStartTime(apiKey2, options.sessionId, now, this.loggerProvider)) !== null && _b !== void 0 ? _b : now : now;
                _q = this;
                return [4, createSessionReplayJoinedConfigGenerator(apiKey2, options)];
              case 1:
                _q.joinedConfigGenerator = _v.sent();
                return [4, this.joinedConfigGenerator.generateJoinedConfig()];
              case 2:
                _r = _v.sent(), joinedConfig = _r.joinedConfig, localConfig = _r.localConfig, remoteConfig = _r.remoteConfig;
                this.config = joinedConfig;
                this.setMetadata(options.sessionId, joinedConfig, localConfig, remoteConfig, (_c = options.version) === null || _c === void 0 ? void 0 : _c.version, VERSION3, (_d = options.version) === null || _d === void 0 ? void 0 : _d.type);
                this.pageLeaveFns = [];
                if (options.sessionId && ((_e = this.config.interactionConfig) === null || _e === void 0 ? void 0 : _e.enabled)) {
                  scrollWatcher = ScrollWatcher.default({
                    sessionId: options.sessionId,
                    type: "interaction"
                  }, this.config);
                  this.pageLeaveFns = [scrollWatcher.send(this.getDeviceId.bind(this)).bind(scrollWatcher)];
                  this.scrollHook = scrollWatcher.hook.bind(scrollWatcher);
                  this.clickHandler = new ClickHandler(this.loggerProvider, scrollWatcher);
                }
                managers = [];
                storeType = this.config.storeType;
                if (storeType === "idb" && !((_f = getGlobalScope()) === null || _f === void 0 ? void 0 : _f.indexedDB)) {
                  storeType = "memory";
                  this.loggerProvider.warn("Could not use preferred indexedDB storage, reverting to in memory option.");
                }
                this.loggerProvider.log("Using ".concat(storeType, " for event storage."));
                globalScope3 = getGlobalScope();
                if (!(this.config.useWebWorker && globalScope3 && globalScope3.Worker)) return [3, 4];
                return [4, Promise.resolve().then(() => (init_worker(), worker_exports))];
              case 3:
                _s = _v.sent(), compressionScript2 = _s.compressionScript, trackDestinationScript2 = _s.trackDestinationScript;
                compressionWorkerScript = compressionScript2;
                trackDestinationWorkerScript = trackDestinationScript2;
                _v.label = 4;
              case 4:
                _v.trys.push([4, 6, , 7]);
                return [4, createEventsManager({
                  config: this.config,
                  type: "replay",
                  minInterval: (_g = this.config.flushIntervalConfig) === null || _g === void 0 ? void 0 : _g.minIntervalMs,
                  maxInterval: (_h = this.config.flushIntervalConfig) === null || _h === void 0 ? void 0 : _h.maxIntervalMs,
                  maxPersistedEventsSize: this.config.maxPersistedEventsSizeBytes,
                  storeType,
                  trackDestinationWorkerScript,
                  shouldSend: function() {
                    return !_this.isBelowMinSessionDuration();
                  }
                })];
              case 5:
                rrwebEventManager = _v.sent();
                this.rrwebEventManager = rrwebEventManager;
                managers.push({ name: "replay", manager: rrwebEventManager });
                return [3, 7];
              case 6:
                error_1 = _v.sent();
                typedError = error_1;
                this.loggerProvider.warn("Error occurred while creating replay events manager: ".concat(typedError.toString()));
                return [3, 7];
              case 7:
                if (!((_j = this.config.interactionConfig) === null || _j === void 0 ? void 0 : _j.enabled)) return [3, 11];
                payloadBatcher = this.config.interactionConfig.batch ? clickBatcher : clickNonBatcher;
                _v.label = 8;
              case 8:
                _v.trys.push([8, 10, , 11]);
                return [4, createEventsManager({
                  config: this.config,
                  type: "interaction",
                  minInterval: (_k = this.config.interactionConfig.trackEveryNms) !== null && _k !== void 0 ? _k : INTERACTION_MIN_INTERVAL,
                  maxInterval: INTERACTION_MAX_INTERVAL,
                  maxPersistedEventsSize: this.config.maxPersistedEventsSizeBytes,
                  payloadBatcher,
                  storeType,
                  trackDestinationWorkerScript
                })];
              case 9:
                interactionEventManager = _v.sent();
                managers.push({ name: "interaction", manager: interactionEventManager });
                return [3, 11];
              case 10:
                error_2 = _v.sent();
                typedError = error_2;
                this.loggerProvider.warn("Error occurred while creating interaction events manager: ".concat(typedError.toString()));
                return [3, 11];
              case 11:
                this.eventsManager = new (MultiEventManager.bind.apply(MultiEventManager, __spreadArray([void 0], __read(managers), false)))();
                if (this.eventCompressor) {
                  this.eventCompressor.terminate();
                }
                onFullSnapshotProcessed = this.config.eagerFullSnapshotSend === true ? function() {
                  return _this.sendEvents();
                } : void 0;
                this.eventCompressor = new EventCompressor(this.eventsManager, this.config, this.getDeviceId(), compressionWorkerScript, onFullSnapshotProcessed);
                if (this.pendingEmitEvents.length > 0) {
                  pending_2 = this.pendingEmitEvents.splice(0);
                  try {
                    for (pending_1 = __values(pending_2), pending_1_1 = pending_1.next(); !pending_1_1.done; pending_1_1 = pending_1.next()) {
                      _t = pending_1_1.value, event_1 = _t.event, sessionId = _t.sessionId;
                      this.eventCompressor.enqueueEvent(event_1, sessionId);
                    }
                  } catch (e_2_1) {
                    e_2 = { error: e_2_1 };
                  } finally {
                    try {
                      if (pending_1_1 && !pending_1_1.done && (_u = pending_1.return)) _u.call(pending_1);
                    } finally {
                      if (e_2) throw e_2.error;
                    }
                  }
                }
                this.pageLeaveFns = __spreadArray(__spreadArray([], __read(this.pageLeaveFns), false), [
                  function() {
                    var _a5;
                    if (!_this.config || !((_a5 = _this.identifiers) === null || _a5 === void 0 ? void 0 : _a5.sessionId) || !rrwebEventManager)
                      return;
                    var events = rrwebEventManager.getBeaconEvents();
                    if (!events.length)
                      return;
                    var deviceId = _this.getDeviceId();
                    if (!deviceId)
                      return;
                    if (_this.isBelowMinSessionDuration())
                      return;
                    rrwebEventManager.trackDestination.sendBeacon({
                      events,
                      sessionId: _this.identifiers.sessionId,
                      deviceId,
                      apiKey: _this.config.apiKey,
                      serverZone: _this.config.serverZone
                    });
                  }
                ], false);
                return [4, this.initializeNetworkObservers()];
              case 12:
                _v.sent();
                if ((_l = getGlobalScope()) === null || _l === void 0 ? void 0 : _l.opener) {
                  messenger = getOrCreateWindowMessenger();
                  enableBackgroundCapture(messenger);
                  messenger.setup(__assign({ logger: this.loggerProvider }, this.config.serverZone && { endpoint: AMPLITUDE_ORIGINS_MAP[this.config.serverZone] }));
                }
                this.loggerProvider.log("Installing @amplitude/session-replay-browser.");
                this.teardownEventListeners(false);
                return [4, this.evaluateTargetingAndCapture({ userProperties: options.userProperties, page: this.getCurrentPageForTargeting() }, true)];
              case 13:
                _v.sent();
                needsUrlTracking = this.config.targetingConfig || ((_p = (_o = (_m = this.config.privacyConfig) === null || _m === void 0 ? void 0 : _m.urlMaskLevels) === null || _o === void 0 ? void 0 : _o.length) !== null && _p !== void 0 ? _p : 0) > 0;
                if (needsUrlTracking) {
                  this.setupUrlChangeListener();
                }
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.setSessionId = function(sessionId, deviceId) {
        return returnWrapper(this.asyncSetSessionId(sessionId, deviceId));
      };
      SessionReplay2.prototype.asyncSetSessionId = function(sessionId, deviceId, options) {
        var _a4, _b, _c, _d;
        return __awaiter(this, void 0, void 0, function() {
          var previousSessionId, currentDeviceId, isSessionChange, deviceIdForReplayId, joinedConfig;
          return __generator(this, function(_e) {
            switch (_e.label) {
              case 0:
                previousSessionId = (_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.sessionId;
                currentDeviceId = this.getDeviceId();
                if (previousSessionId !== void 0 && previousSessionId === sessionId && (deviceId === void 0 || deviceId === currentDeviceId) && ((options === null || options === void 0 ? void 0 : options.userProperties) === void 0 || Object.keys(options.userProperties).length === 0)) {
                  return [
                    2
                    /*return*/
                  ];
                }
                this.latestUrlChangeTargetingEvaluationId++;
                this.sessionTargetingMatch = false;
                this.lastShouldRecordDecision = void 0;
                if (previousSessionId) {
                  this.sendEvents(previousSessionId);
                }
                isSessionChange = previousSessionId !== sessionId;
                if (isSessionChange) {
                  (_b = this.rrwebEventManager) === null || _b === void 0 ? void 0 : _b.dropPendingBeaconEvents();
                }
                deviceIdForReplayId = deviceId || this.getDeviceId();
                this.identifiers = new SessionIdentifiers({
                  sessionId,
                  deviceId: deviceIdForReplayId
                });
                if (isSessionChange) {
                  this.sessionStartTime = Date.now();
                  this.suppressedSendCount = 0;
                  this.hasEmittedGateDecision = false;
                  if ((_c = this.config) === null || _c === void 0 ? void 0 : _c.apiKey) {
                    setReplayStartTime(this.config.apiKey, sessionId, this.sessionStartTime, this.loggerProvider);
                    if (previousSessionId !== void 0) {
                      removeReplayStartTime(this.config.apiKey, previousSessionId, this.loggerProvider);
                    }
                  }
                }
                if (!(this.joinedConfigGenerator && previousSessionId)) return [3, 2];
                return [4, this.joinedConfigGenerator.generateJoinedConfig()];
              case 1:
                joinedConfig = _e.sent().joinedConfig;
                this.config = joinedConfig;
                _e.label = 2;
              case 2:
                if (!((_d = this.config) === null || _d === void 0 ? void 0 : _d.targetingConfig)) return [3, 4];
                return [4, this.evaluateTargetingAndCapture({ userProperties: options === null || options === void 0 ? void 0 : options.userProperties, page: this.getCurrentPageForTargeting() }, false, true)];
              case 3:
                _e.sent();
                return [3, 6];
              case 4:
                return [4, this.recordEvents()];
              case 5:
                _e.sent();
                _e.label = 6;
              case 6:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.getSessionReplayProperties = function() {
        var _a4;
        var config2 = this.config;
        var identifiers = this.identifiers;
        if (!config2 || !identifiers) {
          this.loggerProvider.warn("Session replay init has not been called, cannot get session replay properties.");
          return {};
        }
        var shouldRecord = this.getShouldRecord();
        var eventProperties = {};
        if (shouldRecord) {
          eventProperties = (_a4 = {}, _a4[DEFAULT_SESSION_REPLAY_PROPERTY] = identifiers.sessionReplayId ? identifiers.sessionReplayId : null, _a4);
          if (config2.debugMode) {
            eventProperties[SESSION_REPLAY_DEBUG_PROPERTY] = JSON.stringify({
              appHash: generateHashCode(config2.apiKey).toString()
            });
          }
        }
        void this.addCustomRRWebEvent(CustomRRwebEvent.GET_SR_PROPS, {
          shouldRecord,
          eventProperties
        }, this.eventCount === 10);
        if (this.eventCount === 10) {
          this.eventCount = 0;
        }
        this.eventCount++;
        return eventProperties;
      };
      SessionReplay2.prototype.sendEvents = function(sessionId) {
        var _a4, _b;
        var sessionIdToSend = sessionId || ((_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.sessionId);
        var deviceId = this.getDeviceId();
        if (this.eventsManager && sessionIdToSend && deviceId) {
          if (this.isBelowMinSessionDuration()) {
            var startTime = this.sessionStartTime;
            var minMs = this.config.minSessionDurationMs;
            this.loggerProvider.log("Session ".concat(sessionIdToSend, " not sent: duration ").concat(Date.now() - startTime, "ms is below minimum ").concat(minMs, "ms."));
            this.suppressedSendCount++;
            return;
          }
          if (!this.hasEmittedGateDecision && ((_b = this.config) === null || _b === void 0 ? void 0 : _b.minSessionDurationMs) !== void 0 && this.recordCancelCallback && this.recordFunction) {
            this.hasEmittedGateDecision = true;
            var elapsedMs = this.sessionStartTime !== void 0 ? Date.now() - this.sessionStartTime : void 0;
            void this.addCustomRRWebEvent(CustomRRwebEvent.REPLAY_GATE_DECISION, {
              sessionId: sessionIdToSend,
              suppressedSendCount: this.suppressedSendCount,
              elapsedMs,
              minSessionDurationMs: this.config.minSessionDurationMs
            }, false);
          }
          this.eventsManager.sendCurrentSequenceEvents({ sessionId: sessionIdToSend, deviceId });
        }
      };
      SessionReplay2.prototype.initialize = function(shouldSendStoredEvents) {
        var _a4;
        if (shouldSendStoredEvents === void 0) {
          shouldSendStoredEvents = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          var deviceId;
          return __generator(this, function(_b) {
            if (!((_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.sessionId)) {
              this.loggerProvider.log("Session is not being recorded due to lack of session id.");
              return [2, Promise.resolve()];
            }
            deviceId = this.getDeviceId();
            if (!deviceId) {
              this.loggerProvider.log("Session is not being recorded due to lack of device id.");
              return [2, Promise.resolve()];
            }
            this.eventsManager && shouldSendStoredEvents && void this.eventsManager.sendStoredEvents({ deviceId });
            return [2, this.recordEvents()];
          });
        });
      };
      SessionReplay2.prototype.shouldOptOut = function() {
        var _a4, _b;
        var identityStoreOptOut;
        if ((_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.instanceName) {
          var identityStore = getAnalyticsConnector(this.config.instanceName).identityStore;
          identityStoreOptOut = identityStore.getIdentity().optOut;
        }
        return identityStoreOptOut !== void 0 ? identityStoreOptOut : (_b = this.config) === null || _b === void 0 ? void 0 : _b.optOut;
      };
      SessionReplay2.prototype.getShouldRecord = function() {
        if (!this.identifiers || !this.config || !this.identifiers.sessionId) {
          this.loggerProvider.warn("Session is not being recorded due to lack of config, please call sessionReplay.init.");
          return false;
        }
        if (!this.recordingEnabled) {
          this.loggerProvider.log("Session ".concat(this.identifiers.sessionId, " not being captured because recording was stopped."));
          return false;
        }
        if (!this.config.captureEnabled) {
          this.loggerProvider.log("Session ".concat(this.identifiers.sessionId, " not being captured due to capture being disabled for project or because the remote config could not be fetched."));
          return false;
        }
        if (this.shouldOptOut()) {
          this.loggerProvider.log("Opting session ".concat(this.identifiers.sessionId, " out of recording due to optOut config."));
          return false;
        }
        var shouldRecord = false;
        var message = "";
        var matched = false;
        if (this.config.targetingConfig) {
          if (!this.sessionTargetingMatch) {
            message = "Not capturing replays for session ".concat(this.identifiers.sessionId, " due to not matching targeting conditions.");
            this.loggerProvider.log(message);
            shouldRecord = false;
            matched = false;
          } else {
            message = "Capturing replays for session ".concat(this.identifiers.sessionId, " due to matching targeting conditions.");
            this.loggerProvider.log(message);
            shouldRecord = true;
            matched = true;
          }
        } else {
          var isInSample = isSessionInSample(this.identifiers.sessionId, this.config.sampleRate);
          if (!isInSample) {
            message = "Opting session ".concat(this.identifiers.sessionId, " out of recording due to sample rate.");
            this.loggerProvider.log(message);
            shouldRecord = false;
            matched = false;
          } else {
            shouldRecord = true;
            matched = true;
          }
        }
        if (this.lastShouldRecordDecision !== shouldRecord && this.config.targetingConfig) {
          void this.addCustomRRWebEvent(CustomRRwebEvent.TARGETING_DECISION, {
            message,
            sessionId: this.identifiers.sessionId,
            matched,
            targetingParams: this.lastTargetingParams
          });
          this.lastShouldRecordDecision = shouldRecord;
        }
        return shouldRecord;
      };
      SessionReplay2.prototype.getBlockSelectors = function() {
        var _a4, _b, _c;
        var blockSelector = (_c = (_b = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.privacyConfig) === null || _b === void 0 ? void 0 : _b.blockSelector) !== null && _c !== void 0 ? _c : [];
        if (blockSelector.length === 0) {
          return void 0;
        }
        return blockSelector;
      };
      SessionReplay2.prototype.getMaskTextSelectors = function() {
        var _a4, _b;
        var privacyConfig = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.privacyConfig;
        var effectiveLevel = privacyConfig ? getEffectiveMaskLevel(this.currentPageUrl, privacyConfig) : void 0;
        if (effectiveLevel === "conservative") {
          return "*";
        }
        if ((_b = privacyConfig === null || privacyConfig === void 0 ? void 0 : privacyConfig.urlMaskLevels) === null || _b === void 0 ? void 0 : _b.some(function(rule3) {
          return rule3.maskLevel === "conservative";
        })) {
          return "*";
        }
        var urlMaskLevels = privacyConfig === null || privacyConfig === void 0 ? void 0 : privacyConfig.urlMaskLevels;
        if ((privacyConfig === null || privacyConfig === void 0 ? void 0 : privacyConfig.defaultMaskLevel) === "conservative" && urlMaskLevels && urlMaskLevels.length > 0) {
          return "*";
        }
        var maskSelector = privacyConfig === null || privacyConfig === void 0 ? void 0 : privacyConfig.maskSelector;
        if (!maskSelector) {
          return;
        }
        return maskSelector;
      };
      SessionReplay2.prototype.getRecordingPlugins = function(loggingConfig) {
        var _a4, _b, _c, _d, _e, _f;
        return __awaiter(this, void 0, void 0, function() {
          var plugins, urlTrackingPlugin2, getRecordConsolePlugin2, error_3;
          return __generator(this, function(_g) {
            switch (_g.label) {
              case 0:
                plugins = [];
                try {
                  urlTrackingPlugin2 = createUrlTrackingPlugin({
                    ugcFilterRules: ((_b = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.interactionConfig) === null || _b === void 0 ? void 0 : _b.ugcFilterRules) || [],
                    enablePolling: ((_c = this.config) === null || _c === void 0 ? void 0 : _c.enableUrlChangePolling) || false,
                    pollingInterval: (_d = this.config) === null || _d === void 0 ? void 0 : _d.urlChangePollingInterval,
                    captureDocumentTitle: (_e = this.config) === null || _e === void 0 ? void 0 : _e.captureDocumentTitle
                  });
                  plugins.push(urlTrackingPlugin2);
                } catch (error) {
                  this.loggerProvider.warn("Failed to create URL tracking plugin:", error);
                }
                if (!((_f = loggingConfig === null || loggingConfig === void 0 ? void 0 : loggingConfig.console) === null || _f === void 0 ? void 0 : _f.enabled)) return [3, 4];
                _g.label = 1;
              case 1:
                _g.trys.push([1, 3, , 4]);
                return [4, Promise.resolve().then(() => (init_rrweb_plugin_console_record(), rrweb_plugin_console_record_exports))];
              case 2:
                getRecordConsolePlugin2 = _g.sent().getRecordConsolePlugin;
                plugins.push(getRecordConsolePlugin2({ level: loggingConfig.console.levels }));
                return [3, 4];
              case 3:
                error_3 = _g.sent();
                this.loggerProvider.warn("Failed to load console plugin:", error_3);
                return [3, 4];
              case 4:
                return [2, plugins.length > 0 ? plugins : void 0];
            }
          });
        });
      };
      SessionReplay2.prototype.getRecordFunction = function() {
        return __awaiter(this, void 0, void 0, function() {
          var record2, error_4;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                if (this.recordFunction) {
                  return [2, this.recordFunction];
                }
                _a4.label = 1;
              case 1:
                _a4.trys.push([1, 3, , 4]);
                return [4, Promise.resolve().then(() => (init_rrweb_record(), rrweb_record_exports))];
              case 2:
                record2 = _a4.sent().record;
                this.recordFunction = record2;
                return [2, record2];
              case 3:
                error_4 = _a4.sent();
                this.loggerProvider.warn("Failed to load rrweb-record module:", error_4);
                return [2, null];
              case 4:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.recordEvents = function(shouldLogMetadata) {
        if (shouldLogMetadata === void 0) {
          shouldLogMetadata = true;
        }
        return __awaiter(this, void 0, void 0, function() {
          var pendingArgs;
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                if (this.recordEventsInFlight) {
                  this.recordEventsPendingShouldLogMetadata = shouldLogMetadata;
                  return [
                    2
                    /*return*/
                  ];
                }
                this.recordEventsInFlight = true;
                _a4.label = 1;
              case 1:
                _a4.trys.push([1, , 6, 7]);
                return [4, this._recordEvents(shouldLogMetadata)];
              case 2:
                _a4.sent();
                _a4.label = 3;
              case 3:
                if (!(this.recordEventsPendingShouldLogMetadata !== null)) return [3, 5];
                pendingArgs = this.recordEventsPendingShouldLogMetadata;
                this.recordEventsPendingShouldLogMetadata = null;
                return [4, this._recordEvents(pendingArgs)];
              case 4:
                _a4.sent();
                return [3, 3];
              case 5:
                return [3, 7];
              case 6:
                this.recordEventsInFlight = false;
                this.recordEventsPendingShouldLogMetadata = null;
                return [
                  7
                  /*endfinally*/
                ];
              case 7:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.shouldAbandonRecordStart = function(generation) {
        return !this.recordingEnabled || generation !== this.recordingGeneration;
      };
      SessionReplay2.prototype._recordEvents = function(shouldLogMetadata) {
        var _a4, _b, _c, _d, _e, _f, _g, _h;
        if (shouldLogMetadata === void 0) {
          shouldLogMetadata = true;
        }
        return __awaiter(this, void 0, void 0, function() {
          var config2, shouldRecord, sessionId, generation, recordFunction, networkLoggingConfig, trackUrl, ignoredUrls, interactionConfig, loggingConfig, hooks, ugcFilterRules, crossOriginIframesEnabled, coordinateChildren, childMode, plugins, error_5;
          var _this = this;
          return __generator(this, function(_j) {
            switch (_j.label) {
              case 0:
                config2 = this.config;
                shouldRecord = this.getShouldRecord();
                sessionId = (_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.sessionId;
                if (!shouldRecord || !sessionId || !config2) {
                  return [
                    2
                    /*return*/
                  ];
                }
                this.stopRecordingEvents();
                generation = this.recordingGeneration;
                return [4, this.getRecordFunction()];
              case 1:
                recordFunction = _j.sent();
                if (!recordFunction || this.shouldAbandonRecordStart(generation)) {
                  return [
                    2
                    /*return*/
                  ];
                }
                return [4, this.initializeNetworkObservers()];
              case 2:
                _j.sent();
                if (this.shouldAbandonRecordStart(generation)) {
                  return [
                    2
                    /*return*/
                  ];
                }
                networkLoggingConfig = (_b = config2.loggingConfig) === null || _b === void 0 ? void 0 : _b.network;
                trackUrl = getServerUrl2(config2.serverZone, config2.trackServerUrl);
                ignoredUrls = [SESSION_REPLAY_SERVER_URL, SESSION_REPLAY_EU_URL, SESSION_REPLAY_STAGING_URL, trackUrl];
                (_c = this.networkObservers) === null || _c === void 0 ? void 0 : _c.start(function(event) {
                  if (ignoredUrls.some(function(url) {
                    return event.url.startsWith(url);
                  }))
                    return;
                  void _this.addCustomRRWebEvent(CustomRRwebEvent.FETCH_REQUEST, event);
                }, networkLoggingConfig);
                interactionConfig = config2.interactionConfig, loggingConfig = config2.loggingConfig;
                hooks = (interactionConfig === null || interactionConfig === void 0 ? void 0 : interactionConfig.enabled) ? {
                  mouseInteraction: this.eventsManager && ((_d = this.clickHandler) === null || _d === void 0 ? void 0 : _d.createHook({
                    eventsManager: this.eventsManager,
                    sessionId,
                    deviceIdFn: this.getDeviceId.bind(this),
                    mirror: recordFunction.mirror,
                    ugcFilterRules: (_e = interactionConfig.ugcFilterRules) !== null && _e !== void 0 ? _e : [],
                    performanceOptions: (_f = config2.performanceConfig) === null || _f === void 0 ? void 0 : _f.interaction
                  })),
                  scroll: this.scrollHook
                } : {};
                ugcFilterRules = (interactionConfig === null || interactionConfig === void 0 ? void 0 : interactionConfig.enabled) && interactionConfig.ugcFilterRules ? interactionConfig.ugcFilterRules : [];
                this.loggerProvider.log("Session Replay capture beginning for ".concat(sessionId, "."));
                _j.label = 3;
              case 3:
                _j.trys.push([3, 5, , 6]);
                crossOriginIframesEnabled = !!((_g = config2.crossOriginIframes) === null || _g === void 0 ? void 0 : _g.enabled);
                coordinateChildren = ((_h = config2.crossOriginIframes) === null || _h === void 0 ? void 0 : _h.coordinateChildren) !== false;
                childMode = crossOriginIframesEnabled && isInIframe();
                if (childMode && coordinateChildren) {
                  this.crossOriginParentSignalCleanup = listenForParentSignals({
                    onStart: function() {
                      if (!_this.recordingEnabled) {
                        return;
                      }
                      _this._recordEventsInChildMode(recordFunction, sessionId, config2, hooks);
                    },
                    onStop: function() {
                      try {
                        _this.recordCancelCallback && _this.recordCancelCallback();
                        _this.recordCancelCallback = null;
                      } catch (error) {
                        var typedError = error;
                        _this.loggerProvider.warn("Error occurred while stopping child iframe replay capture: ".concat(typedError.toString()));
                      }
                    }
                  });
                  if (this.startChildRecordingOnSetup) {
                    this.startChildRecordingOnSetup = false;
                    this._recordEventsInChildMode(recordFunction, sessionId, config2, hooks);
                  }
                  return [
                    2
                    /*return*/
                  ];
                }
                this.startChildRecordingOnSetup = false;
                return [4, this.getRecordingPlugins(loggingConfig)];
              case 4:
                plugins = _j.sent();
                if (this.shouldAbandonRecordStart(generation)) {
                  return [
                    2
                    /*return*/
                  ];
                }
                this.recordCancelCallback = recordFunction(__assign(__assign({}, this.buildRRWebRecordOptions(config2, hooks, function(event) {
                  if (_this.shouldOptOut() || !_this.recordingEnabled) {
                    _this.loggerProvider.log(_this.shouldOptOut() ? "Opting session ".concat(sessionId, " out of recording due to optOut config.") : "Session Replay capture stopping for ".concat(sessionId, "."));
                    _this.stopRecordingEvents();
                    _this.sendEvents();
                    return;
                  }
                  if (event.type === EventType.Meta) {
                    event.data.href = getPageUrl(event.data.href, ugcFilterRules);
                  }
                  if (_this.eventCompressor) {
                    _this.eventCompressor.enqueueEvent(event, sessionId);
                  } else {
                    _this.pendingEmitEvents.push({ event, sessionId });
                  }
                }, "Error while capturing replay: ")), { plugins, recordCrossOriginIframes: crossOriginIframesEnabled }));
                if (crossOriginIframesEnabled && !childMode && coordinateChildren) {
                  if (!this.crossOriginIframeCoordinator) {
                    this.crossOriginIframeCoordinator = new CrossOriginIframeCoordinator();
                  }
                  this.crossOriginIframeCoordinator.start();
                }
                void this.addCustomRRWebEvent(CustomRRwebEvent.DEBUG_INFO);
                if (shouldLogMetadata) {
                  void this.addCustomRRWebEvent(CustomRRwebEvent.METADATA, this.metadata);
                }
                return [3, 6];
              case 5:
                error_5 = _j.sent();
                this.loggerProvider.warn("Failed to initialize session replay:", error_5);
                return [3, 6];
              case 6:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.buildRRWebRecordOptions = function(config2, hooks, emit, errorLogPrefix) {
        var _this = this;
        var privacyConfig = config2.privacyConfig;
        return __assign(__assign({ emit, inlineStylesheet: config2.shouldInlineStylesheet, inlineImages: config2.inlineImages, hooks, maskAllInputs: true, maskTextClass: MASK_TEXT_CLASS, blockClass: BLOCK_CLASS, blockSelector: this.getBlockSelectors(), applyBackgroundColorToBlockedElements: config2.applyBackgroundColorToBlockedElements, maskInputFn: maskFn("input", privacyConfig, function() {
          return _this.currentPageUrl;
        }), maskTextFn: maskFn("text", privacyConfig, function() {
          return _this.currentPageUrl;
        }), maskAttributeFn: maskAttributeFn(privacyConfig, function() {
          return _this.currentPageUrl;
        }), maskTextSelector: this.getMaskTextSelectors() }, config2.fullSnapshotIntervalMs !== void 0 && { checkoutEveryNms: config2.fullSnapshotIntervalMs }), {
          recordCanvas: false,
          captureAdoptedStyleSheets: config2.captureAdoptedStyleSheets,
          // Strip nodes that are never rendered by the rrweb replay player.
          // None of these affect visual fidelity; omitting them reduces snapshot size.
          slimDOMOptions: {
            script: true,
            comment: true,
            headFavicon: true,
            headWhitespace: true,
            headMetaDescKeywords: true,
            headMetaSocial: true,
            headMetaRobots: true,
            headMetaHttpEquiv: true,
            headMetaAuthorship: true,
            headMetaVerification: true
          },
          errorHandler: function(error) {
            var typedError = error;
            if (typedError.message.includes("insertRule") && typedError.message.includes("CSSStyleSheet")) {
              throw typedError;
            }
            if (typedError._external_) {
              throw typedError;
            }
            _this.loggerProvider.warn(errorLogPrefix, typedError.toString());
            return true;
          }
        });
      };
      SessionReplay2.prototype._recordEventsInChildMode = function(recordFunction, sessionId, config2, hooks) {
        try {
          this.recordCancelCallback && this.recordCancelCallback();
          this.recordCancelCallback = null;
          this.recordCancelCallback = recordFunction(__assign(__assign({}, this.buildRRWebRecordOptions(config2, hooks, function() {
          }, "Error while capturing replay (child iframe): ")), { recordCrossOriginIframes: true }));
          this.loggerProvider.log("Session Replay child iframe capture beginning for session ".concat(sessionId, "."));
        } catch (error) {
          this.loggerProvider.warn("Failed to initialize session replay in child iframe mode:", error);
        }
      };
      SessionReplay2.prototype.getDeviceId = function() {
        var _a4;
        return (_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.deviceId;
      };
      SessionReplay2.prototype.getSessionId = function() {
        var _a4;
        return (_a4 = this.identifiers) === null || _a4 === void 0 ? void 0 : _a4.sessionId;
      };
      SessionReplay2.prototype.start = function() {
        return returnWrapper(this._start());
      };
      SessionReplay2.prototype._start = function() {
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                this.recordingEnabled = true;
                this.startChildRecordingOnSetup = true;
                return [4, this.recordEvents()];
              case 1:
                _a4.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplay2.prototype.stop = function() {
        this.recordingEnabled = false;
        this.startChildRecordingOnSetup = false;
        this.recordingGeneration++;
        this.stopRecordingEvents();
        this.sendEvents();
      };
      SessionReplay2.prototype.flush = function(useRetry) {
        var _a4;
        if (useRetry === void 0) {
          useRetry = false;
        }
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_b) {
            return [2, (_a4 = this.eventsManager) === null || _a4 === void 0 ? void 0 : _a4.flush(useRetry)];
          });
        });
      };
      SessionReplay2.prototype.shutdown = function() {
        var _a4, _b;
        this.recordingEnabled = false;
        this.startChildRecordingOnSetup = false;
        this.recordingGeneration++;
        (_a4 = this.urlChangeCleanup) === null || _a4 === void 0 ? void 0 : _a4.call(this);
        (_b = this.crossOriginParentSignalCleanup) === null || _b === void 0 ? void 0 : _b.call(this);
        this.crossOriginParentSignalCleanup = null;
        this.teardownEventListeners(true);
        this.stopRecordingEvents();
        this.sendEvents();
      };
      SessionReplay2.prototype.mapSDKType = function(sdkType) {
        if (sdkType === "plugin") {
          return "@amplitude/plugin-session-replay-browser";
        }
        if (sdkType === "segment") {
          return "@amplitude/segment-session-replay-plugin";
        }
        return null;
      };
      SessionReplay2.prototype.setMetadata = function(sessionId, joinedConfig, localConfig, remoteConfig, replaySDKVersion, standaloneSDKVersion, sdkType) {
        var hashValue = (sessionId === null || sessionId === void 0 ? void 0 : sessionId.toString()) ? generateHashCode(sessionId.toString()) : void 0;
        this.metadata = {
          joinedConfig,
          localConfig,
          remoteConfig,
          sessionId,
          hashValue,
          sampleRate: joinedConfig.sampleRate,
          replaySDKType: this.mapSDKType(sdkType),
          replaySDKVersion,
          standaloneSDKType: "@amplitude/session-replay-browser",
          standaloneSDKVersion
        };
      };
      SessionReplay2.prototype.initializeNetworkObservers = function() {
        var _a4, _b, _c;
        return __awaiter(this, void 0, void 0, function() {
          var NetworkObserversClass, error_6;
          return __generator(this, function(_d) {
            switch (_d.label) {
              case 0:
                if (!(((_c = (_b = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.loggingConfig) === null || _b === void 0 ? void 0 : _b.network) === null || _c === void 0 ? void 0 : _c.enabled) && !this.networkObservers)) return [3, 4];
                _d.label = 1;
              case 1:
                _d.trys.push([1, 3, , 4]);
                return [4, Promise.resolve().then(() => (init_observers(), observers_exports))];
              case 2:
                NetworkObserversClass = _d.sent().NetworkObservers;
                this.networkObservers = new NetworkObserversClass();
                return [3, 4];
              case 3:
                error_6 = _d.sent();
                this.loggerProvider.warn("Failed to import or instantiate NetworkObservers:", error_6);
                return [3, 4];
              case 4:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      return SessionReplay2;
    })()
  );

  // node_modules/@amplitude/session-replay-browser/lib/esm/session-replay-factory.js
  var getLogConfig = function(sessionReplay) {
    return function() {
      var config2 = sessionReplay.config;
      var _a4 = config2 || getDefaultConfig2(), logger = _a4.loggerProvider, logLevel = _a4.logLevel;
      return {
        logger,
        logLevel
      };
    };
  };
  var createInstance3 = function() {
    var sessionReplay = new SessionReplay();
    return {
      init: debugWrapper(sessionReplay.init.bind(sessionReplay), "init", getLogConfig(sessionReplay)),
      evaluateTargetingAndCapture: debugWrapper(sessionReplay.evaluateTargetingAndCapture.bind(sessionReplay), "evaluateTargetingAndRecord", getLogConfig(sessionReplay)),
      setSessionId: debugWrapper(sessionReplay.setSessionId.bind(sessionReplay), "setSessionId", getLogConfig(sessionReplay)),
      getSessionId: debugWrapper(sessionReplay.getSessionId.bind(sessionReplay), "getSessionId", getLogConfig(sessionReplay)),
      getSessionReplayProperties: debugWrapper(sessionReplay.getSessionReplayProperties.bind(sessionReplay), "getSessionReplayProperties", getLogConfig(sessionReplay)),
      flush: debugWrapper(sessionReplay.flush.bind(sessionReplay), "flush", getLogConfig(sessionReplay)),
      start: debugWrapper(sessionReplay.start.bind(sessionReplay), "start", getLogConfig(sessionReplay)),
      stop: debugWrapper(sessionReplay.stop.bind(sessionReplay), "stop", getLogConfig(sessionReplay)),
      shutdown: debugWrapper(sessionReplay.shutdown.bind(sessionReplay), "shutdown", getLogConfig(sessionReplay))
    };
  };
  var session_replay_factory_default = createInstance3();

  // node_modules/@amplitude/session-replay-browser/lib/esm/index.js
  var init2 = session_replay_factory_default.init;
  var setSessionId2 = session_replay_factory_default.setSessionId;
  var getSessionId2 = session_replay_factory_default.getSessionId;
  var getSessionReplayProperties = session_replay_factory_default.getSessionReplayProperties;
  var flush2 = session_replay_factory_default.flush;
  var start2 = session_replay_factory_default.start;
  var stop = session_replay_factory_default.stop;
  var shutdown = session_replay_factory_default.shutdown;
  var evaluateTargetingAndCapture = session_replay_factory_default.evaluateTargetingAndCapture;

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/helpers.js
  init_tslib_es6();

  // node_modules/@amplitude/analytics-types/lib/esm/event.js
  var IdentifyOperation3;
  (function(IdentifyOperation4) {
    IdentifyOperation4["SET"] = "$set";
    IdentifyOperation4["SET_ONCE"] = "$setOnce";
    IdentifyOperation4["ADD"] = "$add";
    IdentifyOperation4["APPEND"] = "$append";
    IdentifyOperation4["PREPEND"] = "$prepend";
    IdentifyOperation4["REMOVE"] = "$remove";
    IdentifyOperation4["PREINSERT"] = "$preInsert";
    IdentifyOperation4["POSTINSERT"] = "$postInsert";
    IdentifyOperation4["UNSET"] = "$unset";
    IdentifyOperation4["CLEAR_ALL"] = "$clearAll";
  })(IdentifyOperation3 || (IdentifyOperation3 = {}));
  var RevenueProperty2;
  (function(RevenueProperty3) {
    RevenueProperty3["REVENUE_PRODUCT_ID"] = "$productId";
    RevenueProperty3["REVENUE_QUANTITY"] = "$quantity";
    RevenueProperty3["REVENUE_PRICE"] = "$price";
    RevenueProperty3["REVENUE_TYPE"] = "$revenueType";
    RevenueProperty3["REVENUE_CURRENCY"] = "$currency";
    RevenueProperty3["REVENUE"] = "$revenue";
  })(RevenueProperty2 || (RevenueProperty2 = {}));
  var SpecialEventType2;
  (function(SpecialEventType3) {
    SpecialEventType3["IDENTIFY"] = "$identify";
    SpecialEventType3["GROUP_IDENTIFY"] = "$groupidentify";
    SpecialEventType3["REVENUE"] = "revenue_amount";
  })(SpecialEventType2 || (SpecialEventType2 = {}));

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/constants.js
  var PROPERTY_ADD_OPERATIONS = [
    IdentifyOperation3.SET,
    IdentifyOperation3.SET_ONCE,
    IdentifyOperation3.ADD,
    IdentifyOperation3.APPEND,
    IdentifyOperation3.PREPEND,
    IdentifyOperation3.POSTINSERT
  ];

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/helpers.js
  var parseUserProperties = function(event) {
    if (!event.user_properties) {
      return;
    }
    var userPropertiesObj = {};
    var userPropertyKeys = Object.keys(event.user_properties);
    userPropertyKeys.forEach(function(identifyKey) {
      if (PROPERTY_ADD_OPERATIONS.includes(identifyKey)) {
        var typedUserPropertiesOperation = event.user_properties && event.user_properties[identifyKey];
        userPropertiesObj = __assign(__assign({}, userPropertiesObj), typedUserPropertiesOperation);
      }
    });
    return userPropertiesObj;
  };

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/version.js
  var VERSION5 = "1.35.1";

  // node_modules/@amplitude/plugin-session-replay-browser/lib/esm/session-replay.js
  var DEFAULT_EVENT_PROPERTY_PREFIX2 = "[Amplitude]";
  var DEFAULT_SESSION_REPLAY_PROPERTY2 = "".concat(DEFAULT_EVENT_PROPERTY_PREFIX2, " Session Replay ID");
  var SessionReplayPlugin = (
    /** @class */
    (function() {
      function SessionReplayPlugin2(options) {
        this.name = SessionReplayPlugin2.pluginName;
        this.type = "enrichment";
        this.config = null;
        this.sessionReplay = {
          flush: flush2,
          getSessionId: getSessionId2,
          getSessionReplayProperties,
          init: init2,
          setSessionId: setSessionId2,
          start: start2,
          stop,
          shutdown,
          evaluateTargetingAndCapture
        };
        this.options = __assign({ forceSessionTracking: false }, options);
        this.srInitOptions = this.options;
      }
      SessionReplayPlugin2.prototype.setup = function(config2, _client) {
        var _a4, _b, _c, _d, _e, _f, _g, _h;
        return __awaiter(this, void 0, void 0, function() {
          var identityStore, userProperties, error_1;
          return __generator(this, function(_j) {
            switch (_j.label) {
              case 0:
                _j.trys.push([0, 2, , 3]);
                config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider.log("Installing @amplitude/plugin-session-replay, version ".concat(VERSION5, "."));
                this.config = config2;
                if (this.options.forceSessionTracking) {
                  if (typeof config2.defaultTracking === "boolean") {
                    if (config2.defaultTracking === false) {
                      config2.defaultTracking = {
                        pageViews: false,
                        formInteractions: false,
                        fileDownloads: false,
                        sessions: true
                      };
                    }
                  } else {
                    config2.defaultTracking = __assign(__assign({}, config2.defaultTracking), { sessions: true });
                  }
                }
                identityStore = getAnalyticsConnector(this.config.instanceName).identityStore;
                userProperties = identityStore.getIdentity().userProperties;
                this.srInitOptions = {
                  instanceName: this.config.instanceName,
                  deviceId: (_a4 = this.options.deviceId) !== null && _a4 !== void 0 ? _a4 : this.config.deviceId,
                  optOut: this.config.optOut,
                  sessionId: this.options.customSessionId ? void 0 : this.config.sessionId,
                  loggerProvider: this.config.loggerProvider,
                  logLevel: this.config.logLevel,
                  flushMaxRetries: this.config.flushMaxRetries,
                  serverZone: this.config.serverZone,
                  configServerUrl: this.options.configServerUrl || ((_b = this.config.remoteConfig) === null || _b === void 0 ? void 0 : _b.serverUrl),
                  trackServerUrl: this.options.trackServerUrl,
                  sampleRate: this.options.sampleRate,
                  privacyConfig: {
                    blockSelector: (_c = this.options.privacyConfig) === null || _c === void 0 ? void 0 : _c.blockSelector,
                    maskSelector: (_d = this.options.privacyConfig) === null || _d === void 0 ? void 0 : _d.maskSelector,
                    unmaskSelector: (_e = this.options.privacyConfig) === null || _e === void 0 ? void 0 : _e.unmaskSelector,
                    defaultMaskLevel: (_f = this.options.privacyConfig) === null || _f === void 0 ? void 0 : _f.defaultMaskLevel
                  },
                  debugMode: this.options.debugMode,
                  shouldInlineStylesheet: this.options.shouldInlineStylesheet,
                  inlineImages: this.options.inlineImages,
                  version: { type: "plugin", version: VERSION5 },
                  performanceConfig: this.options.performanceConfig,
                  storeType: this.options.storeType,
                  useWebWorker: (_g = this.options.useWebWorker) !== null && _g !== void 0 ? _g : (_h = this.options.experimental) === null || _h === void 0 ? void 0 : _h.useWebWorker,
                  userProperties,
                  applyBackgroundColorToBlockedElements: this.options.applyBackgroundColorToBlockedElements,
                  interactionConfig: this.options.interactionConfig,
                  captureDocumentTitle: this.options.captureDocumentTitle,
                  enableUrlChangePolling: this.options.enableUrlChangePolling,
                  urlChangePollingInterval: this.options.urlChangePollingInterval,
                  captureAdoptedStyleSheets: this.options.captureAdoptedStyleSheets,
                  crossOriginIframes: this.options.crossOriginIframes,
                  flushIntervalConfig: this.options.flushIntervalConfig,
                  eagerFullSnapshotSend: this.options.eagerFullSnapshotSend,
                  captureFullSnapshotOnFocus: this.options.captureFullSnapshotOnFocus,
                  maxPersistedEventsSizeBytes: this.options.maxPersistedEventsSizeBytes,
                  maxSingleEventSizeBytes: this.options.maxSingleEventSizeBytes,
                  sendTimeoutMs: this.options.sendTimeoutMs,
                  handleSendEvents: this.options.handleSendEvents,
                  handleFetchConfig: this.options.handleFetchConfig
                };
                return [4, this.sessionReplay.init(config2.apiKey, this.srInitOptions).promise];
              case 1:
                _j.sent();
                return [3, 3];
              case 2:
                error_1 = _j.sent();
                config2 === null || config2 === void 0 ? void 0 : config2.loggerProvider.error("Session Replay: Failed to initialize due to ".concat(error_1.message));
                return [3, 3];
              case 3:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayPlugin2.prototype.onSessionIdChanged = function(sessionId) {
        var _a4;
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_b) {
            switch (_b.label) {
              case 0:
                (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.loggerProvider.debug("Analytics session id is changed to ".concat(sessionId, ", SR session id is ").concat(String(this.sessionReplay.getSessionId()), "."));
                return [4, this.sessionReplay.setSessionId(sessionId).promise];
              case 1:
                _b.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayPlugin2.prototype.onOptOutChanged = function(optOut) {
        var _a4;
        return __awaiter(this, void 0, void 0, function() {
          var _b;
          return __generator(this, function(_c) {
            switch (_c.label) {
              case 0:
                (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.loggerProvider.debug("optOut is changed to ".concat(String(optOut), ", calling ").concat(optOut ? "sessionReplay.shutdown()" : "sessionReplay.init()", "."));
                if (!optOut) return [3, 1];
                this.sessionReplay.shutdown();
                return [3, 4];
              case 1:
                _b = this.config != null;
                if (!_b) return [3, 3];
                return [4, this.sessionReplay.init(this.config.apiKey, this.srInitOptions).promise];
              case 2:
                _b = _c.sent();
                _c.label = 3;
              case 3:
                _b;
                _c.label = 4;
              case 4:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayPlugin2.prototype.execute = function(event) {
        var _a4, _b, _c;
        return __awaiter(this, void 0, void 0, function() {
          var sessionId, sessionRecordingProperties, sessionId, userProperties, globalScope3, sessionRecordingProperties, error_2;
          return __generator(this, function(_d) {
            switch (_d.label) {
              case 0:
                _d.trys.push([0, 9, , 10]);
                if (!this.options.customSessionId) return [3, 4];
                sessionId = this.options.customSessionId(event);
                if (!sessionId) return [3, 3];
                if (!(sessionId !== this.sessionReplay.getSessionId())) return [3, 2];
                return [4, this.sessionReplay.setSessionId(sessionId).promise];
              case 1:
                _d.sent();
                _d.label = 2;
              case 2:
                sessionRecordingProperties = this.sessionReplay.getSessionReplayProperties();
                event.event_properties = __assign(__assign({}, event.event_properties), sessionRecordingProperties);
                _d.label = 3;
              case 3:
                return [3, 8];
              case 4:
                sessionId = (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.sessionId;
                if (!(sessionId && sessionId !== this.sessionReplay.getSessionId())) return [3, 6];
                return [4, this.sessionReplay.setSessionId(sessionId).promise];
              case 5:
                _d.sent();
                _d.label = 6;
              case 6:
                if (!(sessionId && sessionId === event.session_id)) return [3, 8];
                userProperties = void 0;
                if (event.event_type === SpecialEventType.IDENTIFY) {
                  userProperties = parseUserProperties(event);
                }
                globalScope3 = getGlobalScope();
                return [4, this.sessionReplay.evaluateTargetingAndCapture({
                  event,
                  userProperties,
                  page: ((_b = globalScope3 === null || globalScope3 === void 0 ? void 0 : globalScope3.location) === null || _b === void 0 ? void 0 : _b.href) != null ? { url: globalScope3.location.href } : void 0
                })];
              case 7:
                _d.sent();
                sessionRecordingProperties = this.sessionReplay.getSessionReplayProperties();
                event.event_properties = __assign(__assign({}, event.event_properties), sessionRecordingProperties);
                _d.label = 8;
              case 8:
                if (event.event_type === SpecialEventType.IDENTIFY && event.event_properties) {
                  delete event.event_properties[DEFAULT_SESSION_REPLAY_PROPERTY2];
                }
                return [2, Promise.resolve(event)];
              case 9:
                error_2 = _d.sent();
                (_c = this.config) === null || _c === void 0 ? void 0 : _c.loggerProvider.error("Session Replay: Failed to enrich event due to ".concat(error_2.message));
                return [2, Promise.resolve(event)];
              case 10:
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayPlugin2.prototype.teardown = function() {
        var _a4;
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_b) {
            try {
              this.sessionReplay.shutdown();
              this.config = null;
            } catch (error) {
              (_a4 = this.config) === null || _a4 === void 0 ? void 0 : _a4.loggerProvider.error("Session Replay: teardown failed due to ".concat(error.message));
            }
            return [
              2
              /*return*/
            ];
          });
        });
      };
      SessionReplayPlugin2.prototype.getSessionReplayProperties = function() {
        return this.sessionReplay.getSessionReplayProperties();
      };
      SessionReplayPlugin2.prototype.start = function() {
        return __awaiter(this, void 0, void 0, function() {
          return __generator(this, function(_a4) {
            switch (_a4.label) {
              case 0:
                return [4, this.sessionReplay.start().promise];
              case 1:
                _a4.sent();
                return [
                  2
                  /*return*/
                ];
            }
          });
        });
      };
      SessionReplayPlugin2.prototype.stop = function() {
        this.sessionReplay.stop();
      };
      SessionReplayPlugin2.pluginName = "@amplitude/plugin-session-replay-browser";
      return SessionReplayPlugin2;
    })()
  );
  var sessionReplayPlugin = function(options) {
    return new SessionReplayPlugin(options);
  };

  // src/amp-entry.js
  var q2 = new URLSearchParams(location.search);
  var cfg = window.__PROBE__ || {};
  var apiKey = q2.get("k") || cfg.apiKey;
  var cell = q2.get("cell") || cfg.cell || "unknown";
  var srOptions = cfg.sessionReplay || {};
  document.getElementById("cellid").textContent = cell;
  document.getElementById("buildid").textContent = cfg.buildId || "dev";
  var line = document.createElement("p");
  line.className = "sub";
  line.id = "amp-status";
  line.textContent = "Amplitude: initialising\u2026";
  document.querySelector("h1").after(line);
  var MARKERS = [
    ["c1_css_inlined", "EXTERNAL STYLESHEET APPLIED"],
    ["c1_css_url", "css/probe.css"],
    ["c2_inlinestyle", "INLINE STYLE APPLIED"],
    ["c3_font_url", "fonts/ProbeFace.ttf"],
    ["c4_img_url", "img/photo.png"],
    ["c5_bgimg_url", "img/bg.png"],
    ["c6_inline_svg", "50,6 62,38"],
    ["c7_extsvg_url", "svg/sprite.svg"],
    ["c8_gfonts_css_url", "fonts.googleapis.com"],
    ["c8_gfonts_inlined", "fonts.gstatic.com"],
    ["any_data_image", "data:image"],
    ["any_data_font", "data:font"],
    ["any_data_octet", "data:application/octet-stream"],
    ["rr_dataURL_attr", "rr_dataURL"]
  ];
  var inspected = 0;
  function countOf(hay, needle) {
    let n4 = 0, i2 = 0;
    for (; ; ) {
      i2 = hay.indexOf(needle, i2);
      if (i2 === -1) break;
      n4++;
      i2 += needle.length;
    }
    return n4;
  }
  async function gunzip(u8) {
    const ds = new DecompressionStream("gzip");
    const stream = new Blob([u8]).stream().pipeThrough(ds);
    return await new Response(stream).text();
  }
  async function inspectPayload(body) {
    if (inspected >= 4) return;
    inspected++;
    let text;
    let wire;
    if (typeof body === "string") {
      text = body;
      wire = "plain";
    } else {
      wire = "gzip";
      try {
        text = await gunzip(body);
      } catch (e2) {
        console.log("[PROBE] capture gunzip failed " + e2);
        return;
      }
    }
    const out = { batch: inspected, wire, bytes: text.length };
    for (const [name, needle] of MARKERS) out[name] = countOf(text, needle);
    console.log("[PROBE] capture " + JSON.stringify(out));
    if (inspected === 1) {
      const urls = /* @__PURE__ */ new Set();
      const attr2 = /\\"(?:src|href|xlink:href)\\":\\"([^\\"]+)\\"/g;
      let m2;
      while ((m2 = attr2.exec(text)) !== null) urls.add("attr " + m2[1]);
      const cssurl = /url\(\\?["']?([^)\\"']+)\\?["']?\)/g;
      while ((m2 = cssurl.exec(text)) !== null) urls.add("css  " + m2[1]);
      console.log("[PROBE] capture-urls " + JSON.stringify([...urls].slice(0, 30)));
      for (const needle of ["ProbeFace.ttf", "img/bg.png", "rr_dataURL", "data:image"]) {
        const i2 = text.indexOf(needle);
        console.log("[PROBE] capture-ctx " + needle + " :: " + (i2 === -1 ? "NOT PRESENT" : text.slice(Math.max(0, i2 - 70), i2 + 20).replace(/\s+/g, " ")));
      }
      const CH = 800;
      const total = Math.ceil(text.length / CH);
      console.log("[PAYLOAD] begin " + total);
      for (let i2 = 0; i2 < total; i2++) {
        console.log("[PAYLOAD] " + i2 + " " + text.slice(i2 * CH, (i2 + 1) * CH));
      }
      console.log("[PAYLOAD] end " + total);
    }
  }
  if (!apiKey) {
    line.textContent = "Amplitude: NO API KEY \u2014 SDK not initialised";
  } else {
    add(sessionReplayPlugin(Object.assign({
      sampleRate: 1,
      debugMode: true,
      // Raw JSON on the wire so the inspector above can read it. Upload-side only:
      // this runs after capture, so it cannot change what rrweb serialised.
      enableTransportCompression: false,
      handleSendEvents: async (req) => {
        inspectPayload(req.body).catch((e2) => console.log("[PROBE] inspect error " + e2));
        return fetch(req.url, {
          method: req.method,
          headers: req.headers,
          body: req.body,
          keepalive: req.keepalive
        });
      }
    }, srOptions)));
    init(apiKey, {
      serverZone: "US",
      logLevel: 4,
      // Debug
      defaultTracking: false,
      autocapture: {
        sessions: true,
        pageViews: true,
        formInteractions: false,
        fileDownloads: false,
        elementInteractions: false
      }
    });
    setTimeout(() => {
      const sid = getSessionId();
      const did = getDeviceId();
      line.textContent = "Amplitude OK \xB7 session " + sid + " \xB7 device " + did;
      window.__PROBE_IDS__ = { sessionId: sid, deviceId: did, cell };
      console.log("[PROBE] ids " + JSON.stringify(window.__PROBE_IDS__));
      console.log("[PROBE] srOptions " + JSON.stringify(srOptions));
      console.log("[PROBE] replayUrl https://app.amplitude.com/analytics/amplidemo/session-replay/project/866372/search/replay?sessionReplayId=" + encodeURIComponent(did + "/" + sid));
      track("probe_loaded", { cell, origin: location.origin, href: location.href });
    }, 1500);
    document.addEventListener("visibilitychange", () => {
      console.log("[PROBE] visibilitychange -> " + document.visibilityState + " session " + getSessionId());
    });
  }
  setTimeout(() => {
    const rows = performance.getEntriesByType("resource").filter((e2) => !/amplitude\.com/.test(e2.name)).map((e2) => e2.initiatorType + " " + e2.name);
    console.log("[PROBE] resources " + JSON.stringify(rows));
    const img = document.getElementById("c4img");
    console.log("[PROBE] ondevice img4complete=" + img.complete + " naturalWidth=" + img.naturalWidth);
    document.fonts.ready.then(() => {
      console.log("[PROBE] ondevice ProbeFace=" + document.fonts.check("16px ProbeFace") + " RockSalt=" + document.fonts.check('16px "Rock Salt"'));
    });
  }, 3e3);
})();
